# PYM (Peace Your Mind) — 개발 계획서 (v1 확정, 2026-08-18)

> FindYourMind(FYM, Demo_28_FYM)의 검증된 골격을 재사용하는 자매 프로젝트.
> FYM에서 이미 실증된 부분은 "FYM 승계"로 표시하고 근거를 반복하지 않는다.
> 이 문서는 PYM 고유의 결정과 신규 작업에 집중한다.

---

## 1. 목표와 제약

**목표**: 사용자가 현재 기분·마음 상태를 자연어로 입력하거나 키워드로 선택하면,
그 감정에 닿는 성경 구절을 보여주고, 그 구절·주제를 다루는 유튜브 영상 리스트로
이어주는 서비스. 클릭 시 유튜브로 이동한다.

**확정 제약**
- 운영 시 LLM/외부 AI API 호출 없음 — 감정 분류는 온디바이스(키워드 사전)
- 운영 서버 없음 — 스탠드얼론 PWA, GitHub 배치 + 정적 파일 배포
- 비로그인, 데이터는 디바이스에만 저장
- YouTube API 쿼터는 제한적 자원으로 취급 (일 10,000 units, FYM 배치와 공유 가능성 고려)

**확정 결정 (2026-08-18)**
| 항목 | 결정 |
|---|---|
| 프로젝트명 | PYM (Peace Your Mind) |
| 채널 범위 | 기독교 전반 — 장로교 중심으로 시작하되 초교파 방송 포함. **이단 배제는 절대 조건** |
| 성경 번역본 | **개역한글(1961) 본문 수록** — 퍼블릭 도메인. verses.json에 번역본 필드를 두어 추후 개역개정(성서공회 계약 시) 교체를 데이터 작업으로 가능하게 함 |
| 영상 수집 | **전면 화이트리스트** — 일상 배치에서 search.list를 쓰지 않는다 |

**스택 (FYM 승계)**: React PWA + IndexedDB + GitHub Pages, 완성 후 Capacitor 래핑.
분류는 키워드 사전 단독 (FYM Phase 2와 동일하게 모델 없이 시작, 모델 자리만 열어둠).

---

## 2. 전체 아키텍처

```
[GitHub Actions — 일 1회 cron]
   ├─ channels.list  : 화이트리스트 채널 → uploads 재생목록 ID   (50채널당 1 unit)
   ├─ playlistItems  : 채널별 최근 업로드 수집                    (페이지당 1 unit)
   ├─ videos.list    : 길이·공개상태·지역 확인                    (50건당 1 unit)
   ├─ 주제 태깅      : 제목 기반 주제 키워드 매칭 (온배치, API 0)
   ├─ 필터·검증      : FYM 필터 골격 승계 (blocklist·길이·쇼츠)
   └─ videos.json 생성 → gh-pages 배포
            │
            ▼  version.json 비교 후 변경 시만 fetch (FYM 승계)
[PWA (React)]
   ├─ 감정 입력: 자연어 / 대분류→세분류 선택        (FYM 승계)
   ├─ 온디바이스 분류: 키워드 사전 + 위기 우선 검사  (FYM 사전 이식)
   ├─ verses.json: 감정→구절 매핑                    (앱 번들, 정적)
   ├─ 화면 흐름: 공감 문구 → 구절 카드 → 영상 리스트
   └─ 클릭 → youtube.com 이동
```

핵심 원칙 (FYM 승계): 앱은 YouTube API를 직접 호출하지 않는다.
API 키는 GitHub Actions Secrets에만 존재한다.

**FYM과의 구조적 차이 3가지**
1. 감정과 영상 사이에 **구절 계층**이 들어간다 (감정 → 구절 → 주제 → 영상)
2. 영상 수집이 검색이 아니라 **화이트리스트 채널의 업로드 순회**다
3. 그 결과 일일 쿼터가 7,900 → **150 units 이하**로 떨어진다

---

## 3. 감정 → 구절 → 영상 3계층 체계

### 3.1 감정 체계 (FYM 승계)

대분류 9 × 세분류 24, taxonomy.yaml 단일 소스 방식 그대로.
FYM의 키워드 사전(878개)과 정규화 규칙, 위기 우선 검사 순서를 이식한다.
공감 문구·마무리 문구는 PYM 톤("평안" 지향)으로 새로 쓴다.

### 3.2 구절 계층 (신규 — 이 프로젝트의 핵심 큐레이션)

- 세분류 24개 × 구절 8~12개 = **약 250구절**, verses.json으로 앱 번들 (API 0, 배치 불필요)
- 각 구절은 `emotion_tags`(세분류 복수 가능)와 `theme`(주제) 를 갖는다
- 주제는 20~30개로 시작: 위로, 소망, 평안, 감사, 인도, 회복, 기다림, 용기, 찬양 … (Phase 1에서 확정)
- **매핑 근거를 주석으로 남긴다** (FYM taxonomy 방식). 감정-구절 연결은 신학적으로
  민감하므로, 맥락을 벗어난 인용(proof-texting)이 되지 않는지 구절마다 확인한다
- 문체 검수: 개역한글로 읽었을 때 자연스러운 구절을 우선 선정한다

### 3.3 주제 → 영상 매칭 (신규)

영상에는 구절이 아니라 **주제**를 매칭한다. 이유: 구절 단위(250개) 매칭은
유명 구절 외에 영상이 없어 빈 화면이 나온다. 주제 단위(20~30개)면 화이트리스트
채널의 설교·찬양·묵상 영상으로 충분히 채워진다.

- 배치가 영상 **제목**을 주제 키워드 사전으로 태깅한다 (설명란은 보지 않는다 — FYM에서
  설명란 검사가 역설적 오차단을 낳은 교훈 승계)
- 주제 키워드도 전수 검토 대상이다. FYM 교훈 "단어가 장르를 부른다"가 여기도 적용된다:
  `치유`는 극단적 신유 집회를, `축복`은 번영신학 계열을, `간증`은 자극적 서사물을
  부를 수 있다. 새 키워드는 실제 상위 결과를 보고 넣는다

#### 태깅 전제 개정 (2026-08-19) — 미태깅을 버리지 않고 폴백으로 노출한다

**개정 전 전제**: 영상의 주제를 제목에서 알아낸다 → 못 알아내면 노출하지 않는다.

첫 실 API 배치가 이 전제를 무너뜨렸다. 승인 채널 15개의 1,069건 중 **제목이 주제를
말하는 콘텐츠는 CGN 생명의 삶 100건뿐**이었고(미태깅 0%), 나머지는 다음이었다.

```
본문+제목형  381건  "[아침예배] 삶이 변했습니까? (겔 43:1-12)_베이직교회_조정민 목사_20260819"
공지·기타    293건  "✔극동방송 70주년 기념 호남권 전도대회 1부 LIVE"
프로그램형   141건  "인천제일교회 이제일 목사(크리스천 칼럼 107회) - 장애물을 뛰어넘는 예배자"
곡목·찬양    156건  "성도여 다 함께 + Jehovah + 하나님의 부르심 | 주일 6부예배 찬양 하이프레이즈"
통독형        25건  "[231일차] 예레미야 12~14장 | CGN과 함께 일-일 통독"
→ 미태깅 822건 (77%)
```

검색으로 영상을 찾던 FYM에서는 검색어와 제목이 같은 언어를 썼다. PYM은 채널이
자기 관행대로 붙인 제목을 받는다. **사전을 키워도 이 구조는 바뀌지 않는다** —
제목의 정보 구조 자체에 주제어가 없기 때문이다.

**개정 후 전제**: 주제 태깅은 되는 만큼 하고(정밀도 우선), 안 되는 영상도
**형식(media_type)과 채널 신뢰도**로 폴백 노출한다.

```
1층  주제 태깅분   매핑 주제에 걸린 영상. 근거가 분명하다        source: "theme"
2층  폴백         미태깅 영상을 media_defaults 형식으로 채운다   source: "fallback"
                 상한 12/20 (60%) — 화면 전체가 폴백으로 덮이지 않게 한다
```

- 폴백 풀은 **그 세분류의 기본 형식 + unknown**이다. unknown은 양쪽 토글에 노출되는
  값이라 어느 쪽에도 빈 화면을 만들지 않는다.
- 폴백에도 **채널당 상한 3 · 형식 균형 · 일별 회전**을 그대로 적용한다. 정렬은
  주제분과 같은 최신순 라운드로빈이며 **채널 간 우열을 두지 않는다** — 태깅률이
  낮은 채널은 콘텐츠가 나빠서가 아니라 제목 관행이 다를 뿐이다(CGN 성경통독 0%).
- **위기 화면은 폴백을 쓰지 않는다.** 위기 풀은 crisis_eligible 채널의 확정
  큐레이션만 쓰고 미달 시 직전 결과를 유지한다 (7절).
- 두 층은 `source` 필드로 데이터에 구분해 남긴다. **앱은 이 둘을 구분 없이 섞지
  않는다** — "이 감정에 맞춰 고른 말씀 / 이 채널의 최근 영상"처럼 층이 보여야 한다.
  근거: 사용자가 "이게 왜 내 감정에 맞지?"라고 느끼는 순간 화면 전체의 신뢰가
  떨어진다. 정직하게 나누면 그 위험이 없다. (화면 문구는 Phase 3에서 확정)

**실측 결과 (2026-08-19 데이터에 개정안을 적용)**

```
24개 화면 × 20슬롯 = 480  →  총 노출 452건 (주제분 283 + 폴백 169)
20건 달성 17/24 · 최소 12건 (개정 전에는 0건 화면이 1개, 목표 달성 2개)
```

**경보 2종을 함께 둔다** — 폴백은 메우는 장치이지 해결이 아니기 때문이다.

| 경보 | 조건 | 뜻 | 실측 |
|---|---|---|---|
| `theme_fallback_heavy` | 폴백 > 50% | 주제 사전·채널 구성이 그 감정을 못 받친다 | 9개 |
| `theme_too_few` | 최종 < 8건 | 화면으로 성립하지 않는다 | 0개 |

20건을 못 채우는 것 자체는 허용한다. 채우려고 폴백을 무제한으로 늘리면 그 화면은
"감정에 맞춘 목록"이 아니라 "채널 최신 목록"이 되기 때문이다.

### 3.4 media_type — 말씀/찬양 구분 (Phase 2 구현)

같은 감정, 같은 주제라도 **어떤 형식으로 받고 싶은지는 사람마다 다르다.**
지친 사람에게는 30분 설교보다 찬양이 낫고, 막막한 사람에게는 그 반대다.
그래서 영상에 주제와 별개로 `media_type`(sermon / worship)을 붙인다.

**판별 신호 3가지** (구현·사전은 `themes.yaml`의 `media_types` 섹션)

| 순위 | 신호 | 판정 |
|---|---|---|
| 1 | 채널 `content_type` | `sermon`·`worship` 전용 채널은 여기서 끝난다. `devotion`은 sermon |
| 2 | 제목 어휘 | **mixed 채널이 실제 판별 대상이다.** 본문 장절 패턴("요한복음 3:16")이 가장 강한 신호 |
| 3 | 길이 | 보조. 설교 30분 이상 / 찬양 10분 이하. 찬양 메들리가 1시간을 넘기도 해 단독 판정 근거로 쓰지 않는다 |

못 가리면 `unknown`으로 두고 **양쪽 토글 모두에 노출한다.** 판별 실패로 영상이
사라지는 것보다 낫고, `unknown` 비율이 리포트에 남아 사전을 고칠 근거가 된다.
(주제 태깅의 `untagged`와 다르다 — 거기서는 "어느 감정에 맞는지 모른다"가 곧
"보여줄 근거가 없다"이지만, 여기서는 형식만 모를 뿐 감정 적합성은 이미 판정됐다.)

**쿼터는 늘지 않는다.** 채널 `content_type`(allowlist)과 영상 `title`·`duration`
(videos.list)은 배치가 이미 받아오는 값이다. 추가 호출이 없다.

#### 노출 방식 — 전환이 필수 요건이다

기본값은 감정 세분류마다 정한다(`themes.yaml`의 `media_defaults`).
`exhaustion`·`anxiety` 계열은 찬양 우선(듣기만 하면 되니까),
`frustration`·`boredom` 계열은 설교 우선(내용을 원하는 상태)이다.
`themes.yaml` 매핑에 이미 그 결이 있다 — 앞 두 계열은 `quiet_worship`·
`joy_praise`가, 뒤 두 계열은 `daily_word`·`bible_story`·`guidance`가 붙어 있다.

**사용자 전환은 옵션이 아니라 필수 요건이다.**

- 영상 리스트 **상단에 `[말씀] / [찬양]` 토글**을 둔다. 한 번의 탭으로 바뀐다.
- **설정 화면에 숨기지 않는다.** 결과 화면에 둔다.
  감정 분류가 완벽하지 않으므로 **기본값이 안 맞는 경우가 상당수 나올 것을
  전제한다.** 그때 사용자가 설정을 찾아 들어가야 한다면 그냥 이탈한다.
- 한쪽 풀이 비어도 **토글을 비활성화하지 않는다.** "지금은 말씀 영상만 있어요"처럼
  상태를 문장으로 보여준다 (FYM 문구 원칙 — 명령형 대신 대화형).
  비활성화된 버튼은 사용자에게 "고장"으로 읽히지만, 문장은 상황 설명으로 읽힌다.

> 이 토글은 Phase 3(PWA 조립)에서 만든다. 다만 **데이터가 먼저 준비돼야 하므로**
> Phase 2 배치가 `media_type`을 채워 넣는다.

---

## 4. 데이터 스키마

### 4.1 verses.json (앱 번들, 정적)

```json
{
  "translation": "krv1961",
  "themes": ["comfort", "hope", "peace", "gratitude"],
  "verses": [
    {
      "id": "psa.42.5",
      "ref": "시편 42:5",
      "text": "내 영혼아 네가 어찌하여 낙망하며 ...",
      "emotion_tags": ["sadness.sorrow", "sadness.lonely"],
      "theme": "hope",
      "note": "낙망을 부정하지 않으면서 소망으로 향하는 구절 — 우울 계열 대표"
    }
  ]
}
```

- `translation` 필드가 최상위에 있어 추후 개역개정 전환 시 파일 교체로 끝난다
- `note`는 매핑 근거 (빌드 시 앱 번들에서 제거 가능, 소스에는 유지)
- 크기 예상: 250구절 × ~200자 ≈ 50KB

### 4.2 videos.json (배치 산출물)

FYM 스키마 승계 + 카테고리 키를 감정이 아닌 **주제**로 한다:

```json
{
  "version": "2026-08-20T00:00:00Z",
  "subcategories": [
    {
      "id": "sadness.sorrow",
      "themes": ["comfort", "lament", "hope"],
      "media_default": "worship",
      "videos": [
        { "videoId": "...", "title": "...", "channel": "...",
          "publishedAt": "...", "duration": "PT35M10S",
          "media_type": "sermon", "source": "theme" },
        { "videoId": "...", "...": "...", "source": "fallback" }
      ]
    }
  ],
  "crisis": { "updated_at": "...", "videos": [ ... ] }
}
```

**키가 주제(theme)에서 감정 세분류로 바뀌었다 (2026-08-19).** 폴백 도입의 결과다 —
폴백은 세분류의 `media_defaults`에 달려 있고, "이 화면이 몇 건 나가는가"도 세분류
단위로만 정해진다. 주제별 목록을 내보내고 조합을 앱에 맡기면 배치가 화면 성립
여부(`theme_too_few`)를 판정할 수 없다. **주제는 여전히 태깅 단위이며**, 주제별
집계는 `build_report.json`에 남는다(진단용).

- `crisis` 최상위 분리 + 빌드 시 교차 검증 단언 (FYM 승계 — 구조로 안전을 보장)
- `media_type`은 `sermon` / `worship` / `unknown` 중 하나 (3.4절).
  앱이 `[말씀]/[찬양]` 토글로 거르며, `unknown`은 양쪽 모두에 노출한다.
  **주제당 물량을 셀 때 media_type별로도 세어야 한다** — 한쪽이 0이면
  토글을 눌러도 빈 화면이 나오므로, 리포트에 주제×media_type 분포를 남긴다
- `source`는 `theme` / `fallback` 중 하나 (3.3절). **앱은 두 층을 섞지 않는다.**
  목록 순서도 주제분이 먼저이며, 배치가 그 순서로 내보낸다
- 주제당 영상 15~20개, 채널당 상한 3 + 라운드로빈 (FYM 승계)
- 설교 영상은 30분 이상이 흔하므로 FYM의 길이 하한 필터 값은 재검토한다
  (쇼츠 배제는 유지하되, 상한은 두지 않는 방향 검토)

### 4.3 IndexedDB (FYM 승계)

`videos_cache` + `settings`. verses.json은 앱 번들이므로 캐시 불필요.

---

## 5. 채널 화이트리스트 정책

**전면 화이트리스트**: 위기 카테고리뿐 아니라 모든 영상이 승인 채널에서만 나온다.
이유: 한국 기독교 유튜브는 이단 채널이 일반 검색어에 대량 유입되며,
채널명·제목 위장 때문에 blocklist(네거티브)로는 막을 수 없다.
포지티브 목록만이 구조적으로 안전하다.

### 5.1 승인 기준 (channel_allowlist.yaml에 명문화)

0. **범위** — 개신교, 한국어 콘텐츠로 한정한다.
   가톨릭·정교회 채널과 외국어 전용 채널은 범위 밖이다.
   **이단이라서가 아니라 데이터 정합성 때문이다** — 구절이 개역한글(개신교
   번역)이고 themes.yaml 주제·문구가 개신교 어휘로 짜여 있어, 다른 전통의
   영상을 붙이면 사용자가 받는 구절과 영상의 어휘가 어긋난다.
   (2차 발굴에서 가톨릭TV·CGN Japan이 후보로 올라와 명시하게 됐다)

1. **정체 확인** — 다음 중 하나로 소속·정체성이 공개 확인되는 채널
   - 주요 교단(예장 통합·합동·고신·백석, 감리회, 기성 등) 총회/공식 미디어
   - 소속 교단이 확인되는 교회의 공식 채널
   - 초교파 기독교 방송 (CTS, CBS, CGNTV, 극동방송 등)
2. **이단 배제 (절대 조건)** — 주요 교단이 이단·사이비로 규정한 단체 및 그 연계
   채널은 어떤 경우에도 넣지 않는다. 판단이 애매하면 넣지 않는다.
   신규 채널은 한국교회 주요 교단의 이단 규정 목록과 대조 후 승인한다
3. **콘텐츠 성격** — 설교·성경 강해·묵상·찬양(CCM) 중심.
   정치 시사·이단 반박 콘텐츠가 주력인 채널은 교리가 건전해도 제외 (서비스 톤 훼손)
4. **자동 점검 (FYM 승계)** — blocklist 통과율, 최근 업로드 지속성, 쇼츠 비중

### 5.2 운영

- 후보 발굴: `suggest_channels.py` 방식 (수집 → 사람 검토 시트 → 승인). 월 1회 수준,
  이때만 search.list 사용
- 검토 시트에 `교단/소속 확인` 열 추가 — 자동 점검이 판단할 수 없는 항목임을 명시
- 시트 파일명에 타임스탬프 포함 + 드라이런이 실제 결과를 덮어쓰지 않게 함
  (FYM에서 실제 661 units 결과가 드라이런에 덮인 사고의 재발 방지)
- 초기 목표: 채널 20~30개로 시작, 40~50개까지 확장

---

## 6. 쿼터 예산

| 작업 | API | 주기 | 비용 |
|---|---|---|---|
| 채널 50개 → uploads ID | channels.list | 일 1회 | 1 |
| 채널별 업로드 1~2페이지 | playlistItems.list | 일 1회 | 50~100 |
| 상세 확인 (50건 묶음) | videos.list | 일 1회 | 10~20 |
| **일일 배치 합계** | | | **~150** |
| 채널 후보 발굴 | search.list 포함 | 월 1회 | ~600 |

- **API 키는 FYM과 분리한다 (확정)** — PYM 전용 GCP 프로젝트·키 발급.
  일 10,000을 단독으로 쓰므로 여유가 매우 크고, FYM quota_log와의 통합도 불필요하다
- FYM의 quota_log·일일 상한 체계를 승계하되 단순화한다 — Actions 예약값은 200,
  FYM에서 문제가 됐던 배치 몫 이중 예약 이슈는 예약값이 작아 사실상 무의미해진다
- FYM 교훈 승계: 429/403 판정은 메시지의 limit 문자열로 가른다. 실제 API 실행은 사전 승인

---

## 7. 위기 정책

FYM의 구조(위기 검사 최우선, 상담 안내 최상단, crisis 풀 분리·교차 검증)를 승계하고
PYM 고유로 다음을 추가한다:

- 위기 화면의 구절은 감정 매핑을 타지 않는 **별도 고정 큐레이션** (운영자 확정 목록)
- 죄·심판·형벌 프레임의 구절은 위기 화면에서 배제한다. 위기 상태의 사용자에게
  종교적 부담을 지우는 방향의 콘텐츠는 이 서비스가 낼 수 있는 최악의 출력이다
- 위기 영상 풀 화이트리스트는 일반 풀보다 좁게 — 정신건강 전문 기관·상담 사역 채널 중심
- "추천"이라는 단어를 쓰지 않고 자동재생하지 않는다 (FYM 승계)

---

## 8. 단계별 계획

| Phase | 내용 | 산출물 | API 소모 |
|---|---|---|---|
| **0. 정책·채널** | 화이트리스트 기준 확정, 초기 채널 20~30개 승인, 주제 체계(20~30개) 초안 | channel_allowlist.yaml, taxonomy 주제 섹션 | 후보 발굴 ~600 |
| **1. 구절 큐레이션** | 250구절 선정·태깅·근거 기록, 개역한글 본문 입력·검수 | verses.json | 0 |
| **2. 배치 개작** | FYM build_videos.py 골격 유지, search 제거 → 재생목록 수집 + 주제 태깅 + **media_type 판별**로 교체, 테스트 이식 | scripts/, 사전 점검 테스트 | 드라이런 0, 실측 ~150 |
| **3. PWA 조립** | FYM 앱 이식 — 분류·입력 화면 재사용, 구절 카드 화면 신규, **[말씀]/[찬양] 토글**, PYM 톤 문구 | app/ 배포 | 0 |
| **4. 운영·확장** | 배치 관찰, 채널 확장, Capacitor 래핑 | — | 일 ~150 |

작업량 배분: 코드 신규 작업은 FYM 대비 30% 미만. 실제 무게는 Phase 0~1의
큐레이션(채널 승인 + 구절 선정)에 있다.

---

## 9. FYM 재사용 맵

| FYM 자산 | PYM 처리 |
|---|---|
| taxonomy.yaml 감정 체계·키워드 878 | 이식 (문구만 PYM 톤으로 교체) |
| 위기 검사 우선순위·crisis 분리 구조 | 이식 + 7절 고유 정책 추가 |
| build_videos.py 필터·검증·경보·리포트 골격 | 유지, 수집부만 교체 |
| quota_log·일일 상한·Actions 예약 | 이식 (예약값 200) |
| suggest_channels.py 검토 시트 워크플로우 | 이식 + 교단 확인 열, 덮어쓰기 방지 |
| 배포 워크플로 2종 (build / deploy-app) | 이식 |
| 앱 분류·입력·오프라인·SW | 이식 |
| 테스트 (normalize·blocklist·quota + 앱 49) | 이식 후 PYM 케이스로 보강 |

---

## 10. 착수 전 확인 사항

- [ ] 개역한글 퍼블릭 도메인 상태 공식 확인 (착수 전 1회)
- [ ] 저장소 이름·배포 URL 결정 (Demo_29_PYM 가칭)
- [x] API 키 — **FYM과 분리 확정** (PYM 전용 키 발급 필요, Phase 2 전까지)
- [x] 영상 길이 필터 — 하한(쇼츠 배제)만 유지, 상한 없음
- [x] 이단 반박 주력 채널 — 제외 확정
