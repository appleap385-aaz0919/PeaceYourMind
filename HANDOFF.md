# HANDOFF — 세션 인수인계

작성 2026-08-18 (KST) · 개정 2026-08-31 (Phase 0~3 완료 · Phase 4 진행 중 · 서비스 가동 중 — https://appleap385-aaz0919.github.io/PeaceYourMind/)
다음 세션의 남은 주요 과제는 **안드로이드 출시 마무리다** (3절).
2026-08-31에 Capacitor 래핑 · 아이콘 · 구절 알림 · 알림 소리/진동이 들어왔다 (2.90~2.94절)
2026-09-02에 AdMob이 붙고 **실제 광고 값으로 교체**됐다 (2.95~2.102절). 남은 것은 **Play 등록과 서명 키**다
✅ 실기기 결함 셋을 한 번에 고쳤다 — 배경면 · 320×50 · 간격 상수 (2.103 진단 · 2.104 적용·실측)
✅ 상단 검은 여백은 **안 3(그대로 둔다)** 으로 확정 — 재검토 조건을 달았다 (2.105)
✅ 키보드 가림도 고쳤다 — 축소를 듣고 입력란을 끌어온다 (2.106 진단 · 2.107 적용·실측)
★★ 태블릿이 **갈래 A**였다 — 이 세션의 미검증이 전부 닫혔다 (2.111)
⛔⛔ **실기기는 시험 기기가 아니다 — 배너를 누르지 말 것** (2.113)
★★★ **갈래 판정 기준이 틀렸다** — 가르는 것은 웹뷰 버전이 아니라 **창이 edge-to-edge인가**다.
     갈래가 셋이고 **Android 14 이하는 전부 갈래 C**다 (2.115) ← 이번 세션 최대 발견
✅ 시스템 바 색 결함을 고쳤다 — 갈래 C에서 앱이 밝은 띠 둘에 끼여 있었다 (2.115)
★★ **배터리 세이버가 구절 알림을 364일 밀어 놓는다** — 조용한 실패다 (2.115)
✅ 2.94 VIBRATE 질문을 닫았다 — 권한 없이 시스템이 대신 울린다 (2.115)
✅ ads.txt가 **「승인됨」**이 됐다 — 기다린 것이 맞았다. ⛔ apex 가설도 닫혔다 (2.121)
✅ 배너가 세 갈래에서 **위 16 · 아래 16 · 배경면 82dp**로 대칭이 됐다 (2.118 안 2)
✅ 결과 화면 탭 행에 **「구절 알림」 토글**과 상단 토스트가 들어갔다 (2.119)
⛔ 토글이 안 켜지던 것은 **OS 영구 거절**이었다 — 우리 표식이 아니다 (2.119)
★ 그 영구 거절 상태를 **감지할 수 있다** — 물어본 뒤 checkPermissions가 가른다 (2.120)
✅ 영구 거절 뒤의 탈출구를 만들었다 — 토스트로 알린다. 설정으로 보내지 않는다 (2.120)
✅ 토스트 움직임이 대칭이 됐다 — 나감 10px · 0.45s · 머무름 4.6초 (2.120)
⛔⛔ **모바일 웹에 동작하지 않는 알림 토글이 나갔다** — 18시간 배포돼 있었다 (2.122) ✅ 고쳐서 배포됐다
★ 원인은 검사의 **비대칭**이었다 — 앱 전용이 웹으로 새는 방향에 검사가 없었다 (2.122)
✅✅ **Play 콘솔이 풀렸다** — 앱을 만들었고 콘텐츠 선언을 전부 마쳤다 (2.123)
✅✅ **A2가 닫혔다** — 앱 서명 키를 Google이 이미 생성해 뒀다. 고를 것이 없었다 (2.123)
⛔ 그것으로 **2.94의 미확인 전제**도 닫혔다 — Play 앱 서명이 기본 적용이다 (2.123)
✅ **업로드 키를 만들었다** — 서명된 AAB가 나오고 지문을 대조했다 (2.125)
✅ **배너 잔존 결함이 닫혔다** — 원인은 **경합 조건**이었다 · 안 1(두 겹) 적용 (2.126)
⛔ 그 결함은 **웹 검사로는 영영 안 잡힌다** — 네이티브 뷰 하나가 어긋난 것이다 (2.126)
⚠⚠ 그리고 **천천히 시험하면 안 보인다** — 창이 0.59초다. 앞 세션의 10초 대기가
   창을 매번 비켜 갔다 (2.126)
★★ **통으로 밟기를 처음으로 끝까지 했다** — 그 밟기가 위 결함을 잡았다 (2.127)
✅ AdMob 「검토 필요」는 **정상**이다 — 스토어 등재를 전제로 하는 요건이다 (2.124)
★ 배너를 건드리면 **scripts/measure_banner.py** 로 세 갈래를 한 번에 잰다
★ "이 PC가 죽으면 무엇이 없어지나" — 3-부록 **「백업 목록」** 절에 다 있다 (2026-09-04)
✅ **배터리 세이버를 넘었다** — allowWhileIdle 한 줄. 「영영 안 온다」가 「1분 31초 늦게 온다」가 됐다 (2.116 ①)
⛔ 시험 기기 등록은 **콘솔에 AAID · 코드에 해시**다. 둘은 변환되지 않는다 (2.117)
★★ 다음 세션은 **3절 「다음 세션은 여기서 시작한다」** 부터 읽는다
🔴 windowBackground=ink에 **예상 못 한 대가**가 드러났다 — 유지/되돌림 판단 대기 (2.111)
최신 상태는 `git log`로 확인할 것

⚠ **규모 수치는 손으로 적지 않는다.** `scripts/stats.py`가 생성하고 `--check`가 게이트다 (2.43절).

> 이 문서는 **다음 세션이 바로 일을 이어받기 위한 것**이다.
> 정책·구조는 `PLAN.md`, 주제 체계는 `themes.yaml`, 원문 출처는 `data/krv/SOURCE.md`에 있고
> 여기서 반복하지 않는다. 여기 적는 것은 **원본만 읽어서는 알 수 없는 것** —
> 왜 그렇게 됐는지, 지금 무엇이 걸려 있는지다.

---

## 1. 현재 상태

**저장소** https://github.com/appleap385-aaz0919/PeaceYourMind (`main`, 로컬과 동기화됨)
**로컬** `D:\jaehyuk.myung\claude_demo\Demo_29_PYM`

> FYM(`FindYourMind`)과 **완전히 분리된 별도 레포**다. API 키도 분리한다(`PLAN.md` 6절).
> 커밋 author는 FYM과 동일하게 `aaz0919 <appleap385@gmail.com>`로 맞췄다 —
> 첫 커밋이 회사 이메일로 잡혀 있어 push 전에 amend했다. 이 저장소의 로컬 설정이라
> 새로 clone하면 다시 지정해야 한다.

| Phase | 상태 |
|---|---|
| 0 정책·채널 | **완료** — 승인 15건 등록. 1c 대조 완료, 제외 1건 기록 |
| 1 구절 큐레이션 | **완료** — 건수는 위 규모 블록이 유일한 출처다. 2026-08-19 확장으로 세분류당 10~15로 넓혔다 (2.24절) |
| 1.5 검증·추출 파이프라인 | **완료** — 이번 세션 산출물 (`verify_verses.py` + `fill_verses.py`) |
| 2 배치 개작 | **완료** — 파이프라인 정상(61 units) + 태깅 전략 확정(2.14절) + 사전 보강(2.17절).<br>⚠ 폴백 비율·달성 화면 수는 **매일 달라진다** — 여기 적지 않는다. 최신값은 배포본 `data/diagnostics.json` |
| 3 PWA 조립 | **완료** — gh-pages 배포·실동작 확인 (2.16절). 테스트 건수는 위 규모 블록 |
| 4 운영·확장 | **진행 중** — 수정 [0]~[7] · 이어서 읽기 탭 · 색/여백 위계 · 부정 계열 커버리지 ·<br>장 문맥 재검토 · 시편 실측 · 반복 보강 · 비시편 한계선 · cron 대조 **전부 완료**(2.26~2.42).<br>수치 단일화 · [6] 동점 처리 완료(2.43·2.44).<br>3차 발굴 실행·판정·승인 1건·배치 반영 완료(2.45~2.49).<br>출시 준비 정리 — 🔴 판단 대기 5건(2.50 · 3절).<br>★ 웹 AdSense 광고 구현·유저 사이트 레포 완료 — 심사 대기(2.55).<br>★ 안드로이드 래핑·아이콘·구절 알림·알림 소리/진동 완료(2.90~2.94 · 2026-08-31).<br>**남은 주요 과제는 안드로이드 출시 마무리다**(3절) |

### ★ 이 서비스의 근원 제약 — 주제 풀이 얇다

무엇을 고치든 이 한계는 그대로다. **[6][7]을 해도 바뀌지 않는다.**

> **출처** `retag_titles.py` · 로컬 코퍼스 `dist/titles.json` 1,067건(2026-08-19 덤프) · 2026-08-24 재확인
> ⚠ 이 표는 **코퍼스에 따라 달라진다.** 배포본과 절대 수치를 맞추지 말 것(2.42절).
>   배포본의 값은 `data/diagnostics.json`의 `theme_pool`에 있다(2.43절에서 추가).

```
daily_word 163 · gratitude 21 · love 20 · trust 20 · joy_praise 17
quiet_worship 12 · hope 11 · peace 10 · renewal 8 · presence 7 · patience 7
bible_story 6 · comfort 4 · courage 2 · lament 2 · justice 2
guidance 1 · strength 1 · forgiveness 1 · creation 1 · new_thing 1
self_control 0 · rest 0
```

**23개 주제 중 10개가 5건 이하이고, `self_control`·`rest`는 0건이다.**
집합을 아무리 잘 갈라도 **나눌 것이 없는 구간이 있다.**

```
그래서 안 되는 것   주제 집합 분리로 화면을 가르기 (풀이 12건이면 5화면이 나눠 갖지 못한다)
                   사전 어휘 확장 (2.28절 — self_control 후보 13종이 전부 0건)
그래서 되는 것      순서 분산 (2.32절) · 오탐 제거 (2.29절) · 품질 필터 (2.26절)
근본 해결          채널 발굴 하나뿐이다 (3절 10번)
```

⚠ 이 표를 다시 뽑는 법: `python scripts/retag_titles.py`의 [주제별] 절이 그대로 이 표다.
사전이나 채널을 바꾼 뒤에는 **이 표부터 다시 보는 것이 순서다.**

**규모**

<!-- STATS:BEGIN 규모 -->
<!-- scripts/stats.py --write 로 생성한다. 손으로 고치지 말 것. -->
```
구절       303  = 감정 293 + 위기 10
           감정 중 시편 163 (55.6%) · 비시편 130
           read_from 지정 18건 · 검토 후 제외 15건
장 본문     178개 장 4,442절 (39권) — 감정 구절이 닿는 장만
           app/public/krv/ · .gitignore 대상 · 매 빌드 재생성
주제       24개 · 제목 어휘 97 · 형식 어휘 30
감정 분류   대분류 9 · 세분류 24 · 키워드 923
채널       allowlist 22 (하한 15까지 여유 7)
           blocklist 0 · reviewed_out 13
게이트      파이썬 5종 + 앱 310건
```
<!-- STATS:END -->

원문 데이터는 66권 1,189장 31,102절이고 바뀌지 않는다.

구절 수의 이력과 배분 (수치가 아니라 **판단**이라 여기 남긴다)

```
299 → 286(3건 제외, 2.38) → 291(5건 추가, 2.40) → 293(2건 추가, 2.41)
세분류당 10~15로 불균일하다. 개수가 아니라 "확실히 맞는 구절이 몇 개까지
있는가"로 정했다 (PLAN.md 3.2 · HANDOFF 2.24)
1차분 anxiety.* 30 · 2차분 sadness.*+exhaustion.* 60
3차분 anger.*+frustration.* 60 · 4차분 긍정 계열 80
+ crisis 전용 10 (최상위 crisis 키, 감정 매핑 미적용)
24개 주제 전부에 구절이 배정돼 있다 (crisis_fixed 포함)
read_from은 이어서 읽기가 여는 자리를 큐레이션이 정한 것이다
```

**게이트** — 전부 종료 코드 0 (2026-08-24 확인). API 소모 0.

```
python scripts/verify_verses.py --check   전건 원문 일치 + verified + 위기/감정 분리
python scripts/fill_verses.py --verify    추출 경로 전건 재현
python scripts/tagging_test.py            주제·형식 판별 + themes.yaml 게이트 6종 고장 테스트
python scripts/spread_test.py             채널 분산·형식 균형·위기 유지 + 무결성 단언 고장 테스트
python scripts/messages_test.py           공감·마무리 문구 중복·유사도 (임계 0.85)
python scripts/stats.py --check           ★ 이 문서의 규모 수치가 실측과 같은지
cd app && npm test                        앱 화면 규칙 (구절·위기 격리·이어서 읽기)
```

⚠ 건수를 이 표에 적지 않는다. 위 규모 블록이 유일한 출처다 (2.43절).

**분석 도구** (게이트가 아니다 — 사전을 고칠 때 쓴다, API 0)

```
python scripts/retag_titles.py            dist/titles.json 재태깅 현황
                        --probe <어간>     그 어간이 잡는 제목 전부 (오탐 실측)
                        --songs           콘티를 곡명 단위로 갈라 적중률
                        --screens         화면 조립 재현 (폴백 비율)
```

---

## 2. 이번 세션에서 한 일

### 2.1 개역한글 원문 데이터 확보 — 판별 기준이 결정적이었다

대조 기준이 될 원문을 GitHub에서 찾았다. 후보는 여럿이었지만
**진짜 개역한글인지 가르는 것은 star 수도 설명도 아니라 옛 표기 보존 여부**였다.

채택: `bluesaurel/Korean-Bible-1961-KRV` @ `65cdce8`
(sha256·URL·형식은 `data/krv/SOURCE.md`)

**탈락 1 — `yuhwan/Bible-krv` (star 10, 후보 중 최다)**
시편 46:10이 "알**지**어다". 현대화된 사본이다. 정답지 1건으로 즉시 갈렸다.

**탈락 2 — `scj-peter/HolyBibleKRV`를 제2 소스(심판관)로 쓰려던 계획**

여기가 이번 세션에서 가장 배울 것이 많았던 지점이다.
불일치가 났을 때 "초안이 틀렸나, 원문 데이터가 틀렸나"를 가릴 심판관이 필요해
독립 소스를 받아 전수 비교했다. 두 소스가 **26.4% 불일치**했고,
어느 쪽이 신뢰할 만한지는 옛 표기 분포가 답했다.

| 옛 표기 | 채택본 | scj-peter |
|---|---:|---:|
| `-ㄹ찌라도` | 263 | **2** |
| `-ㄹ찌니라` | 461 | **2** |
| `세째` / `네째` | 46 / 37 | **1 / 1** |

scj-peter는 부분적으로 현대 맞춤법화된 사본이었다.

> **표본 검사의 한계가 실증됐다.** 정답지 6건에 든 시편 46:10은
> `-ㄹ찌어다` 계열이고, 이 계열만은 scj-peter에도 486건 살아 있었다.
> 즉 **정답지 6건만으로 검증했다면 이 데이터도 통과했을 것이다.**
> 분포를 보지 않았으면 못 잡았다. 다음에 원문 데이터를 교체할 때도
> 정답지 대조와 **분포 확인을 둘 다** 할 것 (`SOURCE.md` 갱신 절차에 넣어뒀다).

**그래서 제2 소스는 저장소에 넣지 않았다.** 옛 표기 판정에 쓸 수 없기 때문이다.
불일치 판정에는 대신 대한성서공회 공식 사이트를 3차 확인으로 썼다 (아래 2.3).

### 2.2 verify_verses.py — 자동 수정하지 않는 것이 설계의 핵심

불일치를 자동으로 고치지 않는다. diff만 출력하고 `verified: false`로 남긴다.
이유는 동일성유지권만이 아니라 **불일치의 원인이 두 가지**라서다.

```
(a) text가 틀렸다               -> text를 원문으로 교체하면 된다
(b) ref가 틀렸다 (절 경계 오류)  -> text만 고치면 잘못된 구절이 박제된다
```

`verses.yaml`의 시편 46:1이 (b)였다(1차 셀프 리뷰에서 검출·정정된 이력).
기계는 (a)와 (b)를 구분하지 못한다. 그래서 판단을 사람에게 남겼다.
대신 범위 참조는 **절 단위 원문을 따로 출력**해서 사람이 (a)/(b)를 즉시 가릴 수 있게 했다.

**주석을 지키기 위해 PyYAML로 덤프하지 않는다.** `verses.yaml`의 주석은
부산물이 아니라 큐레이션 자산이다 — 선정 근거, 검수 이력, 저작인격권 규칙,
감정 계열 구분선이 전부 주석에 있다. `safe_load` → `safe_dump` 한 번이면 전부 사라진다.
그래서 읽기는 PyYAML로 하되 **쓰기는 라인 단위 in-place 편집**이다
(`scripts/lib/verses_io.py`). 건드리는 것은 `verified` / `verified_by` / `verified_at`
세 줄뿐이고 나머지 모든 바이트는 그대로 남는다.

**기존 `verified: true`는 덮어쓰지 않는다.** 1차분 6건은 사람이 웹 원문과
직접 대조한 이력이 `verified_by`에 남아 있다. 스크립트가 자기 이름으로 덮으면
독립적으로 이뤄진 검증 기록이 사라진다. 재확인 자체는 전건에 대해 계속 돌고,
어긋나면 `(!) 기존 verified: true` 플래그와 함께 리포트에 뜬다.
**갱신에서 빠지는 것이지 검사에서 빠지는 것이 아니다.**

### 2.3 30건 대조 — 5건 불일치, 전부 초안 오류

1차 실행 결과 25/30 일치. **기존 `verified: true` 6건은 전건 재확인 통과**
(원문 데이터 신뢰도가 여기서 한 번 더 확인됐다).

불일치 5건은 전부 `ref`가 정확하고 `text`만 틀린 (a) 유형이었다.

| id | 유형 | 초안 → 원문 | 확인 |
|---|---|---|---|
| `php.4.6` | 쉼표 누락 | `간구로 너희` → `간구로, 너희` | 두 소스 일치 |
| `psa.55.22` | **단어 추가** | `맡겨 버리라 저가 너를` → `맡겨 버리라 너를` | 두 소스 + 성서공회 |
| `php.4.19` | 띄어쓰기 | `풍성한 대로` → `풍성한대로` | 소스 갈림 → **성서공회** |
| `jos.1.9` | **어구 누락** | `…함께 하느니라` → `…함께 하느니라 하시니라` | 두 소스 일치 |
| `deu.31.8` | 띄어쓰기 | `함께 하사` → `함께하사` | 소스 갈림 → **성서공회** |

`php.4.19`·`deu.31.8`은 두 소스가 갈려 대한성서공회 공식 사이트
(`bskorea.or.kr/bible/korbibReadpage.php?version=HAN`)로 3차 확인했고
둘 다 채택본이 맞았다. `psa.55.22`는 초안이 **없는 단어를 넣은** 유형이라
(개역개정 "그가 너를 붙드시고"의 영향으로 보인다) 별도로 공식 확인했다.

사용자 승인 후 5건 반영 → **30건 전건 `verified: true`**.

### 2.4 고장 테스트 — 4가지 유형 전부 검출

게이트가 실제로 작동하는지 일부러 고장 내서 확인했다. 전부 복원했고
파일 sha256이 `90d126c6…`로 되돌아온 것까지 확인했다.

| 고장 | 검출 | 종료 코드 |
|---|---|---|
| 옛 표기 현대화 (`알찌어다`→`알지어다`) | 불일치 1건 + `(!) 기존 verified: true` | 1 |
| `verified: false` 잔존 (본문은 전건 일치) | 게이트 조건 2 단독 발동 | 1 |
| 절 경계 오류 (`46:1`→`46:1-2`) | 불일치 + **절 단위 원문 출력** | 1 |
| 없는 절 참조 (`46:200`) | ref 오류 | 1 |

두 번째가 중요하다. 본문 30건이 전부 일치해도 `verified: false`가 하나 있으면
실패한다 — 두 조건이 독립적으로 작동한다.

### 2.5 2차분 큐레이션 60구절 — 본문 오류 0건

`sadness.*` 3개 + `exhaustion.*` 3개, 세분류당 10구절. 누적 90구절.

**방식 전환의 효과가 실측됐다.**

```
1차분 (타이핑)  30건 중 8건 오류 = 27%
2차분 (추출)    60건 중 0건 오류 =  0%
```

`verify_verses.py`가 이번에는 한 건도 잡지 않았다. 검출기가 아니라
안전망으로 작동한 것이고, 이것이 정상 상태다.

**선정에서 걸러낸 것** — 유형과 사례는 `verses.yaml` 말미 요약 주석에 있다.
핵심은 **유명한 구절일수록 위험할 수 있다**는 것이었다.
욥 1:21("취하신 자도 여호와시오니")은 상실 구절로 가장 널리 인용되지만
상실 직후에 수용을 요구하는 방향이고, 빌 4:13("모든 것을 할 수 있느니라")은
`themes.yaml`의 `strength` intent가 직접 지목한 구절이지만 무기력한 사람에게는
자신의 현재가 반증으로 보인다. **주제의 intent는 방향을 정하는 것이고
개별 구절 채택은 별개 판단이다** — 3차분에서도 같은 구분이 필요하다.

**원문 데이터 발견** — `-ㄹ지어다`/`-ㄹ찌어다` 혼재가 원문 그대로임을 확인했다.
첫 세션의 판정 방향이 이 구간에서는 뒤집힌다. 상세는 `data/krv/SOURCE.md` 4절.

### 2.6 3차분 큐레이션 60구절 — 화자를 바꿔서 푼 문제

`anger.*` 3개 + `frustration.*` 3개. 누적 150구절. **본문 오류 0건**(2차분에 이어 두 번째).

**이 차수의 난점은 데이터가 아니라 선정이었다.**
분노를 다루는 유명 구절 대부분이 화난 사람을 질책한다.

```
잠 29:11  "어리석은 자는 그 노를 다 드러내어도"
전  7:9   "노는 우매자의 품에 머무름이니라"
약 1:20   "사람의 성내는 것이 하나님의 의를 이루지 못함이니라"
잠 16:32  "노하기를 더디하는 자는 용사보다 낫고"   (비교 우위 —
          이미 화난 사람은 자동으로 하위에 놓인다)
```

`themes.yaml`의 `self_control` intent가 "정죄하지 않으면서 가라앉히는 방향,
**잠언 계열**"이라고 정의했지만 실제 잠언의 분노 구절은 대부분 정죄한다.

**해법은 어휘를 버리는 것이 아니라 화자를 바꾸는 것이었다.**
"너는 노하기를 더디 하라"(잠언)를 버리고
"여호와는 노하기를 더디 하시며"(시 103:8)를 취했다.
같은 어휘가 요구에서 서술로 바뀌면 정죄가 사라진다. 잠언에서는 15:1
한 건만 살렸다.

**주제 해석도 한 번 바꿨다.** `anger.rage`에서 `self_control`을 "억누름"이
아니라 **"쏟아낼 자리를 준다"**로 읽었다 — `psa.62.8`("마음을 토하라"),
`psa.142.2`("원통함을 토하며"), `psa.55.1`("응답하소서"). 격분에 통제를
요구하면 짜증 화면보다 더 크게 어긋난다.

**배분으로 정책을 구현했다.** `anger.unfair`를 `justice` 5 / `lament` 3 /
`forgiveness` 2로 두어 `themes.yaml` mapping 순서를 구절 수로도 반영했다.
남긴 forgiveness 2건도 사용자에게 용서를 요구하지 않는 형식만 골랐다.
마 6:14-15("용서하지 아니하면 아버지께서도 용서하지 아니하시리라")는
조건에 더해 **미용서에 대한 위협**이 붙어 제외했다.

3차분에서는 원문 데이터 문제가 새로 나오지 않았다(`SOURCE.md` 5절).

### 2.7 4차분 90구절 — Phase 1 큐레이션 완료

긍정 계열 8개 + `calm.stable` + crisis 전용. **누적 250구절, 본문 오류 0건(3회 연속).**

**위험의 종류가 바뀌었다.** 1~3차분은 "질책·요구가 얹히는 것"이 위험이었는데
4차분은 **번영신학·자기계발 톤**이었다. 제외한 대표 사례:

```
수 1:8   "주야로 묵상하여 ... 네가 형통하리라"   묵상 구절 중 최다 인용
시 1:3   "그 행사가 다 형통하리로다"             렘 17:7-8로 대체
시 37:4  "네 마음의 소원을 이루어 주시리로다"    설렘 화면에 직격
엡 3:20  "구하는 것에 더 넘치도록"
```

감사 구절은 근거를 **받은 것이 아니라 하나님의 성품**에 두는 쪽으로 골랐다
(`psa.107.1` "그는 선하시며 그 인자하심이 영원함이로다").

**확정 규칙 3이 실제로 작동했다.** 매핑 주제가 겹치는 세분류를 구절로 갈랐고,
지배 어휘가 갈라졌다 — `joy.delight`는 몸짓(뛰놀며·손바닥·춤), `flutter.thrill`은
강도(소리를 발하여·힘을 다하여·크게), `boredom.dull`은 다시 보게(눈을 열어서·맛·꿀),
`boredom.novelty`는 모르는 것으로(큰 광경·인물 나열). **주제를 공유해도 구절은
한 건도 겹치지 않고, 4차분 신규 복수 태깅은 0건이다.**

**crisis 전용 섹션의 구조.** `verses.yaml` 최상위 `crisis:` 키 + `crisis.` id 접두사.
`PLAN.md` 4.2가 `videos.json`에 정한 "crisis 최상위 분리 + 빌드 시 교차 검증 단언"을
구절에 그대로 적용했다. `verify_verses.py`의 `check_crisis_separation()`이
네 가지를 단언하고 하나라도 어긋나면 게이트가 실패한다(고장 테스트로 확인).

위기 풀의 선정 기준은 감정 풀보다 엄격하다. **시편 34:18을 뺀 것이 그 사례다** —
`sadness.sorrow`의 대표 구절이지만 후반부 "중심에 **통회**하는 자를 구원하시는도다"가
위기 상태에서 구원의 조건처럼 읽힐 여지가 있어, 조건이 없는 시편 147:3으로 대체했다.
**감정 화면에서 문제가 되지 않는 표현이 위기 화면에서는 문제가 된다.**

**`calm.stable` 누락과 보강.** 4차분 대상 8개에서 빠져 있었다. 3차분 HANDOFF를
쓸 때 "1차분 `psa.4.8` 교차 태깅으로 1건 있다"고 괄호 처리한 것이 목록에서
탈락시킨 원인이었다. 최종 집계에서 24개 중 유일하게 10건 미만인 것이 드러나
10건을 보강했고, 그 결과가 정확히 250건이다.

**원문 데이터 발견 — 절 합본 구간 19개.** 시편 92:1-3처럼 원문이 여러 절을
나누지 않고 합쳐 싣는 구간이 있다. 범위 ref로 쓰면 문장이 반복되므로
`krv_source.py`가 `RefError`로 막는다. 기존 구절은 전부 안전했다.
**이 유형은 정답지 대조로도 옛 표기 분포로도 잡히지 않고, 두 소스가 같은 결과를
내므로 교차 검증으로도 안 드러난다** (`SOURCE.md` 5절).

### 2.8 Phase 2 준비 — 도구 두 개와 결함 두 건

**`gen_verses_json.py`** (신규, API 0). `verses.yaml` -> `verses.json`,
60,613바이트 / 감정 240 + 위기 10. `note`·`verified*`를 제거하고
`crisis`를 최상위로 분리한다.

게이트 5종을 넣었고 그중 셋은 고장 테스트로 실제 차단을 확인했다.

```
verified: false 잔존          확인  종료 코드 1
개수 기대치(240 + 10)          확인  종료 코드 1
themes.yaml에 없는 theme       확인  종료 코드 1
위기·감정 풀 분리              verify_verses.py 규칙 재사용
입출력 id 집합 일치            개수만 보면 "같은 수의 다른 구절"을 못 잡는다
```

**작성 중 결함을 하나 잡았다.** 첫 생성 결과의 `text` 끝에 `
`이 붙어 있었다 —
YAML 폴디드 스칼라(`>`)가 값 끝에 개행을 붙이기 때문이다. `verify_verses.py`는
정규화 후 비교하므로 **소스 검사로는 드러나지 않던 결함**이고, 앱에 나가는
문자열에서만 보인다. 공백 정규화를 넣고 `assert_matches_source()`를 추가해
**정규화된 결과를 원문과 다시 대조**하게 했다. 검사 대상이 다르다는 것이 요점이다 —
`verify_verses`는 `verses.yaml`의 값을, 이 함수는 JSON에 실제로 들어간 문자열을 본다.

**`suggest_channels.py`** (FYM 이식 + PYM 고유 변경). `lib/`에
quota · quota_log · youtube · actions_status · normalize · channel_blocklist를
이식했고, allowlist는 PYM 필드로 새로 썼다.

- **`교단/소속 확인` 열** — 요약표에 비운 채 내보낸다. 승인 기준 1·2는 기계가
  판단할 수 없다. 검색은 이단 채널도 똑같이 데려온다 — 그들도 주일예배를 올린다.
- **타임스탬프 파일명** — `channel_candidates.20260818-064420.md`. 드라이런은
  `.dry-run`까지 붙어 두 겹으로 막는다 (FYM에서 661 units 결과가 덮인 사고).
- **검색어 6종 신규** — `themes.yaml` 전역 금지어를 그대로 적용하고, PYM 발굴에서만
  피하는 것(`설교` 단독 · `예언` · `환상` · `부흥회`)을 추가했다.
- **성격 신호어** — 제목 blocklist 사전이 없는 대신 신유·집회 / 번영신학 /
  자극 서사 / 신비주의 어휘를 센다. **차단이 아니라 표시**다.
- **quota_log 예약값 200** (FYM 7,900). 배치가 search를 쓰지 않아 실제 ~150이다.

**결함 2건 수정**

1. **빈 필수 필드가 통과했다** (`_is_blank`). 원본은
   `not str(entry.get(f, "")).strip()`로 검사했는데, YAML에서 값을 비운 키는
   빈 문자열이 아니라 `None`이고 `str(None)`이 `"None"`이라 truthy가 된다.
   `suggest_channels`가 내보내는 블록이 정확히 그 형태(`affiliation:` 비움)라,
   **"채우지 않으면 배치가 돌지 않는다"는 설계가 그대로 무력화되는** 상황이었다.
   `allowlist.py`·`channel_blocklist.py` 양쪽을 고치고 4케이스로 재확인했다.
   → **FYM 원본에도 남아 있다. 4.3절 참조.**
2. **`.gitignore`가 타임스탬프 파일명을 못 잡았다.** `channel_candidates.md`는
   정확히 그 이름만 매칭한다. 와일드카드로 바꿨다 — 그대로 뒀으면 실제 검토 시트가
   전부 추적 대상이 됐다.

이번 세션 API 소모 **0** (전부 드라이런).

### 2.9 Phase 0 채널 발굴 — 2회 실행과 기준 3개 개정

**발굴 2회, 1,324 units.**

```
1차 church      661 units  영상 300 → 채널 89 → 상위 30 조사
2차 institution 661 units  영상 299 → 채널 71 → 상위 30 조사  (중복 3건뿐)
채널 설명 확인    2 units  channels.list 2회 (42채널)
```

1차 검색어가 예배 이벤트 어휘라 개별 교회만 잡혔다. 2차를 기관형
(CTS·CBS·CGN·극동방송 + 성경통독)으로 짜서 갈래를 갈랐고, **중복이 3건뿐**
(전부 찬양 사역 단체 — 두 세트의 워십 검색어가 겹친 지점)이었다.
`DISCOVERY_QUERIES`를 `QUERY_SETS`로 나눠 1차 검색어를 보존했다.

**기준을 세 번 고쳤다. 전부 실측이 기준의 결함을 드러낸 경우다.**

**① 기준 1 (정체 확인) — 교단 확인 요구를 완화**
개정 전 기준으로는 **30개 후보 중 한 건도 승인할 수 없었다.**
교회 홈페이지에 교단을 명시하지 않는 것이 일반적이기 때문이다
(분당우리교회 홈페이지에 교단 표기 없음). 기준의 목적은 교단명 확정이 아니라
"운영 주체가 실재하는 정상 기독교 기관인가"를 확인하는 것이므로,
a(공식 채널 확인) · b(실체 정보) · c(이단 규정 대조)로 바꿨다.

이때 **두 방향의 신뢰도가 다르다**는 것도 실측으로 드러났다 — 분당우리교회
홈페이지가 링크한 것은 청년교구 채널이었고 발굴된 메인 채널과 달랐다.
그래서 1a를 "채널→홈페이지 방향 우선"으로 명시했다.

**② 기준 1b — 채널 설명도 유효한 근거로 인정**
교단이 홈페이지가 아니라 **채널 설명에서 확인된 사례가 3건**이었다
(오륜교회 "예장합동 수경노회", 꿈의교회, 낮은담교회 "기독교한국침례회").
채널 소유자가 직접 기재한 것이라 홈페이지보다 신뢰도가 높거나 같다.

**③ 기준 4 — 쇼츠 비율 → 쇼츠 제외 건수**
2차에서 CTS기독교TV·CGN·CBSJOY·ANOINTING 등 **가장 원하던 기관형 채널이
콘텐츠 문제 없이 쇼츠 비중만으로 실격**됐다. FYM 기준을 맥락 없이 가져온
탓이다 — FYM은 검색으로 모으니 채널 단위 쇼츠 비중이 품질 신호였지만,
PYM은 영상 단위로 길이 필터를 건다. 게다가 배치는 채널당 50~100건을
순회하는데 검토 시트는 10건만 본다. **표본 창이 좁아 생긴 오판이었다.**
`MIN_NON_SHORT_VIDEOS = 2`로 바꿔 **14건이 회복**됐다.

**④ 범위 조건 신설 (기준 0)** — 개신교·한국어로 한정. 가톨릭TV와 CGN Japan이
자동 점검을 통과해 올라오면서 명시하게 됐다. **이단이라서가 아니라 개역한글과
themes.yaml이 개신교 기반이라 어휘가 어긋나는 문제**라는 근거를 함께 남겼다.

**성격 신호어 조정** — `환상`을 뺐다. CGN 생명의 삶(큐티 채널)이 에스겔 40장
"새 성전 **환상**"으로 걸렸다. `축복`은 유지하되 오탐 사례(극동방송 국가
중보기도)를 주석에 남겼다. **자동 탈락시켰다면 큐티 채널과 매일기도 채널을
모두 잃었을 것** — 신호어를 차단이 아니라 표시로 둔 설계가 값을 했다.

**`channel_reviewed_out.yaml` 신설** — 검토를 마치고 제외한 10건을 기록한다.
blocklist("승인됐다가 문제된 채널")와 성격이 다르다. `suggest_channels.py`가
읽어 시트에 판단 근거를 표시하고 **상위 N 선정에서 빼 새 후보에 자리를 넘긴다.**
배치는 이 파일을 보지 않는다 — 화이트리스트에 없으면 애초에 수집되지 않는다.

**승인 ≠ 태깅** — `themes.yaml`에 명시했다. `PLAN` 후보 풀이 지목한
`새롭게하소서`가 CBS 대표 **간증** 프로그램인데 `간증`은 전역 금지어다.
승인해도 그 영상은 `untagged`로 빠지며 **이것은 의도된 동작**이다.
고칠 것은 태깅 규칙이 아니라 그 채널을 승인할 이유가 있었는지다.

### 2.10 media_type 설계 — 말씀/찬양 축 (구현은 Phase 2)

같은 감정·같은 주제라도 **어떤 형식으로 받고 싶은지는 사람마다 다르다.**
지친 사람에게는 30분 설교보다 찬양이, 막막한 사람에게는 그 반대가 맞다.
영상에 주제와 별개로 `media_type`(sermon / worship)을 붙이기로 했다.
**설계만 했고 구현은 Phase 2 배치에서 한다.**

**판별 신호 3가지** — 채널 `content_type` → 제목 어휘 → 길이 순.
전용 채널(MARKERS WORSHIP·CBS설교)은 1단계에서 끝나고,
**mixed 채널(오륜·꿈의교회·우리들교회)이 실제 판별 대상이다.**
본문 장절 패턴("요한복음 3:16")이 가장 강한 신호이며, 책 이름 66권은
`scripts/lib/krv_source.py`의 `BOOK_KEYS`를 재사용한다 — 사전을 두 벌 두지 않는다.

**판별 키워드 사전을 주제 태깅 사전과 분리했다.** 용도가 다르기 때문이다.

```
themes[].title_keywords       "어느 감정 화면에 갈 것인가"
                              → 전역 금지어(은혜·치유·축복·간증·기적)를 쓰지 않는다
media_types[].title_keywords  "말씀인가 찬양인가"만 가린다
                              → 이미 수집된 영상을 분류하는 것이라 장르를 부를
                                위험이 없다. "설교" 같은 넓은 단어를 여기서는 쓴다
                                (발굴 검색어에서는 너무 넓어 피했던 바로 그 단어다)
```

⚠ 두 사전을 섞지 말 것. 한쪽 원칙을 다른 쪽에 적용하면 둘 다 망가진다.
`themes.yaml` 빌드 검증 4번에 "media_types에는 금지어 규칙을 적용하지 않는다"를
명시해 뒀다.

**`unknown`을 두고 양쪽 토글 모두에 노출한다.** 주제 태깅의 `untagged`와 다르다 —
거기서는 "어느 감정에 맞는지 모른다"가 곧 "보여줄 근거가 없다"이지만, 여기서는
형식만 모를 뿐 감정 적합성은 이미 판정된 상태다. 판별 실패로 영상이 사라지는
것보다 낫고, `unknown` 비율이 사전을 고칠 근거가 된다.

**전환은 옵션이 아니라 필수 요건이다.** 영상 리스트 상단에 `[말씀]/[찬양]` 토글을
두고 **설정 화면에 숨기지 않는다.** 감정 분류가 완벽하지 않아 기본값이 안 맞는
경우가 상당수 나올 것을 전제한 요건이다 — 그때 사용자가 설정을 찾아 들어가야
한다면 그냥 이탈한다. 한쪽 풀이 비어도 **토글을 비활성화하지 않고**
"지금은 말씀 영상만 있어요"처럼 문장으로 상태를 보여준다(FYM 문구 원칙).
비활성화된 버튼은 "고장"으로 읽히지만 문장은 상황 설명으로 읽힌다.

**기본값 배분** — `media_defaults` 24건. worship 19 / sermon 5.
sermon은 `frustration.*`(3) + `boredom.*`(2)이다. 이 두 계열의 매핑 주제가
`guidance`·`patience`·`daily_word`·`bible_story`로 내용 중심이라 결이 맞는다.
**조정 후보 2건을 주석에 표시해 뒀다** — `anger.unfair`(매핑이 justice·lament로
내용 중심이지만 억울한 상태에서 긴 설교를 들을 여력은 별개 문제),
`calm.stable`(daily_word를 갖고 있어 sermon 여지). 토글 사용 로그가 쌓이면
기본값이 뒤집히는 세분류가 보일 것이다.

**쿼터는 늘지 않는다** — 채널 `content_type`과 영상 `title`·`duration`은 배치가
이미 받아오는 값이다.

**Phase 2가 추가로 해야 할 일** — 리포트에 **주제 × media_type 분포**를 남긴다.
한쪽이 0이면 토글을 눌러도 빈 화면이 나오므로, 주제당 물량을 셀 때 형식별로도
세어야 한다.

### 2.11 기준 1c 대조와 채널 승인 15건 — Phase 0 종료

**여기서 배울 것은 "공개된 1차 자료가 하나뿐이었다"는 사실이다.**

전수 대조가 가능한 교단 총회 공식 목록은 **예장통합 것 하나뿐이었다** —
「이단사이비 관련 총회 주요 결의」 제4회~제110회(1915~2025, 결의행 161개, PDF).
고신은 이대위 자료실에 통합 목록이 없었고(게시물 25건, 대부분 이단경계주일 자료),
합동·합신은 공개본을 찾지 못했다.

그래서 시트에 **범위를 명시하고 "목록에 없음"을 "이단이 아님"으로 쓰지 않았다.**
합동·합신·기감·기침은 개별 검색에 의존했으므로 통합과 신뢰도가 다르다.
`channel_criterion_c.20260818.md` 1절이 그 경계를 적어 둔 자리다.

**16건 중 규정 이력이 나온 것은 1건뿐이었고, 2차 자료가 원문과 달랐다.**

```
연세중앙교회 / 윤석전
  통합 83회(1998)  이단성 여부 질의 → 해교단 일임·주시
  통합 87회(2002)  해 교단 답변서 접수 → 주시 철회      ← 규정이 아니라 철회다
  합신 85회(2000)  집회참석금지                          ← 2차 자료에만 있다
```

2차 자료 여럿이 통합 결의를 "이단 규정"인 것처럼 옮겨 놓았다. **원문을 열지
않았으면 반대로 읽었을 것이다.** 첫 세션의 원문 데이터 교훈(2.1절)이 채널 쪽에서
그대로 반복됐다 — 표본이나 요약이 아니라 원본을 봐야 한다.

합신 건은 1차 확인에 실패해(홈페이지 인증서 오류) **"판단이 애매하면 넣지 않는다"로
제외**했다. `channel_reviewed_out.yaml`에 `criterion: "1c"` · `recheckable: true`로
남겼고, 재검토 조건은 **합신 총회 원문 확인**이다(2차 자료로는 재검토하지 않는다).

**오륜교회는 규정 대상이 교회가 아니라 행사였다.** 고신 73회(2023)가 "경계"로
결의한 것은 오륜교회가 주최하는 다니엘기도회다. 교회·담임 규정이 아니므로 승인했으나,
PYM이 그 채널의 영상을 수집하는 이상 그 행사 영상이 풀에 들어올 수 있어 `note`에 남겼다.

**경계선 3건의 판정이 전부 뒤집혔다 — 그래서 3차 발굴 없이 15건에 닿았다.**

| 채널 | 기존 판정 | 뒤집힌 근거 |
|---|---|---|
| 극동방송 | b가 주파수만 | 채널 설명이 링크한 공식 홈페이지에서 재단법인·1956 개국·대표 확인. 기준 1b는 "채널 설명 **또는 홈페이지에**"다 |
| 광주극동방송 | a 미충족 | 공식 홈페이지 `gj.febc.net`이 건 **재생목록의 소유 채널**이 후보 채널과 동일함을 확인 |
| 연세중앙교회 | a 미충족 | 채널 설명에 교단·홈페이지가 다 있어 a·b는 충족. 걸린 것은 c였다 |

광주극동방송이 특히 쓸모 있는 선례다. 채널 설명에 홈페이지 링크가 없어도
**홈페이지가 게시한 재생목록의 소유 채널을 대조하면** 기준 1a의 단서
("홈페이지→채널 방향만 있으면 동일한지 확인")를 만족시킬 수 있다.
분당우리교회 사례처럼 부속 채널로 어긋나는지도 같은 방법으로 갈린다.

**동명 채널이 실제 사고 위험이었다.** "우리들교회"라는 이름의 채널이 둘인데
**서로 다른 교회**다(하나는 판교/김양재, 하나는 천마산/예장합동 수도노회).
후자는 교단까지 적혀 있어 근거가 더 좋아 보인다 — 이름으로 대조하면 틀린다.
낮은담·베이직에도 동명 채널이 있다. 금지 ID 3건을 `channel_allowlist.yaml`
헤더와 해당 `note`에 박아 뒀다.

**API를 쓰지 않고 확인했다 (소모 0 units).** 채널 설명·외부 링크·재생목록 소유자·
채널 ID 역추적을 전부 공개 페이지 HTML에서 뽑았다. 1차 발굴 시트가
`.gitignore`(`channel_candidates*.md`)로 남아 있지 않아 교회 5건의 `channel_id`를
이 방법으로 복구했다. **다만 페이지 구조 의존이라 배치에는 넣지 않는다** —
일회성 검토 전용이다. 방법은 시트 6절에 적어 뒀다.

**로더 고장 테스트 5종을 돌렸다** — 빈 필드 / 키 삭제 / 핸들 형식 ID /
잘못된 `content_type` / ID 중복. 전부 차단됐다. 특히 첫 번째는 2.8절에서 고친
`_is_blank()`가 실제로 작동하는지를 본 것이다(FYM에는 아직 이 결함이 있다 — 4.3절).

### 2.12 Phase 2 배치 본체 — build_videos.py (2026-08-19)

**수집 구조가 FYM과 근본적으로 다르다.** FYM은 카테고리마다 검색이 달라
`videos.list`를 카테고리별로 불렀다. PYM의 후보는 채널 업로드 하나뿐이고
주제는 그 **뒤에** 제목으로 갈린다. 그래서 검증은 전체 후보에 대해 한 번만 돌고
태깅은 API 없이 그 결과 위에서 한다.

```
채널 15개 → channels.list 1회 → uploads 재생목록 → playlistItems 채널당 2페이지
          → videos.list (전체 후보 1회 통과) → 필터 → 제목 태깅 → 주제별 선정
드라이런 실측  후보 1,138 → 확보 770 → 태깅 633 / 미태깅 137(18%) → 주제 23개 448건
쿼터          예상 61 / 실측 50 units  (예상은 응답 없는 채널을 모르므로 보수적 상한)
```

**`playlistItems.list`에 페이지네이션을 넣었다** (`lib/youtube.py`). 없던 기능이다.
채널당 100건(2페이지)을 기본으로 잡은 이유는 쇼츠다 — CTS·CBSJOY·극동방송·
ANOINTING은 발굴 표본 10건 중 절반 이상이 3분 미만이었고, 50건만 읽으면 그런
채널의 잔존이 한 자릿수로 떨어져 "목록에 있으나 실질적으로 없는 채널"이 된다.
**채널당 1 unit을 더 쓰는 쪽이 길이 필터를 푸는 것보다 옳다** (기준 4 개정 2.9절과 같은 판단).

**회전(rotation) 판단이 FYM 일반 카테고리와 반대다.** FYM은 넣었다가 되돌렸고
PYM은 넣었다. 어휘가 아니라 **채널 목록의 성격이 다르기 때문**이다.

```
FYM 일반  검색 관련성 순으로 늘어선 50~80채널, 슬롯 20
          → 회전은 "관련성 상위 채널을 통째로 건너뛰기"가 된다
PYM       사람이 승인한 15채널, 슬롯 20. 전원이 동등한 자격이다
          → 슬롯 > 채널이라 회전은 "전원이 돌아가며 들어온다"가 된다
          그런데 상한 3 × 15채널 = 45 > 20이라 뒤쪽 채널은 2바퀴에 못 든다
```
즉 FYM **위기 카테고리**와 같은 상황이고, 거기서 회전이 하던 일을 여기서 한다.

**테스트가 라운드로빈의 공평성 결함을 잡았다.** 형식 균형 때문에 한 주제에서
라운드로빈을 여러 번 부르는데, 앞선 호출에서 1건씩 받은 채널이 다음 호출에서도
앞자리를 지켜 **뒤쪽 채널이 0건이 됐다.**

```
15채널 × 2건(말씀만) 실측   고치기 전  앞 10채널이 2건씩 20슬롯을 다 가져감, 뒤 5채널 0건
                            고친 뒤    5채널 2건 + 10채널 1건
```
매 바퀴 "지금까지 적게 뽑힌 채널부터"로 정렬해 고쳤다. **한 호출 안에서만
공평한 것으로는 부족하다** — 회전을 넣은 이유가 승인 채널 전원을 목록에
넣는 것이기 때문이다. 드라이런에서도 주제당 채널 수가 10~11 → 12~13으로 늘었다.

**형식 균형은 요구사항을 한 걸음 넘겼다.** HANDOFF 3절이 배치에 요구한 것은
"리포트에 주제 × media_type 분포를 남기는 것"이었다. 그런데 리포트만 남기면
풀에 찬양이 있는데도 상한·순서 때문에 20슬롯 밖으로 밀려 **매일 "찬양 0"을
보고만 하게 된다.** 그래서 선정 자체를 형식별로 나눴다(말씀 10 / 찬양 10 →
남는 쪽이 나머지). 한쪽뿐인 주제는 그대로 20건을 채우므로 균형이 상한이 되지 않는다.

`unknown`에는 별도 몫을 두지 않았다. **양쪽 토글 모두에 노출되므로 어느 쪽의
빈 화면도 만들지 않기 때문**이다. 경보(`media_type_gap`)도 원자료가 아니라
토글 기준으로 판정한다 — sermon 0건이어도 unknown이 있으면 화면은 비지 않는다.

**드라이런의 역할이 FYM과 달라져서 DryRunClient를 손봤다.** FYM 드라이런은
필터·쿼터만 보면 됐지만 PYM은 **제목으로 주제와 형식을 가른다.** 제목이 전부
"[dry-run] 잔잔한 영상"이면 태깅·판별 경로가 한 줄도 실행되지 않고 전건이
untagged로 떨어져, 리포트가 배치의 실제 동작을 보여주지 못한다. 셋을 고쳤다.

- `title_pool` — 주제 사전에서 합성한 제목 + 어느 주제에도 안 걸리는 제목 4종
- `playlist_items`가 만든 videoId → 그 채널로 귀속 (실제 API의 uploads 재생목록과
  같은 성질). 없으면 채널별 잔존 건수 리포트를 검증할 수 없다
- 길이·업로드 시각 분산 — 30분↑/10분↓/중간 세 구간이 다 나와야 길이 신호가 실행된다

⚠ **이것은 판별 규칙의 검증이 아니다.** 사전에서 만든 제목이라 당연히 걸린다.
규칙의 정오는 `tagging_test.py`가 손으로 쓴 제목으로 본다. 드라이런이 보는 것은
파이프라인이다.

⚠ **드라이런에서 채널 2개가 "접근 불가"로 뜨는 것은 합성이다.** DryRunClient가
채널 ID 해시 끝자리로 삭제 채널을 흉내 내는 것이고(경보 경로 검증용),
실제 CTS·CBSJOY에 문제가 있는 것이 아니다.

**제목 장절 패턴에 방어를 두 겹 넣었다.** `BOOK_KEYS`에는 1음절 약어가 많아
(행·요·사·시…) 그대로 쓰면 일상어에 걸린다.

```
(?<![가-힣])  앞 글자가 한글이면 매칭 안 함   "여행 12장" → 사도행전으로 잡히던 것
숫자 뒤 표지 필수 (: · 장 · 편 · 범위)       "감사 3가지 이유" → 이사야로 잡히던 것
```

**영문 키워드는 단어 경계로만 맞춘다.** themes.yaml이 `QT`에 명시적으로 요구한
규칙이다. 대신 **"Q.T." 표기는 놓친다** — 점이 경계가 되어 갈린다. 오탐과 미탐 중
미탐을 고른 것이고, 그것이 themes.yaml의 요구다.

**`--only`는 쿼터를 줄이지 않는다.** FYM에서는 카테고리별 검색이라 줄었지만
PYM은 수집이 채널 단위라 어느 주제를 고르든 같은 영상을 받아온다.
태깅·선정 결과만 좁혀 보는 옵션이다 (도움말에 적어 뒀다).

**제목 용어 blocklist는 비어 있다 — 알고 비운 것이다.** `apply_filters`에 인자
자리는 뒀지만 PYM은 넘기지 않는다. 전면 화이트리스트에서는 제목 필터의 역할이
작고 사전을 급히 만들면 정상 영상을 떨어뜨리는 쪽이 더 큰 위험이라는 판단이
`suggest_channels.py`(성격 신호어) 때와 같다. **배치가 매 실행 로그로 이 사실을
말한다.** Phase 3에서 FYM taxonomy.yaml을 이식하면 그 사전이 그대로 들어올 자리다.

**본체는 오케스트레이션만 남겼다.** 한 파일에 다 쓰니 1,443줄이 됐고(FYM은 1,214),
단계별 로직을 `lib/`으로 갈랐다 — `collect` · `selection` · `crisis` · `report` ·
`results`(자료형). 파일당 120~340줄이고 본체는 767줄이다. `results.py`를 따로 둔 것은
순환 의존 때문이다 — 네 모듈이 같은 자료형을 주고받는다.
**`selection.py`가 특히 독립 모듈일 값이 있다** — 채널 분산·형식 균형 규칙이
한 곳에 모여 있고, `spread_test.py`가 그 파일만 상대로 편중된 풀을 던진다.

**themes.yaml 빌드 검증 6종을 로더에서 한다.** 호출을 잊는 실패 모드를 없애려고
`load_themes()` 안에 넣었고, 6종 전부 고장을 주입해 실제로 막히는지 확인했다.
검증 1(mapping ↔ taxonomy 대조)만 **건너뛴다** — taxonomy.yaml이 Phase 3에서
들어오기 때문이고, 건너뛴 사실을 로그로 남긴다.

### 2.13 첫 실 API 실행 — 코드는 돌았고 **데이터가 서비스로 성립하지 않는다** (2026-08-19)

61 units. 예상치와 정확히 일치했다(`quota_log` 누적 PT 2026-08-18 기준 1,383).
파이프라인은 전 구간 정상이고 산출물도 나왔다. **문제는 그다음이다.**

```
수집   15채널 × 100건 = 1,500
필터   1,069 잔존 (이용불가 6 · 3분미만 425 · 국내차단 0 · 쇼츠표기 0)
태깅   247건 (23%)  ← 미태깅 822건 (77%)
선정   주제 23개에 118건. 목표(주제당 15~20)를 채운 주제는 2개뿐
경보   36건 — theme_low_yield 18 · media_type_gap 12 · theme_empty 3 · untagged_high 1 · 위기 2
```

**미태깅 77%가 이번 실측의 전부다.** 나머지 숫자는 전부 여기서 파생된다.

원인은 사전의 세부가 아니라 **전제**였다. `themes.yaml`의 `title_keywords`는
"두려워하지" · "쉼" · "잔잔한 찬양"처럼 **영상의 주제를 말하는 어휘**다.
그런데 승인 채널이 실제로 쓰는 제목은 그런 문장이 아니다.

```
설교  "어느 때까지 지체하겠느냐 - 주경훈 목사 | 오륜교회 주일설교 (2026-08-16)"
      → 본문 제목 + 설교자 + 예배 종류 + 날짜. 주제어가 들어갈 자리가 없다
찬양  "성도여 다 함께 + Jehovah + 하나님의 부르심 + 푯대를 향하여 |
       오륜교회 주일 6부예배 찬양 하이프레이즈 (2026-08-16)"
      → 곡 목록이다. 주제어가 아니라 곡 제목이 나열된다
```

**검색으로 영상을 찾던 세계의 어휘**를 화이트리스트 채널 제목에 적용한 것이다.
FYM에서는 맞는 전제였다 — 검색어와 제목이 같은 언어를 쓰기 때문이다.
PYM은 채널이 자기 관행대로 붙인 제목을 받는다. **PLAN.md 3.3의 "배치가 영상
제목을 주제 키워드 사전으로 태깅한다"가 실측에서 무너진 지점이다.**

**주제별 확보량** — 0건 3개 · 미달 18개 · 달성 2개

```
0건    self_control · guidance · quiet_worship
        quiet_worship이 특히 뼈아프다. 매핑에서 6개 세분류가 이 주제를 쓰는데
        (불안·짜증·지침·평온 계열) 풀이 통째로 비었다. 찬양 영상은 잔뜩 있는데
        제목이 곡 목록이라 "잔잔한 찬양" 같은 키워드에 하나도 안 걸린다
달성    gratitude 16 · daily_word 15
미달    나머지 18개 (1~14건)
```

**`daily_word`는 다른 종류의 미달이다.** 풀이 163건인데 선정은 15건이다 —
그 163건이 채널 3개(CGN 생명의 삶 100건 등)에서만 나와 **채널당 상한 5 × 3채널 =
15**가 천장이 됐다. 분산 정책이 물량을 누른 것이고, 이건 사전 문제가 아니다.
다른 주제가 채워지면 자연히 풀리지만, 안 풀리면 "한 주제의 공급 채널이 3개뿐일 때
상한을 어떻게 볼 것인가"를 정해야 한다.

**형식 편중 12건도 같은 뿌리다.** worship 0건인 주제가 12개인데, 찬양 영상이
없어서가 아니라 **찬양 영상이 어느 주제에도 태깅되지 않아서** 풀에 못 들어온다.
선정의 형식 균형 로직은 정상 작동했다 — 나눌 것이 없었을 뿐이다.

**채널별 잔존 — 쇼츠 예상이 절반만 맞았다**

```
채널                  수집 → 잔존 → 태깅 → 선정
오륜교회               100 →  77 →  19 → 16
꿈의교회               100 →  62 →  28 → 15
낮은담교회             100 →  99 →   7 →  7
우리들교회             100 →  96 →  52 → 14
베이직교회             100 → 100 →   8 →  8
CTS기독교TV           100 →  38 →   4 →  4
CGN                  100 →  58 →   7 →  7
CGN 생명의 삶          100 → 100 → 100 → 17   ← 태깅률 100%. 큐티 채널이라 제목이 주제어다
CGN 성경통독           100 → 100 →   0 →  0   ← 잔존은 만점, 태깅 0
CBSJOY               100 →  48 →   2 →  2
CBS설교               100 →  86 →   8 →  8
ANOINTING            100 →  78 →   5 →  5
옹기장이선교단          100 →  60 →   3 →  3
극동방송               100 →  19 →   0 →  0   ← 잔존 최저 + 태깅 0
광주극동방송            100 →  48 →   4 →  4
```

- **필터 후 0건인 채널은 없었다.** 걱정하던 실격은 일어나지 않았다.
- **쇼츠 비중 예상은 빗나갔다.** 발굴 표본에서 쇼츠가 많던 ANOINTING은 78건이
  남았고, 표본에서 문제없던 극동방송이 19건으로 최저였다.
  **10건 표본으로 채널 성격을 판정할 수 없다는 것이 실측으로 확인됐다**
  (기준 4를 "비율"에서 "건수"로 바꾼 2.9절의 판단이 옳았던 셈이다).
- **실질적으로 기여가 0인 채널이 둘 있다** — CGN 성경통독·극동방송. 잔존은
  각각 100·19인데 태깅이 0이라 서비스에 한 건도 못 올라간다. `channel_zero_yield`
  경보는 잔존 기준이라 이 둘을 잡지 못했다. **경보 조건에 "태깅 0"을 넣어야 한다.**

**media_type 판별은 잘 작동했다** — 미판별 73건(7%)뿐이다.

```
sermon 812 · worship 184 · unknown 73
판정 근거  channel 858 · duration 94 · none 73 · title 33 · scripture 11
```
채널 `content_type`이 80%를 결정했다. 설계대로다(전용 채널은 1단계에서 끝난다).
**본문 장절 신호가 11건뿐인 것은 의외였다** — 설교 제목에 장절을 안 쓰는 채널이
대부분이고, 쓰더라도 "요한복음 3장 16절"이 아니라 본문 문장을 제목으로 쓴다.

**다음 세션이 판단할 것 — 태깅 전략** (대책을 정하지 않고 선택지만 남긴다)

```
a) 사전을 실제 제목 어휘로 다시 쓴다
   장점: 구조 변경 없음. 단점: 곡 목록·설교자명 제목에는 붙일 어휘 자체가 없다
b) 재생목록 이름을 신호로 쓴다 (playlists.list, 채널당 1 unit)
   교회 채널은 "주일설교" · "수요예배 찬양" 식으로 재생목록을 나눠 둔다.
   쿼터는 15 units 늘어난다. media_type에는 확실히 유효하고 주제에는 약하다
c) **구절 계층을 태깅에 쓴다 — PYM에만 있는 자산이다**
   verses.yaml 250구절에는 이미 theme이 달려 있다. 제목의 본문 장절을 그 구절과
   맞추면 "요한복음 3:16 설교" → 그 구절의 theme으로 붙는다.
   다만 이번 실측에서 장절 패턴이 11건뿐이라 단독으로는 부족하다
d) 설명란을 본다 — FYM이 명시적으로 배제한 경로다(오차단 사고).
   다시 열려면 그때의 근거를 뒤집을 실측이 필요하다
e) 주제 체계를 줄인다 — 24개 주제가 이 물량에 맞는가를 되묻는 선택지
```

**분명한 것 하나** — 채널을 늘리는 것(3차 발굴)은 이 문제를 풀지 못한다.
77%가 미태깅인 상태에서 채널을 두 배로 늘리면 미태깅도 두 배가 된다.
**태깅 전략을 먼저 정하고 그다음에 채널을 늘려야 한다.**

**리포트 표본을 채널별로 바꿨다** — 첫 실행에서 `untagged_sample` 15건이 전부
오륜교회였다(앞에서부터 잘랐으므로). 채널마다 5건씩 담도록 고쳤다.
다음 실행부터는 어느 채널의 어떤 제목이 안 걸리는지가 리포트만으로 보인다.

### 2.14 태깅 전략 확정 — 주제 + 폴백 2층 구조 (2026-08-19)

2.13절의 선택지 a~e 중 어느 하나를 고르는 대신, **전제를 바꿨다.**

```
기존  영상의 주제를 제목에서 알아낸다 → 못 알아내면 노출하지 않는다
변경  주제 태깅은 되는 만큼 하고(정밀도 우선), 안 되는 영상도
      형식(media_type)과 채널로 폴백 노출한다
```

근거는 사전의 문제가 아니라는 실측이다. 1,069건 중 제목이 주제를 말하는 콘텐츠는
CGN 생명의 삶 100건뿐이고 나머지는 설교 문장·곡목·프로그램 브랜드였다.
**사전을 키워도 제목의 정보 구조 자체가 바뀌지 않는다.** PLAN.md 3.3을 개정했고
근거·수치를 거기 남겼다.

**폴백 상한 12/20 (60%)** — 사용자 결정. 20건을 못 채우는 것은 허용하되 채우려고
폴백을 무제한으로 늘리지 않는다. 그러면 그 화면은 "감정에 맞춘 목록"이 아니라
"채널 최신 목록"이 되기 때문이다.

**실측 데이터에 새 코드를 그대로 돌려 확인했다 (API 0, `dist/titles.json` 재사용)**

```
24화면 × 20슬롯 = 480  →  총 노출 452건 (주제분 283 + 폴백 169)
20건 달성 17/24 · 최소 12건 (anger.irritation)
경보 theme_fallback_heavy 9건 · theme_too_few 0건
```

폴백 비율이 높은 순: anger.irritation 100% · calm.ease 92% · exhaustion.tired 86% ·
anger.unfair 71% · frustration.suppressed 67% · anxiety.restless 63% · anger.rage 63% ·
sadness.lonely 60% · anxiety.tension 55%. **분노 계열 전체가 상위에 있다** —
`self_control`·`quiet_worship` 두 주제가 빈 풀이기 때문이고, 채널 구성이 그 감정을
못 받치고 있다는 뜻이다.

**경보 2종을 새로 뒀다. 폴백은 메우는 장치이지 해결이 아니기 때문이다.**

```
theme_fallback_heavy  폴백 > 50%    그 감정을 주제로 못 받치고 있다 (warning)
theme_too_few         최종 < 8건     화면으로 성립하지 않는다 (critical)
```

**media_type 판별 우선순위를 뒤집었다 — 제목이 채널보다 앞이다.**

실측이 순서를 뒤집었다. 개정 전 1순위였던 채널 `content_type`은 "그 채널이 주로
무엇을 올리는가"이지 "이 영상이 무엇인가"가 아니다.

```
오륜교회(sermon)   주일 각 부 예배 찬양 콘티 55건
우리들교회(sermon)  찬양 26건 · 꿈의교회(sermon) 찬양 14건
→ 1,069건 중 81건이 찬양인데 sermon으로 판정됐다.
  앱에서 [찬양] 토글을 누르면 이 81건이 통째로 사라진다.
새 순서  제목 → 장절 → 채널 → 길이     말씀 813→732 / 찬양 184→265
```

**장절보다도 제목이 앞인 이유**는 3건이지만 방향이 분명하다 —
`"시편 92편 + 주 이름 찬양 + … | 주일 2부예배 찬양 헤세드"`처럼 **찬양 콘티의 곡명이
시편**인 경우가 있다. 장절을 먼저 보면 설교가 된다.

**videos.json의 키가 주제에서 세분류로 바뀌었다.** 폴백이 세분류의 `media_defaults`에
달려 있고, "이 화면이 몇 건 나가는가"도 세분류 단위로만 정해진다. 주제별 목록을
내보내고 조합을 앱에 맡기면 **배치가 화면 성립 여부를 판정할 수 없다.**
주제별 집계는 `build_report.json`에 남겼다(진단용).

**`source` 필드를 데이터에 남긴다 — 앱은 두 층을 섞지 않는다.**

이것은 UI 취향이 아니라 신뢰의 문제다. 사용자가 "이게 왜 내 감정에 맞지?"라고
느끼는 순간 화면 전체를 의심하게 된다. **정직하게 나누면 그 위험이 없다** —
"이 감정에 맞춰 고른 말씀 / 이 채널의 최근 영상"처럼 층이 보이면 된다.
문구는 Phase 3에서 정하되 **섞는 선택지는 닫아 뒀다**(PLAN.md 3.3 명시).
배치는 `assert_fallback_untagged()`로 폴백에 태깅분이 섞이면 배포를 막는다.

**폴백 정렬은 채널 간 우열을 두지 않는다.** 태깅률이 낮은 채널은 콘텐츠가 나빠서가
아니라 제목 관행이 다를 뿐이다(CGN 성경통독 태깅률 0%, 그러나 성경 통독 방송이다).
순위를 매기면 그 관행 차이가 노출 차별이 된다. 최신순 + 채널 라운드로빈,
채널당 상한 3은 주제분과 동일하다.

**남은 관찰 — 같은 폴백이 여러 화면에 보인다.** `worship` 기본값 세분류가 17개라
그 화면들의 폴백 앞부분이 겹친다. 사용자는 한 번에 한 화면만 보므로 당장 문제는
아니지만, 세분류별 오프셋을 주면 해소된다. 실사용 로그가 쌓인 뒤 판단할 것.

**사전 보강은 하지 않았다 (별도 작업).** 곡명 258개 중 현재 사전에 걸리는 것은
20개(7.8%)이고, 어미만 다른 근접 미스가 29개(11.2%)다 — 어간 수준으로 다듬으면
19%까지 오른다. `love` 계열에 11개가 몰려 있다(`사랑하시` → `사랑하심을`·`그 사랑`).
**themes.yaml 원칙대로 어간을 줄일 때마다 오탐을 실측해야 하므로** 이번 범위에서 뺐다.

### 2.15 Actions 워크플로 — 드라이런 → 실행 → 배포까지 확인 (2026-08-19)

`.github/workflows/build.yml` 하나만 만들었다. `deploy-app.yml`은 만들지 않았고
근거는 아래에 있다.

**cron을 쿼터가 아니라 업로드 시각에 맞췄다 — FYM과 다른 판단이다.**

```
FYM  UTC 08:30  검색 배치(일 7,900)라 "리셋 직후에 하루치 확보"가 중요했다
PYM  UTC 09:30  하루 ~62 units. 리셋과의 거리가 무의미하다.
     ★ 2026-08-26 개정 — 2.68절: 21:30 UTC(KST 06:30) 슬롯이 추가돼 하루 2회다.
       아래 '새벽예배 + 주일예배' 가정 중 **주일예배 부분은 실측과 다르다**(2.68 ②).
       units도 채널 15개 시절 값이다 — 22채널은 1회 ~93.
     (KST 18:30)  대신 새벽예배(KST 05~07)·주일예배(KST 12~14)가 모두
                  창 안에 들어오는 시각을 골랐다
```
FYM과 1시간을 띄운 것은 두 저장소의 실행 로그를 사람이 구분해 읽기 위해서다.
API 키가 분리돼 있어 쿼터는 서로 간섭하지 않는다.

**게이트를 배치보다 먼저 돌린다.** "배포 전에만 통과하면 된다"가 아니라 여기서
깨지면 배치를 돌릴 이유가 없고, 먼저 세우면 쿼터도 아낀다.
`verify_verses --check` · `tagging_test` · `spread_test` 3종.

**CI와 로컬의 차이는 인자 3개뿐이다** — 같은 스크립트, 같은 기본값을 탄다.

```
--previous _previous/data/videos.json   gh-pages에서 체크아웃한 직전 배포본
--dump-titles                           제목 원자료를 아티팩트로 남긴다
--no-reserve-actions-batch              ★ 아래 참조
```

**`--no-reserve-actions-batch`가 CI에만 붙는 이유**를 헷갈리지 말 것.
그 예약(200)은 **로컬 실행이 Actions 배치 몫을 미리 비워두는** 장치다.
CI에서는 이 실행이 곧 그 배치이므로, 자기 몫을 자기가 또 예약하면 이중 계상이 된다.

**quota_log는 로컬과 CI가 다르게 남는다 — 구조적 한계이고 알고 두는 것이다.**

```
로컬  .quota_log.json에 날짜별 누적이 쌓인다 (gitignore, 기기마다 다름)
      다음 실행이 "오늘 이미 얼마 썼는지"를 보고 판단한다
CI    러너가 매번 초기화되므로 로그가 남지 않는다. 매 실행이 0에서 시작한다
      → 같은 날 CI가 두 번 돌면 서로의 소모를 모른다 (62 units라 실질 위험은 없다)
      → 대신 두 곳에 남긴다: Job Summary(사람이 즉시 보는 곳) + 아티팩트
```
**실패한 실행도 쿼터를 쓴다.** `build_videos.py`가 중단되더라도 `finally`에서
기록하므로, 리포트가 없어도 `.quota_log.json`에는 남고 그 파일이 아티팩트로 올라간다.

**드라이런은 배포와 Issue 생성을 건너뛴다.** DryRunClient가 채널 몇 개를 일부러
"삭제됨"으로 흉내 내므로(경보 경로 검증용) 가짜 critical이 뜬다. 그것으로 Issue를
만들면 안 된다.

**실행 확인 (2026-08-19)**

```
드라이런  run 32208616171  성공 · 게이트 3종 통과 · 세분류 24개 · 스키마 검증 통과
          배포·Issue·정리 단계 전부 스킵됨
실제 실행  run 32208740306  성공 · 61 units · 452건 · gh-pages 브랜치 생성
          data/videos.json(179KB) · data/version.json 배포 확인
```

**`직전 배포본 가져오기`가 매 실행 빨간 주석을 남긴다** — gh-pages가 없던 첫 실행의
잔재다. `continue-on-error: true`라 잡은 실패이고 job은 성공했다. 브랜치가 생겼으니
다음 실행부터는 사라진다. **없어지지 않으면 그때는 진짜 문제다.**

**deploy-app.yml을 만들지 않은 이유.** `app/`에는 `src/data/verses.json` 하나뿐이고
`package.json`도 빌드 도구 결정도 없다. FYM의 것은 `npm ci` · `npm test` ·
`npm run build` · `app/dist`를 전제로 짜여 있어, 지금 옮기면 Node 버전·빌드 명령·
산출 경로를 **추측으로 박아 두는 것**이 된다. Phase 3에서 만들되 두 불변식은 반드시:

```
1. concurrency 그룹을 build.yml과 공유한다 (gh-pages-deploy)
2. keep_files: true — 없으면 앱 배포가 data/를 통째로 지운다
```

**⚠ 첫 실행에서 Issue가 44건 생겼다.** (→ 2026-08-19 조정 완료. `alerts.py`가
경보마다 `route`를 들고 오고, 진단은 Job Summary로만 가며 Issue는 제목 중복 판정 +
`renotify_days` 기본 7일이 걸린다. 이 항목은 더 이상 할 일이 아니다.) 경보 하나당 Issue 하나라는 FYM 방식을
그대로 썼는데, PYM은 경보 수가 훨씬 많다(주제 23개 × 진단 경보). 44건 중 32건은
`theme_low_yield`·`media_type_gap`·`theme_empty` — **"지금 데이터 상태"에 가깝지
"조치할 사건"이 아니다.** 이대로 두면 매일 쌓여 `auto` 라벨 자체를 아무도 안 보게
된다. 정책 조정이 다음 세션 첫 과제다 (3절 8번).

### 2.16 Phase 3 앱 — 구현·배포 (2026-08-19)

FYM 앱의 셸·분류·PWA 장치를 이식하고 PYM 화면 세 가지를 새로 만들었다.
화면 흐름과 문구는 구현 전에 검토받고 확정했다(공감 문구 → 구절 카드 → 토글 →
영상 2층 → 마무리, 한 화면 세로 배치).

**로딩 지연은 1000ms다 — 800ms가 아니다.** 지시받은 값이 800ms였으나 FYM
taxonomy.yaml에 "즉답→800→1000을 비교해 1000 채택"이라는 확정 기록과 "800ms는
이전 검토의 잔재"라는 정정 주석이 있었다. 그대로 이식하면 이미 뒤집은 결정을
되돌리는 것이라 1000ms로 갔고, 생성기 게이트가 1000 미만을 거부하게 해뒀다.

**새 화면에서 구조로 막은 것**

```
구절 카드    본문을 가공하지 않는다(동일성유지권). 테스트가 slice·replace·trim이
             verse.text에 닿는지 소스에서 확인한다
             "다른 구절 보기"는 랜덤이 아니라 순환 — 랜덤이면 같은 구절이 다시
             나와 버튼이 고장 난 것처럼 읽힌다
출처 표기    구절 카드 안 + 정보 화면. 중복이 아니라 요건이다(성명표시권은 본문이
             보이는 자리에, 정보 화면 표기는 앱 전체 고지)
토글         한쪽이 비어도 **비활성화하지 않는다.** 비활성 버튼은 "고장"으로 읽히고
             문장은 상황 설명으로 읽힌다 → "지금은 말씀 영상만 있어요"
source 층    layersFor가 {theme, fallback}을 돌려준다. 평평한 배열을 주지 않으므로
             합치려면 호출부가 의도적으로 합쳐야 하고 그런 코드는 리뷰에서 보인다
위기 화면    MediaToggle·VideoList를 import하지 않고 목록을 직접 그린다. 나중에 저
             컴포넌트에 폴백·토글이 추가돼도 이 화면은 영향을 받지 않는다
```

**테스트 61개** (FYM 49개 기준 충족). 위기 경로는 데이터 플래그와 **소스 코드**
양쪽을 본다 — `media_type_toggle: false`만 믿지 않고 컴포넌트가 실제로 무엇을
import하는지, 상담 안내가 구절보다 앞 마크업인지 검사한다.

**실행으로만 잡힌 버그가 하나 있었다.** `withMinDuration`에 프라미스를 넘겨
결과·위기 화면이 로딩에서 영원히 돌았다(`work is not a function`). 테스트 58개는
전부 통과하는 상태였다 — **화면을 실제로 렌더링해야 드러나는 유형**이라, 앞으로도
구현 후 실행 확인을 생략하지 말 것. 회귀 테스트를 붙였다.

**배포 (deploy-app.yml)**

두 불변식을 지켰다: concurrency 그룹 `gh-pages-deploy` 공유, `keep_files: true`.
배포 후 확인 결과 `data/` 3개 파일(videos·version·diagnostics)이 그대로 남았다 —
없었으면 앱 배포가 그걸 지우고 다음 배치까지 앱이 번들 시드로만 살았을 것이다.

```
https://appleap385-aaz0919.github.io/PeaceYourMind/
gh-pages 루트  index.html · assets/ · icons/ · manifest.webmanifest · sw.js · .nojekyll
gh-pages data/ videos.json(179KB) · version.json · diagnostics.json
```

**배포본 실동작 확인** (실제 URL 렌더링)

```
원격 데이터   data/version.json?t=… → data/videos.json?v=2026-08-19T02:53:24Z
              (version 비교 후에만 본체를 받는 경로가 그대로 작동)
서비스워커    등록됨 scope=/PeaceYourMind/
IndexedDB     캐시됨 version=2026-08-19T02:53:24Z · 세분류 24 · 영상 452
              → 번들 시드(144건)가 아니라 배포 데이터를 쓰고 있다
썸네일        8/8 로드 (첫 스크린샷에서 비어 보인 것은 lazy loading이었다)
콘솔 오류     없음
```

**GitHub Pages를 이때 처음 켰다** (`source: gh-pages / root`). 브랜치에 파일이
있어도 Pages가 꺼져 있으면 404다 — 첫 배포에서 5분쯤 헤맨 지점이다.

**워크플로에서 잡은 결함 2건**

```
1. 배포 게이트에 requests가 없어 spread_test가 ModuleNotFoundError로 멈췄다.
   PyYAML만 설치하고 있었다 → scripts/requirements.txt로 교체.
   **게이트에서 멈췄으므로 배포는 되지 않았다** — 설계대로 작동한 것이다
2. 실패 알림이 "Resource not accessible by integration"으로 실패했다.
   permissions에 contents만 있고 issues가 없었다 — 배포가 깨진 것을 알리는
   장치가 정작 알리지 못하는 상태였다. 배포 자체는 안전했지만 다음 실패는
   아무도 모르게 지나갔을 것이다
```

### 2.17 주제 사전 보강 — 실효 구간은 찬양 곡명이었다 (2026-08-19)

직전 세션이 남긴 2번 과제다. **2.14절의 "사전을 키워도 소용없다"는 절반만
맞았다** — 제목 종류를 갈라 보지 않아 하나로 보였던 것이다.

```
설교 730건 · 태깅 28.5%   설교자+교회+예배+날짜+"문학적 제목"+본문
                          주제어가 들어갈 자리가 구조적으로 없다.
                          "김관성 목사 낮은담교회 새벽만나 2026.8.19
                           "잠든 주, 잠잠해진 바다" 누가복음 8:22-25"
찬양 265건 · 태깅 12.5%   곡명이 곧 주제어다. 못 잡은 이유는 어휘가 없어서가
                          아니라 **어미가 갈려서**였다
```

한글이 음절 단위라 `사랑하시`는 `사랑하심을`을 못 잡는다(심 ≠ 시). `기뻐하`는
`기뻐해`를, `두려워하지`·`두려워말라`는 `두려워 말고`를 못 잡는다. 사전이 틀린
게 아니라 활용형 한 칸을 못 덮고 있었다.

**도구를 먼저 만들었다 — `scripts/retag_titles.py` (API 0).**

`--dry-run`으로는 이 일을 못 한다. 드라이런 제목은 사전에서 합성한 것이라 항상
걸리기 때문이다. 실측 덤프 `dist/titles.json`(1,067건)에 사전을 다시 걸어 본다.

```
(기본)      태깅 현황 — 덤프 대비 신규/상실까지 센다
--probe     후보 어간이 잡는 제목을 전부 보여준다. **오탐 판정은 사람이 한다**
--songs     콘티 제목을 곡명으로 갈라 적중률을 본다 (408종 중 28종)
--screens   배치의 build_subcategories를 그대로 불러 화면을 재조립한다
            — 폴백 비율은 화면 단위에서만 정해지므로 여기까지 봐야 한다
```

**결과 — 게이트 4종(+`messages_test`·드라이런) 전부 통과, 태깅 상실 0건.**

| | 개정 전 | 개정 후 |
|---|---|---|
| 태깅 | 247건 (23.1%) | 279건 (26.1%) |
| 폴백 | 177건 (39.2%) | **151건 (33.3%)** |
| 20건 달성 | 17/24 | **19/24** |
| `theme_fallback_heavy` | 9개 | **5개** |

주제별로는 `love` 1→20, `trust` 14→23, `joy_praise` 11→19, `peace` 7→10,
`gratitude` 19→22, `guidance` **0→1**(빈 풀 하나 해소). 오륜교회 태깅률이
24.7%→46.8%로 올랐다 — 찬양 콘티를 올리는 채널이라 개정이 그대로 반영됐다.

**어간을 무조건 줄이지 않았다. 이게 이번 개정의 핵심 판단이다.**

`love`에서 `사랑하시`→`사랑하`로 줄여 실측하니 적중 9건 중 4건이 **방향이 반대**
였다("우리가 주를 더욱 사랑하고", "그러므로 사랑하자"). 이 주제는 사랑받음이지
사랑함이 아니다. 어간을 줄이는 대신 **주어를 하나님으로 묶는 표지**만 넣었다 —
존칭 어미(시·심·신·셨)와 소유격 결합형(그 사랑·주 사랑·주의 사랑).
`tagging_test.py`에 정탐 4건 + **비태깅 2건**을 고정해 뒀다. 나중에 "줄이면 더
잡힌다"며 되돌리는 것을 막는 장치다.

**넣지 않은 것도 기록해 둔다.** `할 수 있다 하신`·`강함 되시네`·`그리
아니하실지라도`처럼 곡 하나만 잡는 문자열은 실측 정탐이었지만 뺐다. 넣으면
themes.yaml이 큐레이션 문서가 아니라 곡목록이 된다. 넣은 것은 여러 곡·설교에
걸치는 관용어(신실·송축·선하심)뿐이다.

**남은 폴백 과다 5개는 사전으로 못 고친다 — 확인했다.**

`anger.irritation`(폴백 100%, 주제분 0건)이 기대는 `self_control`·
`quiet_worship` 두 주제는 후보 어휘를 전부 실측해도 1,067건에 **없다**.

```
분노 0 · 노하 0 · 온유 0 · 절제 1(이미 daily_word) · 연주곡 1(이미 hope)
```

사전 문제가 아니라 채널 구성 문제다. 3차 발굴이 무엇을 찾아야 하는지가
이걸로 정해졌다 — 분노를 다루는 설교와 잔잔한 연주 찬양이다.

### 2.18 UI 개정 9건 — 대부분 "FYM으로 되돌리기"였다 (2026-08-19)

지시받은 9건 중 **2건은 이미 되어 있었다**(직전 커밋 9360912). 나머지 7건을
고쳤고, 그중 다수는 새로 만드는 일이 아니라 **FYM으로 되돌리는 일**이었다.

```
[1] 공감 문구 15.5→17.5px   이미 완료 (9360912). 구절 크기 연동까지 되어 있다
[2] 폴백 헤더 문구 개정      이미 완료 (9360912). 테스트도 그 문구를 검사 중
[3] 입력 화면 배치           glow·중앙정렬·수직여백·캡션 — FYM 구조로 맞춤
[4] 입력창 포커스·높이       textarea 4줄 → input 1줄, outline 제거 + 밑줄 강조
[5] 구절 카드 출처 표기 제거  정책 변경 — 아래 별도 기록
[6] 구절 도입 문구 제거      lead prop까지 삭제
[7] 카테고리 칩 pill 복원    radius 3 → 99
[8] 구절 본문 20/18/16.5 → 22/20/18
[9] FYM 확정 UI 문구 복원    캡션 "오늘의 마음" · 버튼 "마음 들여다보기"
```

**[9]의 전제 하나는 사실과 달랐다 — placeholder는 원래 FYM 것이다.**
PYM의 placeholder 풀 6개는 FYM `taxonomy.yaml`과 **글자 단위로 같다.**
지시가 지목한 "잘 정리되지 않아도 괜찮아요. 떠오르는 대로"도, 쓰라고 한
"지금 기분을 한 줄로 적어봐요"도 **둘 다 그 풀 안에 있다.** PYM이 새로 쓴 것이
아니라 진입할 때마다 랜덤으로 하나가 뽑히는 것이고(FYM도 같다), 그날 뽑힌 것이
긴 쪽이었을 뿐이다. 그래서 풀을 건드리지 않았다 — [9] 원칙("FYM에 대응물이
있으면 FYM 것을 쓴다")을 이미 만족하고 있기 때문이다. 하나로 고정하고 싶으면
그건 회전을 없애는 별개의 결정이다.

**[3]의 원인 진단이 정확했다.** "지금은 어떤 마음인가요?"가 입력 필드 라벨처럼
읽힌 것은 문구 탓이 아니었다 — 그 문장은 재방문 인사 풀(`same_day`)의 8번째
항목이고, 좌측 정렬 + 캡션 없음 + glow 없음이 겹쳐 인사가 라벨로 읽힌 것이다.
배치를 FYM 구조로 되돌리자 같은 문구가 인사로 읽힌다.

**실측 — FYM 실물과 나란히 재서 맞춘 것이다 (360×640)**

```
             FYM      PYM(개정 후)
헤드라인 상단  92px     86px      (셸 상단 패딩 40 vs 34 차이)
입력창 상단   220px    222px
버튼 하단     347px    349px
줄바꿈        normal   normal     둘 다 2줄
```

⚠ **FYM도 360px에서 헤드라인이 단어 중간에서 끊긴다**("있나/요").
PYM도 같다("어떤가/요"). `word-break: keep-all`을 넣으면 고쳐지지만
**넣지 않았다** — 지시가 "FYM 실제 스타일에 맞춰라"였고, 이건 FYM의 현재
동작이다. 고칠지는 두 앱을 함께 정할 문제라 판단을 남겨 둔다(4.6절).

**[5] 출처 표기 정책 변경 — 두 곳 → 한 곳**

```
전   구절 카드 + "이 앱에 대해"   근거: 구절만 보고 나가는 사용자에게도 보여야
후   "이 앱에 대해"만             근거: 카드 상자를 걷어낸 뒤 본문 아래 10.5px
                                  회색 한 줄이 구절의 여운을 끊는 마지막 요소가 됐다
```

⚠ **표기를 없앤 것이 아니다.** 성명표시권은 저작재산권과 달리 **만료되지 않으므로**
앱 어딘가에는 반드시 있어야 한다. 자리가 하나뿐이라는 건 그 하나가 **더
중요해졌다**는 뜻이다. `verses.test.js`가 세 가지를 고정한다.

```
1. "이 앱에 대해" 화면에 표기가 있다              ← 없어지면 요건 위반
2. 그 화면으로 가는 길이 있다                     ← 도달 못 하면 고지가 아니다
3. 구절 카드·위기 화면에는 없다                   ← 조용히 되돌아가지 않게
```

**[7] 라운드 언어는 요소마다 다르다 — 일괄 적용이 원인이었다.**
앞선 "pill을 걷어내라"는 지시는 **구절 카드 안의 pill 버튼**을 두고 한
말이었는데(카드 상자 위에 pill이 얹혀 면이 겹쳤다) 선택 칩까지 적용됐다.
칩은 pill(99), 입력·주 버튼·카드는 각진 3~4px. 구분을 코드 주석에 남겼다.

**[A] 카테고리 화면도 같은 헤더를 쓴다 — 뒤늦게 발견한 누락 (2026-08-19)**

입력 화면만 고치고 선택 모드는 빠져 있었다. "골라서 찾을래요"를 누르면
캡션·인사·glow가 통째로 사라져 **다른 화면으로 넘어간 것처럼** 보였다.

원인은 구조였다. FYM은 헤더를 `mode === "text" ? … : …` **바깥**에 두어
두 모드가 공유하는데, PYM은 헤더가 텍스트 모드 분기 **안**에 있었다.
헤더를 밖으로 빼고 `TextMode`·`SelectMode` 두 컴포넌트로 갈랐다.
React가 헤더를 같은 엘리먼트로 조정하므로 DOM이 유지되고 `.rise`도 다시
돌지 않는다 — 전환이 "바뀌는" 것이 아니라 "아래쪽만 갈리는" 것으로 보인다.
**헤더를 분기 안으로 다시 옮기지 말 것.**

선택 모드 요소도 FYM 값으로 맞췄다. 질문은 헤더와 **격이 다르다** —
헤더는 말을 거는 자리라 중앙·명조·25px이고, 질문은 목록 안내라 좌측·고딕·13px다.
앞서 SERIF 16.5px이던 것을 되돌렸다. 되돌아가기 링크도 칩 왼쪽 끝에 맞춰
좌측 정렬로 바꿨다(가운데 두면 칩 어디에도 걸리지 않는 자리에 뜬다).

```
             FYM      PYM(개정 후)
헤더 유지     3화면    3화면 (입력·대분류·세분류 전부)
질문         13px muted 좌측   13px muted 좌측
칩           radius 99, gap 8, 좌측   같음
되돌아가기    13px muted 좌측    같음
```

**[9] 원칙을 문서로 고정한다**

> **FYM에 대응물이 있는 UI 문구는 FYM 것을 쓴다.** 프로토타입으로 검증이 끝난
> 문구를 다시 쓸 이유가 없다. 새로 쓰는 것은 **PYM에만 있는 요소**에 한한다 —
> 공감·마무리 문구 485건, 구절·토글·폴백 헤더가 그것이다.

원래 이식 지시의 "FYM 문구를 그대로 베끼지 말고 PYM 톤으로 새로 써라"는
**감정별 문구 485건을 위한 것**이었는데 검증이 끝난 UI 문구에까지 적용됐다.

### 2.19 placeholder 개정과 "지쳐" 누락 — 화면 문구가 분류율을 정한다 (2026-08-19)

**placeholder는 사용자가 무엇을 적을지 정한다.** 분류기는 감정 키워드로만
동작하므로, 문구가 감정이 아닌 것을 가리키면 그 답은 어디에도 안 걸린다.
UX 취향이 아니라 분류 정확도 문제다.

**실측이 지적보다 정확했다 — 축이 달랐다.**

지적은 "사건형 vs 감정형"이었는데, 재 보니 **가장 나쁜 것은 사건형이 아니라
평가형**이었고 **축은 시제가 아니라 "무엇을 가리키는가"**였다.

```
평가형 답변  괜찮았어요 · 좋았어요 · 그냥 그랬어요 · 나쁘지 않았어요   1/6  ★ 최악
사건형 답변  회의가 길었어요 · 친구랑 밥 먹었어요 · 출근길이 막혔어요   3/8
과거형 감정  힘들었어요 · 불안했어요 · 외로웠어요 · 지쳤어요          5/6
현재형 감정  불안해요 · 외로워요 · 설레요 · 기뻐요                  5/6
```

평가형이 더 나쁜 이유는 분명하다 — 사건 문장은 "야근"·"발표"·"시험"처럼
명사가 우연히 사전에 있어 걸릴 여지라도 있지만, **평가어("괜찮다"·"그저
그렇다")는 감정 사전에 아예 없다.** 그리고 과거형 감정이 5/6로 잘 걸리므로
시제는 축이 아니다. "오늘 마음, 어땠어요?"의 문제는 과거형이 아니라
`어땠어요?`다.

6개 중 4개를 갈았다(사건형 2 · 평가형 유발 1 · 방향 없음 1 개작). 작성 기준
5항을 `taxonomy.yaml` `ui.placeholders` 주석에 남겼다.

**"지쳐" 누락 — themes.yaml 사랑하시/사랑하심을과 같은 유형**

`지쳤`·`지치`는 있는데 `지쳐`가 없어 "지쳐요"·"너무 지쳐"·"지쳐 있어요"가
어디에도 안 걸렸다. 한글이 음절 단위라 생기는 한 칸 누락이다.

원칙대로 오탐을 먼저 쟀다. **다만 여기엔 titles.json 같은 실측 코퍼스가
없다** — 사용자 입력 기록을 남기지 않기 때문이다(그게 이 앱의 설계다).
그래서 ① 키워드 충돌 분석 ② 위험 문장을 구성한 프로브 ③ 기존 키워드와의
대조, 세 가지로 대신했다.

```
① 충돌   "지쳐버"(burnout)와 겹치지만 점수 규칙(긴 키워드 우선)이 갈라 준다.
         실측 확인 — "지쳐버렸어요"는 지쳐버(3자)가 지쳐(2자)를 이겨 burnout 유지
② 오탐   normalize가 띄어쓰기를 접으므로 "말도 안 되지 쳐다보기도 싫어요"가
         "…되지쳐다…"가 되어 걸린다. 구성한 위험 문장 3/3 오탐
③ 대조   **새 위험이 아니다.** 기존 "지치"도 "쉽지 치우기도 싫어요"를 잡는다.
         세분류 키워드 893개 중 2음절이 236개(26%)이고 전부 같은 조건이다
```

**넣기로 판단했다.** 얻는 것("지쳐요"는 매우 흔한 표현)이 크고, 오탐은
이미 26%가 갖고 있는 조건에 합류하는 것이지 새로 만드는 위험이 아니다.
오탐이 나도 결과는 exhaustion.tired라 방향이 반대인 화면으로 가지 않는다.

⚠ 근본 원인은 **정규화가 어절 경계를 지우는 것**이다. 2음절 키워드는 전부
어절을 넘어 매칭될 수 있다. 고치려면 매칭 엔진에 경계 개념을 넣어야 하고,
그건 두 앱 공통 과제라 이번 범위에서 뺐다(4.7절).

### 2.20 유사 문구 게이트와 16건 재작성 (2026-08-19)

**완전 일치만 보던 검사가 한 글자 차이를 전부 통과시키고 있었다.**

```
0.97  "조용한 소리 하나만 두고 자요."(피로) / "조용한 소리 하나 두고 자요."(안정)
0.96  "오늘 하루도 견뎠어요."(짜증)       / "오늘 하루 견뎠어요."(슬픔)
```

`difflib.SequenceMatcher` 기반 검사를 넣고 **임계값 0.85**로 잡았다. 480건을
전수로 재고 구간별로 눈으로 확인해 정한 값이다.

```
0.90+   (7쌍)  전부 같은 문장. 조사 하나·동의어 하나 차이
0.85~90 (10쌍) 대부분 같다 — "삼키느라 애쓴 하루" / "참느라 애쓴 하루"
0.80~85 (32쌍) **갈린다.** "마음이 팽팽한 날이군요"(긴장) /
               "마음이 고요한 날이군요"(안정)처럼 반대 감정에 같은 틀을
               의도적으로 쓴 것이 섞여 있다
0.75~80 (42쌍) 대개 정상 변주
```

더 내리면 정상 변주까지 잡아 **문장을 어색하게 비트는 압력**이 생긴다 —
이 파일이 위기 인접 검사에서 이미 경계한 실패 모드다. 세분류 **안**은 검사하지
않는다(실측 0쌍 — 작성자가 그룹 안에서는 잘 갈랐고 충돌은 전부 그룹을 넘나든다).

**16건을 고쳐 18쌍을 전부 풀었다** (최소 커버 계산. 위기 문구는 대상 제외 —
안전 문구는 신중히 쓰인 것이라 상대 쪽을 고쳤다).

```
        0.85+   0.80+   0.75+   0.70+   최고
개정 전   18     50      95     177    0.97
개정 후    0     28      57     116    0.85
```

걸린 쌍만 푼 게 아니라 전체가 내려간 것은 **허브 문장**을 바꿨기 때문이다.
"여기까지 온 것…충분해요" 계열 8개가 18쌍 중 6쌍을 만들고 있었고, 단어를
바꾸는 대신 세분류마다 다른 축으로 옮겼다("소리 내지 않았다고 없던 일이
되진 않아요"/억눌림 · "참아서 없어지는 종류가 아니에요"/억울함 ·
"안 되는 날을 세는 건 그만해도 돼요"/진전 없음).

⚠ **개정 후 최고값이 0.8485다 — 임계까지 여유가 0.0015뿐이다.**

```
joy.delight     "오늘은 신나는 걸로 골라볼게요."
boredom.novelty "오늘은 낯선 걸로 골라볼게요."
```

의도된 병렬 구성으로 보여 손대지 않았다. **이 두 문장 근처를 건드리면 게이트가
걸린다.** 모르고 만나면 "왜 갑자기 실패하지"가 되므로 여기 적어 둔다.
그때 할 일은 임계값을 올리는 것이 아니라 둘 중 하나를 다른 축으로 옮기는 것이다.

문구 개수 검사도 **하한**으로 이름을 고쳤다(`MIN_MESSAGES = 10`).
전에는 "모든 세분류가 10건씩 채워졌다"여서 정원처럼 읽혔는데, 개수는
세분류마다 달라도 된다.

### 2.21 주석이 소스 검사를 오염시킨 세 번째 사건 — 이번엔 조용했다 (2026-08-19)

이 앱의 소스에는 설명 주석이 길고 그 주석이 **코드를 인용한다.** 소스를 원문
그대로 검사하면 실행되는 코드가 아니라 그것을 설명한 문장이 잡힌다.

```
1회차  위기 화면 검사가 "MediaToggle을 import하지 않는다" 주석을 위반으로   거짓 실패
2회차  토글 밑줄 검사가 "전에는 borderBottom 단축을 썼다" 주석을 위반으로   거짓 실패
3회차  폴백 헤더 검사가 개정 이력 주석("지금: 딱 맞는 건 아니지만…")을
       통과 근거로 삼았다                                              거짓 통과 ★
```

**3회차가 위험했다.** 1·2회차는 시끄럽게 실패해 바로 드러났지만 3회차는
조용했다 — 실제 렌더 문구를 `XXX`로 바꿔도 68개 테스트가 전부 통과했다.
검사가 있다고 믿는 채로 검사가 없는 상태였고, 실험으로 확인했다.

원인은 헬퍼를 **일부만** 썼기 때문이다. `crisisSrc`만 주석을 걷고 있었고
`verses.test.js`의 소스 4개와 `behavior.test.js`의 2곳은 원문을 읽었다.

```
고친 것  test/helpers.js 신설 — readSource()가 유일한 소스 읽기 경로다
         두 테스트 파일의 소스 읽기를 전부 그리로 돌렸다
감시     "소스를 읽는 테스트는 전부 readSource()를 지난다" 테스트를 추가했다.
         test/ 안에서 .jsx/.js를 readFileSync로 직접 읽으면 실패한다 —
         4회차가 나올 자리를 기계가 지킨다
```

### 2.22 떠 있는 "다시 적기" — 넣고, 빼고, 다시 넣었다 (2026-08-19)

하루에 세 번 손댄 자리다. **왜 한 번 뺐다가 되살렸는지가 이 절의 요점이다.**

```
1차 넣음  실측 360×640, 말씀 14건에서 하단 "다시 적어보기"가 1,573px —
          2.5화면을 내려야 닿았다. 그 사이 돌아갈 길이 화면에 없다
2차 뺌    "떠 있는 버튼이 이 앱의 결과 안 맞는다"고 판단
3차 되살림 ★ **뺀 판단이 틀렸다.** 문제는 떠 있는 것 자체가 아니었다
```

**틀린 이유** — 그때 관찰한 것은 "버튼이 화면에 안 붙고 문서와 함께 흘러간다"
였다. 그것을 "떠 있는 버튼이 안 어울린다"로 읽고 제거했는데, 실제 원인은
**`.rise` 안에 렌더해서 `position: fixed`가 동작하지 않은 것**이었다(4.8절).
즉 평가한 대상이 의도한 동작이 아니라 **고장 난 동작**이었다.

FYM 동작은 처음부터 이것이다 — 스크롤을 내리면 우하단에 떠서 따라온다.
같은 컴포넌트를 FYM 원본에서 다시 가져오고 **`.rise` 바깥**에 렌더했다.

**실측으로 확인했다 (4.8의 방법 그대로)**

```
스크롤    0 → opacity 0 · aria-hidden true · pointer-events none   (숨김)
         60 → 숨김 유지                                            (임계 100px)
        150 → opacity 1 · aria-hidden false · pointer auto         (노출)
        400 → 노출 유지
어느 위치에서도 뷰포트 top = 575px · containingBlockCause = null   → 진짜 fixed
FYM 같은 조건에서 575px · 이동 0 — **두 앱이 같아졌다**
```

**렌더 위치를 `Shell` 한 곳으로 고정했다.** 화면 컴포넌트는 전부 `.rise`
안이라, 호출부에 맡기면 같은 실수가 다시 난다. `behavior.test.js`는 "있는가"가
아니라 **"어디서 그리는가"**를 검사한다 — 있어도 자리가 틀리면 동작하지 않기
때문이다. 화면 컴포넌트 안으로 옮기는 고장을 주입해 실패를 확인했다.

⚠ **교훈** — 이식한 것이 이상하게 보이면 **"원래 이런 건가"보다 "제대로 붙었나"를
먼저 보라.** 이번 세션에 이 유형이 네 번 나왔다(pickVerse previousId ·
FloatingRestart import 누락 · 이번 렌더 위치 · 테스트 헬퍼 일부 적용).

### 2.24 구절 확장 — 절차와 판단 기준 (2026-08-19~)

세분류당 10 → 10~15로 넓히는 작업. 계열 단위로 나눠 진행한다.
개수 배분과 근거는 PLAN.md 3.2에 있고, **여기에는 절차와 사고 방식**을 적는다.

**후보를 내기 전에 반드시 하는 것 — 순서를 지킨다**

```
1. 제외 목록 대조   verses.yaml 말미 [선정에서 걸러낸 것] + 세분류 헤더 주석
                    + 개별 note의 [제외 기록]
                    ★ 이걸 빼먹어서 실제로 두 번 걸렸다 (빌 4:13 · 시 27:14).
                      승인 후에 발견하면 되돌리는 비용이 든다
2. 기존 수록 대조   같은 절·인접 절·같은 시편이 이미 쓰였는가
                    (scratchpad의 조회 도구가 "이미 있음"을 표시한다)
3. 주제 축 분포     **개수보다 편중을 먼저 본다** (아래)
4. 계열 안 겹침     같은 계열의 다른 세분류와 이미지가 겹치지 않는가
5. 절 단위 함정     앞뒤에 책망·조건절·화제 전환이 붙는가
6. ★ 첫 20자 낭독   **그 화면의 사용자가 처음 읽는 20자를 소리 내어 읽어 본다**
                    (아래 규칙 참조)
```

**★ 채택하면서 위험을 적게 되면 그 자리에서 판단을 끝낸다**

이번 확장의 가장 큰 실수였다. **note에 위험을 적고 진행한 구절이 넷이었고
넷 다 되돌려졌다.**

```
마 10:19  "과장되게 읽힐 수 있다"고 적고 채택   → 제외
요 11:6   "맥락 의존이 크다"고 적고 채택        → 제외
전 5:12   "판정처럼 읽힐 여지"라 적고 채택      → 제외
시 103:10 "편중이 문제가 되면 먼저 뺄 후보"     → 제외
시 128:2  같은 유형이었고 승인 단계에서 걸렸다
```

**note는 사람이 읽고 화면에는 본문이 뜬다.** 위험을 적는 것으로는 아무것도
막지 못한다. 적어야 할 만큼 걸리는 것이면 그 자리에서 빼는 것이 맞다.

같은 판단을 빠르게 하는 방법이 **첫 20자 낭독**이다. 위 넷은 전부 첫머리에서
드러난다 — "너희를 넘겨줄 때에"(면접 앞둔 사람) · "나사로가 병들었다
함을"(진전 없는 사람) · "노동자는 …잠을 달게 자거니와"(잠 못 드는 사람) ·
"우리의 죄를 따라"(짜증 난 사람). 절 전체의 뜻이 옳아도 **첫인상이 그 화면과
맞지 않으면 맞지 않는 것이다.**

**개수보다 주제 편중이 우선이다** — 이 확장의 핵심 기준이다.

```
anxiety.worry     trust 10/13   "맡기라"만 열 번. 요구 없는 축을 넣었다
anxiety.tension   courage 10/13 그중 넷이 "두려워 말라" 명령형
anxiety.restless  peace 9/10    "잠잠하라"는 상태 요구뿐, 근거가 없었다
```

편중이 있으면 개수를 채우는 것보다 **부족한 축을 채우는 것**이 먼저다.
같은 말을 열 번 반복하는 화면은 열다섯 번 반복해도 나아지지 않는다.

**후보를 보고 숫자를 고친다 — 반대가 아니다**

`anxiety.restless`를 13으로 잡았다가 **11에서 멈췄다.** 후보를 넓게 뒤졌는데
전부 걸렸다(시 62:5는 이미 있는 62:1과 거의 같은 문장, 시 40:1은 40:1-2가
이미 수록, 시 37:7은 후반부가 책망조, 눅 10:42는 "마리아는 이 좋은 편을
택하였으니"가 비교·판정을 들여온다). 억지로 채우지 않는 것이 기준이다.

**완료 — 250 → 299건 (감정 289 + 위기 10), 태그 연결 253 → 302**

> 확장 직후 303건이었다가 **재검토로 4건을 되돌려 299건이 됐다**
> (마 10:19 · 요 11:6 · 전 5:12 · 시 103:10 — 위 "위험을 적고 넘어간" 유형).

```
세분류                전 → 후   세분류                전 → 후
anxiety.worry        13 → 15   joy.grateful         10 → 15
anxiety.tension      13 → 14   joy.delight          10 → 13
anxiety.restless     10 → 11   joy.proud            10 → 11
sadness.sorrow       10 → 15   flutter.anticipation 10 → 11
sadness.lonely       13 → 15   flutter.thrill       10 → 11
sadness.loss         10 → 14   calm.ease            10 → 11
exhaustion.burnout   10 → 14   calm.stable          11 → 11
exhaustion.tired     10 → 12   boredom.novelty      10 → 12
exhaustion.listless  10 → 11   boredom.dull         10 → 10
anger.unfair         10 → 15   frustration.stuck    12 → 14
anger.irritation     10 → 11   frustration.blocked  11 → 12
anger.rage           10 → 12   frustration.suppressed 10 → 12
```

**목표에 못 미친 채로 멈춘 곳이 7개다. 그게 이 작업의 결과다.**

```
calm.stable  11 (목표 12)  후보가 이미 있는 문장과 겹쳤다 — 시 62:2
                           "요동치 아니하리로다"가 시 16:8과 같은 끝맺음이다
boredom.dull 10 (유지)     성경에 권태를 위로하는 본문이 사실상 없다
joy.proud    11 (목표 12)  시 128:2가 "복되고 형통하리로다"로 끝난다
restless     11 (목표 13)  후보 6건이 전부 중복·책망·판정에 걸렸다
listless     11 (목표 13)  후보가 마른 게 아니라 이미 채워져 있었다
loss         14 · blocked 13 · frus.stuck 14
```

**축 편중을 푸는 것이 개수보다 먼저였다** — 실제로 대부분의 화면에서
새 축이 0에서 시작했다.

```
anxiety.worry     trust 10/13     → love·요구 없는 trust를 넣었다
anxiety.tension   courage 10/13   → 그중 넷이 "두려워 말라" 명령형이었다
anxiety.restless  peace 9/10      → patience 0
sadness.loss      comfort·renewal 두 축뿐 → lament·presence·hope 각 0
exhaustion.burnout rest·renewal 두 축뿐  → lament 0
                  "쉬어도 된다"는 허가는 "내가 소진됐다"는 말이 먼저 있어야 한다
joy 3개           gratitude·joy_praise 두 축뿐 → creation 0
```

**제외 목록에 유형 두 개를 새로 세웠다** (verses.yaml 말미)

```
되돌아오는 조건  앞부분은 모범인데 끝에서 조건으로 돌아온다
                 사 30:18("여호와께서 기다리시나니" → "그를 기다리는 자는 복이")
                 사 49:23 · 시 37:34
                 ⚠ 앞부분에 감동해서 채택하기 쉽다. 끝까지 읽을 것
형통 약속        4차분 위험(번영신학)의 재발. note로 관리할 수 없다 —
                 화면에 뜨는 것은 본문이다. 시 128:2가 이 기준으로 빠졌다
```

**한 책·한 편 편중 — 다음 차수의 과제**

```
시편 171/303건 (56%) · 82편 사용 · 이사야 28건(9%) · 나머지 36책이 34%
3회 이상 쓴 편   시 119(9) · 103(7) · 23(5) · 145(5) · 27(4) · 34(4) · 42(4)
한 화면에 2회 이상  시 119→boredom.dull 4회 · 시 103→anger.irritation 3회
                    시 31·42→sadness.sorrow · 시 145→joy.grateful
                    시 98→flutter.thrill · 시 16→calm.ease · 시 119→novelty
```

시편 56%는 감정 언어가 시편에 몰려 있는 결과라 자체로 결함은 아니다. 다만
**한 화면 안에서 같은 편이 3~4회 나오는 곳**(시 119→boredom.dull, 시 103→
anger.irritation)은 "다른 구절 보기"를 눌렀을 때 같은 편이 연달아 나올 수 있다.
회전이 id 기반 순환이라 순서상 인접하기도 한다. **다음에 손볼 곳은 여기다.**

**크기 영향은 작다**

```
verses.json  60,614B → 72,123B    gzip 16,436 → 19,099B (+2,663B)
앱 번들 gzip  94,423B → 97,888B   (+3,465B, +3.7%)
```

**짧은 구절(≤45자)은 107 → 127건(44%)**으로 늘었지만 비율은 45% → 44%로
거의 같다. 긴 구절만 골라 채우지 않았다는 뜻이다.

### 2.23 배치 관찰 — 첫 cron 실행 (2026-08-19 09:59 UTC)

예약은 09:30이고 Actions 지연으로 29분 늦게 시작했다 — **30분쯤 늦는 것이 정상이다.**
로컬 재현치(2.17절)와 **정확히 일치했다.**

```
             로컬 재현        실제 배치
총 노출       454건           454건
  주제분      303 / 폴백 151   동일
20건 달성     19/24           19/24
폴백 과다      5건             5건 (화면까지 동일)
```

재현 방법(`retag_titles.py --screens`)이 옳다는 것이 여기서 확인됐다. 이제
**배치를 돌리지 않고도 사전·채널 변경의 효과를 미리 볼 수 있다.**

⚠ **일치했다는 것은 파이프라인이 재현 가능하다는 뜻이지 데이터가 옳다는 뜻이 아니다.**
그 454건이 실제로 무엇인지는 제목으로 확인해야 했고, 결과가 달랐다 — 2.25절.

### 2.25 원인 조사 — "감정 체계와 안 맞는 느낌"의 정체 (2026-08-19)

직전 3절이 진입점으로 지목한 조사다. **원인 후보 셋을 갈랐고 넷째가 나왔다.**
API 소모 0 — 배포된 `data/videos.json`(gh-pages, `2026-08-19T09:59:35Z` cron)과
현재 `themes.yaml`·`taxonomy.json`을 직접 읽었고, 분류는 `app/src/lib/classify.js`를
그대로 호출했다.

> ⚠ **조사 리포트(HTML)는 scratchpad에 있어 세션이 끝나면 사라진다.**
> 그래서 핵심 수치와 증거 목록을 여기 남긴다. 전부 재현 가능하다(끝의 [재현]).

**판정**

| | 판정 | 근거 |
|---|---|---|
| ① 분류 빗나감 | 일부 사실 (10%) | 입력 30건 중 3건, 그중 1건은 **방향이 반대** |
| ② 태깅 안 됨 | **사실 · 가장 크다** | 폴백 151건의 실체가 영상 **23종**뿐 |
| ③ 주제 매핑 | 틀리진 않았다 — **구분을 못 한다** | 세분류 5쌍이 20건 전부 동일한 목록 |
| ④ 기본 토글 | **사실 · 화면 4개** (새로 나옴) | 열었을 때 보이는 목록에 주제분 0건 |

**핵심 수치 — `폴백 33.3%`는 실제보다 낙관적인 지표다**

```
총 노출          454건 / 24화면
감정 신호 없음    196건 (43.2%)   = 폴백 151 + "감정을 말하지 않는 주제분" 45
서로 다른 영상    141종            24개 화면 전체가 이 141종으로 만들어져 있다
폴백의 실체        23종            폴백 151건 노출의 정체가 영상 23종이다
```

`큐티`·`매일 성경`·`강해`처럼 **형식만 말하고 감정은 말하지 않는 어휘**로만 태깅된
45건이 "주제분"에 섞여 있다. 그 45건이 화면 셋에 몰려 있어, 폴백 25%로
**괜찮아 보이던** 그 화면들이 실제로는 감정 신호 0%다.

```
화면                     노출  감정신호  신호없는주제분  폴백   감정신호없음
anger.irritation          12      0           0        12     100%
calm.stable               20      0          15         5     100%
boredom.dull              20      0          15         5     100%
boredom.novelty           20      0          15         5     100%
calm.ease                 13      1           0        12      92%
exhaustion.tired          14      2           0        12      86%
anger.unfair              17      5           0        12      71%
frustration.suppressed    18      6           0        12      67%
anxiety.restless          20     10           0        10      50%
anger.rage                20     10           0        10      50%
exhaustion.burnout        20     11           0         9      45%
anxiety.tension / frustration.stuck / exhaustion.listless
  / flutter.anticipation  20     12           0         8      40%
sadness.loss              20     13           0         7      35%
frustration.blocked       20     17           0         3      15%
sadness.sorrow            20     17           0         3      15%
sadness.lonely            20     19           0         1       5%
flutter.thrill            20     19           0         1       5%
anxiety.worry             20     20           0         0       0%
joy.proud / delight / grateful
                          20     20           0         0       0%
```

**[증거 A] 짜증·여유·피로 화면의 폴백 12건이 글자 하나까지 같다**

`anger.irritation` · `calm.ease` · `exhaustion.tired`가 **같은 12건**을 낸다.
감정과 무관한 정도가 아니라 **모집 공고·기념행사·홍보 영상**이 섞여 있다.

```
꿈의교회     2026/08/16(주일) 꿈의교회, 희망의 찬양대_삼천리 반도 금수강산
우리들교회   [주일찬양] 휘문채플 / 26.08.16 / 주님 뜻대로 살기로 했네 / 주의 은혜라 / …
CTS         [CTS 추천픽] 독립유공자 26인 집안, 목회자 사모가 된 자녀들 | 광복 81주년…
CGN         '인정'받아야만 괜찮은 사람이라고 느끼는 당신에게 | 믿어야 산다 EP.8
CBSJOY      담배 냄새 가득한 예배당, 누군가는 그 아이를 위해 기도했다 │새롭게 하소서
ANOINTING   2026_0806ㅣ어노인팅목요예배ㅣ인도_ 양민호  #성문교회         ← 112분
옹기장이     세상을 사는 지혜 | 옹기장이 40주년 카운트다운콘서트 2026     ← 홍보
극동방송     [경건생활 365] 8월 12일 - 진짜로 믿으십니까
광주극동     광주극동방송 어린이합창단 신입단원 모집 '2026전도대회…'      ★ 모집 공고
오륜교회     나의 가장 낮은 마음 + 예수 나의 첫사랑 되시네 + … 수요 오전예배 찬양
꿈의교회     2026/08/09(주일) 꿈의교회, 희망의 찬양대_거룩하시다
우리들교회   [주일찬양] 판교채플 / 26.08.16 / 나는 주를 섬기는 것에 / …
```

폴백 23종이 **동시에 떠 있는 화면 수**(내림차순) —
`15 · 14 · 13 · 13 · 12 · 11 · 11 · 10 · 7 · 6 · 5 · 5 · 5 · 4 · 4 · 4 · 4 · 2 · 2 · 1 · 1 · 1 · 1`

즉 상위 8종이 10~15개 화면에 걸쳐 있다. 폴백 정렬이 "최신순 + 채널 라운드로빈"이라
**모든 화면이 같은 후보를 같은 순서로 가져가기 때문**이다(2.14절에서 그렇게 정했다).

**[증거 B] 폴백 0%인 화면이 판별식이었다 — 답은 반반이다**

`anxiety.worry`(폴백 0%)는 **실제로 괜찮다.** 20건 중 15건 안팎이 걱정하는 사람에게
내밀 만하다("인생의 갈림길에서 두려워하는 당신에게" · "염려 대신 감사를" ·
"믿음이란 아주 작은 문제도 하나님께 맡기는 것"). 어긋난 2건은 원인이 서로 다르다.

```
[아침예배] 평안히 거주할 곳 (겔 38:1-13)      peace ← 평안. 곡과 마곡 침공 예언이다
인생의 지름길이 아닌 우회로는 견딜 수 없다면   trust ← 의지. "인생의 지"가 붙어 생겼다
```

`joy.grateful`(폴백 0%)은 **폴백이 한 건도 없는데 문제가 있다.** 20건이
`joy.proud`·`joy.delight`와 한 건도 다르지 않고, 그 안에 오탐이 들어 있다.

```
[아침예배] 불행을 기뻐하는 죄 (겔 25:1-17)              joy_praise ← 기뻐
[생명의 삶 큐티] 하나님 백성의 패망을 기뻐하는 죄         joy_praise ← 기뻐
감사드림교회 차영아 목사(TV강단 32회) - 순종에 하늘의…    gratitude ← 감사 (교회 이름)
```

→ **4.5절이 "2건이 비용을 정당화하는가"로 남겨 둔 그 오탐이 밝은 화면 4개에 나가고 있다.**

**[증거 C] 목록이 100% 동일한 화면 5쌍 — 원인이 둘로 갈린다**

```
joy.proud = joy.delight = joy.grateful   20건 전부   주제 집합이 아예 같다
                                                      (셋 다 gratitude + joy_praise)
boredom.dull = boredom.novelty           20건 전부   같음 (둘 다 bible_story + daily_word)
anxiety.restless = anger.rage            20건 전부   ★ 매핑은 다른데
                                                      (peace+quiet_worship / peace+self_control)
                                                      두 주제가 빈 풀이라 남은 게 같은 폴백뿐
```

뒤쪽이 특히 나쁘다. **불안 계열과 분노 계열이 같은 20건이 된다.**

**[증거 D] 분류 30건 — 감정어가 길이 싸움에서 진다**

정공법 24건 중 22건 적중. 위기 오탐 없음("짜증나 죽겠어" → 짜증). 빗나간 3건:

```
계속 걱정이 돼서 아무것도 손에 안 잡혀요   → exhaustion.listless (기대: 걱정)
    손에안잡혀 1005 > 손에안잡 1004 > 걱정 1002
아무도 내 얘길 들어주지 않는 것 같아 외로워요 → frustration.suppressed (기대: 외로움)
    들어주지않 1005 > 외로 1002
요즘 재미있는 게 하나도 없어요            → joy.delight  ★ 방향이 정반대
    "재미있"만 걸리고 뒤의 "하나도 없어요"를 아무도 보지 않는다
```

점수 규칙은 `(걸린 키워드 수 × 1000) + 키워드 길이 합`이다. "더 구체적으로 서술한
쪽이 상태에 가깝다"는 취지인데, **한국어는 감정어가 짧고 상황 묘사가 길다.**
취지와 반대로 작동하는 구간이 있고, **부정 표현 처리는 아예 없다.**

⚠ **이 원인만 구절까지 함께 틀린다.** 구절은 세분류에 직접 붙어 있어 폴백 개념이
없지만, 세분류 자체가 틀리면 그 화면의 구절이 나간다. 영상·구절 양쪽 대응이 필요하다.

**[증거 E] 태깅 오탐 4유형** (주제분으로 나간 영상 118종 전수 검사)

```
어절 경계   강남중앙침례교회 최병락 목사(TV강단 42회 복 있는 사람)
            renewal ← 회복.  회차 "42회" + "복 있는"이 붙어 회복이 됐다
            → 상실감 · 번아웃 화면
어절 경계   인생의 지름길이 아닌 우회로는 견딜 수 없다면
            trust ← 의지.  정규화가 띄어쓰기를 지우며 생겼다 (4.7절 그 문제다)
            → 걱정 화면
부정 문맥   [아침예배] 불행을 기뻐하는 죄 (겔 25:1-17)
            [생명의 삶 큐티] 하나님 백성의 패망을 기뻐하는 죄
            joy_praise ← 기뻐.  → 뿌듯함 · 즐거움 · 감사 · 두근거림 (밝은 화면 4개)
고유명사    감사드림교회 차영아 목사(TV강단 32회) - 순종에 하늘의 문이 열립니다
            gratitude ← 감사.  교회 **이름**에서 걸렸다. 내용은 순종 설교
표면 일치   최일관 목사 낮은담교회 새벽만나 "안식일의 주인" 누가복음 6:1-5
            rest ← 안식.  안식일 **규례**를 다루는 신학 설교다. 지친 사람의 쉼이 아니다
```

**[증거 F] 화면을 열면 주제분이 안 보인다 — 기본 토글이 가린다**

앞의 모든 수치는 "화면에 실린 목록"이고, 사용자가 **처음 보는 것**은 기본 토글이
걸러낸 뒤다. 기본 형식으로 보이는 목록에 주제분이 **0건**인 화면이 넷이다.

```
화면                 기본      노출  기본토글로 보임  그중 주제분
anger.irritation     worship    12        12            0   폴백만 보인다
anger.unfair         worship    17        12            0   주제분 5건이 전부 sermon
exhaustion.tired     worship    14        12            0   "안식일의 주인" 2건이 sermon
calm.stable          worship    20         5            0   5건만 보이고 전부 폴백
```

거기에 형식 오판이 겹친다. **제목에 "찬양"이 명시된 콘티 9종이 sermon으로 판정됐다.**

```
오 신실하신 주 + 주의 약속하신 말씀 위에서 + … | 오륜교회 … 주일 4부예배 찬양 프레전스
   sermon 걸림 ('말씀')  ·  worship 걸림 ('찬양')  → 동점 → 제목 단계 확정 실패
   → 장절 없음 → 채널 content_type(오륜=sermon) → sermon
```

곡명 안의 `말씀`(주의 약속하신 **말씀** 위에서 · **말씀**하소서)이나 예배 이름
`금요기도`가 sermon 어휘로 함께 걸려 동점이 된다. 2.14절에서 "제목이 채널보다 앞"으로
순서를 뒤집었지만 **양쪽이 걸리면 그 개정이 무력화된다** — 판별 규칙은
"**한쪽만** 걸릴 때 확정한다"이기 때문이다.

**[재현] 전부 API 0**

```bash
git fetch origin gh-pages:refs/remotes/origin/gh-pages
git show origin/gh-pages:data/videos.json > <scratch>/prod.json
   # subcategories[].videos[].source 가 theme/fallback을 가른다. 화면별 목록이 그대로 있다
python scripts/retag_titles.py --screens     # 화면 재조립 — 배포본과 일치함이 확인됐다
python scripts/retag_titles.py --songs       # 곡명 단위 적중률 (408종 중 46종)
python scripts/retag_titles.py --probe 기뻐   # 오탐 실측
   # 분류는 node로 app/src/lib/classify.js를 직접 부른다 (taxonomy.json 필요,
   #  Windows에서는 import 경로를 file:/// URL로 줘야 한다)
```

### 2.26 [1] 폴백 품질 필터 — 어휘 목록 하나로는 안 됐다 (2026-08-20)

3절 수정 순서의 첫 항목. **감정에 안 맞는 것과 콘텐츠가 아닌 것은 다르다**는
구분을 폴백 층에 넣었다. 폴백은 앞을 감수하기로 한 층이지 뒤까지 감수하는
층이 아니다(2.14절).

**실측이 먼저 막았다 — 나이브한 키워드 목록은 찬양을 지운다.**

'주년'·'콘서트'를 통째로 걸면 이것들이 함께 사라진다.

```
주 여호와는 나의 힘 & 찬송할 수 있을 때 | 옹기장이 40주년 카운트다운 콘서트 2026
할렐루야 존귀하신 주 | 옹기장이 40주년 카운트다운콘서트 2026
주의 나라와 영광 이곳에 | 옹기장이 40주년 카운트다운콘서트 2026
성령의 리듬 | 옹기장이 40주년 카운트다운콘서트 2026
[고난주간 3일] 나를 기념하라, 우리를 위해 흘리신 언약의 피 — 최후의 만찬
한국중앙교회 임석순 목사 (CBS 주일강단 540회) - 영적 광복을 위한 오늘의 핍박
```

전부 실제 콘텐츠다. 첫 줄은 **[2]에서 `strength` 예시로 지목된 바로 그 곡**이다.
반대로 `극동방송 70주년 기념 호남권 전도대회 | FEBC … 2부 LIVE`에서는 같은
어휘가 영상 자체를 가리킨다.

> **가르는 축은 어휘의 유무가 아니라 제목 머리가 무엇인가다.**
> 세로줄 뒤는 "어디서 찍었는가"이고 머리는 "무엇인가"다.

**규칙 3층** (`lib/selection.py`)

```
(a) PROMO_ANYWHERE  위치 무관   모집·신입단원·공고·접수·채용·지원자격·선발
                                하이라이트·예고편·티저·스케치·홍보영상
                                돌파기념·구독자
(b) PROMO_HEAD      머리에만    주년·축제·전도대회·총회·음악회·발대식
                                페스티벌·체육대회·바자회·헌당·임직
(c) PROMO_SERIES    시리즈 지정 "광복 81주년"   ← 사람이 지정한다
```

(c)가 왜 필요한가 — CTS 광복 시리즈의 머리는 `독립유공자 26인 집안, 목회자
사모가 된 자녀들`이라 (b)에 안 걸린다. 그렇다고 `광복`을 통째로 넣으면 CBS
설교를 잃는다. **머리만으로 못 가르는 구간이 남았고**, 그 경계는
**[2]의 곡명 사전이 생기면 자동으로 갈린다** — 머리가 곡명인지 판정할 수 있게
되기 때문이다. 그때 (c)를 다시 볼 것.

**측정 — 화면은 얇아지지 않았다**

```
폴백 후보   788 → 768건 (20건 제외)    정상 콘텐츠 손실 0건
화면 노출   454 → 454건 · 폴백 151 → 151 · 성립불가 0개
실제 교체   4종 / 노출 23건분
```

788건 풀에서 20건을 빼도 다음 후보가 자리를 메운다. 교체 내역:

```
빠짐                                          대신 들어옴
13화면 [CTS] 독립유공자 26인 집안…|광복 81주년  [CTS] 실패 안에서 깨달은 하나님의 방법 |
                                               이정민 아나운서 | 원더풀우먼
 7화면 광주극동 어린이합창단 신입단원 모집       광주극동방송 전도부흥회 | 지선 &
                                               아이빅미니스트리 찬양 FULL
 2화면 [생방송] 2026 나라사랑축제 (익산)         [성경의 무림고수를 찾아서 특별 세미나_2편]
                                               로마서 1000독, 성경 100독…
 1화면 극동방송 70주년 기념 호남권 전도대회 LIVE  십계명 제 8계명 #십계명 #말씀
```

**"8건 미만이면 그대로 둔다"는 코드가 필요 없었다.** `select_fallback_videos`가
풀에서 `need`만큼 뽑고 모자라면 적게 돌려주므로 **되메우는 경로 자체가 없다.**
`theme_too_few`(critical, 8건 미만)가 이미 그 자리에 있다.
⚠ 뺀 것을 다시 넣는 코드를 만들지 말 것 — 그 순간 이 정책이 무력해진다.

**주제분에는 걸지 않는다.** 주제어가 걸린 영상은 그 나름의 근거가 있고 두 층은
판단 기준이 다르다. 실측에서도 제외 20건은 전부 미태깅이었고, 유일하게 걸릴
뻔한 태깅분(`시작하는 순간 떼창 유발… | 옹기장이 40주년`)은 머리가 깨끗해
(b)에 안 걸린다.

**검증** — `spread_test.py` 10번. 실측 제목 13종을 고정했다(제외 9 · 유지 4).
고장 주입도 넣었다 — `주년`을 (b)에서 (a)로 옮기면 찬양 콘티가 걸리는 것을
확인하고 되돌린다. **사전을 넓히려는 시도가 여기서 먼저 깨진다.**

드라이런 합성 제목 4종 중 둘(`성탄 축하 행사 스케치`·`청년부 수련회 하이라이트`)이
이 필터에 걸리도록 두었다. 드라이런이 필터의 **양쪽 분기를 모두 태운다** —
넷이 전부 통과하면 필터가 꺼져 있어도 드라이런이 아무 말을 하지 않는다.

**리포트** — `build_report.json`의 `tagging.fallback_excluded`(건수)와
`fallback_excluded_sample`(이유+제목 15건). 매일 몇 건이 걸리는지 보이지 않으면
사전이 과하게 잡기 시작해도 알 수 없다.

### 2.27 ★ 위기 사전에 `자살`이 없었다 — 긴급 수정 (2026-08-20)

실사용 제보로 드러났다. **"자살하고 싶어" · "자살" · "죽어"가 전부 미분류였다.**
가장 도움이 필요한 사람이 아무것도 못 받는 상태였다.

**원인은 순서가 아니라 사전이었다.** `classify()`는 정규화 직후 무조건 위기부터
본다(감정 점수 계산에 닿기 전에 반환한다). 그 설계는 정상 작동하고 있었다.
문제는 **55종 어디에도 `자살`이 없었다**는 것이다.

```
놓치던 8종 (실측)
  자살 · 자살하고 싶어 · 자살할까 · 자살 생각이 나요
  죽어 · 죽어버릴까 · 목숨을 끊고 싶어요 · 극단적인 생각이 들어요
```

`죽어버리`는 있는데 `죽어버릴까`가 안 걸린 것은 **한글 음절 문제**다 —
버리+ㄹ까 = 버릴까. `사랑하시`/`사랑하심을`(2.17절), `지쳤`/`지쳐`(2.19절)와
같은 유형이 위기 경로에서 반복됐다.

**★ 왜 여기까지 왔나 — 회귀 테스트가 사전을 사전으로 검사하고 있었다.**

```
기존 검사   crisis_keywords 전건이 crisis로 분류되는가   → 항상 통과한다
            사전에 없는 표현은 구조적으로 검사 대상이 되지 않는다
```

**"사전에 있는 것이 걸리는가"만 봤고 "있어야 할 것이 사전에 있는가"는 아무도
보지 않았다.** 이것이 이번 사고의 진짜 원인이다. 게이트가 초록인 채로 구멍이
운영에 나갔다.

**부분 문자열로 쓸 수 없는 말이 있다 — 실측이 갈랐다**

taxonomy.yaml의 [새 키워드를 넣기 전 검사] 절차를 그대로 돌렸다(위기 21종 ·
비위기 23종).

```
자살    → "자살골 넣었어요" "자살률 기사" "자살 예방 강의"
죽어    → "할머니가 죽어서 슬퍼요" "강아지가 죽어서" "죽어라 일했어요"
죽여    → "저 인간 죽여버리고 싶다"  (타인 지향 = anger.rage)
극단적  → "극단적인 디자인" "극단적인 성격"
```

★ **사별이 결정적이었다.** `죽어`를 부분 문자열로 넣으면 "할머니가 죽어서
슬퍼요"가 위기로 간다. 그 사람은 `sadness.loss`에 공들여 큐레이션한 상실 구절을
받아야 하는데 못 받게 된다. **이건 "상담 안내를 불필요하게 보여주는" 감수 가능한
오탐이 아니다** — 받아야 할 것을 빼앗는다.

**그래서 층을 나눴다 — `crisis_exact` 신설**

```
crisis_keywords  부분 문자열   70종 (15종 추가)
crisis_exact     정확 일치      죽어 · 죽여 · 자살 · 죽음
```

입력 전체가 그 말 하나뿐이면 위기다. 감정 입력 상자에 "죽어" 두 글자만 적는 것은
사별 서술도 관용구도 아니다. 비교 전에 자모(ㅠㅋㅎ)를 걷어내 "죽어ㅠㅠ"도 잡는다.

⚠ **`normalize()`는 건드리지 않았다.** `scripts/lib/normalize.py`와 문자 단위로
같아야 하고 배치의 blocklist 매칭이 그 동일성에 걸려 있다. 자모 제거는
`classify.js`의 `isCrisisAlone()` 안에만 있다.

**추가한 15종** (전부 오탐 0 실측)

```
자살하 · 자살할 · 자살충동 · 자살생각 · 자살밖에
죽어버릴 · 죽어버려 · 죽어야겠 · 죽어야하 · 죽어야지 · 죽여줘
목숨을끊 · 목숨끊 · 극단적인생각 · 극단적인선택을
```

**결과 — 위기 21/21 포착 · 오탐 0/23**

```
할머니가 죽어서 너무 슬퍼요   → sadness.sorrow   (위기 아님. 정상)
강아지가 죽어서 눈물이 나요   → sadness.sorrow
저 인간 죽여버리고 싶다       → anger.rage
짜증나 죽겠어                 → anger.irritation
죽어 · 죽어ㅠㅠ · 죽 어 · 죽어... → 전부 위기
```

**회귀 테스트를 다시 짰다 — 사전을 참조하지 않는다**

`app/test/behavior.test.js`에 **손으로 적은** 위기 표현 29종 / 비위기 19종을
고정했다. 사전이 줄거나 활용형이 어긋나면 여기서 깨진다.

```
고장 A  죽어를 crisis_keywords로 옮긴다   → 생성기가 거부 (종료 코드 1)
고장 B  자살 계열 5종을 사전에서 뺀다      → 커버리지 테스트가 놓친 6건을 이름으로 보고
```

생성기(`gen_taxonomy_json.py`)에도 게이트를 넣었다 — `crisis_exact`가 비어 있거나
`crisis_keywords`와 겹치면 빌드가 실패한다. **겹치는 순간 사별이 위기로 빨려
들어가기 때문이다.**

⛔ **`crisis_exact`의 말을 `crisis_keywords`로 옮기지 말 것.** 두 층을 합치면
이번에 막은 것이 그대로 되돌아온다.

### 2.28 [2] 태깅 원천 재점검 — 어휘 문제와 물량 문제는 다르다 (2026-08-20)

두 가지를 실측했다. **결론이 정반대로 갈렸다.**

**전체 키워드 감사 — 95종 중 53종(56%)이 코퍼스에 0건**

실측 코퍼스(`dist/titles.json` 1,067건)에 `themes.yaml`의 모든 `title_keywords`를
걸어 봤다. 키워드가 **100% 죽은 주제가 정확히 둘**이었다.

```
★ quiet_worship  6/6 죽음   잔잔한 찬양 · 조용한 찬양 · 묵상 찬양
                            피아노 찬양 · CCM 피아노 · 새벽 찬양
★ self_control   4/4 죽음   노하기를 · 마음을 다스리 · 화가 날 때 · 분을 품
```

이 둘이 폴백 과다 화면의 원인이었다(2.17절이 지목한 그 두 주제다).
**죽은 키워드는 두 부류이고 조치가 다르다.**

```
[A] 검색어 유물   사용자가 검색창에 칠 말. 제작자는 제목에 쓰지 않는다
                 잔잔한 찬양 · 슬플 때 · 억울할 때 · 화가 날 때 · 두려움을 이기
                 기다리는 시간 · 오늘의 말씀 · 아침 묵상 · 성경 이야기 · 새로운 시작
                 → 형태가 신호다: "~할 때" · "~하는 시간" · "~ 모음"
                 → 어휘를 갈면 고쳐진다
[B] 물량 부족     정상 어휘인데 이 채널 구성에 그런 콘텐츠가 없다
                 노하기를 · 분을 품 · 탄식 · 임마누엘 · 신원 · 독수리 · 범사에
                 → 어휘를 갈아도 안 잡힌다. 채널 발굴 대상이다
```

⚠ **조치가 필요한 수준은 ★ 두 주제뿐이다.** 나머지는 살아 있는 키워드가 물량을
만든다 — `daily_word`는 6종 중 4종이 죽었지만 `큐티`·`매일 성경`이 163건을 만든다.

---

**(a) `quiet_worship`은 검색어 유물이 맞았다 — 고쳤다**

`잔잔`이라는 두 글자가 코퍼스 1,067건에 **0회**다. FYM은 그 말로 검색해 영상을
찾았지만 PYM은 채널이 자기 관행대로 붙인 제목을 받는다. 실제 어휘는 이것이었다.

```
찬양대        6건  "꿈의교회, 희망의 찬양대_거룩하시다"
Instrumental  4건  "8-1. 내 삶의 이유라 | Anointing Instrumental Series Vol.8"
연주곡        1건  "어노인팅 연주곡 시리즈 Vol.8 - 소망 (Hope) 전곡"
CCM PIANO     1건  "[CCM PIANO] 훈계로 다스려주소서 피아노ver."
피아노ver     1건  (같은 영상. 표기 관행이라 따로 둔다)
                   → 합집합 12건, 전부 오탐 0 (--probe로 건별 확인)
```

**12건 태깅으로 5개 세분류가 살아났다.**

```
                  개정 전 → 개정 후
전체 태깅          279 → 289
폴백              151 (33.3%) → 121 (25.5%)
20건 달성          19/24 → 22/24
폴백 과다           5개 → 2개
최소 노출          12건 → 17건
anger.irritation  0/12 → 12/20   ·  calm.ease   1/13 → 13/20
exhaustion.tired  2/14 → 14/20   ·  calm.stable 15/20 → 20/20
```

⛔ **`워십`을 넣지 않았다.** 넣으면 30건이 들어와 폴백이 22.3%까지 내려가지만,
그 30건은 **하이프레이즈·블레싱워십·램넌트워십** 같은 고조된 예배 찬양 콘티다.
intent가 "조용한 찬양·연주"인데 일반 찬양까지 받으면 **`joy_praise`와 구분이
사라지고 [5] 주제 집합 분리와 정면으로 충돌한다.** `tagging_test.py`가 이 셋을
비태깅으로 고정하고, `워십`을 넣으면 그 자리에서 깨진다(고장 주입으로 확인).

⚠ **어휘로는 여기까지다 — 겹침은 안 풀렸다.**

```
공급 채널이 2개뿐이다   꿈의교회 찬양대 6 · ANOINTING Instrumental 5 (+옹기장이 1)
그래서 quiet_worship을 쓰는 5개 화면의 겹침이 82~90%로 남는다
   짜증 ↔ 여유 90%  ·  짜증 ↔ 피로 82%  ·  여유 ↔ 피로 82%
   12건 중 8건이 5개 화면 전부에 나간다
```

**[4] 폴백 오프셋과 [5] 주제 집합 분리로도 이건 안 풀린다** — 나눌 원본이 12건뿐이기
때문이다. **채널 발굴(3절 10번)이 필요하다는 뜻이고, 그것이 이 항목의 결론이다.**

---

**`self_control`은 어휘가 아니라 물량이었다**

후보 어휘 13종을 전부 쟀다.

```
분노 0 · 온유 0 · 원망 0 · 감정 0 · 견디 0 · 절제 1(이미 태깅) · 인내 5(이미 태깅)
다스리 4  ← ★ 전부 방향이 반대다
    "하나님이 다스리십니다" 스가랴 6:1-8   ·  "오셔서 다스리소서"
    "살아계시며 다스리시네"
    하나님의 통치이지 내 마음을 다스리는 것이 아니다.
    taxonomy.yaml [방향이 반대인가] 검사에 걸리는 바로 그 유형이다.
```

**사전으로 못 고친다.** 2.17절 결론이 `self_control`에 대해서는 옳았다.
3절 10번(채널 발굴)에 무엇을 찾아야 하는지 적어 뒀다.

---

**(b) 곡명 사전 — 하지 않기로 했다. 근거를 남긴다**

> ⛔ **나중에 다시 후보로 올라올 아이디어다.** 그때 이 절을 먼저 읽을 것.

전제는 "설교 제목 표현은 무한하지만 곡명은 유한하고 반복된다"였다.
**유한한 건 맞는데 반복되지 않는다.**

```
콘티에서 갈라낸 곡명 408종 (연 480회 출현)
그중 360종(88%)이 **단 1개 영상에만** 등장한다
   2개 영상 34종 · 3개 8종 · 4개 4종 · 5개 1종 · 6개 1종
```

사전 한 줄이 평균 1.18개 영상만 커버한다. 커버리지를 재면 이렇다.

```
상위 N곡   미태깅 찬양 포착   찬양 미태깅 대비   전체 1,067 대비
   30        23건            11%            2.2%
  100        34건            17%            3.2%
  300       106건            51%            9.9%
  408       204건            99%           19.1%
```

**상위 30곡을 손으로 매핑해도 2.2%이고, 10%에 닿으려면 300곡을 넣어야 한다.**
그러면 `themes.yaml`이 큐레이션 문서가 아니라 곡목록이 된다 — 2.17절이
"곡 하나만 잡는 문자열은 넣지 않는다"고 경계한 바로 그 상태다. **그 판단이 옳았다.**

⚠ **이 측정의 한계 — 2주 스냅샷이다.** 같은 곡이 몇 달 뒤 다시 불리는 것은
여기서 보이지 않으므로 **장기 반복률이 과소평가돼 있다.** 다만 그것을 확인하려면
`--dump-titles`가 몇 달치 쌓여야 한다.

> **재검토 조건: 제목 덤프가 몇 달치 누적된 뒤에 같은 측정을 다시 한다.**
> 그전에는 다시 열지 않는다.

### 2.29 [3] 태깅 오탐 4유형 — 전역 규칙 하나로는 하나밖에 못 잡는다 (2026-08-20)

2.25절 [증거 E]가 찾아낸 네 유형을 전부 막았다. **넷의 메커니즘이 서로 다르다는
것이 이 작업의 결론이다** — 하나의 규칙으로 묶으려 하면 셋을 놓친다.

```
어절 경계   "인생의 지름길" → 의지      SHORT_KEYWORD_SYLLABLES = 2
부정 문맥   "불행을 기뻐하는 죄"        NEGATIVE_CONTEXT + BRIGHT_THEMES
고유명사    "감사드림교회"              ORG_SUFFIXES
표면 일치   "안식일의 주인" → rest      SURFACE_EXCLUSIONS (사람이 지정)
채널명 접두어 "[쉼과회복] 주일예배" → rest  strip_brand_prefix   ← 2026-08-24 추가 (2.47절)
```

**★ 다섯 번째 유형은 2026-08-24에 3차 발굴이 데려왔다 — `사전이 채널명을 콘텐츠로 착각한다`.**
앞의 넷은 전부 **제목 안의 말**이 문제였다. 이것은 제목 안의 말이 아니라
**채널이 자기 이름을 제목마다 붙인 것**이라, 제목만 봐서는 오탐인지 알 수 없다.
채널명과 대조해야 갈린다 — 넷과 또 메커니즘이 다르다.
⚠ 대괄호를 전부 떼는 안은 실측에서 태깅 118건을 잃어 버렸다(2.47절 ③).

전역 부정 규칙은 **4유형 중 1개(부정 문맥)만** 잡는다. 나머지 셋은 부정 어휘를
갖고 있지 않다.

**★ 어절 경계 — 손실 실측이 설계를 정했다**

전체 적용하면 무엇을 잃는지부터 쟀다. 태깅 근거 330건 중 **어절 경계를 넘어야만
걸리는 것이 25건**이었고, 그 25건의 성격이 갈렸다.

```
규칙                    상실   내용
S 전체 적용             25건   ★ 19건이 공백 포함 키워드의 **정탐**
                               그 사랑 · 주의 사랑 · 주 사랑 · 새 힘 · 다시 일어
                               사랑한다 말하 · CCM PIANO
                               키워드 자체에 공백이 있으니 제목의 공백을 넘는 것이 옳다
N 공백 없는 키워드 전체   6건   2건이 `함께하시` 정탐
                               "주 나와 함께 하시니" — 한국어 띄어쓰기 관행 차이다
L2 2음절 이하만          4건   ★ **전부 오탐. 정탐 손실 0건**
```

```
L2가 잡는 4건 (전부 오탐)
  의지 ← "하나님의 지독한 사랑"      의지 ← "인생의 지름길"
  의지 ← "하나님의 지혜"             회복 ← "TV강단 42회 복 있는 사람"
```

⚠ **조사(2.25절)에서는 `의지` 오탐을 1건으로 봤는데 실은 3건이었다.**
노출된 화면만 봤기 때문이다. 코퍼스 전체를 재야 규모가 보인다.

⛔ `SHORT_KEYWORD_SYLLABLES`를 3 이상으로 올리지 말 것. 오늘 데이터에서는
L2와 L3이 같지만(3음절 경계 초과 매칭이 0건), 올리면 `함께하시` 계열이 걸릴
자리가 생긴다. `tagging_test.py`가 0과 99 양쪽으로 고장을 주입해 확인한다.

**★ 이것은 배치 전용이라 앱과 무관하게 고쳤다 — 4.7절을 갈랐다**

HANDOFF 4.7은 어절 경계 문제를 "두 앱 공통 과제"로 묶어 뒀다. **그 묶음이 틀렸다.**

```
제목 태깅 (lib/tagging.py)     배치 전용. normalize.parity 제약 없음 → 오늘 고쳤다
입력 분류 (app/classify.js)    앱·배치 두 normalize가 문자 단위로 같아야 한다
                               → [7]에서 별도로 판단한다. 비용이 완전히 다르다
```

`normalize()` 자체는 건드리지 않았다. 경계 판정은 `_normalized_with_gaps()`가
**공백 위치만 따로 들고 다니는** 방식이라 정규화 결과에 영향이 없다.

**부정 문맥 — 4.5절 대안 그대로**

주제마다 `exclude_keywords`를 다는 대신 전역 규칙 하나를 뒀다.

```
NEGATIVE_CONTEXT  죄·심판·멸망·패망·저주·진노·재앙·불행·형벌·징계
BRIGHT_THEMES     joy_praise · gratitude  ← 이 둘만 막는다
실측              2건 차단 · 밝은 주제 태깅 41건 중 2건만 막힌다 (정탐 손실 0)
```

⛔ `hope`·`patience`를 BRIGHT_THEMES에 넣지 말 것. "고난을 인내하고 죄를 이기는
부활 소망"처럼 **부정 어휘와 함께 오는 것이 정상**인 주제다. 넣으면 정상 콘텐츠를
잃는다(고장 주입으로 확인).

**표면 일치 — 기계가 못 가른다. 사람이 지정한다**

`SURFACE_EXCLUSIONS = (("안식일", "rest"),)`. "안식일의 주인"(눅 6:1-5)은 안식일
**규례**를 다루는 신학 설교다. 글자는 `안식`이 맞지만 지친 사람이 찾는 쉼이 아니다.
폴백 시리즈 지정(`lib/selection.PROMO_SERIES`)과 같은 방식이다 — **못 가르는
구간이 있다는 것을 인정하고 사람이 적는다.**

> 참고: `rest`의 `안식`이 코퍼스에서 잡는 것이 **2건뿐이고 둘 다 안식일**이었다.
> 즉 이 키워드는 현재 100% 오탐이었다.

**결과 — 오탐 9건 제거, 정탐 손실 0건**

```
태깅        289 → 282건
폴백        25.5% → 27.2%      ← 올라간 것이 정상이다
20건 달성    22/24 유지 · 성립불가 0
```

⚠ **폴백 비율이 올라간 것을 되돌리려 하지 말 것.** 오탐을 남기면 폴백 지표는
좋아지지만 그 영상은 밝은 화면에 계속 나간다. **지표를 속이는 쪽이 나쁘다.**
`exhaustion.burnout`이 11/20 → 8/20으로 얇아진 것도 `회복` 오탐이 빠진 결과다.


### 2.30 ★[재측정] — [1][2][3] 뒤에 다시 쟀다 (2026-08-20)

3절 순서표가 지정한 재측정 지점이다. 조사(2.25절)와 **같은 지표**로 다시 쟀다.

```
지표                          조사(2.25)   재측정      변화
총 노출                          454        475      ↑21
  주제분                         303        346      ↑43
  폴백                           151        129      ↓22
★ 감정 신호 없음                  196        171      ↓25   43.2% → 36.0%
서로 다른 영상 종수                141        144       ↑3
폴백 영상 종수                     23         23        -
폴백 0%인 화면                      4          6       ↑2
폴백 과다(>50%)                    5          3       ↓2
기본 토글로 주제분 0건인 화면        4          1       ↓3
```

**⚠ 목록이 100% 동일한 화면은 5쌍 그대로다 — 구성만 바뀌었다.**

```
조사      anxiety.restless=anger.rage · joy 3쌍 · boredom 1쌍
재측정    anger.irritation=exhaustion.tired · joy 3쌍 · boredom 1쌍
             ↑ 해소                              ↑ ★ 새로 생겼다
```

**[2]의 quiet_worship 개정이 새 동일 쌍을 만들었다.** 매핑은 다른데 실효 공급이
같아졌기 때문이다.

```
anger.irritation   매핑 self_control + quiet_worship   실제공급 {quiet_worship: 12}
exhaustion.tired   매핑 rest         + quiet_worship   실제공급 {quiet_worship: 12}
                        ↑ 0건               ↑ [3]에서 안식 오탐이 빠져 0건
```

> **[5]는 "같은 주제 집합"이 아니라 "빈 주제를 뺀 실효 집합"을 봐야 한다.**
> 이것이 재측정의 가장 중요한 결과다.

**[4][5][6][7] 필요성 재판정 (사용자 결정 2026-08-20)**

```
[5] 주제 집합 분리   ★ 필요성 상승 → 다음 작업
    동일 5쌍이 전부 [5] 영역이 됐다. 단, 집합을 나눠서 풀리는 쌍과
    공급 부족이라 안 풀리는 쌍을 **먼저 갈라야** 한다
[6] 동점 처리       필요성 급감 → 뒤로 미룬다
    토글 주제분 0건 화면이 4→1이고, 남은 anger.unfair는 **형식 오판이 아니다**.
    주제분 5건이 전부 진짜 설교(큐티·새벽만나)인데 기본값이 worship이라
    안 보이는 것이다 — media_defaults 문제다. [5] 작업 중에 같이 본다
[4] 폴백 오프셋     보류 → [5] 이후 재판단
    동일 5쌍 중 오프셋으로 풀리는 것이 **0쌍**이다. 전부 주제분이 같아 생겼다
[7] 분류           긍정 8종 반영 완료(2.31절). 부정 표현 처리만 남았다
```

> 정정 — 조사 때 "찬양 콘티 sermon 오판 9종"은 **노출된 141종 기준**이었다.
> 코퍼스 1,067건 전체로 세면 19종이다. 늘어난 것이 아니라 분모가 다르다.

### 2.31 ★ "기분이 좋아"가 미분류였다 — 긴급 수정 (2026-08-20)

실사용 제보. **위기 사전 사고(2.27절)와 같은 유형이 긍정 계열에서 반복됐다.**

```
기분 좋아    → joy.delight   키워드 `기분좋`
기분좋다     → joy.delight
기분이 좋아  → 미분류        ★ 조사 '이'가 끼어 `기분좋`이 깨진다
```

`죽어버리`가 있는데 `죽어버릴까`를 놓친 것과 정확히 같다. 한글 음절 문제다.

**긍정 계열이 실제로 얇았다**

```
개정 전   긍정 3계열(joy·flutter·calm) 세분류당 31.7  ·  부정 5계열 39.5   (-20%)
          세분류 키워드 893개 중 `좋` 포함이 9개뿐
개정 후   긍정 32.9
```

**커버리지 26건 — 6건 미분류였고 5건을 고쳤다**

```
✗ 기분이 좋아 · 기분이 좋아요 · 좋다 · 너무 좋아 · 오늘 하루 좋았어요   → 고쳤다
✗ 괜찮아요   → 고치지 않았다. **평가형**이라 감정을 직접 말한 것이 아니고,
              placeholder 기록이 "평가형이 가장 나쁘다(1/6)"고 판정한 유형이다.
              NO_MATCH → 선택 화면이 설계된 동작이다
```

**추가한 8종** (부정 표현 오탐 0 실측)

```
좋다 · 너무좋 · 진짜좋 · 참좋 · 좋았어 · 좋은하루 · 기분째 · 기분이좋아
```

⛔ **넣지 않은 것 — 되돌리려는 시도를 막기 위해 남긴다**

```
좋아      오탐 6건  "기분이 안 좋아" · "하나도 안 좋아" · "좋아하는 사람이 생겼어요"
좋아요    오탐 1건  "마음이 안 좋아요"
기분이좋  오탐 1건  "기분이 좋지 않아요"  → 어미까지 붙인 `기분이좋아`로 넣었다
```

`app/test/behavior.test.js`가 부정 6종을 고정하고, `좋아`를 넣으면 3건이 새는 것을
고장 주입으로 확인했다.

**"좋아요" 단독은 여전히 미분류다 — 알고 두는 것이다 (사용자 결정)**

위기 경로처럼 **단독 입력 정확 일치 층**(`crisis_exact`)을 열면 잡히지만 열지 않았다.

> 위기는 놓치면 사람이 위험해서 단독 입력까지 잡아야 했지만, "좋아요" 하나를
> 놓쳐 선택 화면이 나오는 것은 감당 가능한 결과다. 1건을 위해 층을 새로 여는
> 비용이 크고, 그 층이 생기면 "이것도 단독으로 넣자"가 계속 나온다.
> → [7] 부정 표현 처리와 함께 본다.

**★ 이 사고에서 배운 것 — 화면이 정상인 것과 분류가 정상인 것은 별개다**

> 조사(2.25절)에서 "일이 너무 많아서 하루종일 정신이 없었어요 → NO_MATCH"를
> **설계된 안전 동작**으로 판정했다. 그 판정 자체는 맞다 — 감정어가 없는 입력이다.
> 그런데 그 판정을 **NO_MATCH 전반에 적용해** "화면이 깨지지 않으니 결함이 아니다"로
> 넘어갔다. "기분이 좋아"는 감정을 **직접 말한** 입력인데 같은 NO_MATCH로 묶였다.
>
> **가르는 기준: 입력에 감정어가 있는가.**
>   감정어 없음 → NO_MATCH가 옳다 (선택 화면으로 보낸다)
>   감정어 있음 → NO_MATCH는 사전 결함이다
>
> 2.27절(위기)과 이 절이 같은 교훈을 두 번 준 셈이다 — **"사전에 있는 것이
> 걸리는가"가 아니라 "있어야 할 것이 사전에 있는가"를 물어야 한다.**


### 2.32 [5] 주제 집합 분리 — 셋은 갈렸고 하나는 공급 문제였다 (2026-08-20)

동일 5쌍을 성격별로 갈랐다. **네 건이 서로 다른 답을 냈다.**

**A. joy 3쌍 — 집합 분리로 풀렸다. 다만 구절의 축은 옮길 수 없었다**

구절 큐레이션의 축(감사 어휘 / 공을 돌림 / 신체 동작, 2.7절 확정 규칙 3)을 영상
주제로 옮기려 했는데 **재료가 없었다.**

```
공을 돌림   주께 영광 0 · 영광 돌리 1 · 주가 하셨 0 · 은혜입니다 0 · 주께 돌리 0
신체 동작   춤추 0 · 뛰놀 0 · 손뼉 0 · 손을 들 0 · 춤을 0 · 노래하리 2(이미 태깅)
```

찬양 콘티 제목은 **곡명 나열**이라 그런 축이 제목에 드러나지 않는다.
새 주제를 만들 근거가 없어 기존 2주제로 갈랐다.

```
joy.grateful  [gratitude]              풀 21 → 주제분 16/20
joy.delight   [joy_praise]             풀 17 → 주제분 16/20
joy.proud     [gratitude, joy_praise]  풀 38 → 주제분 20/20
```

셋을 2주제로 가르면 하나는 합집합을 가질 수밖에 없다. **부분 겹침은 남지만
100% 동일은 사라졌다.**

**B. boredom 1쌍 — 절반만 풀린다. bible_story는 채널 발굴 대상이다**

`bible_story` 4건이 **전부 꿈의교회 저녁예배 `강해`** 하나였다(키워드 5종 중
4종이 검색어 유물). 확장 후보를 쟀다.

```
성경 속   +2건  "임용한 박사의 성경 속 전쟁" — 진짜 bible_story다. 넣었다
이야기    +12건 대부분 CTS 광복 시리즈다. 성경 이야기가 아니다 → 넣지 않았다
인물 · 열전 · 일대기 · 다시 읽 · 히스토리   전부 0건
```

**키워드로는 4 → 6건이 한계다.** 축 배분은 구절 때와 같게 갔다.

```
boredom.dull     [daily_word]                        다시 보게    주제분 15/20
boredom.novelty  [bible_story, new_thing, creation]  모르는 것으로  주제분  8/20
겹침 100% → 15%
```

⛔ `novelty`에 `daily_word`를 주면 15/20으로 두꺼워지지만 **겹침이 90%로
되돌아간다**(실측). 겹치는 두꺼움보다 얇은 구분이 낫다.

**C. anger.irritation = exhaustion.tired — 집합 분리로 안 풀린다**

시도했다가 **더 나빠졌다.** `anger.irritation`에 `peace`를 주면
`anxiety.restless`(peace + quiet_worship)와 **새로 100% 동일**해진다.

```
anger.irritation  self_control 0 + quiet_worship 12
exhaustion.tired  rest         0 + quiet_worship 12
calm.ease         creation     1 + quiet_worship 12
```

> **세 화면이 12건짜리 quiet_worship 하나에 갇혀 있다.** 파트너 주제가 전부
> 0~1건이라 실효 집합이 {quiet_worship} 하나로 수렴한다. 공급 문제다.

→ 임시 대응으로 **세분류별 오프셋**을 넣었다(아래). 근본 해결은 3절 10번.

**D. anger.unfair — media_default를 worship → sermon으로 바꿨다**

매핑 3주제(justice 2 · lament 2 · forgiveness 1)의 풀이 **전부 sermon**이라
기본값 worship에서는 주제분이 한 건도 안 보였다.

```
worship  보이는 12건 · 주제분 0     ← 개정 전
sermon   보이는 17건 · 주제분 5
```

---

**★ 세분류별 오프셋 — [4]가 다른 형태로 되살아났다**

```
[4]의 원래 목적   폴백 겹침 해소   → 보류됐다. 동일 5쌍 중 오프셋으로 풀리는 것이 0쌍
[4]의 새 목적     주제분 순서 분산 → 채택. 같은 주제 풀을 공유하는 화면의 시작점을 달리한다
```

**⚠ 이것은 세트를 바꾸지 않는다. 순서만 바꾼다.** 세트를 가르려면 화면당 몫을
12 → 4로 깎아야 하고 그러면 폴백이 8건 는다. 사용자가 바로 알아채는 것은
스크롤 없이 보이는 첫 화면이므로, **주제분을 잃지 않으면서 첫 화면을 다르게
만드는 쪽**을 골랐다(사용자 결정).

```
짜증  변함없는 은혜 / 위대한 주 메들리 / 나의 모습 나의 소유 / 어노인팅 연주곡
피로  거룩하시다 / 내 맘에 한 노래 있어 / 변함없는 은혜 / 위대한 주 메들리
여유  어노인팅 연주곡 / 내 삶의 이유라 / 예수 열방의 소망 / 구주를 생각만 해도
```

⚠ **세분류 id를 해시해서 쓰지 않는다.** 처음에 그렇게 했다가 `anger.irritation`과
`exhaustion.tired`가 12로 나눈 나머지에서 **둘 다 4**로 충돌해 아무것도 달라지지
않았다. **해시는 충돌을 막아 주지 않는다.** 매핑 순서의 자리를 쓰면 같은 풀을
공유하는 화면이 연속된 자리에 있어 나머지가 자연히 갈린다 —
quiet_worship을 쓰는 다섯 화면이 0·3·13·20·21번이고 오프셋 0·1·3·8·9로 나뉜다.
⚠ 파이썬 내장 `hash()`도 쓰지 않는다 — `PYTHONHASHSEED`로 프로세스마다 달라진다.

**결과**

```
                조사(2.25)  [3] 직후   [5] 후
목록 100% 동일     5쌍       5쌍       0쌍
자카드 90%+        8쌍       8쌍       5쌍   ← 집합 기준. 순서로는 갈렸다
감정 신호 없음    43.2%     36.0%     36.6%
폴백             33.3%     27.2%     30.3%
토글 주제분 0건     4화면     1화면     0화면
서로 다른 영상     141종     144종     155종
```

⚠ 폴백이 27.2% → 30.3%로 올랐다. **되돌리지 말 것** — 나쁜 영상이 는 것이 아니라
세 화면이 나눠 갖게 되면서 각자 몫이 줄었다. 2.29절의 판단과 같은 논리다.


### 2.33 [7] 부정 표현 처리 — 긍정 계열에만 건다 (2026-08-20)

**★ 먼저 확인한 것 — 이 작업은 `normalize.parity` 제약을 받지 않는다**

```
normalize() 함수 자체        parity 대상. 건드리면 Python도 고쳐야 하고 영상 태깅에 영향
classify.js 점수·부정 처리   JS 전용. **배치는 감정 키워드를 매칭하지 않는다**
                            (lib/taxonomy.py는 세분류 id만 읽는다)
taxonomy.yaml 키워드         JSON으로 전달만 된다
```

→ **`normalize()`를 건드리지 않는 한 자유롭다.** [3]의 어절 경계가 배치 전용이라
자유로웠던 것과 방향만 반대다. `normalize()`는 손대지 않았고 parity 검사 3종이 통과한다.

**문제 규모 — 실측 18건 중 12건이 빗나갔다**

```
부정 8건   "행복하지 않아요" → joy.delight   ★ 방향이 정반대다
           "설레지 않아요" → flutter · "여유가 없어요" → calm
           "재미있는 게 하나도 없어요" → joy.delight
길이 4건   감정어가 상황 묘사어에 길이로 진다
```

**★ 이중 부정 위험 — 부정 계열에 걸면 안 된다는 것이 실측으로 갈렸다**

부정 계열 감정어에 "않/없"이 붙으면 뜻이 **뒤집히지 않는다. 오히려 지속된다.**

```
슬픔이 가시지 않아요    슬픔이 계속된다는 뜻이다
걱정이 떠나지 않아요    걱정이 계속된다는 뜻이다
짜증이 가시질 않아      짜증이 계속된다는 뜻이다
→ 전체 계열에 걸면 위 6건이 **전부 무효화**된다.
  가장 도움이 필요한 상태의 입력을 통째로 잃는다.
```

**`lib/tagging.py`의 `BRIGHT_THEMES`와 같은 구조다** — 밝은 쪽에만 건다.
`POSITIVE_CATEGORIES = {joy, flutter, calm}`.

**두 층에 걸어야 했다**

세분류에서 무효화한 것이 **대분류 폴백에서 되살아났다** — "설레지 않아요"가
flutter 세분류에서 걸러졌는데 `matchCategory`가 다시 flutter를 집었다.
`scoreSubcategories`와 `matchCategory` 양쪽에 같은 규칙을 건다.

**길이 문제 — 일반 규칙이 아니라 키워드 중복이었다**

4건을 뜯어보니 **점수 규칙을 바꿀 일이 아니었다.**

```
2건은 복합 감정이라 현재 결과도 타당하다
   "일이 안 풀려서 불안해요" → frustration.blocked  (일이 안 풀린다고 명시했다)
   "아무것도 못 하겠고 슬퍼요" → exhaustion.burnout  (둘 다 말했다)
1건은 **키워드 중복**이었다  ← 고쳤다
   `손에안잡`(anxiety.restless)과 `손에안잡혀`(exhaustion.listless)가 갈려 있었고
   길이 합에서 **긴 쪽이 항상 이겨** 짧은 쪽이 죽은 키워드가 돼 있었다.
   실측: `손에안잡혀`를 빼도 짧은 쪽이 4/4 전부 덮고,
        "무기력해서 손에 안 잡혀요"는 `무기력`이 함께 걸려 그대로 남는다(2008 vs 1004)
1건은 진짜 모호하다  → 두지 않았다
   "아무도 내 얘길 들어주지 않는 것 같아 외로워요"
   `들어주지않`(억눌림)과 `외로`(외로움) 둘 다 타당하다
```

> **점수 규칙(길이 합)은 바꾸지 않았다.** 실패 4건 중 규칙 탓인 것이 하나도
> 없었다. 규칙을 고치는 대신 **중복 키워드를 정리**하는 것이 이 프로젝트의 방식에
> 맞는다 — 사전 문제를 규칙으로 덮으면 다른 곳이 깨진다.

**결과 — 12/18 → 4/18**

```
부정  8건 → 1건  ("재미가 없다" → exhaustion.listless.
                 joy가 무효화된 뒤 의욕없 계열로 갔다. 방향이 크게 틀리지 않는다)
길이  4건 → 3건  (2건은 복합 감정이라 타당 · 1건은 모호)
긍정 정탐 25/26 유지 · 앱 테스트 79 → 85개
```

**⚠ 알려진 한계 — 창 안의 무관한 부정어를 가르지 못한다**

```
"재미있었고 후회도 없어요"  →  NO_MATCH
   없는 것은 **후회**인데 창(8글자) 안에 `없`이 있어 `재미있`이 무효화된다.
   문법적으로 가르려면 파싱이 필요하다.
   결과가 NO_MATCH(선택 화면)라 **안전한 방향의 오탐**이고 빈도가 낮아 감수한다.
```

`behavior.test.js`가 이 한계를 **검사로 고정**해 뒀다 — 나중에 고쳐지면 그 검사가
깨지고, 그때 주석을 지우면 된다.

⚠ "화가 가라앉지 않아요"는 `sadness.sorrow`로 간다. `가라앉`이 sadness 키워드라
그렇고 **부정 처리와 무관한 별개 문제다**(규칙을 꺼도 결과가 같다). 감정 어휘
소속의 문제이지 [7]의 문제가 아니다.


### 2.34 이어서 읽기 탭 — 원문을 그대로 보여주니 새 함정이 나왔다 (2026-08-20)

구절이 속한 장을 앞뒤로 읽는 탭을 넣었다. `[이어서 읽기] │ [말씀] [찬양]`.
**설계안을 먼저 내고 승인을 받은 뒤 구현했다.** 아래는 원본만 읽어서는 알 수
없는 것 — 왜 그렇게 정했고, 조사 중에 무엇이 갈렸는가다.

**★ 지시받은 예시가 성립하지 않았다 — 이것이 설계를 바꿨다**

"롬 8:38-39가 떴으면 40절부터"로 지시받았는데 **로마서 8장은 39절로 끝난다.**
289건 전부에 "끝 절 + 1"을 돌려 보고 나서야 드러났다.

```
남은 절이 한 페이지 이상   240건 (83%)   그 자리에서 시작한다
남은 절이 1~2건            29건 (10%)   장 끝에 맞춰 뒤로 당긴다
이어 읽을 절이 0건         20건  (7%)   장 처음부터 연다
```

20건에는 시 23:6·롬 8:38-39·마 28:20처럼 **이 앱의 대표 구절**이 들어 있다.
규칙이 없으면 그 화면들이 전부 빈 화면이 됐다. `chapters.test.js`가 20건을
개수까지 박아 고정한다 — 큐레이션이 바뀌면 거기서 먼저 걸린다.

> 교훈: **예시로 받은 값을 그대로 믿지 말고 전수로 돌려 볼 것.** 이 건은
> 지시가 틀린 게 아니라 데이터가 예외였고, 그 예외가 7%였다.

**★ 합본 구간 — 지금까지 숨어 있다가 이 기능에서 처음 드러났다**

개역한글은 여러 절을 합쳐 싣는 구간이 있고, 원문 데이터는 절 수(31,102)를
맞추려고 그 합본 본문을 **각 절 번호에 똑같이 복제**한다. 지금까지 화면에
나온 적이 없는 이유는 `krv_source.verses_in_range`가 그런 ref를 **거부**해
수록 대상에서 뺐기 때문이다. 장 전체를 그리면 막을 것이 없다.

```
174개 장 전수 조사
  이사야 30장   1·2절이 같은 본문       -> "1-2"로 묶어 한 번만 그린다
  시편 92편     1·2·3절이 같은 본문     -> "1-3"
```

동일성유지권 문제는 없다. 본문을 한 글자도 바꾸지 않고, **원문의 원래 제시
방식으로 되돌리는 것**이다(대한성서공회도 "1-3"으로 표시한다).

⚠ **반대 방향이 더 위험하다.** 같은 조사에서 5개 장이 중복으로 걸렸는데 전부
**후렴**이었다 — 원문이 의도적으로 반복한다.

```
시편 46:7·11 · 시편 57:5·11 · 시편 107:8·15·21·31 · 왕상 19:10·14 · 민 6:1·22
```

판별은 **절 번호가 붙어 있는가** 하나로 갈린다. 그래서 `chapterUnits`는 집합으로
중복을 세지 않고 이웃만 본다 — Set으로 세는 순간 후렴이 함께 걸린다.
합본을 묶는 검사는 실패하면 눈에 띄지만(같은 문장이 두 번 보인다), **후렴을
묶지 않는 검사는 실패해도 조용하다.** 시편 107편의 후렴 넷이 하나로 접혀도
화면은 멀쩡해 보이고 아무도 모른다. 그쪽이 동일성유지권 위반이다.

**데이터 — 번들에 넣지 않았다 (실측으로 갈렸다)**

```
현재 앱 번들                 gzip    98,225
전문 66권 번들 추가          gzip +1,254,618   12.8배  -> 탈락
닿는 장 174개 번들 추가      gzip   +165,904    1.7배  -> 탈락
장 단위 원격 (채택)          gzip 장당 1,234(중앙값) · 최대 4,616 (시편 119편)
```

번들에 넣는 두 안은 **탭을 한 번도 열지 않는 사용자가 비용을 전부 낸다.**
책 단위 청크도 검토하고 버렸다 — 감정 구절 289건 중 **시편이 166건(57%)**이라
시편 청크(gzip 47,944)를 절반 이상의 경우에 받게 된다.

실제 증가분은 **+3,710 gzip (98,225 → 101,935, +3.8%)**이고 대부분 verses.json에
붙은 `read` 필드다.

**★ ref 파싱을 JS로 옮기지 않았다**

`parse_ref`는 66권 한글 정식명·약어 사전을 들고 있다. 옮기면 그 순간 **두 번째
구현**이 되고, 이 저장소는 그 비용을 안다(`normalize.parity.test.js`가 존재하는
이유). 그래서 **파싱을 빌드 타임에 끝냈다** — `gen_verses_json.py`가 구절마다
`read: {book, chapter, from}`을 계산해 붙인다. 앱은 문자열을 파싱하지 않고,
이식도 패리티 테스트도 필요 없다.

**위기 화면 격리 — 두 겹으로 막았다**

```
데이터   위기 풀에는 read를 붙이지 않는다 (gen_verses_json.py가 단언한다)
화면     CrisisScreen이 ChapterReader를 import하지 않는다 (behavior.test.js)
```

한 겹만 뚫려도 닿지 못한다. 위기 풀을 최상위로 분리한 것과 같은 판단이다 —
구조로 막으면 그 실수가 불가능해진다.

**결정 넷 (승인받은 것)**

```
A 탭 순서    [이어서 읽기] │ [말씀] [찬양] — 구분선이 본문/영상 형식을 가른다
B 데이터     원격. 오프라인 첫 방문 실패는 "지금은 본문을 못 불러왔어요"로 말한다
             ⚠ 앱 전체가 죽은 것처럼 읽히지 않게 주어를 본문으로 뒀다.
               구절 화면 자체는 번들이라 멀쩡하다
C 탭 이름    360px 실측 — 한 줄에 들어가고 **여유 108px**. "이어서 읽기" 확정
D 말하지 않는 것  장 처음부터 열린 것(20건)도, 위에 뜬 구절이 어느 것인지도
             표시하지 않는다. 장 머리와 절 번호가 이미 알려준다
```

**분량과 크기 — 360×640 실측**

`VerseCard`가 남긴 기준선(131자 / 18px / 행간 1.95 -> 211px = 6줄)에서 줄당
21.8자를 얻어 4,283절 전부에 적용했다. **중앙값 45자가 줄바꿈 경계에 걸린다.**

```
18px  줄당 21.8자 -> 45자 = 3줄     4절 페이지 442px
17px  줄당 23.1자 -> 45자 = 2줄     4절 페이지 294px    1px 차이로 33% 준다
```

17px / 4절로 정했고, 실제 렌더에서 추정과 줄 수가 정확히 일치했다(34자=2줄 ·
57자=3줄 · 49자=3줄 · 74자=4줄). 17px은 구절 최소 단계 18px보다 작아
**위계도 함께 지킨다** — 구절이 주인공이라는 것이 크기로도 유지된다.

⚠ 구절 카드의 22/20/18 사다리는 **쓰지 않았다.** 그 기준은 "360px에서 구절과
공감 문구가 한 화면에 들어오는가"이고 "구절 한 개가 주인공"이라는 전제 위에
있다. 이어서 읽기는 전제가 둘 다 다르다.

**연동 — 코드가 없다**

완전 연동(A)은 `<ChapterReader key={verse.id} />` 한 줄로 끝났다. key가 바뀌면
React가 새로 마운트하므로 읽던 위치가 그 자리에서 사라진다. 탭 선택(pane)은
리셋하지 않는다 — 새 장 머리가 뜨므로 화면이 스스로 설명한다.

**★ pane을 mediaType의 세 번째 값으로 만들지 않았다**

`mediaType`은 IndexedDB에 영속된다. 이어서 읽기가 그 값이 되면 **다음 방문에
결과 화면이 영상 없이 본문부터 열리고**, 개인정보처리방침이 전제하는
"마지막으로 고른 형식"의 뜻이 조용히 바뀐다. 그래서 상태를 둘로 뒀고,
`pane`은 저장하지 않는다 — **이 기능은 기기에 남기는 것을 늘리지 않는다.**

**같은 실수를 두 번 할 뻔했다**

`ChapterReader`의 [이전]/[다음] 초안이 `borderBottom` 단축과 `borderBottomColor`
덮어쓰기를 섞었다 — 2026-08-19에 MediaToggle에서 고친 바로 그 결함이다.
검사가 `MediaToggle.jsx` **한 파일만** 보고 있어서 잡히지 않았다.
`behavior.test.js`의 그 검사를 파일 목록으로 바꿔 두 컴포넌트를 함께 본다.

> 교훈: **한 곳만 지키는 회귀 검사는 다음 컴포넌트를 막지 못한다.**

**부수적으로 고친 것**

```
gen_verses_json.py  감정 풀과 위기 풀을 각자의 허용 목록으로 검사한다.
                    전에는 둘 다 APP_FIELDS(넓은 쪽)로 봐서, 위기 항목에
                    theme이 섞여도 그 자리에서는 걸리지 않았다
ChapterReader       절 번호와 본문 사이에 줄바꿈 없는 공백(U+00A0).
                    margin만으로는 화면에서만 떨어지고 textContent는
                    "36이에"로 붙는다 — 복사해 나가는 사람과 스크린리더가
                    그것을 받는다. 일반 공백이 아닌 이유는 그 자리가 줄바꿈
                    지점이 되면 번호만 앞줄에 남을 수 있어서다
sw.js               /krv/는 cache-first다. 원문은 절대 바뀌지 않으므로
                    앱 셸의 network-first를 쓸 이유가 없다.
                    덕분에 **한 번 연 장은 오프라인에서도 열린다**
```

**남은 것**

```
가변 페이징   절 길이가 4~166자로 40배 차이라 고정 4절은 페이지 높이가
              100~660px로 흔들린다. 문자 수 예산으로 바꾸면 균일해지지만,
              절 번호가 화면에 있어 사용자가 "몇 절씩 넘는지" 감을 잡는데
              가변이면 그 감이 사라진다.
              다시 열 조건: 페이지 길이가 들쭉날쭉하다는 신호
오프라인 첫 방문  못 받는다. 174개 장을 지연 청크로 번들에 넣는 안이 대기 중이다.
              다시 열 조건: "네트워크가 없어도 구절 화면은 떠야 한다"를
              **장 본문까지 확장한다고 판단할 때** (지금은 판단하지 않았다)
```


### 2.35 밝기 단계가 둘뿐이었다 — 위계는 색 하나로 만들 수 없다 (2026-08-20)

"공감 문구와 탭 라벨의 색이 같아 위계가 안 잡힌다"는 지적에서 시작했는데,
재어 보니 **화면 전체에 밝기가 두 층뿐**이었다.

```
                        색                크기     서체            대비(#141E24 기준)
구절 본문                #E9EEEA T.mist   22px    Nanum Myeongjo   14.41:1
탭(선택)                 #E9EEEA T.mist   14px    Noto Sans KR     14.41:1
공감 문구                #8DA29E T.muted  17.5px  Nanum Myeongjo    6.28:1
탭(비선택)               #8DA29E T.muted  14px    Noto Sans KR      6.28:1   <- 같은 토큰
```

**읽는 글(공감 문구)과 누르는 것(비선택 탭)이 아래 층을 공유하고 있었다.**
그래서 문구가 탭 줄의 일부처럼 읽혔다. 서체(명조/고딕)와 크기(17.5/14)가 이미
다른데도 색이 같으면 같은 덩어리로 읽힌다 — **색이 다른 신호를 덮는다.**

⚠ **이 표를 다시 뽑는 법**: 배포본에서 `getComputedStyle`로 색을 뽑고 배경
`#141E24` 기준으로 대비를 계산한다. 이 기준은 HANDOFF 2.21이 쓴 것과 같고,
그 절의 기록값(T.muted 6.28:1 · `#ffffff66` 3.78:1)과 정확히 일치하는 것으로
계산법을 검증했다. **다른 화면에서 "위계가 안 잡힌다"는 말이 나오면 이 표부터
뽑을 것** — 층이 몇 개인지 세는 것이 먼저다.

**고친 방향 — 글이 아니라 컨트롤을 내렸다**

문구를 올리는 안(T.mist 80% = 9.56:1)이 시각 효과는 더 컸는데 두 가지가 걸렸다.

```
① 앱의 목소리는 T.muted로 통일돼 있다
   공감 문구도 마무리 문구도 T.muted다. common.jsx 주석에 근거가 있다 —
   "다시 적어보기" 링크를 3.78:1까지만 올린 이유가 "바로 위 마무리 문구보다
   밝아지면 안 되기 때문"이다. 공감만 올리면 그 통일이 깨지고,
   맞추려면 마무리 문구까지 함께 올려야 한다
② 의미상으로도 "글은 그대로 두고 컨트롤이 물러난다"가 맞다
```

**얼마나 내릴지는 AA가 정했다 — 배경이 그라데이션이라 구간별로 쟀다**

탭 라벨은 아이콘이 아니라 **읽는 글자**라 WCAG AA 4.5:1에 걸린다.

```
배경                      현재 100%   D9 85%(채택)   CC 80%
plum #221A28 (상단)        6.25:1      4.91:1       4.50:1   <- 정확히 경계, 여유 0
ink #141E24 (중단)         6.28:1      4.91:1       4.53:1
inkDeep #0B1216 (하단)     7.01:1      5.35:1       4.87:1
```

⚠ **가장 불리한 구간에서 재야 한다.** 중단(ink)만 봤으면 CC를 4.53:1로 읽고
통과시켰을 것이다. 상단에서 4.50:1 — 배경을 조금만 손대도 깨지는 값이다.

**결과 — 세 층**

```
구절 · 탭(선택)   14.41:1
공감 문구          6.28:1   손대지 않음
탭(비선택)         4.91:1   ${T.muted}D9 — 새 색이 아니라 기존 토큰에 알파
```

선택 탭은 손대지 않았다(T.mist + jade 60% 밑줄). 2026-08-19에 고친 자리이고,
"선택된 것만 뚜렷하게"가 이 개정의 목적이라 오히려 유지가 요건이다.

> 남는 것: 위기 화면·입력 화면·About 화면은 아직 재지 않았다. 같은 증상이
> 나오면 위 표부터 뽑고, **색을 올리기 전에 그 색이 다른 곳에서 무엇을
> 뜻하는지** 확인할 것 — 이번에 공감 문구를 못 올린 이유가 그것이었다.

⚠ **이 절만 읽고 멈추지 말 것 — 여기서 한 조정은 문제를 풀지 못했다.**
이 절은 "밝기 단계가 둘뿐"이라는 관찰까지가 유효하고, 그 뒤에 내린 처방
(비선택 탭을 85%로)은 **눈에 보이지 않았다.** 이어서 여백을 벌린 것도
실패했고, 실제로 문제를 푼 것은 **탭 줄 위의 1px 구분선**이다.
경위와 그로부터 나온 규칙은 전부 **2.36절**에 있다. 그쪽을 함께 읽을 것.


### 2.36 ★ 세 번 만에 맞췄다 — 색·여백·구조 신호 (2026-08-20)

> **수치가 움직였다는 것과 눈에 보인다는 것은 다르다.**
> 그리고 **글자만 나란히 놓인 컨트롤은 색으로 구분되지 않는다.**
> 이 절이 남는 이유는 그 두 문장이고, 아래는 그것을 세 번 만에 배운 기록이다.

**증상** "공감 문구와 탭 라벨의 색이 같아 위계가 안 잡힌다.
구절 아래 공감 문구는 **읽는 글**이고 탭 라벨은 **누르는 것**인데, 같은 색이라
문구가 탭 줄의 일부처럼 읽힌다."

---

#### 1차 — 색 (비선택 탭 T.muted → 85%) · 실패

대비가 6.28 → 4.91로 움직였다. 근거도 틀리지 않았다(앱의 목소리는 T.muted로
통일 · AA 4.5 하한 · 선택 탭 유지). **그런데 눈으로는 구분되지 않았다.**
대비비 22% 감소가 지각적으로는 약 15% 차이일 뿐이다.

더 내릴 길도 막혀 있었다. 탭 라벨은 아이콘이 아니라 **읽는 글자**라 AA 4.5:1에
걸린다.

```
100%  6.28:1      85%(적용)  4.91:1
 80%  4.53:1  <- AA 여유 +0.03      75%  4.13:1  <- AA 미달
```

6.28에서 4.5까지가 가진 폭의 전부이고, 80%로 가도 또 8% 차이다.
**15%로 안 된 것이 8%로 될 리 없다.**

**★ 왜 색으로는 안 되는 구조였나 — 색이 이미 포화 상태다**

```
                      색           대비      크기·서체
구절 본문              T.mist      14.41:1   22px 명조
탭(선택)               T.mist      14.41:1   14px 고딕 + jade 밑줄
영상 제목              T.mist      14.41:1   14px 고딕
공감 문구              T.muted      6.28:1   17.5px 명조
주제분 헤더            T.muted      6.28:1   11.5px 고딕 자간 0.2em
채널명                 T.muted      6.28:1   11px 고딕
탭(비선택)             T.muted 85%  4.91:1   14px 고딕
폴백 헤더              #ffffff3d    2.18:1   11.5px 고딕 자간 0.14em
```

**T.mist가 3역, T.muted가 3역을 겸한다.** 층을 더 만들 여지가 색에는 없었다.

---

#### 2차 — 여백 (24 / 56으로 벌림) · 실패

실측하니 여백이 거꾸로였다. 구절 34px ↔ 공감 문구 ↔ 탭 30px —
**공감 문구가 구절보다 탭에 더 가까웠다.** 근접이 색보다 강한 신호이므로
이것이 원인이라고 판단하고 24 / 56으로 2.3배 벌렸다.

**배포하고 보니 나아지지 않았다.** 사용자 지적이 정확했다 —

```
구절 블록이 무거워졌다    공감 문구가 구절·장절과 한 덩어리로 뭉쳐 위쪽이 빽빽해졌다
벌어진 자리가 비어 보였다  56px는 "구간이 갈린다"가 아니라 "그냥 떨어져 있다"로 읽혔다
정작 원래 문제는 그대로   탭 줄은 여전히 위의 문장과 같은 종류로 읽혔다
```

> **근접은 신호이지만 위계는 아니다.** 여백은 요소를 묶고 가를 뿐,
> "이쪽은 읽는 것이고 저쪽은 누르는 것"이라는 **종류의 차이**를 말하지 못한다.
> 빈 공간에는 방향이 없다.

이 변경은 **되돌렸다**(여백 34 / 26으로 복귀).

---

#### 3차 — 구조 신호 (탭 줄 위 1px 구분선) · 성공

빠져 있던 것은 색도 여백도 아니었다.

**탭 줄에 "컨트롤"이라는 표시가 아무것도 없었다.** 배경도 테두리도 없이 글자만
나란히 놓여 있고, 선택된 것에만 1px 밑줄이 있을 뿐이다. 그러니 바로 위 문장과
같은 종류로 읽힐 수밖에 없었다. 필요한 것은 네 번째 밝기 층이 아니라
**"여기서 읽는 구간이 끝난다"는 구조 신호**였고, 그 일은 선이 한다.

```
탭 줄 위   borderTop 1px #ffffff1f · paddingTop 18px
공감 문구  아래 여백 26px (= 선까지의 거리. 원래 30px에서 조정)
구절       아래 여백 34px (원래대로 복귀)
```

선을 쓰는 것은 이 앱의 언어에 어긋나지 않는다. VerseCard가 카드 상자를 걷어낸
것은 **면**을 없애기 위해서였지 선을 금지한 것이 아니다 — 그 주석이 인용하는
FYM 규칙이 그대로다: "입력은 밑줄 하나, **구분은 1px 선 하나**".
색도 새로 만들지 않았다. `#ffffff1f`는 바로 옆 세로 divider(탭 사이 실선)가
이미 쓰는 값이라 가로·세로 두 선이 같은 굵기·같은 밝기다.

---

#### 되돌리지 않은 것 둘 — 옳았지만 위계를 푼 것은 아니다

```
폴백 헤더 색   #ffffff3d(2.18:1) -> ${T.muted}CC(4.53:1)
   2.18:1은 읽히지 않는 수준이었다. 이 헤더는 장식이 아니라 **정직성 장치**다 —
   "딱 맞는 건 아니지만"을 먼저 말해 기대를 걸지 않게 하는 문장이라
   (PLAN.md 3.3) 안 읽히면 그 장치가 작동하지 않는다.
   ⚠ 다만 체감 차이는 거의 없었다. 측정으로 두 배 넘게 밝아졌는데 눈에는
     그만큼 오지 않았다 — 1차와 같은 함정에 폴백 헤더 쪽에서도 걸린 셈이다.

비선택 탭 85%
   AA 안전하고 "선택된 것만 뚜렷하게"에 기여한다.
   ⚠ 코드 주석에 **"그것이 해결책은 아니었다"** 를 적어 두었다. 다음 사람이
     그 값을 보고 "색으로 해결됐구나"로 읽으면 같은 함정에 다시 걸린다.
```

#### 검토하고 버린 것

```
공감 문구 축소 (17.5 -> 15px)   차이가 거의 없고, 오히려 탭(14px)과 크기가
                               가까워져 더 나빠진다
탭 형태 변경 (밑줄 대신 다른 형태)  불필요. 구분선이 "여기부터 컨트롤"을 말해주면
                               밑줄만으로 충분하다. 형태를 바꾸면 2026-08-19에
                               고친 밑줄 결함 자리를 다시 건드리게 된다
```

⚠ 여백을 되돌렸으므로 VerseCard의 360×640 실측표는 **원래 값 그대로 유효하다**
(공감 문구 하단 358px · 여유 282px). 2차 시도 중 배포본에서 다시 재 그 표가
맞다는 것을 확인했다.

---

**★ 그래서 앞으로 지킬 것**

```
1. 시각 조정은 **렌더를 보고 판단한다.** 대비 수치는 하한을 지키는 도구이지
   "구분이 되는가"의 답이 아니다. 수치가 움직였다는 것과 눈에 보인다는 것은
   다르다 — 이 저장소는 그것을 세 번 만에 배웠다.

2. ★ **글자만 나란히 놓인 컨트롤은 색으로 구분되지 않는다.**
   컨트롤이라는 표시(선·면·형태)가 없으면 읽는 글과 같은 종류로 읽힌다.
   색을 몇 단계 바꾸든 종류가 같아 보이는 것은 그대로다.

3. **근접은 신호이지만 위계는 아니다.** 여백은 요소를 묶고 가를 뿐 종류의
   차이를 말하지 못한다. 벌린 자리가 "구간이 갈린다"로 읽히지 않고 "그냥
   비었다"로 읽히면 여백이 답이 아니라는 뜻이다.

4. 색을 만지기 전에 2.35절의 표를 뽑아 **그 색이 몇 가지 역할을 겸하는지** 센다.
   이미 3역이면 네 번째 층을 색으로 만들 수 없다.

5. 배경이 그라데이션이면 **픽셀을 떠서** 대비를 계산한다. 스톱 값으로 추정하면
   실제보다 보수적이거나 낙관적인 답이 나온다.
   (playwright로 글자 없는 여백을 잘라 Pillow로 읽는다 — scratchpad bg.mjs)
```


### 2.37 ★ 부정 계열 재점검 — 사전에 `나쁘`가 하나도 없었다 (2026-08-20)

실사용 제보 "기분이 나빠가 안 걸린다". 2.31절(긍정)과 같은 유형으로 보였는데
**조사 문제가 아니었다.**

```
기분이 나빠   미분류
기분 나빠     미분류   <- 조사가 없어도 안 걸린다
기분나쁘다    미분류
```

세분류 키워드 900개에 **`나쁘`·`나빠`·`나뻐`가 하나도 없었다.**
2.31이 "조사가 끼면 깨진다"였다면 이번은 **어휘 자체가 없었다.**
`자살`이 위기 사전에 없었던 2.27절과 같은 종류다.

**커버리지 48건 실측 — 17건 미분류(35%)**

```
개정 전 17건 미분류   기분이나빠 계열 4 · 기분이/마음이 안좋 3 · 섭섭 · 분해
                     화가치밀 · 눈물만 · 스트레스받아 · 마음이복잡
                     기분이않좋 · 실타 · 시러 · 힘드러 · 현타
개정 후  4건          분해 · 실타 · 시러 · 현타  (아래 [고치지 않은 것])
```

**★ 오탐도 하나 나왔다 — "기분이 별로야"가 상실감 화면으로 가고 있었다**

```
"기분이 별로야" -> 정규화 "기분이별로야" -> `이별`(sadness.loss)에 부분 일치
```

사별·이별을 다루는 화면이라 **방향이 정반대**다. 4.7절 어절 경계 문제의
실제 피해 사례다. `기분이별로`(5자)를 넣어 `이별`(2자)보다 길게 만들어
점수 규칙으로 이기게 했다.

**★ 이번에 배운 것 — 오탐 코퍼스는 발명하는 것이 아니다**

후보에 `별로야`를 넣고 직접 만든 대조 문장 50건으로 "오탐 0"을 얻었다.
**틀린 결과였다.** taxonomy.yaml에 2026-08-18자 ⛔ 기록이 이미 있었다 —

```
⛔ "별로야"도 넣지 말 것. 정규화가 공백을 지우므로 "별로 야근이 많아",
   "별로 야한 장면 없어", "별로 야식 안 먹어"가 전부 걸린다.
```

내가 만든 코퍼스에 그 세 문장이 없었을 뿐이다. **⛔ 주석이 이 저장소의
누적 오탐 코퍼스다.** 21곳에서 118개 문장을 뽑아 대조군에 넣고 다시 재
오탐 0을 확인했다(직접 작성 49 + ⛔ 기록 118 = 167건).

```
⚠ 절차: 키워드를 추가하기 전에 taxonomy.yaml의 ⛔ 문장을 전부 걷어
  대조군에 넣는다. 새로 만든 문장만으로 재면 이미 밟은 지뢰를 다시 밟는다.
```

**추가한 20종**

```
anger.irritation   기분나빠 기분나쁘 기분나뻐 기분이나빠 기분이나쁘 기분이나뻐
                   기분이별로 스트레스받
sadness.sorrow     섭섭 눈물만 기분이안좋 기분안좋 마음이안좋 마음안좋
                   기분이않좋 마음이않좋
anger.rage         화가치밀 치밀어
anxiety.worry      마음이복잡
exhaustion.tired   힘드러
```

⚠ `기분이안좋` 계열을 **sadness.sorrow로 보낸 것은 판단이다.** 감정은 분명하나
짜증/우울로 갈리는 입력이라, 틀릴 때 더 부드러운 쪽으로 보냈다 — 짜증 문구를
잘못 내미는 것보다 슬픔 문구를 잘못 내미는 편이 덜 다치게 한다.

**⛔ 고치지 않은 것 4건 — 되돌리려는 시도를 막기 위해 남긴다**

```
분해     `분해`를 넣으면 **차분해·충분해**가 걸린다. 평온한 사람이 억울함
         화면으로 간다. 2026-08-14 `분한` 기각(차분하다·충분하다)과 같은 자리다.
         억울함은 `억울`이 이미 잡는다
실타·시러 의도적 구어 표기다. 대응을 시작하면 끝이 없다(시로·시러여·실쿠나).
         2.31절이 "괜찮아요"에 선을 그은 것과 같은 판단이다
현타     신조어이고 감정 방향이 갈린다(허무·자괴·무기력). 근거가 약해 미룬다
```

**알고 두는 것 — 이중 부정**

"기분이 나쁘지 않아요"·"섭섭하지 않아요"는 부정 계열로 분류된다.
부정 계열에 부정 무효화를 걸지 않는 설계의 결과다(classify.js — "슬픔이
가시지 않아요"가 슬픔의 **지속**을 뜻하기 때문).
⚠ **새 결함이 아니다.** 기존 키워드로 같은 검사를 하면 9건 중 8건이 똑같이
걸린다("화나지 않아요"·"우울하지 않아요"·"억울하지 않아요"…).

**회귀 테스트** — `behavior.test.js`에 43건 고정. 사전을 참조하지 않고 손으로 적었다.
함정별로 **어느 계열로 가면 안 되는지**를 따로 단언한다.

```
⚠ "부정이면 무조건 실패"로 적었다가 거짓 실패를 냈다 — "별로 야근이 많아"는
  기존 키워드 `야근`에 걸려 exhaustion.tired로 가고, 그건 틀린 결과가 아니다.
  막아야 하는 것은 `별로야`가 만들던 **짜증** 오분류다.
```

**★ 고장 주입이 무효였던 일 — `npm test`로는 검증되지 않는다**

키워드 4개를 taxonomy.json에서 지우고 `npm test`를 돌렸더니 그대로 통과했다.
**`pretest`가 `gen:data`를 돌려 주입을 지운 뒤 테스트가 시작되기 때문**이다.

```
잘못된 방법   taxonomy.json 수정 -> npm test        (생성기가 덮어쓴다)
옳은 방법     taxonomy.json 수정 -> node --test test/behavior.test.js
```

바꿔 돌리니 2건이 정확히 실패했다(부정 커버리지 · 상실감 오탐).
⚠ **생성물을 고쳐서 하는 고장 주입은 전부 이 함정에 걸린다.** taxonomy.json ·
verses.json · themes.json이 모두 `gen:data` 대상이다.


### 2.38 ★ 장 문맥 재검토 — 큐레이터의 범위 제한이 기능 하나로 무효화됐다 (2026-08-20)

"이어서 읽기"(2.34절)를 넣으면서 **구절이 속한 장이 사용자에게 열리게 됐다.**
299건은 그 축 없이 뽑힌 것이라 전수 재검토했다. 개선이 아니라 **이미 나가고
있던 문제**였다.

**★ 이 절의 핵심 — 시편 88:1-2**

원래 note에 이렇게 적혀 있었다.

```
[범위 주의] 시편 88편은 성경에서 가장 어두운 시편으로 마지막이 "흑암"으로
끝난다. 1-2절만 쓰는 이유가 그것이다 — 부르짖음까지는 허용이지만 그 뒤의
절망 서술은 이 화면의 방향이 아니다.
```

**큐레이터는 알고 있었고 범위를 제한했다. 그 판단은 옳았다** — 당시에는 구절만
노출됐다. 이어서 읽기가 그 제한을 무효화했고, 지금은 3-6절이 첫 페이지에 온다:
음부 · 무덤 · 살륙 · 깊은 웅덩이.

> 교훈: **기능을 추가하면 기존 큐레이션의 전제가 바뀐다.** "구절만 보인다"가
> 전제였던 판단이 전부 다시 검토 대상이 된다. 새 기능의 비용은 그 기능의
> 코드가 아니라 **그것이 무효화하는 기존 판단**에 있다.

**★ 두 번째 발견 — 제외한 구절이 되살아난다**

```
시 1:3   번영신학으로 빼고 렘 17:7-8로 대체했는데, 시 1:2를 통해
         이어서 읽기 첫 페이지 **첫 줄**에 다시 떴다 → 대체 작업이 무효화됐다
시 143:7 "무덤에 내려가는 자" — 위기 인접으로 뺐는데 시 143:4의 첫 페이지에 있었다
```

전수 조사 결과 **13건**(첫 페이지 7 · 장 뒤쪽 6). 이것이 게이트를 만든 이유다.

**판정 기준 — 2층**

```
1층 차단 (수록 불가)
  · 첫 페이지(다음 4절)에 위기 인접 어휘   음부 · 무덤 · 죽기를 구하 · 소망이 없
  · 첫 페이지에 처형·살륙·참수 장면
  · 첫 페이지에 사용자를 지목하는 심판 명단   "두려워하는 자들과…"
  · 첫 페이지에 excluded 색인의 절            ← 기계가 강제한다
  · 그 장이 통째로 심판 선언이다              나훔 1 · 시편 88 유형
2층 기록 (수록하되 note에 남긴다)
  · 장 뒤쪽에 위 항목이 있다
```

⚠ **가르는 근거**: 첫 페이지는 탭을 열면 **무조건 보인다. 선택이 아니다.**
장 뒤쪽은 [다음]을 눌러 능동적으로 간 결과다. 장 전체를 무결하게 요구하면
수록 가능 구절이 급감한다 — 성경은 심판과 전쟁을 담고 있다.

**조치 — read_from이 핵심 도구였다**

```
① 뺀다 3건        시 88:1-2(장 전체) · 시 143:4 · 시 1:2 (첫 페이지에 제외 구절)
② read_from 16건   장 안의 다른 자리를 연다. **앱 변경 0** —
                   "장 처음부터" 분기(chapters.js)를 그대로 재사용한다
③ 리더에서만 제외   ⛔ 쓰지 않기로 했다. "다른 구절"을 누를 때마다 본문이
                   있다 없다 하면 고장으로 읽힌다 (ResultTabs 원칙과 같은 축)
④ 2층 기록 8건
```

read_from 대표 사례:

```
계 21:5  [기대]   8절 불못 명단("두려워하는 자들"이 첫 항목) → 1절부터
                 21:1-4 "모든 눈물을 그 눈에서 씻기시매"가 오히려 더 맞다
사 66:13 [상실감]  16절 "살륙 당할 자가 많으리니" → 10절부터 (위로 구간)
삼상 17:45 [새자극] 46절 "네 머리를 베고… 시체로" → 32절부터 (사자와 곰 이야기)
시 103:8 [짜증]   10절이 제외 구절 → 11절부터 "아비가 자식을 불쌍히 여김 같이"
```

**★ 자동 스캔만으로는 못 잡는다 — 두 방향으로 틀렸다**

```
오탐  시 103:1-2가 "파멸에서 구속하시고"로 걸렸다. 3-5절은 감사 화면에 완벽하다
누락  계 21:4는 상위 32위 안에 없었는데, 첫 페이지에 불못 명단이 있었다
```

그래서 **안전 임계 어휘만 전수로** 다시 훑었다(음부·무덤·시체·살륙·머리를 베·
불과 유황·두려워하는 자들). 17건이 나왔고 그중 5건이 자동 스캔 상위에 없던
것이다. **위험도 순 정렬은 우선순위이지 목록이 아니다.**

**게이트 — 제외 목록을 구조화했다**

주석 [제외 기록]만으로는 기계가 검사할 수 없었다. `verses.yaml`에 `excluded`
구조화 색인(ref + 사유 분류)을 두고 `gen_verses_json.py`가 두 방향을 본다.

```
① 색인의 모든 ref가 파일 어딘가의 주석에 언급되는가  → 없으면 생성 실패
   (근거 서술 없는 제외를 못 넣게 한다)
② 수록 구절의 이어서 읽기 첫 페이지에 색인의 절이 오는가 → 오면 생성 실패
```

⚠ **서술은 주석에 그대로 뒀다.** 색인은 ref와 사유 분류만 갖는다 — "왜 뺐는가"는
한 줄로 줄일 수 없는 판단 자산이다. 색인은 **판단을 기계가 강제할 수 있게 하는
색인**이지 판단의 대체물이 아니다.

⚠ **역방향은 강제하지 않는다.** 주석에서 ref를 기계로 뽑으면 오탐이 난다 —
실측에서 41건이 뽑혔는데 실제는 13건이었다(제외 기록 산문이 수록 구절 자신을
자주 언급한다). **거짓 실패가 쌓이면 사람이 게이트를 끈다.**

게이트가 실제로 5건을 잡았다(시 27:10·27:13 → 27:14 / 시 103:6·8·9 → 103:10).
고장 주입(시 1:2 되살리기)으로 정확히 걸리는 것도 확인했다.

⚠ 표기 대조에서 한 번 걸렸다 — 색인은 정식명("시편 1:3"), 주석은 약어("시 1:3")를
쓴다. 같은 절의 모든 표기를 만들어 대조하도록 고쳤다. **표기 차이로 거짓 실패가
나면 그것도 게이트를 끄게 만든다.**

**절차에 7번을 추가했다**

```
1 제외 목록 대조 → 2 기존 수록 → 3 주제 축 → 4 계열 겹침
→ 5 절 단위 함정 → 6 첫 20자 낭독 → 7 ★ 장 문맥 (첫 페이지를 읽는다)
```

**남은 것**

```
⚠ boredom.dull이 10 → 9건이 됐다 (시 1:2 제거). 하한 미달이며 "같은 장 반복"
  보강(시편 119장 ×4)과 함께 채운다 — 다음 작업이다
감정 구절 289 → 286 · 장 파일 174 → 172
```


### 2.39 시편 비율 실측 — 몰림은 큐레이션의 게으름이 아니라 정경 장르 분포다 (2026-08-20)

"구절을 늘리되 시편 비율을 낮추자"는 요청에 **먼저 실측**했다. 결론은 예상과
달랐다 — **억지로 낮추면 품질이 떨어진다.**

**분포 (302 태깅 중 시편 172 = 57%, 38책)**

```
시편 몰림 심한 화면    calm.stable 91% · calm.ease 82% · joy.grateful 80%
                     joy.delight 77% · anger.rage 75% · sadness.sorrow 73%
이미 다양한 화면      flutter.anticipation 18%(7책) · boredom.novelty 25%(9책)
                     anxiety.tension 36%(8책)
```

문제는 전역 비율이 아니라 **일곱 화면의 몰림**이다.

**★ 비시편 후보를 실제로 뒤졌다 — 적중 100건당 약 5건이 쓸 만하다**

```
탐색                    적중  쓸만함  왜 떨어지는가
calm.stable(평안·반석)    26     1    "평안히 가라"는 **작별 인사**다. 역대기의
                                    "그 땅이 평안하니라"는 정치적 평온이고,
                                    왕상 2:6은 사람을 죽이라는 지시다
calm.ease(안식·쉼)       14     0    14건 전부 **안식일 규례**(번제 일정·입번 순서).
                                    개역한글에서 `안식`은 압도적으로 율법 용어다
joy.delight             14    0~1   대상 16장은 우주적 찬양(시 96편 병행본)
sadness.sorrow          20    1~2   대부분 3인칭 서사. 아모스의 `애통`은 심판의 결과다
욥기 13장 이후            40    1~2   변론·죄책 다수. 13:14 자해 이미지,
                                    13:15 "내가 소망이 없노라"는 위기 인접
미사용 예언서 10권         26    1~2   나훔 1:7은 장 문맥에서 탈락(2.38절),
                                    미 7:8은 절이 "나의 대적이여"로 시작해 탈락
서신서 calm 재탐색        16     0    ★ 아래
```

**★ 서신서 16건이 한 건도 예외 없이 셋 중 하나였다**

```
편지 인사    "은혜와 평강이 네게 있을찌어다"  (딤전 1:2 · 골 1:2 · 벧전 5:14)
명령형 권면   "하라" · "옷입고" · "주장하게 하라" · "겸손하라" · "족한 줄로 알 것이니라"
교리 서술    "사랑은 진리와 함께 기뻐하고" (고전 13:6 — 감정이 아니라 사랑의 정의)
```

**명령형은 1~3차분에서 배제한 "질책·요구가 얹히는" 유형과 같다.**
서신서는 권면이 장르이고 율법서는 규례가 장르다.
**1인칭으로 자기 감정을 말하는 장르는 시편(과 애가·욥기 일부)뿐이다.**

> 즉 시편 몰림은 **정경의 장르 분포**다. 그리고 **몰림이 가장 심한 곳(평온 계열)이
> 대안이 가장 없는 곳**이다 — 우연이 아니다.

**⚠ 정정 — 대표 사례 둘은 이미 수록돼 있었다**

```
요 11:35 "예수께서 눈물을 흘리시더라"   → sadness.sorrow (수록됨)
왕상 19:7 (로뎀나무, 천사가 어루만짐)     → exhaustion.burnout (수록됨)
```

"복음서 서사·역사서 장면" 각도는 **이미 걷힌 상태**다. 남은 것은 삼상 1장(한나)
정도다. 서사 구절이 키워드 검색에 안 걸리는 것은 맞지만, 그 이유로 **놓쳐 있던
것이 아니라 이미 찾아져 있었다.**

**목표 비율 — 전역 비율을 목표로 삼지 말 것**

사용자는 전역 비율을 경험하지 않는다. 한 화면에 머물기 때문이다.

```
주 지표      세분류 시편 상한 70%
             달성 시 전역 57% → 55% (약 13건)
             ⚠ calm.stable(91%) · calm.ease(82%)는 **지금 근거로 달성 불가**다.
               비시편 대안이 사실상 없다 — 예외로 기록한다
현실적 상한   55%. 50%는 약 42건이 필요하고 "덜 맞는 구절"이 늘어난다
             (확장 250→299 때 확인한 그 지점)
더 나은 지표  같은 장 반복 30건. 이어서 읽기 때문에 무게가 커졌다 —
             boredom.dull에서 "다른 구절"을 네 번 눌러도 이어서 읽기는
             계속 시편 119편을 연다
```

**보류 — ①(비시편 추가)**
13건으로 57% → 55%면 체감이 거의 없다. ③(장 문맥, 2.38절)과 ②(같은 장 반복)를
하면서 자연히 비시편이 늘 것이므로 그 결과를 보고 다시 판단한다.

**도구** — 탐색 스크립트는 세션과 함께 사라졌다. 다시 만들 때의 요건:
수록 절·제외 색인·합본 구간을 제외하고, 길이 창(20~90자)으로 거르고,
**시편을 기본 제외**한 뒤 본문을 눈으로 읽는다. 적중을 세는 것으로는 판단이 안 된다.


### 2.40 같은 장 반복 보강 — 추가로 풀되, 절차 7번이 후보 하나를 잡았다 (2026-08-20)

2.38절에서 3건이 빠지며 `boredom.dull`이 하한(10) 미달인 9건이 됐다. 그 자리와
"같은 장 반복"을 **교체가 아니라 추가로** 풀었다.

**결과**

```
                         전 -> 후    시편          같은 장 반복
boredom.dull            9 -> 12   67% -> 50%   시편 119장 x4 (그대로)
sadness.sorrow         14 -> 15   71% -> 67%   시 31·42장 각 x2 (그대로)
frustration.suppressed 11 -> 12   64% -> 58%   없음
전체                  286 -> 291   57% -> 55.6%  하한 미달 0개
```

추가한 5건이 **전부 비시편**이라 [1](비시편 확대)이 부수적으로 진행됐다.

**추가한 것**

```
눅 10:23  [dull]       "너희의 보는 것을 보는 눈은 복이 있도다"
                       이 화면의 축을 가장 짧게 말한다 — 새 것을 요구하지 않고
                       **보는 쪽**을 바꾼다. 시 119:18이 요청이면 이것은 선언이다
사 40:8   [dull]       "풀은 마르고 꽃은 시드나… 말씀은 영영히 서리라"
신 32:7   [dull]       "옛날을 기억하라… 네 아비에게 물으라"
                       이 화면의 다른 축(옛 이야기를 다시 듣기). 렘 15:16·롬 15:4와
                       같은 축이되 **묻는 쪽**을 말해 겹치지 않는다
고후 1:4  [sorrow]     "모든 환난 중에 있는 자들을 능히 위로하게 하시는 이시라"
삼상 1:15 [suppressed] 한나 — "나는 마음이 슬픈 여자라… 나의 심정을 통한 것뿐이니이다"
                       억눌린 것을 쏟아내는 것이 취한 것으로 오해받는 자리다
```

**★ 절차 7번이 실제로 후보를 잡았다 — 고후 1:4**

기본(5절부터)이면 첫 페이지 4번째 줄이
`1:8 "힘에 지나도록 심한 고생을 받아 **살 소망까지** 끊어지고"`다. 위기 인접이다.
`read_from: 3`으로 옮기니 3-6절이 전부 위로 문맥이 됐다.

⚠ **게이트가 아니라 절차가 먼저 잡았다.** 게이트는 excluded 색인만 보므로 이 건은
걸리지 않았을 것이다. 절차와 게이트는 겹치는 것이 아니라 **서로 다른 것을 잡는다.**

**탈락시킨 것 — 근거를 남긴다**

```
롬 12:15 "우는 자들로 함께 울라"
  구절 자체는 슬픔 화면에 좋다. 그런데 첫 페이지(16-19절)가 전부 명령형이다 —
  "스스로 지혜 있는체 말라" · "악으로 악을 갚지 말고" · "원수를 갚지 말고".
  슬픔 화면에 요구가 연속으로 온다. 롬 12장 전체가 권면장이라 read_from도 없다.
  ⚠ 1층 차단 목록에 "요구"는 없다. 그래도 뺀 것은 **선정 원칙 1**(감정을
    판단·질책하지 않는다) 때문이다. 절차는 목록이 아니라 원칙으로도 판단한다.
눅 6:21 "이제 우는 자는 복이 있나니"  마 5:4(팔복 병행구)와 겹친다 (절차 2)
전 1:9  "해 아래는 새 것이 없나니"    지루한 사람에게 **체념을 확인시킨다.**
                                    이 화면의 방향은 "다시 보게 됨"이지 "역시 없다"가 아니다
히 4:12 "좌우에 날선 어떤 검보다도… 찔러 쪼개기까지"  부담이 크다
사 53:4 "우리의 질고를 지고"          뒷부분 "징벌을 받아서"가 같은 절 안에 있다 (절차 5)
```

**남은 것 — 정직하게**

```
시편 119장 x4 (boredom.dull)    12건 중 4건 = 33%. 9건일 때 44%에서 내려왔지만
                               **추가만으로는 못 푼다.** 20% 아래로 가려면 20건
                               규모가 필요한데 그만한 후보가 없다. 교체를 검토하려면
                               그 4건 중 약한 것을 가려야 한다
시 31·42장 x2 (sadness.sorrow)  15건 중 각 2건 = 13%. 눈에 띄는 수준은 아니다
나머지 중복 8화면               각 x2. 우선순위가 낮아 손대지 않았다
calm.ease 82% · calm.stable 91% 2.39절대로 **비시편 대안이 없다.** 예외로 둔다
```

**탐색에서 배운 것**

`열조`로 옛 이야기 축을 찾으려 했더니 202건 중 대부분이 왕조 기록의
"열조와 함께 잤다"(사망 관용구)였다. **개역한글의 어휘는 현대어 직관과 자주
어긋난다** — `안식`이 율법 용어였던 것(2.39절)과 같은 종류다.


### 2.41 ★ 비시편 확대의 한계선 — 네 화면은 못 채운다 (2026-08-20)

> 목표는 "시편 비율 55%"가 **아니다.** 시편 외의 구절을 늘리는 것이고 비율은
> 부수적 결과다. 그래서 55.6%에 도달한 뒤에도 멈추지 않고 남은 화면을 마저 뒤졌다.
> 이 절은 **그 결과 못 채운 것과 그 이유**다. 다시 묻지 않도록, 그리고 같은
> 재탐색을 반복하지 않도록 남긴다.

**세분류 시편 상한 70% — 최종 상태**

```
calm.stable   91%  +4 필요   ⛔ 달성 불가
calm.ease     82%  +2 필요   ⛔ 달성 불가
joy.delight   77%  +2 필요   ⛔ 달성 불가   ← 이번에 확인
joy.grateful  75%  +2 필요   ⛔ 달성 불가   ← 이번에 확인
anger.rage    75% → 69%      ✔ 해소 (사 57:16)
```

**채운 것 2건**

```
딤전 4:4  [grateful]  "지으신 모든 것이 선하매 감사함으로 받으면 버릴 것이 없나니"
                      일상의 것을 **받는 태도**. 기존이 "감사하라"(명령)와
                      "내가 감사하오며"(고백)뿐이라 겹치지 않는다
사 57:16  [rage]      "내가 영원히는 다투지 아니하며… 그 영과 혼이 내 앞에서
                      **곤비할까** 함이니라" — 하나님이 노를 거두시는 **이유가
                      "네가 지칠까 봐"다.** 시 103:9가 선언이면 이것은 이유다
                      ⚠ read_from: 18 — 기본(17절)은 첫 줄이 "그의 탐심의 죄악을
                        인하여"로 죄를 지목한다 (선정 원칙 1)
```

---

**★ 품질 때문에 못 넣은 15건 — 근거를 남긴다**

⚠ **"비율 목표를 채웠으니 불필요"가 아니다.** 전부 절차 1~7 중 어딘가에 걸렸다.
아래를 다시 후보로 올리려면 **그 사유가 왜 더 이상 유효하지 않은지**를 먼저 적을 것.

```
[번영신학 인접 — 4차분 제외 기준]
  고후 9:15  grateful  구절은 좋다("말할 수 없는 그의 은사를 인하여 감사하노라").
                       그러나 고린도후서 9장 **전체가 연보 독려**이고 read_from
                       후보 자리(8-11절)도 "풍성하게" · "부요하여 너그럽게"다

[지배 어휘 겹침 — 절차 4]
  살전 3:9   grateful  "어떠한 감사함으로 하나님께 **보답할꼬**"
                       시 116:12 "무엇으로 보답할꼬"와 지배 어휘가 같다
  대상 13:8  delight   "**힘을 다하여** 뛰놀며" — 힘을 다하여는 flutter.thrill의
                       지배 어휘다(4차분 확정 규칙 3). 게다가 9-10절이 **웃사의 죽음**

[기존 수록과 중복 — 절차 2]
  골 3:16    grateful  골 3:17이 이미 수록 — 같은 장
  행 2:28    delight   이미 수록된 **시 16:11의 인용**이다. 사실상 같은 문장
  눅 6:21    sorrow    마 5:4(팔복 병행구)와 같은 말

[같은 장 중복을 새로 만든다 — 이번 작업(2.40)의 취지와 정반대]
  렘 31:13   delight   렘 31:25가 exhaustion.burnout에 있다
  사 12:3    delight   이사야 12장이 flutter.thrill에 2건 있다

[절 단위 함정 — 절차 5]
  느 8:10    delight   109자인데 핵심("여호와를 기뻐하는 것이 너희의 힘이니라")이
                       **맨 끝**이고 첫 20자가 "느헤미야가 또 이르기를"이다.
                       핵심만 쓰면 proof-texting, 전체를 쓰면 앞이 길고 밋밋하다
  민 14:18   rage      "여호와는 노하기를 더디하고 인자가 많아" 뒤에 **같은 절 안에서**
                       "형벌 받을 자는 결단코 사하지 아니하고… 삼사대까지"가 온다
  사 53:4    sorrow    "우리의 질고를 지고" 뒤에 "**징벌**을 받아서"가 같은 절 안에

[질책·요구가 얹힌다 — 선정 원칙 1]
  잠 14:29 · 15:18 · 16:32 · 19:11   rage
                       전부 **사람에게** "노하기를 더디 하라"고 요구한다.
                       격분한 사람에게 내밀 수 없다. (위 4건을 1건으로 센다)
  욜 2:13    rage      첫 20자가 "너희는 옷을 찢지 말고 마음을 찢고" — 회개 요구
  느 9:31    rage      구절은 좋으나 첫 페이지 32-35절이 **죄 고백 연속**이다
                       ("우리는 악을 행하였사오나" · "악행을 그치지 아니한")
  히 13:15   grateful  첫 페이지 16-19절이 **전부 명령형**
                       ("잊지 말라" · "순종하고 복종하라" · "기도하라")
  롬 12:15   sorrow    같은 유형. 16-19절이 "스스로 지혜 있는체 말라" ·
                       "악으로 악을 갚지 말고" · "원수를 갚지 말고"

[장 문맥 — 절차 7]
  요 15:11   delight   13절 "사람이 친구를 위하여 **자기 목숨을 버리면**"이
                       즐거움 화면 첫 페이지에 온다
  눅 15:32   delight   장 마지막 절이라 1절부터 열리는데 2절이
                       "바리새인과 서기관들이 **원망하여**… 죄인을 영접하고"
```

---

**★ 왜 이 네 화면만 못 채우는가 — 구조적이다**

2.39절이 서신서·율법서에서 찾은 것과 같은 결론이 **감정 축별로** 다시 나왔다.

```
calm.ease     개역한글에서 `안식`은 압도적으로 **율법 규례 용어**다
              (번제 일정 · 입번 순서). 감정으로서의 쉼은 시편과 마 11:28뿐
calm.stable   "평안히 가라"는 **작별 인사**이고 역대기의 "그 땅이 평안하니라"는
              정치적 평온이다
joy.delight   몸짓 축(뛰놀며 · 춤추며)의 비시편 구절이 전부
              **웃사의 죽음 · 미갈의 업신여김 · 금송아지 · 심판** 문맥이거나
              같은 장 중복을 만든다. 성경에서 개인이 기뻐 뛰는 장면은
              대개 **그 뒤에 사건이 따라온다**
joy.grateful  감사는 서신서에 많지만 형태가 둘뿐이다 —
              **명령형**("감사하라")이거나 **연보 문맥**이다.
              1인칭 감사 고백은 시편의 장르다
```

> **이 넷은 예외로 둔다.** calm 두 화면과 같은 자리다. "채우지 못했다"가 아니라
> **"채울 것이 없다"** 이고, 억지로 채우면 위 15건 중 하나를 넣는 것이 된다.

**다시 열 조건**

```
· 3차 채널 발굴처럼 **구절 쪽에도 새 소스**가 생길 때 (다른 번역본은 불가 —
  개역한글 고정이 PLAN.md 1절 확정 사항이다)
· 세분류 축 자체를 다시 볼 때. 예컨대 joy.delight의 축을 "몸짓"에서 넓히면
  후보가 생기지만, 그러면 flutter.thrill과의 경계(4차분 확정 규칙 3)가 무너진다
· ⛔ "비율이 아직 55%대다"는 다시 열 조건이 **아니다.** 비율은 결과이지 목표가 아니다
```

**최종 상태**

```
감정 구절 293 (289 → 286 → 291 → 293)
태깅 306 · 시편 169 (55.2%) · 비시편 137
70% 초과 4화면 — 전부 위 사유로 예외 처리
```


### 2.42 ★ cron 대조 — 재현이 배포본과 어긋났고, 원인은 도구가 아니라 코퍼스였다 (2026-08-20)

[0]~[7] 전부를 반영한 첫 실 배치다. **예약 09:30 UTC, 실제 시작 10:01:32Z (31분 지연).**

**대조 결과 — 어긋났다**

```
                     로컬 재현        실 배치         차이
총 노출               475건           474건          -1
  주제분 / 폴백        331 / 144       326 / 148      주제분 -5
폴백 비율             30.3%           31.2%          +0.9%p
20건 달성             22/24           21/24          -1
폴백 과다             4건             4건            동일
성립 불가             0건             0건            동일
```

화면 5개가 달랐다.

```
boredom.novelty     주제 8→7 · 계 20→19   ← 유일하게 총량이 줄었다
anger.irritation    주제 12→10 (폴백 8→10)
exhaustion.tired    주제 12→10 (폴백 8→10)
calm.ease           주제 13→12 (폴백 7→8)
joy.grateful        주제 16→17 (폴백 4→3)   ← 유일하게 늘었다
```

**원인 — 두 후보를 갈랐다**

```
① 회전 시드(day_of_year)   로컬은 231(어제), 실 배치는 232
   → `--screens --day 232`로 다시 돌렸더니 **475 / 331+144로 동일**했다.
     시드는 원인이 아니다.
② 코퍼스                   ★ 이것이다
   로컬 dist/titles.json  1,067건 (2026-08-19 11:16 덤프)
   실 배치 확보량          1,068건 (오늘 새로 수집)
   배치는 매일 채널별 최근 100건을 새로 걷는다. **새 영상이 올라오면 오래된
   것이 밀려나고, 밀려난 자리에 따라 주제 태깅 결과가 달라진다.**
```

**★ 그래서 2.23절의 "재현이 배포본과 일치했다"를 다시 읽어야 한다**

그때 일치한 이유는 재현 도구가 정확해서만이 아니라 **같은 날 같은 코퍼스**를
썼기 때문이다. 하루가 지나면 코퍼스가 바뀌고, 그러면 재현도 어긋난다.

```
⚠ retag_titles.py --screens의 유효 범위
   같은 날(코퍼스가 같을 때)  선정·폴백 로직 검증에 정확하다
   다른 날                    ±1% 수준의 오차가 정상이다.
                              **사전·채널 변경의 효과를 재는 데는 여전히 유효하다** —
                              변경 전후를 같은 titles.json으로 비교하면 되기 때문이다
   ⛔ 절대 수치를 배포본과 맞추는 용도로 쓰지 말 것
```

**판정 — 이 차이는 결함이 아니다**

주제분 -5(1.5%)는 하루치 업로드가 만드는 변동 폭 안이다. 근거:
폴백 과다 4건과 성립 불가 0건이 **정확히 같고**, 화면별 순위도 거의 같다.
로직이 달랐다면 이 둘이 먼저 흔들린다.

**함께 확인한 것**

```
태깅          확보 1,068건 중 282건 태깅 / 786건 미태깅(74%) · 복수 태깅 31건
폴백 품질 필터  공고·행사·홍보 20건 제외 — [1]이 실 배치에서 동작한다
위기          "확보량 0건 < 최소 12건 — 필터를 완화하지 않고 직전 결과를 유지한다"
              → 설계대로 동작. crisis_eligible 승인이 0개라 예상된 결과다
경보          quiet_worship 10건 < 하한 15 · sermon 0건
              daily_word worship 0건
              → 3차 채널 발굴이 필요한 지점이 실 배치에서 다시 확인됐다
쿼터          여유 9,436 units (일일 상한 9,500 대비 64 소모)
```

**★ 3차 발굴 목표가 실 배치로 재확인됐다**

```
self_control    0건 (풀 0)      · rest 0건 (풀 0)
quiet_worship  10건 (풀 10)     하한 15 미달 · sermon 0건
bible_story     4건 (풀 4)
forgiveness     1건 · guidance 1건 · courage 2건 · justice 2건
daily_word     15건 (풀 164)    ← 풀은 두꺼운데 채널 상한 5로 15건만 나간다
```

⚠ `daily_word`가 풀 164인데 15건만 나가는 것은 **채널 상한(5)** 때문이다.
공급 부족이 아니라 분산 규칙의 결과다. 3차 발굴에서 이 항목을 "부족"으로
착각하지 말 것.


---

### 2.43 ★ 같은 수치가 두 곳에 있었고 갈라졌다 — 생성물로 바꿨다 (2026-08-24)

세션을 열고 상태를 보고하는데 1절과 3절의 숫자가 서로 달랐다. 세 건을 지목받았고,
실측하다 **지목되지 않은 네 번째**가 나왔다.

```
                 1절                    3절          실측 (2026-08-24)
장 본문     174개 장 4,283절         178개 장        178개 장 4,442절 (39권)
앱 테스트    105건 (+Phase 3줄 61건)  110건          110건
quiet_worship 12                     10 (풀 10)     ★ 둘 다 맞다 — 아래
구절        293 (감정 283 + 위기 10)  같은 값         감정 293 + 위기 10 = 303
```

**★ 구절은 양쪽이 똑같이 틀렸다.** 한쪽만 갱신하고 다른 쪽을 잊은 것이 아니라,
`verses.yaml`의 `verses:` 키 길이(감정 293)를 **총계로 오인해 적은 값**이 그대로
복사돼 두 곳에 남았다. `crisis:` 10건이 총계에서 빠져 있었고, 그 상태로
"감정 283"이라는 있지도 않은 내역이 만들어졌다.

⚠ 그래서 "3절이 최신으로 보인다"는 추정은 **근거가 약한 정도가 아니라 틀렸다.**
장 본문·앱 테스트는 3절이 맞았지만 구절은 양쪽이 틀렸다. 문서만 읽어서는
가릴 수 없었다 — **추정이 필요했다는 것 자체가 결함이다.**

**왜 갈라졌나 — 원인이 셋이고 성격이 다르다**

```
① 손으로 두 벌 적었다        고치는 사람이 한쪽만 고친다. 가장 흔한 유형
② 이름은 같은데 다른 것을 쟀다  ★ quiet_worship이 이 유형이다
                            "풀"이라는 한 단어가 두 측정을 가리키고 있었다
                              로컬 12  retag_titles.py · dist/titles.json 1,067건(08-19 덤프)
                              배치 10  build_videos.py · 그날 새로 걷은 1,068건
                            도구도 코퍼스도 다르다. 어느 쪽도 틀리지 않았고,
                            **같은 이름을 쓴 것이 틀렸다**
③ 애초에 잘못 적힌 값이 복사됐다  구절 303이 이 유형. 사본을 늘리면 오류도 복제된다
```

**고친 방식 — ①③은 생성으로, ②는 분리로**

`scripts/stats.py`를 만들었다. 저장소 파일만 읽어 규모를 재고, HANDOFF의
표시 구간(HTML 주석 `STATS:BEGIN 규모` ~ `STATS:END`)을 채운다. 1절과 3절이 **같은 측정에서
렌더된 같은 블록**이라 갈라질 수 없다.

```
python scripts/stats.py            실측 표를 출력한다
python scripts/stats.py --write    HANDOFF.md의 표시 구간을 갱신한다
python scripts/stats.py --check    문서와 실측이 다르면 종료 코드 1  ← 게이트에 넣었다
```

**★ 무엇을 넣지 않을지가 더 중요했다.**

```
넣는다      저장소 파일만 읽으면 언제나 같은 답이 나오는 것
           구절 · 장 본문 · 어휘 · 채널 · 테스트 수
넣지 않는다  코퍼스에 따라 매일 달라지는 것
           주제별 풀 크기 · 폴백 비율 · 총 노출
           이런 값은 **적는 순간 낡는다.** stats.py에 넣으면 --check가
           매일 실패하는 게이트가 되어 아무도 안 보게 된다
```

코퍼스 의존 수치는 숫자를 지우는 대신 **출처를 붙였다** — 어느 도구가, 어느
코퍼스로, 언제 쟀는지. 1절의 주제 풀 표와 3절의 발굴 목표 블록이 그렇게 바뀌었다.

**배포본이 풀 크기를 남기게 했다** (`lib/report.py`)

`quiet_worship`이 지금 배포본에서 몇인지 확인하려다 알았다 — **되찾을 수 없었다.**
`diagnostics.json`은 `themes_low_yield: ["quiet_worship", …]`처럼 **경보 목록만**
들고 있어서 "얇다"는 알지만 "얼마나"는 버려진다. 그래서 추가했다.

```
theme_pool    {주제: 풀 크기}    ← 3차 발굴의 판단 근거가 되는 값
theme_picked  {주제: 선정 건수}
```

주제가 24개뿐이라 파일이 커지지 않는다. 주간 비교 스크립트는 키를 하나씩
기본값과 함께 읽으므로 **옛 기준선과도 호환된다**(build.yml 변경 없음).

⚠ 다음 배치부터 남는다. 그 전까지는 로컬 재현값을 쓰되 코퍼스를 밝힐 것.

**만성인지 확인했다** — 2026-08-23 run 8(kept 1,071) 배포본 진단에서도
`rest`·`self_control`은 비어 있고 `quiet_worship`은 저수확 + 형식 공백에
그대로 남아 있다. 사흘 뒤에도 같으므로 **하루 변동이 아니라 만성이다.**
3차 발굴 목표는 유효하다.

**게이트에 들어갔다** — `stats.py --check`가 파이썬 게이트의 여섯 번째다.
이제 문서와 실측이 갈라지면 **다음 세션이 아니라 게이트가 먼저 안다.**

---

### 2.44 ★ [6] 동점 처리 — 보류 판단이 틀렸다 (2026-08-24)

**⚠ 먼저 적어야 할 것 — 이 항목을 보류한 판단이 틀렸다.**

3절 보류 표에 이렇게 적혀 있었다.

```
[6] 동점 처리 (11번)  필요성 급감. 토글 주제분 0건 화면 4→0
                     다시 열 조건: 토글을 눌렀을 때 빈 화면 제보
```

`anger.unfair`가 `media_defaults` 문제로 밝혀졌고 **지표(0건 화면 수)가 4에서
0으로 떨어졌으므로** 필요성이 급감했다고 봤다. 그런데 실사용에서는
**다른 화면에서 계속 보였다.** "말씀 탭에 찬양 영상이 계속 보인다"가 제보 문구다.

```
무엇이 틀렸나   0건 화면 수는 "토글이 비었는가"만 센다.
              **토글 안에 엉뚱한 것이 들어 있는가는 세지 않는다.**
              찬양 콘티가 sermon으로 판정되면 말씀 탭은 비지 않는다 — 차 있다.
              내용이 틀렸을 뿐이다. 지표는 이걸 볼 수 없는 자리에 있었다.
교훈          **지표가 아니라 화면으로 판단했어야 했다.**
              다시 열 조건을 "빈 화면 제보"로 잡은 것도 같은 실수다 —
              증상이 빈 화면이 아니었으므로 그 조건은 영영 안 걸린다.
⚠ 다음에도   보류 사유가 "지표가 좋아졌다"일 때는, 그 지표가 증상을 볼 수 있는
              자리에 있는지 먼저 확인할 것. 못 보는 지표는 좋아져도 의미가 없다.
```

**원인 — "제목이 채널보다 앞"이 동점 구간에서만 무효였다**

`classify_media_type`은 제목 어휘가 **한쪽만** 걸릴 때 확정한다. 양쪽이 걸리면
제목 증거를 **통째로 버리고** 장절 → 채널 → 길이로 내려간다. 그 아래는 전부
sermon 쪽으로 기운다(오륜교회·꿈의교회·우리들교회가 sermon 채널이다).

동점을 만든 sermon 어휘가 둘 다 형식과 무관했다.

```
곡명 안의 말씀   "주의 약속하신 말씀 위에서" · "말씀하소서" · "말씀이 육신 되어"
                → 곡명은 가사 제목이지 "이 영상이 무엇인가"가 아니다
예배명          "금요기도회" · "저녁예배"
                → 예배명은 **때**이지 형식이 아니다
```

2026-08-19 개정이 "제목이 채널보다 앞"으로 뒤집어 찬양 81건을 살렸는데,
**그 개정이 동점 구간에는 닿지 않았다.**

**고친 방식 — 곡명 구간과 그 외를 나눠 판정한다**

`retag_titles.py --songs`가 쓰던 곡명 분해를 `lib/tagging.py`로 옮기고
(두 벌을 두면 한쪽만 고쳐져 조용히 갈라진다) 동점 구간에서 재사용한다.

```
1  제목을 (곡명, 그 외)로 가른다. "그 외" = 앞머리 대괄호 표지 + 세로줄 뒤 + 비곡명 조각
2  곡명이 2개 미만이면 **손대지 않는다** ← 가드 ①
3  "그 외"에서만 형식 어휘를 다시 센다. 한쪽만 남으면 그것으로 확정
4  "그 외"에서도 동점이면(예: "금요기도회 찬양") worship — 곡명 나열 구조가 증거다
   ⚠ worship 어휘가 **남아 있을 때만** 준다 ← 가드 ②
```

**가드 두 개가 각각 다른 정탐을 지킨다 — 없으면 둘 다 뒤집힌다**

```
가드 ①  곡명 1개는 콘티가 아니다
        "[LIVE] W워십ㅣ… 저녁예배 _ 냄새는 감출 수 없다ㅣ김학중 목사 설교 잠언 강해"
        "[사역자설교] 울림 웬즈데이 워십 | 사무엘하 19:31-39 - 최대규 목사"
        "[생명의 삶 큐티] 찬양의 이유, 영원한 인자하심 |시편 117:1~118:7| 김상수 목사"
        셋 다 **실제 설교**이고 worship 어휘는 프로그램명·설교 제목에서 왔다

가드 ②  ★ 곡명 2개 이상이라고 콘티인 것은 아니다
        우리들교회 사역자설교는 /를 메타데이터 구분자로 쓴다 (실측 18건)
          "[사역자설교] 성경을 이루려 함이니라 / 마가복음 14:43-52 - 임대선 목사
           / 정의학 초원지기 | 2026.08.05"
        곡명이 2~3개로 잡히지만 "그 외"에 sermon("설교")만 남아 sermon으로 확정된다
        ⚠ **회복된 24건 중 5건이 같은 채널(우리들교회)이다.** 한 채널이 두 형식을
          올리므로 채널로는 못 가르고, 이 가드가 유일하게 가르는 자리다
```

**실측 — 24건 회복, 전부 sermon → worship** (`dist/titles.json` 1,067건)

```
동점            31건
  회복          24건   전부 sermon → worship
  손대지 않음     7건   곡명 1개(가드 ①) 5건 · 이미 worship 2건

형식 분포   sermon 730 → 706 (-24) · worship 265 → 289 (+24) · unknown 72 (변화 없음)
회복 채널   오륜교회 13 · 꿈의교회 6 · 우리들교회 5
```

**★ 24건 전부가 개정 전에 `channel` 근거로 판정되고 있었다.** 제보의 진단이
정확했다 — 무력화된 것이 정확히 "제목이 채널보다 앞"이었다.

⚠ **화면 조립(폴백·노출)에는 영향이 없다.** `--screens` 총량이 475 / 331+144로
그대로다. 형식은 토글 축이고 주제는 선정 축이라 서로 독립이다.
**바뀐 것은 "말씀 탭을 눌렀을 때 무엇이 보이는가" 하나다.**

**도구가 개정을 못 보고 있었다 — 함께 고쳤다**

개정을 넣고 `retag_titles.py`를 돌렸는데 `[형식별]`이 개정 전 수치를 그대로 찍었다.
주제는 재태깅하면서 **형식만 덤프에 저장된 값을 읽고 있었다.**

```
고친 것  retag()가 형식도 다시 판별하고, 덤프 대비 변화를 보여준다
        [형식별]  덤프 -> 재판별
          sermon     730 ->   706  -24
          worship    265 ->   289  +24
          형식이 바뀐 제목 24건 — 새 판정 근거: conti 24
⚠ 함정   덤프에 channelContentType이 없다. allowlist에서 channelId로 채워야
        채널 단계가 살아나 배치와 같은 결과가 나온다.
        채우지 않으면 sermon이 730이 아니라 671로 보인다(실측).
        이걸 모르고 재면 "개정 효과 59건"이라는 틀린 수를 얻는다
```

**회귀 테스트 6건** (`tagging_test.py` MEDIA_CASES) — 회복 3유형(예배명·곡명 안
말씀·슬래시 콘티)과 가드 3유형(W워십·사역자설교·큐티 찬양)을 각각 고정했다.
가드 쪽이 없으면 규칙을 넓혔을 때 정탐이 조용히 뒤집힌다.

**주석 하나를 고쳤다 — 이 저장소에서 네 번째다** (2.21절 계열)

`_NOT_A_SONG`에 "이 목록은 곡명 **통계**만 정리한다. 여기서 무엇을 빼든 실제
태깅 결과는 달라지지 않는다"고 적혀 있었다. 그 말은 **이제 틀리다** — 같은
분해가 형식 판정에 쓰이므로 이 정규식을 고치면 판정이 움직인다. 옮기면서 고쳤다.

---

### 2.45 3차 발굴 준비 — 검색어 세트와 위기 후보 표시 (2026-08-24)

발굴은 아직 **실행하지 않았다.** 세트 정의와 시트 개정만 끝냈다.

**검색어 세트 `theme` 추가 — 갈래가 앞의 둘과 다르다**

1·2차는 "좋은 채널"을 찾았고 승인 15개를 얻었다. 그런데 실 배치에서 비는 자리는
채널의 품질이 아니라 **주제의 공급**이었다. 그래서 3차는 "어떤 채널인가"가 아니라
**"무엇을 올리는 채널인가"**로 짰다.

```
분노 조절 설교      self_control (풀 0)  화·미움을 **사람의 문제**로 다루는 설교
기독교 상담         self_control · rest  상담·심리 결 사역 채널 (3절 10번 1순위 메모)
쉼과 회복 말씀       rest (풀 0)          ⚠ `안식`을 안 쓴다 — 검색에서도 안식일이 온다
찬양대 찬양         quiet_worship        ★ 아래 참조
찬양 피아노 연주     quiet_worship        Instrumental 갈래
성경 인물 강해       bible_story (풀 4)   1차 '성경 강해'보다 좁힌다
C채널              기관형 잔여           2차에서 6종 제한으로 빠진 후보
```

**★ "성가대"가 아니라 "찬양대"다 — 내가 처음에 틀렸다**

초안은 `성가대 찬양`이었다. 그런데 **같은 줄의 주석에는 "채널 제목 관행이
'찬양대'"라고 적어 놓고** 검색어만 성가대로 썼다. 지적을 받고 실측했다.

```
찬양대   6건   전부 꿈의교회 — "꿈의교회, 희망의 찬양대_거룩하시다"
성가대   0건
```

한국 개신교는 압도적으로 **찬양대**를 쓴다. "성가대"는 가톨릭 쪽이거나 오래된
표현이다. [2] 실측(2.28절)에서 잡힌 것도 "희망의 찬양대"였다.

⚠ 교훈은 어휘가 아니라 **절차**다. 주석에 관행을 적어 두고 검색어를 다르게 쓴
것이라, 근거와 실행이 같은 줄에서 어긋났다. **검색어를 정할 때는 그 어휘가
실측 코퍼스에 몇 건 있는지부터 셀 것** — `retag_titles.py --probe`가 그 도구다.

**위기 후보 표시를 시트에 넣었다 (`CRISIS_SIGNAL_TERMS`)**

승인 15개가 전부 `crisis_eligible: false`라 위기 화면에 영상이 0건이다(3절 9번).
설계대로 동작하는 상태지만 **채널을 따로 찾으러 나가는 일**이 과제로 남아 있었다.
3차의 "기독교 상담" 검색어가 어차피 그 갈래를 데려오므로, **지나가는 김에**
표시해 둔다. 나중에 같은 검색을 다시 돌리지 않아도 되게 하는 것이 목적이다.

```
표시되는 곳   요약 표의 `위기 후보` 열     "3건 (상담, 불안장애, 우울)"
             YAML 블록 앞 주석          ★ 위기 후보 표시 — …
어휘         상담 · 심리 · 정신건강 · 마음치유 · 우울 · 불안장애
             자존감 · 트라우마 · 중독 · 회복탄력
```

★ **`SIGNAL_TERMS`와 방향이 반대라 딕셔너리를 나눴다.** 기존 신호어는
"이건 좀 봐야 한다"는 **주의** 표시다. 위기 후보는 반대로 "이건 쓸 수 있을지도
모른다"는 표시다. 한 딕셔너리에 섞으면 시트의 `성격 신호` 열에서 둘이 같은
얼굴로 나와 **의미가 뒤집힌다.**

⚠ 표시일 뿐 승인이 아니다. `crisis_eligible` 검토는 일반 승인보다 엄격하다.
⚠ 오탐을 전제로 읽는다. "불안"·"우울"은 일반 설교 제목에도 흔하다. 그래서
  자동 판정에 쓰지 않고 세어서 보여주기만 한다 — 신호어를 탈락 조건으로 뒀다면
  2차에서 큐티 채널과 매일기도 채널을 모두 잃었을 것이라는 사례가 그대로 적용된다.

**★ 배치 재실행은 발굴·승인이 끝난 뒤 한 번에 한다**

[6] 동점 처리(2.44절)는 **코드만 고쳤고 데이터는 그대로다.**

```
배포본 videos.json   2026-08-23 run 8 실행분 — 그때의 media_type 판정이 들어 있다
실측                 고유 158건 중 **11건**이 말씀 탭에 남아 있는 찬양 콘티
                    오륜교회 9 · 우리들교회 2
```

지금 앱에서 말씀 탭에 찬양이 보이는 것은 **개정이 안 먹은 것이 아니라
데이터가 아직 안 바뀐 것이다.** 채널이 추가되면 어차피 배치를 다시 돌려야 하므로
그때 함께 반영한다. 발굴 → 승인 → 배치 한 번으로 끝낸다.

⚠ 이 순서를 바꿔 지금 배치를 돌리면 쿼터만 쓰고 같은 일을 두 번 한다.
⚠ 재실행 뒤에 확인할 것: 위 11건이 찬양 탭으로 옮겨졌는가.
  배포본 `data/videos.json`에서 `media_type`을 직접 세면 된다.

---

### 2.46 ★ 3차 발굴 실행 — 통과 22개인데 목표는 대부분 안 찼다 (2026-08-24)

```
실행       python scripts/suggest_channels.py --query-set theme
소모       761 units (오늘 누적 761 / 일일 상한 9,500)
수확       영상 344건 → 채널 141개 집계 → 상위 30개 조사
판정       자동 점검 통과 22 · 실격 6 · 이미 등록 2 (CBSJOY · 꿈의교회)
기검토 제외  4개를 건너뛰었고 그만큼 새 후보가 상위 N에 들어왔다
시트       channel_candidates.theme.20260824-014724.md (.gitignore 대상 — 로컬에만 있다)
```

**★ 통과 22개를 수확으로 읽으면 안 된다.** 목표별로 보면 이렇다.

```
self_control   ⛔ 0건        1순위였는데 아무것도 못 건졌다
rest           △ 1개        채널명 접두어 문제가 있다 — 아래
quiet_worship  △ 4개 중 1개  나머지 3개는 **지금 사전으로 태깅되지 않는다**
bible_story    △ 후보 있음   C채널 계열은 태깅되지 않는다
위기 후보       ⛔ 표시는 됐으나 셋 다 목적에 안 맞는다
```

**★ self_control — 검색이 아니라 대상이 없다**

```
후보 30개의 최근 영상 제목 330건 중
  분노 · 미움 · 원망 · 용서   0건
  감정                      1건 ("회복의 순서는 몸 - 감정 - 생각", 29초 쇼츠)
검색어 "분노 조절 설교"가 반환한 영상도 44건뿐이다 (나머지 6개는 전부 50건)
```

2.28절이 "어휘 문제가 아니라 물량 문제"라고 했는데, **발굴에서도 같은 답이 나왔다.**
한국 개신교 유튜브에 "화를 다스리는 법"을 정면으로 다루는 채널이 얇다.

⚠ 다음에 시도할 것은 검색어 미세조정이 아니다. 갈래를 바꿔야 한다 —
  가정·부부·자녀 갈등 상담 사역, 회복 프로그램(중독·분노), 기독교 심리상담 학회 계열.
⛔ "분노"류 단어를 더 넣어 보는 시도는 하지 말 것. 이번에 44건이 나온 이유가
   단어 선택이 아니라 콘텐츠 자체의 희소성이다.

**★ rest 후보 — 채널명 접두어가 사전을 통째로 속인다**

`쉼과 회복이 있는 교회` (구독 856 · 영상 1,994 · 매일 업로드 · 쇼츠 0%)는
자동 점검을 1위로 통과했다. 그런데 **모든 제목에 `[쉼과회복]` 접두어가 붙는다.**

```
[쉼과회복] 주일예배 라이브 - 2026.08.23 (3부)        → rest, renewal
[쉼과회복] 수요 성경강좌 '창세기 시리즈(8)'           → rest, renewal
[쉼과회복] 온가족 연합 찬양예배 2026.08.16           → rest, renewal
```

`rest` 풀 0이 단번에 메워지지만 **내용이 쉼과 무관하다.** 지친 사용자가
"주일예배 라이브 3부(1시간 7분)"를 받는다. `renewal`까지 함께 오염된다.

⚠ 이것은 채널의 잘못이 아니라 **우리 사전이 채널명을 콘텐츠로 착각하는 것**이다.
  ★ **2.29절 오탐 목록의 다섯 번째 유형으로 등재했다** — `채널명 접두어`.
  앞의 넷은 전부 제목 안의 말이 문제였는데, 이것은 채널이 자기 이름을 제목마다
  붙인 것이라 **제목만 봐서는 오탐인지 알 수 없다.** 채널명과 대조해야 갈린다.
⛔ 풀 0이 메워진다는 이유로 승인하지 말 것. 숫자는 좋아지고 화면은 나빠진다.
→ 판정과 방어 구현은 2.47절. **채널은 승인하지 않았고, 방어는 별도로 넣었다.**

**★ quiet_worship — 후보는 나왔는데 사전이 못 잡는다**

```
Worship Flow 워십플로우   ✓ quiet_worship   "Piano Instrumental"이 걸린다
택피아노 TAEK PIANO       ✗ gratitude만     "피아노 찬양"은 사전에 없다
mini Music 미니뮤직       ✗ 미태깅          "연주 찬양"은 사전에 없다 (`연주곡`뿐)
Sound of LIFE            ✗ 미태깅          "광주교회 주일찬양"
```

현재 사전은 `찬양대 · 연주곡 · Instrumental · CCM PIANO · 피아노ver`다.
2.28절에서 **기존 두 채널의 관행에 맞춰** 고친 것이라, 새 채널의 관행을 못 받는다.

후보 어휘를 실측했다 (`--probe`, API 0).

```
"피아노 찬양"   현재 코퍼스 적중 0건   → 오탐 위험 없음. 새 채널이 들어와야 값이 생긴다
"연주 찬양"     현재 코퍼스 적중 0건   → 같음
"주일찬양"     적중 14건 · 순증 11건  ⛔ **넣지 말 것**
              전부 우리들교회 회중 찬양 콘티다. quiet_worship은 잔잔한 연주 찬양이지
              회중 찬양이 아니다. 풀은 늘지만 화면의 뜻이 달라진다
```

**★ 위기 후보 — 표시는 작동했고, 셋 다 목적에 안 맞았다**

2.45절에서 넣은 `위기 후보` 열이 실제로 3개 채널을 잡았다. 기계는 동작했다.

```
상처입은상담사_엘로이   9건 (상담·심리)  ⛔ **상담사 직업 채널**이다 —
                                    자격증 · 센터 창업 · 내담자 응대 요령.
                                    위기의 사람이 아니라 상담사가 대상이다.
                                    기독교 사역 채널인지도 확인되지 않는다
더메시지랩              1건 (중독)      ⛔ 조정민 목사 설교 채널. "헛된 가치들에
                                    중독되는 성도" 쇼츠에서 온 오탐이다
써니데이 SunnyDay      5건 (상담·심리)  ⛔ 146일 업로드 없음으로 실격
```

⚠ **표시 기능의 실패가 아니다.** 어휘는 정확히 걸렸고, 걸린 채널이 목적에 안 맞았을
  뿐이다. 자동 판정으로 뒀다면 상담사 직업 채널이 위기 화면에 들어갔을 것이다 —
  **표시로 두고 사람이 읽게 한 설계가 여기서 값을 했다**(SIGNAL_TERMS와 같은 사례).
⚠ 위기 채널은 여전히 0개다. 3절 9번은 그대로 열려 있다.

**실격 6개** — 전부 객관 기준이고 판단이 아니다.

```
90일 내 업로드 없음   성경시대(140일) · 성경인물백과(356일) · 써니데이(146일)
쇼츠 제외 2건 미만   Christian BGM(1건) · 숭실사이버대(0건) · 에녹일기(0건)
```

⚠ `Christian BGM`은 아깝다 — quiet_worship 갈래인데 쇼츠 90%라 걸렸다.
  긴 영상을 올리기 시작하면 다시 볼 것.

**다음 세션이 판단할 것 — 승인은 아직 하나도 하지 않았다**

```
1  quiet_worship 사전에 "피아노 찬양"·"연주 찬양"을 넣을 것인가
   → 넣어야 택피아노·미니뮤직이 값을 한다. 현재 오탐 0건으로 실측됨
2  `쉼과 회복이 있는 교회`를 승인할 것인가
   → 승인하려면 대괄호 접두어 방어가 사전 쪽에 먼저 있어야 한다
3  C채널 계열 3개(TV·굿데이·설교)의 교단/소속 확인
   → C채널방송:설교는 쇼츠 0%·10/10로 자동 점검이 가장 깨끗하다
4  self_control은 이번 갈래로는 안 된다 — 다음 발굴의 갈래를 다시 짤 것
```

⚠ 시트의 `교단/소속 확인` 열과 `affiliation` 필드가 비어 있다. **이단 규정 대조
  전에는 승인하지 않는다**(기준 1a/1b/1c · 2.9·2.11절). 자동 점검 통과는
  승인이 아니라 "사람이 볼 가치가 있다"는 뜻이다.

---

### 2.47 ★ 3차 후보 판정 — 사전 2건 개정, 승인 0건, 1c 대조 3건 (2026-08-24)

2.46절이 남긴 판단 넷에 답이 나왔다.

**① quiet_worship 사전에 `피아노 찬양` · `연주 찬양`을 넣었다**

```
넣기 전   택피아노 gratitude만 · mini Music 미태깅   → 승인해도 값을 못 한다
넣은 뒤   둘 다 quiet_worship
현재 코퍼스 1,067건 영향 0건 (태깅 284 그대로 · quiet_worship 12 그대로)
```

★ **`피아노 찬양`은 2026-08-20에 "0건이라" 뺐던 어휘다.** 다시 넣는 것이므로
근거가 그때와 어떻게 다른지 `themes.yaml` 주석에 적었다.

```
2026-08-20   현재 코퍼스에 0건이다 → 뺀다 (검색창 어휘였다)
2026-08-24   0건인 것은 같다. 그러나 **발굴이 데려온 실제 채널의 실제 제목**에
             이 관행이 있다. 승인되면 값이 생긴다
```

⛔ **"0건이니 죽은 어휘"라며 다시 지우지 말 것.** 공급 채널이 승인되기 전까지
0건인 것이 정상이다. 지우려면 택피아노·미니뮤직 계열이 allowlist에 없음을 먼저 확인할 것.
회귀 테스트를 붙여 뒀다 — 지우면 `tagging_test.py`가 걸린다.

⛔ `주일찬양`은 **뺐다.** 적중 14건·순증 11건으로 물량은 가장 크지만 전부 회중 찬양
콘티다. intent가 "조용한 찬양·연주"인데 회중 찬양을 받으면 **joy_praise와 구분이
무너진다** — 2026-08-20에 `워십`을 뺀 것과 정확히 같은 이유다.

**② `쉼과 회복이 있는 교회` — 승인하지 않는다**

접두어 방어를 넣더라도 이 채널의 콘텐츠는 주일예배 라이브(1시간 7분)와 성경강좌다.
`rest` 풀에 맞는 콘텐츠가 아니다. **풀 0이 메워지는 것처럼 보이는 것이 함정이다.**

**③ ★ 그러나 대괄호 접두어 방어는 별도로 넣었다 — 오탐 다섯 번째 유형이다**

이 채널을 안 넣어도 **다른 채널이 같은 관행을 쓸 수 있고**, "사전이 채널명을
콘텐츠로 착각한다"는 것은 그 자체로 결함이다. `strip_brand_prefix`를 만들었다.

★ **처음 떠올린 안(대괄호를 전부 뗀다)은 실측이 뒤집었다.**

```
그 안의 대가   현재 코퍼스에서 태깅 118건 상실
              [생명의 삶 큐티] 100건 · [매일성경] 18건 — 전부 daily_word
              daily_word의 주력 공급이 통째로 날아간다
```

**대괄호가 브랜드인지 내용인지는 괄호 모양으로 갈리지 않는다.** 그래서 채널명과 대조한다.

```
접두어를 정규화한 것이 채널명을 정규화한 것 안에 있으면 브랜드다
  [쉼과회복]      vs 쉼과 회복이 있는 교회   포함  → 뗀다
  [생명의 삶 큐티]  vs CGN 생명의 삶         미포함 → 둔다
  [매일성경]      vs 김학중 목사_꿈의교회…    미포함 → 둔다
실측: 접두어가 있는 440건 중 **떼어지는 것 0건.** 순수한 방어다
```

⚠ 형식 판별에는 적용하지 않았다. 거기서는 앞머리 대괄호가 `[주일찬양]`처럼
**형식의 증거**로 쓰인다(2.44절). 주제는 "무엇에 관한 내용인가"이고 형식은
"어떤 형태인가"라, 브랜드가 답이 될 수 있는 쪽은 형식뿐이다.

⚠ `tag_themes(title, themes, channel_name)`으로 채널명이 선택 인자다. 주지 않으면
예전과 똑같이 동작한다 — 배치와 `retag_titles.py` 양쪽을 배선했고 회귀 테스트가 있다.

**④ C채널 계열 3건 — 1c 대조 완료, 승인 판단은 사람 몫으로 남긴다**

상세는 `channel_criterion_c.20260824.md`. 요지만 옮긴다.

```
P1 전수 대조   예장통합 제4회~제110회(1915~2025) 결의 목록 271행을 다시 내려받아 대조
              씨채널 0 · C채널 0 · 채널 0 · 방송 0 · TV 0
              ★ 이단옹호언론 규정 대상이 **전부 신문·저널이고 방송사는 하나도 없다**
                (기독저널·주일신문·교회와이단·들소리신문·기독교신문·로앤처치)
1a           채널 API의 customUrl과 홈페이지가 링크한 주소가 **정확히 일치**
                @globalcch · @cchannelnews · @cchannelsermon
              2.9절이 경계한 사고(홈페이지가 다른 채널을 링크)는 여기서 없다
1b           ⛔ 성립하지 않는다 — 세 채널 설명 어디에도 링크·교단 표기가 없다
실체          씨채널방송 · 사업자등록번호 211-88-08347 · 서울 강동구 양재대로 1585
교단          초교파, 표기 없음. ⚠ CBS처럼 이사회 구성으로 문서화된 사례와 **신뢰도가 다르다**
```

⚠ **P1 PDF의 크기가 지난번과 다르다** — 2026-08-18 기록 109,562바이트, 이번 111,643바이트.
표제의 회기·연도(제110회/2025)는 같다. 재업로드인지 내용 추가인지 확인하지 못했다.
**다음 1c 대조 때 이 값을 다시 비교할 것.**

★ **기준 3에서 셋이 갈렸다.**

```
C채널방송 : 설교   10건 전부 교회 설교(21~40분) · 쇼츠 0/10   가장 깨끗하다
C채널방송 : TV    성경교실·[풀다보면] · 쇼츠 5/10
C채널 굿데이      10건 중 6건이 교계 뉴스 리포트 · 3분 미만 5건  ⚠ 기준 3
                정치 논평은 아니지만 **뉴스는 감정 화면에 놓을 콘텐츠가 아니다**
```

⚠ **승인해도 지금 사전으로는 주제 풀이 안 는다.** 실측했다.

```
"C채널 [풀다보면] 32회 - 바울의 제2차 전도여행"        → 미태깅
"소문난 성경교실 741회 … 은혜로 읽는 창세기"           → 미태깅
bible_story 어휘는 성경 인물·성경의 인물·강해·성경 배경·성경 이야기·성경 속
```

`daily_word` 폴백 후보로는 들어가지만 `bible_story`를 채우려면 **사전 작업이 함께** 필요하다.
⛔ `성경교실`을 넣기 전에 `--probe`로 오탐을 먼저 잴 것.

**⑤ self_control — 지금 정하지 않는다**

2.28절이 "어휘가 아니라 물량"이라 판정했고 2.46절 발굴이 같은 결론을 냈다.
**세 번째 시도 전에 "무엇을 찾아야 하는가"부터 다시 본다.**

```
먼저 답할 것   분노를 다루는 기독교 콘텐츠가 한국 유튜브에 **실재하는가**
              (실재하는데 못 찾은 것인지, 애초에 얇은 것인지가 갈리지 않았다)
⛔ 하지 말 것   검색어를 더 넣어 보는 시도. 두 번의 실측이 같은 답을 냈다
```

---

### 2.48 ★ 첫 승인 1건 — 그리고 프로그램명을 사전에 넣지 않기로 했다 (2026-08-24)

3차 발굴의 후보 22개 중 **하나를 승인했다.** allowlist 15 → **16**.

```
승인   C채널방송 : 설교   UC9Ac9DjBEmNqjAYxf8V1HPA   content_type: sermon
제외   C채널 굿데이       기준 3 — 교계 뉴스 주력
제외   C채널방송 : TV     기준 3 — 쇼츠·강연 혼재, 계열에서 설교 채널이 더 깨끗하다
```

⚠ 제외 2건은 **이단 문제가 아니다.** 1a·1c는 셋 다 통과했다. `channel_reviewed_out.yaml`의
`criterion: "3"`이 그 뜻이다 — 콘텐츠 성격이 서비스에 안 맞는 것이지 채널이 나쁜 것이 아니다.

**★ "승인해도 풀이 안 는다"는 지적이 사전 판단을 갈랐다**

승인 대상 채널의 최근 10건을 현재 사전으로 태깅해 봤다.

```
['lament', 'bible_story']  삼손(6) : 부르짖는 자의 샘 [김하나 목사의 말씀 강해] [C채널]
[]                        새은혜교회 황형택 목사 | 모든 것 하늘의 아빠 아버지께 [C채널] 복음강단
[]                        기쁨의교회 박진석 목사 | 광야에 길을 사막에 강을 내는 법 [C채널] 비전메시지
[]                        (이하 7건 전부 미태깅)

주제 태깅 1/10 · 미태깅 9 → 폴백행
```

걸린 1건은 `강해`가 잡았다. **이미 사전에 있는 어휘가 제 일을 한 것이다**(코퍼스 적중 4건).

**⛔ 결론 — 프로그램명은 넣지 않는다**

지목받은 `성경교실`·`풀다보면`은 **먼저 논점이 소멸했다.** 둘 다 승인하지 않은
`C채널방송 : TV`의 프로그램이다. 승인한 설교 채널의 프로그램명은
`복음강단`·`비전메시지`·`말씀의창`이다. 그래서 그쪽을 놓고 판단했고, 답은 "넣지 않는다"다.

```
① 프로그램은 주제를 말하지 않는다
   `복음강단`은 25분짜리 주일 설교 방송의 이름이고 **매회 내용이 다르다.**
   "압살롬아, 내 아들아"(탄식)와 "믿음으로 살리라"(맡김)가 같은 프로그램이다.
   어느 주제에 매핑하든 그 프로그램 전편이 그 주제로 태깅된다.

② ★ 그것은 우리가 방금 막은 오탐을 우리 손으로 만드는 것이다
   2.47 ③에서 `strip_brand_prefix`를 넣은 이유가 정확히 이것이다 —
   **사전이 채널·프로그램 이름을 콘텐츠로 착각하는 것.**
   `[쉼과회복]`을 막아 놓고 `복음강단`을 넣으면 앞뒤가 안 맞는다.

③ 실측 근거도 없다
   --probe 결과 성경교실·풀다보면·복음강단·비전메시지·말씀의창 **전부 0건**이다
   (현재 코퍼스에 C채널이 없으니 당연하다). 2.47 ①의 `피아노 찬양`과는 경우가 다르다 —
   그쪽은 **곡명이 곧 콘텐츠**라 채널이 승인되면 값이 생기지만, 프로그램명은
   승인돼도 "내용"을 말해 주지 않는다.
```

⚠ `daily_word`가 `큐티`를 어휘로 갖고 있어 헷갈릴 수 있다. 그것은 **형식이자 내용**이다 —
  큐티는 "짧은 매일 묵상"이라는 콘텐츠 자체를 가리키고 daily_word의 intent와 같다.
  `복음강단`은 그런 정보를 담고 있지 않다. **프로그램명이라고 다 같은 것이 아니다.**

**그러면 이 채널의 값은 무엇인가 — 폴백 다양성이다**

```
주제 풀    거의 기여하지 않는다 (1/10)
폴백       ★ 여기가 값이다. 2.25절 [증거 A]가 "폴백 151건이 영상 23종을
           20개 화면이 돌려 쓰는 구조"라고 진단했다. 새 공급원이 그 겹침을 줄인다
분산       채널당 상한 5가 있어 한 채널이 화면을 독점하지 않는다
품질       [1] 폴백 품질 필터(2.26절)를 통과할 콘텐츠다 — 공고·행사·홍보가 아니다
```

⚠ 이 기대는 **배치를 돌려야 실측된다.** 폴백 비율과 겹침이 실제로 나아졌는지는
다음 절에서 확인한다.

**★ 남은 문제는 그대로다 — `bible_story`는 여전히 얇다**

이 채널을 넣어도 `bible_story` 풀은 사실상 안 는다. 2.46절이 "키워드로는 여기까지가
한계"라고 적은 자리이고, 이번 승인으로 바뀌지 않았다.

⛔ 그 얇음을 프로그램명으로 메우려는 시도를 **하지 말 것.** 위 ①~③이 그 이유다.
   메우려면 **성경 인물·배경 강해를 실제로 올리는 채널**을 더 찾아야 한다.

---

### 2.49 ★ 승인 후 배치 — 11건이 옮겨졌고, 내 예측 하나가 틀렸다 (2026-08-24)

`gh workflow run build.yml`로 한 번 돌렸다. 66 units · 1분 2초 · 성공.
[6] 동점 처리 반영과 신규 채널 수집을 **한 번에** 했다(2.45절에서 정한 순서).

**★ 검증 — 지시받은 확인 항목이 통과했다**

```
                          직전(08-23 run 8)   이번(08-24)
배포본 고유 영상            158건               170건
말씀 탭에 남은 찬양 콘티     11건                **0건**
                          오륜교회 9 · 우리들교회 2
```

**[6]이 배포본에 반영됐다.** 2.44절에서 고친 코드가 08-23 배포본에는 옛 판정으로
남아 있었고(2.45절 기록), 이번 배치로 데이터가 따라왔다.

**배포본 지표 — 전 항목이 나아졌다**

```
                        직전      이번      비고
수집(kept)              1,071    1,171     +100 (신규 채널)
총 노출                   476      480
폴백                      134      112
폴백 비율               28.2%    23.3%     -4.9%p
주제 저수확 경보           16건      14건     해소: bible_story · hope
형식 편중 경보              7건       6건     해소: new_thing
폴백 과다 화면              4건       3건     해소: boredom.novelty
20건 미달 화면              2건       0건     ★ 해소: anger.unfair · frustration.suppressed
미태깅 비율             72.7%    72.7%     변화 없음
```

⚠ **이 개선을 전부 신규 채널의 공으로 돌리지 말 것.** 같은 배치에 세 가지가 함께
들어갔다 — 신규 채널 · [6] 동점 처리 · quiet_worship 어휘 2건. 게다가 코퍼스가
4일치 다르다(2.42절). **분리 측정을 하지 않았으므로 기여도를 나눌 수 없다.**

**★ theme_pool이 처음으로 배포본에 남았다** (2.43절 개정)

```
self_control    풀   0   선정  0     ⛔ 그대로
rest            풀   0   선정  0     ⛔ 그대로
quiet_worship   풀  10   선정 10     그대로 — 공급 채널이 안 늘었으니 당연하다
bible_story     풀  23   선정 20     ★ 아래
daily_word      풀 165   선정 15     채널 상한 5의 결과다 (부족이 아니다)
```

이제 **"그때 풀이 몇이었나"를 다시 추정하지 않아도 된다.** 다음 세션은 이 값을 읽으면 된다.

**★ 내 예측이 틀렸다 — `bible_story`는 안 늘 것이라고 했는데 늘었다**

2.48절에 이렇게 적었다: "이 채널을 넣어도 `bible_story` 풀은 사실상 안 는다."

```
근거로 삼은 것   발굴 시트의 최근 10건을 태깅 → 1/10만 걸렸다
실제             신규 채널이 낸 13건 중 **11건이 주제분**(폴백 2건)
                bible_story 저수확 경보가 **해소**됐다
```

**무엇이 틀렸나 — 표본이 10건이었다.**
발굴 시트는 채널당 최근 10건만 보여 준다. 배치는 **채널당 100건**을 걷는다.
10건에는 `복음강해`·`금요성령집회` 같은 다른 프로그램이 안 들어 있었고,
`강해`가 붙는 제목이 100건 안에는 훨씬 많았다.

⚠ **2.29절에서 이미 같은 실수를 했다** — "조사에서는 `의지` 오탐을 1건으로 봤는데
  실은 3건이었다. 노출된 화면만 봤기 때문이다." 이번에는 시트 10건만 봤다.
  **표본 창이 좁으면 방향까지 틀린다.**
⚠ 2.9절 ③(쇼츠 비율 → 쇼츠 제외 건수)도 "표본 창이 좁아 생긴 오판"이었다.
  **이 저장소에서 세 번째다.** 시트의 10건으로 채널의 성격은 볼 수 있지만
  **태깅 수확은 못 잰다.**

⛔ 다만 **2.48절의 판단(프로그램명을 사전에 넣지 않는다)은 바뀌지 않는다.**
   그 판단의 근거는 수확량이 아니라 "프로그램은 주제를 말하지 않는다"였고,
   실제로 늘어난 것도 프로그램명이 아니라 **`강해`라는 내용 어휘**가 잡은 것이다.
   ★ 오히려 이번 결과가 그 판단을 뒷받침한다 — **이미 있던 내용 어휘로 충분했다.**

**함께 고친 것 — 워크플로에 박혀 있던 낡은 수치**

`build.yml`이 `echo "구절 대조 통과 (250건)"`을 찍고 있었다. 실제는 303건이다.
2.43절과 같은 유형(같은 값을 두 곳에 손으로 적는다)이라 **건수를 지우고 스크립트가
세게 뒀다.** 게이트 자체는 정상 동작하고 있었으므로 표시만 틀렸던 것이다.

**남은 것 — 발굴 목표는 대부분 그대로다**

```
self_control · rest    풀 0. 이번 승인으로 바뀌지 않았다
quiet_worship          풀 10 · sermon 0건. 어휘는 준비됐으나(2.47 ①)
                       택피아노·미니뮤직이 승인되지 않아 값이 없다
위기 채널              여전히 0개. 위기 화면은 상담 안내 + 구절 10건으로 정상 동작
```

⚠ `quiet_worship`의 `피아노 찬양`·`연주 찬양`은 **지금도 코퍼스 적중 0건이다.**
  공급 채널이 없으니 정상이다 — 2.47 ①의 ⛔를 다시 읽을 것.

---

### 2.50 출시 준비 정리 — 이메일 점검과 광고 정책 확인 (2026-08-24)

3절 「스토어 출시 준비」를 항목별 상태 + `🔴 판단 필요` 표기로 다시 짰다.
여기에는 **그 판단의 근거와 출처**만 남긴다.

**① 이메일 노출 점검 — 배포본 0건**

```
검사 범위   추적 파일 89개 전수 · gh-pages 배포본 전수 · 로컬 무시 파일 · git 설정
결과        배포본(실제 노출면) **0건**
            추적 파일 2건 — HANDOFF.md(커밋 author 기록) · About.jsx(빈 상수의 예시 주석)
            커밋 author 80건 전부 appleap385@gmail.com
            ★ 회사 이메일 0건 — 1절이 기록한 "첫 커밋 amend"가 유효했음이 확인됐다
```

⚠ 재현: `git grep -nIE "[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}"` 와
  gh-pages 트리 순회. 둘 다 API·네트워크 소모 0이다.
⚠ `CONTACT`에 실제 주소를 넣는 순간 **앱 화면에 공개된다.** 지금은 빈 문자열이라
  그 줄이 아예 그려지지 않는다.

**② ★ 광고 — 계획한 구조가 정책에 걸린다는 것을 원문으로 확인했다**

계획은 "영상 클릭 → 팝업 디스플레이 광고 → 닫으면 유튜브"였다.
**AdSense 프로그램 정책이 광고를 놓을 수 없는 자리로 팝업을 명시 열거한다.**

```
근거 (원문)
  "Displayed in pop-ups or pop-unders, including where a page containing
   Google ads ... is loaded in a pop-up or pop-under."
  "Sites may not ... contain pop-ups or pop-unders that interfere with
   site navigation."
  "Floating box scripts"
     → AdSense Program policies
       https://support.google.com/adsense/answer/48182

  "Be careful when placing links, play buttons, download buttons, navigation
   buttons ... near ads because they might lead to accidental clicks."
     → Ad placement policies
       https://support.google.com/adsense/answer/1346295

  "publishers may not ask others to click or view their ads" (보상형 제외)
     → "무료 서비스를 위해 최소한의 광고를 노출합니다" 문구가 **강제 노출과 붙으면**
       시청 요청으로 읽힐 수 있다
```

**★ 핵심은 "자체 구현이냐 Google 구현이냐"다.** 트리거는 문제가 아니었다.

```
Google의 비네트(vignette)  전면 광고이고 **기본 트리거가 "사이트 링크 클릭 시"**다
                        — 원래 원하던 UX와 사실상 같다
차이                     구현·빈도·정책 판단을 Google이 진다. 닫기 버튼과
                        브라우저 뒤로가기로 즉시 닫힌다. 빈도 상한 기본 10분
                        (1분~1시간 선택)
★ 링크 단위 제외          "특정 링크가 비네트를 띄우지 않게" 하는 공식 방법이 있다
                          https://support.google.com/adsense/answer/16531962
```

⚠ **같은 UX인데 한쪽은 위반이고 한쪽은 아니다.** 개발자가 만든 인터스티셜은
  "사용자 행동을 막고 광고를 보여주는" 형태로 판정되고, 같은 동작이라도 Google이
  게재하면 빈도·닫기·정책 준수를 Google이 보증한다. **직접 만들지 말 것.**

**③ 그런데 정책을 통과한다고 넣어야 하는 것은 아니다**

```
이 앱은 위로받으러 온 사람이 쓴다. 영상을 누르는 순간이 가장 조용해야 할
순간이고, 거기에 전면 광고를 넣으면 2.35·2.36절에서 세 번 만에 잡은
톤(조용함·요구 없음)과 정면으로 충돌한다.
```

★ **이 판단이 인피드로 이어졌다 (같은 날 2.51절).** 전면 광고를 피하고
**영상을 누르는 순간을 건드리지 않는** 자리를 골랐다는 점에서 결론이 바뀐 것이
아니라 **자리를 찾은 것**이다.
⚠ 이 절의 "출시 시점에 광고를 넣지 않는다"는 서술은 **2.51절에서 뒤집혔다.**
  정책 판정(자체 팝업 위반 · 자체 구현과 Google 구현의 차이)은 그대로 유효하다.

⛔ **위기 화면에는 어떤 형태의 광고도 넣지 않는다. 확정이다.**
⚠ 자동 게재는 **페이지 단위로 동작**한다. PWA 단일 페이지 구조에서 "위기 화면일
  때만 끄기"가 실제로 되는지 **구현 전에 확인해야 한다.** 안 되면 자동 게재를 쓰지 않는다.
  ★ 인피드에는 이 함정이 없다 — 자리를 우리가 정하므로 제외가 구현으로 보장된다.

**④ About "영상" 문구 — "아무 말도 안 한다"보다 "다르게 말한다"**

빼야 할 것은 **"승인"이라는 보증 어휘**이지 메커니즘 설명이 아니다.

```
아무 말도 안 하면   이단 오해가 생겼을 때 **화면에서 댈 근거가 사라진다.**
                 allowlist는 저장소에 있지 사용자가 보는 곳에 없다
다르게 말하면      "미리 정해 둔 채널 목록에서만 가져옵니다"는 보증이 아니라
                 사실 진술이고, "검색으로 모으지 않는다"가 오해를 가장 빨리 푼다
```

★ 부수 효과 — 지금은 첫 문단의 "승인했다"와 둘째 문단의 "이 앱의 입장과 같지
  않을 수 있습니다"가 **미묘하게 충돌한다.** 보증 어휘를 빼면 그 어긋남도 사라진다.

⚠ 이 문구를 고정하는 앱 테스트가 **없다**(확인함). 고쳐도 게이트가 안 걸리므로
  바꿀 때 회귀 테스트를 함께 넣을지 정할 것.

**⑤ 아직 아무것도 고치지 않았다**

`About.jsx`도 광고도 손대지 않았다. 이 절과 3절은 **판단 재료**다.
🔴 표시가 붙은 항목은 사용자가 정해야 진행된다.

---

### 2.51 기록 삭제 버튼과 인피드 광고 결정 (2026-08-24)

**① 기록 삭제 — 문서가 약속한 것을 코드가 지키게 했다**

`taxonomy.yaml`이 "설정에 기록 삭제 버튼을 둔다"고 적어 두었고 `db.js`에
`clearAllLocalData()`가 있는데 **호출부가 없었다.** 설정 화면 자체가 없었다.
개인정보처리방침이 그 문장을 쓸 수 없었던 이유이기도 하다.

```
자리      "이 앱에 대해" 화면의 "기록" 절 아래
확인 단계  버튼 자신이 한 번 더 묻는다 — window.confirm을 쓰지 않았다.
          브라우저 기본 대화상자는 **시스템 목소리**이고 이 화면은 조용히 말하는 자리다
            "이 기기에서 기록 지우기" → "…되돌릴 수 없어요. [지울게요] [그만두기]"
            → "지웠어요. 다음에 오시면 처음처럼 맞이할게요."
          물러설 길("그만두기")을 나란히 둔다
```

**★ 캐시 3종을 흔적인가 성능인가로 갈랐다**

```
pym-thumbs-*   지운다   어떤 영상을 봤는지가 남는다
pym-krv-*      지운다   어떤 장을 열었는지가 남는다.
                       성경 본문 자체는 개인정보가 아니지만
                       **"내가 무엇을 읽었는지"는 흔적이다**
pym-shell-*    남긴다   앱 화면 파일이다. 누가 써도 같고 흔적이 아니다.
                       지우면 오프라인에서 앱이 안 열린다 — 잃는 것만 있다
```

⚠ 캐시는 지워도 다시 열면 다시 받는다. **되돌릴 수 없는 것은 IndexedDB 쪽**
  (방문 기록·회전 상태)뿐이고, 확인 단계는 그것 때문에 둔다.
회귀 테스트 3건 (`verses.test.js`) — 연결 여부 · 확인 단계 · 앱 셸 보존. 앱 112 → 115.

**② 광고 — 인피드로 도입한다 (같은 날 결정이 바뀌었다)**

앞서 "출시 시점에 넣지 않는다"고 적었다(2.50 ③). 인피드로 바꿨다.
**정책 판정은 그대로다** — 자체 팝업은 위반이고, 자체 구현과 Google 구현의 차이가 핵심이다.

★ **인피드를 고른 실질적 이유가 하나 더 있다.** 자동 게재(비네트)는 페이지 단위로
동작해서 PWA 단일 페이지에서는 "위기 화면일 때만 끄기"가 보장되지 않는다.
**인피드는 자리를 우리가 정하므로 제외가 구현으로 보장된다.**

**★ 실측 — 헤드리스 크로미움, 360px 뷰포트**

```
콘텐츠 폭        320px   ★ AdSense 320 규격과 정확히 일치 (좌우 여백 20px씩)
항목 간격        16px
제목 1줄 항목    52px    썸네일(52)이 높이를 정한다 — meta는 41px
제목 2줄 항목    62px    meta(62)가 높이를 정한다 — 제목 21×2 + 부제 16 + 사이 4
```

⚠ 브라우저 확장이 안 붙어 있어 playwright로 쟀다. 시드 데이터 4건이 전부 2줄이라
  1줄 값은 DOM에서 제목을 짧게 바꿔 다시 쟀다.
⚠ 폭이 320에 **정확히** 맞는 것은 우연이다 — App.jsx의 `padding: "34px 20px 40px"`가
  360에서 320을 남긴다. **좌우 여백을 바꾸면 광고 규격이 어긋난다.**

**밀도 — 20건 기준 (전부 2줄, 총 1,544px)**

```
3개 × 100px   총 1,892px · 비중 15.9% · 광고당 영상 6.7개   ← 추천
4개 × 100px   총 2,008px · 비중 19.9% · 광고당 영상 5.0개
3개 ×  50px   총 1,742px · 비중  8.6%
4개 ×  50px   총 1,808px · 비중 11.1%
```

★ 넷 다 Better Ads Standards의 모바일 밀도 상한(30%) 아래다. **밀도는 제약이 아니다.**

**★ 320×50을 쓰지 않는 이유 — 정책이 톤과 반대 방향을 가리킨다**

```
320×50은 1줄 항목(52px)과 **2px 차이**다. 잘 섞이는 것이 장점처럼 보이지만
AdSense 정책은 정확히 그것을 금지한다:
  "Format site content so that it is difficult to distinguish it from ads."
```

⚠ **톤 보존을 "비슷하게 보이게"로 풀면 위반이다.** "조용하게"로 풀어야 한다.
⛔ 92×52 썸네일 + 제목 + 부제 레이아웃을 광고에 흉내내지 말 것.
⛔ 라벨은 **"광고" 또는 "스폰서 링크"**만 허용된다 — "추천"·"함께 보기"는 위반이다.

**설계** — 라벨은 폴백 층 헤더와 같은 언어(11.5px · 자간 0.2em · `T.muted`),
배경 `#ffffff08`, 테두리 없음, 위아래 여백 20px(항목 간격 16px보다 넓게), 라운드 3px.
**간격이 곧 경계다.** 새 색을 만들지 않고 팔레트와 기존 알파값만 쓴다.

⚠ 2.35·2.36절이 색·여백·구조 신호로 위계를 세 번 만에 잡았다. 광고 블록은 그 위계에
  층을 하나 더 만든다 — **넣은 뒤 그 절의 절차로 다시 잴 것.**


**★ 광고 자리는 고정할 수 없다 — 층 경계와 겹친다 (2026-08-24 실측)**

`320×100` 3개로 정했고, 처음에 5·11·17번 항목 뒤로 잡았다. **배포본으로 확인하니
6개 화면에서 폴백 층 헤더와 겹쳤다.**

```
폴백 시작 위치가 화면마다 다르다 (배포본 24화면 실측)
  9:2화면 · 10:1 · 11:3 · 13:1 · 14:2 · 16:2 · 18:3 · 19:2 · 20:2 · 폴백없음:6

11번 자리   anger.irritation(11) · anger.rage(11) · exhaustion.tired(11) → 헤더 바로 앞
17번 자리   joy.delight(18) · joy.grateful(18) · exhaustion.listless(18) → 헤더 바로 앞
```

⚠ **층 경계는 이 화면의 정직성 장치다.** 폴백 헤더는 "딱 맞는 건 아니지만"을 먼저
말해서 사용자가 기대를 걸지 않게 하는 문장이고(VideoList.jsx 주석), 그 앞뒤에
광고가 오면 **헤더가 광고의 라벨처럼 읽히거나 층이 뒤섞인다.**

**규칙 — 자리를 층에 상대적으로 정한다**

```
기준 자리     5 · 11 · 17번 항목 뒤
첫 광고       최소 5항목 뒤    "열자마자 광고"를 피한다 (약 390px)
층 경계       앞뒤 2항목 이상  헤더와 광고가 붙지 않게 한다
꼬리          뒤에 2항목 이상  광고 뒤에 항목 하나만 남는 자리를 피한다
겹치면        ±1 → ±2 → ±3 순으로 가장 가까운 빈자리로 민다
```

**전 24화면 검증 — 경계 충돌 0건, 광고는 항상 3개**

```
조정 없음 15화면   [5, 11, 17]
조정      9화면    [5,12,17] 3 · [5,11,16] 2 · [5,11,15] 3 · [5,10,17] 1
간격      기본 [5,6,6,3] · 조정 시에도 4~7 범위를 벗어나지 않는다
```

⚠ 폴백이 없는 6화면(주제분 20)은 경계가 없으므로 기준 자리를 그대로 쓴다.
⚠ 이 검증은 **그날의 배포본 기준**이다. 폴백 비율은 매일 달라지므로(2.42절)
  **자리를 상수로 박지 말고 규칙으로 계산할 것.** 규칙은 어느 분포에서도 성립한다.

**⛔ 광고를 넣지 않는 곳 (확정)** — 위기 화면 · "이어서 읽기" 탭.

> ⚠ **정정됨 (2026-08-31 · 2.95절).** 이어서 읽기 탭은 **광고 있음**으로 바뀌었다.
> 지금 유효한 범위는 — 광고 없음: 위기 화면 · 알림 콜백 화면 /
> 광고 있음: 이어서 읽기 · 말씀 · 찬양 탭.

**③ 개인정보처리방침을 광고 조항을 넣어 다시 썼다**

```
고친 것   "광고·광고쿠키 없음" → 2절 신설 (AdSense 게재 · 쿠키 · 맞춤 광고 거부 방법)
추가      외부 통신 표에 pagead2.googlesyndication.com 등 3개 도메인
추가      "기록 지우기로는 광고 쿠키가 지워지지 않는다"는 사실
추가      광고를 넣지 않는 화면을 방침에 **명시**했다 — 정책이자 약속이다
⛔ GDPR   고려하지 않는다 — 유럽 출시 안 함이 전제다 (2026-08-24 결정)
```

⚠ 따라오는 것: 스토어 "광고 포함" 신고 · `ads.txt` · 🔴 문의처는 여전히 비어 있다.
⚠ **2026-08-24 교정** — 여기서 `ads.txt`를 "승인 후 gh-pages 루트"로 적었다. **틀렸다.**
  도메인 루트에서만 읽히므로 유저 사이트 레포에 둔다. 승인을 기다릴 필요도 없다 (선언일 뿐이다). 완료 — 2.55 ⑤.

---

### 2.52 ★ AdSense 정책 확인 — 앱에서는 못 쓴다 (2026-08-24)

두 가지를 공식 문서로 확인했다. **하나는 설계를 정정했고, 하나는 설계를 갈랐다.**

---

**① 인피드 광고는 크기를 지정할 수 없다 — 우리가 정한 것은 디스플레이다**

```
인피드    data-ad-format="fluid"
          폭은 **항상 컨테이너 폭**(최소 250px) · 높이는 AdSense가 자동 조정
          네이티브 형식이라 템플릿(이미지·헤드라인 크기, 글꼴, 색)을 편집기에서 맞춘다
디스플레이 320×100 같은 **고정 크기를 지정할 수 있다** (반응형도 선택 가능)
```

★ **그러므로 2.51절에서 확정한 "320×100"은 인피드가 아니라 디스플레이 광고다.**
  리스트 사이에 넣는 것은 **자리의 문제**이지 형식의 문제가 아니다 —
  디스플레이 광고를 피드 중간에 두는 것은 정상적인 배치다.

⚠ **인피드를 쓰면 안 되는 이유가 하나 더 있다.** 인피드는 네이티브 형식이라
  **주변 콘텐츠처럼 보이는 것이 목적**이다. 그런데 우리는 2.51절에서 정반대로
  판단했다 — `320×50`이 항목과 2px 차이라 "잘 섞여서" 위험하다고 봤고,
  정책도 "콘텐츠와 구분하기 어렵게 만들지 말 것"을 요구한다.
  **인피드는 그 경계에 정확히 서 있는 형식이다.** 쓰려면 라벨과 시각 분리를
  훨씬 더 신경 써야 하고, 우리가 얻는 것이 없다.

⚠ 인피드는 높이를 우리가 못 정하므로 **층 경계 회피 규칙의 높이 계산도 불확정**해진다.
  2.51절의 밀도 실측(15.9%)이 성립하지 않게 된다.

**→ 판정: 디스플레이 광고 320×100. 2.51절의 설계는 그대로 유효하다.**
  ⚠ 다만 "인피드 광고"라는 **이름을 버린다.** 형식 이름이 아니라 자리 설명으로 쓸 것 —
  "리스트 사이 디스플레이 광고"가 정확하다.

---

**② ★ 앱에서는 AdSense를 쓸 수 없다 — AdMob이어야 한다**

```
"Integrated into a software application (does not apply to AdMob) of any kind,
 including toolbars."
   → AdSense Program policies, 광고를 놓을 수 없는 자리 열거
     https://support.google.com/adsense/answer/48182

"You are not allowed to place AdSense for Mobile Content ads in any sort of app.
 The only option for Mobile Apps is AdMob."

"If you attempt to use AdSense code in your app instead of AdMob, if you fail to
 comply with these policies, we reserve the right to disable ad serving to your app
 and/or close your AdMob account at any time."
   → AdMob Behavioral policies https://support.google.com/admob/answer/2753860
```

⚠ **계정 정지가 걸린 조항이다.** "해보고 안 되면 바꾸자"로 접근할 자리가 아니다.

**`WebView API for Ads`는 예외가 아니다 — 성격이 다르다**

```
무엇인가   앱의 **웹 콘텐츠 뷰잉 프레임**(인앱 브라우저)에서 AdSense·Ad Manager
          광고가 뜰 때, Mobile Ads SDK로 웹뷰를 등록해 앱 신호(앱 ID·버전)를
          태그에 전달하는 기능
          https://developers.google.com/admob/android/browser/webview/api-for-ads
전제      Google Mobile Ads SDK를 **이미 쓰고 있어야 한다**
          (Android SDK 20.6.0+ / iOS 9.6.0+, 매니페스트·Info.plist 설정)
```

⚠ **문서가 "앱 UI 전체가 웹뷰인 하이브리드 래퍼"를 명시적으로 다루지 않는다.**
  Ad Manager의 기술 요건 문서(`answer/6310245`)에도 그 경우가 없다.
  ⛔ **회색 지대다. 확정적으로 "된다"고 말할 수 없다.**
  위 ②의 금지 조항이 명시적이고 제재가 계정 정지라, **모호할 때 강한 쪽을 따른다.**

---

**★ 그래서 웹과 앱이 갈린다**

```
웹 (PWA · GitHub Pages)   AdSense 정상. 사이트 등록은 GitHub Pages URL로 한다
                         https://appleap385-aaz0919.github.io/PeaceYourMind/
                         → 2.51절 설계(320×100 · 3개 · 층 경계 회피) 그대로 유효
Capacitor 앱             AdSense 코드를 그대로 넣으면 위반 위험. AdMob이어야 한다
```

**AdMob으로 갈 때 무엇이 달라지는가**

```
크기     320×100은 AdMob에도 있다 (LARGE_BANNER) — **크기는 살아남는다**
자리     ⛔ **여기가 깨진다.** AdMob 배너는 네이티브 SDK가 그리는 뷰라
        웹뷰 안 리스트의 5·11·17번 사이에 끼워 넣을 수 없다.
        네이티브 레이어(보통 화면 상·하단 고정)에 뜬다
위기 제외 ⛔ **다시 어려워진다.** 네이티브 배너를 화면마다 붙였다 떼려면
        PWA의 화면 전환을 네이티브가 알아야 한다 (Capacitor 브리지 작업).
        2.51절이 "인피드는 자리를 우리가 정하니 제외가 구현으로 보장된다"고
        적은 장점이 사라진다
방침     2절이 AdSense를 명시한다. 앱에 AdMob을 쓰면 **다시 써야 한다**
```

**★ 가장 단순한 선택지 — 웹만 광고를 넣고 앱은 광고 없이 낸다**

```
웹        AdSense 디스플레이 320×100 · 3개 · 층 경계 회피 (설계 그대로)
앱        광고 없음 → 스토어 "광고 없음"으로 신고 · 심사가 단순해진다
          ⚠ 그러면 방침 2절이 앱에서는 과다 고지가 된다.
            Capacitor 착수 시점에 "웹에서는 광고가 나온다"로 문안을 조정할 것
나중에    앱 수익이 필요해지면 AdMob을 **별도 설계**로 올린다.
          웹 설계를 앱에 옮기려 하지 말 것 — 자리 개념이 다르다
```

⚠ 이 선택은 **비용이 거의 0이다.** 앱은 아직 착수도 안 했고(출시 준비 6번),
  웹 광고는 지금 그대로 진행할 수 있다. **앱을 만들 때 다시 판단하면 된다.**

🔴 **판단 필요** — 위 선택지를 택할 것인가, 아니면 처음부터 AdMob 전제로 갈 것인가.

---

### 2.53 앱 AdSense — 위험을 줄이는 방법 조사 (2026-08-24)

2.52절에서 "앱에는 AdMob"이라고 판정했으나 **앱에도 AdSense를 넣는 방향으로 결정**됐다.
제재가 계정 정지라는 것을 알고 내린 판단이다. 여기서는 **위험을 줄이는 방법**을 정리한다.

---

**① 실제 사례 — 경로는 있고, 제재도 실재한다**

★ **Mobile Ads SDK 팀의 공식 답변이 경로를 제시한다.**

```
"if you display web content that implements ads with AdSense code or Google
 Publisher Tag in your app through WebView, you should use this API
 [WebView API for Ads] to enable ads monetization."
"it never mentioned that using custom hostname may violate the policy"
   → google-admob-ads-sdk 그룹, Cordova 앱 질문에 대한 답변
     https://groups.google.com/g/google-admob-ads-sdk/c/f0pqGvsAYvc
```

권고도 함께 나온다 — **"웹뷰 URL을 앱에서 열린 페이지에 따라 바꾸고, 그 URL은
웹 버전과 같아야 한다."** 즉 **번들 복제본이 아니라 실제 사이트를 열라는 뜻이다.**

⚠ **그러나 제재 사례가 실재하고, 축이 다르다.**

```
"Valuable Inventory: Framed Sites"
"we may not show Google ads on pages or apps with little or no value and/or
 excessive advertising to the user. This includes pages or apps that are
 mirroring or framing content from other sources without adding value."
   → 계정 영구 정지. 이의신청 "final"로 기각
     https://groups.google.com/g/google-admob-ads-sdk/c/soXtLw4IXPc
```

★ **그 앱은 AdMob이었고 자기 콘텐츠였는데도 걸렸다.** 구조가 "네이티브 배너 +
웹뷰 한 장"이었다. **위험의 축은 "AdSense냐 AdMob이냐"가 아니라
"앱이 웹을 그대로 비추기만 하는가"다.**

⚠ Capacitor + AdMob에서도 Play 정책 "Interfering with Apps, Third-party Ads,
  or Device Functionality" 사례가 있다 (capacitor-community/admob#1).
  **형식을 바꿔도 하이브리드 래퍼라는 사실은 그대로 심사 대상이다.**

⛔ **가장 큰 위험은 따로 있다 — 계정은 하나다.**
   앱 때문에 AdSense 계정이 정지되면 **웹 광고도 함께 죽는다.**
   앱에서 얻을 수익보다 잃을 것이 크다는 점을 계산에 넣을 것.

---

**② 시스템 브라우저로 열기 — 정책은 깨끗하지만 대안이 아니다**

```
정책   브라우저에서 열리면 그건 웹 게재다. 문제가 없다.
      Chrome Custom Tabs는 Ad Manager 기술 요건 문서가 통합 옵션으로 열거한다
UX    ⛔ **광고를 보이려고 사용자를 브라우저로 보내는 구조가 된다.**
      이 앱은 위로받으러 온 사람이 쓰고 조용함이 자산인데, 화면 전환이 가장 거칠다.
      2.50절에서 자체 팝업을 거절한 것과 **정신적으로 같은 문제**다 —
      광고를 위해 사용자의 흐름을 끊는다
```

**→ 판정: 대안이 아니다.** 정책만 보면 통과하지만 이 앱의 성격과 맞지 않는다.
⚠ 단, **영상 클릭 시 유튜브로 나가는 것은 이미 있는 흐름이다.** 그 자리를 광고에
  쓰자는 안(2.50절 자체 팝업)은 이미 정책 위반으로 기각됐다. 되살리지 말 것.

---

**③ AdMob 배너를 리스트 사이에 — 방법이 없다는 판단이 맞다**

```
왜 안 되나   AdMob 배너는 **네이티브 뷰**이고 영상 목록은 **웹뷰 안의 DOM**이다.
            서로 다른 레이어라 사이에 끼워 넣을 수 없다
오버레이는?  DOM에 빈자리를 만들고 그 화면 좌표에 네이티브 배너를 띄우는 것은
            기술적으로 가능하다. 그러나
              · 스크롤 중 좌표가 어긋난다 (관성 스크롤에서 특히)
              · 배너가 웹뷰 위에 떠서 콘텐츠를 가리는 순간이 생긴다
              · ⛔ Play의 "Interfering with Apps, Third-party Ads" 범주가
                정확히 그 모양이다. 실제 사례가 있다
```

**→ AdMob을 쓴다면 배너는 화면 하단 고정이 정상이고, 2.51절의 자리 설계는 버려야 한다.**

---

**④ 그래도 앱에 AdSense를 넣는다면 — 위험을 줄이는 구성**

```
a) Capacitor의 server.url을 **실제 사이트**로 지정한다
   https://appleap385-aaz0919.github.io/PeaceYourMind/
   → SDK 팀 권고("웹뷰 URL이 웹 버전과 같아야 한다")를 그대로 따른다
   ⛔ 번들 복제본(localhost)을 열면 AdSense 사이트 등록 도메인과 어긋나고,
     "framing content"로 읽힐 여지가 커진다
   ⚠ 대신 오프라인 첫 실행이 약해진다 — 지금의 서비스워커 전략과 충돌을 점검할 것

b) WebView API for Ads를 붙인다 (Mobile Ads SDK + 웹뷰 등록)
   → SDK 팀이 "should use this API"라고 한 바로 그 경로다. 붙이지 않으면
     "앱 안의 AdSense"가 아무 신호 없이 뜨는 상태가 된다
   ⚠ **Capacitor용 공식 플러그인이 없다.** 네이티브 코드를 직접 넣어야 한다
     (Android WebView / iOS WKWebView 등록 + 매니페스트·Info.plist)

c) ★ **앱이 웹보다 더 하는 것을 만든다 — 이것이 유일한 실질적 방어다**
   "Framed Sites"는 **콘텐츠가 아니라 앱의 부가가치**를 본다.
   지금 우리 앱은 웹과 사실상 동일하다. 후보:
     · 오프라인 동작 (이미 있다 — 서비스워커·IndexedDB·번들 시드)
     · 알림 (정한 시각에 구절 한 절)
     · 홈 위젯
   ⚠ 오프라인은 이미 있으므로 **스토어 설명과 스크린샷에서 그것을 드러내야** 한다.
     심사자가 "웹과 뭐가 다른가"를 묻는 자리이기 때문이다

d) 위기 화면·"이어서 읽기" 제외는 **그대로 보장된다**
   ⚠ **정정됨 (2.95절)** — 이어서 읽기는 이제 광고 있음이다.
     ⛔ 이 항목의 논거(웹 DOM에서 자리를 정하므로 보장된다)는 **앱에서 무효다** —
       앱은 AdMob 고정 띠배너로 가기로 했고, 띠는 화면이 바뀌어도 자리를 지킨다.
       제외는 이제 **화면별 숨김 처리**로 지켜야 한다.
   ★ 광고 자리를 웹 DOM에서 정하므로 2.51절의 규칙(층 경계 회피 포함)이
     앱에서도 똑같이 동작한다. **AdSense를 앱에 넣는 실질적 장점이 이것이다.**
     AdMob으로 가면 이 보장이 깨진다(위 ③).
   ⚠ 회귀 테스트(verses.test.js)가 웹·앱 공통으로 지킨다 — 소스가 하나이기 때문

e) 개인정보처리방침 — **고칠 것이 거의 없다**
   현재 2절이 "이 앱은 … 광고를 게재합니다"로 매체를 특정하지 않는다.
   ⚠ 다만 앱 출시 시점에 "웹과 앱 모두에서"를 명시하는 편이 정확하다.
   ⛔ AdMob으로 갈아탄다면 그때는 2절을 다시 써야 한다(제공자가 바뀐다)

f) 스토어 신고 — "광고 포함"으로 신고한다. Capacitor 착수 시 함께 처리
```

**⚠ 남는 위험 — 줄일 수는 있어도 없앨 수는 없다**

```
· "Framed Sites" 판정은 사람이 한다. (c)를 해도 보장이 아니다
· 이의신청은 "final"로 기각된 사례가 있다
· 계정이 하나라 **앱의 위험이 웹 수익까지 끌고 간다**
```

🔴 **판단 필요 — 순서를 나누는 안**

```
1단계   웹만 AdSense로 시작한다 (지금 바로 가능, 위험 0)
2단계   Capacitor를 만들 때 (a)(b)(c)를 갖춘 뒤 앱에도 켠다
근거    웹 광고는 앱과 무관하게 오늘 진행할 수 있고, 앱은 아직 착수도 안 했다.
        **순서를 나누면 웹 수익을 먼저 확보한 상태에서 앱 위험을 판단하게 된다.**
```

---

### 2.54 ★ 광고 결정 확정 — 웹은 지금, 앱은 나중에 (2026-08-24)

2.52·2.53절의 조사를 거쳐 확정했다. **방향은 앱에도 AdSense이고, 시점만 나눈다.**

```
웹 (PWA)          AdSense 디스플레이 320×100 · 3개 · 층 경계 회피 규칙 ·
                 "광고" 라벨 · 위기·이어서 읽기 제외
                 ⚠ 제외 범위는 2.95절에서 바뀌었다 (이어서 읽기 → 광고 있음)
                 ★ **지금 구현한다** (설계는 2.51절 그대로)
앱 (Capacitor)    AdSense를 켠다. ⛔ **AdMob으로 바꾸지 않는다**
                 시점만 미룬다 — Capacitor 착수 시 (a)(b)(c)를 갖춘 뒤 켠다
```

**왜 AdMob으로 바꾸지 않는가**

```
매체를 바꿔도 Framed Sites 위험은 그대로다 — 정지 사례가 AdMob 앱이었다(2.53 ①)
그리고 AdMob으로 가면 2.51절의 자리 설계를 통째로 버려야 한다.
  네이티브 배너는 웹뷰 DOM 사이에 못 들어가고(2.53 ③),
  위기 화면 제외 보장도 깨진다.
→ **잃는 것은 확실하고 얻는 것은 불확실하다.**
```

---

**★ 2단계(앱) 착수 시 반드시 챙길 것**

```
(a) server.url을 실제 사이트로     https://appleap385-aaz0919.github.io/PeaceYourMind/
    ⛔ localhost 번들을 열면 framing으로 읽힌다. AdSense 등록 도메인과도 어긋난다
    ⚠ **오프라인 첫 실행 전략과 충돌한다** — 서비스워커·번들 시드가 전제인 구조다.
      그때 반드시 점검할 것 (보류 표의 "오프라인 첫 방문"과 묶인다)

(b) WebView API for Ads 부착      Mobile Ads SDK + 웹뷰 등록
    SDK 팀이 "should use this API"라고 한 그 경로다(2.53 ①)
    ⚠ **Capacitor용 공식 플러그인이 없다.** 네이티브 코드를 직접 넣어야 한다
      (Android WebView / iOS WKWebView 등록 + 매니페스트·Info.plist)

(c) ★ 앱이 웹보다 더 하는 것       Framed Sites를 피하는 **유일한 실질적 방어**
    오프라인 동작은 **이미 있다** → 스토어 설명·스크린샷에서 그것을 드러낼 것.
      심사자가 "웹과 뭐가 다른가"를 묻는 자리이기 때문이다
    추가 후보: 알림(정한 시각에 구절 한 절) · 홈 위젯

(f) 스토어 신고                   "광고 포함"
```

⚠ (e) 개인정보처리방침은 고칠 것이 거의 없다 — 2절이 매체를 특정하지 않는다.
  앱 출시 시 "웹과 앱 모두에서"를 명시하는 편이 정확하다.

---

**⛔ 하지 않기로 한 것 — 시스템 브라우저로 여는 안**

```
정책만 보면 통과한다(브라우저 게재다). 그러나 광고를 보이려고 사용자를
브라우저로 보내는 구조가 된다. 이 앱은 조용함이 자산인데 **화면 전환이
가장 거친 방식**이다.
→ 2.50절에서 자체 팝업을 거절한 것과 **같은 이유**다.
```

---

**★ 내 판단 오류 — "AdMob으로 바꾸면 안전하다"고 암시했다**

2.52절에서 "앱에는 AdMob이어야 한다"고 적었다. 금지 조항("Integrated into a
software application (does not apply to AdMob)")만 읽고 **매체를 바꾸면 위험이
해소된다는 뜻으로 쓴 것**이다.

```
실제로는   정지 사례의 앱이 **AdMob을 쓰고 있었다.** 걸린 이유는 매체가 아니라
          "Valuable Inventory: Framed Sites" — 앱이 웹 콘텐츠를 그대로 비추면서
          부가가치가 없다는 판정이었다
★ 위험의 축   "AdSense냐 AdMob이냐"가 아니라
            **"앱이 웹을 그대로 비추기만 하는가"**다
```

⚠ **무엇을 잘못했나 — 금지 조항 하나를 읽고 축을 정했다.**
  조항은 "AdSense는 앱에서 안 된다"를 말할 뿐 "AdMob이면 된다"를 말하지 않는다.
  **제재 사례를 먼저 찾았다면 축이 다르다는 것이 바로 보였다.**
  이번에는 사용자가 사례 조사를 지시해서 드러났다.

⚠ 다음에도: **정책 문서로 "무엇이 금지인가"를 확인했으면,
  실제 제재 사례로 "무엇 때문에 걸리는가"를 따로 확인할 것.** 둘이 다를 수 있다.
  ⛔ 대안을 제시할 때 "그래서 B로 가면 된다"는 문장은 **B가 안전하다는 근거를
    따로 대기 전에는 쓰지 말 것.** A가 금지라는 사실은 B의 안전을 증명하지 않는다.

---

### 2.55 ★ 웹 AdSense 광고 구현 — 설계대로 만들었다 (2026-08-24)

2.51절의 설계를 그대로 구현했다. **새로 정한 것은 없고**, 실측이 뒤집은 것이
두 가지 있다(아래 ④⑤).

**① 자리 계산을 순수 함수로 뺐다 — `app/src/lib/ads.js`**

```
adPositions({themeCount, fallbackCount}) -> [5, 12, 17] 같은 배열
  번호는 "몇 번째 항목 뒤인가"이고 두 층을 이어 붙인 통합 번호다
positionsWithin(spots, offset, length)   -> 층 안의 번호로 옮긴다
  화면이 층마다 별도 <ul>이라 필요하다 (VideoList)
```

⛔ **5·11·17을 화면 코드에 박지 않았다.** 회귀가 그것을 감시한다 —
`videoListSrc`에 `[5, 11, 17]` 꼴이 있으면 테스트가 깨진다.

**★ 2.51절의 배포본 24화면 실측표를 테스트에 그대로 넣었다**

```
폴백없음·9·10·14·16·20  ->  [5,11,17]   조정 없음 15화면
폴백 시작 11            ->  [5,12,17]   3화면
폴백 시작 13            ->  [5,10,17]   1화면
폴백 시작 18            ->  [5,11,15]   3화면
폴백 시작 19            ->  [5,11,16]   2화면
```

구현이 이 표를 **한 칸도 틀리지 않고 재현했다.** 규칙이 옳다는 것을 실측으로
확인한 셈이다. ⚠ 이 표는 그날의 스냅숏이므로 배포본이 달라졌다고 고치지 말 것 —
같은 파일에 "어떤 층 배분에서도(0~40건 전수) 네 규칙이 성립한다"는 검사가 따로 있다.

**② 층 경계 규칙이 실제로 무엇을 지키는가**

```
첫 광고    5항목 뒤부터        열자마자 광고를 피한다
층 경계    앞뒤 2항목 이상      폴백 헤더가 광고 라벨처럼 읽히는 것을 막는다
꼬리      뒤에 2항목 이상      광고 뒤에 항목 하나만 남는 자리를 피한다
겹치면    ±1 -> ±2 -> ±3      같은 거리면 **앞쪽(작은 번호)을 먼저** 본다
```

★ "같은 거리면 앞쪽"은 2.51절에 없던 것이다. 뒤로 밀면 꼬리 여유를 깎아
다음 광고와 붙기 쉽고 목록 끝에서 규칙이 연쇄로 깨진다. 실측표의
`[5,11,15]`(18번 화면)가 이 선택으로만 재현된다.

⚠ **자리를 못 찾으면 광고를 버린다.** 규칙을 어기고 끼워 넣지 않는다 —
목록이 짧으면 광고가 2개거나 0개다. 7건짜리 목록이면 `[5]` 하나뿐이다.

**③ slot이 비어 있으므로 광고 요소를 그리지 않는다**

광고 단위 ID는 승인 후 발급된다. `AD_SLOT = ""`인 동안은 `<ins>`를 아예
만들지 않는다 — 빈 회색 상자는 "고장"으로 읽힌다. 개발 서버에서만
자리표시자가 뜬다.

★ **부수 효과 하나가 좋다.** `AD_SLOT`이 상수 빈 문자열이라 번들러가
광고 가지를 통째로 접는다. 배포본 JS에 `adsbygoogle` 문자열이 **0건**이다
(실측). 승인 전까지는 광고 코드가 사용자에게 내려가지 않는다.

**④ ⚠ 실측이 뒤집은 것 — `import.meta.env?.DEV`는 배포본에 남는다**

자리표시자를 개발 전용으로 접으려고 옵셔널 체이닝을 썼다. 빌드해서 확인하니
**자리표시자 문구가 배포본 번들에 그대로 있었다.**

```
import.meta.env?.DEV   vite가 정적 치환을 못 한다 -> 가지가 살아남는다
import.meta.env.DEV    false로 접힌다 -> 가지가 통째로 빠진다  ★ 이쪽
```

동작은 양쪽 다 맞다(프로덕션에서 false다). 다른 것은 **번들에 남느냐**이고,
그것은 `grep`으로만 보인다. 회귀를 붙였다.

⚠ 일반화: **"조건이 false다"와 "코드가 빠졌다"는 다른 사실이다.**
개발 전용 가지를 만들 때는 빌드 산출물을 열어 확인할 것.

**⑤ ⚠ 실측이 뒤집은 것 — `ads.txt`는 gh-pages 루트가 아니다**

2.51·3절이 "AdSense 승인 후 gh-pages 루트에 올린다"고 적어 뒀다. **틀렸다.**

```
ads.txt는 **도메인 루트에서만** 읽힌다   https://<도메인>/ads.txt
PYM은 프로젝트 페이지다                 -> gh-pages 루트 = /PeaceYourMind/
따라서 거기 두면 /PeaceYourMind/ads.txt가 되어 **무효다**
```

★ 그리고 도메인 루트(`https://appleap385-aaz0919.github.io/`)가 **404였다.**
AdSense는 사이트를 도메인 단위로 등록하고 크롤러가 루트부터 훑는다.

**해결 — 유저 사이트 레포를 새로 만들었다**

```
레포     appleap385-aaz0919/appleap385-aaz0919.github.io  (public · Pages main /)
index    앱 소개 두 문장 + /PeaceYourMind/ 링크 + 소유권 확인 스니펫
ads.txt  google.com, pub-5074778849873789, DIRECT, f08c47fec0942fa0
```

⛔ **즉시 리다이렉트를 걸지 않았다.** 스크립트가 로드되기 전에 페이지를 떠나면
소유권 확인과 검토에 불리하다. 링크만 두고 사용자가 누르게 한다.

**⑥ SPA에서 push를 한 번만 부르는 법**

`adsbygoogle.push`는 `<ins>`당 정확히 한 번이다. 두 번 부르면 AdSense가
"already have ads in them"으로 거부하고 그 자리가 빈 채로 남는다.

```
방어 1   useRef 플래그 — 같은 인스턴스에서 두 번 돌지 않는다
방어 2   목록 항목을 <Fragment key={video.videoId}>로 감쌌다
         감정 전환·토글·데이터 갱신으로 목록이 다시 조립되면 key가 바뀌어
         조각이 통째로 새로 마운트되고 <ins>도 새로 생긴다
```

⚠ 같은 영상이 같은 자리에 남으면 다시 마운트하지 않는다 — **이미 채워진
광고는 건드리지 않는 것이 맞다.**

⚠ push는 `try`로 감쌌다. 광고 차단기나 오프라인이면 던진다. **화면은 살아
있어야 한다** — 광고는 이 앱의 본체가 아니다.

**⑦ 회귀 31건을 붙였다** (`app/test/ads.test.js` · 앱 119 -> 140 선언)

```
자리 계산   실측 24화면 재현 · 0~40건 전수 규칙 검증 · 짧은 목록에서 버리기
정책       라벨 "광고"만 · 금지 어휘 · 11.5px/0.2em/T.muted · 320x100 ·
           data-ad-format 없음 · 목록 항목 레이아웃 흉내 금지
배포 설정   client ID가 코드·앱·방침 세 곳에서 같다 · 스니펫이 <head>에 있다 ·
           방침 페이지에는 광고 자리가 없다 · 좌우 여백 20px 불변
```

⚠ **정정됨 (2.95절)** — 제외 범위가 바뀌었고, 그 회귀는 **소스 텍스트만 본다.**
  Shell에서 그리는 고정 띠배너는 이 검사에 안 걸린다.

⚠ 화면 제외(위기·이어서 읽기)는 **기존 회귀가 이미 지킨다.** 그 목록의
`AD_TOKENS`에 `AdSlot`이 미리 들어 있었고, 이번 구현이 그 이름을 그대로 썼다 —
2026-08-24에 "넣는 순간 걸리게" 미리 둔 검사가 실제로 작동한 것이다.

⛔ 구조적으로도 샐 수 없다. `CrisisScreen`과 `ChapterReader`는 목록을 직접
그리고 `VideoList`를 쓰지 않는다. 광고는 `VideoList` 안에만 있다.

**⑧ 방침에 한 줄 — 일괄 거부 안내**

`aboutads.info/choices` 한 줄을 2절 "개인 맞춤 광고를 원하지 않으실 때" 목록,
Google 광고 설정 **다음**에 넣었다. 서드 파티 광고 게재를 끄지 않으므로
AdSense 필수 콘텐츠 조항이 적용된다.

⛔ NAI·youronlinechoices는 넣지 않는다(결정 사항).

**⑨ ⛔ GDPR/CMP는 방침에 넣지 않는다 — 확정**

```
왜        Google은 EEA·영국 게재에 **인증 CMP**를 요구한다. CMP 없이 문구만
         넣으면 방침과 구현이 어긋난다 — 방침이 하지 않는 일을 말하게 된다
제재인가   아니다. CMP 미구축은 위반이 아니라 **그 지역 광고 제한**일 뿐이다
정합성    "유럽 출시 안 함" 결정(2.51 ③)과 같은 방향이다
다시 열 때  유럽 출시를 결정할 때. 보류 표에 등재했다
```

**⑩ AdSense 계정 — 등록하고 심사를 기다린다**

```
등록 도메인   appleap385-aaz0919.github.io
client       ca-pub-5074778849873789
소유권 확인   코드 스니펫 방식 (앱 index.html · 방침 · 유저 사이트 세 곳 <head>)
상태         검토 대기
slot         미발급 — 승인 후 app/src/lib/ads.js의 AD_SLOT에 채운다
```

⚠ 승인되면 할 일은 **`AD_SLOT` 한 줄**이다. 그 순간 광고가 뜨므로,
채우기 전에 360px에서 위계를 다시 재는 것이 순서다(2.36절 절차).
`ads.test.js`의 "slot이 비면" 검사도 그때 갱신해야 한다.

**⑪ ⚠ 곁가지로 드러난 것 — 규모 수치의 "앱 N건"은 선언 수다**

`stats.py`가 줄 첫머리 `test(`를 세는데, 주석에 "러너 집계와 같다
(정적 110 = 러너 110)"고 적혀 있었다. **지금은 다르다.**

```
정적 140   선언된 test( 수
러너 150   실제 실행 수
```

반복문이 `test()`를 여러 번 부르는 자리 때문이다 — `verses.test.js`의
`AD_FREE_SCREENS`(1→2)와 이번 `ads.test.js`의 실측 분포표(1→10).
**AD_FREE_SCREENS 쪽은 이번 세션 이전부터 있었다** — 즉 "앱 119건"도
이미 1건 적게 적혀 있었고 아무도 몰랐다. 게이트가 문서와 정적 수치만
대조하기 때문이다.

고치지 않고 **정의를 고정했다.** 실행 수를 알려면 node를 띄워야 하는데
그러면 "저장소 파일만 읽어 같은 커밋이면 같은 답"이라는 이 스크립트의 성질이
깨진다. 주석을 사실대로 바꾸고 차이를 적어 뒀다.

✅ **확정 (2026-08-25) — 선언 수로 둔다. 러너 집계로 바꾸지 않는다.**

```
근거 1   러너 집계는 node 실행을 요구한다 → "저장소 파일만 읽어 같은 커밋이면
        같은 답"이라는 stats.py의 성질이 깨진다
근거 2   실행 수는 게이트가 이미 검증한다 — 두 번 셀 이유가 없다
```

⛔ **정적 수치와 러너 수치가 다른 것을 결함으로 보고 이 판단을 다시 열지 말 것.**
  세는 대상이 다를 뿐이다. 차이의 출처(반복문 자리)는 위에 적혀 있다.
  ★ 그러므로 반복문으로 `test()`를 부르는 자리를 새로 만들면 두 수치는 **더**
    벌어진다. 정상이다. 게이트는 정적 수치만 문서와 대조한다.

---

### 2.56 CONTACT 확정과 OPERATOR 노출처 (2026-08-24)

**① 문의처를 `appleap385@gmail.com`으로 확정했다 — 빈 칸을 채운 것이 아니다**

```
이전   aaz0919@gmail.com        c7471e8에서 넣었던 값
지금   appleap385@gmail.com     사용자 확정값
```

⚠ **이미 배포돼 있던 주소를 바꾼 것이다.** 방침 8절이 공개 페이지라 이전 주소가
이미 인터넷에 나가 있었다. 되돌릴 수 없는 종류의 변경이므로 기록해 둔다.
두 곳(`About.jsx`의 `CONTACT` · 방침 8절)을 함께 바꿨고, 기존 회귀
"방침과 앱이 같은 문의처를 말한다"가 둘의 일치를 계속 지킨다.

**② "배포본 이메일 0건"이라는 전제를 의도적으로 바꿨다 — 게이트로 만들었다**

2.50 ①이 손으로 훑어 "배포본 0건"을 확인했다. 그 전제는 **c7471e8 시점에
이미 깨져 있었다** — 문의처를 넣는 순간 배포본에 이메일이 1건 생겼는데,
그 사실을 감시하는 장치가 없었다(수동 감사는 1회성이다).

```
바꾼 것   "0건"  ->  "확정된 CONTACT 하나만"
검사      app/src의 화면 소스 + 방침 HTML에서 이메일을 전수로 긁어
          전부 CONTACT와 같은지 본다 (verses.test.js)
```

★ **주소를 여기에 적어 두지 않았다.** 검사가 `About.jsx`의 `CONTACT`를 읽어
기준으로 삼는다 — 주소를 바꾸려면 여전히 한 곳만 고치면 된다.
⚠ 이제 회사 이메일이나 다른 주소가 화면 쪽에 섞여 들어오면 게이트가 걸린다.

**③ OPERATOR 노출처 — 한 곳뿐이다. 값은 아직 비워 둔다**

```
파일     app/src/components/About.jsx:42   const OPERATOR = ""
화면     "이 앱에 대해" > **만든 곳** 절 (문의처·버전과 같은 절)
문장     ⛔ **문장이 없다.** 라벨도 수식도 없이 값만 한 줄로 그린다
         <p style={styles.body}>{OPERATOR}</p>
순서     만든 곳 / OPERATOR / CONTACT / "버전 x.y.z"
조건     빈 문자열이면 그 줄을 아예 그리지 않는다.
         셋 다 비면 "만든 곳" 절 자체가 사라진다
방침      ⛔ 없다. 2026-08-24에 "운영자" 줄을 뺐고 지금도 없다
```

⚠ **값이 곧 문장이다.** 라벨이 없으므로 `"개인 개발자"`라고 넣으면 화면에
"개인 개발자" 한 줄만 뜬다. 이름을 넣으면 이름만 뜬다.
**무엇을 넣든 그것이 그대로 공개 문구가 된다** — 그래서 사용자가 정한다.

🔴 **판단 필요** — 개인인지 팀/법인인지. 정해지면 `OPERATOR` 한 줄이다.

---

### 2.57 찬양 3곳 승인과 OPERATOR 확정 (2026-08-24)

**① allowlist 16 → 19 — 3차 후보 찬양 계열을 승인했다**

사용자가 채널 정보란·커뮤니티를 대조해 확인했고(2026-08-24), 그 결과로 승인했다.

```
택피아노 | TAEK PIANO      UCxg0moz5sPXoREVDuML2VVg   worship
mini Music 미니뮤직        UCa1a7JFwJfDs-1nbc1YWZDw   worship
Worship Flow 워십플로우     UCKSp2fsctC4MO33IOb8U5XA   worship
```

★ **기준 1의 근거가 기존 16건과 다르다.** 셋 다 교회도 선교단체도 아닌
**독립 연주·플레이리스트 채널**이라 "교단 소속"으로 정체를 확인할 수 없다.
대신 **교리 콘텐츠 부재 + 운영 주체 확인**을 근거로 삼았다.

⚠ 이 유형은 설교자·교단이 제목에 드러나지 않아 정체 확인이 가장 어렵다.
같은 유형을 다음에 또 만나면 **이 항목의 근거 형식을 그대로 쓸 것** —
`affiliation_verified`에 "무엇이 없어서 안전한가"를 적는 방식이다.

**② mini Music의 "번영신학 1"을 실격 사유가 아닌 것으로 판정했다**

```
신호가 붙은 제목   "[미니뮤직 LIVE] 실시간 연주 찬양 | 테마= 축복합니다"
왜 걸렸나         전역 금지어 `축복`
실제로는          그 문자열이 **연주 곡명 「축복합니다」**다
판정              곡명 적중이지 채널의 콘텐츠 성격이 아니다 → 기준 3 통과
```

⛔ **이 신호를 근거로 재검토를 다시 열지 말 것.** 같은 곡을 다시 올리면 같은
신호가 또 뜬다. 다시 열려면 **곡명이 아닌** 근거가 필요하다.
⚠ 일반화: 성격 신호는 제목 문자열 적중이라 **고유명사(곡명·프로그램명)에서
오탐이 난다.** 숫자가 작을 때는 어느 제목이 걸렸는지 먼저 볼 것.

**③ ⚠ `crisis_eligible`을 항목에 적지 않았다 — 관행을 따랐다**

지시는 "셋 다 false"였고 결과도 false다. 다만 **키를 쓰지 않는 방식**으로 했다.

```
기존 16건   어느 항목에도 crisis_eligible 키가 없다. 로더 기본값이 false다
새 3건에만 적으면   나머지 16건이 다른 값인 것처럼 읽힌다
확인          로더로 검증 — 19건 전부 crisis_eligible=False
```

파일 머리말의 "아래 15건" 주석이 stale이었다(승인이 16건이었다). 19건으로
고치면서 **"어느 항목에도 이 키를 적지 않는다"를 규칙으로 명시**했다.

**④ 배치는 돌리지 않았다 — 내일 cron이 반영한다**

```
지금        allowlist만 바뀌었다. dist/videos.json은 어제 것 그대로다
내일        cron 배치가 새 채널 3곳을 수집한다
그때 볼 것   quiet_worship 풀이 실제로 늘었는가 (지금 10건)
           `피아노 찬양`·`연주 찬양` 어휘가 값을 하는가
```

⚠ **수동으로 돌리지 않는 것이 지시였다.** API 소모 0을 유지한다.
⚠ 배포본 `data/diagnostics.json`의 `theme_pool`이 유일한 실측 출처다 —
   여기에 예상 수치를 적지 않는다.

★ Worship Flow는 **사전 개정 없이도 값을 하던 유일한 채널**이다
("Piano Instrumental"이 걸린다). 나머지 둘은 2.47 ①의 어휘 추가가 있어야
quiet_worship으로 간다 — ⛔ 그 어휘를 "0건이니 죽었다"며 지우지 말 것.

**⑤ OPERATOR 확정 — 값 자체가 문장이다**

```
값     "개인 개발자가 만들어 운영합니다"
자리   About "만든 곳" 절. 라벨 없이 <p>{OPERATOR}</p> 하나 (2.56 ③)
```

★ **단어가 아니라 문장을 넣었다.** 라벨이 없는 자리라 "개인"을 넣으면 화면에
"개인" 한 줄만 뜬다. 값이 곧 공개 문구가 되는 구조를 전제로 고른 값이다.

⚠ About.jsx의 `TODO(스토어 출시)` 주석을 걷어냈다 — 둘 다 채워져 더는 TODO가
아니다. 빈 문자열이면 줄을 안 그리는 구조는 남겼고, **둘 다 비지 않았는지
회귀가 지킨다**(`verses.test.js`).

**⑥ About에 「광고」 절을 넣었다 — 표시 조건을 광고 렌더 조건에 묶었다**

```
문안   "이 앱은 무료로 운영됩니다. 무료 서비스 제공을 위해 영상 목록에
        광고를 최소한으로 싣습니다. 성경 본문을 읽는 자리에는 광고를 두지 않습니다."
조건   {AD_SLOT ? ... : null}  — 광고가 안 나가는 동안 이 문단도 안 그린다.
        번들러가 통째로 접어 배포본에 문장이 남지도 않는다(실측 0건)
위기    **일부러 언급하지 않는다**(사용자 결정). 제외 자체는 코드·방침 그대로다
```

★ **초안의 "말씀을 읽는 자리"를 "성경 본문"으로 바꿨다 — 진행 전에 걸러냈다.**

```
이 앱에서 "말씀"은 **설교 영상 탭의 이름**이다 (ResultTabs LABELS)
  말씀 = 설교 영상 · 찬양 = 찬양 영상 · 이어서 읽기 = 성경 본문
그리고 광고는 VideoList 안에만 있으므로 **말씀 탭은 광고가 뜨는 자리다**
→ "말씀을 읽는 자리에는 광고를 두지 않습니다"는 화면이 곧바로 반증한다
```

⚠ 일반화: **화면 문구를 쓸 때 그 단어가 이미 UI 라벨인지 먼저 볼 것.**
  일상어로는 자연스러운 말이 이 앱에서는 특정 탭을 가리키는 고유명일 수 있다.
  회귀가 `말씀을 읽는 자리`의 복귀를 막는다.

**⑦ About "폰트가 다르다" — ⚠ 첫 진단이 틀렸다 (2026-08-24 정정)**

사용자가 "개인정보처리방침 버튼만 폰트가 다르다"고 짚었다.
처음에 **"버튼에 `fontFamily`가 없어 브라우저 기본 서체로 그려진다"고 적었는데
그것은 틀렸다.**

```
Shell의 전역 스타일에 이미 있다 —  button{ font-family:inherit; cursor:pointer }
                                   (App.jsx:603)
따라서 About의 버튼들도 **처음부터 페이지 서체를 쓰고 있었다.**
```

**실제 차이는 크기와 색이었다.**

```
개인정보처리방침   14px · jade   (styles.body 안의 styles.link)
이 기기에서 지우기  13px · muted  (styles.erase)
```

⚠ **무엇을 잘못했나 — 인라인 스타일만 보고 전역 CSS를 확인하지 않았다.**
  "빠져 있으니 이게 원인"은 그럴듯했지만, 같은 파일 안(App.jsx)의 전역 규칙이
  이미 채우고 있었다. **없는 것을 원인으로 지목하기 전에 다른 층위에서
  채워 주고 있지 않은지 볼 것.**

인라인 `fontFamily: "inherit"`는 그대로 뒀다 — 중복이지만 해롭지 않고,
전역 규칙이 사라져도 버티는 쪽이 낫다. 회귀도 남긴다.

**기록 절 정렬** — 지우기 버튼과 방침 링크를 한 행에 뒀다(gap 14px).
실측(playwright · 360/320px 두 폭):

```
erase    top 595 · w 131 · 13px Noto Sans KR · #8DA29E
privacy  top 595 · w  96 · 13px Noto Sans KR · #7FB3A3
같은 행 ✓ · 간격 14px ✓ · 버튼 1줄 ✓ · 가로 스크롤 없음 ✓ (320px에서도)
```

**⑧ 만든 곳·기록 절 색 통일 — 세 번 갈렸다가 하나로 정해졌다**

```
1차   OPERATOR jade 14px            "초록색 계열로"
2차   CONTACT  muted 13px           "기록 지우기와 동일하게"   → 두 줄이 갈렸다
3차   ★ 전부 13px · T.muted         "완전히 같게"              → 확정
```

★ **이 화면의 조용한 기준선을 13px · `T.muted` 하나로 못 박았다.**
실측(360px)에서 다섯 요소가 전부 같은 값이다:

```
erase · privacy · operator · contact · version
  → 13px · rgb(141,162,158) · Noto Sans KR   (서로 다른 조합 1개)
```

⚠ 다른 것은 **밑줄뿐이다** — 누를 수 있는 것에만 붙는다(방침 링크·메일).
  운영자 줄은 링크가 아니라 밑줄이 없다.
⛔ `styles.link`(jade)를 이 자리에 다시 쓰지 말 것. link는 본문 안의 링크가 쓴다.
회귀가 네 스타일의 크기·색을 함께 검사한다.

**⑨ About에 떠 있는 "돌아가기"를 넣었다 — 2.22절 규칙 그대로**

이 화면도 절이 여섯이라 하단 버튼까지 한참 내려가야 한다. 결과 화면의
"다시 적기"와 **같은 조건·같은 자리·같은 모양**이다.

```
등장    스크롤 100px 뒤          모양   floatingStyle() 공유 — 우하단 · 알약 · 블러
복귀    closeAbout 하나를 하단 버튼과 **함께 쓴다** (경로를 둘로 만들지 않는다)
하단    고정 "돌아가기"는 **유지한다**(사용자 결정). 대체가 아니라 닿기 전까지만 대신한다
겹침    하단 버튼이 화면에 들어오면 떠 있는 쪽이 숨는다
        ⚠ 위치를 비교하지 않고 **보이는가**로 판단한다 — 하단은 가운데, 떠 있는 쪽은
          우측이라 픽셀이 안 겹칠 수도 있지만 피하려는 것은 **중복 노출**이다
```

**실측 (360×640 · HANDOFF 2.22와 같은 조건)**

```
scroll   0 · 60   opacity 0    · aria-hidden true    숨김 (임계 100px)
scroll 150        opacity 0.98 · aria-hidden false   노출
scroll 399·맨아래  opacity 0    · aria-hidden true    하단 버튼 등장 → 숨김
어느 위치에서도 뷰포트 top = 575px · scroll 200→510에서 변화 0px  → 진짜 fixed
```

★ **575px는 2.22절이 FloatingRestart에서 잰 값과 정확히 같다.** 두 버튼이
같은 자리에 선다.

⚠⚠ **Shell에서 그린다.** `.rise` 안에 두면 transform이 containing block을 만들어
  fixed가 죽는다(4.8절). 2026-08-19에 실제로 그렇게 넣었다가 되돌린 자리다.
  `behavior.test.js`가 두 버튼 **모두**의 렌더 위치를 검사한다 —
  떠 있는 버튼을 또 추가하거든 그 목록에 이름을 넣을 것.

**⑩ ⚠ 곁가지 — 테스트에 제어문자가 들어갔다**

회귀를 스크립트로 써 넣다가 `\b`가 **실제 백스페이스 바이트(0x08)로** 파일에
들어갔다. 정규식이 `/fontSize: 13<0x08>/`가 되어 **절대 매치되지 않았고**,
터미널에서는 보이지도 않았다.

⚠ 증상이 "코드는 맞는데 테스트만 실패"로 나타난다. `cat -A`로 바이트를 봐야
  드러난다. 소스를 스크립트로 생성할 때는 **제어문자 스캔을 함께 돌릴 것.**


---

### 2.58 서비스 표시 이름 변경 — 하루에 두 번 바뀌었다 (2026-08-25)

**① 표시명만 바꿨다. 경로·레포·약어는 그대로다** *(1차 — 최종값은 ⑥)*

```
표시명   Peace Your Mind  →  Peace in Your Mind
경로     /PeaceYourMind/      ⛔ 그대로
레포     PeaceYourMind        ⛔ 그대로
약어     PYM                  ⛔ 그대로 (in을 반영해 PIYM으로 바꾸지 않는다 — 사용자 결정)
```

★ **왜 경로는 구명을 유지하는가 — 이유가 셋이다.**

```
1  AdSense 심사가 이 URL로 진행 중이다. 경로가 바뀌면 심사가 끊긴다
2  manifest의 start_url·scope가 경로다. 바꾸면 **이미 설치된 PWA가 깨진다**
   (서비스워커 scope=/PeaceYourMind/ 로 등록돼 있다)
3  앱으로 배포하면 경로는 사용자에게 보이지 않는다 — 구명 유지가 확정 방침이다
```

**② 바꾼 자리 (①사용자 노출 · ③문서)**

```
① 앱      app/index.html <title>
① 앱      manifest.webmanifest  name · short_name
① 방침    privacy/index.html  <title> · <h1> · 1절 본문 · 돌아가기 링크 문구
① 루트    유저 사이트 index.html  <title> · <h1> · "열기" 링크 문구  (다른 레포)
③ 문서    PLAN.md 제목·표 · themes.yaml peace intent · verses.yaml 대표 구절 note
③ 주석    icons/icon.svg 머리말
```

⛔ **손대지 않은 자리(②)** — vite.config.js `BASE` · manifest `start_url`·`scope` ·
  app/index.html의 manifest·아이콘 href · About.jsx `PRIVACY_URL` ·
  deploy-app.yml base 가드 · 방침 돌아가기 링크의 **href**(문구만 바꿨다).

⚠ **About 화면에는 서비스명이 없다.** "이 앱에 대해"라고만 부른다 —
  그래서 바꿀 것이 없었다. 이름이 안 나오는 것이 이 화면의 설계다.

**③ ⚠ 표시명은 한 상수로 모을 수 없다 — 그래서 일치 검사로 묶었다**

```
노출 자리 4곳이 전부 정적 파일이다
   app/index.html · manifest.webmanifest · privacy/index.html · 유저 사이트 index.html
빌드가 이 파일들에 문자열을 주입하지 않는다 (public/은 그대로 복사된다)
게다가 유저 사이트는 **다른 레포**라 어떤 상수도 닿지 않는다
```

★ **의견 — 상수화는 권하지 않는다.** 값을 하나로 만들려면 Vite HTML 치환과
manifest 생성 단계를 새로 들여야 하는데, **얻는 것은 1년에 한 번 있을까 한
이름 변경**이고 잃는 것은 "public/은 그대로 나간다"는 단순함이다.
대신 회귀 3건으로 **자리끼리 어긋나지 않는지**를 지킨다 — 문의처를
`CONTACT` 일치 검사로 묶은 것과 같은 방식이다(2.56 ①).

⚠⚠ **유저 사이트는 이 회귀가 못 본다.** 다른 레포라 테스트가 파일에 닿지 않는다.
  ★ 이름을 또 바꾸거든 **그쪽 index.html을 손으로 함께 고칠 것.** 회귀 주석에도
  같은 경고를 적어 뒀다.

**④ 회귀 3건 (주입으로 무는 것을 확인했다)**

```
표시 이름이 모든 노출 자리에서 같다        5자리 대조
옛 표시 이름이 사용자에게 보이는 곳에 없다   "Peace Your Mind" 잔존 0
이름을 바꿔도 경로·식별자는 구명 그대로다    BASE · start_url · scope · PRIVACY_URL
```

주입 확인 — `manifest.name`을 옛 이름으로 되돌리자 **2건이 실패했다.**

⛔ "약어 PYM 유지" 회귀는 **만들지 않았다.** PYM은 주석과 내부 식별자에만 있고
  화면에 나오지 않는다. 게다가 `readSource()`가 주석을 걷어내므로 검사 대상이
  애초에 없다 — 만들면 늘 통과하는 장식이 된다. 확정 사항은 여기 문서로 남긴다.

**⑤ 곁가지 — `readAppFile()`을 새로 뚫었다**

`vite.config.js`는 `src/` 밖이라 `readSource()`가 닿지 않는데, `test/`에서
`readFileSync`로 `.js`를 직접 읽으면 **4회차 방지 검사가 막는다**(설계대로다).
`helpers.js`에 `readAppFile()`을 더해 원본 읽기를 그 파일 안에만 두었다.

⚠ 이 파일의 주석도 `/PeaceYourMind/`를 인용한다 — 느슨한 `includes()` 검사를
  쓰면 코드를 지워도 주석만으로 통과한다. 3회차 거짓 통과와 같은 모양이다.

**⑥ ★ 같은 날 다시 줄였다 — `Peace in Mind` (2026-08-25, 2차·최종)**

```
Peace Your Mind  →  Peace in Your Mind  →  Peace in Mind
     (원래)              (1차, 18자)          ★ 2차 확정, 13자
```

★ **근거 — `short_name`이 `name`과 용도가 다르다는 것을 1차에서 놓쳤다.**

```
name        설치 대화상자 · 앱 목록에 뜬다. 길어도 된다
short_name  **홈 화면 아이콘 밑 라벨**이다. 길면 런처가 자른다
```

1차에서 둘 다 18자(`Peace in Your Mind`)로 넣었다. 동작에 문제는 없지만
홈 화면에서 잘릴 자리였다. 13자로 줄여 **둘을 같은 값으로 유지**한다 —
갈라 놓으면 사용자가 두 이름을 보게 된다.

⚠ **하루에 두 번 바꾼 이력을 지우지 않는다**(사용자 결정). 위 ①~⑤는 1차
  시점의 사실이고 그대로 둔다. 최종값은 이 ⑥이다.
⛔ **약어 PYM은 이번에도 그대로다.** 화면에 노출되는 곳이 없고, HANDOFF의
  과거 기록 표기가 흔들리면 이력이 당시 사실과 어긋난다(사용자 결정).

**갱신한 자리 — 1차와 같은 10자리 + 회귀**

```
① 본 레포 7   index.html <title> · manifest name·short_name ·
             방침 <title>·<h1>·1절 본문·돌아가기 문구
① 유저 사이트 3  <title> · <h1> · "열기" 링크 문구  (다른 레포 — 손으로)
③ 문서 5      PLAN.md ×2 · themes.yaml · verses.yaml note · icon.svg
② 경로 12     ⛔ 이번에도 손대지 않았다
```

**회귀 갱신 — 옛 이름을 "쌓는다"**

```
RETIRED_NAMES = ["Peace Your Mind", "Peace in Your Mind"]
```

⛔ **이 배열에서 이름을 빼지 말 것.** 하나를 빼면 그 이름이 어딘가에 남아도
  검사가 통과한다. 이름이 또 바뀌면 **현재 값을 이 배열에 밀어 넣는다.**
⚠ 셋 중 어느 것도 서로의 부분 문자열이 아니라 부분 일치 함정이 없다.
  ★ 다음 이름을 고를 때 이 성질을 확인할 것 — 예컨대 `Peace Mind`로 가면
    아무 문제 없지만, 옛 이름을 **포함하는** 이름을 고르면 검사가 헛돈다.

**회귀 1건 추가 — 길이와 일치를 함께 지킨다**

```
name과 short_name이 같고, 홈 화면 라벨이 잘릴 길이가 아니다   (상한 13자)
```

이번 재변경의 **근거 자체를 검사로 박았다.** 다시 길어지면 실패한다.

주입 확인 — `name`에 옛 이름 둘을 각각 넣어 **둘 다 실패**했고,
`short_name`을 20자로 늘리자 길이 검사가 **실패**했다. (전부 복원)


---

### 2.59 형식 보장 폴백 — 토글 한쪽이 비지 않게 (2026-08-25)

**⓪ ★ 먼저 목표를 갈라 둔다 — 이 절이 푼 것은 절반이다**

사용자의 최종 목표는 **"모든 세분류에서 양쪽 탭이 비지 않고 충분히 나오되,
정확도를 잃지 않는 것"**이다. 이것은 서로 다른 두 문제다.

```
"비지 않게"    ← 이번 수정(MEDIA_FLOOR=4)이 푼다. 선정 단계의 구조 문제였다
"충분히"       ← 주제 풀 공급 문제다. **채널 발굴로만 풀린다**
```

주제 23개 중 **10개가 풀 5건 이하**이고 `self_control`은 0건이다. 이 상태에서
"충분히"를 만들려면 폴백을 늘려야 하는데, **폴백은 감정에 맞춰 고른 영상이 아니다.**
늘릴수록 정확도가 떨어진다.

⛔ **다음 세션이 N을 키워서 이 문제를 풀려고 하지 말 것.** N=4는 "빈 화면을 막는
  바닥"이지 "적정 노출량"이 아니다. 6으로 올리면 11개 화면이 영향을 받고, 그중
  상당수가 지금 정상인 화면이다 — 정확한 목록을 흐린 목록으로 바꾸는 거래가 된다.
  ★ "충분히"의 해법은 3절의 채널 발굴이다.

---

**① 무엇이 문제였나 — 공급이 늘수록 나빠지는 구조**

```
need = min(THEME_MAX_VIDEOS - len(picked), FALLBACK_MAX_PER_SUBCATEGORY)
pool = [... if t.media.media_type in (media_default, UNKNOWN)]
```

두 겹으로 막혀 있었다. `need`는 **개수만** 보고, 폴백 풀은 **화면 기본 형식만**
통과시켰다. 20건이 전부 찬양이어도 need가 0이라 폴백이 아예 돌지 않았고,
돌았더라도 반대 형식은 못 들어왔다.

★ **실측 인과 — 찬양 3곳 승인이 방아쇠였다.**

```
quiet_worship 풀 10 → 207   (2026-08-24 배치)
  → 주제분만으로 20슬롯이 참 → need = 0 → 폴백 층 소멸
  → 폴백이 넣어 주던 unknown 3건이 [말씀] 탭의 유일한 재고였다
  → anger.irritation · exhaustion.tired 의 [말씀] 탭 3건 → 0건
```

⚠ **폴백 비율 23.3% → 8.3%라는 "개선"과 같은 사건의 양면이다.** 폴백이 줄어서
좋아 보였지만, 줄어든 자리가 약한 형식의 유일한 재고였다.

---

**② 편집 결정 — 찬양 기본 화면에 말씀 폴백을 넣는다 (사용자 확정)**

이것이 이 변경의 전제다. 데이터 문제가 아니라 편집 판단이라 사용자가 정했다.

```
media_default는 "먼저 보여준다"는 판단이지 "반대 형식을 안 보여준다"가 아니다.
[말씀] 토글의 존재 자체가 그 증거이고, 사용자가 그것을 눌렀다는 것은
설교를 보고 싶다고 명시한 것이다. 거기서 0건은 편집 의도가 아니라 빈 화면이다.
토글이 갈라 주므로 찬양 목록에 말씀이 섞이지도 않는다(visibleVideos).
```

⚠ **반대 형식은 MEDIA_FLOOR까지만이다(갈래 1).** 기본 형식이 16/20을 지켜
편집 의도가 남는다. 바닥 메우기이지 동등한 자리가 아니다.

---

**③ 채택한 설계 — (c) 선정 단계에서 슬롯을 예약한다**

```
(a) 주제분을 밀어낸다   기각 — 근거가 강한 것을 빼고 약한 것을 넣는 방향이 된다.
                       게다가 "무엇을 뺄지" 규칙을 새로 만들어야 한다
(b) 20을 넘긴다        기각 — 20은 데이터 상한이 아니라 제품 결정이다(선택 부담)
(c) 처음부터 안 채운다  ★ 채택 — 형식 균형을 이미 담당하는 fill_balanced 안에서 끝난다
```

`fill_balanced`의 3차 순회를 둘로 나눴다.

```
3-a  약한 쪽을 MEDIA_FLOOR까지 **먼저** 채운다 (풀에 있으면 여기서 끝)
3-b  그래도 모자라면 그 수만큼 슬롯을 비우고 20 미만을 돌려준다
```

⚠ **`fill_balanced`는 이제 항상 20을 채우지 않는다.** 호출부가 빈 자리를 폴백으로
메운다. 20을 넘기지는 않는다.

폴백 풀 필터는 **없애지 않았다.** `target`을 인자로 받게 해서, 슬롯 그룹마다
목표 형식이 달라진다(`select_fallback_layer`가 두 번 부른다).

```
형식 보장분   target = 약한 형식      MEDIA_FLOOR까지만
개수 보충분   target = media_default  남은 자리
```

⚠ 형식 보장분을 **먼저** 뽑는다. 개수 보충분이 남은 자리를 다 가져가고 나면
  보장분이 들어갈 자리가 없다.

---

**④ ★ N=4의 근거 — 배포본 실측 분포에서 나왔다**

```
24화면 약한 쪽 건수:  중앙값 6 · 1사분위 4 · 최소 0
```

4는 중앙값 아래라 건강한 화면을 건드리지 않는다. 감도:

```
N=3 :  4개 화면      N=5 :  7개 화면
N=4 :  5~6개 화면 ★   N=6 : 11개 화면  ← 절반이면 "구제"가 아니라 재설계다
```

⛔ 숫자만 옮기지 말 것. 근거를 `selection.py`의 `MEDIA_FLOOR` 주석에 함께 적었다.

---

**⑤ ⚠⚠ 곁가지에서 진짜 결함이 하나 나왔다 — 예약이 채널 상한을 무너뜨렸다**

`select_theme_videos`의 사다리 종료 조건이 `len(picked) >= THEME_MAX_VIDEOS`였다.
예약을 넣자 **20에 영영 닿지 못해** 사다리가 끝까지 올라갔다.

```
독식채널이 상한 3을 지키던 풀에서 → 상한 5까지 올라가 5건을 가져갔다
```

`spread_test.py` 1절이 잡았다. 목표를 `THEME_MAX_VIDEOS - reserved_slots(picked)`로
고쳤다.

⛔ **비워 둔 자리를 "미달"로 세지 말 것.** 그 자리는 폴백이 받기로 되어 있다.
★ 이 결함은 새 기능이 **기존 완화 로직의 종료 조건과 충돌**해서 생겼다. 목표값을
  바꾸는 변경을 할 때는 그 값을 비교하는 자리를 전부 찾을 것.

---

**⑥ 검증 — 설계안 추정표와 실제를 대조했다. ⚠ 세 곳이 어긋났다**

배포본(2026-08-24T10:12Z)에 새 산술을 적용해 재계산했다.

```
                     추정   실제
anger.irritation      4      4    일치
exhaustion.tired      4      4    일치
calm.ease             3      3    일치
anger.unfair          1      3    ★ 어긋남
boredom.dull          2      3    ★ 어긋남
boredom.novelty       0      1    ★ 어긋남
영향 화면            5개    6개
```

**원인 — 기준선을 잘못 잡았다.** 추정표는 `N - 약한쪽(주제분+폴백 전체)`로 셌는데,
실제 코드는 `N - 약한쪽(주제분만)`으로 센다. 폴백은 지금 계산하는 대상이라
기준선에 넣을 수 없다.

★ **코드 쪽이 옳다.** 개수 보충분이 우연히 unknown을 데려오기를 기대하는 것이
바로 이번 사고의 원인이었다(그 3건이 사라져서 0이 됐다). **바닥은 보장돼야지
기대되면 안 된다.**

**⚠ 그리고 그 보고의 경보 예측도 틀렸다 — 정정한다.**

```
보고했던 것   anger.unfair 폴백 50% → 55%, theme_fallback_heavy 신규 발생
실제          50% → 50%. **경보는 뜨지 않는다**
```

형식 보장 슬롯은 폴백 위에 얹히는 것이 아니라 **같은 빈 슬롯에서 나온다.**
폴백 총량은 `min(빈 슬롯, 12)`로 그대로이고, **구성만 바뀐다.** 폴백 비율이
오르는 것은 주제분이 실제로 줄어든 3개 화면뿐이다.

```
개수가 바뀌는 화면 3개   anger.irritation 20→16 · exhaustion.tired 20→16 · calm.ease 20→17
                        폴백 비율 0% → 20% · 20% · 15%   (전부 경보 임계 아래)
구성만 바뀌는 화면 3개   anger.unfair · boredom.dull · boredom.novelty — 비율 불변
무영향 18개
```

⛔ 그래도 **`FALLBACK_HEAVY_RATIO`를 건드리지 않는다.** `anger.unfair`는 주제분이
  10건뿐이라 50%가 사실이고, 경보는 그 사실을 말하는 것이 맞다.

**⑦ ★ A-1에서 "다음 차례"로 짚은 세 화면이 함께 구제된다**

`boredom.dull`은 `daily_word` 165건인데 채널 상한 때문에 16/20이었다. 말씀 채널이
하나만 더 승인되면 20이 차서 찬양 탭이 1건이 될 예정이었다.

```
주제분이 20을 채우는 날의 시뮬레이션
  boredom.dull      20 → 17 · [찬양] 1 → 4   (규칙 없었으면 1건)
  boredom.novelty   20 → 19 · [찬양] 3 → 4   (규칙 없었으면 3건)
  anger.unfair      20 → 17 · [찬양] 1 → 4   (규칙 없었으면 1건)
```

⚠ **배치는 돌리지 않았다.** 오늘 18:30 cron이 반영한다. 위는 전부 배포된
  산출물로 한 재계산이고, 실제 결과는 내일 아침에 확인할 것.

⛔ 대안 A(주제 매핑에 말씀 공급 주제 추가)는 **기각됐다** — 2026-08-20 [5]
  주제 집합 분리(동일 목록 5쌍 → 0쌍)와 정면 충돌한다.

---

### 2.60 분류 실패에서 입력창을 비운다 (2026-08-25)

**① 사용자 결정과 근거**

```
마음이 힘들어 적은 부정적 문장이 화면에 그대로 남아 있는 것이 이 앱에 맞지 않다.
```

**② ⚠ 두 복귀 경로가 갈려 있었다 — 그것이 결함이다**

```
결과 → '다시 적기'      reset()          setText("") 있음   ✅ 처음부터 비우고 있었다
분류 실패 → '골라서 찾기'  인라인 핸들러    setText("") 없음   ❌
```

인라인 핸들러가 `reset`을 우회하고 있었다. `setText("")`를 한 곳(`resetTo`)에 두고
목적지별로 얇은 함수를 씌웠다.

```
resetTo(mode)      ★ 입력창을 비우는 유일한 자리
reset()            → resetTo("text")     결과에서 돌아올 때
resetToPicker()    → resetTo("select")   분류 실패에서 나올 때
```

⚠⚠ **`resetTo`를 `onBack`에 직접 넘기면 안 된다.** `Msg`가 `onClick={onBack}`으로
  넘겨서 **클릭 이벤트가 첫 인자로 들어온다** — `mode` 자리에 이벤트 객체가 앉는다.
  그래서 인자 없는 함수 둘로 나눴다. 회귀가 이것을 검사한다.

**③ 🔴 안내 문구 — 약하다. 제안만 하고 적용하지 않았다**

```
현재  "제가 잘 못 알아들었어요. 아래에서 가까운 마음을 골라주실래요?"
      (taxonomy.json ui.no_match · sub는 빈 문자열)
```

⚠ 두 군데가 어긋난다.

```
1. "아래에서"인데 **아래에 아무것도 없다.** 버튼("골라서 찾기") 하나뿐이고,
   고르는 화면은 그것을 눌러야 나온다
2. 이제 입력창을 비우므로 **재시도할 실마리가 사라진다.** 그런데 문구는
   다시 적어도 된다는 말을 하지 않는다. 다시 적으려면 고르는 화면으로 가서
   "직접 적기"를 한 번 더 눌러야 한다 — 두 번 이동이다
```

제안:

```
title  "제가 잘 못 알아들었어요."
sub    "가까운 마음을 골라 보시거나, 다른 말로 다시 적어 주셔도 좋아요."
```

🔴 **적용하지 않았다 — 사용자 확인 대기.** 문구를 바꾸면 `sub`가 처음으로 채워지고,
  "다시 적어도 된다"를 말하려면 그 경로가 두 번 이동인 것도 함께 볼 문제다.

---

### 2.61 말씀 채널 3곳 승인 — 목적이 다른 승인이다 (2026-08-25)

**① ★ 왜 넣었나 — 말씀 공급이 구조적으로 얇다**

```
화면 기본 형식   찬양 19 : 말씀 5
최근 증설        전부 찬양 (2026-08-24 택피아노 · mini Music · Worship Flow)
결과             대부분 세분류의 [말씀] 탭이 4~7건대에 머문다
```

2.59의 `MEDIA_FLOOR=4`는 **"비지 않게"까지만** 푼다. 이 승인이 **"충분히"** 쪽이다.
⛔ 둘을 섞지 말 것 — 폴백을 늘려 "충분히"를 만들면 정확도를 잃는다(2.59 ⓪).

```
allowlist 19 → 22
  분당우리교회   UCBh3Qv-rKu6ZDxEcrio70Hw   sermon
  갓피플TV      UCLbOYoltlCD0LN06fSwKTsQ   sermon
  사랑의교회     UCceXHeKTEwpcA7MmDvScXNQ   mixed
content_type 분포  sermon 9 · mixed 6 · worship 5 · devotion 2
```

⚠ 배치는 돌리지 않았다 — 2026-08-25 18:30 cron이 반영한다.

---

**② ⚠⚠ "C채널 계열"은 넣지 않았다 — 이미 전부 처리돼 있었다**

지시는 "말씀 채널 4곳(분당우리교회·사랑의교회·갓피플TV·C채널 계열)"이었지만,
C채널 계열 3개는 **2026-08-24에 이미 판정이 끝나 있다.**

```
C채널방송 : 설교   UC9Ac9DjBEmNqjAYxf8V1HPA   ✅ 이미 allowlist에 있다 (중복이 될 뻔했다)
C채널 굿데이       UCe6ZSDh2UJFJvVuO6MKEWeA   ⛔ reviewed_out — 기준 3 (뉴스 주력)
C채널방송 : TV     UCWC6OG8J2sYmAOT8a23-K8g   ⛔ reviewed_out — 기준 3 (쇼츠 5/10 · mixed)
```

★ **제외 두 건을 되살리지 않았다. 근거가 서로 다른 것을 말하기 때문이다.**

```
제외 사유        기준 3 — 콘텐츠 성격 (뉴스 리포트 · 쇼츠 비중)
이번 승인 근거    "정상적인 기독교 채널이며 이단이 아니다" = 기준 1·2
```

두 문서 모두 **"이단 문제로 제외한 것이 아니다"**라고 명시한다. 즉 이번 판단은
그 제외 사유를 건드리지 않는다. 게다가 이번 승인의 목적이 **말씀 공급 확충**인데,
굿데이는 교계 뉴스 채널이라 말씀을 공급하지 않고 TV는 쇼츠가 절반이다.

🔴 **되살리려면 기준 3에 대한 판단이 따로 필요하다.** 두 항목 모두
  `recheckable: true`이고 `recheck_condition`이 적혀 있다.

---

**③ 🔴 교단명을 적지 않았다 — 지어내지 않기 위해서다**

지시는 "교회는 소속 교단"이었으나 **저장소 안에 1차 자료가 없다.**

```
분당우리교회   2.9절 ①에 "홈페이지에 교단 표기 없음"이 이미 기록돼 있다 —
              기준 1 완화(교단명 확정 → 실체 확인)의 근거가 된 사례가 바로 이 교회다
사랑의교회     저장소 안에 근거 없음
갓피플TV      교회가 아니다 — 지시대로 성격으로 적었다(기독교 미디어 · 초교파 제휴)
```

★ **실재하는 교회에 대한 사실 진술이라 추정으로 채우지 않았다.** 기준 1은
"교단명 확정"이 아니라 "운영 주체가 실재하는 정상 기독교 기관인가"이므로
요건은 충족한다. 교단이 확인되면 `affiliation` 한 줄씩이다.

⚠ **1c(이단 규정 대조)는 사용자 대조에 의존한다.** `channel_criterion_c.20260824.md`는
  C채널 계열 3건만 다루므로 이 셋은 그 문서에 없다.

---

**④ ⚠ 갓피플TV의 고유 위험 — 제휴 재배포 구조**

제목이 전부 `… : 갓피플TV [공식제휴]`로 끝나고 본문은 제휴 교회 목회자의 설교다.
최근 10건에서만 제휴처 8곳이 확인된다(수원하나교회·제자광성교회·주내힘교회·
베이직교회·기쁨이있는교회·우리는교회·뉴송처치 등).

⛔ **채널 하나를 승인한 것이 그 8곳을 승인한 것은 아니다.** C채널방송:설교와 같은
  형태지만 제휴처 수가 훨씬 많다. 120일 재검토 때 **제휴처 목록이 바뀌었는지**를
  함께 볼 것. allowlist 항목에도 같은 경고를 적었다.

**⑤ 태깅 전망 — 판별 경로가 셋 다 다르다**

```
갓피플TV      제목에 "설교"가 직접 있다      → media_type 1순위(제목 어휘)로 확정. 가장 깨끗하다
분당우리교회   제목에 성경 장절이 노출된다     → 2순위(본문 장절)로 sermon
사랑의교회     성경 낭독물이라 형식 어휘가 없다 → 2순위에 기대야 한다. **가장 불확실하다**
```

⚠ 세 채널 다 첫 배치 뒤 `theme_pool`과 `media_type_gaps`를 다시 볼 것.
  ⚠ 분당우리교회 새벽기도회가 호세아 연속 강해라 **한 주제로 쏠릴 수 있다.**

---

### 2.62 분류 실패 문구와 경로를 함께 고쳤다 (2026-08-25)

**① 문구만 고치면 화면이 거짓말을 한다**

```
전   "제가 잘 못 알아들었어요. 아래에서 가까운 마음을 골라주실래요?"
후   "제가 잘 못 알아들었어요."
     "가까운 마음을 골라 보시거나, 다른 말로 다시 적어 주셔도 좋아요."
```

⚠ 새 문구가 "다시 적어도 된다"고 말하는데 **그 길이 두 번 이동이었다** —
고르는 화면으로 간 뒤 거기서 "직접 적기"를 또 눌러야 했다.
★ 그래서 이 화면에 **'다시 적기' 보조 버튼을 함께 넣었다.** 문구와 경로를 따로
고치면 안 되는 자리다. 회귀가 **둘의 일치**를 검사한다 — 문구에 "다시 적"이
있으면 버튼도 있어야 하고, 없으면 없어야 한다.

```
주 버튼   "골라서 찾기"  테두리 jade 13px   → resetToPicker
보조      "다시 적기"    #ffffff66 12.5px   → reset
```

보조 버튼 모양은 `Closing`의 "다시 적어보기"를 그대로 썼다 — 새로 정하지 않았다.
⚠ `onAlt`가 있을 때만 그린다. 빈 입력 화면(`empty_input`)에는 안 뜬다.

**② `no_match`를 문자열에서 배열로 바꿨다**

`empty_input`이 이미 `items: [제목, 부연]`이었다. 두 실패 화면의 모양을 갈라
두지 않으려고 같은 형태로 맞췄다. `gen_taxonomy_json.py`도 함께 고쳤다.

---

### 2.63 `구려`·`구질` 계열 어휘 추가 — 어절 결합만 (2026-08-25)

실사용 조사에서 "기분이 구려"·"기분이 구질구질해"가 미분류였다.
**매칭 실패가 아니라 사전에 어휘가 아예 없었다** — 2026-08-20 `나쁘` 사고와 같은 유형이다.

```
추가   기분이구려 · 기분구려 · 기분이구질구질   → anger.irritation (키워드 920 → 923)
```

⛔ **단독형을 넣지 않았다. 실측 근거가 있다.**

```
`구려`      "고구려 역사 공부가 안 돼" · "그렇게 하시구려"가 걸린다
`구질구질`   "날씨가 구질구질하다" — 감정 진술이 아니라 날씨 진술이다
```

⚠ `기분구질구질`은 **일부러 뺐다.** 조사 없는 "기분 구질구질해"는 실제로 쓰지 않는
말투다. `{기분, 기분이}` 짝을 기계적으로 채우지 않는다.

**오탐 실측 (2.14절 절차 · API 0)**

```
정탐        의도한 7문장 전부 anger.irritation
오탐        단독형 함정 8문장 전부 nomatch — 한 건도 새지 않았다
사전 충돌   923개 어휘와의 부분 문자열 포함 관계 0건
전수 회귀   923개 어휘를 그대로 입력했을 때 새 어휘 때문에 소속이 바뀐 것 0건
위기 경로   "죽고 싶어" · "기분이 구려 죽고 싶다" 둘 다 crisis 유지
```

★ `retag_titles.py --probe`는 **쓰지 않았다** — 그것은 영상 제목 사전용이고,
이 어휘는 사용자 입력에 걸린다. 대상이 다르면 도구도 다르다.

---

### 2.64 영상 상한을 탭별로 — 총량 20에서 탭당 20으로 (2026-08-26)

**① ⚠ 어제 (b)안을 기각한 근거는 이 재해석에서 **무효**다 — 정정한다**

```
2026-08-25 기각 사유   "20은 데이터 상한이 아니라 제품 결정이다(선택 부담)"
2026-08-26 재해석      탭별 20은 **사용자가 한 번에 보는 목록**을 20 이하로 유지한다.
                      그 결정을 어기지 않는다
```

★ **총량 20은 두 탭이 서로의 자리를 뺏는 구조였다.** 찬양이 16이면 말씀은 4밖에
못 가진다. 그런데 사용자는 한 탭만 본다 — 총량으로 묶을 이유가 없었다.
제 기각 논리가 이 안을 막지 못한다. 사용자 지적이 맞다.

**② 무엇을 바꿨나**

```
select_tab_layers()   탭마다 주제분 → 그 탭의 부족분을 그 탭 폴백이 (신설)
TAB_MAX_VIDEOS = 20   탭 하나가 보여 줄 최대
FALLBACK_MAX_PER_TAB  12 → 20  ⚠ 정책 반전 (아래 ④)
```

★ **주제분이 늘면 폴백이 자동으로 줄어드는 성질은 그대로다.** 각 탭에서
주제분을 먼저 채우고 `need = 20 - 이미 보이는 수`만 폴백으로 메운다.

⚠ 채널 상한은 탭마다 따로 걸린다 — **한 채널이 말씀 3 + 찬양 3까지** 가질 수 있다.
전에는 `used`를 형식 사이에 공유해 3이 상한이었다. 탭이 갈라진 이상 한 탭 안에서
3이면 충분하다(사용자는 두 탭을 겹쳐 보지 않는다).

**③ ⛔ 어제 넣은 것 둘을 걷어냈다 — 되살리지 말 것**

```
fill_balanced의 "반대 형식 자리 예약"   ⛔ 제거
select_fallback_layer(형식 보장분)      ⛔ 제거
reserved_slots()                        ⛔ 제거
```

⚠⚠ **예약을 되살리면 모든 탭에서 무조건 4자리가 빈다.** `fill_balanced`는 이제
**탭으로 걸러진 풀**을 받는다 — 그 풀에는 한 형식 + unknown만 있어 반대 형식이
구조적으로 0이고, `weak_side`가 언제나 부족을 외친다. 주석에 못 박아 뒀다.

`MEDIA_FLOOR = 4`는 **남긴다.** 다만 역할이 좁아졌다 — 이제 슬롯 예약이 아니라
**공급이 모자라 탭이 20에 못 닿았을 때의 진단선**이다. 판정 기준도 지시대로
"총 20 안에서 약한 쪽"에서 **각 탭의 부족분**으로 바뀌었다(`need`가 곧 그 값이고
언제나 MEDIA_FLOOR보다 크다).

**④ ⚠⚠ 폴백 60% 천장을 없앴다 — 정책 반전이다**

```
전   FALLBACK_MAX_PER_SUBCATEGORY = 12   "폴백이 화면을 다 덮지 못하게 막는다"
후   FALLBACK_MAX_PER_TAB        = 20   한 탭이 100% 폴백이 될 수 있다
```

근거(사용자 2026-08-26): 폴백 층은 헤더가 "딱 맞는 건 아니지만"이라고 **이미
밝히고 있으므로** 정직성이 훼손되지 않고, **선택지가 많은 편이 사용자에게 낫다.**
주제분이 0인 탭이 실제로 있는데(anger.irritation의 [말씀]) 12로 끊으면 이유 없이
8자리를 비우는 셈이다.

⛔ **"폴백이 과반이다"를 결함으로 보고 이 값을 다시 낮추지 말 것.** 그것은
**공급 부족의 신호**이고, 그 사실은 아래 경보가 계속 말한다.

**⑤ 경보를 탭별로 잰다**

```
전   fallback_ratio = 폴백 / 화면 전체
후   tab_fallback_ratio = {말씀: …, 찬양: …}  ← 경보는 이것으로만 판정
```

⚠⚠ **화면 평균으로 두면 한 탭이 100% 폴백이어도 가려진다.** 실측으로 확인했다 —
말씀 100% · 찬양 0%인 화면의 평균이 38%다. 어느 탭도 설명하지 못하는 숫자다.
`SubcategoryResult.fallback_ratio`는 남겨 뒀지만 독스트링에 "경보에는 쓰지 말 것"을
적었다.

**⑥ 실측·추정 수치**

```
화면당 고유 영상   20.0건 → 39.5건 (1.98배)   ※ unknown이 양쪽 탭에 세지만 데이터는 한 벌
videos.json       188KB → 약 372KB           ※ 슬롯당 402바이트 x 948
폴백 재사용        평균 3.9개 화면 → 6.7개 화면
                  (미태깅 실효 재고: 말씀쪽 50 · 찬양쪽 41 · 겹치는 unknown 14)
48개 탭 전부 20건에 닿는다 (재고가 받친다)
```

⚠ 크기·재사용은 **추정이다.** 실제 값은 2026-08-26 18:30 cron 결과로 확인한다.
말씀 채널 3곳(2.61)이 같은 배치에서 처음 반영되므로 주제분 비중도 함께 오른다.

**⑦ ★ 광고 노출이 최대 6개가 된다**

광고 자리 계산은 **이미 탭별**이다(`layersFor` → `VideoList` → `adPositions`).
그래서 규칙은 그대로 성립하고 고칠 것이 없다. 다만 두 탭이 다 차면서

```
한 목록 안 밀도   3 / 20 = 15%   (불변)
한 화면에서 양쪽을 다 보면   3 → 최대 6개
```

⚠ Better Ads 기준은 **한 화면(뷰포트) 안의 밀도**라 위반이 아니다. 그래도
승인 후 AD_SLOT을 채울 때 이 사실을 함께 볼 것.

**⑧ ⚠ 곁가지 — 폴백의 진짜 천장은 상수가 아니라 채널 수다**

`select_fallback_videos`는 채널당 상한 3을 걸고 **완화 사다리가 없다.**
따라서 그 탭 폴백의 천장은 `3 x (그 형식 미태깅 채널 수)`다.

```
실측(2026-08-19 코퍼스)  말씀 12채널 → 36 · 찬양 10채널 → 27 · unknown 5채널 → 14
```

⚠ 테스트 픽스처를 4채널로 뒀다가 "20에 못 찬다"로 **잘못 읽을 뻔했다** —
코드가 아니라 픽스처의 한계였다. 그 성질을 검사로 못 박았다(spread_test 8-b).

---

### 2.65 빈 광고 칸이 32px 여백을 만들던 결함 (2026-08-26)

**① 무엇이었나**

```jsx
{adsAfter?.has(index + 1) ? (
  <li style={styles.item}><AdSlot /></li>   // AD_SLOT === "" → 안쪽만 null
) : null}
```

`<li>`가 **조건 없이** 그려졌다. 목록이 `display:grid · gap:16px`라 높이 0인 칸이
격자 행 하나를 차지하고 양옆에 16px씩 붙었다.

```
라이브 실측 (360px)
  정상 항목 간격          16px
  빈 광고 칸이 낀 자리    32px   ← 정확히 2배
  "짜증나" [찬양]  16건 중 5·11·14번 뒤 3곳
```

★ 광고 자리가 화면마다 달라 **"어떤 화면엔 있고 어떤 화면엔 없다"**로 보였고,
사용자는 이것을 "폴백 층 헤더가 사라지고 여백만 남았다"로 읽었다.

**② ⚠⚠ 프로덕션 전용이었다 — 회귀 설계의 핵심**

dev에서는 `AdSlot`이 자리표시자를 그려 간격이 정상이다. **개발 중에는 보이지
않는다.** 2026-08-24 광고 구현(4811cf8) 이래 이틀간 아무도 못 봤다.

⛔ **회귀를 dev 조건으로 쓰지 말 것.** `AD_SLOT`이 빈 값(=지금)일 때 성립해야 한다.
`AD_SLOT_RENDERS_FOR_PROD(slot, isDev)`로 두 조건을 다 검사하고, 그 식이
원문과 같은지도 대조한다.

**③ 고친 방법**

```js
export const AD_SLOT_RENDERS = Boolean(AD_SLOT) || IS_DEV;   // AdSlot.jsx
{AD_SLOT_RENDERS && adsAfter?.has(index + 1) ? (<li>…</li>) : null}
```

⚠ `IS_DEV`는 `import.meta.env.DEV` 그대로여야 vite가 정적으로 접는다
(2.55의 옵셔널 체이닝 사고와 같은 자리). 형태를 회귀가 지킨다.

**④ ⚠ 헤더 자체는 결함이 아니었다**

`fallback.length > 0`일 때만 그린다. `layers`가 **탭별로 걸러진 뒤** 넘어오므로
헤더는 그 탭에 폴백이 있을 때만 뜨고 토글을 바꾸면 유지되지 않는다 — 설계대로다.

★ 다만 MEDIA_FLOOR(2.59) 이후 **폴백이 한 탭에만 몰려** 그 현상이 두드러졌다.
`anger.irritation`의 [말씀] 탭은 통째로 폴백 층이라 주제분 헤더가 아예 없었다.
탭별 상한(2.64)으로 두 탭이 다 차면서 이 쏠림은 완화된다.

---

### 2.66 폴백 천장 20을 유지한다 — 2026-08-19 결정을 뒤집은 자리 (2026-08-26)

**① 원래 천장이 막던 것**

2026-08-19에 **사용자 결정**으로 `FALLBACK_MAX_PER_SUBCATEGORY = 12`(20슬롯의 60%)를
두었다. 당시 기록(2.14절)의 문장 그대로:

```
20건을 못 채우는 것은 허용하되 채우려고 폴백을 무제한으로 늘리지 않는다.
그러면 그 화면은 "감정에 맞춘 목록"이 아니라 **"채널 최신 목록"**이 되기 때문이다.
```

★ **다만 그 천장이 실제로 막은 것은 비율이 아니라 길이였다.** 도입 당시
`anger.irritation`은 **폴백 100%**였다 — 주제분이 0이라 폴백 12건이 전부였고
12/12 = 100%다. 천장은 "폴백만으로 된 목록이 길어지는 것"을 막았을 뿐,
"폴백으로 뒤덮이는 것" 자체는 막지 못했다(PLAN 3.3의 표현보다 실제 동작이 약하다).

**② 2026-08-26 결정 — 유지한다 (사용자)**

탭당 20으로 바꾸면서 **"탭이 비지 않는 것"을 "채널 최신 목록이 되는 우려"보다
우선하기로** 정했다. 근거는 2.64 ④와 같다 — 폴백 층은 헤더가 "딱 맞는 건
아니지만"이라고 이미 밝히고 있고, 선택지가 많은 편이 사용자에게 낫다.

**③ 두 안의 실측 차이 — 비율은 같고 길이만 다르다**

```
                     천장 12        천장 20 (채택)
20건에 닿는 탭        30 / 48        48 / 48
20에 못 닿는 탭       18 (최소 12)   0
폴백 과반 탭          24             24      ← 같다
폴백 100% 탭           2              2      ← 같다
```

⚠ **경보는 두 안을 구별하지 못한다.** 과반 탭 24개는 어느 쪽이든 같다.
길이가 12냐 20이냐는 경보 밖의 문제다 — 그래서 이 판단은 데이터가 아니라
편집 결정이었고, 사용자가 정했다.

**④ ⛔⛔ 다음 세션에게 — 천장을 되살리지 말 것**

```
⛔ "폴백이 과반인 탭이 24개나 된다"를 근거로 FALLBACK_MAX_PER_TAB을 낮추지 말 것.
   그 수는 천장을 12로 되돌려도 **똑같이 24개다**(위 표). 천장은 비율을 못 바꾼다.
⛔ "화면이 채널 최신 목록처럼 보인다"는 관찰도 근거가 되지 않는다. 그것은
   **공급 부족의 증상**이지 이 상수의 문제가 아니다 — 해법은 채널 발굴이다(2.59 ⓪).
✅ 되살릴 유일한 근거: 사용자가 "탭이 짧아도 좋으니 폴백을 줄이자"고 **다시 정할 때.**
   그때는 이 절과 2.14절을 함께 읽고, 18개 탭이 12~19건으로 짧아지는 것을 감수한다는
   뜻임을 확인할 것.
```

⚠ 이 변경은 **지시에 없던 정책 변경이었다.** 2026-08-26에 조사·보고했고 사용자가
유지를 택했다. 같은 일이 반복되지 않게 적어 둔다 — **사용자 결정으로 박힌 상수를
바꿀 때는 먼저 묻는다.**

---

### 2.67 MEDIA_FLOOR를 정식 경보로 — theme_too_few와 합쳤다 (2026-08-26)

**① 무엇이 문제였나 — 화면 단위 검사가 눈이 멀었다**

`theme_too_few`는 **화면 합계**(주제분+폴백)가 `SUBCATEGORY_MIN_VIDEOS`(8) 미만일 때
울렸다. 탭별 상한(2.64) 이후로는 그 수가 **사용자가 보는 것과 다르다.**

```
말씀 20 · 찬양 2 인 화면
  화면 합계 22 → 옛 검사 통과
  찬양 탭 2건  → 찬양 쪽 사용자에게는 사실상 빈 화면
```

그리고 MEDIA_FLOOR는 `logger.error`로만 남아 **아무도 보지 않았다.**
"있다고 믿는 검사가 없는 상태"다.

**② 어떻게 합쳤나 — 겹치는 게 아니라 한쪽이 다른 쪽을 포함한다**

```
전   theme_too_few   화면 합계 < 8        (critical)
     MEDIA_FLOOR     탭 < 4               (로그만)
후   theme_too_few   탭 노출 < 4          → critical  "사실상 빈 탭"
                     탭 노출 < 8          → warning   "목록 성립 불가"
```

★ **탭 검사가 화면 검사를 수학적으로 포함한다.** unknown이 양쪽에 세이므로
각 탭의 노출 건수는 화면 합계 이하다(`vis <= count`). 따라서 화면 합계가 8 미만이면
**두 탭 모두 반드시 걸린다** — 옛 검사가 잡던 것을 하나도 놓치지 않는다.
회귀로 그 성질을 고정했다(spread_test 8-c).

⚠ 경보 **type은 `theme_too_few` 그대로 뒀다.** 라우팅 표(Issue행)와 Issue 이력의
연속성을 깨지 않기 위해서다. 제목·본문이 어느 탭인지 말한다.

**③ 심각도를 둘로 나눈 이유**

```
< 4  critical   그 형식으로 내놓을 것이 사실상 없다
< 8  warning    얇다. 목록으로 성립하지 않는다
```

두 값이 하나였다면 "4는 8보다 훨씬 나쁘다"를 표현할 방법이 없다. MEDIA_FLOOR가
숫자로만 남아 있던 것을 **심각도의 이름**으로 바꾼 셈이다.

⚠ 이제 `MEDIA_FLOOR`는 선정 로직에 관여하지 않는다(2.64 ③). **경보 임계값이
유일한 쓰임이다.** 그 사실을 상수 주석에 적어 둘 것.

---

### 2.68 ★ 배치를 하루 2회로 — 원래 근거는 살아 있고, 가정 하나가 틀렸다 (2026-08-26)

`build.yml`에 `cron: "30 21 * * *"`(KST 06:30)를 추가했다. 기존 09:30 UTC
(KST 18:30)는 **그대로 둔다.**

**① 2.15절의 결정은 뒤집히지 않았다.**

18:30을 고른 근거는 "쿼터 리셋과의 거리"가 아니라 **"그날 올라온 영상을 담느냐"**
였다(2.15절). 실측으로 재확인했다 — KST 18:00까지 그날 업로드의 **86%**가 들어온다.
그 성질이 유지되므로 슬롯 추가는 기존 판단과 충돌하지 않는다. **18:30을 옮긴 것이
아니라 앞에 하나를 더 놓은 것이다.**

**② ★ 그러나 주석이 적어 둔 업로드 분포는 틀렸다.**

2.15절 주석은 "새벽예배 KST 05~07 + 주일예배 KST 12~14에 몰린다"고 적었다.
`dist/titles.json` 30일 523건으로 시각 분포를 처음 실측했다.

```
KST 00시   94건  ████████████████████  ★ 이 중 91건이 정각 00:00
KST 05~07시 120건  새벽예배            — 주석이 맞은 부분
KST 15~19시 156건  ← 두 번째 봉우리는 여기다
KST 12~14시  25건  ← 주석이 지목한 창은 실측상 가장 얇다
```

**가장 큰 단일 봉우리는 아무도 몰랐던 KST 00시 예약 발행이다.** 91건이 정각
00:00에 몰려 있고 채널이 특정된다 — **CBS설교 61 · CGN 생명의 삶 30**. 사람이
그 시각에 올리는 것이 아니라 예약 업로드다.

이 물량이 18:30 한 번 체제에서 **18.5시간을 대기**했다. 새벽 05~07시(120건)까지
합치면 전체의 34%가 반나절 이상 묵혀졌다. 06:30 슬롯이 겨냥한 것이 이것이다.

```
평균 노출 지연   11.8h → 6.2h        90분위   22.3h → 11.5h
```

⚠ **주일예배 창(12~14시)을 근거로 무엇을 판단하지 말 것.** 실측 25건뿐이다.
2.15절 주석의 그 문장은 검증 없이 쓰인 추정이었고, 이번에 정정했다.

**③ 06:30을 고른 이유 — 07:30이 최적인데도**

지연만 보면 07:30이 최적(평균 5.5h)이다. 그런데 Actions는 예약보다 ~30분 늦게
시작하는 것이 정상이라(2.23절) **06:30 예약이 실제로는 07:00 전후에 돈다.**
12시간 등간격이기도 해서 06:30으로 정했다.

**④ ★ 목록은 뒤섞이지 않는다 — 이 성질이 결정의 근거였다**

채널 회전 위상은 `day_of_year`이고 **UTC 기준**이다(`build_videos.py:710`).

```
09:30 UTC (KST 18:30 당일)     → yday = D
21:30 UTC (KST 06:30 이튿날)   → yday = D   ← 같다
09:30 UTC (KST 18:30 이튿날)   → yday = D+1
```

**아침 실행은 전날 저녁의 회전 위상을 그대로 물려받는다.** 목록 재배열은 지금처럼
하루 한 번 KST 18:30에만 일어나고, 아침 실행은 새 영상을 얹기만 한다. 정서적
안정을 다루는 앱에서 "열 때마다 목록이 달라지는" 위험이 **구조적으로 없다.**
이 성질이 없었다면 2회 전환을 권하지 않았을 것이다. 깨지 말 것.

**⑤ 쿼터는 제약이 아니었다**

`quota.py` 산정기로 뽑았다. 15채널 산정치 64가 실측 61·62·64·66과 맞아 산정기
자체가 검산됐다.

```
채널 22개(현재)  93/회   2회 186   일 한도 10,000의 1.9%
채널 50개(목표) 209/회   2회 418   4.2%
```

⚠ 옛 주석의 "하루 ~62 units"는 **채널 15개 시절 값**이다. 93으로 정정했다.

**⑥ 함께 고친 것 — cron 한 줄로 끝나지 않았다**

```
build.yml  주간 진단 요약 · 주간 기준선 갱신
           → github.event.schedule == '30 9 * * *' 게이트 추가
build.yml  실패 알림 Issue 제목에 슬롯 표기 추가
quota_log  ACTIONS_BATCH_RESERVE  200 → 500
```

★ **주간 진단 게이트가 이번 변경의 진짜 위험이었다.** 게이트가 날짜
(`getUTCDay`)뿐이라 월요일에 두 슬롯이 모두 통과했다. 그러면

```
1회차  지난주 기준선과 비교 → 기준선을 오늘 값으로 덮어씀
2회차  ★ "지난주"가 아니라 12시간 전(오늘 1회차)과 비교
```

이 되어 **주간 비교가 12시간 비교로 조용히 전락한다.** 실패하지도 않고 로그에도
안 남는 종류의 고장이다. 두 스텝(요약·기준선)은 **반드시 같은 조건**이어야 한다 —
한쪽만 돌면 기준선이 안 밀리거나 비교 없이 밀린다.

수동 실행(`workflow_dispatch`)도 게이트에서 빠진다. 임의 실행이 주간 기준선을
말없이 갈아치우면 안 되기 때문이다.

⚠ `ACTIONS_BATCH_RESERVE`는 **하루치 총액**이지 1회분이 아니다. 200은 22채널
2회(186)에 여유가 14뿐이었고 30채널(252)이면 깨졌다. PLAN 목표인 50채널
2회(418)까지 덮도록 500으로 올렸다.

**⑦ 방침 문구 — 원래 적어둔 것이 틀렸다 (주기와 무관)**

4절에 "처방침에 '영상 목록을 하루 한 번 내려받는다'가 들어가야 한다"고 적혀
있었다. **서버 갱신 주기와 앱 확인 주기를 혼동한 문장이다.**

```
서버   build.yml 하루 2회 갱신
앱     sync.js CHECK_INTERVAL_MS = 6시간 — 앱을 열었을 때 확인한다
```

고지 대상은 **앱의 동작**이므로 "하루 한 번"도 "하루 두 번"도 아니다. 정확히는
**"앱을 열 때 최대 6시간에 한 번 내려받는다"**이고, 실제 횟수는 사용자가 앱을
얼마나 자주 여는지에 달렸다. 4절 문구를 그렇게 고쳤다.

다행히 **배포된 방침 페이지는 부정확하지 않다** — `app/public/privacy/index.html`
에는 횟수를 못박은 문장이 없고 "갱신 주기를 지키는 데 씁니다"뿐이다. 스토어
제출 문구를 새로 쓸 때 위 숫자를 그대로 옮기지 말 것.

**⑧ ②(코드 변경 확인)를 위해 주기를 건드릴 필요는 없었다**

`workflow_dispatch`는 이미 있었고 `dry_run: true`는 **API 0**이다(2.49절에서
실제로 `gh workflow run build.yml`을 썼다). 주기 변경은 **①(새 영상 반영)만의
사안**이다. 두 목적을 섞어 판단하지 말 것.

⚠ 남은 맹점: **CI는 쿼터 로그가 안 남는다**(2.15절). 이제 서로의 소모를 모르는
실행이 하루 2개 + 수동이다. 지금 안전한 이유는 회계가 맞아서가 아니라 **금액이
작아서**다(186 / 9,500). 채널이 늘면 이 맹점이 커진다.

---

### 2.69 ★ 배치가 죽어 있었다 — 임포트 한 줄 (2026-08-26)

**2.68 작업 중 드라이런을 돌리다 우연히 발견했다. 주기 변경과 무관한 기존 결함이다.**

```
NameError: name 'select_theme_videos' is not defined
  build_videos.py:271  in build_themes()
```

`4702779`("영상 상한을 탭별로")에서 `lib.selection` 임포트 목록을 `select_tab_layers`
중심으로 다시 쓰면서 **`select_theme_videos`가 빠졌다.** 그런데 `build_themes()`는
여전히 그 함수를 부른다. 그래서 **배치가 필터·태깅까지 다 하고 주제 진단 단계에서
죽는다.** 오늘(2026-08-26) 18:30 cron이 이 상태로 돌 뻔했다.

**★ 왜 게이트가 못 잡았나 — 이게 진짜 교훈이다**

```
verify_verses  통과   구절만 본다
tagging_test   통과   태깅 규칙만 본다
spread_test    통과   ★ select_theme_videos를 lib.selection에서 직접 임포트한다
                      → build_videos.py의 임포트 누락을 볼 수 없다
```

**세 게이트 모두 `build_videos.py`를 실행하지 않는다.** 배포 전 게이트가 다 초록인데
배치는 첫 실행에서 죽는 구멍이 있었다. 게이트가 "라이브러리는 맞는가"만 보고
"진입점이 라이브러리를 제대로 부르는가"는 안 봤다.

⚠ **드라이런(`--dry-run`, API 0)이 유일하게 이걸 잡는다.**
✅ **처리됨 — 2.70절.** 같은 날 CI 게이트에 넣었고, 결함을 재현해 **기존 게이트
3종은 exit 0인데 드라이런만 exit 1**로 잡는 것을 역검증했다.

**고친 내용**

임포트 한 줄 복구가 전부다. 호출부를 `select_tab_layers`로 바꾸는 문제가 아니다 —
둘은 역할이 다르다.

```
select_tab_layers    화면(세분류) 구성. build_videos.py:387
select_theme_videos  주제별 진단·경보. build_videos.py:271
                     ★ select_tab_layers도 내부에서 이걸 부른다(selection.py:511)
```

임포트 자리에 그 사실을 주석으로 적어 뒀다. 다시 정리할 때 `build_themes`를 함께 볼 것.

**검증**: 드라이런 exit 0 · 실제 소모 76 units(하드캡 잔여 9,724) · 24세분류 정상 산출.

---

### 2.70 ★ CI 게이트에 진입점 드라이런 — 2.69의 구멍을 막았다 (2026-08-26)

2.69에서 남긴 "다음 세션 후보 1순위"를 처리했다. `build.yml` 배포 전 게이트에
`build_videos.py --dry-run`을 넣었다. **API 0이다.**

**★ 역검증 — 구멍이 실재했음을 증명했다**

임포트를 다시 빼서 2.69의 결함을 재현한 뒤 각 게이트를 돌렸다.

```
                결함 있는 코드    판정
verify_verses      exit 0        못 잡음
tagging_test       exit 0        못 잡음
spread_test        exit 0        못 잡음   ← lib에서 직접 임포트하므로 볼 수 없다
★ 드라이런        exit 1        ★ 잡는다
```

**게이트 3종이 전부 초록인데 배치는 죽는다**는 것을 실제로 재현해 확인했다.
셋 다 `build_videos.py`를 실행하지 않기 때문이다 — "라이브러리는 맞는가"만 보고
"진입점이 라이브러리를 제대로 부르는가"를 안 본다. 드라이런은 수집→필터→태깅→
선정→위기→리포트 기록까지 **진입점 전 구간**을 탄다.

**흔들리지 않는다 — 확인하고 넣었다**

게이트는 간헐 실패하면 없느니만 못하다. `DryRunClient`에 난수가 없음을 코드로
확인하고 2회 연속 실행으로 실측했다.

```
세분류·videoId    순서까지 완전 동일
diagnostics       동일
차이              version 타임스탬프뿐
```

**dist에 쓰지 않는다 — 이 경로를 바꾸지 말 것**

`--out-dir "$RUNNER_TEMP/gate-dryrun"`. 합성 데이터가 배포 산출물과 섞이면 앱이
그걸 진짜로 받는다("공개 배포 대상만 남기기" 주석의 그 위험이다). 워크스페이스
밖에 쓰고 버린다. 로컬 재현에서 `dist/`가 실제로 안 건드려지는 것도 확인했다.

⚠ `--dump-titles`는 넣지 않았다. 스모크에 불필요하고 수백 KB를 쓴다.
⚠ `--previous`는 넣었다. 파일이 없어도(첫 실행·gh-pages 미존재) 정상 통과한다 —
  로컬에 `_previous`가 없는 상태로 검증했다.

**★ 다른 진입점 전수 조사 — 미커버는 하나뿐이다**

AST로 33개 파이썬 파일의 미정의 전역 이름을 전수 검사했다. **현재 0건**이다
(2.69의 그 결함이 유일했고 고쳐졌다). CI 실행 여부는 이렇다.

```
build_videos.py            ★ 이번에 추가 (build.yml 게이트)
verify_verses.py           build.yml · deploy-app.yml
tagging_test.py            build.yml · deploy-app.yml
spread_test.py             build.yml · deploy-app.yml
messages_test.py           deploy-app.yml
gen_taxonomy_json.py       deploy-app.yml의 npm run gen:data — 실제로 실행된다
gen_verses_json.py         "
gen_krv_chapters.py        "
앱(main.jsx 등)            vite build + npm test — 정적 임포트 오류는 빌드가 잡는다

★ suggest_channels.py      **어디서도 안 돈다** — 유일한 미커버 진입점
   fill_verses.py · retag_titles.py · stats.py   일회성 도구
```

`suggest_channels.py`도 `--dry-run`이 `DryRunClient`라 **API 0**이다(`:917`).
이번에 수동으로 돌려 **exit 0**을 확인했다 — 지금은 같은 잠복 결함이 없다.
쿼터 로그도 오염시키지 않는다(드라이런은 기록하지 않음을 실측).

⚠ **CI에 넣지 않은 이유**: 월 1회 수동 발굴 도구이고 어느 워크플로의 목적에도
속하지 않는다. 넣는다면 별도 워크플로(예: PR 검사)가 맞다. **판단 대기 항목.**
넣기 전까지는 발굴 전에 `--dry-run`을 먼저 돌리는 습관이 유일한 방어다.

**함께 고친 것 — deploy-app.yml에 같은 표류가 남아 있었다**

```
- python scripts/verify_verses.py --check ... && echo "구절 원문 대조 통과 (250건)"
+ ... && echo "구절 원문 대조 통과"
```

`build.yml`은 2026-08-24에 "250건"이 실제 303건과 어긋난 것을 발견해 건수를
지웠는데(2.43 유형) **`deploy-app.yml`에는 그대로 남아 있었다.** 실측 확인:
verify_verses는 **303건**(250은 틀림), messages_test는 485건(맞음). 원칙대로
둘 다 지웠다 — 건수는 스크립트가 세서 제 출력에 적는다.

⚠ deploy-app.yml에는 배치 드라이런을 **넣지 않았다.** 앱을 배포하는 워크플로이고
배치 진입점은 그 경로에 없다. 대신 `gen:data`·`npm test`·`vite build`가 그
워크플로의 진입점 커버리지를 맡는다.

---

### 2.71 ★ 2026-08-26 배치 결과 — 셋 다 확인됐고, 셋이 남았다 (2026-08-27)

`run 32956543139` · schedule · 09:30Z 예약 → **10:06:37Z 시작(36분 지연, 정상 범위)**
· 1분 7초 · success.

**① 확인된 것 — 2.64·2.68·2.70이 전부 의도대로 동작했다**

```
드라이런 게이트 첫 실전 통과   10:06:45  약 1.1초       (2.70)
48/48 탭 20건 충족            최소 20 · 최대 24 · 평균 20.8   (2.64 탭당 20)
videos.json                  363,667바이트  추정 372KB 대비 2% 오차
```

`screens_below_min: 0` · `screens_below_target: 0`. **`theme_too_few`는 0건**이다
(critical·warning 모두) — 전 탭이 20건 이상이라 임계 4/8에 걸릴 수가 없다.
2.67에서 MEDIA_FLOOR를 흡수한 그 경보가 "안 뜨는 것이 정답"인 상태다.

크기는 직전 192,932 → **1.89배**다. 탭당 20 전환의 직접 효과이고 추정이 잘 맞았다.

⚠ 전체 경보 38건 중 critical은 **하나뿐**이다 — `theme_empty: self_control` 풀 0건.
이게 아래 ③의 절반을 설명한다.

**② ⏸ 06:30 슬롯이 발동하지 않았다 — 확인 대기**

2.68에서 넣은 신규 슬롯(21:30 UTC)이 첫 발동일에 돌지 않았다.

```
08-26 10:06Z  데이터 배치  schedule/success
08-26 21:30Z  ────────────  실행 없음  ★
```

**설정은 정상임을 확인했다.**

```
origin/main의 build.yml    cron 두 개 다 있음 (GitHub API로 파일 직접 확인)
워크플로 state             active
21:00~23:00Z 구간          어떤 워크플로도 실행 기록 없음 (큐·실패·취소 포함)
```

GitHub Actions는 새 cron의 첫 발동을 거르거나 예약 실행을 부하로 흘려보내는 일이
있다. **한 번 안 돈 것만으로 단정하지 말 것.**

✅ **해소됐다 (2026-08-27 확인) — 슬롯은 정상이고, 지연이 극단적이었을 뿐이다.**

```
run 33028573553  event=schedule  success
  예약 21:30Z → 실제 시작 2026-08-27 00:58Z (KST 09:58)   ★ 3시간 28분 지연
```

**어느 슬롯인지는 게이트로 판별했다.** "주간 진단 요약"·"주간 기준선 갱신" 두
스텝이 `skipped`였다 — 그 스텝의 `if`가 `github.event.schedule == '30 9 * * *'`
이므로, **skipped = 저녁 슬롯이 아니다 = 21:30Z 슬롯**이다.

★ 부수 확인: **2.72에서 넣은 슬롯 게이트가 실전에서 의도대로 동작했다.**

⚠ 다만 **지연이 21~42분이 아니라 3시간 28분이었다.** KST 06:30을 겨냥했는데
실제로는 09:58에 돌았다. `build.yml` 주석의 "06:30 예약이 실제로는 07:00 전후에
돈다"는 예측은 이 실행에는 맞지 않았다. 노출 지연 개선폭이 계산(평균 11.8h→6.2h)
보다 작을 수 있다 — **몇 회 더 관찰한 뒤 판단할 것.** 지연이 계속 3시간대면
예약 시각을 앞당기는 것(예: 20:00Z)을 검토할 수 있다.

**③ ★ 세 화면의 [말씀] 탭이 폴백으로만 산다 — 구조적 원인**

```
                    노출  주제분  폴백   배정 주제 (풀/선정)
anger.irritation     20     0     20    self_control 0/0 · quiet_worship 246/20
exhaustion.tired     20     1     19    rest 3/3 · quiet_worship 246/20
calm.ease            20     1     19    quiet_worship 246/20 · creation 2/2
```

**셋 다 20건을 채웠다** — 탭당 20 이전이라면 비었거나 얇았을 탭이다. 그 점에서
2.64는 목적을 달성했다. 그러나 **주제분이 0·1·1이고 원인은 공급이 아니라 배정이다.**

```
quiet_worship  246건인데 sermon 0건    ← media_type_gap 경보가 정확히 이걸 가리킨다
self_control     0건 (critical)
rest             3건 · creation 2건
```

세 화면의 주력 주제가 `quiet_worship`인데 그 246건이 **전부 찬양**이다. 말씀 탭에
넣을 주제분이 원천적으로 없다. 남은 배정 주제도 3건·2건·0건으로 빈약하다.

말씀 탭의 유일한 주제분 1건씩은 이렇다.

```
exhaustion.tired  분당우리교회 | 2026-08-16 | 쉼이 없는 당신에게
calm.ease         CBS설교 | 하나님이 천지를 창조하시니라
```

셋 다 `theme_fallback_heavy [sermon]` 경보가 떴다 — **경보 체계는 제대로 작동한다.**
빈 화면이 아니라 "주제와 무관한 것으로 채워진 화면"이라는 사실이 드러나 있다.

**④ 분당우리교회 — 호세아 쏠림은 안 일어났다. 이유가 예상과 다르다**

우려했던 연속 강해 쏠림이 없었다. 주제분 39 노출이 **19개 세분류에 분산**됐고
최다가 4건이다. 그런데 이유가 "잘 분산돼서"가 아니다.

```
호세아 강해 10건 중 9건이 미태깅(themes=[]) — 주제분에 아예 못 들어갔다
걸린 것은 1건뿐: "회복의 길 | 호세아 7장" → renewal
```

⚠ **쏠림이 없는 것은 태깅이 잘돼서가 아니라 안 돼서다.** 연속 강해 제목
("바람을 따라가는 삶에서…", "여호와께 돌아가자")은 주제 어휘에 안 걸린다.
쏠림 위험은 사라진 게 아니라 **사전이 좋아지면 그때 나타난다.** 사전 보강 시
이 채널을 다시 볼 것.

실제 최다 노출은 찬양대 곡이다(주 소망 의지하리 6회 · 내 맘에 한 노래 있어 5회 ·
Laudate Dominum 5회). `content_type=sermon`인데 worship 23 노출인 이유가 이것이고,
2026-08-19에 "제목이 채널보다 앞"으로 뒤집은 판단이 실측으로 재확인된 자리다.

**⑤ 사랑의교회 unknown 8건 — 곡명 사전 미보강이 데이터로 드러났다**

⚠ 먼저: **사랑의교회는 `content_type: mixed`다.** "sermon으로 판별되는지"를 묻는
것 자체가 전제 오류다 — 영상마다 제목·장절로 가리는 것이 설계된 동작이고,
그 판별은 대체로 정확하다(찬양대 곡명 → worship, `[사랑공동체 성경읽기] 에스겔`
→ scripture로 sermon).

문제는 unknown 8건(8.4%)이다.

```
[none] 1248s  예수 피를 힘입어/문들아 머리 들어라/나는 주만 높이리/…   찬양 메들리 ×6
[none] 1300s  [사랑의교회] 구별됨은 은혜입니다 - 조충현 목사            설교 ×2
```

**곡명만 `/`로 나열해 "찬양" 어휘가 제목에 없다.** 길이도 600~1800s 중간대라
길이 규칙(30분↑/10분↓)으로도 못 가른다. 2.14절이 남긴 **곡명 사전 미보강**
(258곡 중 사전에 걸리는 것 20개 = 7.8%)이 그대로 나타난 자리다.

당장 화면이 비지는 않는다(unknown은 양쪽 탭에 다 보인다). 대신 **토글의 의미가
흐려진다** — [말씀]을 눌렀는데 찬양 메들리가 섞인다.

**⑥ 탭당 20인데 15개 탭이 21~24건이다 — 조사 완료, 결정은 안 났다**

⛔ **아직 아무것도 고치지 않았다. 사용자 판단 대기 중이다.**

*어느 단계에서 넘는가 — 선정이 아니라 화면 합류에서 넘는다*

`select_tab_layers`는 두 패스(SERMON → WORSHIP)가 **하나의 `theme`/`fallback`
리스트**에 쌓는 구조다. 각 패스는 자기 기여를 정확히 20으로 자른다.

```python
for tagged in picked[:TAB_MAX_VIDEOS]:        # 자기 기여 ≤ 20
need = min(max(0, TAB_MAX_VIDEOS - have), FALLBACK_MAX_PER_TAB)   # 폴백도 20까지만
```

**넘는 것은 unknown이 양쪽 탭에 보이기 때문이다.** 두 방향 모두 일어난다.

```
말씀 탭  SERMON 패스가 정확히 20으로 맞춘 뒤, WORSHIP 패스가 담는 unknown이
         말씀 탭에도 보인다. 사후 보정이 없어 20을 넘는다.
찬양 탭  have에 앞 패스의 unknown을 세지만, 자기 theme 기여는 picked[:20]으로만
         자른다. have가 이미 20을 넘어도 줄이지 않는다.
```

실측이 이 설명과 맞는다. 10개 탭은 `자기형식 20 + 초과 = unknown 수`로 정확히
떨어지고, 나머지 5개는 자기 패스가 unknown 일부를 자기 20에 넣은 경우다.

```
21건 6개 · 23건 3개 · 24건 6개 = 15개 / 48   (말씀 8 · 찬양 7)
최대 24건 (anger.unfair·frustration.suppressed·sadness.loss·
           exhaustion.burnout·boredom.dull·boredom.novelty 말씀)
```

⚠ **앱은 자르지 않는다.** `VideoList`가 `videos.map()`으로 전부 그린다
(`slice` 없음). **사용자가 실제로 24건을 본다.**

*의도와 어긋나는가 — 어긋난다. 다만 지금 값이 해로운 게 아니라 상한이 없는 것이 문제다*

"탭당 20"의 의도는 한 번에 보는 목록의 상한이었는데, 코드가 보장하는 것은
**"각 패스의 기여가 20 이하"**라는 다른 명제다. 지금 24는 20% 초과라 실질 해가
작다. 진짜 문제는 **구조상 상한이 40이라는 것**이다 — WORSHIP 패스가 담는 20이
전부 unknown이면 말씀 탭은 40이 된다. 지금 24에 그치는 것은 unknown이
**고유 273건 중 14건(5.1%)** 으로 적어서일 뿐, 보장된 값이 아니다.

⚠ 즉 unknown 비율이 오르면 **아무 경보 없이 조용히 벌어진다.** ⑤의 곡명 사전을
보강하면 unknown이 줄어 이 문제도 함께 완화되지만, 반대로 새 채널이 unknown을
많이 내면 악화된다.

*정확히 20으로 맞추려면 — 무엇을 잃는가*

근본 긴장은 이것이다. **목록 한 벌 + unknown 양쪽 노출**을 동시에 유지하면서
두 탭을 독립적으로 20에 맞추는 것은 불가능하다. 셋 중 하나를 포기해야 한다.

```
A. 탭별 목록 분리       videos.json에 탭마다 목록을 둔다 → 각 탭 정확히 20
   잃는 것   "데이터는 한 벌" 성질 · unknown 중복으로 페이로드 증가 ·
             앱 계약 변경(visibleVideos·layersFor·toggleCounts + 테스트 5종)

B. 뒷 패스의 unknown 채택 억제   room = 20 - visible_count(theme, tab)로 자른다
   잃는 것   ★ unknown은 양쪽을 동시에 메우는 값이다. 조이면 약한 쪽 탭이
             얇아진다 — anger.irritation 말씀처럼 주제분 0인 탭이 20을 못 채운다.
             **2.64가 해결한 문제로 되돌아간다.**
   ⚠ 게다가 B는 찬양 탭만 고친다. 말씀 탭 초과는 뒷 패스가 원인이라 안 고쳐진다.

C. 사후 절단                    넘치는 탭에서 초과분을 뺀다
   잃는 것   순환한다 — unknown을 빼면 반대 탭도 하나 잃어 20 아래로 떨어질 수 있다.
             단독으로는 성립하지 않는다.

D. 그대로 두고 경보만 단다      탭 노출이 N을 넘으면 진단에 남긴다
   잃는 것   없다. 20으로 맞춰지지도 않는다. 40까지 조용히 벌어지는 것만 막는다.
```

✅ **결정됐다 — D안. 상세는 2.72절.** (2026-08-27 사용자)
근거는 "24건 자체가 선택 부담을 늘리지 않고, 진짜 위험은 상한이 보장되지
않는다는 점"이다. ⛔ **B·C는 기각됐다. 다시 꺼내지 말 것**(2.72 ②).

---

### 2.72 ★ 탭 20 초과 — D안 채택. 고치지 않고 드러낸다 (2026-08-27)

2.71 ⑥의 조사 결과를 놓고 **사용자가 D안(경보)을 골랐다.**

**① 채택 근거 (사용자 2026-08-27)**

```
24건 자체는 선택 부담을 유의미하게 늘리지 않는다.
진짜 위험은 구조적 상한이 40이고, 지금 24에 그치는 것이
unknown 5.1%라는 **우연**이라는 점이다.
A안은 "데이터 한 벌" 성질과 앱 계약을 바꾸는 비용에 비해 얻는 것이 4건 차이다.
```

즉 **고치는 대상은 24가 아니라 "상한이 보장되지 않는다는 사실"** 이다.
숫자를 20에 맞추는 것이 아니라, 조용히 40까지 벌어지는 경로를 막는다.

**② ⛔⛔ 다음 세션 경고 — B나 C로 부분 수정하지 말 것**

D를 골랐다는 것은 "나중에 B로 조금 고치자"가 아니다. **B와 C는 검토를 마치고
기각했다.** 다시 꺼내지 말 것.

```
B  뒷 패스의 unknown 채택 억제 (room = 20 - visible_count)
   ⛔ unknown은 **약한 쪽 탭을 메우는 값**이다. 조이면 주제분 0인 탭이
      20을 못 채운다 — anger.irritation [말씀]이 정확히 그 탭이다.
      **2.64가 푼 문제로 되돌아간다.**
   ⛔ 게다가 찬양 탭만 고쳐진다. 말씀 탭 초과는 뒷 패스가 원인이라 그대로 남는다.

C  사후 절단
   ⛔ 순환한다. 목록이 한 벌이라 unknown을 빼면 **반대 탭도 하나 잃는다** —
      그쪽이 20 아래로 떨어질 수 있다. 단독으로 성립하지 않는다.
```

★ **20으로 정확히 맞추는 길은 A안(탭별 목록 분리)뿐이다.** 그것도 공짜가 아니다 —
`videos.json` 구조 변경 · unknown 중복으로 페이로드 증가 · 앱 계약 변경
(`visibleVideos`·`layersFor`·`toggleCounts` + 테스트 5종). 사용자가 그 비용을
치르겠다고 **명시적으로 말하기 전에는 착수하지 않는다.**

이 경고는 `selection.py`의 `TAB_MAX_VIDEOS` 주석과 `alerts.py`의
`tab_over_cap` docstring에도 같이 적어 뒀다 — 코드만 보는 사람도 걸리게.

**③ 임계 26 / 30 — 20 바로 위가 아닌 이유**

```
의도치        20
오늘 최대     24   (초과분 4 · unknown 고유 273건 중 14건 = 5.1%)
WARN         26   ★
CRIT         30   ★
구조적 상한   40
```

**21에서 울리면 15개 탭이 매일 운다. 그건 소음이지 신호가 아니다.**
이 경보의 목적은 "초과를 잡는 것"이 아니라 — 초과는 D안이 **허용하기로 한
것이다** — **"오늘의 정상 범위를 벗어난 것"** 을 잡는 것이다.

```
26   오늘 최대보다 2 위. 초과분이 4→6으로 50% 늘 때 운다.
     오늘 값으로는 안 울리므로 소음이 아니고, unknown이 눈에 띄게 늘면 운다.
30   의도치의 1.5배이자 20과 40의 정확한 중간.
     여기까지 오면 "탭당 20"이라는 말이 사실상 의미를 잃는다.
```

⚠ **이 값을 낮춰 "초과 자체"를 잡으려 하지 말 것.** 낮추면 매일 우는 경보가 되어
아무도 안 보게 된다 — MEDIA_FLOOR가 `logger.error`로만 남아 아무도 안 보던
그 상태(2.67)를 반대 방향으로 재현한다.

**④ 정식 경보다 — 로그만 남기지 않았다**

`alerts.py`에 `tab_over_cap`을 넣었다. `theme_too_few`의 **반대쪽 끝**이다.

```
theme_too_few   탭이 너무 얇다   < 4 critical · < 8 warning
tab_over_cap    탭이 너무 두껍다  >= 26 warning · >= 30 critical
둘 다 그 탭에서 실제로 보이는 건수를 본다 (unknown 포함)
```

⛔ **`SUMMARY_ONLY_TYPES`에 넣지 않았다. 옮기지 말 것.** 이웃인
`theme_fallback_heavy`가 summary라 같이 묶고 싶어지지만 성격이 다르다.
저쪽은 매일 같은 값이 뜨는 상태 보고이고, 이쪽은 임계를 정상 범위 **위**에 두어
평소에는 아예 울리지 않게 만든 경보다. 울린다 = 어제와 달라졌다 = 사건 → Issue.
summary로 옮기면 설계가 무의미해진다.

**⑤ unknown 비율을 진단에 남겼다 — 이 문제의 선행 지표다**

탭이 20을 넘는 만큼은 곧 **뒷 패스가 담은 unknown 수**다. 그래서 unknown이
오르면 탭 최댓값도 따라 오른다. 경보가 울리기 전에 여기서 먼저 보인다.

```
unknown_ratio     형식(media_type) 판별 실패 비율   ★ 선행 지표
unknown_kept      그 건수
tabs_over_cap     20을 넘은 탭 목록 (경보 임계와 무관하게 매일 남긴다)
tab_max_exposed   그날의 탭 최댓값 — 추세를 한 숫자로 본다
```

⚠ **`untagged_ratio`와 다른 것이다.** 저쪽은 **주제** 태깅 실패, 이쪽은
**형식** 판별 실패다. 둘은 독립이고 원인도 다르다 — 형식 실패의 대표 사례가
곡명만 나열된 찬양 제목이다(2.71 ⑤ 사랑의교회).

⚠ 경보는 임계를 넘을 때만 울리지만 **진단은 매일 남긴다.** 임계 아래에서 조금씩
오르는 추세를 주간 비교로 보려면 경보와 무관하게 숫자가 있어야 한다.

**⑥ ★ 틀린 주석을 함께 정정했다**

`selection.py`의 `TAB_MAX_VIDEOS` 주석에 이렇게 적혀 있었다.

> 탭별 20은 한 번에 보는 목록을 20 이하로 유지하므로 **그 결정을 어기지 않는다**

**사실이 아니다.** 2026-08-25에 선택 부담을 근거로 기각했던 (b)안을 2.64에서
되살릴 때 쓴 정당화인데, 그 전제가 성립하지 않는다. 이 상수가 보장하는 것은
"한 탭이 20건 이하"가 아니라 **"각 패스의 기여가 20 이하"** 라는 다른 명제다.

경보를 새로 다는 자리 바로 옆에 반증된 문장을 남겨 둘 수 없어 함께 고쳤다.
⚠ 2.64 ①의 "기각 근거가 이 재해석에서는 성립하지 않는다"도 **부분적으로
틀린 기록**이 됐다 — 그 판단의 전제였던 "20 이하 유지"가 사실이 아니었다.
다만 **결론(탭별 20 채택)은 사용자가 2026-08-27에 재확인했다** — 24건이
선택 부담을 유의미하게 늘리지 않는다는 것이 ①의 근거다.

**⑦ 검증**

```
경보 구성        warning/critical 두 심각도 정상 생성
역검증           임계를 21/22로 임시 하향 → 16건 발동(warning 15 · critical 1)
                 복원 후 26/30에서는 오늘 데이터로 0건 (최대 24 < 26)
진단 필드        드라이런에서 4개 필드 정상 출력
2026-08-26 실데이터 적용 시   tabs_over_cap 15건 · tab_max_exposed 24 · 경보 0건
                             → 기록은 남고 경보는 조용하다. 설계대로다.
```

★ **`spread_test`의 "모든 경보 생성기가 라우팅표에 있다" 검사가 내 누락을 잡았다.**
`route_for()`는 미등록 타입을 기본값 ISSUE로 흘려보내므로 코드는 그냥 돌았는데,
그 검사가 `tab_over_cap`을 잡아 실패시켰다. **경보를 새로 만들면 `spread_test.py`의
`ISSUE_EXPECTED`/`SUMMARY_EXPECTED`에도 등록해야 한다** — 어느 갈래인지 사람이
명시적으로 밝히게 하는 장치다. 2.70에서 만든 드라이런 게이트와 성격이 같다.

---

### 2.73 ★ unknown 조사 — 판별 실패가 아니라 제3의 콘텐츠였다 (2026-08-27)

⛔ **조사만 했다. 사전도 판별 규칙도 고치지 않았다**(2.14 원칙 — 오탐 실측이 따라야 한다).

**① 사용자가 신고한 세 사례는 서로 다른 문제였다**

```
CGN  믿어야 산다 EP.7      26:48   unknown  reason=none   ← 진짜 unknown
CGN  젠센 프랭클린의 부흥    21:49   unknown  reason=none   ← 진짜 unknown (시리즈 6편 전부)
CTS  염려 대신 감사를        3:00   worship  reason=duration  ★ unknown이 아니다
```

앞의 둘은 4단계를 모두 빠져나갔다.

```
1 제목 어휘   미매치 (주제는 붙었다 — trust·peace. 형식 어휘가 없다)
2 본문 장절   없음
3 채널        CGN = mixed → 답을 낼 수 없다
4 길이        1608s·1309s → 사각지대(600~1800s)
```

⚠ **세 번째는 별개 결함이다.** 180초라 `worship_max_seconds: 600`에 걸려 **찬양으로
확정**됐다. 따라서 찬양 탭에만 뜨고 **말씀 탭에는 안 뜬다** — "양쪽에 보인다"는
관찰과 맞지 않는다. 앞의 둘과 섞어 보지 말 것. 별건 등재는 아래 ⑧.

**② ★ unknown 81건의 실제 구성 — 다수가 판별 실패가 아니다**

```
수집 1754건 → sermon 1011 · worship 662 · unknown 81 (4.6%)
판정근거   81건 전부 none
길이       81건 100%가 10~30분 구간   ← 사각지대에 정확히 갇혀 있다
채널       CGN 31 · CBSJOY 25 · CTS 10 · 사랑의교회 8 · 극동방송 5 · 광주극동방송 2
```

81건의 제목을 직접 읽고 분류했다.

```
sermon이 명백 (강해 시리즈·설교)          20건  25%
worship이 명백 (곡명 나열·합창단 공연)      8건  10%
★ 말씀도 찬양도 아님 (간증·인터뷰·뉴스·다큐) 52건  64%
미분류                                  1건
```

**이것이 이번 조사의 핵심이다.** unknown의 다수는 규칙이 못 가린 것이 아니라
**이 앱의 이분법(말씀/찬양)에 애초에 안 맞는 콘텐츠**다.

```
CBSJOY   새롭게 하소서 · 부르심의 소명 더 콜링 · CBS 광장   → 간증·토크
CGN      온전한 인터뷰 · 레디온 교계뉴스 · 휴먼네컷        → 인터뷰·뉴스
CTS      원더풀우먼 Clip · CTS 추천픽(독립운동가 다큐)      → 토크·다큐
```

⛔ **이것들을 말씀이나 찬양 어느 쪽으로 밀어 넣어도 틀린 답이다.** 규칙을 고쳐
"해결"하려 들지 말 것 — 아래 ④가 그 함정을 수치로 보여준다.

배포본 기준: 노출 936회 중 63회(6.7%) · 고유 14건.
성격은 sermon 43% · **제3범주 43%** · worship 14%.

**③ 양쪽 탭 중복 노출 — 24개 중 19개 화면 (79%)**

unknown은 양쪽 탭에 다 보이므로, 사용자는 [말씀]을 눌러도 [찬양]을 눌러도
같은 영상을 본다.

```
중복 0건 5화면 · 1건 6 · 2건 2 · 4건 5 · 5건 3 · 6건 3
최대 6건 — joy.proud · joy.delight · flutter.thrill
```

한 영상이 얼마나 퍼지는지가 더 눈에 띈다.

```
14화면  [극동방송] 이게 바로 하늘의 빌드업인가? … | 하바드 13강
 8화면  [CTS] [Clip] 왜 믿음 좋은 사람들에게… | 원더풀우먼
 8화면  [CGN] 미래목회 심포지엄 … | 온전한 인터뷰 | 레디온 199편
 8화면  [CBSJOY] 10년 선교를 결심했는데 3년 만에 돌아온 이유 │새롭게 하소서
```

그 중복 노출의 **43%가 애초에 어느 탭에도 속하지 않는 콘텐츠**다.

**④ ★ 개선안 A~E — C만 깨끗하다**

⚠ **전제**: 네 안 모두 제목 규칙 **뒤에** 놓인다. 그래서 실제 대상은 이미
판별된 1,673건이 아니라 **길이 단계까지 내려온 194건(그중 unknown 81)** 뿐이다.
전체 코퍼스로 오탐을 재면 위험이 **과대평가**된다 — 이 함정에 빠지지 말 것.

```
안 A  '목사'·'담임'         판정을 바꾸는 대상 10건
      → 맞음 4 · ★틀림 6 (제3범주를 말씀으로)     정확도 40%    위험
      이유: 간증 프로그램의 **출연자가 목사**다.
            "위암 재발… │장성훈 목사│부르심의 소명" 같은 제목이 걸린다.

안 A' 말미 '- OOO 목사'      판정을 바꾸는 대상 3건
      → 맞음 3 · 틀림 0                          정확도 100%   ★ 깨끗하나 좁다
      어휘가 아니라 **구조**를 본다. 사랑의교회 설교 제목 형식에만 걸린다.
      ⚠ CTS "염려 대신 감사를"은 못 잡는다(말미가 아니라 중간에 있다).

안 B  채널 content_type 강화  판정을 바꾸는 대상 0건            ⛔ 무의미
      unknown 81건의 **100%가 mixed 채널**에서 나온다.
      mixed는 sermon도 worship도 아니므로 채널 단계가 애초에 답을 못 낸다.

안 C  곡명 구조 신호 '가/나'   구제 6건 · 오탐 0 · **기존 오분류 1건 교정**  ★ 최선
      '/'로 한글을 잇는 제목 23건 → worship 16 · unknown 6 · sermon 1
      그 sermon 1건은 오탐이 아니라 기존 오분류다:
        "[사랑의교회] 마지막 날에/지금은 엘리야 때처럼/…" — 전부 찬송가 제목
      사전 어간을 줄이는 방식(258곡 중 7.8%)보다 훨씬 안전하다.

안 E  길이 사각지대 축소       1500s: 18건 구제 → sermon 6 · ★제3범주 12   위험
                            1200s: 55건 구제 → sermon 18 · ★제3범주 35 · worship 2
```

★ **A vs A'가 이번 조사의 방법론적 수확이다.** 같은 신호도 **어휘로 보면
부정확하고 구조로 보면 정확하다.** 안 C가 깨끗한 이유도 같다 — 곡명 사전이
아니라 '`/`로 잇는 제목'이라는 구조를 본다. 다음에 판별 규칙을 손볼 때
**어휘를 늘리기 전에 구조 신호가 있는지부터 볼 것.**

**⑤ 2026-08-19 결정과 충돌하지 않는다**

"제목을 채널보다 앞으로"(themes.yaml 판별 우선순위 · 2.14)는 **전용 채널이
반대 형식을 대량으로 올린다**는 문제를 다룬 결정이고, 그 근거(오륜교회 찬양
콘티 55건 등)는 지금도 유효하다.

★ 그런데 **이번 unknown은 전용 채널이 아니라 전부 `mixed`에서 나온다.**
채널 단계가 애초에 답을 못 내므로 순서를 되돌려도 효과가 0이다.
**그 결정이 적용되는 영역이 아니라서 건드릴 이유가 없다.**

**⑥ 탭 20 초과(2.72)와의 관계 — 부분집합임이 확인됐다**

48개 탭 전수 검사.

```
"탭의 초과분 ≤ 그 탭의 unknown 수"    위반 0/48건  → 성립
초과분 합계 39 · unknown 노출 합계 126
```

각 패스는 자기 탭을 정확히 20으로 채우고, 넘치는 것은 **오직 반대 패스의
unknown이 새어 들어올 때뿐**이다. 따라서 **unknown이 0이면 초과도 0**이다.
2.72 ⑤에 "unknown_ratio는 선행 지표"라고 적은 것이 수치로 확인됐다.

형식이 명백한 28건만 제대로 판별했다고 가정한 시뮬레이션:

```
          탭 최대   21건 이상   초과 합계
현재        24        15개        39
개선 후     23         8개        19    ← 절반
```

⚠ 이건 **초과가 얼마나 줄어드는가**만 본 값이다. 실제 배치는 빈자리를 폴백으로
다시 채우므로 탭 건수 자체는 20으로 수렴한다.

**⑦ ⏸ 다음 결정 후보 — 제3범주 52건을 어떻게 다룰 것인가**

규칙을 어떻게 고쳐도 옳게 분류할 수 없다. 말씀도 찬양도 아니기 때문이다.
**판별 문제가 아니라 제품 결정이다.**

```
가. 폴백 후보에서 제외      promo_reason 방식의 확장
나. 제3의 분류를 인정        탭 추가 또는 별도 취급
다. 지금처럼 양쪽에 노출      현행 유지
```

⛔⛔ **[2026-08-27 정정 — 아래 경고는 틀렸다. 실측으로 반증됐다(2.74 ①).]**

> ~~'가'를 고르기 전에 반드시 읽을 것 — 2.64·2.66과 정면 충돌한다.
> 제3범주는 지금 폴백 공급의 일부다. 빼면 폴백이 줄어 **주제분 0인 탭이
> 20을 못 채운다** — `anger.irritation` [말씀]이 정확히 그 탭이고…
> 빼는 양만큼 어디서 채울지 답이 없으면 '가'는 성립하지 않는다.~~

**실측 결과 48/48 탭이 20건을 그대로 유지한다.** `anger.irritation` [말씀]도
20건 그대로다. 초과분만 깎인다(최대 24→23).

★ **왜 틀렸는가 — 후보 수와 실효 재고를 혼동했다.**

```
내가 본 것    폴백 후보 1,148건에서 57건을 빼면 공급이 준다
실제          폴백 실효 천장 = THEME_MAX_PER_CHANNEL(3) × 그 탭에 보이는 채널 수
              = 48~49건.  후보가 1,148이든 1,091이든 천장은 같다.
              57건 제외는 **채널 수를 21개 그대로 둔다** → 천장 불변
```

2.66이 폴백 천장을 12→20으로 올린 근거("`anger.irritation` [말씀]에서 8자리를
비우는 셈")를 **"폴백 공급이 빠듯하다"로 잘못 읽었다.** 그 절이 말한 것은
*한 탭이 가져갈 수 있는 상한*이지 *공급 재고*가 아니다.

⚠⚠ **다음에 같은 혼동을 막을 원칙 — 폴백 공급을 논할 때는 후보 수가 아니라
채널 수를 봐라.** `select_fallback_videos`가 `_round_robin(pool, 3, …)`을 쓰므로
한 채널이 한 탭에 3건까지만 준다. **후보를 아무리 늘려도 채널이 적으면 못 채우고,
후보를 줄여도 채널이 그대로면 안 준다.** 실측 한계점은 **채널 10개 미만**이다
(현재 21개 · 여유 11개).

**⑧ ⏸ 별건 등재 — 길이 규칙에 의한 오분류**

`worship_max_seconds: 600` 때문에 **10분 이하 설교 클립이 찬양으로 확정된다.**

```
길이로 확정된 worship 49건 중 설교 신호가 있는 것 2건 (4%)
  9:59  [사랑의교회] 하나님은 우리의 피난처 - 조성환 목사
  3:00  염려 대신 감사를 | 더하는교회 | 최민우 목사 | 더 기프트     ← 사용자 신고 건
```

지금은 2건(4%)이라 작지만, **짧은 클립을 올리는 채널이 늘면 함께 는다.**
unknown과 달리 이쪽은 **확정 오분류**라 사용자가 [찬양]에서 설교를 본다.

⚠ 안 A′(말미 `- OOO 목사`)를 **길이 규칙 앞에** 두면 이 중 1건이 자동으로
막힌다(사랑의교회 건). CTS 건은 구조가 달라 못 잡는다. 두 문제가 **같은 신호로
부분적으로 연결돼 있다** — 손볼 때 함께 볼 것.

---

### 2.74 ★ 제3범주 제외 — 시뮬레이션과 오탐 실측 (2026-08-27)

**사용자 결정: 진행한다.** 다만 2.14 원칙대로 **오탐 실측을 먼저 했고, 구현은
확인 후**다. ⛔ 아직 아무것도 구현하지 않았다.

**① 시뮬레이션 — 탭은 얇아지지 않는다**

배치의 조립부(`build_subcategories` → `select_tab_layers`)를 **실제 `lib` 코드로
그대로 돌렸다.** 폴백이 빈자리를 다시 채우므로 단순 차감은 과소계산이 된다.

⚠ **기준선 재현을 먼저 검증했다 — 48개 탭 전부 배포본과 일치(불일치 0).**
2.42가 겪은 "재현이 배포본과 어긋나는" 문제가 없다. 그때 원인은 재수집
코퍼스였고, 여기서는 **배치 자신의 덤프(titles.json)** 를 쓴다. API 0.

```
폴백 후보 1,148 → 1,091건 (-57)

              20건 이상   15~19   10~14   5~9   5 미만
현행              48        0       0      0      0
제외 후           48        0       0      0      0    ← 동일
최소 20 / 최대 23 / 평균 20.5   (현행 최소 20 · 최대 24 · 평균 20.8)
```

변화는 8개 탭의 **초과분 −2**뿐이다(24→22, 23→21). 그 탭들은 이미 자기 채우기로
20에 도달해 있었고 21~24는 반대 패스의 unknown 누출이었다(2.72 ⑥). 제3범주
대부분이 unknown이라 **누출만 사라진다.**

세 화면은 **전부 변화 없음**: `anger.irritation` 20(0+20) · `exhaustion.tired`
20(1+19) · `calm.ease` 20(1+19).

**★ 실효 재고와 한계점**

```
                후보      [말씀] 실효재고   [찬양] 실효재고   (탭당 필요 최대 20)
현행           1,148건        48건            49건
제외 후        1,091건        48건            46건
```

폴백 채널을 많은 순서로 하나씩 제거하며 재계산한 결과:
**채널 21→10개까지 48/48 유지 · 9개에서 깨진다**(45/48 · 최소 18).
상위 5개 채널을 통째로 빼도 48/48이 유지된다. **여유 11개 채널.**

**② 채택 근거 (사용자 2026-08-27)**

```
1. 48/48 탭이 20건을 유지하고 세 화면도 변화 없음이 실측으로 확인됐다
2. 실효 천장은 후보 수가 아니라 채널 수 × 3이라 57건 제외가 천장을 안 건드린다.
   채널이 10개 아래여야 깨지는데 현재 21개다
3. 실익은 "채우는 문제"가 아니라 **"무엇으로 채우는가"** — 건수는 유지되면서
   [말씀] 탭에서 간증·뉴스가 빠지고 실제 설교가 그 자리를 채운다
```

**③ ★ 오탐 실측 — 더 넓은 코퍼스에서 쟀다**

과거 실행 아티팩트를 받아 **5개 코퍼스를 합집합**했다(API 0 · GitHub만 사용).

```
2026-08-19 로컬 1,067 · 08-24 run10 1,462 · 08-25 run11 1,465
2026-08-26 run12 1,754 · 08-27 run13 1,759
→ 중복 제거 합집합 **2,023건 · 22채널** (단일일 대비 +15%)
```

⚠⚠ **하루치로 쟀을 때와 결론이 달라졌다. 2.14 원칙이 옳았다.**

```
신호                  매치   실질정탐   ★오탐   판정
S2 전각 구분자 │        32      24       8    ⛔ 기각 — 오탐 25%
S3 괄호 안 인물          3       3       0    ○ 정밀하나 3건뿐
S4 인터뷰·간증 어휘      40      27      ~6    ⛔ 기각 — '이야기'가 너무 넓다
S5 해시태그 3개 이상     15       7      ~6    ⛔ 기각 — 마케팅 습관이다
P  프로그램명 사전 10종   78      70       8    △ 90% — 쪼개야 쓸 수 있다
```

**S2가 무너진 이유** — 잔여 8건이 전부 **CBSJOY 성서학당 예레미야 강해**였다.
`│`를 쓰는 것은 CBSJOY의 편집 습관이고, 그 채널은 간증도 강해도 같은 문자를 쓴다.
2.73 ⑤에서 "구분자는 콘텐츠 구조가 아니라 타이핑 습관"이라 적은 것이
**같은 채널 안에서도 성립**한다는 뜻이다.

**S4가 무너진 이유** — `이야기`가 "찬송가 이야기"(광주극동방송 찬양 해설),
"R.T. 켄달의 말씀과 성령 — 거룩한 불: 간증"(설교 시리즈)까지 잡는다.

**S5가 무너진 이유** — 광주극동방송이 **말씀 콘텐츠에 해시태그를 단다**
("성경구절 12가지 #성경 #말씀 #위로").

**★ P(사전)도 그대로는 못 쓴다 — 라벨과 프로그램명을 구분해야 한다**

`CTS 추천픽`은 **프로그램명이 아니라 편성 라벨**이다. 29건 안에 4개 프로그램이 섞여 있다.

```
광복 81주년 독립 이야기   6건  다큐      정탐
CTS 뉴스 W              5건  뉴스      정탐
내가 매일 기쁘게          4건  간증      정탐
★ 두란노 성경교실         4건  성경 강의  ⛔ 오탐 — 제3범주가 아니다
```

`CBS 광장` 6건 중 4건도 **성경 인물 해설**이다(시드기야·엘리야·안디옥). 제3범주로
보기 어렵다.

```
프로그램명 사전 매치 78건 → 실질 정탐 70건(90%) · 오탐 8건(10%)
   두란노 성경교실 4 · CBS 광장의 성경 해설 4
```

**④ 실측이 말하는 구현 방향**

```
버릴 것   S2 구분자 · S4 어휘 · S5 해시태그   ← 오탐이 크다
남길 것   S3 괄호 안 인물 (오탐 0, 다만 3건)
고칠 것   P 사전을 **라벨이 아니라 실제 프로그램명**으로 쪼갠다
          제외:  CTS 뉴스 W · 내가 매일 기쁘게 · 광복 81주년 · 위대한 유산 ·
                 새롭게 하소서 · 부르심의 소명 · 온전한 인터뷰 · 레디온 ·
                 휴먼네컷 · 원더풀우먼 · 만나고 싶은 사람
          ⛔ 넣지 말 것: CTS 추천픽(라벨) · CBS 광장(성경 해설이 섞임) ·
                        두란노 성경교실(성경 강의)
```

⛔ **구분자(`|`)는 신호로 쓰지 말 것**(사용자 지시 · 2026-08-27). 시각적으로 같은
세로 막대가 유니코드 4종으로 섞여 있다 — `|` U+007C 1,806회 · **`ㅣ` U+3163
한글 자모** 106회 · `│` U+2502 48회 · `｜` U+FF5C 6회.

⏸ **구현은 사용자 확인 대기.** `promo_reason`과 같은 계열로 만든다 —
새 구조를 만들지 않는다.

---

### 2.75 ★ 제3범주 폴백 제외 구현 — 사전 11종 + 보조 1종 (2026-08-27)

2.74의 실측을 근거로 사용자가 승인한 안을 구현했다. **구조 신호 4종 중 3종은
기각됐고, 사실상 사전 단독 + 보조 1종이다.**

**① 무엇을 넣었나 — `promo_reason`의 (d)(e)**

새 구조를 만들지 않았다. 기존 폴백 품질 필터에 갈래를 더했다.

```
(a) 위치 무관   공고·홍보          2026-08-20
(b) 머리에만    행사               2026-08-20
(c) 시리즈      광복 81주년         2026-08-20
(d) 제3범주     프로그램명 11종      ★ 2026-08-27
(e) 제3범주     괄호 안 출연자 표기   ★ 2026-08-27  (S3 · 오탐 0, 3건뿐)
```

제3범주는 **마지막에** 본다 — 위 이유가 먼저 걸리면 그 이유를 남긴다. 로그·리포트의
기존 분류가 바뀌지 않게 하려는 순서다.

```
새롭게 하소서 · 부르심의 소명            CBSJOY 간증
온전한 인터뷰 · 레디온 · 휴먼네컷        CGN 인터뷰·뉴스·다큐
원더풀우먼 · CTS 뉴스 W · 내가 매일 기쁘게  CTS 토크·뉴스·간증
만나고 싶은 사람                        극동방송 인터뷰
광복 81주년 · 위대한 유산                CTS 다큐
```

※ '광복 81주년'·'위대한 유산'은 (c)가 이미 잡는다. 그래도 (d)에 둔 것은 **그
시리즈가 끝나도 프로그램명은 남기 때문**이다. 중복은 순서상 문제가 되지 않는다.

**② ⛔ 넣지 않은 것 — 라벨과 프로그램명은 다르다**

```
CTS 추천픽      **편성 라벨**이다. 29건 안에 다큐 6 · 뉴스 5 · 간증 4와 함께
               두란노 성경교실(성경 강의) 4건이 섞여 있다
CBS 광장        6건 중 4건이 성경 인물 해설(시드기야·엘리야·안디옥)이다
두란노 성경교실   성경 강의다. 제3범주가 아니다
```

**프로그램 하나를 가리키는 이름만 넣는다.** 이 경계를 `spread_test`가 고정한다 —
셋 중 하나라도 사전에 들어가면 MUST_KEEP에서 깨진다.

**③ ⛔ 기각된 구조 신호 3종 — 코드 주석에 근거를 남겼다**

`selection.py`의 `_TRAILER` 위에 왜 안 쓰는지를 적어 뒀다. 되살리지 말 것.

```
S2 전각 구분자 │   32건 중 오탐 8 (25%) — 잔여가 전부 CBSJOY 성서학당 강해였다
S4 인터뷰·간증 어휘 40건 중 오탐 ~6 — '이야기'가 찬송가 해설·설교 시리즈까지 잡는다
S5 해시태그 3개↑   15건 중 오탐 ~6 — 광주극동방송이 말씀에도 해시태그를 단다
```

⛔ **특히 구분자는 신호로 쓰지 말 것**(사용자 지시). 같은 세로 막대가 유니코드
4종이다 — `|` U+007C 1,806회 · **`ㅣ` U+3163(한글 자모)** 106회 · `│` U+2502 48회 ·
`｜` U+FF5C 6회. 콘텐츠 구조가 아니라 편집자의 타이핑 습관이다.

⚠ 사전에 **항목을 추가할 때 오탐 실측이 먼저**라는 것도 상수 주석에 적었다(2.14).

**④ 적용 후 재확인 — 48/48이 20건을 유지한다**

구현한 코드로 다시 돌렸다(시뮬레이션의 임시 패턴이 아니라 실제 `promo_reason`).

```
폴백 후보 1,148 → 1,101건 (제외 67 = 제3범주 47 · 행사 10 · 시리즈 6 · 공고 4)

48개 탭: 20건 이상 **48/48** · 최소 20 · 최대 23 · 평균 20.5
20건 미만: 없음
anger.irritation [말씀] 20 · exhaustion.tired [말씀] 20 · calm.ease [말씀] 20
```

게이트도 갱신했다 — `spread_test` [10]이 MUST_DROP 18종 · MUST_KEEP 10종을 고정한다.

**⑤ ⚠ 잠복 항목 — `_TRAILER`에 `ㅣ`가 없다**

`_TRAILER = re.compile(r"\s*[|｜│]\s*")`에 한글 자모 `ㅣ`가 빠져 있다. 이것만 쓰는
제목이 87건이라 (b) 머리 판정이 제목 전체로 번진다. **다만 그 때문에 과다 제외되는
폴백 후보는 현재 0건**이다(2026-08-27 확인). 고치려면 PROMO_HEAD 전체에 대해
오탐을 다시 재야 하므로 손대지 않았다. 주석으로 남겼다.

---

### 2.76 ★ 방법론 — 하루치로 잰 오탐 0은 믿을 수 없다 (2026-08-27)

**2.14 원칙("오탐 실측이 따라야 한다")이 실제로 사고를 막은 사례다.** 기록해 둔다.

```
2026-08-26 하루치 1,754건으로 재기        S2 오탐 0 · S3 오탐 0 · S5 오탐 0
2026-08-27 5일치 합집합 2,023건으로 재기   S2 오탐 8(25%) · S4 오탐 ~6 · S5 오탐 ~6
```

**코퍼스를 15% 늘렸을 뿐인데 신호 3종이 무너졌다.** 하루치 결과만 보고 넣었다면
CBSJOY 성서학당 예레미야 강해 8건, 광주극동방송 찬송가 해설·성경구절 콘텐츠가
말씀 탭에서 조용히 사라졌을 것이다. **결함이 뜨지 않는 종류의 손실**이다 —
경보도 안 울리고 건수도 20으로 차 있으니 아무도 모른다.

**왜 하루치가 위험한가**

```
한 채널의 편집 관행이 하루에는 한 가지 형태로만 나타난다.
  08-26 코퍼스의 CBSJOY = 간증(새롭게 하소서) 중심
  5일치의 CBSJOY        = 간증 + **성서학당 예레미야 강해 8건**
같은 채널이 같은 구분자로 다른 성격의 콘텐츠를 올린다는 사실이
하루치에서는 보이지 않는다.
```

★ **코퍼스를 넓히는 데 API가 들지 않는다.** 과거 실행의 아티팩트(`build-report-N`)에
`titles.json`이 들어 있고 GitHub에서 그냥 받는다. 2.70이 `--dump-titles`를 남긴
이유가 이것이다("태깅 분석을 API 0으로 하기 위해서다" — build.yml 주석).

```
gh run download <run_id> -D <dir>      # YouTube API 0
→ dist/titles.json 을 videoId로 합집합
```

⚠ **아티팩트는 만료된다.** 이번에 쓸 수 있던 것은 build-report 10·11·12·13
(08-24~27)뿐이고 그 이전은 전부 `expired=True`였다. **넓은 코퍼스가 필요한 판단은
미루지 말 것** — 미루면 재료가 사라진다.

⛔ **다음에 판별 규칙·사전을 손볼 때의 절차**

```
1. 아티팩트를 모아 합집합 코퍼스를 만든다 (API 0)
2. 신호별 정탐/오탐을 잰다 — **하루치로 끝내지 않는다**
3. 오탐이 나온 사례를 spread_test의 MUST_KEEP에 고정한다
4. 그 다음에 넣는다
```

---

### 2.77 ★ 제3범주 필터 첫 실전 — 걸러냈지만 중복은 안 줄었다 (2026-08-28)

`run 33110799771` (sha `d9ad370`) · 09:30Z 슬롯 · success.

**① 필터는 의도대로 걸렀다 — 63건**

```
폴백 후보에서 공고·행사·홍보·제3범주 63건 제외 — 남은 후보 1106건

제3범주 47 · 행사 9 · 공고/홍보 4 · 시리즈(광복) 3

세부  새롭게 하소서 18 · 온전한 인터뷰 6 · 레디온 6 · CTS 뉴스 W 5 ·
      원더풀우먼 4 · 휴먼네컷 4 · 부르심의 소명 3 · 만나고 싶은 사람 1
```

★ `[CTS 추천픽]` 접두어가 붙은 뉴스 5건이 **`CTS 뉴스 W`로 정확히 걸렸다** —
라벨이 아니라 프로그램명으로 키를 잡은 설계(2.75 ②)가 실전에서 확인됐다.

**48/48 탭이 20건을 유지한다.** 최소 20 · 최대 24→**23** · 평균 20.81→20.71.
폴백 비중도 36.5%→35.4%로 내려갔다.

**말씀 탭 품질 개선이 눈에 보인다** — `anger.irritation`·`calm.ease`에서:

```
08-26  [폴백] CTS  [한국교회를 논하다] 601회ㅣ초고령사회와 한국교회 (4)   토크쇼
08-27  [폴백] CTS  선택 이것이 문제다 l 원유경 목사 l 창세기 13장 1-18절   실제 설교
```

`exhaustion.tired`는 주제분이 1→2건으로 늘었다(C채널 "광야행진 (29) 안식에 들어가라").

**② ⚠⚠ 중복 노출이 줄지 않았다 — 늘었다. 예상이 틀렸다**

```
                    화면      화면당 최대   총 노출   고유    한 영상 최대
08-26 직전        19/24         6건       63회    14건    14화면
08-27 필터후      22/24         7건       61회    13건    18화면  ★
```

⛔ **"부수적으로 중복도 준다"던 예상은 틀렸다.** 총 노출만 63→61로 소폭 줄었고
확산은 오히려 넓어졌다.

**원인** — 폴백에서 제3범주가 빠진 자리를 **다른 unknown이 채웠다.**
남은 unknown 13건의 정체가 그것을 말한다.

```
18화면 [fallback] CGN     급진적인 사랑의 원천 (2) | 존 파이퍼의 더 바이블  ← 새 최다 확산
16화면 [fallback] 극동방송  이게 바로 하늘의 빌드업인가? | 하바드 13강
 4화면 [theme]   CGN     평안이 우선입니다 | 젠센 프랭클린의 부흥
 3화면 [theme]   사랑의교회 예수 피를 힘입어/문들아 머리 들어라/…  (곡명 나열)
```

**13건 중 12건이 제3범주가 아니다** — 존 파이퍼 강해·하바드 성경 강의·젠센
프랭클린 부흥·믿어야 산다·사랑의교회 찬양 메들리. 전부 **설교이거나 찬양인데
형식 판별에 실패한 것**이다.

★ **중복 노출은 제3범주의 함수가 아니라 unknown 판별 실패의 함수다.**
제3범주 중 unknown인 것을 빼도 다른 unknown이 그 자리에 온다.
2.73 ⑥의 "초과 ⊆ unknown" 관계는 성립하지만, unknown의 **구성**이 바뀔 뿐
총량은 안 준다. **중복을 줄이려면 형식 판별을 손봐야 한다**(아래 ⑤).

**③ ✅ MUST_KEEP 4건이 실물로 살아남았다**

```
성서학당 강해        1건  "유다가 멸망한 진짜 이유│성서학당 김기석 목사 예레미야 29강"
찬송가 해설          1건  "알고 들으면 더 은혜로운 찬송가 이야기! 64장"
성경구절 해시태그      2건  "듣기만 해도 마음이 편안해지는 성경구절 12가지 #성경 #말씀"
두란노 성경교실·CBS 광장  0건 (그날 선정 안 됨 — 제외된 것이 아니다)
```

**기각한 신호 3종(S2·S4·S5)을 썼다면 이 4건이 조용히 사라졌을 것이다.**
2.76의 방법론이 추상이 아니라 실물로 확인된 자리다.

**④ ⏸ theme 층 경계 — 별도 판단 사항으로 등재**

```
원더풀우먼      3 → 2건 잔존   둘 다 theme 층
모두의 거실      1건 잔존       theme 층 (사전에 없는 제3범주이기도 하다)
```

`promo_reason`은 **설계상 폴백 전용**이다 — "주제어가 걸린 영상은 그 나름의
근거가 있고, 두 층은 판단 기준이 다르다". 그래서 주제 태깅이 붙은 제3범주는
남는다. **폴백에서는 100% 제거됐고 theme 층이 경계다.**

⏸ 이 경계를 유지할지는 판단이 필요하다 — **주제어가 걸렸어도 인터뷰는
인터뷰다.** 다만 폴백 전용이라는 전제를 깨는 것이므로 2026-08-20 결정을
다시 여는 일이고, 오탐 실측이 다시 필요하다.

**⑤ ⏸ 다음 후보 — 중복을 줄이려면 형식 판별(unknown)이다**

2.73 ④의 **안 C(곡명 구조 신호 `가/나`)가 오탐 0으로 남아 있다.** 5일치
합집합에서도 무너지지 않은 유일한 구조 신호다. 사랑의교회 곡명 나열 3건이
지금도 양쪽 탭에 뜬다 — 이것부터 걷어낼 수 있다.

⚠ 그것만으로는 부족하다. 최다 확산 2건(존 파이퍼 18화면 · 하바드 16화면)은
곡명이 아니라 **10~30분 사각지대에 갇힌 강해**다(2.73 ②). 이쪽은 안 E(길이
사각지대 축소)나 A′가 필요한데 둘 다 제3범주를 말씀으로 모는 위험이 있었다
— **제3범주가 이제 폴백에서 빠졌으므로 그 위험이 줄었을 수 있다. 다시 재볼 것.**

**⑥ 진단 수치**

```
                    08-26        08-27
unknown_ratio         —          0.045   ★ 첫 기록
unknown_kept          —          79
tab_max_exposed      24*         23      ↓
tabs_over_cap        15개*       18개    ↑
from_fallback        342         330     ↓ (36.5% → 35.4%)
total_exposed        936         933
```
\* 08-26은 진단 필드 추가 전이라 수동 계산치다.

**tab_over_cap 경보 0건** — 최대 23 < 임계 26. 진단에는 18개 탭이 기록돼 추세는
남았다. **2.72 설계대로 동작한다**(조용하되 기록은 남는다).

---

### 2.78 ⚠⚠ Actions 지연 악화 — 하루 2회 설계의 전제가 흔들린다 (2026-08-28)

**최우선 관찰 항목이다.**

```
08-20 ~ 08-26   21~42분        정상 범위 (2.23에서 "30분쯤 늦는 것이 정상")
08-27 00:58Z     3시간 28분     2.71 ②에서 관찰 대상으로 등재
08-27 19:55Z    10시간 25분     ★★ 09:30Z 슬롯이 19:55Z에 시작
08-27 21:30Z     미실행         08-28 00:22Z 시점에 2시간 52분 경과, 아직 안 돎
```

**① 12시간 등간격이 무너졌다**

09:30Z 슬롯이 19:55Z에 돌면 21:30Z 슬롯과 **1시간 35분** 차이밖에 없다.
2.68이 계산한 노출 지연 개선(평균 11.8h→6.2h)은 이 지연폭에서 성립하지 않는다.
"06:30 예약이 실제로는 07:00 전후에 돈다"던 build.yml 주석도 사실과 다르다.

**② ⚠ 잠복 위험 — 주간 진단이 통째로 건너뛰어질 수 있다**

주간 진단·기준선은 `new Date().getUTCDay() !== 1`로 월요일을 가른다.
**월요일 09:30Z 실행이 14.5시간 이상 밀리면 UTC 화요일로 넘어가 두 스텝이 모두
조용히 건너뛰어진다.** 이번 지연(10h25m)은 그 문턱에 근접했다.

그러면 그 주의 악화 판정이 사라지고, 기준선도 안 밀려 **다음 주는 2주 전과
비교**하게 된다. 실패하지도 로그에 남지도 않는다 — 2.72에서 막았던 것과 같은
종류의 조용한 고장이다.

⚠ 슬롯 판별을 `github.event.schedule`로 하는 것은 지연에 영향받지 않는다(예약
문자열이 그대로 온다). **날짜 판정만 취약하다.**

**③ 확인 방법 — 어느 슬롯인지는 게이트로 가른다**

```
주간 진단·기준선 스텝이  success  → 09:30Z 슬롯 (저녁)
                      skipped  → 21:30Z 슬롯 (아침) 또는 수동
```

두 번 다 이 방법으로 판별했다(2.71 ② · 2.77). 실행 시각만 보면 알 수 없다.

**④ 판단 후보 — 아직 결정하지 않았다**

```
가. 관찰 계속           표본이 3회뿐이다. GitHub 측 일시적 부하일 수 있다
나. 예약 시각 앞당김     지연이 상수라면 09:30Z→더 이른 시각. 하지만 지연이
                     3.5h·10.4h로 들쭉날쭉해 상수로 볼 근거가 없다
다. 날짜 판정 보강      주간 진단을 getUTCDay 대신 예약 회차 기준으로.
                     ②의 잠복 위험만 따로 막는 안 (지연 자체는 못 고친다)
```

⚠ **'나'를 성급히 하지 말 것.** 지연폭이 21분~10시간으로 분산이 크다. 앞당겼는데
지연이 정상으로 돌아오면 이번엔 너무 이른 시각에 돌아 그날 업로드를 놓친다 —
2.68이 18:30을 고른 근거(누적 86%)가 깨진다.

---

### 2.79 ★ 주간 진단이 지연에 무너지던 것을 고쳤다 (2026-08-28)

2.78 ②의 잠복 위험을 사용자 결정으로 구현했다.

**① 무엇이 문제였나**

두 스텝이 **각자** 월요일을 판정했고, 둘 다 **실행 시각**을 봤다.

```
주간 진단 요약     JS   new Date().getUTCDay() !== 1
주간 기준선 갱신   bash  [ "$(date -u +%u)" = "1" ]
```

09:30Z 예약이 **14시간 30분** 이상 밀리면 UTC 화요일이 되어 둘 다 걸린다.
2026-08-27 실측 지연이 **10시간 25분**이라 문턱 코앞이었다. 걸리면 그 주 악화
판정이 사라지고 기준선이 안 밀려 다음 주는 **2주 전과 비교**한다. 실패도 로그도
남지 않는다.

**② 무엇으로 바꿨나 — 축이 둘이다. 하나만 두지 말 것**

```
1. 예약 발동 시각      실행 시각 이전의 가장 가까운 09:30Z를 이번 회차로 본다.
                     10시간을 밀려도 그 값은 안 변한다. 24시간 미만 지연을 흡수.
2. 기준선 이후 경과일   1번만으로는 GitHub이 회차를 **통째로 흘려보낸** 경우를
                     못 잡는다(2026-08-27 21:30Z 슬롯이 실제로 그랬다).
                     8일이 넘으면 요일과 무관하게 돌려 그 주를 안 건너뛴다.
```

⚠ **두 축이 반대 방향의 실패를 각각 막는다.**
경과일만 두면 요일이 표류하고, 예약 시각만 두면 회차 누락을 못 잡는다.

상수는 `scripts/lib/weekly.py`에 있다 — `MIN_GAP_DAYS = 6`(중복 방지),
`STALE_DAYS = 8`(누락 안전망). 8인 이유는 정상 경로가 7.0일 언저리라 오발동하지
않고, 월요일을 놓치면 화요일에 정확히 8.0일이 되어 바로 잡히기 때문이다.

**③ ★ 판정을 한 곳에서 하고 두 스텝이 공유한다**

```yaml
- name: 주간 진단 실행 여부 판정
  id: weekly
  if: ... && github.event.schedule == '30 9 * * *'
  run: python scripts/weekly_gate.py        # GITHUB_OUTPUT에 due=true|false

- name: 주간 진단 요약 (악화 시에만)
  if: ... && steps.weekly.outputs.due == 'true'
- name: 주간 기준선 갱신
  if: ... && steps.weekly.outputs.due == 'true'
```

⛔ **두 스텝에 조건을 따로 쓰지 말 것.** 같은 값을 두 번 계산하는 구조 자체가
어긋남의 원인이었다. 슬롯 가르기(저녁 슬롯만)는 판정 스텝에 남아 그대로 작동한다.

**④ 건너뛴 것도 흔적을 남긴다**

전에는 `core.info`뿐이라 로그를 펼쳐야 보였다. 이제 **Job Summary에 한 줄**이
남는다 — 돌든 안 돌든.

```
- **주간 진단**: 예약 발동 2026-08-31 09:30Z(월) · 기준선 이후 7.0일 → 실행 (예약 발동일이 월요일이다)
- **주간 진단**: 예약 발동 2026-09-02 09:30Z(수) · 기준선 이후 2.0일 → 건너뜀 (예약 발동일이 수요일이다)
```

**⑤ 회귀 — spread_test [12] 8종**

```
정상 월요일 정시                              돈다
★ 지연 10h25m (같은 날)                      돈다     ← 2026-08-27 실측 재현
★★ 지연 16h (자정 넘김·실행일 화요일)          돈다     ← 옛 게이트가 깨지던 지점
중복 방지 (기준선 방금 갱신)                   안 돈다
평일 (수요일 회차)                            안 돈다
★ 누락 안전망 (월요일 놓친 뒤 화요일 8.0일)     돈다
기준선 없음                                   돈다
두 스텝이 같은 출력값 · 실행 시각 판정 잔존 없음   ✅
```

⚠ 마지막 검사는 **주석을 빼고** 본다 — `getUTCDay`·`date -u +%u`는 "왜 없앴는지"를
적어 둔 주석에 이름이 남아 있다. 살아 있는 코드에만 없으면 된다.

---

### 2.80 ★ 안 E 적용(1200s) + unknown 조사 3건 (2026-08-28)

**① ★ 안 E 적용 — `sermon_min_seconds: 1800 → 1200`** (사용자 결정)

```
                unknown  ratio   탭최대  평균     중복화면  최대  총노출  고유  최대확산
현행 1800s         79    0.045    23    20.71    22/24    7    61회   13건   18화면
★ 적용 1200s       26    0.015    21    20.19    11/24    3    15회    3건    9화면
```

**48/48 탭이 20건을 유지**하고(최소 20), 나머지가 전부 개선됐다.
unknown −67% · 중복 노출 −75% · 최대 확산 18→9화면.

★ **2.77 ②에서 "중복은 unknown 판별 실패의 함수"라 적은 것이 이것으로 확인됐다.**
제3범주 필터로는 안 줄던 중복이 형식 판별을 고치자 절반 이하로 떨어졌다.

**[왜 2026-08-27까지는 위험하다고 봤나 — 실측으로 뒤집혔다]**

2.73 ④에서 이 안은 "제3범주를 말씀으로 몬다"는 이유로 위험 판정을 받았다.
5일치 합집합 2,055건으로 다시 재니 전제가 달라져 있었다.

```
1200s로 낮추면 65건 구제 → 말씀 23 · 제3범주 40 · 찬양 0
   그중 promo_reason이 **이미 폴백에서 빼는 것** 36/40
   제3범주 중 **주제 태깅된 것**            0건
→ sermon으로 오분류돼도 화면에 안 나온다
```

**제3범주 필터(2.75)가 이 안의 전제를 바꿨다.** 위험 판정의 근거가 사라진 것이지
판단이 틀렸던 것이 아니다 — 그때는 필터가 없었다.

⚠ **남는 위험 — 사전에 없는 제3범주 2건.** 40건 중 promo_reason에 안 걸리는
것이 4건이고 그중 실제로 문제가 될 수 있는 것이 2건이다(모두의 거실 등).
**새 프로그램이 생기면 그만큼 는다** — `THIRD_PARTY_PROGRAMS`를 따라 늘려야 한다.

⛔ **900s까지 내리지 말 것.** 제3범주가 53건으로 늘고 폴백에서 안 걸리는 것이
5건이 된다. 1200이 실측으로 잡은 선이다.

**짚어주신 영상 커버리지**

```
젠센 프랭클린 7건 → 5 해소 · 존 파이퍼 8건(unk 4) → 4 · 믿어야 산다 8건(unk 7) → 7 ·
하바드 5건 → 5      합계 28건 중 23건 해소
```

경계는 `tagging_test`가 고정한다 — 1199 → unknown · 1200 → sermon · 1500 → sermon.

**② ⏸ [2] unknown을 양쪽 탭에서 제외 — 시뮬레이션만 (미적용)**

```
              20건   최소   최대   평균     주제분   폴백
현행           48     20    23    20.7     608    344 (36.1%)
unknown 제외   48     20    20    20.0     569    351 (38.2%)
```

★ **48/48이 정확히 20건이 된다.** 초과가 완전히 사라진다 — 2.72에서
"A안(탭별 목록 분리)뿐"이라던 정확한 20이 이 방법으로도 달성된다. 초과가 애초에
unknown 누출이었으니 당연한 귀결이다.

⚠ **우려했던 채널 감소는 일어나지 않는다.**

```
[말씀] 16개 채널 → 16개 · [찬양] 16개 → 16개   사라지는 채널 0개
실효 천장  말씀 48→48 · 찬양 46→43 (탭당 필요 20)
```

unknown을 내는 6개 채널이 **전부 타입 있는 영상도 낸다**(CGN unknown 31 ·
sermon 24 / CBSJOY unknown 25 · sermon 22 / …). 채널이 통째로 빠지지 않는다.

⚠ **다만 근거의 강도가 내려간다** — 주제분 −39 · 폴백 +7. unknown 중 주제
태깅이 붙은 것(젠센 프랭클린 `peace` 등)이 함께 빠지기 때문이다.
`joy.proud` 말씀은 17+3 → 11+9가 된다.

⏸ **①을 적용해 unknown이 79→26으로 줄었으므로 이 안의 효과도 달라졌다.
다시 재고 판단할 것.**

**③ ⏸ [4] 길이 하한 600 — 근거가 실측이 아니었다 (미적용)**

`themes.yaml` 주석의 추론이 전부다. **HANDOFF 어디에도 실측 근거가 없다.**

> 설교는 30분 이상이 흔하고 찬양 한 곡은 5분 내외다 … 10분 이하면 worship 쪽 (메들리 여유 포함)

오분류 규모는 **52건 중 6건(12%)** 이고, 그중 명백한 설교는 2건이다
(염려 대신 감사를 3:00 · 하나님은 우리의 피난처 9:59). 3건은 제3범주,
1건은 퍼포먼스다.

⚠ **낮추면 중복이 악화된다.**

```
300s로  22건이 풀림 → 말씀 4건 구제 · 나머지 18건이 unknown
180s로  47건이 풀림 → 말씀 5건 구제 · 나머지 42건이 unknown
```

unknown은 양쪽 탭에 다 뜨므로(2.77 ②) 4~5건을 구제하려고 18~42건을 중복으로
만드는 거래다. **②를 먼저 정하면 이 상충이 사라진다** — 그래서 ② 뒤로 미뤘다.

**④ ⚠ themes.yaml 주석과 구현이 어긋나 있었다 (정정함)**

주석은 길이 신호를 "단독 판정 근거로 쓰지 않는다"고 적었는데 **사실과 다르다.**
앞의 세 단계가 답을 못 내면 이 규칙이 **최종 판정을 내린다**(`reason=duration`).
"1순위가 아니다"와 "단독 판정이 아니다"는 다른 말이고, 실제로는 단독이다.
주석을 고쳤다 — 임계를 정할 때 이 사실을 알고 정해야 한다.

**⑤ ⏸ 별건 등재 — 프로그램명이 형식 어휘로 오인된다**

```
[worship|title] 54:08  그의 마음을 따르는 사람 1 : 조용한 혁명┃한소망교회 최봉규 목사 [C채널] 한소망비전워십
[worship|title] 45:04  그의 마음을 따르는 사람 2 : 왕의 리더십┃한소망교회 최봉규 목사 [C채널] 한소망비전워십
```

**45~54분 설교인데 제목의 "한소망비전워십"(프로그램명)이 찬양 어휘로 잡혔다.**
길이가 30분을 넘어 어떤 길이 규칙으로도 안 잡히고, A′(말미 목사)에도 안 걸린다
— **제목 규칙이 채널·프로그램 이름을 형식 신호로 오인하는 별개 결함**이다.

⚠ 2.48에서 "프로그램명 사전 제외"를 한 적이 있다(주제 태깅 쪽). 같은 유형이
**형식 판별에서도** 나타난 것이다. 손볼 때 그 절을 함께 볼 것.

---

### 2.81 ★ 형식 어휘 정리 + 목록 순서 개편 (2026-08-28)

세 가지를 한 번에 넣었다. 앞의 둘은 배치, 셋째는 앱이다.

**① 단독 '워십'을 형식 어휘에서 뺐다 — `워십팀`·`워십밴드`만 남긴다**

```
전  [찬양, 워십, 경배, CCM, 특송, 연주, 피아노, 찬양대, 워십팀, …]
후  [찬양, 워십팀, 워십밴드, 경배, CCM, 특송, 연주, 피아노, 찬양대, …]
```

5일치 합집합 2,055건에서 **'워십'이 유일한 worship 어휘인 제목 10건이 전부
프로그램·예배 이름**이었다. 진짜 찬양이 0건이다.

```
한소망비전워십 2 · 만나워십 2   ← 41~54분 설교가 worship으로 확정됐다
W워십 4 · 웬즈데이 워십 1 · 기타 1  ← 이미 sermon으로 잡히던 것
```

**실측 — 판정이 바뀌는 것 정확히 4건, 전부 목표. 오탐 0 · 정탐 손실 0.**

```
찬양 탭 합계   480 → 480 (변화 없음) · 최소 20 · 24/24
말씀 탭 합계   489 → 489
48개 탭        최소 20 · 최대 21 · 평균 20.19 (변화 없음)
중복 노출      화면 11/24 · 총 15회 (변화 없음)
media_type     worship 660→656 · sermon 1074→1078
```

★ '워십'을 포함하되 **다른 worship 어휘도 있는 30건은 판정 변화 0건**이다
(`찬양`·`피아노` 등이 남는다). 요청대로 확인했다.

⚠⚠ **근거의 한계 — 이 코퍼스 40건에서의 관찰이다.** "단독 '워십'은 항상
프로그램명"이 규칙으로 참인 것이 아니다. `"8월 워십"`처럼 다른 어휘 없이 '워십'만
쓴 **진짜 찬양**이 나오면 깨진다. themes.yaml 주석에 같은 경고를 적었다.

⏸ **이 안은 설교 내용어 유형을 못 고친다 — 별건으로 남긴다.**
`"무릎 꿇고 찬양하세요"`(34:44) · `"하나님을 찬양합니다"`(33:55) 두 건은 '찬양'이
설교의 **주제**다. '찬양' 단독 매치 106건 중 15건이 sermon인 구조적 한계이고
어휘 조정으로는 풀리지 않는다.

부수 효과: `tagging_test`의 W워십 케이스가 근거 `channel` → **`title`**로 올라갔다.
판정은 그대로 sermon이다 — 동점이 아예 성립하지 않아 1순위에서 확정된다.
그 줄이 `channel`로 되돌아가면 '워십'이 어휘에 다시 들어온 것이다.

**② 주제분 순서를 개별 영상 단위로 섞는다 — 앱에서**

⚠ **배치가 아니라 앱이다.** "화면에 들어갈 때 한 번 섞고, 감정을 다시 입력하면
새로 섞는다"는 요구는 정적 JSON으로는 못 낸다.

```
app/src/lib/videos.js  shuffleThemeLayer(videos, seed)   순수 함수
app/src/App.jsx        씨앗을 useState 초기화로 마운트당 1회
```

`Result`는 뒤로가기 시 `result`가 null이 되어 **언마운트**된다. 그래서 다시
입력하면 새로 뜨고 새 씨앗을 받는다. 스크롤·탭 전환은 리렌더일 뿐이라 씨앗이
그대로고, 섞기가 씨앗에 대해 순수 함수라 몇 번을 다시 계산해도 같은 목록이 나온다.

★ **같은 채널이 연속으로 붙지 않게** 흩는다. 처음엔 인접한 것을 맞바꾸는 방식을
썼는데 3연속 이상에서 남았다(8건 표본 씨앗 4에서 2건 잔존). **남은 개수가 많은
채널을 먼저 내는** 방식으로 바꿔 해결했다 — 씨앗 200개에서 인접 중복 0건이고
서로 다른 순열이 182가지다(섞임이 살아 있다).

⚠ 한 채널이 과반이면 어떻게 놓아도 붙는다. 그때는 감수한다 — 억지로 떼려고
순서를 더 흔들면 무작위성의 의미가 없다. 회귀가 그 경우도 고정한다.

⚠ **폴백은 섞지 않는다** — ③이 최신순을 약속한다.

**③ 폴백 정렬을 전체 최신순으로**

전에는 라운드로빈 픽 순서가 그대로 화면 순서였다. 채널을 한 바퀴씩 도는 순서라
화면에 "채널1 → 채널2 → … → 채널16 → 채널1(2바퀴째)"로 나왔고, 실측
(`anger.irritation` [말씀] 20건)에서 **최신순 역전이 8건**이었다.

폴백 층의 헤더는 **"요즘 올라온 것들"** 이라고 말한다. 그 문장과 목록이 어긋나
있었다. 채널 분산은 선정에서 이미 끝났으므로(상한 3) 순서까지 채널을 따라갈
이유가 없다.

⚠ **정렬만 바꿨다. 채널당 상한은 그대로다** — 무엇이 들어가는지는 달라지지 않고
같은 20건이 다른 순서로 나온다.

**④ ⚠ `fill_balanced` 주석을 정정했다**

> 선정은 라운드로빈으로 하고, **출력 순서는 최신순으로 되돌린다.**

**사실이 아니었다.** 되돌리는 것은 **풀 순서**이고, 풀 순서는 전역 최신순이 아니라
**채널 묶음 + 채널 안에서만 최신순**이다(`collect_uploads`가 채널을 차례로 돌며
각 채널 uploads를 최신순 100건씩 붙인다).

실측(`anger.irritation` [찬양] 2026-08-27): ANOINTING 3건(136일) → 옹기장이
1건(949일) → 택피아노 3건(0·7·14일) → mini Music 3건 … 채널이 묶여 나오고
묶음 안에서만 최신순이었다.

★ 이제 **화면 순서는 앱이 정한다.** 배치가 되돌리는 풀 순서는 *어떤 20건을
담을지*를 정하는 데만 쓰이고 표시 순서가 아니다. 주석에 그렇게 적었다.

**⑤ ★ 날짜 제한을 두지 않기로 확정했다 (사용자 결정)**

조사에서 확인한 사실: **업로드 날짜 제한이 코드 어디에도 없다.** 필터는
unavailable · 지역차단 · 180초 미만 · 쇼츠 표기 · blocklist · 승인목록 밖이 전부다.
유일한 경계는 `DEFAULT_UPLOADS_PER_CHANNEL = 100`이라는 **건수** 제한이고,
그것이 채널마다 전혀 다른 기간을 덮는다(CBS설교 100건 ≈ 5일 · 옹기장이 ≈ 1,043일).

실제 결과로 `anger.irritation` [찬양]에 **949일 된 영상**이, [말씀]에 **246일 된
영상**이 오늘 것과 나란히 있었다.

**결정: 그대로 둔다.** 근거는 이것이다.

```
설교·찬양은 업로드가 잦지 않고 시기성을 크게 타지 않는다.
949일 된 영상이 나오는 것을 감수한다.
```

⛔ **나중에 "오래된 영상이 나온다"를 결함으로 보고 날짜 컷을 넣지 말 것.**
검토를 마치고 감수하기로 한 것이다. 넣으면 공급이 얇은 채널(옹기장이·광주극동
방송)이 목록에서 통째로 빠지고, 그것은 2.66이 폴백 천장을 올려 막으려던 상태다.

⛔⛔ **[2026-08-28 정정 — 이 결정은 부분적으로 뒤집혔다. 근거가 안 맞았다.]**

**무엇이 바뀌었나**

```
폴백    90일 하드 컷 + 화면별 무작위를 넣었다 (2.84)
주제분  신선도 사다리를 넣었다 — 90일 먼저, 정원을 못 채우면 전체 (2.86)
```

★ **위 ⛔ 경고의 근거("공급이 얇은 채널이 목록에서 통째로 빠진다")가 실측에서
성립하지 않았다.** 두 자리 모두에서 그렇다.

```
폴백    90일 컷으로 빠지는 채널은 말씀 1개(광주극동방송)·찬양 2개(택피아노·
        mini Music)뿐이고 **48/48 탭이 20건을 그대로 유지한다.** 실효 천장은
        말씀 48→45 · 찬양 41→33으로 필요 최대 20에 여유가 25·13 남는다
주제분  사다리는 **애초에 빼지 않는다.** 정원을 못 채우면 그 채널이 그대로
        들어온다 — 실측에서 주제분이 622 → **633으로 오히려 늘었다**
```

⚠ **왜 그때 틀렸나 — 후보 수와 실효 재고를 다시 혼동했다.** 2.73 ⑦이 정확히
같은 혼동을 정정한 적이 있다("폴백 공급을 논할 때는 후보 수가 아니라 채널 수를
봐라"). 이 절은 그 교훈이 **날짜 축에도 적용된다**는 것을 못 봤다 — 채널이
빠지는지를 재지 않고 "공급이 준다"로 넘겼다.

⛔ **다만 하드 컷 금지는 주제분에 대해 여전히 유효하다.** 2.86 ①에서 실측으로
다시 확인했다 — 주제분에 90일 하드 컷을 걸면 건수는 48/48로 멀쩡한데
**주제분이 0인 탭이 1개 → 6개**가 되고 "이 마음에 맞춰 고른 영상" 절이 통째로
사라진다. **하드 컷은 이 절의 경고대로 넣지 말 것.** 뒤집힌 것은
"날짜를 축으로 쓰지 말라"이지 "잘라내도 된다"가 아니다.

```
폴백에 하드 컷    ✅ 허용 (2.84) — 재고가 넉넉해 48/48이 유지된다
주제분에 하드 컷   ⛔ 금지 (2.86 ①) — 감정 근거가 없는 화면이 6개 생긴다
주제분에 사다리    ✅ 적용 (2.86) — 밀되 빼지 않는다
```

⚠ **"949일 된 영상을 감수한다"는 판단 자체는 살아 있다.** 사다리 뒤에도 1년 넘은
영상이 보이는 탭이 14개 남고(전부 [찬양]), 그것을 규칙으로 없애지 않는다 —
재고의 바닥이기 때문이다(2.86 ⑦). 바뀐 것은 **"신선한 것이 있는데도 옛것이
앞에 나오는" 경우를 그대로 두지 않는다**는 점이다.

---

### 2.82 ★ 배치 실전 확인 + unknown 격리 (2026-08-28)

**① 수동 배치 결과 — `run 33133717750` (sha `ae52d97`) · 90 units · success**

2.80(안 E)·2.81(워십·섞기·폴백정렬)이 실전에서 확인됐다. **로컬 예측과 거의 일치했다.**

```
                 08-27 직전   ★ 08-28   (로컬 예측)
unknown_kept        79          25        26
unknown_ratio     0.045       0.014     0.015
탭 최대              23          21        21
탭 평균           20.71       20.21     20.19
중복 화면         22/24       11/24     11/24
중복 총 노출        61회        16회      15회
최대 확산         18화면      10화면     9화면
tabs_over_cap      18건        10건
tab_over_cap 경보   0건         0건
쿼터                            90 units (일일 한도의 0.9% · 여유 9,405)
```

**짚어주신 4시리즈**: 존 파이퍼 7/7 · 믿어야 산다 6/6 · 하바드 4/4가 sermon으로
바뀌었다. **워십 4건**(한소망비전워십 2 · 만나워십 2)도 sermon이 됐다 —
근거가 `channel`이다(제목에서 worship이 안 걸리고 `C채널방송:설교`가 답했다).

⚠ **젠센 프랭클린 2건이 unknown으로 남는다** — **17:24(1044s)·18:21(1101s)로
1200s 미만**이라 여전히 사각지대다. 임계를 20분으로 잡은 결과이고 예상된 잔여다.
⛔ 이 2건을 근거로 임계를 900s로 내리지 말 것(2.80 ① 참조).

**② ★ [9] unknown을 양쪽 탭에서 제외 — 적용했다 (사용자 결정)**

```
              탭 최소  최대   평균     주제분   폴백    중복
현행            20     21   20.21    625    345   11/24 · 16회
★ 적용          20     20   20.00    619    341    0/24 ·  0회
```

**탭이 정확히 20건이 되고, 양쪽 탭 중복 노출이 완전히 사라진다.**

★★ **이 안의 값어치가 뒤바뀐 경위를 기록해 둔다 — 판단이 틀렸던 게 아니라
전제가 달라졌다.**

```
2026-08-26  unknown 79건(4.5%)  →  빼면 주제분 −39.  "잃는 게 크다"고 판정
2026-08-28  unknown 25건(1.4%)  →  빼면 주제분 −6.   거의 없다
```

**안 E(2.80)가 사각지대를 10~30분에서 10~20분으로 좁혀 unknown을 1/3로 줄인 것**이
이 안의 비용을 없앴다. 얻는 것은 그대로였다. **한 결정이 다른 결정의 비용을
바꾼 사례**다 — 기각했던 안을 전제가 바뀔 때 다시 재는 습관이 값을 냈다.

2.72에서 "20으로 정확히 맞추는 길은 A안(탭별 목록 분리)뿐"이라 적었는데,
**그 길을 거치지 않고 같은 결과에 도달했다.** 앱 계약도 안 바꿨다.

고친 곳은 넷이다 — 배치 셋(`visible_count` · `select_fallback_videos` 풀 ·
`select_tab_layers` 탭 풀)과 앱 하나(`visibleVideos`).
⚠ **배치와 앱이 같은 기준이어야 한다.** 어긋나면 배치가 세는 수와 사용자가 보는
수가 달라진다. 양쪽 주석에 서로를 가리키는 표시를 넣었다.

⚠⚠ **unknown이 다시 늘면 이 안의 손해가 그만큼 커진다.** 진단의 `unknown_ratio`가
**선행 지표**다(2.72 ⑤). ⛔ 그때 되돌리기 전에 **왜 unknown이 늘었는지부터**
볼 것 — 판별을 고치는 쪽이 먼저다. 되돌리면 중복과 탭 초과가 함께 돌아온다.

⚠ 배포된 캐시·번들 시드에는 unknown이 남아 있다(시드 13건). 이제 어느 탭에도
안 보인다 — 데이터가 아니라 **표시 규칙**이 바뀐 것이라 캐시를 비울 필요는 없다.

**③ ⛔ [10] 길이 하한 600 하향 — 기각으로 닫는다**

```
           unknown        탭 최대   중복
600s(현행)  25 (1.4%)      21     11/24 · 16회
300s        45 (2.5%)      21     16/24 · 23회
180s        68 (3.8%)      22     21/24 · 39회
```

★ **핵심 — 하향은 `worship → sermon`이 아니라 `worship → unknown`으로만 바꾼다.**
"찬양 확정"이 "판정 못 함"이 될 뿐이다.

300s에서 풀리는 20건의 정체를 전수로 봤다.

```
찬송가 해설·곡명 나열·십계명 시리즈 …  18건  ← 진짜 찬양
말씀 계열                            2건  (선교사들의 이야기 · 하나님은 우리의 피난처 - 조성환 목사)
```

**진짜 찬양 18건을 판정 불능으로 만들어 설교 2건을 구제하는 거래**다. 게다가
그 2건 중 하나는 **안 A′(말미 `- OOO 목사`)가 오탐 0으로 잡는다**(2.73 ④).

②를 적용한 지금은 더 나쁘다 — 늘어난 unknown이 **전부 화면에서 사라지므로
순수 손실**이 된다.

```
[9] + 600s   화면에서 빠지는 unknown 25건
[9] + 300s   45건   ← 20건이 더 사라진다. 그 대부분이 진짜 찬양이다
[9] + 180s   68건
```

⛔⛔ **다음 세션 경고 — "설교가 찬양으로 잘못 분류된다"를 근거로 하한을 다시
낮추려 하지 말 것.** 그 문제는 **안 A′로 푸는 것**이다. 하한은 형식을 가르는
축이 아니라 "모르겠다"의 경계이고, 낮추면 아는 것까지 모르는 것으로 만든다.

**④ [3] 폴백 정렬 잔여 결함 수정**

2.81 ③에서 폴백을 최신순으로 바꿨는데 **48개 중 2개 탭(anxiety.tension·
anger.rage [말씀])에 역전이 1건씩** 남아 있었다. `select_fallback_videos`가
**호출마다** 정렬하는데 두 패스의 결과가 하나의 배열에 이어 붙으면서 이어붙인
지점에서 끊긴 것이다. 두 건 다 마지막 항목이 반대 패스에서 담긴 것이었다.

`select_tab_layers` 끝에서 합친 배열을 한 번 더 정렬해 해소했다 — **48/48 단조.**

**⑤ 회귀 — `spread_test [13]` 5종**

```
★ unknown이 어느 층에도 담기지 않는다
★ 말씀 + 찬양 = 전체 (두 탭이 겹치지 않는다)
고장 주입: unknown을 양쪽에 세면 합계가 전체를 넘는다
★ 폴백이 탭마다 단조 최신순이다 (두 패스를 합친 뒤에도)
합친 뒤 한 번 더 정렬한다 — 호출별 정렬만으로는 끊긴다
```

앱 쪽 3종도 뒤집었다(`unknown은 어느 토글에도 노출되지 않는다` ·
`두 탭은 겹치는 영상을 갖지 않는다` · `토글 건수에서 unknown을 빼고 센다`).

**⑥ ⏸ 별건 — 안 A′는 아직 미적용이다**

말미 `- OOO 목사` 구조 신호. 5일치 합집합에서 **오탐 0 · 정탐 3/3**이었다(2.73 ④).
잡히는 것은 사랑의교회 설교 3건이고, 그중 하나(`하나님은 우리의 피난처`, 9:59)는
**길이 하한 오분류를 교정**한다 — ③이 기각된 지금 그 2건을 푸는 유일한 안이다.

⚠ 좁다(3건). 넣을지는 판단이 필요하다.

**⑦ 앱 배포 — 섞기가 실려 있다**

`deploy-app.yml` 08-28 01:39Z push/success (`ae52d97`). 배포 번들
`assets/index-QAPwVbxI.js`에 mulberry32 상수(1831565813)·나눗수(4294967296)·
`Math.imul`이 들어 있다. 코드가 실린 것은 확인했고, 화면 동작은 브라우저로만
볼 수 있어 여기서는 확인하지 못했다.

---

### 2.83 ★ 안 A′ 적용 + 입력창 결함 실체 + 폴백 겹침 조사 (2026-08-28)

**코퍼스**: 과거 실행 아티팩트 6개(build-report 10~15 · 08-24~28)를 videoId로 합집합.
**1,923건 · 22채널.** API 0 (2.76 절차). 이전 세션의 2,055건보다 작다 — 그 사이
아티팩트가 더 만료됐다. ⚠ **넓은 코퍼스가 필요한 판단은 미루지 말 것**(2.76)이
또 확인됐다.

---

**① ★ 안 A′ 적용 — 설교자 크레딧을 구조로 읽는다 (사용자 결정)**

`themes.yaml speaker_credit_signal: true` · `tagging.has_speaker_credit()`.
제목을 구간 구분자로 나눴을 때 한 조각이 **통째로** `OOO(담임/후임/협동/부)?목사`이면
sermon. 각 구간의 말미 `- OOO 목사` · `: OOO 목사`도 한 조각으로 센다.
구분자는 실측에서 쓰이는 것을 모았다 — `|`(985건) `/`(220) `ㅣ`(93) `│`(29) `┃`(21) `｜`(6).

★ **판정이 바뀌는 것은 정확히 2건이고 둘 다 사용자가 지목한 것이다.**

```
3:00  염려 대신 감사를 | 더하는교회 | 최민우 목사 | 더 기프트   worship/duration → sermon/speaker
9:59  [사랑의교회] 하나님은 우리의 피난처 - 조성환 목사        worship/duration → sermon/speaker
```

**전수 대조 (신호 끔 → 켬)**

```
                     끔        ★ 켬
media_type   sermon  1223      1225
             worship  673       671
             unknown   27        27     ← 이 신호는 unknown을 만들지 않는다
근거         duration  181       155    ← 26건이 길이 대신 구조로 옮겨갔다
             channel   411       409
             speaker     –        28
48개 탭      최소 20 · 최대 20 · 평균 20.00 · 48/48   (양쪽 동일 · 달라진 탭 0개)
층           주제분 624→627 · 폴백 336→333 (총 960 불변)
```

★ **판정이 바뀐 것은 2건이지만 근거가 바뀐 것은 28건이다.** 길이 규칙에 기대던
26건이 구조 신호로 옮겨갔다 — 임계값을 건드릴 때 함께 흔들리던 것이 그만큼 줄었다.

**화면에서 무엇이 달라지나 — 전수로 확인했다**

```
염려 대신 감사를   주제 태깅됨(peace · gratitude) → **6개 화면의 주제분에 실려 있다**
                anxiety.restless · anxiety.worry · anxiety.tension ·
                anger.rage · joy.proud · joy.grateful
                → 그 6화면에서 3분짜리 설교가 [찬양]에 있다가 [말씀]으로 옮겨간다
피난처            미태깅 → 오늘 화면에는 0개. 폴백 후보로만 존재한다
                → 오늘 눈에 보이는 변화는 없고 판정만 바로잡힌다
```

**⚠ 순서 — 요구대로 길이 규칙 앞이다. 정확히는 2.5순위(장절 뒤 · 채널 앞)다.**

세 자리(장절 앞 · 장절 뒤 · 채널 뒤) 전부 **결과가 같았다.** 위치는 실측이 아니라
원칙으로 정했다 — 채널 단계는 themes.yaml이 "제목이 아무 말도 하지 않을 때의
기본값"이라고 적어 둔 자리이고, 설교자 크레딧은 **제목이 말하고 있는 것**이다.
뒤에 두면 worship 채널이 올린 설교 클립을 못 잡고, 그것은 2026-08-19에
"제목이 채널보다 앞"으로 뒤집은 것과 같은 종류의 오류가 된다.

**오탐 여유 — 넓다**

```
크레딧이 걸리는 제목        264건
그중 현행 worship            2건  ← 이 2건이 전부다. 나머지 262건은 이미 sermon
→ 규칙을 1순위 앞까지 올려도 뒤집히는 것이 2건뿐이다
```

⚠ **'목사'라는 낱말로 쓰면 안 된다** — 안 A는 정확도 40%였다(2.73 ④).
간증 프로그램의 **출연자가 목사**이기 때문이다. 실제로
"위암 재발… │장성훈 목사│부르심의 소명 더 콜링 180회"는 이 신호에도 걸리지만
**판정을 바꾸지 않는다** — 1440s라 길이 규칙이 이미 sermon으로 확정한 뒤다.
⛔ 그래서 **1순위 앞으로 올리지 말 것.** 지금 안전한 이유가 순서에 있다.

**회귀** — `tagging_test` MEDIA_CASES에 9줄. 정탐 5(콜론·직함 변형 포함) ·
오탐 방어 4("김형민 목사가 전하는…"처럼 구간이 아닌 것 · 제목 어휘가 이기는 것 ·
장절이 이기는 것). **고장 주입 3종**: 신호를 끄면 두 건이 `worship/duration`으로
되돌아가고, 크레딧이 길이 규칙보다 앞에서 판정하는지도 검사한다.

---

**② ★ 입력창 비우기가 안 되던 실체 — 배포가 아니라 세 번째 경로였다**

**결론부터: 배포는 정상이었다.** 2026-08-25 구현(`fbe7dc1`)의 앱 배포가
`run 32833935241` success이고 그 뒤로도 다섯 번 더 배포됐다.
**배포된 번들을 직접 받아 확인했다** — `assets/index-B81sHQe0.js`:

```
if($.kind===$e.CATEGORY){const ne=U.categories.find(...);u(ne),t("select"),i("input");return}
                                                        setSelectedCategory · setMode · setPhase
```

★ **setText("")가 없다.** 코드는 실려 있었고, 그 코드에 이 경로가 없었다.

**무엇이 세 번째 경로인가**

2.60이 고친 것은 두 경로(`reset` · `resetToPicker`)다. 그런데 분류에는
**결과 화면을 거치지 않는 세 번째 출구**가 있다 — `RESULT.CATEGORY`다.
대분류까지만 맞으면 세분류를 되묻느라 **곧장 고르는 화면으로** 간다.

```
"불안해" 입력 → classify가 대분류(anxiety)만 확정 → 세분류 고르는 화면
  → '다시 고르기' → 1단계 → '직접 적기' → **"불안해"가 그대로 남아 있다**
```

⚠ **흔한 입력이다.** 표본에서 "불안해"가 바로 이 경로였다. 사용자가 "분류 실패"라고
부른 화면이 `no_match`가 아니라 이쪽이었다 — `no_match` 경로는 처음부터 정상이었다.

**왜 회귀가 못 잡았나 — 이 저장소가 이미 적어 둔 함정이다**

회귀 3종이 전부 **소스 문자열**을 봤다. `setText("")`가 한 곳인지,
`onBack={resetToPicker}`인지, 복귀 함수가 인자를 받지 않는지. **전부 통과했다.**

```
검사가 볼 수 있던 것   "어떻게 적혀 있는가"
실제 결함             "어디를 안 지나는가"
```

★ `test/helpers.js`가 "거짓 통과가 더 위험하다"고 세 사례를 적어 뒀는데
**네 번째 사례**다. 그 셋은 주석이 검사를 오염시킨 것이었고 이번은 검사 대상이
아예 어긋난 것이다 — **이동을 검사하려면 이동이 값이어야 한다.**

**고친 방식 — 상태 기계를 값으로 꺼냈다** (`app/src/lib/flow.js`)

```
App.jsx  useState 5개(mode·text·phase·result·selectedCategory) → useReducer 하나
         낱개 세터를 자식에게 넘기던 것 → 뜻이 있는 콜백
           onType · onSwitchToPicker · onPickCategory · onStepBack
flow.js  FLOW.ANSWER가 outcome.kind를 보고 CATEGORY면 enterInput으로 보낸다
         enterInput이 **입력창을 비우는 유일한 자리**다
```

★ **불변식**: *입력창의 글자는 사용자가 직접 친 것일 때만 남는다.*
앱이 한 번 응답하면(ANSWER) 그 뒤 입력창이 다시 보이는 모든 상태에서 비어 있다.
⚠ 반대로 **사용자가 스스로** 고르는 화면을 구경하다 돌아오는 것(BACK)은 응답이
아니므로 지우지 않는다 — 아직 사용자의 문장이다. 회귀가 이쪽도 고정한다.

**회귀 — 그래프를 실제로 걷는다** (`behavior.test.js`, 소스 문자열 검사를 대체)

```
★ 앱이 응답한 뒤에는 입력창이 보이는 어느 상태에서도 글자가 남지 않는다
   행동 12종으로 도달 가능한 상태를 넓이 우선으로 전부 만들고 불변식을 검사한다.
   ⚠ 헛도는 것을 막는 검사 2개를 함께 둔다 (상태 20개 초과 · 응답 뒤 입력 상태 존재)
★ 대분류까지만 맞았을 때 — 되묻는 화면을 거쳐 돌아와도 비어 있다   ← 고친 그 경로
고장 주입: 되묻기가 비우지 않으면 불변식이 실제로 깨진다
결과·위기·빈 입력·분류 실패 — 네 복귀 경로가 모두 비운다
사용자가 직접 옮겨 다닌 것만으로는 자기 문장을 잃지 않는다
App이 상태 기계를 우회하지 않는다 — setText/setMode/setPhase 등이 없다
```

**검사가 실제로 잡는지 확인했다** — flow.js의 CATEGORY 분기를 2026-08-28 이전
동작으로 되돌리자 그래프 검사와 경로 검사가 **둘 다 실패**했다. 되돌리니 통과.

⚠ **브라우저로는 확인하지 못했다.** Chrome 확장이 연결되어 있지 않다.
대신 셋으로 갈음했다 — 배포 번들 원문 확인(위) · 상태 기계 그래프 검사 ·
서버 렌더 스모크(App이 렌더되고 입력창이 빈 값으로 나온다). 앱 테스트 188 통과.

---

**③ ⚠⚠ 폴백 겹침 조사 — 24개 화면이 사실상 한 목록을 쓴다 (보고만)**

```
             화면당 건수        24화면이 쓴 고유 영상   후보    평균 겹침률   완전히 같은 쌍
[말씀] 폴백   0~20 (평균 8.8)         20건          906건     100.0%     19쌍 / 231쌍
[찬양] 폴백   0~19 (평균 5.1)         19건          317건     100.0%      3쌍 /  45쌍
```

★★ **겹침률 100%의 뜻은 "비슷하다"가 아니라 "포함된다"이다.** 231쌍 전부에서
**짧은 목록이 긴 목록에 통째로 들어 있다.** 24개 화면의 폴백은 하나의 순서
매겨진 목록을 **앞에서부터 자른 것**이고, 자르는 길이만 화면마다 다르다.
(자카드로는 말씀 59.4% · 찬양 56.4% — 길이 차이가 만든 값이지 구성 차이가 아니다.)

**실제 목록 — 세 화면 나란히 (같은 날 [말씀] 탭)**

```
#   anger.irritation (주제 0 + 폴백 20)      joy.grateful (주제 6 + 폴백 14)   calm.stable (주제 15 + 폴백 5)
 1  갓피플TV  하늘에 속한 사람                   —                              —
 2  분당우리  바람을 따라가는 삶에서              1 (같은 것)                     —
 3  사랑의교회 성경읽기 겔 11~13장               —                              —
 4  C채널    하나님을 더욱 갈망하는…             2 (같은 것)                     —
 5  CGN     믿어야 산다 EP.9                   3 (같은 것)                     —
 6  CBS설교  끊을 수 없습니다                    4 (같은 것)                     —
 7  베이직    신앙을 해독하다                    5 (같은 것)                    1 (같은 것)
 8  CGN통독  236일차 예레미야 27~29장           6 (같은 것)                     —
 9  낮은담    김관성 주일예배                    7 (같은 것)                    2 (같은 것)
10  CBSJOY  모두의 거실 24회                   8 (같은 것)                     —
11  오륜     삶으로 예수를 증명하라              9 (같은 것)                    3 (같은 것)
12  꿈의교회  끌려가겠는가? 끌고 가겠는가?        10 (같은 것)                    4 (같은 것)
13  우리들    마음을 다한 사랑                  11 (같은 것)                    5 (같은 것)
14  꿈의교회  성찬이 있는 예배                    —                              —
15  오륜     하나님과의 친밀함을 사모하라          —                              —
16  극동방송  성경의 무림고수를 찾아서 3편        12 (같은 것)                     —
17  CTS     성령이 임하시면…                   13 (같은 것)                     —
18  낮은담    두 사람 사이에서                    —                              —
19  우리들    앞서가시는 하나님                    —                              —
20  광주극동  십계명 제8계명                     14 (같은 것)                     —
```

`calm.stable`의 5건이 **전부** `anger.irritation`의 20건 안에 있다.
`joy.grateful`의 14건도 마찬가지다. 사용자가 짚은 그림이 맞다 —
**감정을 바꿔도 아래쪽은 같은 목록이고, 위쪽 주제분이 길어진 만큼 짧아질 뿐이다.**

**원인 — 최신순 정렬이 아니다. 폴백에는 회전이 걸린 적이 없다**

```
select_theme_videos    →  rotate_for_subcategory(picked, position)   ★ 화면마다 다르다
select_fallback_videos →  position을 **인자로 받지도 않는다**
                          day_of_year만 받는다 → 하루 단위로만 갈린다
```

★ 2.81 ③의 최신순 정렬은 **순서만** 바꿨고 구성에는 영향이 없다(정렬은 집합을
바꾸지 않는다). 겹침은 그 변경 이전부터 있던 구조적 성질이다.
⚠ 다만 2.82 ②(unknown 제외)가 겹침을 조금 **키웠다** — 전에는 unknown이 말씀
패스에서 먼저 빠지면서 화면마다 찬양 폴백이 미세하게 갈렸는데 그 통로가 사라졌다.

**두 장치가 실제로 하는 일 — 물으신 것에 답한다**

```
채널당 상한 3   한 목록 **안에서** 채널을 흩는다. 목록끼리를 가르지는 않는다.
                실효 천장 = 3 × 16채널 = 48건. 그래서 후보 906건 중 20건만 쓰인다(2.2%)
                (찬양은 317건 중 19건 · 6.0%)
day_of_year     **날짜별로만** 갈린다. 같은 날 24개 화면은 전부 같은 순서를 받는다.
                실측: 하루 뒤 20건 중 1건 교체 · 이틀 뒤 2건 · 일주일 뒤 4건
```

**⏸ 회전을 폴백에도 걸면 어떻게 되나 — 시뮬레이션만 했다. 구현하지 않았다**

```
                           고유 영상          평균 겹침률        완전히 같은 쌍
현행                    말씀 20 · 찬양 19    100.0% · 100.0%      19 · 3
★ 폴백에도 회전         말씀 38 · 찬양 28     79.7% ·  83.4%       0 · 0
```

`rotate_for_subcategory(pool, position)`을 라운드로빈 **앞**에 한 줄 넣는 것이 전부다.
완전히 같은 쌍이 0이 되고 고유 영상이 두 배가 된다.
⚠ **천장은 48건이다**(상한 3 × 16채널). 후보가 906건이어도 회전만으로는 38건까지다 —
더 벌리려면 상한이나 채널 수를 건드려야 하고, 그건 2.66·2.73 ⑦이 다룬 영역이다.
⛔ **판단은 사용자 몫이다.** 폴백 헤더가 "요즘 올라온 것들"이라고 말하므로,
회전이 그 문장을 얼마나 흔드는지(뒤쪽의 오래된 것이 앞으로 올라온다) 함께 봐야 한다.

---

**④ ⏸ 설교 내용어 유형 2건 — 안 A′로는 안 잡힌다. 다른 구조 신호가 하나 있다 (조사만)**

```
2084s  빛의자녀교회 김형민 목사(빛의자녀 472회) - 무릎 꿇고 찬양하세요(샤인영성)   CBS설교(sermon)
2035s  수원은혜교회 황유석 목사 | 인생의 밤에 … 하나님을 찬양합니다 [C채널] 비전메시지  C채널:설교(sermon)
```

**A′로 안 잡힌다 — 두 가지 이유 모두로.**

```
1. 구간이 "OOO 목사"가 아니다.  "김형민 목사(빛의자녀 472회)" · "수원은혜교회 황유석 목사"
2. ★ 잡혔더라도 소용없다. 이 둘은 **1순위(제목 어휘)에서 확정**된다.
   A′는 2.5순위라 1순위를 못 뒤집는다 — 설계상 닿지 않는 자리다.
```

**규모**: worship 어휘가 `찬양` 하나뿐이고 sermon 어휘가 없는 제목 **106건**,
전부 worship 판정. 채널이 sermon/devotion인 것이 83건인데 그 대부분이
오륜교회(49) · 우리들교회(23)의 **진짜 찬양 콘티**다 — 채널은 근거가 못 된다.

**후보 (가) '찬양' + 활용 어미 — ⛔ 기각. 정확도 20%**

"찬양하세요"·"찬양합니다"처럼 어미가 붙으면 설교 문장이라는 가설.

```
매치 10건 → 목표 2건 · 오탐 8건
오탐이 전부 **곡명 자체가 활용형**이다:
  "찬양하라 복되신 구세주" · "난 주만 찬양해" · "나는 찬양하리라" ·
  "감사와 찬양드리며" · "찬양하세" · "영원하신 그 이름 찬양하며"
```

**후보 (나) 길이 — ⛔ 기각.** `찬양` 단독 매치 중 30분 이상 12건 가운데 10건이
진짜 찬양이다(4시간 재즈 플레이리스트 3건 · 광주극동방송 찬송가 콘서트·플레이리스트 5건 ·
옹기장이 2시간 연속듣기 2건). 길이는 이쪽을 못 가른다.

**★ 후보 (다) 'OOO교회 OOO 목사' 구간 — 유일하게 깨끗하다**

```
매치 211건 · 현행 sermon 209 · worship 2
현행 worship인 2건이 **정확히 이 두 건이다.** 오탐 0 · 정탐 2/2
```

⚠⚠ **다만 1순위 앞에 놓아야 효과가 있다.** ①에서 "⛔ 올리지 말 것"이라고 적은
바로 그 자리다. (다)는 A′보다 좁은 구조(교회명 + 인명 + 목사)라 근거가 더 강하지만,
**"제목이 형식을 직접 말하는" 증거를 크레딧이 덮는다**는 위험의 성격은 같다.

```
지금 안전한 이유   찬양 콘티 제목에 "OOO교회 OOO 목사"가 붙는 사례가 코퍼스에 0건
깨지는 날          "오륜교회 김은호 목사 | 주일 찬양 콘티" 같은 제목이 나오면 뒤집힌다
```

⏸ **판단이 필요하다.** 2건을 위해 1순위 앞에 규칙을 하나 더 놓을 것인가.
넣는다면 (다)만 넣고 A′는 2.5순위에 그대로 두는 것이 안전하다 — 둘은 근거의
강도가 다르다.

---

### 2.84 ★ 폴백 90일 컷 + 화면별 무작위 (2026-08-28)

**코퍼스**: 08-28 배치가 실제로 본 단일일 풀 1,770건이 주(主)이고, 합집합 6일치
1,923건으로 교차 확인했다. 배포된 `data/videos.json`(954건)도 함께 읽었다. API 0.

**① ⚠ 조사에서 전제 하나가 뒤집혔다 — 오래된 영상은 폴백에 없다**

배포된 `videos.json`을 층별로 갈랐다.

```
                  건수   중앙값   75%    90%   최고령   ★ 90일 초과
theme  worship    349    44일   287일  592일   978일   142건 (41%)
theme  sermon     264    11일    28일   62일   288일     9건 ( 3%)
fallback sermon   210     1일     4일    4일   247일     7건 ( 3%)
fallback worship  115     2일     5일   67일   272일    10건 ( 9%)
```

★ **폴백은 이미 거의 다 최신이었다.** 2.81 ⑤에서 짚은 949일짜리는 **주제분**에
있다(`anger.irritation [찬양]` 옹기장이 CCM PIANO · 950일).
**폴백에만 컷을 걸면 954건 중 17건만 사라진다.**

**② ★ 그래서 컷과 무작위는 짝이다 (사용자 결정)**

```
컷만        954건 중 17건 감소. 겹침은 100% 그대로 — 사실상 무의미
무작위만    채널의 100건 수집 창을 깊이 긁어 **1020일짜리**가 올라온다
            (옹기장이 1030일 · mini Music 1028일 · 택피아노 867일 백로그)
★ 둘 다     겹침이 풀리면서(말씀 100%→2.6%) 최고령이 90일에 묶인다
```

**두 부분이 서로의 부작용을 막는다.** 하나만 넣으면 둘 다 값이 없다.

**③ ★ (다)안 채택 — 배치가 화면마다 다르게 뽑는다. 앱은 손대지 않았다**

⛔ **앱은 선택지가 아니었다.** `videos.json`은 화면마다 **보여줄 40건(탭당 20)을
그대로** 싣는다. 앱이 가진 것이 그 40건뿐이라 **순서만 바꿀 수 있고 구성은 못
바꾼다.** 겹침은 구성 문제다.

⚠ 주제분 섞기(2.81 ②)가 앱이 맞았던 이유와 정확히 반대다 — 거기는 구성이 이미
옳고 **순서만** 매번 달라지면 됐다.

```
                    겹침 해결   페이로드        잃는 것
(가) 앱에서 순서만      ⛔        363KB 그대로   — (효과가 없다)
(나) 공용 풀 + 앱 추출   ✅        363 → 207KB    배치가 어느 화면에 무엇이 나가는지 모른다
★(다) 배치가 화면마다    ✅        363KB 그대로   스키마·앱 계약 변화 0
```

(나)의 페이로드 절감은 컸다 — 폴백 335항목이 **고유 39건**뿐이라(같은 영상이
평균 8.6개 화면에 실렸다) 중복을 걷으면 43% 줄어든다. 그래도 (다)를 고른 것은
**배치가 화면 구성을 아는 것**이 `spread_test`의 채널 분산·중복 진단이 서 있는
전제이기 때문이다(build_subcategories 독스트링).

**④ 고친 곳 — 두 파일 세 자리**

```
lib/selection.py   FALLBACK_MAX_AGE_DAYS = 90 · drop_stale()      ← 컷
                   _fallback_seed() · _shuffled_for_fallback()     ← 무작위
                   select_fallback_videos(..., position=)          ← 화면 축을 받는다
build_videos.py    build_subcategories(..., now) → drop_stale 호출  ← 거르는 자리는 한 곳
```

★ **`_channel_order(day_of_year)`를 폴백에서 뺐다.** 그것은 **하루 단위** 회전이라
같은 날 24개 화면에 같은 순서를 줬고, **그것이 겹침 100%의 원인이었다.**
날짜별 변화는 시드에 `day_of_year`가 들어가 그대로 유지된다.

⚠ **무작위지만 결정적이다.** sha256으로 `(day_of_year, position, target)`에서
시드를 만든다. ⛔ 내장 `hash()`는 PYTHONHASHSEED로 프로세스마다 달라져 쓰면 안
되고(`rotate_for_subcategory` 주석과 같은 이유), 시드 없는 `random`도 안 된다 —
**하루 두 번 도는 배치가 같은 날 다른 목록을 내면** 사용자 화면이 이유 없이 바뀐다.

**⑤ 적용 후 전수 확인 — 실제 lib 코드로 돌렸다**

```
                        단일일        합집합       (예측)
48개 탭      최소 20 · 최대 20 · 평균 20.00 · 48/48   (양쪽 동일)
폴백 후보    909건        1020건      (오래된 것 제외 203 / 211건)
층           주제분 622 · 폴백 338     주제분 627 · 폴백 333
[말씀] 고유   170건        181건
[말씀] 겹침   3.1%         3.1%        2.7%    ← 100%에서 내려왔다
[찬양] 고유    63건         58건
[찬양] 겹침   44.5%        36.1%       34%
나이 중앙값   말씀 13일 · 찬양 16일     말씀 17일 · 찬양 16일
나이 90분위   말씀 42일 · 찬양 64일     최고령 말씀 85일 · 찬양 90일
```

날짜를 여섯 벌(240·241·242·250·300·5) 돌린 평균은 **말씀 2.6% · 찬양 38.0%** 다.
말씀은 예측과 정확히 맞았고 찬양은 예측(34%)보다 조금 높다.

★ **완전히 같은 쌍 0 · 포함 쌍 0**(말씀). 찬양의 포함 쌍은 전부 **5건 미만의 작은
목록**이 만든 것이고, 5건 이상 목록끼리는 어느 날에도 포함 쌍이 0이다.

**⑥ ⚠⚠ 찬양 38%는 알고리즘의 바닥이다 — 채널 승인 문제로 넘긴다**

90일 안에 재고가 3건 이하인 찬양 채널이 6개다.

```
CGN 1 · CBS설교 1 · 광주극동방송 1 · C채널방송 1 · CBSJOY 2 · 극동방송 3
→ 이 9자리는 **어느 화면에나 똑같이** 들어간다 (천장 33의 27%)
→ 19건 중 9건이 공통이면 47%. 측정값 38%가 그 근처다
```

★ **말씀은 15개 채널이 전부 재고 ≥3이라 2.6%까지 내려간다. 차이를 만드는 것은
규칙이 아니라 재고다.** ⛔ 이 값을 알고리즘으로 더 내리려 하지 말 것 —
**3절 채널 발굴(찬양 계열)** 이 그 자리다.

⚠ **여유는 5채널이다.** 90일 기준 찬양 천장 33, 필요 최대 20. 기여 큰 채널 5개를
더 잃으면 20을 못 채운다(말씀은 9개). 찬양 채널이 빠지면 여기가 먼저 운다.

**⑦ 왜 90일인가 — 60·120과 재서 정했다**

```
      말씀                          찬양
    천장  채널여유  겹침   최고령  |  천장  채널여유  겹침    최고령
없음   48    10   3.2%   296일 |   41     8   28.6%  1020일
120일  45     9   2.6%    97일 |   35     6   36.8%   120일
★90일  45     9   2.7%    86일 |   33     5   34.4%    90일
60일   45     9   2.8%    60일 |   30     4   44.3%    59일
```

⛔ **60일로 내리지 말 것.** 찬양 채널 여유가 4개로 얇아지고 겹침이 44%로 나빠지는데
얻는 것은 최고령 90→59일뿐이다. 120일은 90일보다 나은 것이 없다.
⚠ 병목은 언제나 **찬양 채널 수**다. 말씀은 30일까지 내려도 천장 45로 끄떡없다.

**⑧ 회귀 — `spread_test [10-b]` 13종**

```
★ 경계는 90일이다 — 딱 90일은 남고 하루 더는 빠진다
  1020일짜리(옹기장이·mini Music 백로그 유형)가 빠진다
  날짜를 못 읽으면 거르지 않는다 — 있는 것을 잃지 않는다
  컷으로 뺀 자리를 되메우지 않는다 (drop_promotional과 같은 규칙)
★ 화면마다 다른 목록이 나온다 — 구성이 완전히 같은 쌍이 없다 (0/276)
★ 포함 관계도 없다 — 짧은 목록이 긴 목록의 앞부분이던 것이 결함이었다 (0/276)
  24개 화면이 한 화면의 천장(24건)보다 많은 영상에 닿는다
  무작위여도 채널당 상한 3은 그대로다
★ 같은 (날짜·화면·탭)이면 언제나 같은 목록이다 (배치가 하루 두 번 돈다)
  날짜가 바뀌면 목록도 바뀐다 · 탭이 다르면 시드도 다르다
★ 무작위로 뽑아도 화면 순서는 최신순이다 (2.81 ③이 약속한 것)
고장 주입: 시드에서 position을 빼면 24개 화면이 같은 목록이 된다
```

**⑨ 폴백 헤더 문구는 그대로 둔다 (사용자 결정)**

노출 나이 중앙값이 1~2일에서 13~17일로 올라가지만 "요즘"으로 표현해도 된다는 판단.

★ **이 문장은 전보다 참이 됐다** — 지금까지는 상한이 아예 없어 247일·272일짜리가
"요즘"이라는 이름으로 나가고 있었다. 컷이 그 주장에 처음으로 강제력을 준다.

⛔ **기간을 명시하는 문구로 바꾸지 말 것**("최근 3개월 안에 올라온"). 이 문장의
무게 중심은 "요즘"이 아니라 **"딱 맞는 건 아니지만"** 이고, 정직성 주장은 주제
적합성이지 신선도가 아니다(2026-08-19 개정 근거). `VideoList.jsx`에 적어 뒀다.

---

### 2.85 ⏸ 주제분 날짜 컷 조사 — 건수는 안 무너지고 **근거가 무너진다** (2026-08-28)

조사만 했다. **구현하지 않았다.**

**① 48개 탭은 어느 컷에서도 20건을 유지한다 — 그래서 이 지표로 판단하면 안 된다**

```
주제분 컷    최소  최대   평균     20건    폴백 비율   주제분 0인 탭
   없음      20   20   20.00   48/48    35.2%        1
  365일      20   20   20.00   48/48    42.2%        4
  180일      20   20   20.00   48/48    44.3%        5
   90일      20   20   20.00   48/48    46.8%        6
```

★ **폴백이 빈자리를 그대로 메우기 때문이다.** 컷은 영상을 없애는 것이 아니라
**주제분(근거가 분명한 층)을 폴백(근거가 없는 층)으로 바꾼다.**
주제분 622 → 511건. 화면당 두 건이 매일 근거를 잃는 셈이다.

⚠ **주제분이 0인 탭이 1개 → 6개**가 된다. 그 탭에는 "이 마음에 맞춰 고른 영상"
절이 아예 뜨지 않는다(`VideoList`가 `theme.length > 0`일 때만 그린다).
**감정 앱에서 감정 근거가 사라진 화면**이 6개가 된다는 뜻이다.

**② 어느 주제가 무너지는가 — 전부 찬양 쪽이다**

```
주제               전체    365일   180일    90일
quiet_worship     0/247   0/146   0/99    0/68     ← 가장 크게 깎인다
love               4/28    4/25    4/24    4/21
gratitude          6/27    6/24    5/21    5/20
hope               9/18    9/12    9/12    9/8
comfort             3/5     3/0     2/0     2/0    ← 찬양이 0이 된다
renewal            12/4    12/2    12/0    12/0    ← 0
guidance            1/5     1/3     1/2     1/1
patience            7/1     7/0     7/0     7/0    ← 0
lament             11/1    11/0    11/0    11/0    ← 0
rest                3/2     3/1     3/1     3/0    ← 0
strength            0/3     0/1     0/1     0/1
daily_word        168/1   168/0   168/0  159/0     ← 말씀은 거의 안 줄어든다
```

★ **말씀 주제분은 컷의 영향을 거의 받지 않는다**(daily_word 168→159).
사용자가 이미 짚었던 얇은 주제들(self_control 0/0 · creation 1/1 · courage 1/2)은
**컷으로 더 얇아지지 않는다** — 원래부터 재고가 없어서 컷이 자를 것도 없다.
⚠ 진짜로 얇아지는 것은 **중간 크기의 찬양 주제**다(comfort 5→0 · renewal 4→0 ·
patience 1→0 · lament 1→0 · rest 2→0). 컷이 이들을 0으로 만든다.

**③ ★ 대안 — 하드 컷이 아니라 사다리다. 이쪽이 모든 축에서 낫다**

`per_channel_ladder`(상한 3→4→5)와 같은 모양이다. **신선한 것으로 먼저 채우고,
정원을 못 채울 때만 한 단계씩 넓힌다.**

```
안                   주제분  폴백   폴백%  주제분0  나이중앙  75%   90%   최고령
현행 (제한 없음)         622   338  35.2%      1     20일   88   489    978
하드 컷  90일           511   449  46.8%      6     14일   28    53     88
하드 컷 180일           535   425  44.3%      5     14일   39    86     172
하드 컷 365일           555   405  42.2%      4     15일   45   136     363
★ 사다리 90→전체        633   327  34.1%      1     17일   53   439     969
```

★★ **사다리는 주제분을 오히려 늘린다**(622 → 633). 신선한 것을 먼저 채우면 탭당
20을 더 잘 채우기 때문이다. **폴백 비율도 내려가고**(35.2% → 34.1%),
**주제분 0인 탭도 1개 그대로**다. 하드 컷이 잃는 것을 하나도 안 잃는다.

**전형적인 나이가 크게 개선된다** — 75분위 88일 → 53일.

**④ 사용자가 본 화면이 실제로 고쳐진다**

```
anger.irritation [찬양] 오래된 순
  현행         950일 [주제] 옹기장이 [CCM PIANO] 훈계로 다스려주소서 피아노ver.   ← 신고 건
               136일 [주제] ANOINTING 어노인팅 연주곡 Vol.8 …
★ 사다리        86일 [주제] mini Music 실시간 연주 찬양 …
                51일 [주제] mini Music …
```

**950일 → 86일.** 전수로도 개선된다.

```
1년 넘은 영상이 보이는 탭    현행 21/48  →  ★ 사다리 14/48   (전부 찬양 탭이다)
```

⚠ **남는 14개 탭은 재고가 정말 없는 화면이다.** frustration.stuck ·
frustration.blocked · exhaustion.listless 등으로, 매핑 주제가 quiet_worship
계열뿐이라 **넣을 신선한 찬양이 아예 없다.** 사다리가 마지막 단계에서 옛것을
집는 것은 규칙의 실패가 아니라 **재고의 바닥**이다 — ⑥과 같은 결론이다.

**⑤ ⏸ 판단이 필요한 것**

```
⛔ 하드 컷      권하지 않는다. 건수는 유지되지만 주제분 −111건 · 주제분 0인 탭 1→6.
               "감정에 맞춰 고른 영상" 절이 사라지는 화면이 6개가 된다
★ 사다리        모든 축에서 현행보다 낫다. 잃는 것이 측정되지 않았다
               ⚠ select_theme_videos를 단계별로 두 번 부르는 구조 변경이고,
                 rotate_for_subcategory와의 상호작용을 회귀로 고정해야 한다
그대로 두기     2.81 ⑤에서 "949일 된 영상을 감수한다"고 이미 확정한 자리다.
               ⛔ 그때의 근거(공급이 얇은 채널이 통째로 빠진다)는 **하드 컷에만**
                 해당한다. 사다리는 그 채널을 빼지 않는다 — 뒤로 미룰 뿐이다
```

⚠⚠ **2.81 ⑤의 ⛔ 경고와 충돌하지 않는지 확인할 것.** 그 절은 "나중에 날짜 컷을
넣지 말 것"이라고 적었고 근거는 *"공급이 얇은 채널(옹기장이·광주극동방송)이
목록에서 통째로 빠진다"* 였다. **사다리는 통째로 빼지 않는다.** 정원이 안 차면
그 채널이 그대로 들어온다 — 실측에서 주제분이 오히려 늘었다(622→633).
그래도 **결정을 뒤집는 자리이므로 사용자 확인 없이 넣지 말 것.**

---

### 2.86 ★ 주제분 신선도 사다리 — 밀되 빼지 않는다 (2026-08-28)

2.85에서 조사만 하고 남겨 둔 안이다. **사용자 결정으로 적용했다.**

**① ⛔ 하드 컷은 기각한다 — 건수가 아니라 근거가 무너진다**

```
안                  주제분  폴백   폴백%  주제분0  나이중앙  75%   90%  최고령  1년초과탭
현행(제한 없음)        622   338  35.2%      1     20일    88   489   978    21/48
하드 컷 90일          511   449  46.8%    ★6     14일    28    53    88     0/48
★ 사다리 90→전체      633   327  34.1%      1     17일    53   439   969    14/48
```

하드 컷은 **48개 탭의 건수를 무너뜨리지 않는다**(48/48 그대로). 폴백이 빈자리를
그대로 메우기 때문이다. 그래서 건수로 판단하면 안전해 보이는데, 실제로는
**주제분(근거가 분명한 층)이 폴백(근거가 없는 층)으로 바뀐다.**

⛔⛔ **주제분이 0인 탭이 1개 → 6개가 된다.** 그 탭에는 "이 마음에 맞춰 고른 영상"
절이 아예 뜨지 않는다(`VideoList`가 `theme.length > 0`일 때만 그린다).
**감정 앱에서 감정 근거가 없는 화면은 만들지 않는다** (사용자 결정).

★ **이것이 이번 판단의 방법론적 수확이다 — "48/48 유지"는 안전 신호가 아니었다.**
폴백이 무엇이든 메우므로 그 지표는 **거의 아무것도 안 무너뜨린다.**
2.85에서 컷을 전부 재고도 "무너지는 탭 0개"만 봤다면 하드 컷을 넣었을 것이다.

**② ★ 사다리는 잃는 것이 측정되지 않았다**

`per_channel_ladder`(상한 3→4→5)와 **같은 모양**이다. 좋은 조건으로 먼저 채우고
정원을 못 채울 때만 한 단계 넓힌다. 여기서 '좋은 조건'은 신선도다.

```
주제분        622 → 633건   ★ 오히려 는다
폴백 비율   35.2% → 34.1%   ★ 내려간다
주제분 0인 탭    1 → 1개     그대로
나이 중앙값   20일 → 17일 · 75분위 88일 → 53일   ★ 전형적인 나이가 개선된다
```

★ **주제분이 느는 이유** — 신선한 것을 먼저 채우면 탭당 20을 더 잘 채운다.
1단계에서 채널 상한 3에 걸려 밀린 자리를 2단계가 다른 채널로 메우기 때문이다.

**③ 사용자 신고 건이 실제로 고쳐졌다**

```
anger.irritation [찬양] 오래된 순
  전   950일 [주제] 옹기장이 [CCM PIANO] 훈계로 다스려주소서 피아노ver.   ← 신고 건
       136일 [주제] ANOINTING 어노인팅 연주곡 시리즈 Vol.8 …
  후    86일 [주제] mini Music 실시간 연주 찬양 | 테마= 오늘도 일하시는 성령님
        51일 [주제] mini Music …  ·  23일 [주제] mini Music …  ·  22일 [주제] Worship Flow
```

전수로도 **1년 넘은 영상이 보이는 탭 21/48 → 14/48.**

**④ 구현 — `_theme_by_freshness` 하나**

```
lib/selection.py   THEME_FRESH_DAYS = 90 · THEME_FRESHNESS_LADDER = (90, None)
                   _within_age() · _theme_by_freshness()
                   select_tab_layers(..., now=)          ← 키워드 필수
build_videos.py    select_tab_layers(..., now=now, ...)
```

⚠ **`now`를 키워드 필수 인자로 뒀다.** 날짜를 안 넘기면 사다리가 조용히 꺼지는
기본값을 두지 않는다. 호출부가 6곳(spread_test 5 + build_videos 1)뿐이라 감당된다.

⚠ **폴백의 90일 컷(2.84)과는 다른 축이다.** 저쪽은 후보에서 **빼는** 하드 컷이고
(재고가 넉넉해 빼도 48/48이 유지된다), 이쪽은 **뒤로 미는** 사다리다. 값이 같은
것은 같은 실측에서 나왔기 때문이지만 **상수는 따로 뒀다** — 한쪽을 조정할 때
다른 쪽이 조용히 따라 움직이면 안 된다.

**⑤ ⚠⚠ rotate_for_subcategory와의 상호작용 — 단계마다 건다**

전에는 전체 목록에 한 번 걸었다. 사다리에서는 **단계마다** 건다.

```
왜   전체에 한 번 걸면 회전이 단계 경계를 넘어간다.
     1단계 6건 + 2단계 14건일 때 회전이 신선한 6건을 뒤로 밀어버린다 —
     사다리의 목적이 그 자리에서 사라진다.
⚠   1단계 결과가 1건이면 그 한 건은 모든 화면에서 같은 자리다(회전이 성립하지
     않는다). 재고가 그것뿐이라 감수한다 — 2단계가 갈라 준다.
```

회귀가 둘 다 고정한다 — **1단계가 앞자리를 지키는 것**과 **그 상태에서도 화면마다
순서가 갈리는 것**.

**⑥ 회귀 — `spread_test [11-b]` 10종**

```
★ 신선한 것으로 정원이 차면 옛것은 하나도 안 들어온다
★ 정원을 못 채우면 옛것이 들어온다 — 통째로 빼지 않는다 (2.81 ⑤ 정정의 근거)
★ 그래도 신선한 것이 먼저다 — 앞 3건이 1단계 결과다
★ 경계는 90일이다 — 딱 90일이 앞, 하루 더는 뒤
⛔ 사다리의 마지막 단계가 '제한 없음'이다 — 아니면 기각된 하드 컷이 된다
★ 1단계 안에서도 화면마다 순서가 갈린다 (회전이 살아 있다)
  구성은 그대로다 — 회전은 순서만 바꾼다
★ 두 단계로 담겨도 1단계(신선)가 앞자리를 지킨다 — 회전이 경계를 넘지 않는다
고장 주입: 마지막 단계를 없애면(하드 컷) 20건이 3건이 된다
고장 주입: 회전을 빼면 5개 화면이 같은 순서가 된다
```

⚠ `spread_test`에 **`TEST_NOW` 고정 시각**을 뒀다. 픽스처의 `published_at`
(2026-08-18)이 10일 된 것이 되어 1단계에 들어가고, 기존 검사의 뜻이 그대로
유지된다. ⛔ `datetime.now()`를 쓰면 시간이 흘러 픽스처가 2단계로 밀리고
**같은 테스트가 다른 경로를 타게 된다.**

**⑦ ⚠⚠ 남는 14개 탭은 재고의 바닥이다 — 규칙으로 못 고친다**

```
969일  frustration.stuck      [찬양]  주제=guidance,hope
969일  frustration.blocked    [찬양]  주제=patience,hope
969일  exhaustion.listless    [찬양]  주제=strength,hope
969일  flutter.anticipation   [찬양]  주제=new_thing,hope
878일  sadness.sorrow         [찬양]  주제=comfort,lament,hope
856일  anxiety.worry          [찬양]  주제=trust,peace
763일  boredom.dull           [찬양]  주제=daily_word
… 전부 [찬양] 탭이다. [말씀]은 0개다.
```

★ **원인은 주제별 재고다. 90일 안에 찬양이 0건인 주제가 10개다.**

```
self_control · justice · forgiveness · patience · lament ·
comfort · renewal · rest · daily_word · bible_story
(반대로 90일 안에 말씀이 0건인 주제는 3개뿐 — self_control · strength · quiet_worship)
```

⛔ **단계를 더 넣어도 없는 것은 안 나온다.** 이것을 규칙으로 고치려 하지 말 것.
HANDOFF 3절 10번 '3차 채널 발굴' **2순위(찬양·연주 채널)** 가 그 자리이고,
그 절에 이 수치를 연결해 뒀다.

---

### 2.87 ★ 배치 실전 확인 — 폴백 무작위 + 주제분 사다리 첫 실행 (2026-08-28)

**`run 33143782965` (수동 · dry_run 아님) · success · 90 units**
트리거 05:06:55Z → 시작 05:06:58Z(**지연 3초**) → 종료 05:08:06Z(68초).
⚠ 수동 실행이라 이 값은 **예약 슬롯의 지연(2.78)에 대해 아무것도 말하지 않는다** —
`workflow_dispatch`와 `schedule`은 큐가 다르다.

```
                     08-28 01:43   ★ 08-28 05:07   (로컬 예측)
48개 탭          최소 20 · 최대 20 · 평균 20.00 · 48/48
주제분 / 폴백         619 / 335      633 / 327      633 / 327   ★ 정확히 일치
폴백 비율                            34.1%          34.1%
주제분 0인 탭                            1개            1개   (anger.irritation [말씀])
주제분 나이     중앙값 17일 · 75% 53일 · 90% 440일 · 최고령 969일   (17 · 53 · 439 · 969)
1년 초과 탭                          14/48          14/48   말씀 0 · 찬양 14
unknown_kept           25             25
unknown_ratio       0.014          0.014
tab_max_exposed        21     ★      20
tabs_over_cap        10건     ★       0건
total_exposed         954            960   = 24화면 × 40건
```

★ **`tab_max_exposed` 21→20 · `tabs_over_cap` 10→0.** 직전 실행은 `ae52d97` 기준이라
unknown 격리(`0331edf`) 전이었다 — **2.82 ②가 실전에서 확인된 것도 이번이 처음**이다.

**폴백 겹침 — 100%에서 내려왔다**

```
[말씀] 고유 162건 · 겹침 4.5% · 완전히 같은 쌍 0 · 포함 쌍 0 · 화면 20개
[찬양] 고유  63건 · 겹침 39.0% · 완전히 같은 쌍 0 · 포함 쌍 10(5건 이상만 0) · 화면 12개
```

⚠ **말씀 겹침이 예측(2.6%)과 어긋났다 — 4.5%.** 이번 실행의 `titles.json`으로 로컬에서
다시 돌리니 **162건·4.5%로 정확히 재현**됐다. **코드가 아니라 코퍼스 차이**다 —
2.6%는 아티팩트 합집합으로 날짜 6벌을 돌린 **평균**이었고(단일일은 3.1%),
이번 실행이 실제로 수집한 풀이 그것과 달랐다. ⚠ 예측 밴드를 평균 하나로 좁혀
말한 것이 잘못이다. 다음부터는 범위로 적을 것.

★ **가장 읽기 쉬운 지표 — 같은 영상이 몇 개 화면에 실리는가**

```
직전 (01:43)   폴백 335항목 / 고유  39건 → 평균 8.59개 화면
★ 이번 (05:07)  폴백 327항목 / 고유 225건 → 평균 1.45개 화면
주제분은 2.54개 화면으로 변화 없음 (사다리는 구성이 아니라 신선도를 바꾼다)
```

**경보·쿼터** — 36건(직전 35). 새로 뜬 것 1건: `폴백 과다: anger.rage [worship]`
(찬양 폴백 재고가 90일 컷으로 310→130건이 된 결과 · 탭 건수는 20 그대로).
critical은 `theme_empty: self_control` 하나이고 Issue #47이 이미 열려 있다 —
**새 Issue 생성 0건**(재알림 억제 정상). 쿼터 90 units · 오늘 두 번 합쳐 180(1.8%).

---

### 2.88 ★ 사용자 신고 형식 오분류 7건 — 유형과 안 (2026-08-28)

화면에서 직접 확인한 7건. **코퍼스는 7일치 합집합 1,927건**(아티팩트 7개 · API 0).

**① 어느 단계에서 무엇에 걸렸나**

```
①  땅끝에서 온 편지 (219s · CGN mixed · 주제분 hope · 5개 화면)
②  넘어져도 다시 일어나는 회복의 비밀 (256s · 광주극동 mixed · 주제분 renewal · 2개 화면)
④  성령의 바람 | CBS TV '예수 영광의 면류관' (279s · CBSJOY mixed · 폴백 · 3개 화면)
    → 셋 다 어휘·장절·크레딧 전부 없음 → 채널 mixed → **길이 ≤600 → worship 확정**
③  인생의 밤에 … 찬양합니다 (2035s · C채널:설교 · 폴백 · 9개 화면)
⑤  무릎 꿇고 찬양하세요 (2084s · CBS설교 · 폴백 · 8개 화면)
    → 1순위 어휘 worship=['찬양'] **한 낱말이 단독으로 확정**
⑥  [사랑의교회] 예수 피를 힘입어/… (1248s · mixed · 주제분 joy_praise · 3개 화면)
⑦  [사랑의교회] 두 손 들고/… (1226s · mixed · 주제분 joy_praise · 3개 화면)
    → 어휘 하나도 안 걸림 → **길이 ≥1200 → sermon 확정**
```

★ ①②④⑥⑦은 **길이 규칙이 최종 판정을 내린 것**이다(2.80 ④가 "길이는 실제로 단독
판정"이라 적은 자리). ③⑤는 '찬양' 한 낱말이 1순위에서 확정했다.

**② 유형 — 셋 + 못 고치는 것 하나**

```
A  ⑥⑦   곡명 나열 (2.73 ④ 안 C)        곡명 5개가 '/'로 이어졌는데 어휘가 안 걸린다
B  ③⑤   설교 내용어 (2.85 ④)           '찬양'이 설교의 **주제**다
C  ①④   ★ 제3범주 프로그램 클립 (새 유형)  선교사 다큐 · TV 프로그램 클립
—  ②     짧은 설교 클립                 제목에 형식 신호가 **하나도** 없다
```

★ **①④는 media_type 문제가 아니다.** 말씀도 찬양도 아니어서 **화면에서 빠져야**
하고 그건 `promo_reason`의 영역이다. ⚠ 그런데 ①은 `hope`로 태깅돼 **주제분**에
있고 promo 필터는 폴백 전용이라 닿지 않는다 — **2.77 ④가 열어 둔 구멍**이다.

**③ 안별 실측 — 임계와 가드를 전부 쟀다**

**안 A — 곡명 나열을 단독 신호로**

```
                                    매치   판정 변화   ⛔ 오탐
곡명≥2 · 가드 없음                    252      32       25   우리들교회·꿈의교회·CBS설교
곡명≥3 · 가드 없음                    140      14        7   우리들교회 사역자설교
곡명≥3 · G1(나머지에 sermon 어휘 없음)   120       7        0
곡명≥3 · G2(곡명에 장절/크레딧 없음)      130       7        0
★ 곡명≥3 · G1 + G2                  117       7        0
```

⛔ **곡명 2로 내리면 무너진다** — `2026/8/23`·`(분당한신교회 / 윤교희 목사)`·
`예수 + a는 가짜`가 곡명으로 잘린다. **3이 경계다.**

★ **자리가 결과를 가른다.**

```
1순위 **앞**        7건 고침 · 근거가 새 규칙으로 옮겨가는 것 110건
★ 동점 뒤·장절 앞    6건 고침 · 근거가 옮겨가는 것 2건
```

1건을 더 얻으려고 110건의 근거를 새 규칙에 매다는 거래다. **동점 뒤를 골랐다.**
놓치는 1건(`마지막 날에/…/주님 말씀하시면` — 곡명 안의 '말씀'이 1순위에서 확정)은
신고 대상이 아니다.

**안 B — 1순위 앞에 규칙을 더하지 않는 길**

2.85 ④의 `'OOO교회 OOO 목사'` 신호는 1순위 앞에 놓아야 해서 위험 판정이 났었다.
★ **더 좁은 길이 있었다 — 어휘 매칭만 좁힌다.**

```
크레딧이 있으면 worship 어휘에서 '찬양'만 세지 않는다
  ⑤ worship/title → 어휘 없음 → 채널 CBS설교 → sermon/channel
  ③ worship/title → 어휘 없음 → 채널 C채널방송:설교 → sermon/channel
매치 2건 · 판정 변화 2건 · 근거만 바뀌는 것 0건 · ⛔ 오탐 0
```

★ **sermon을 주장하지 않는다.** 잘못 걸린 어휘 하나를 빼면 **기존 채널 규칙이
답한다.** 순서를 안 건드리므로 2.14/2.83의 "제목이 채널보다 앞"과 충돌하지 않는다.
참고로 `'OOO교회 OOO 목사'` 211건 중 worship 어휘가 걸리는 것은 3건뿐이고,
나머지 1건(꿈의교회 · '예배실황')은 영향이 없다.

**안 C — 제3범주 사전 2개 + 주제분에도 적용**

```
'땅끝에서 온 편지'   1건 매치 — ①   '예수 영광의 면류관'   1건 매치 — ④   전부 제3범주
주제분에도 적용하면 주제 태깅된 621건 중 3건이 빠진다 (위 둘 + 원더풀우먼)
```

⚠ 사전 확장만으로는 ①이 안 고쳐진다(주제분이라 폴백 필터가 안 닿는다).
**경계를 옮기는 것이 본체**다.

**④ ⛔ ②는 규칙으로 못 고친다 — 기록만 한다**

`새해, 넘어져도 다시 일어나는 회복의 비밀 (이기는 삶을 위하여)` (256s).
제목에 형식 신호가 **하나도 없다.** 유일한 지렛대는 `worship_max_seconds: 600`인데,
길이로 worship이 **확정**된 47건을 전수로 보면:

```
진짜 찬양 ~25 (사랑의교회 단곡 8 · 광주극동 찬송가 17)
말씀 계열 ~12 (십계명 제3~10계명 8 · 경건생활 365 3 · 짧은 설교 클립)
제3범주   ~10 (CTS 뉴스 W · 광복 81주년 · CGN 후원 스토리 …)
```

규칙을 없애면 47건이 전부 unknown이 되고, **unknown은 이제 어느 탭에도 안 나오므로**
(2.82 ②) 진짜 찬양 25건이 **순수 손실**이다.
⛔⛔ **이 건을 근거로 `worship_max_seconds`를 낮추지 말 것.** 2.82 ③의 기각 근거가
그대로 유효하고, unknown 격리 이후로는 그때보다 더 나쁘다.

---

### 2.89 ★ 안 A·B·C 적용 — 신고 7건 중 6건 교정 (2026-08-28)

2.88의 세 안을 전부 적용했다 (사용자 결정).

**① 고친 곳**

```
lib/tagging.py     _CHURCH_SPEAKER · has_church_speaker_credit() ·
                   SERMON_TOPIC_WORDS · _effective_keywords()          ← 안 B
                   CONTI_STRUCTURE_MIN_SONGS=3 · is_conti_structure()  ← 안 A
                   REASON_SONGLIST · classify_media_type 1.6순위 배선
lib/selection.py   THIRD_PARTY_PROGRAMS += 땅끝에서 온 편지 · 예수 영광의 면류관
                   drop_promotional 독스트링 — 경계가 폴백 전용이 아니게 됐다
build_videos.py    build_subcategories가 drop_promotional을 **두 번** 부른다
                   화면 풀이 tagged → themed
```

**② 전수 확인 — 예측과 정확히 일치**

```
                        실측        예측
판정 변화           8건 / 1,927건    8건    ⛔ 오탐 0
주제분에서 제외          3건          3건
48개 탭        최소 20 · 최대 20 · 평균 20.00 · 48/48
층 구성          주제분 629 · 폴백 331   629 · 331
분포        sermon 1089 · worship 659 · unknown 21 (25에서 −4, 콘티가 구제)
```

**판정이 바뀐 8건 전수** — 2건은 sermon으로, 6건은 worship으로.

```
worship/title    → sermon/channel    2084s  빛의자녀교회 김형민 목사(…) - 무릎 꿇고 찬양하세요
worship/title    → sermon/channel    2035s  수원은혜교회 황유석 목사 | … 하나님을 찬양합니다
sermon/duration  → worship/songlist  1248s  [사랑의교회] 예수 피를 힘입어/…          ← 신고 ⑥
sermon/duration  → worship/songlist  1226s  [사랑의교회] 두 손 들고/…               ← 신고 ⑦
unknown/none     → worship/songlist   625s  [사랑의교회] 슬픈 마음 있는 사람/…
unknown/none     → worship/songlist   723s  [사랑의교회] 주께서 높은 보좌에/…
unknown/none     → worship/songlist  1193s  [사랑의교회] 주 예수 이름 높이어/…
unknown/none     → worship/songlist   618s  [사랑의교회] 찬송으로 보답할 수 없는/…
```

**신고 7건의 결말 — 6건 교정 · 노출 기준 31/33개 화면**

```
① ★ worship/주제분 → 화면에서 제외(제3범주)   5개 화면
② ⛔ worship/주제분 → 그대로                2개 화면   ← 못 고친다(2.88 ④)
③ ★ worship/폴백   → sermon/channel        9개 화면
④ ★ worship/폴백   → 화면에서 제외(제3범주)   3개 화면
⑤ ★ worship/폴백   → sermon/channel        8개 화면
⑥ ★ sermon/주제분  → worship/songlist       3개 화면
⑦ ★ sermon/주제분  → worship/songlist       3개 화면
```

**③ ⚠ 2.77 ④를 닫는다 — 제3범주 필터의 경계를 주제분까지 옮겼다**

사용자 판단: **"주제어가 걸렸어도 인터뷰는 인터뷰다."** 말씀 탭에서도 찬양 탭에서도
뺀다. 실측 비용은 주제 태깅 621건 중 3건이고 48/48은 그대로다.
⚠ `spread_test`에 있던 "폴백 전용이다"를 고정하던 검사는 **없앴다** — 계약이 바뀐
것이고, 새 계약은 `[10-c]`가 고정한다.

**④ ⚠⚠ 안 A의 확인되지 않은 취약점 — 게이트에 박아 뒀다**

두 가드가 **모두 우리들교회의 현재 제목 관행**에 기대고 있다.

```
"[사역자설교] 하나님의 율법을 따라 / 에스라 7:11-20 - 이성훈 목사 / 김형진 평원지기"
  G1이 막는다: 나머지 구간의 '[사역자설교]' → **설교**
  G2가 막는다: 곡명 자리의 **에스라 7:11-20**(장절) · **이성훈 목사**(크레딧)
```

그 채널이 접두사를 떼고 장절 표기를 바꾸면 **7건이 한꺼번에 오탐**이 된다.
★ 그래서 그 7건 제목을 `tagging_test`의 `WORSHIP_MUST_NOT`에 박았다 —
**관행이 바뀌어 오분류가 생기면 게이트가 실패한다.** ⛔ 이 목록을 지우지 말 것.

**⑤ 회귀**

```
tagging_test  MEDIA_CASES 12줄
  ★ 신고 ⑥⑦이 worship/songlist  ·  unknown이던 것도 구제된다
  ⛔ 곡명 2개는 콘티가 아니다  ·  G1 가드  ·  G2 가드
  ⚠ 자리 확인 — "주일 찬양대 특송 가/나/다/라"가 worship/**title**이다.
     songlist로 바뀌면 규칙이 1순위 앞으로 올라간 것이다
  ★ 신고 ③⑤가 sermon/channel  ·  크레딧이 없으면 '찬양'은 그대로 형식 어휘
  ⚠ '예배실황'처럼 다른 worship 어휘는 그대로 센다 (실측 꿈의교회 건)
tagging_test  WORSHIP_MUST_NOT 7건 + 고장 주입 3종 + 크레딧 인식 4종
  고장 주입: 임계를 2로 내려도 가드 둘이 사역자설교를 막는다
  고장 주입: 임계를 2로 내리면 곡명 2개짜리가 콘티로 잡힌다 (오탐 25건의 문)
  ⛔ SERMON_TOPIC_WORDS가 ('찬양',) 하나뿐인지
spread_test  [10-c] 제3범주 주제분 적용 4종
  ★ drop_promotional을 두 번 부른다  ·  화면 풀이 themed다
  ★ 주제 태깅된 제3범주 3종을 전부 뺀다
  ⛔ 사전 확장이 정상 콘텐츠를 물지 않는다 ("땅끝까지 이르러…" · "영광의 면류관 - 새찬송가")
```

**⑥ ⚠⚠ 확인되지 않은 위험 — 배치 몇 회 뒤에 확인할 것**

세 안의 **오탐 0은 7일치 코퍼스(1,927건)에서 잰 값이다.** 다음 셋은 확인되지 않았다.

```
① 안 A — 다른 채널이 '/' 관행을 쓰기 시작하면 어떻게 반응할지 모른다.
   지금 규칙에 **매치되는** 117건은 사랑의교회·낮은담교회·오륜교회에 몰려 있다
   (그중 실제로 songlist가 **근거**가 되는 것은 8건이다 — 아래 ⑥ '매치 수 ≠ 근거 수')
② 안 C — 새 프로그램이 생기면 **사람이 사전에 추가해야 하고, 안 하면 조용히 쌓인다.**
   경보가 없다. 2.80 ①이 남긴 "THIRD_PARTY_PROGRAMS를 따라 늘려야 한다"와 같은 부채다
③ 코퍼스가 7일치뿐이라 **계절성 제목을 못 봤다** — 부활절·성탄절 특집,
   송구영신·신년 시리즈는 제목 관행이 다를 수 있다
```

⛔ **확인 방법 — 배치 3~5회 뒤에 다음을 본다.**

⚠⚠ **먼저 읽을 것 — 매치 수 ≠ 근거 수.** 이 둘을 섞으면 지표를 거꾸로 읽는다.

```
매치 수   규칙의 조건에 맞는 제목의 수. is_conti_structure()가 True를 주는 것.
          7일치 합집합 1,927건에서 **117건**
근거 수   그 규칙이 **실제로 판정을 낸** 수. reason == "songlist"인 것.
          단일일 실행에서 **8건** (2026-08-28 07:49 실측)
```

★ **차이가 나는 이유** — 안 A는 **동점 뒤·장절 앞(1.6순위)** 에 있다. 곡명 나열
제목의 대부분은 나머지 구간에 '찬양'·'예배실황' 같은 어휘가 있어 **1순위에서 이미
확정**된다. `songlist`가 근거가 되는 것은 **앞 단계에서 아무도 답하지 않았을 때뿐**이다.
그래서 8 ≪ 117이 정상이고, 이 값이 117 근처로 오르면 오히려 **규칙이 1순위 앞으로
올라간 것**을 의심해야 한다(⛔ 2.88 ③의 금지 사항).

⚠ 다른 reason도 같다. `speaker`(안 A′)도 매치 264건에 근거 27~28건이다.
**표에 적는 기준선은 언제나 근거 수다.**

```
unknown_ratio        지금 0.012(21/1,769). 오르면 콘티 가드가 과하게 막는 것일 수 있다
media_types 분포     sermon 1088 · worship 660. worship이 급히 늘면 안 A 오탐을 의심한다
reason: songlist     지금 **8건**(전부 사랑의교회). 두 자리 수 후반으로 늘면
                     **다른 채널이 '/' 관행을 시작한 것**이고 그때 오탐을 다시 재야 한다
                     ⛔ 117과 비교하지 말 것 — 그건 매치 수다 (위 참조)
reason: speaker      지금 27건. 안 A′의 지표다
게이트 실패          WORSHIP_MUST_NOT이 깨지면 우리들교회가 관행을 바꾼 것이다
```

★ 이 항목을 3절 '여기서 시작한다'에 등재했다.

---

### 2.90 ★ Capacitor 안드로이드 래핑 1단계 — 번들 실행 + 앱 빌드 분리 (2026-08-31)

`d103b06` · 출시 준비 6번. 배너·알림 없이 껍데기만 올린다.

**웹과 앱이 갈리는 자리는 서빙 오리진 하나다.**

```
웹    https://appleap385-aaz0919.github.io/PeaceYourMind/
앱    https://localhost/  (번들)
→ base · 데이터 경로 · 서비스워커 · 광고 · 방침 링크가 전부 갈린다
  가르는 것은 vite --mode capacitor 하나, 분기점은 define __IS_APP__ 하나다
```

⛔ **server.url을 쓰지 않는다 — 2.54절 (a)를 뒤집은 결정이다.**
2.54절은 광고 위험 때문에 "server.url을 실제 사이트로"라고 적어 뒀다. 그러나 그렇게
하면 설치 직후 네트워크가 없을 때 아무것도 못 띄운다 — sync.js가 지키기로 한
"네트워크가 없어도 구절 화면은 떠야 한다"가 **첫 실행에서** 깨진다. Capacitor 공식
문서도 server.url을 프로덕션용이 아니라고 못박는다.
→ **앱 광고 2단계의 전제가 바뀌었다.** 보류 표의 그 항목을 다시 열 때 이 절을 먼저 읽을 것.

★ krv 178장(942KB)이 APK에 들어간다 → 보류 표의 "오프라인 첫 방문"이 **앱에서는 공짜로 풀린다.**

⛔ **AdSense를 앱 빌드에서 물리적으로 들어낸다.** 지금 AD_SLOT이 비어서 "어차피 안 뜬다"는
근거가 되지 못한다 — 웹 심사가 통과해 값이 채워지는 날 **같은 소스가 앱에서도 광고를 켠다.**
약속을 기억에 맡기지 않고 빌드가 지운다(index.html 주석과 script를 **따로** 센다 ·
산출물 전수 검사 · ads.js가 앱에서 AD_SLOT을 비운다 · 방침 사본 제외).

⚠ **가드를 짜다가 실제로 당했다.** "둘 중 하나라도 지워졌으면 통과"로 짰더니 주석만
지워지고 script는 남았는데 검사가 통과했다(new RegExp + 템플릿 리터럴에서 문자 클래스가
뭉개졌다). → 리터럴 정규식으로 바꿨다.
**가드가 가려지는 것은 가드가 없는 것보다 나쁘다.**

⛔ 앱 빌드는 `dist`가 아니라 `dist-app`에 쓴다. deploy-app.yml이 `./app/dist`를 웹에
올리므로 같은 자리에 쓰면 앱 번들이 웹으로 배포될 수 있다.

⚠ 이 시점에는 실기기 확인을 못 했다 — 빌드 JDK가 없었다("Unsupported class file major
version 69"). 2.91절에서 기기와 함께 풀렸다. 지금의 JDK 경로는 3절 실행 환경에 적었다.

---

### 2.91 ★ 아이콘 교체 — 03-c "끊긴 원 · 숨" + gen_icons.py 복원 (2026-08-31)

`b4c6024` · Capacitor 기본값(하늘색 X)을 전부 걷어내고 앱의 얼굴을 만들었다.

기존 icon.svg는 배경 + 빛뿐이라 형태가 없었다. 아름답지만 **홈 화면에서 못 찾는다.**
빛은 그대로 두고 얇은 원으로 윤곽만 줬고 위쪽 90°를 열어 뒀다 — "들이쉬는 중"으로
읽히게 하려는 것이고, 입력 화면 `.orb`(10초 주기 호흡)와 같은 결이다.
⛔ 십자가는 넣지 않았다. 이 앱은 종교색을 앞세우지 않고 "마음"을 먼저 말해왔다.

**48dp에서 틈이 보이는가 — 이것이 이 도형을 쓸 근거다.**

```
03-a 닫힌 원    획 78.7  틈 118.3  대비 -39.6   (틈 없음)
03-c 90° w13   획 79.7  틈  32.2  대비 +47.5   ★ 틈이 배경까지 내려간다
```

⚠ 틈을 더 넓히거나 선을 굵혀도 대비는 47→53으로 거의 안 오른다(62°에서 이미 배경에
닿는다). 90°는 가독성이 아니라 "한눈에 열려 있다"를 택한 것이다.

⛔ **손으로 만든 자산에서 생성물로.** icon.svg 머리말이 "gen_icons.py가 생성한다"고
적어 두었는데 **그 스크립트가 저장소에 없었다.** 색을 바꾸려 해도 아무도 다시 뽑을 수
없는 상태였다. 다시 썼고 이번엔 커밋했다. 색은 `app/src/theme.js`에서 읽는다 —
두 곳에 적으면 반드시 갈라지고, 갈라져도 아무도 모른다.

**★ 흰 번쩍임을 없앤 자리 — styles.xml 한 줄이다.**
스플래시만 어둡게 해서는 소용이 없었다. `BridgeActivity.onCreate()`가
`AppTheme_NoActionBar`로 갈아타는데 **그 테마의 부모가 DayNight라 라이트 모드 기기에서
windowBackground가 흰색**이다. 웹뷰 첫 프레임 전까지 그 흰 화면이 보인다.
→ `android:windowBackground=#0B1216`.

---

### 2.92 ★ 구절 알림 — 정한 시각에 구절 한 절 (2026-08-31)

`77547ff` · **앱이 웹보다 더 하는 것**(2.54 c의 후보였다).

⛔ **화면과 다른 기준으로 고른다 — 이것이 이 기능의 핵심 판단이다.**
결과 화면은 사용자가 감정을 **말한 뒤에** 고르므로 1인칭 고백(시편)이 강하다.
알림은 정반대다. 사용자는 아무 말도 하지 않았다. 그 자리에 "내 영혼아 네가 어찌하여
낙망하며"가 도착하면 **감정을 넘겨짚는다.** 2.39가 비시편의 약점으로 본 성질
(약속·초대·명령형)이 여기서는 강점이 된다.

```
293 − 시편 163 − notify:false 23 = 107건 · 책 35종 · 최다 이사야 27건
제외는 데이터가 정한다 — verses.yaml의 notify:false + notify_note(왜 뺐는지)
⛔ 코드에 id를 박지 않는다. 박으면 큐레이션과 갈라진다
```

**규칙** — 14일 rolling window(앱을 열 때마다 채운다. 크론도 백그라운드도 없다.
★ 14일 안 열면 멎는다 — 떠난 사람을 부르지 않는다) · 같은 책 이틀 연속 금지 ·
최근 30일 제외(⛔ NOTIFY_SEEN 전용 키. VERSE_INDEXES를 재사용하면 화면의 구절 회전이
건너뛰어진다) · 마르면 연속 금지 → 30일 창 순으로 푼다.

**★ 실기기에서만 드러난 결함 넷 — 전부 조용히 실패했다.** 화면에는 아무 오류도 없고
알림만 안 왔다.

```
① LocalNotifications.then() is not implemented on android
   플러그인 프록시를 async 함수에서 그대로 return하면 JS가 thenable로 보고
   .then()을 부른다 → 상자에 담아 돌려준다 { ln: ... }
② schedule()이 영영 응답하지 않았다
   isExactNotification 기본값이 true인데 SCHEDULE_EXACT_ALARM을 걷어냈다.
   플러그인이 시스템 "알람 및 리마인더" 화면을 열고 결과를 기다린다
   → isExactNotification: false
③ About이 dataRef(영상 데이터)를 구절 데이터로 넘겼다 → 풀이 빈 배열
   → notify.js가 versesData를 직접 읽는다. 틀린 것을 넘길 자리를 없앴다
④ 권한 응답을 기다리는 동안 알림 절이 통째로 사라졌다
```

⚠ 설계 구멍 하나 — 기본값 켜짐 + "토글 켤 때만 권한"을 같이 두면 사용자가 토글을 누를
일이 없어 **영영 묻지 않는다.** About을 열 때 묻도록 했다(첫 실행에 묻지 않는다는
약속은 그대로다).

**★ 이 구멍이 실제로 막혔는지 실사용자 경로로 확인했다 (2026-08-31).**
재설치 후 `pm grant` 없이, 손으로 화면을 밟았다.

```
첫 화면          팝업 없음 ✓  ("처음 오셨네요" — 약속대로다)
About 진입       팝업 뜸 ✓   ★ 알림 절이 **이미 그려진 상태**에서 떴다 (결함 ④ 방지 확인)
거부             토글 꺼짐 ✓ · 시각 입력 사라짐 ✓ · "권한이 없어 보낼 수 없어요" ✓
                예약 0건 ✓ · POST_NOTIFICATIONS granted=false
About 재진입      토글 꺼짐 유지 ✓ → **저장값도 꺼졌다**(스위치가 거짓말하지 않는다)
                다시 묻지 않는다 ✓ (`if (!s.on) return`)
토글 켜기         팝업 다시 뜸 ✓ → 허용 → 예약 14건 · granted=true
```

⚠ 관찰 — About을 **재진입하면** "권한이 없어 보낼 수 없어요" 문장이 사라진다.
  `denied`가 요청을 실제로 한 그 순간에만 켜지기 때문이다. 나중에 다시 들어온 사용자는
  **왜 꺼져 있는지 모른 채 꺼진 토글만 본다.** 토글을 누르면 다시 물으므로 막다른 길은
  아니고, "앱을 실패로 말하지 않는다"는 태도와도 일치한다. 고칠지는 판단이 필요하다.

---

### 2.93 ★ 알림 문구 시각 중립화 · 참조 중복 제거 · NOTIFY_SEEN 누적 (2026-08-31)

`6db0d5d` · 사용자가 시각을 **밤으로** 정할 수 있는데 문구가 "아침"이었다.

```
About 절 제목   아침 알림 → 구절 알림
알림 화면 머리   오늘 아침의 구절 → 오늘의 구절
채널 name       아침 구절 → 구절 알림
코드 이름       MorningVerse.jsx → DailyVerse.jsx · morningVerse → dailyVerse
```

⛔ **채널 id는 morning_verse 그대로 뒀다.** 사용자가 보는 것은 name이고 id는
안드로이드가 기기에 저장하는 내부 키다. 바꾸면 이미 설치된 기기에 채널이 하나 더 생기고
옛 채널이 설정 화면에 **유령으로 남는다.** 이 판단은 2.94절에서 다시 시험대에 올랐고
그대로 유지됐다.

**★ NOTIFY_SEEN 누적 버그** — 앱을 열 때마다 기록이 쌓여 실기기에서 107건까지 찼다.
풀이 107건이므로 **풀 전체가 "최근 보냄"으로 굳기 직전**이었다.
원인은 순서다. 30일 지난 것을 버린 뒤(`pruneSeen`), **아직 오지 않은 예약**을
버려야 한다(`dropScheduled`) — 바로 위에서 그것들을 취소했으므로 보낸 적이 없다.

---

### 2.94 ★ 알림 소리·진동 — 원인이 세 겹이었다 (2026-08-31)

`1ebac35` · 관측은 "알림은 도착하는데 소리도 진동도 없다"였다.

**★ 방법론 — 관측 대상이 조사 대상이 아니었다.**
사람이 본 알림은 id 4299·4203이었고 그것들은 `channel=default`로 나갔다. 우리가 고치려던
`morning_verse` 채널의 알림이 **아니었다.** 저장소 어디에도 그 id가 없다
(`git log --all -S 4299 -- app/` 0건 · 소스의 `.schedule()` 호출은 notify.js 하나 ·
설치 번들에도 없다). 웹뷰 디버깅 콘솔로 임시로 쏜 것이 조사 대상으로 둔갑해 있었다.
⛔ **다음에도**: 증상을 재현했다고 말하기 전에, 그 알림이 **우리 채널로 나갔는지**를
`notification_enqueue`의 `channel=`로 먼저 확인할 것.

**원인은 세 겹이고, 어느 하나만 고쳐도 증상은 안 바뀐다.**

```
① 기기가 무음 모드였다        ringer mode = SILENT · STREAM_NOTIFICATION Muted: true
                            부팅 후 한 번도 안 바뀌었다(Ringer mode change history 비어 있음)
② 채널 진동이 꺼져 있었다      mVibrationEnabled=false
                            ⛔ 우리가 vibration을 안 넘겨서 플러그인 기본값 false가 이겼다
③ 시험 기기에 진동 모터가 없다  Galaxy Tab S9 FE — android.hardware.vibrator 미보유
                            모든 진동 요청이 ignored_unsupported로 기록된다
```

**소리는 처음부터 정상이었다.** 채널이 `mSound=content://settings/system/notification_sound`
(기기 기본 알림음)로 잡혀 있었고 importance도 3이었다. 무음을 풀고 실제 회차를 발생시켜
확인했다 — **로그로**, 사람 귀가 아니라.

```
16:42:29.990  notification_enqueue  id=4100 channel=morning_verse   ← 우리 채널로 나갔다
16:42:30.215  new player piid:695   usage=USAGE_NOTIFICATION/CONTENT_TYPE_SONIFICATION
16:42:30.767  player piid:695 event:started      ← 실제 재생
16:42:32.986  player piid:695 event:stopped      (2.2초 · 44100Hz · deviceId:2 스피커)
레코드         isNoisy=true · mIntercept=false · mSuppressedVisualEffects=0
              mVibration=null   ← 진동만 없다
```

⛔ **채널은 한 번 만들어지면 코드로 못 바꾼다.** 갱신되는 것은 name·description·group뿐이고
importance는 사용자가 손대지 않았을 때 **낮추는 것만** 된다. 같은 id로 지웠다 다시 만드는
우회도 막혀 있다 — 옛 설정이 복원된다.
→ `vibration: true`는 **재설치한 기기에서만** 반영된다. 실기기로 확인했다.

```
재설치 전  mVibrationEnabled=false
uninstall  → morning_verse 채널이 시스템에서 사라진다 (앱 데이터 삭제로는 안 지워진다)
재설치 후  mVibrationEnabled=true · mImportance=3 · mSound=기본 알림음
```

⚠ **비용이 0인 시점은 출시 전인 지금뿐이다.** 출시 뒤에 이 값을 바꾸면 기존 사용자에게는
영영 반영되지 않는다.

★ **출시 후 사용자 문의로 올 자리다.** "소리가 안 나요"라는 문의에 대고
"앱 데이터를 지워 보세요"라고 답하면 **아무것도 해결되지 않는다** — 채널은 앱 데이터가
아니라 시스템의 알림 정책에 산다. 데이터 삭제로도, 캐시 삭제로도 안 지워지고
**패키지 삭제(재설치)만이 지운다.** 실기기에서 확인했다.
→ 답해야 할 것은 둘 중 하나다. **설정 → 앱 → Peace in Mind → 알림 → 구절 알림**에서
  사용자가 직접 켜거나, 재설치하거나. ⛔ 앱이 코드로 해 줄 수 있는 일은 없다.

**⚠ 플러그인 타입 문서가 구현과 다르다.** `definitions.d.ts`는 "sound를 안 주면
소리가 없다"고 적어 놓았는데, Kotlin 구현은 sound가 없으면 `setSound`를 **아예 호출하지
않고** 생성자 기본값(기기 기본 알림음)을 남긴다. 실기기 dumpsys가 그것을 확인해 준다.
⛔ 문서를 믿고 `sound`에 파일명을 넣지 말 것 — 사용자가 자기 기기에서 고른 소리를 덮는다.

**⛔ `notification_alert` 로그를 근거로 쓰지 말 것 — 삼성이 필드를 바꿔 놓았다.**
AOSP는 `(key, buzz, beep, blink)` 세 개의 0/1을 적는데 이 기기는 **네 개**를 적고 값도
0/1이 아니다. 같은 무음 모드에서 `0,11,0,0`과 `13,13,0,0`이 함께 나왔다 —
**해석이 서지 않는다.**
→ 소리 여부는 **오디오 계층**에서 본다. `dumpsys audio`의 플레이어 이벤트에
  `usage=USAGE_NOTIFICATION`으로 `event:started`가 찍히면 실제로 울린 것이다.
  레코드 쪽 보조 증거는 `isNoisy` · `mIntercept` · `mSuppressedVisualEffects`다.

**⚠ 테스트 함정 — FLAG_ONLY_ALERT_ONCE.** 플러그인이 모든 알림에 이 플래그를 강제로 붙인다
(`LocalNotificationManager.kt`). 같은 id를 짧은 간격으로 반복 발송하면 앞 알림이 알림창에
남아 있는 동안에는 **소리 없이 갱신만** 된다. 손으로 반복 테스트할 때 "소리가 안 난다"는
결론이 여기서 나올 수 있다.

**결정 — A안(기본 알림음 + 진동). 근거는 되돌릴 수 있는 방향이 한쪽뿐이라는 것이다.**

```
importance 3으로 출시  → 나중에 코드로 2까지 낮출 수 있다 (사용자가 안 건드린 경우)
importance 2로 출시    → 코드로는 영영 못 올린다. 재설치 외엔 길이 없다
```

그 밖의 근거 — 기본 알림음은 **사용자가 자기 기기에서 이미 고른 소리**이지 우리가 고른
소리가 아니다("조용함이 자산"과 충돌하지 않는 자리가 정확히 여기다) · importance 3은
헤드업 팝업 없이 소리만 낸다(4는 화면을 가로챈다) · 하루 한 번, 사용자가 정한 시각이라
침입성의 총량이 이미 낮다 · 무음 모드는 뚫지 않는다.

⛔ **"진동만" 안은 만들 수 없다.** 채널 sound를 null로 둬야 하는데 플러그인의 `sound`
옵션은 `res/raw` 파일명만 받는다. importance를 2로 낮추면 소리와 진동이 함께 죽는다
(안드로이드는 3 미만에서 알리지 않는다). 네이티브를 건드려야 한다.
★ 다만 **사용자는 시스템 설정에서 그 조합을 스스로 만들 수 있다** — 알림음 "없음" + 진동 켜기.
사용자가 소리를 끄거나 채널을 차단하는 것도 전부 가능하다(지금 mUserLockedFields=0이라
아직 아무것도 안 건드린 상태다. 한 번 바꾸면 그 항목이 잠기고 앱은 더 이상 못 내린다).

**게이트** — 테스트 29 → 33건. 채널 인자를 단언하는 것이 **0건이었다.**
`vibration: true` 줄을 지우면 실패하는 것을 확인했다. 주석을 지운 뒤 단언하므로
주석만으로는 통과하지 않는다.

**확인한 것과 못 한 것을 나눈다.**

```
확인 가능(했다)   createChannel 인자(테스트) · 재설치 후 mVibrationEnabled=true
                실제 소리 재생(오디오 플레이어 이벤트) · 우리 채널로 나갔다는 것
확인 불가(였다)   실제 진동. 시험 기기에 진동 모터가 없다 — 휴대폰이 필요하다
                VIBRATE 권한 없이 채널 진동이 동작하는지도 같은 이유로 검증 불가
                (매니페스트에 VIBRATE가 없다. 플러그인도 선언하지 않는다)
✅ 닫혔다        2026-09-03 · 휴대폰(Galaxy Z Flip)에서 실측했다 — 바로 아래
```

#### ✅ 닫혔다 — VIBRATE 권한 없이 울린다 (2026-09-03 · 사용자 확정)

```
APK가 실제로 선언한 권한 (aapt dump permissions · 병합 결과)
  INTERNET · AD_ID · ACCESS_NETWORK_STATE · RECEIVE_BOOT_COMPLETED · WAKE_LOCK ·
  POST_NOTIFICATIONS · ACCESS_ADSERVICES_{AD_ID,ATTRIBUTION,TOPICS} ·
  FOREGROUND_SERVICE · DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION
  ⛔ **android.permission.VIBRATE는 없다.** 우리도 플러그인도 선언하지 않는다

실측 (dumpsys vibrator_manager · 구절 알림이 실제로 나간 직후)
  startTime 09-03 09:31:26.392 · durationMs **1102** · status **finished**
  attrs Usage=NOTIFICATION
  uid **1000** · opPkg **android**
  reason Notification (io.github.appleap385.peaceinmind 10198)
```

★ **uid가 1000(system)이고 opPkg가 android다.** 우리 앱이 진동을 요청한 것이 아니라
**시스템이 채널 설정을 읽고 대신 울렸다.** 그래서 앱에 VIBRATE 권한이 필요 없다 —
통설이 맞았고 이제 실측 근거가 있다.

```
⛔ **VIBRATE를 매니페스트에 추가하지 말 것** (사용자 확정 · 2026-09-03)
   권한을 늘리면 스토어 권한 목록만 길어지고 얻는 것이 없다
★ 태블릿에서 보이던 ignored_unsupported는 **권한 문제가 아니라 모터 부재**였다.
   Galaxy Tab S9 FE는 android.hardware.vibrator를 갖고 있지 않다.
   ⚠ 다음 세션이 이 로그를 다시 보면 권한을 의심하기 쉽다 — 아니다
✅ 휴대폰 진동 하드웨어  Vibrator id=0 · mCapabilities=[AMPLITUDE_CONTROL]
```

⚠ 이 조사에서 사람 귀는 세 번 틀렸다 — 무음 모드 · 다른 채널(4299·4203) · 진동 모터 부재.
**로그가 결론을 냈다.**

**고아 예약은 남지 않았다.** 세 곳을 다 봤다 — AlarmManager 14건 · NOTIFICATION_STORE 14건
(둘 다 전부 4100~4113) · 알림창 0건. 플러그인 8.3.1의 `LocalNotificationRestoreReceiver`가
이미 발송된 일회성 예약을 부팅 때 **다시 걸지 않는다.** 그 가드가 없는 버전이었다면
4299가 재부팅마다 되살아났을 것이다.
⚠ 만약 남는다면 `cancelOurs`로는 못 지운다(대역 밖이다) — `getPending()` → `cancel()`이나
재설치가 필요하다. shared_prefs만 지우는 것은 반쪽이다(AlarmManager 예약이 남는다).

---

### 2.95 ★ 광고 노출 범위 정정 — 이어서 읽기 탭이 뒤집혔다 (2026-08-31)

**이것이 최신 결정이다. 아래 표가 08-24 방침을 대체한다.**

```
광고 없음   위기 화면 · 알림 콜백 화면(DailyVerse)
광고 있음   이어서 읽기 탭 · 말씀 탭 · 찬양 탭
```

⚠ **뒤집힌 것은 「이어서 읽기 탭」 하나다.** 2.51절이 "위기 화면 · 이어서 읽기 탭"
둘 다 제외로 확정했었고, 그 뒤 2.53·2.54·2.55가 그 문장을 그대로 물려받았다.
**결정은 다른 대화에서 났고 문서에 반영되지 않았다** — 2026-08-31에 발견했다.

★ **오늘 여섯 번째 같은 병이다**(3절 「문서가 코드보다 뒤에 서는 문제」).
앞의 다섯은 코드가 앞서고 문서가 뒤처진 것이었는데, 이번은 **결정이 앞서고 문서가
뒤처진 것**이다. 종류가 하나 늘었다 — 그리고 이쪽은 stats.py가 못 잡는다.
사람의 결정은 코드에 없기 때문이다.

**옛 내용이 남아 있던 곳 — 문서 4곳 · 코드 2곳**

```
HANDOFF  2.51 ⑥   "⛔ 광고를 넣지 않는 곳 (확정) — 위기 화면 · 이어서 읽기 탭"
         2.53 ④d  "위기 화면·이어서 읽기 제외는 그대로 보장된다"
         2.54     "웹(PWA) … 위기·이어서 읽기 제외"
         2.55     "화면 제외(위기·이어서 읽기)는 기존 회귀가 이미 지킨다"
         → 넷 다 **역사 기록이라 본문은 고치지 않고** 정정 표시만 달았다.
           2.56의 이전/지금 대조표를 남겨 둔 것과 같은 방식이다
코드     verses.test.js  AD_FREE_SCREENS에 ChapterReader.jsx가 들어 있다
         About.jsx       "성경 본문을 읽는 자리에는 광고를 두지 않습니다"
         → ⛔ **아직 고치지 않았다.** 아래 🔴가 먼저다
```

**🔴 판단 필요 — About 광고 절 문구가 새 결정과 충돌한다**

```
지금 문구  "성경 본문을 읽는 자리에는 광고를 두지 않습니다"
          · 사용자 확정값이다 (2.51 ⑤)
          · verses.test.js가 문장 단위로 고정하고 있다
충돌      이어서 읽기는 **성경 본문을 읽는 자리다.** 광고를 넣으면
          이 문장이 화면에 의해 반증된다 — 방침이 하지 않는 일을 말하게 된다
⛔ 임의로 고치지 않았다. 선택지와 대가는 세션이 정리해 보고했고 사용자가 정한다
```

⚠ **알림 콜백 화면의 회귀는 새 배치에서 유효하지 않다.** `notify.test.js`의
"알림 화면에 영상과 광고가 없다"는 `DailyVerse.jsx`의 **소스 텍스트**만 본다.
고정 띠배너는 Shell에서 그리므로 그 파일에 토큰이 남지 않는다 —
**테스트는 통과하면서 띠는 화면에 남는다.** `AD_FREE_SCREENS`(위기·이어서 읽기)도
같은 구조라 같은 구멍이 있다. 배치를 바꾸면 회귀도 함께 바꿔야 한다.

---

### 2.96 ★ AdMob 조사와 배치 재설계 — 🔴 자료 (2026-08-31 · 승인 대기)

> ⛔ **조사와 자료뿐이다.** 앱을 등록하지 않았고 아무것도 구현하지 않았다.
> ⚠ 아래에 "콘솔에서 확인"이라고 적은 것은 **세션이 확인하지 못한 것**이다.
> 조사 결과와 화면 확인을 섞지 않는다 (Play 앱 서명 전제에서 배운 것이다).

**계정 — ✅ 닫힘.** AdMob 계정이 AdSense와 공유된다. 별도 신청이 없었다(사용자 확인).
화면이 말한 것 — 계정 인증 진행 중(최대 2주) · 진행 순서는 **앱 추가 → 광고 단위 추가**
(광고 단위 카드는 앱 추가 전까지 잠긴다) · 인증 완료 전에도 계정 이용은 가능하다.

---

#### (라) 방침·신고 영향 — ★ 먼저 읽을 것. 여기서 사실 하나가 어긋나 있었다

**⚠ 방침 2절은 매체를 특정하고 있다.** "매체 구분 없이 두기로 했다"는 기억과 다르다.

```
배포된 문구  "이 앱은 무료로 운영하기 위해 영상 목록 사이에
             Google AdSense 광고를 게재합니다."
→ 앱이 AdMob이면 **이 문장이 앱에서 거짓이 된다.** 제품 이름이 다르다.
★ 2.54 (e)가 "매체를 특정하지 않는다"고 적은 것은 **그때의 문구**를 말한 것이고,
  2.55에서 광고를 구현하며 AdSense를 명시하는 문구로 바뀌었다.
  ⛔ 오늘 일곱 번째 같은 병이다 — 결정과 문서가 아니라 **문서와 문서**가 어긋났다.
```

**✅ 방침 문구 확정 (2026-08-31) — 매체명을 다시 특정하지 않는다.**

```
고침  "Google AdSense 광고를 게재합니다"
     → "Google의 광고 서비스를 통해 광고를 게재합니다"
     "영상 목록 사이에" → "영상 목록이 있는 화면에"
근거  ★ **매체가 또 바뀌면 또 어긋난다.** 사용자가 알아야 하는 것은 구글 광고가
     들어간다는 사실이지 제품명이 아니다.
     ⛔ 오늘 일곱 번 반복된 병의 뿌리가 **문서에 바뀔 값을 박아둔 것**이다
⚠ 배치("사이에")도 같은 이유로 걷어냈다 — 웹은 목록 사이, 앱은 하단 띠다
⚠ 재배포 — app/public/privacy/는 vite가 dist로 복사하고 deploy-app.yml의
  paths(app/**)가 걸린다. 커밋 push로 **자동 재배포된다**
✅ About·스토어 초안과 갈리지 않는다 — 둘 다 매체명을 쓰지 않는다.
  방침 26줄의 AdSense 주석은 소유권 확인 태그 설명이라 그대로 둔다
```

**수집 주체** — 둘 다 Google이지만 **제품이 다르고 수집 항목이 다르다.**

```
AdSense(웹)   브라우저 쿠키·IP·기기 정보
AdMob(앱)     ★ **광고 ID(AAID)**를 쓴다. 쿠키가 아니라 기기 식별자다
              SDK가 com.google.android.gms.permission.AD_ID를 매니페스트에 병합한다
              → 권한 목록이 늘고, 데이터 안전 신고가 바뀐다
```

**🔴 데이터 안전 신고가 통째로 바뀐다 — 이것이 가장 큰 영향이다**

```
지금 초안    수집 없음 · 공유 없음
AdMob 적용 후 **"기기 또는 기타 ID" 수집 O · 공유 O (광고 목적)**
            항목 — 기기 또는 기타 ID(광고 ID) · 대략적 위치(IP 기반일 수 있다) ·
                   앱 상호작용(광고 노출·클릭)
            목적 — 광고 또는 마케팅
            암호화 전송 O · 사용자 삭제 요청 경로는 별도 판단
⚠ "지금은 광고가 안 나간다"는 신고 근거가 되지 않는다. **코드가 들어 있으면
  수집 가능성이 존재한다.** 보수적으로 신고하는 것이 맞다 (아래 (다) 참조)
```

**아동 대상 — AdMob에도 따로 있다**

```
Play Console  앱 콘텐츠 > 타겟층 및 콘텐츠
AdMob         앱별 설정 + 코드의 RequestConfiguration
              TFCD (tag for child-directed treatment)
              TFUA (tag for users under age of consent)
⚠ 셋이 서로 어긋나면 그 자체가 문제다 — 방침 5절이 "아동 대상 아님"으로
  이미 확정돼 있으므로 **셋 다 그것에 맞춘다.** 고칠 것은 방침이 아니라 설정 쪽이다
  (출시 준비 표 #10과 같은 논리다)
```

---

#### (다) 인증 2주가 일정에 미치는 영향 — 새 병목이다

```
✅ 인증 전에도 되는 것   앱 추가 · 광고 단위 추가 (콘솔 안내)
🔴 콘솔에서 확인할 것    **인증 전에 광고가 실제로 나가는가, 빈 채로 있는가**
                       세션이 확인할 수 없다. 아래 콘솔 목록에 넣었다
                       ⚠ 새 광고 단위는 인증과 무관하게도 서빙까지 시간이 걸린다 —
                         "안 나온다"를 인증 탓으로 단정하지 말 것
```

**✅ 확정 (2026-08-31) — AdMob이 들어간 상태로 신고한다.** 아래가 그 근거다.

**🔴 판단 — 인증 전 상태로 심사에 제출할 수 있는가**

```
기술적으로는 된다. 문제는 **데이터 안전 신고를 무엇으로 하느냐**이고,
답은 하나다 — **AdMob이 들어간 상태로 신고한다.**
  근거 ① 신고는 "앱이 무엇을 할 수 있는가"를 말한다. 광고가 오늘 안 나가는 것은
        서버 사정이고 앱의 성질이 아니다
  근거 ② 나중에 광고가 나가기 시작하면 신고를 바꿔야 하는데, 그 시점을
        아무도 지켜보지 않는다. **미래에 사람이 기억해야 하는 설계는 실패한다**
        (오늘 여섯 번 확인했다)
⛔ 반대로 "수집 없음"으로 내고 나중에 고치는 안은 권하지 않는다.
  그 사이에 광고가 한 번이라도 나가면 신고가 거짓이 된 상태로 배포돼 있게 된다
```

**인증을 기다리는 동안 병행할 수 있는 것**

```
✅ 지금   배치 설계 확정(아래 (가)) · Play Console 등록 · 서명 키 ·
         등록정보 문구 · 방침 개정 초안 · 플러그인 통합과 빌드 확인
⏸ 뒤로   스크린샷 (광고 자리가 화면에 생기니 지금 찍으면 다시 찍는다) ·
         데이터 안전 신고 제출 (배치가 확정돼야 항목이 정해진다)
```

---

#### (나) Capacitor 통합

```
플러그인      @capacitor-community/admob  ⚠ 커뮤니티 플러그인이다(공식 아님).
                                        Capacitor 메이저 버전과의 호환을 먼저 볼 것
네이티브 의존   Google Mobile Ads SDK (play-services-ads)
APK 영향      대략 +2~3MB (dex + 리소스 + 전이 의존성)
              ⚠ 실측할 것 — 지금 APK가 5.1MB이므로 비율로는 작지 않다
minSdk        지금 24. Ads SDK 최신 요구선(보통 21~23)보다 높아 문제없다
⛔ 매니페스트   APPLICATION_ID meta-data가 **필수다.** 없으면 앱이 시작하자마자
              죽는다(런타임 크래시). 테스트 ID로라도 넣어 두어야 빌드가 산다
⚠ 권한 병합    SDK가 com.google.android.gms.permission.AD_ID를 넣는다
              → 「앱」 실측 블록의 권한 줄이 바뀐다. stats.py가 그것을 잡는다
```

**★ 지금 "앱에 광고가 없다"를 강제하는 장치 셋 — 없애지 않고 나눈다**

```
① ads.js       AD_SLOT = IS_APP ? "" : WEB_AD_SLOT
② appbuild.test.js  산출물에 adsbygoogle 0건
③ About        광고 절이 AD_SLOT에 묶여 있다 (값이 없으면 문단을 안 그린다)

⛔ **셋 다 그대로 둔다.** 이것들이 말하는 것은 "앱에 광고가 없다"가 아니라
  **"앱에 AdSense가 없다"**이고, AdMob이 들어와도 그 사실은 변하지 않는다.
  AdSense 프로그램 정책이 앱 안의 AdSense를 금지하는 것(2.52)이 그대로이기 때문이다.
★ ③만 손봐야 한다 — About 광고 문단이 웹에서는 AD_SLOT에, 앱에서는
  AdMob 활성 여부에 묶여야 한다. 지금 구조로는 앱에서 문단이 영영 안 그려진다.
```

---

#### (가) 배치 재설계 — 최하단 고정 띠배너 (높이 100)

⚠ **웹과 갈린다.** 웹은 `ads.js`의 `BASE_SLOTS [5,11,17]`로 목록 항목 **사이**에
끼우고, 앱은 화면 최하단에 **떠 있는 띠**다. 두 매체가 다른 물건을 쓴다.

**① 형식 — 높이 100을 지정할 수 있는가**

```
고정 크기 배너
  BANNER            320×50
  LARGE_BANNER      320×100   ★ 웹의 320×100과 **정확히 같다**
  MEDIUM_RECTANGLE  300×250
적응형 앵커 배너 (구글 권장)
  너비는 기기 폭에 맞추고 **높이는 SDK가 정한다** (폭에 따라 대략 50~90dp,
  화면 높이의 15%를 넘지 않게 제한)
  ⛔ 높이 100을 지정할 수 없다
네이티브 광고        목록 안 삽입용. ⛔ 웹뷰 DOM 사이에는 못 넣는다 (2.53 ③ 그대로)
전면·보상형·앱 오프닝  ⛔ 화면을 가로챈다. 이 앱의 태도와 정면으로 어긋난다
```

→ **높이 100을 원하면 LARGE_BANNER 하나뿐이다.**
⚠ 대가 — 폭이 320 고정이라 360dp 화면에서 좌우 20dp씩 남는다(가운데 정렬).
태블릿에서는 훨씬 많이 남아 **띠가 화면 폭을 채우지 못한다.**

**② 화면별 숨김 — 이 안의 핵심 검증 지점**

```
⛔ 원리를 뒤집는다 — **기본이 숨김이고, 허용 화면에서만 띄운다.**
  "전환할 때 숨긴다"로 짜면 브리지 왕복 지연 동안 띠가 남는다.
  ★ 노출 위험의 방향이 바뀐다 — 늦게 뜨는 것은 안전하고, 늦게 사라지는 것이 사고다.
⚠ 콜드 스타트도 이것으로 풀린다 — 알림을 탭해 앱이 처음 뜰 때 첫 화면이
  DailyVerse다. 기본이 숨김이면 배너를 **아예 만들지 않는다.**
```

**새 회귀 — 소스 텍스트로는 못 잡는다(지금 구조의 결함)**

```
지금    notify.test.js  DailyVerse.jsx에 "AdSlot" 같은 토큰이 없는가
        AD_FREE_SCREENS  CrisisScreen.jsx · DailyVerse.jsx에 토큰이 없는가
⛔ 둘 다 **파일 안의 글자만 본다.** 띠는 Shell에서 그리므로 그 파일들에
  토큰이 남지 않는다 → **통과하면서 띠는 화면에 남는다.**

새 검사   ① 순수 함수로 뽑는다 — bannerVisible(screen) → boolean
           테스트가 화면 종류마다 직접 단언한다. 위기·알림 구절은 false
         ② ⚠ ①만으로는 "App이 그 함수를 실제로 부르는가"를 못 본다 →
           소스 단언을 하나 붙인다 (Shell/App의 useEffect가 그 함수를 쓴다)
         ③ 화면 종류 목록을 App.jsx의 분기와 **같은 상수**에서 가져온다.
           목록이 갈리면 새 화면이 조용히 광고 허용으로 들어온다
```

**③ 하단 요소와의 겹침**

```
떠 있는 것    FloatingRestart · FloatingBack
             ★ 둘이 common.jsx의 floatingStyle **하나**를 쓴다 (bottom: 24)
             → 한 곳만 고치면 둘 다 올라간다. .rise 밖 구조(4.8)는 건드리지 않는다
흐름 안의 것   "이 앱에 대해" 링크 (App.jsx styles.about — fixed가 아니다)
             About 하단 "돌아가기" 버튼 (styles.back)
             → 문서 **끝**에 있어 띠가 덮는다. Shell의 padding-bottom(지금 40)을
               띠 높이만큼 늘려 밀어 올린다
⚠ 조건부여야 한다 — 띠가 없는 화면에서 padding을 늘리면 빈 여백만 생긴다
```

**④ 목록 하단 여백과 스크롤 끝** — ③의 padding-bottom 조정으로 함께 풀린다.
띠 높이 + 안전 영역(제스처 바)만큼 늘린다. 마지막 항목이 영구히 가려지지 않는다.

**⑤ ads.js 정리 — 없애지 않고 웹 전용으로 명시한다**

```
그대로 둔다   BASE_SLOTS · adPositions · positionsWithin — 웹 자리 계산이다
            AD_SLOT = IS_APP ? "" : WEB_AD_SLOT — **앱에 AdSense가 없다**는 장치다
            appbuild.test.js의 adsbygoogle 0건 — 같은 이유로 유지
            verses.test.js의 24화면 회귀 — 웹 자리 계산 검사다. 유지
바꾼다       ads.js 머리에 "이 파일은 **웹 전용**이다"를 못박는다
            24화면 회귀의 이름·주석에 "웹"을 명시한다 — 앱에도 적용된다고
            오해할 자리다
새로 만든다   앱 전용 모듈 (예: adsApp.js) — bannerVisible(screen) 등
```

**⑥ 360px 위계**

```
띠는 **덮는 것**이지 레이아웃을 미는 것이 아니다(fixed).
실제로 줄어드는 것은 "한 번에 보이는 세로"이고, padding-bottom을 늘리면
스크롤 길이가 는다. → 색·여백 **위계 자체는 안 바뀐다.**
⚠ 그러나 **한 화면에 들어오는 층 수가 준다.** 결과 화면의
  "구절 → 이어서 읽기 → 영상" 세 층이 360×640에서 한 화면에 안 들어올 수 있다.
→ 2.36 규칙 5항 재측정이 필요하다. **에뮬레이터 AVD가 정확히 그 폭이다**(360×640dp)
```

---

**안 — 갈리는 지점은 형식 하나다. 나머지(②③④⑤)는 두 안이 같다.**

```
안 1 「정확한 100 · LARGE_BANNER」
  얻는 것  ★ 웹과 같은 320×100. 두 매체의 광고 크기가 같아 설명이 하나로 간다
          높이가 고정이라 padding-bottom·floating bottom을 **상수로** 잡을 수 있다
  잃는 것  ⚠ 360dp 화면에서 좌우 20dp씩 빈다. 태블릿에서는 크게 빈다
          ⛔ 구글이 권장하지 않는 형식이다 — 채움률·수익이 적응형보다 낮을 수 있다

안 2 「적응형 앵커 배너」
  얻는 것  기기 폭을 꽉 채운다. 구글 권장이라 채움률이 유리하다
  잃는 것  ⛔ **높이를 못 정한다.** "높이 100"이라는 지시를 만족하지 못한다
          높이가 기기마다 달라 하단 요소 조정을 **런타임 값**으로 해야 한다
          (SDK가 알려주는 높이를 받아 CSS 변수로 넘기는 배선이 하나 는다)
          ⚠ 태블릿에서 90dp 안팎이면 100보다 낮아 웹과도 다르다

안 3 「목록 안 네이티브 삽입」  ⛔ **못 한다.** 기록만 남긴다
  네이티브 뷰를 웹뷰 DOM 사이에 넣을 수 없다(2.53 ③). 오버레이는 스크롤에서
  좌표가 어긋나고 Play의 "Interfering with Apps" 범주에 걸린다
```

**✅ 확정 — 안 1 (LARGE_BANNER 320×100). 2026-08-31 사용자 결정.**

```
★ 조건 하나가 붙었다 — **광고가 채워지지 않으면 띠를 접는다.**
  빈 띠가 하단 100px을 먹는 것은 **광고 없이 자리만 차지하는 최악의 상태**다.
  웹에서 AD_SLOT이 비면 요소를 아예 안 그리기로 한 것과 같은 원칙이다.
  구현 — bannerSpace({screen, filled})가 **채워졌을 때만** 100을 준다.
        filled 기본값이 false다. 로드 실패·미채움이면 다시 0으로 접는다.
  ⚠ 접힐 때 하단 요소(떠 있는 버튼 · 문서 끝)도 **함께 내려와야 한다** —
    같은 값 하나로 구동하므로 구조적으로 따라온다. 실기기에서 확인할 것
```

**세션 의견(확정 전) — 안 1.** 근거 둘.
① 지시가 "높이 100"이고, **그것을 만족하는 형식은 하나뿐이다.**
② 높이가 상수면 ③④의 조정이 상수가 된다 — 런타임 높이를 CSS로 넘기는 배선이
사라지고, 그 배선은 **띠가 뜨기 전과 후에 레이아웃이 한 번 움직이는** 문제를 낳는다.
⚠ 대가(좌우 여백·낮은 채움률)는 실측으로만 재확인할 수 있다. 안 2로 바꾸는 것은
나중에도 가능하다 — 채널 설정과 달리 **되돌릴 수 있는 자리다.**

---

#### ⏱ 출시 후 실측 항목 — 채움률 (등재 · 2026-08-31)

```
무엇     LARGE_BANNER의 **채움률(fill rate)**
왜      ⚠ 구글이 권장하지 않는 형식이다. 적응형 앵커 배너보다 낮을 수 있다는 것이
        안을 고를 때 이미 나온 지적이고, **실측 없이는 크기를 알 수 없다.**

⛔⛔ 언제 — **④가 끝나기 전에는 재지도 말 것** (2026-09-02에 강화했다)
        ④ = AdMob에서 스토어를 연결하고 **준비성 심사를 통과**한 시점 (2.100)
        ★ 그 전까지 이 앱은 **「제한된 게재(limited ad serving)」**를 받는다.
          "스토어에 등재되지 않음"으로 만든 앱에 걸리는 것이고, 준비성 요건 셋
          (게시됨 · 스토어 등재 · AdMob 연결)이 전부 출시 이후에나 충족된다.
          심사는 2~3일. 출처 support.google.com/admob/answer/10564477
        ⛔ 그 상태에서 잰 낮은 채움률은 **형식 탓이 아니라 심사 탓이다.**
          ⚠⚠ 이 발견이 없었다면 낮은 수치를 보고 **형식 탓으로 판단해 안 2로
            옮겼을 것이다.** 옮긴 뒤에도 수치가 안 오르고, 그때는 원인이
            형식인지 심사인지 가릴 방법이 없다 — 변수를 둘로 만든 셈이 된다.
        ★ **재고 나중에 버리는 것보다 안 재는 것이 낫다.** 잰 숫자는 남고,
          남은 숫자는 인용된다(2.42가 같은 이유로 코퍼스 수치를 문서에서 뺐다).
낮으면   **안 2(적응형 앵커)로 옮긴다.**
        ★ 옮길 수 있는 자리다 — 채널 설정(2.94)이나 서명 키와 달리 되돌릴 수 있다.
        ⚠ 옮기면 높이가 런타임 값이 되어 하단 조정 배선이 하나 는다(2.96 ⑥)
⛔ 비교 대상을 만들어 두지 않았다 — 같은 앱에서 두 형식을 동시에 재지 못한다.
  판단은 절대 수치가 아니라 "이 정도면 바꿀 만한가"가 된다
```

---

#### ✅ 레이아웃 실측 — 360×640dp 에뮬레이터 (2026-09-02)

⛔ **소스를 한 줄도 안 건드렸다.** 실행 중인 웹뷰 콘솔(CDP)에서 DOM에 직접
`padding-bottom`과 가짜 띠를 넣어 보고 되돌렸다. 확인용 분기를 소스에 남기면
다음 사람에게 "여기는 임시 코드가 허용되는 자리"로 읽힌다(사용자 결정).

```
환경   AVD pym_phone_360 · 1080×1920 480dpi
      실측 뷰포트 **360 × 592 CSS px** (dpr 3) — 상태바·제스처바를 뺀 값이다
방법   adb forward + Runtime.evaluate. 앱 소스·번들 모두 그대로
```

**띠 있음(filled=true) — 주입값이 코드의 계산과 같다**

```
Shell padding-bottom   40px → **140px**   = calc(40px + 100px)
떠 있는 "다시 적기"      24px → **124px**   = 24 + bottomInset
띠                     320 × 100 · 가운데 정렬 · 뷰포트 360
```

✅ 확인된 것

```
· 스크롤 끝에서 **마지막 항목이 완전히 보인다** — 띠가 영구히 가리지 않는다
· 닫는 문구 · "다시 적어보기" · **"이 앱에 대해"가 전부 띠 위**에 있다
  → 문서 끝 요소가 padding으로 밀린다
· 떠 있는 버튼이 띠 바로 위로 올라간다 (floatingStyle 한 곳만 고쳤다)
· 접으면(filled=false) 넷이 **함께 내려온다** — padding 40px · 버튼 24px ·
  띠 제거. 하단 100px이 회수된다
```

⚠ 관찰 둘

```
① "이 앱에 대해"가 띠 바로 위에 아슬아슬하다. padding 140 중 띠가 100을 먹고
   남는 것이 40px인데, 그 아래 **제스처 바가 또 있다.** 겹치지는 않는다.
   ⛔ 지금 고치지 않는다 — 실기기에서 한 번 더 보고 판단할 것
② About 화면의 하단 "돌아가기"는 **영향을 받지 않는다.** 그 화면은 띠가 없어
   bottomInset이 0이기 때문이다(bannerVisible(ABOUT)=false).
   ★ 확인 항목이 설계상 무의미해진 자리다. 배선은 그대로 살아 있어
     허용 목록이 바뀌면 그때 동작한다
```

**✅ 좌우 여백 — 안 ① 확정 (배경 없이 그대로)**

```
360 − 320 = 좌우 20px씩 화면 배경이 비친다. 목록 항목의 썸네일 왼쪽 끝과
텍스트 오른쪽 끝이 띠 옆으로 보인다.
★ 근거(사용자) — 접었을 때 아무것도 안 남는 것이 이 결정의 핵심이고
  ②(배경 한 겹)는 배경 띠가 남을 여지를 만든다. 좌우 여백이 광고가 화면의
  일부가 아니라 **얹힌 것**임을 드러내는 것은 이 앱에서 정직한 표현이다
```

**⏸ 미검증 — 플러그인 뒤로 미룬다**

```
filled이 **실제 콜백으로** false로 돌아올 때 하단 요소가 함께 내려오는가
  지금 확인한 것은 DOM 값을 되돌린 것까지다. bannerAdFailedToLoad가
  실제로 오는 경로는 플러그인이 붙어야 볼 수 있다
전환 순간 한 프레임도 노출되지 않는가
  기본이 숨김이라 원리상 안전하지만, 브리지 왕복을 실측한 적은 없다
```

---

#### ⏱ 알림 지연 관찰 — 며칠 더 기록한다 (등재 · 2026-09-02)

```
09-01  예약 09:00 → 실제 09:42:25   **42분 늦음**   ← allowWhileIdle **false** 시절
09-02  예약 09:00 → 실제 09:01:54   2분 늦음        ← allowWhileIdle **false** 시절
둘 다 channel=morning_verse · 소리 재생됨 · 앱을 열지 않은 상태
────────────────────────────────────────────────────────────────
⛔⛔ **2026-09-03 · allowWhileIdle을 true로 바꿨다 (2.116 ①). 기준선이 여기서 갈린다.**
  · 위 두 값과 이후 값을 **같은 표에서 평균 내지 말 것**
  · 이후 값은 set(RTC) 이 아니라 setAndAllowWhileIdle(RTC_WAKEUP)의 값이다
  · ⚠ 이 관찰 기기(태블릿)는 배터리 세이버가 **꺼져 있었다**(low_power=0 실측).
    그래서 위 두 값은 세이버와 무관한 순수 절전 지연이다
  ★ 14일이 차기 전에는 결론을 적지 않는다 (사용자 지시 · 2026-09-03)
```

★ **의도된 결과다.** `allowWhileIdle: false` + `type=RTC`(WAKEUP 아님)라 화면이
꺼져 절전에 들어가면 깨어날 때까지 미뤄진다. 알람시계의 감각을 쓰지 않기로 한
결정(2.92)이 이 모양으로 나타난 것이다. **지금 결정을 바꾸지 않는다.**

⚠ 그러나 사용자에게는 **"아침 알림이 9시 42분에 오는 날"**이 된다.
편차가 2분~42분이라 한두 번으로는 예외인지 일상인지 가릴 수 없다.

```
할 일   며칠 더 기록한다. ⛔ 지금은 관찰만이다. 코드를 건드리지 않는다
보는 법  adb shell logcat -b events -d | grep notification_enqueue | grep peaceinmind
        ⚠ 이벤트 버퍼는 며칠이면 돈다. 날마다 찍어 두지 않으면 사라진다
기준    **대부분 30분 이상 늦는다면** 다시 볼 값이 있다
```

**★ 그때 검토할 것을 미리 적어둔다**

```
① SCHEDULE_EXACT_ALARM 없이 조정 여지가 있는가
   · setAndAllowWhileIdle — allowWhileIdle을 true로. Doze 중에도 깨운다.
     ⚠ 권한이 필요 없다. 지금 우리가 **명시적으로 false로 둔 값**이다
     ⚠ 시스템이 앱당 빈도를 제한한다(Doze에서 대략 15분에 한 번) —
       하루 한 번짜리에는 걸릴 이유가 없다
   · type을 RTC → RTC_WAKEUP으로. 기기를 깨워서 정시에 띄운다
   ⛔ 플러그인의 schedule 옵션으로는 allowWhileIdle만 만질 수 있다.
     RTC_WAKEUP은 allowWhileIdle이 true일 때 따라온다
     (LocalNotificationManager.setExactIfPossible 참조)
② 2.92의 결정과 어디까지 양립하는가
   "알람시계의 감각은 우리 것이 아니다"가 거절한 것은 **정확한 알람**
   (SCHEDULE_EXACT_ALARM · setExact)이다. 기기를 깨우는 것과 정확한 것은
   **다른 축이다.**
   ★ allowWhileIdle은 "정확히 09:00"을 주지 않는다 — 여전히 비정확 알람이고
     창이 있다. 다만 **절전 때문에 몇 시간 미뤄지는 것**을 막는다.
   → 양립한다고 볼 여지가 있다. 다만 판단은 실측 뒤에 한다
③ ⛔ 하지 않을 것 — SCHEDULE_EXACT_ALARM을 되돌리는 안
   2.92가 걷어낸 이유가 그대로다(스토어 권한 한 줄 · Android 14 신규 설치에
   자동 부여 안 됨 · schedule()이 멈추는 사고). 지연이 아무리 커도 이건 아니다
```

---

#### 🔴 알림 지연 기록 자동화 — 제안 (2026-09-02 · 승인 대기)

> ⛔ **구현하지 않았다.** 방식만 적는다.
> ★ 사람이 시켜서 찍는 방식은 잊힌다 — **오늘 아침에 겪은 병이 그것이다**(2.95).
> "매일 찍겠습니다"는 미래에 사람이 기억해야 하는 설계이고, 그것은 실패한다.

**⚠ 시점을 놓치면 그날 기록이 사라진다.** 이벤트 버퍼는 며칠이면 돈다
(2026-09-02 확인: 08-31 18:44부터만 남아 있었다 — 약 2.5일치).

**표본이 얼마나 필요한가**

```
지금    둘뿐이다 (42분 · 2분). ⛔ 아무 결론이 안 난다 —
       평균도 분산도 두 점으로는 의미가 없고, 42분이 예외인지 일상인지 못 가린다
최소    **7일.** 근거 셋
       ① 요일 주기가 한 바퀴 돈다. 주말은 기기를 안 쓰는 시간이 길어
         Doze가 더 깊게 들어간다 — 평일만 보면 낙관 쪽으로 치우친다
       ② "대부분 30분 이상"이라는 기준을 7개 표본이면 4/7 같은 형태로 말할 수 있다
       ③ 이벤트 버퍼가 2.5일이라 **매일 찍는 습관이 서는지**를 겸해서 본다
권장    **14일.** 알림 창이 14일이라 한 사이클과 맞고, 요일 효과를 두 번 본다
        ⚠ 7일에서 결론이 뚜렷하면(예: 6/7이 30분 초과) 그때 앞당겨 판단해도 된다
⛔ 그 전에는 결론을 적지 않는다. 표본이 적을 때 방향까지 틀린 사례가
  이 저장소에 셋 있다(3절 「일하는 방식」 ①)
```

**안 셋 — ⛔ 아직 고르지 않았다**

```
안 ㄱ  Windows 작업 스케줄러 + 수집 스크립트   ★ 권한다
  무엇   scripts/collect_notify_log.py (가칭)를 매일 1회 돌린다
        adb로 이벤트 로그를 뽑아 notification_enqueue · notification_alert ·
        오디오 플레이어 event:started 를 골라 **한 줄씩 누적**한다
  언제   ⚠ 09:00 알림을 보려면 **그 뒤**여야 하고, 버퍼가 도는 것보다 **자주**여야 한다
        → 하루 1회, 정오쯤. 지연이 최대 몇 시간이어도 잡힌다
  ⚠ 기기가 연결돼 있어야 한다. 안 붙어 있으면 그날은 못 찍는다 —
    스크립트가 "기기 없음"을 **기록으로 남긴다.** 빈 날과 못 찍은 날을 구분한다
  ★ 사람이 기억하지 않아도 된다. 그것이 이 안의 전부다

안 ㄴ  기기 안에서 로그를 파일로 흘린다 (logcat -f)
  ⚠ adb 세션이 끊기면 멈춘다. 재부팅에도 안 살아난다
  ⛔ 권한다 하지 않는다 — "돌고 있는 줄 알았는데 멈춰 있었다"가 가장 나쁜 실패다

안 ㄷ  세션이 매일 수동으로 찍는다
  ⛔ 지금 하고 있는 방식이고, 오늘 병의 이름이 그것이다
```

**🔴 저장 위치 — 판단이 필요하다**

```
안 A  저장소 안 (예: data/notify_delay.tsv)
  ★ 다음 세션이 자동으로 본다. HANDOFF에서 가리킬 수 있다
  ⚠ 관찰 데이터가 **매일 커밋에 섞인다.** 14일이면 커밋 14개가 늘고,
    git log가 그만큼 시끄러워진다
  ⚠ 이 저장소는 dist/·app/public/krv/를 .gitignore로 뺐다 —
    **생성물은 커밋하지 않는다는 태도**가 이미 있다. 관찰 데이터도 그쪽에 가깝다

안 B  스크래치패드에 두고 결론만 HANDOFF에   ★ 권한다
  ★ 태도가 일관된다 — 생성물은 저장소에 넣지 않는다
  ★ 커밋이 안 늘어난다. 판단이 끝나면 결론 한 절만 남는다
  ⚠ **스크래치패드는 세션마다 다르다.** 다음 세션이 못 본다
    → 그래서 고정 경로를 쓴다: D:/jaehyuk.myung/claude_demo/_obs/notify_delay.tsv
      (저장소 밖 · 세션과 무관 · 사용자 기기에 남는다)
  ⚠ 기록이 사라져도 아무도 모른다 → HANDOFF에 **경로와 시작일**을 적어 두고,
    결론을 적을 때 "며칠치를 봤는지"를 함께 적는다

안 C  저장소 안이되 .gitignore로 뺀다
  ⚠ 두 안의 나쁜 쪽만 남는다 — 저장소 안에 있는데 공유되지 않는다.
    다음 세션이 "있는 줄 알고" 찾다가 없는 상태가 된다
```

**세션 의견 — 안 ㄱ + 안 B.**

```
근거  ① 이 저장소는 생성물을 커밋하지 않는다. 관찰 로그는 생성물이다
     ② 판단에 쓰이는 것은 **결론**이지 원자료가 아니다. 결론은 HANDOFF에 남는다
     ③ 커밋 14개가 늘면 git log가 시끄러워지고, 그것이 다음 세션이 상태를
       읽는 비용이 된다
⚠ 대가 — 원자료가 저장소 밖이라 **다른 기기·다른 사람은 못 본다.**
  지금은 개인 개발자 한 명이고 기기도 하나라 그 대가가 0에 가깝다.
  ⛔ 팀이 되면 다시 볼 것
```

**기록 형식(제안)** — 한 줄에 하루, 탭 구분. ⛔ 사람이 읽으려고 만들지 않는다.

```
날짜        예약     실제        지연(분)  채널            소리   비고
2026-09-01  09:00   09:42:25    42       morning_verse   O     -
2026-09-02  09:00   09:01:54    2        morning_verse   O     -
2026-09-03  09:00   -           -        -               -     기기 미연결
```

---

#### ⚠ 콘솔 확인 목록 (옛 정렬 — **2.100절이 대체한다**)

> ⛔ **이 목록을 쓰지 말 것.** 화면 순서를 모르고 임의로 A/B/C로 나눈 것이다.
> 2.100절이 **앱 생성 전/후**를 기준으로 다시 정렬했고 항목도 셋 늘었다
> (C6 App ID 불변 · C7 사용자 측정항목 · B4 기본 언어).
> 아래는 그때의 기록으로만 남긴다.

#### 🔴 콘솔 확인 목록 — 화면 순서대로 (옛것)

> 세션이 확인할 수 없는 것만 적었다. 답을 주시면 문서의 전제를 닫는다.

```
[A] Play Console — 등록 흐름
  A1  개발자 계정 등록에서 **본인확인**이 어디에 나오고 얼마나 걸린다고 안내하는가
  ✅ A2·A3는 **닫혔다** (2026-09-04) — Play 앱 서명이 기본 적용이고
     앱 서명 키가 이미 「사용 중」이다. 우리가 고를 것이 없었다.
     🔴 남은 것은 **업로드 키 생성**이다 (13절 「앱 서명 키」 4~5항)
  A4  업로드 형식이 **AAB 필수**로 나오는가, APK도 받는가

[B] Play Console — 앱 콘텐츠
  B1  "광고" 항목이 어디에 있는가. 지금 답은 **"광고 포함"**이다
      ⚠ 2.54 f의 기록과 같지만 이유가 다르다 — 그때는 AdSense, 지금은 AdMob
  B2  "데이터 보안(데이터 안전)" 양식의 위치와, **"기기 또는 기타 ID"** 항목이
      수집/공유 양쪽에 있는지
  B3  "타겟층 및 콘텐츠"에서 아동 대상 설정이 무엇으로 잡혀 있는가
      → 방침 5절이 "아동 대상 아님"이므로 그것에 맞춘다

[C] AdMob
  C1  ★ **인증 전에 광고가 실제로 나가는가.** 앱·광고 단위를 만든 뒤
      "광고 게재 준비 완료" 같은 상태 표시가 있는지, 보류 문구가 붙는지
  C2  앱 추가 화면에서 **플랫폼(Android/iOS)** 과 "앱이 스토어에 등록돼 있는가"를
      묻는다. ⛔ 아직 등록 전이므로 "아니오"로 두는 선택지가 있는지 확인
  C3  광고 단위 만들기에서 형식 목록에 **배너**가 있고, 크기를 고르는 자리가
      있는가 (LARGE_BANNER 320×100을 지정하는 것은 코드 쪽이지만, 콘솔이
      형식을 제한하는지 본다)
  C4  앱별 **아동 대상(TFCD/TFUA)** 설정이 어디에 있는가
  C5  AdSense 계정과 공유된다면 **지급 정보·세금 정보가 이미 채워져 있는가**
```

---

### 2.97 ★ (나) Capacitor AdMob 통합 조사 — 🔴 자료 (2026-09-02 · 승인 대기)

> ⛔ **설치하지 않았다.** npm 레지스트리 조회와 공개 문서 열람만 했다.
> 자료의 출처를 값마다 적었다 — 조사와 화면 확인을 섞지 않는다.

#### ① 플러그인 상태 — ✅ 우리 스택과 정확히 맞는다

```
패키지     @capacitor-community/admob@8.1.0
최근 배포   2026-08-14 (조사 시점 3주 전)  — 관리되고 있다
peer      dependencies: @capacitor/core ^8.0.0
          ★ 우리가 8.5.0이다. **메이저가 맞는다**
engines   node >= 22   (우리 24.14.1)
크기       배포본 489KB (JS + 네이티브 소스. APK 증가분은 아니다 — ② 참조)
저장소     github.com/capacitor-community/admob
⚠ 커뮤니티 플러그인이다. Capacitor 공식이 아니다 — 메이저 업그레이드 때
  이 플러그인이 따라오지 못하면 우리가 묶인다. @capacitor/local-notifications는
  공식이라 그 위험이 없었다. **새로 생기는 의존이다**
```

**⛔ SDK 요구선이 우리와 정확히 같다 — 바꿀 것이 없다**

```
플러그인 android/build.gradle   minSdk 24 · compile 36 · target 36
우리 variables.gradle          minSdk 24 · compile 36 · target 36
Google 문서 요구선              minSdk 23 이상 · compileSdk 35 이상
→ 셋 다 만족한다. variables.gradle을 손댈 이유가 없다
```

#### ② 네이티브 의존성과 용량

```
com.google.android.gms:play-services-ads:25.4.+     광고 SDK 본체
com.google.android.ump:user-messaging-platform:4.0.0  ★ 동의 SDK (아래 참조)
androidx.appcompat 1.7.1 · coordinatorlayout 1.3.0 · core-ktx 1.15.0
org.jetbrains.kotlin:kotlin-stdlib 2.2.20
⚠ appcompat·coordinatorlayout·kotlin-stdlib는 Capacitor가 이미 끌고 있다 —
  새로 더해지는 것은 사실상 play-services-ads와 UMP다
```

⚠ **APK 증가분은 지금 말할 수 없다.** 라이브러리 크기는 R8 축약·리소스 병합에
따라 달라지므로 **빌드해서 재는 것 말고 방법이 없다.** 지금 APK가 5.13MB이고,
붙인 뒤 같은 방식으로 재서 차이를 적는다. ⛔ 추정치를 문서에 박지 않는다
(2.43이 없앤 표류와 같은 종류다).

**★ UMP가 딸려 온다 — 보류 항목 하나와 닿는다**

```
user-messaging-platform은 **구글의 인증 CMP**다. 보류 표의 「GDPR·CMP 문구」가
"유럽 출시를 결정할 때는 문구가 아니라 인증 CMP 구축이 먼저다"로 닫혀 있는데,
AdMob을 붙이면 **그 SDK가 이미 앱 안에 들어온다.**
⚠ 들어오는 것과 **구성하는 것**은 다르다. 동의 양식은 AdMob 콘솔에서 만들고
  코드에서 불러야 동작한다. 그래도 "구축"의 비용이 크게 준다.
→ 🔴 유럽 출시 판단을 다시 볼 재료가 생겼다. 지금 결정하지 않는다
```

#### ③ APPLICATION_ID — 어디에 넣고, 계정 없이 붙일 수 있는가

**넣는 자리는 두 곳이다** (플러그인 README)

```
android/app/src/main/AndroidManifest.xml   <application> 안
    <meta-data android:name="com.google.android.gms.ads.APPLICATION_ID"
               android:value="@string/admob_app_id" />
android/app/src/main/res/values/strings.xml
    <string name="admob_app_id">…</string>
⛔ 빠뜨리면 앱이 시작하자마자 죽는다 — "Missing application ID" (Google 문서)
★ 값을 strings.xml로 뺀 구조라 **매니페스트를 건드리지 않고 ID만 갈아 끼울 수 있다**
```

**★★ 계정 없이 붙일 수 있다 — 미검증 둘이 지금 닫힌다**

```
샘플 App ID    ca-app-pub-3940256099942544~3347511713
배너 데모 단위   ca-app-pub-3940256099942544/9214589741
출처          developers.google.com/admob/android/{quick-start,test-ads}
★ 구글 문서가 명시한다 — "이 광고 단위는 당신의 AdMob 계정과 연결되지 않으므로
  무효 트래픽이 발생할 위험이 없다." **계정도 앱 등록도 필요 없다.**
⛔ 출시 전에 실제 ID로 바꿔야 한다. 문서가 못박는다
⚠ 플러그인에도 initializeForTesting · isTesting 옵션이 따로 있다 —
  데모 단위와 별개다. 실기기를 테스트 기기로 등록하는 쪽이다
```

**→ 인증 2주를 기다리지 않고 지금 검증할 수 있는 것**

```
✅ filled 콜백 (bannerAdLoaded / bannerAdFailedToLoad)이 실제로 오는가
✅ 전환 순간 한 프레임도 노출되지 않는가 — 위기·알림 화면으로 오가며 본다
✅ APK 증가분 실측
✅ AD_ID 권한이 실제로 병합되는가 (아래 ⑤)
⛔ 못 여는 것 — 채움률. 데모 단위는 항상 채워진다. 안 1의 실측 항목은 그대로 남는다
```

#### ④ "앱은 AdMob, 웹은 AdSense"로 다시 나누는 설계

**지금 앱에 광고가 없다는 것을 강제하는 장치 셋**

```
ads.js            AD_SLOT = IS_APP ? "" : WEB_AD_SLOT
appbuild.test.js  앱 산출물에 adsbygoogle · googlesyndication 0건
About.jsx         광고 절이 AD_SLOT에 묶여 있다 (값이 없으면 문단을 안 그린다)
```

⛔ **앞의 둘은 그대로 둔다.** 그것들이 말하는 것은 "앱에 광고가 없다"가 아니라
**"앱에 AdSense가 없다"**이고, AdSense 프로그램 정책이 앱 안의 AdSense를
금지하는 것(2.52)은 AdMob이 들어와도 변하지 않는다.
★ 오히려 **AdMob을 붙인 뒤에 더 중요해진다** — 앱에 광고가 생기면 "여기 AdSense도
켜면 되지 않나"가 실제 선택지처럼 보이는 순간이 온다. 그때 걸릴 것이 이 둘이다.

**🔴 셋째만 고쳐야 한다 — 지금 구조로는 앱에서 광고 문단이 영영 안 그려진다**

```
지금   {AD_SLOT ? <광고 절> : null}    앱에서 AD_SLOT이 "" 이므로 항상 null
       ⚠ 그런데 앱에는 이제 광고가 뜬다 → **고지가 사라진 채 광고만 나간다**
```

안 셋. ⛔ 아직 고르지 않았다.

```
안 A  "광고를 그리는가"를 매체별 값 하나로 모은다
     ads.js에 adsEnabled = IS_APP ? APP_ADS_ON : Boolean(WEB_AD_SLOT)
     About은 그 값 하나만 본다
     ★ About이 매체를 몰라도 된다 — 지금과 같은 모양을 유지한다
     ⚠ APP_ADS_ON을 무엇으로 둘지가 남는다(상수? 광고 단위 ID 유무?)
안 B  About이 두 값을 직접 본다 — {(AD_SLOT || IS_APP) ? … }
     ⚠ 판단이 화면에 들어간다. adsApp.js를 만들며 피한 모양이다
안 C  앱 광고 문단을 따로 쓴다 (웹은 "영상 목록에", 앱은 "화면 아래에")
     ★ 정확하다 — 배치가 실제로 다르다
     ⚠ 확정 문구 회귀가 둘로 갈린다. 문장이 늘면 어긋날 자리도 는다
```

⚠ 어느 안이든 **verses.test.js의 확정 문구 회귀**를 함께 봐야 한다. 지금
"About 광고 문단은 광고가 실제로 나갈 때만 그려진다"가 AD_SLOT에 묶여 있다.

#### ⑤ AD_ID 권한이 「앱」 블록에 반영되는가 — ⚠ 반영되지 않는다

```
Google 문서   SDK 20.4.0 이상은 com.google.android.gms.permission.AD_ID를
             **자동으로 선언한다**(매니페스트 병합). 우리가 쓸 것은 25.4다
⛔ 그런데 stats.py의 「앱」 블록은 **우리 매니페스트만 읽는다.**
  플러그인이 병합해 넣는 것은 보지 않는다 — 그렇게 설계했다
  (커밋된 파일만 읽어야 --check가 같은 커밋에서 같은 답을 낸다)
→ **AD_ID가 늘어도 블록의 권한 줄은 그대로다.** 흐름이 작동하지 않는다
```

🔴 판단 셋. ⛔ 아직 고르지 않았다.

```
안 ㄱ  우리 매니페스트에 AD_ID를 **명시적으로 선언한다**
      ★ 블록이 잡는다. 스토어 권한 목록에도 우리가 의도한 것으로 남는다
      ⚠ 병합으로도 어차피 들어오므로 **중복 선언**이다. 다만 무해하다
      ★ 2.92에서 SCHEDULE_EXACT_ALARM을 tools:node="remove"로 **명시적으로
        걷어낸 것과 대칭이다** — 권한은 우리 매니페스트에서 다룬다는 태도
안 ㄴ  블록이 병합 결과(빌드 산출물)를 읽게 한다
      ⛔ 권한다 하지 않는다. node_modules·빌드 산출물을 읽으면 같은 커밋에서
        같은 답이 안 나오고, --check가 게이트로 성립하지 않는다
안 ㄷ  그대로 두고 APK로만 확인한다 (2.92가 한 방식)
      ⚠ 사람이 기억해야 한다. 오늘 여러 번 실패한 방식이다
```

**세션 의견 — 안 ㄱ.** 근거: 이 저장소는 이미 **권한을 우리 매니페스트에서
다루기로** 했다(SCHEDULE_EXACT_ALARM 제거). 병합에 맡기면 권한 목록이
"어디서 왔는지 모르는 것"이 되고, 그것이 오늘 반복된 병의 모양이다.
중복 선언의 비용은 0이다.

---

### 2.98 ★ AdMob 부착 검증 — 데모 단위로 미검증 둘을 닫았다 (2026-09-02)

⛔ 계정 인증을 기다리지 않았다. 구글 데모 단위는 계정도 앱 등록도 필요 없다(2.97 ③).

```
플러그인    @capacitor-community/admob@8.1.0
App ID     ca-app-pub-3940256099942544~3347511713   (구글 샘플)
배너 단위   ca-app-pub-3940256099942544/9214589741   (구글 데모)
기기       AVD pym_phone_360 · 360×640dp · API 35
⛔ 두 ID 모두 **저장소에 없다** — 아래 「App ID의 성질」 참조
```

#### ✅ 닫힌 것

**① filled 콜백이 실제로 온다**

```
채워짐    bannerAdLoaded → padding 40px → **140px** · 떠 있는 버튼 24 → **124px**
실패     비행기 모드에서 bannerAdFailedToLoad 발생(logcat 확인) → padding **40px 유지**
        "Error while connecting to ad server: Unable to resolve host"
        "Notifying listeners for event bannerAdFailedToLoad"
```

⚠ **true → false 왕복은 못 봤다.** 한 번 채워진 배너가 접히는 경로는 사실상 없다 —
자동 새로고침은 AdMob 콘솔 설정이고 우리 코드는 재요청을 하지 않는다(`resumeBanner`만).
★ 그래서 접힘이 실제로 쓰이는 자리는 **첫 로드 실패**이고, 그것은 확인했다.

**② ★ 전환 순간 한 프레임도 노출되지 않는다 — 사람 눈이 아니라 녹화로 쟀다**

```
방법   adb screenrecord로 전환을 녹화하고 ffmpeg으로 프레임을 뜯어
      프레임마다 (가) 배너 자리의 밝은 픽셀 비율 (나) 화면 내용 지문을 쟀다
      ⛔ 눈으로 보지 않았다 — 사람 눈은 한 프레임을 못 잡는다

frame  배너 자리 밝기   내용 변화량
    1        0.698            0     ← 결과 화면. 띠 있음
    2        0.056            6     ← 띠가 이미 사라졌다
    3        0.000         1070     ← **화면 내용이 바뀐 프레임**. 띠 없음
→ 노출된 프레임 0개. ★ 띠가 화면 내용보다 **먼저** 사라졌다
```

⚠ 한계 둘. 결론을 이보다 세게 말하지 말 것.

```
① 녹화가 약 10fps였다(62프레임 / 6.4초). **100ms보다 짧은 노출은 못 잡는다.**
   다만 관측된 것은 "노출이 없다"보다 강하다 — 띠가 내용보다 **먼저** 사라졌다
② 잰 전환은 결과 → 입력이다. 위기·알림 화면 진입의 **대리**다
   ★ 그런데 실제 위험 경로는 하나뿐이다 —
     결과 → 위기는 입력 화면을 거치므로(다시 적기 → 입력 → 로딩 → 위기)
     띠가 이미 몇 초 전에 숨는다. **위험한 전환이 아니다.**
     결과 → 알림 구절만이 직행이고(알림 탭), 그것은 알림이 와야 재현된다
```

**③ APK 증가분 실측**

```
기준선(플러그인 없음)   debug  5,133,131 bytes
플러그인 부착 후        debug 12,430,536 bytes   → +7,297,405 (+6.96 MiB · **+142%**)
                     release 8,084,583 bytes (7.71 MiB)
⚠ release **기준선은 재지 않았다** — 증가분은 debug끼리의 비교다
⛔ 추정치를 적지 않은 것이 맞았다. 앞서 "2~3MB"로 어림했는데 실측은 7MB다
★ minifyEnabled가 release에서도 **false**다(Capacitor 기본값).
  R8을 켜면 크게 줄 여지가 있으나 리플렉션이 걸린 SDK라 위험이 따른다.
  🔴 출시 전 판단 항목으로 남긴다
```

**④ AD_ID 병합 — 명시 선언이 작동한다. 그리고 하나가 더 붙는다**

```
APK 실측(aapt2)      INTERNET · AD_ID · RECEIVE_BOOT_COMPLETED · WAKE_LOCK · POST_NOTIFICATIONS
설치 후 dumpsys      위에 더해 **android.permission.ACCESS_ADSERVICES_AD_ID**
✅ 우리 명시 선언과 SDK 병합이 충돌하지 않는다 — AD_ID가 한 번만 나온다
⚠ **ACCESS_ADSERVICES_AD_ID는 「앱」 블록이 못 잡는다.** SDK가 병합해 넣고
  우리 매니페스트에 없기 때문이다. ⑤ ㄱ이 AD_ID는 잡지만 이것은 놓친다.
  🔴 이것도 명시 선언할지는 따로 판단할 것 — 지금 결정하지 않는다
```

#### ★ 배너 실측 — LARGE_BANNER가 그대로 적용된다

```
스크린샷 픽셀 측정   세로 300px = **100.0dp** · 가로 960px = **320.0dp** (dpr 3)
광고 요청 URL       format=320x100_as
좌우 여백          20dp씩 (360 − 320) — 안 ①대로 배경이 비친다
⚠ 크리에이티브 문구가 "AdMob Adaptive Banner"라 적응형처럼 보이지만
  그것은 **광고 소재의 글자**일 뿐이다. 컨테이너는 우리가 지정한 320×100이다
```

#### ⚠⚠ App ID의 성질 — 가장 위험한 자리다

```
★ 값이 없거나 형식이 틀리면 **앱이 시작하자마자 죽는다.**
  광고가 안 나오는 것이 아니라 앱이 죽는다("Missing application ID").
  JS가 광고를 안 켜도 같다 — Mobile Ads SDK가 앱 시작 시점에 매니페스트의
  meta-data를 읽기 때문이다.
⛔ 그래서 빈 값으로 가지 않게 했다 — **빌드가 선다.**
```

**저장 방식** — 값은 저장소에 없다.

```
app/android/admob.properties          ⛔ .gitignore. 값이 여기 있다
app/android/admob.properties.example  ✅ 커밋. 형식과 경고만
build.gradle이 읽어 resValue로 admob_app_id를 만들고 매니페스트가 그것을 가리킨다
⚠ 새로 clone하면 파일이 없어 **빌드가 선다.** 그것이 의도다 —
  메시지가 무엇을 하라고 말한다(3절 실행 환경에 옮겨 적었다)
✅ 형식 검사도 있다 — 광고 단위 ID(/ 구분)를 App ID 자리에 넣으면 잡는다.
  가장 흔한 혼동이라 메시지에 그 문장을 넣었다
```

**🔴 실제 단위로 교체할 때의 절차 — 미리 정해 둔다**

```
1  AdMob 콘솔에서 앱 등록 → App ID(~ 구분)와 배너 단위 ID(/ 구분)를 받는다
2  admob.properties의 admob.appId를 실제 App ID로 바꾼다
   ⚠ 광고 단위 ID를 넣지 말 것. 형식 검사가 잡지만 먼저 확인하는 편이 빠르다
3  ADMOB_BANNER_ID 환경변수를 실제 배너 단위로 바꿔 sync:app
4  ⛔ **설치해서 앱이 뜨는지부터 본다.** 광고가 나오는지가 아니라 **앱이 죽지 않는지**다
   App ID가 틀리면 여기서 즉시 죽는다. 광고 유무는 그다음이다
5  APK의 assets에 데모 ID가 남아 있지 않은지 본다
   python으로 zip을 열어 "3940256099942544"를 찾는다(이 절의 방법 그대로)
6  logcat에서 bannerAdLoaded를 확인한다
```

**🔴 릴리스에 데모 ID가 들어가는 것을 막을 장치가 필요한가 — 판단이 필요하다**

```
지금 막는 것   ⛔ 없다. 데모 ID는 gitignore된 파일과 환경변수에만 있어
             **저장소에는 안 남지만**, 로컬에서 그대로 release를 말면 들어간다
필요한가      ★ 세션 의견 — **필요하다.** 근거 셋
             ① 이 값은 틀려도 **화면이 멀쩡하다.** 데모 광고가 잘 나온다 —
               조용한 실패의 전형이고, 오늘만 두 번 겪었다
             ② 6단계 절차 중 5번이 사람의 기억에 기대는 자리다
             ③ 검사가 싸다 — release 산출물에 "3940256099942544"가 있으면 실패
어디에        appbuild.test.js가 이미 앱 산출물을 전수 검사한다. 같은 자리다
             ⚠ 다만 그 테스트는 dist-app(웹 자산)을 본다. App ID는 APK 안의
               resValue라 **거기서는 안 보인다.** 두 곳을 봐야 한다
✅ **만들었다 — 2.99절**(2026-09-02 승인). assembleRelease·bundleRelease를 함께 막는다
```

---

### 2.99 ★ 릴리스에 데모 광고 ID가 드는 것을 막는다 (2026-09-02)

**왜 장치가 필요했나 — 이 값은 틀려도 화면이 멀쩡하다.**
데모 ID를 그대로 두고 출시해도 **광고가 잘 나온다.** 구글의 시험 광고가 뜨기 때문이다.
수익만 0이고 아무 신호가 없다 — 3절 「일하는 방식」 ⓪-2의 "실패가 성공처럼 보이는" 자리다.
⚠ 반대로 값이 **없거나 형식이 틀리면** 앱이 즉시 죽는다(2.98). **두 실패의 모양이
정반대라 장치도 둘이다** — 형식 검사는 빌드 구성 단계, 데모 검사는 릴리스 태스크다.

**어디서 잡는가 — 테스트가 아니라 gradle의 릴리스 경로**

```
⛔ npm test로는 못 잡는다   데모 ID는 **debug에서는 있어야 정상**이고
                        build:app에는 debug/release 구분이 없다.
                        테스트로 넣으면 데모로 시험할 때마다 게이트가 깨진다
⛔ appbuild.test.js도 반쪽  dist-app(웹 자산)만 본다 → 배너 단위는 잡아도
                        **App ID는 못 본다**(APK의 resValue다)
★ gradle                릴리스 산출물을 만드는 것은 로컬 gradle뿐이다.
                        막아야 할 자리가 거기다
```

**★ 두 값이 한 문자열로 잡힌다** — 데모 App ID와 데모 광고 단위가 같은 퍼블리셔를 쓴다.

```
샘플 App ID    ca-app-pub-3940256099942544~3347511713
데모 배너 단위   ca-app-pub-3940256099942544/9214589741
                          ^^^^^^^^^^^^^^^^ 이 하나로 둘 다 걸린다
```

**★ assembleRelease와 bundleRelease를 함께 막는다.**
Play가 받는 것은 **AAB**다 — assemble만 막으면 정작 올리는 산출물이 안 걸린다
(사용자 지적. 설계에서 빠질 뻔한 자리다).

**✅ 만든 뒤 실제로 돌려 확인했다** (⓪-2 규칙)

```
assembleRelease  데모 상태  → ❌ 실패 · 문제 [1][2]를 **함께** 짚는다
bundleRelease    데모 상태  → ❌ 실패 (`Task :app:verifyNoDemoAdIds FAILED`)
assembleDebug    데모 상태  → ✅ 통과 — 데모로 시험하는 흐름이 살아 있다
assembleRelease  실제 값    → ✅ 통과
```

**메시지가 무엇을 말하는가** — 이 게이트가 걸리는 순간은 출시 직전이고 그때 사람은 급하다.

```
· 어느 값인가        [1] App ID  /  [2] 배너 단위 — 번호로 나눈다
· 지금 값이 무엇인가   App ID는 실제 값을 찍어 준다
· 어디를 고치는가     [1] android/admob.properties의 admob.appId
                   [2] 환경변수 ADMOB_BANNER_ID
· 무엇으로 바꾸는가   ~ 로 구분되는 값 / 빗금으로 구분되는 값 — 혼동을 콕 집는다
· ⚠ 다시 하는 법     "sync를 다시 해야 번들이 바뀝니다. gradle만 다시 돌리면 그대로입니다"
· 고친 뒤 확인       "설치해서 **앱이 뜨는지부터** 봅니다(App ID가 틀리면 즉시 죽습니다)"
★ 둘 다 틀렸으면 **둘 다 보여 준다.** 하나 고치고 다시 돌려 또 걸리는 일이 없다
```

**⚠ 남는 구멍 둘 — 이 게이트를 믿고 넘어가지 말 것**

```
① `cap sync` 없이 gradle만 도는 경우
   검사는 src/main/assets/public을 본다. sync를 안 하면 **옛 자산을 검사한다.**
   ⛔ 그러나 이것은 **이 장치와 무관하게 이미 있던 위험이다** —
     sync를 안 하면 애초에 옛 앱이 빌드된다. 이 게이트가 새로 만든 구멍이 아니고,
     이 게이트로 없앨 수 있는 것도 아니다.
   ★ 구분을 남기는 이유 — 나중에 "게이트가 있으니 괜찮다"로 읽는 것을 막는다.
     게이트는 **이번 빌드에 든 것**을 볼 뿐, 빌드가 최신인지는 모른다
② 사람이 gradle을 우회해 예전 APK/AAB를 올리는 경우
   ⛔ 어떤 장치로도 못 막는다. 절차(2.98의 교체 6단계)가 유일한 방어다
```

---

### 2.100 ★ 등록 절차 — AdMob 앱 · Play 앱 (2026-09-02 · ✅ AdMob 등록 완료 · ⛔ Play 미등록)

> ⛔ **절차 정리다.** 등록은 사용자가 화면을 보고 한다.
> 출처를 항목마다 적었다 — 공개 문서에서 확인한 것과 화면에서 봐야 할 것을 섞지 않는다.

#### ★ 선후 — AdMob이 먼저다 (2026-09-02 확정 · 사용자)

```
근거   내부 테스트에 올릴 **AAB도 릴리스 빌드**이고, 2.99가 데모 ID가 든
      릴리스를 막는다. 그래서 **실제 App ID와 배너 단위가 먼저 있어야**
      Play에 올릴 산출물을 만들 수 있다.
⚠ 반대 방향의 의존은 없다 — AdMob 앱 등록은 Play의 값을 하나도 안 묻는다
  ("스토어에 등록되지 않음"을 고를 수 있다).
```

**전체 사슬 — 네 토막이고 중간에 기다림이 둘이다**

```
✅ ① AdMob 앱 등록 (스토어에 없음)   → App ID · 배너 단위 ID를 받았다 (사용자)
✅ ② 실제 값으로 빌드               → 2.101에서 6단계 전부 실측으로 닫았다
⛔ ③ Play 앱 만들기 → 내부 테스트 → 출시
     ⚠ 그 앞에 **업로드 서명 키**가 있다(A2) — 지금 release 산출물은 서명이 없다
⛔ ④ AdMob에서 **스토어 연결** → 심사  → 제한 해제

⏳ 기다림 1   AdMob 계정 인증 (최대 2주 · 진행 중)
⏳ 기다림 2   앱 준비성 심사 **2~3일** (④ 이후. 출처: support.google.com/admob/answer/10564477)
```

⚠⚠ **①~③ 동안 광고가 온전히 나가지 않는다.**

```
AdMob 문서가 못박는다 — "스토어에 등록되지 않음"으로 만든 앱은
**승인 전까지 제한된 게재(limited ad serving)**를 받는다.
그리고 준비성 요건 셋은 전부 출시 이후에나 충족된다 —
  ① 앱이 게시돼 있을 것  ② 지원되는 스토어에 등재돼 있을 것  ③ AdMob에서 연결돼 있을 것
★ 그러므로 **첫 출시본은 광고가 안 차거나 덜 차는 상태로 나간다.** 정상이다.
⛔ 그것을 보고 "LARGE_BANNER 채움률이 낮다"고 판단하지 말 것 —
  2.96의 채움률 실측 항목은 **④가 끝난 뒤에** 시작한다
```

---

#### ㉮ AdMob 앱 등록

**화면이 묻는 것** (출처: support.google.com/admob/answer/9989980)

```
1  플랫폼                    → **Android**
2  "지원되는 앱 스토어에 등재돼 있습니까?"
                            → ⛔ **아니요**. Play에 아직 앱이 없다
3  앱 이름                   → **Peace in Mind**
                            ⚠ 표시 이름과 같게 둔다. AdMob 안에서만 쓰는 이름이지만
                              다르면 나중에 어느 앱인지 대조가 어려워진다
4  사용자 측정항목(User metrics) 사용 여부
                            → 🔴 **판단 필요.** 아래 참조
```

**✅ 사용자 측정항목 — 켜지 않는다 (2026-09-02 확정 · 사용자)**

```
무엇     AdMob이 앱의 사용 지표(세션·유지율 등)를 수집해 보여 준다
결정     ⛔ **끈다.**

근거    ① 방침 1절이 "이용 분석 도구를 하나도 넣지 않았습니다"다.
          켜면 **그 문장이 거짓이 된다.**
        ② ★ 이 앱은 **감정 상태를 입력받고 아무것도 전송하지 않는다**는 것이
          사용자에게 하는 약속의 핵심이다. 광고는 방침에 적고 화면에 고지도
          붙였지만, **분석은 안 넣기로 한 것**을 지킨다.
        ③ 수익 최적화의 이득이 그 약속을 깨는 값보다 크지 않다.

⛔⛔ **"켜면 수익이 오른다"는 이유로 다시 열지 말 것.**
  그것이 이 결정을 뒤집을 근거였다면 애초에 켰을 것이다. 위 ③이 그 값을
  이미 비교한 결과다. 다시 열려면 **약속 쪽이 바뀌어야 한다** —
  방침 1절을 고치고, 그것을 사용자에게 고지하는 일이 먼저다.
  ⚠ 이 앱에서 방침 문구를 고치는 것은 배포된 문서를 고치는 일이다(2.95).
★ 끄고 시작해도 광고는 나간다. 잃는 것은 우리가 안 보던 지표뿐이다
```

**출시 후 스토어 연결 — ★ ID는 안 바뀐다**

```
절차   출시 뒤 AdMob으로 돌아와 앱을 **스토어 등록정보에 연결한다**
       (출처: support.google.com/admob/answer/9989980)
★ 연결이 App ID를 바꾸지 않는다 → **다시 빌드하지 않아도 된다**
⚠ 문서의 서술이 "패키지명/스토어 ID 변경"과 App ID를 구분해 다룬다는 데서 읽은 것이다.
  🔴 **연결 화면에서 App ID가 그대로인지 눈으로 확인할 것** — 바뀌면 재빌드다
  (이 한 줄이 콘솔 확인 목록 C6이다)
```

**광고 단위 만들기**

```
형식     **배너(Banner)** 하나만 만든다
★ 콘솔은 **크기를 묻지 않는다.** 배너 크기는 코드에서 정한다
  (출처: developers.google.com/admob/android/banner/anchored-adaptive)
  → 우리가 LARGE_BANNER 320×100을 쓰는 것은 **콘솔에 드러나지 않는다.**
    적응형/고정의 구분도 콘솔에 없다. 단위 하나가 어느 크기든 받는다
⚠ 그래서 나중에 안 2(적응형)로 옮겨도 **광고 단위를 새로 만들 필요가 없다.**
  코드 한 줄이다 — 2.96이 "되돌릴 수 있는 자리"라고 한 근거가 이것이다
```

**✅ 광고 단위 이름 — `pym-banner-bottom` 확정 (2026-09-02 · 사용자)**

```
쓴다     `pym-banner-bottom`   (무엇이 · 어디에)
피한다   `pym-large-banner-320x100`
근거    ★ 위에서 본 대로 **크기는 콘솔에 없다.** 이름에만 박으면
        코드는 적응형으로 갔는데 이름은 320×100인 상태가 생긴다 —
        오늘 일곱 번 본 "문서가 코드보다 뒤에 서는" 모양 그대로다
        ⚠ 이름은 **콘솔에서 나중에 바꿀 수 있지만**, 바꿔야 한다는 것을
          아무도 알려주지 않는다. 처음부터 안 박는 편이 싸다
★ 자리(bottom)는 박아도 된다 — 그것은 우리가 바꾸지 않기로 한 것이다(2.96 ②)
```

**받은 값을 어디에 넣는가** — 2.98의 교체 6단계로 이어진다.

```
App ID (물결 ~)     android/admob.properties  의  admob.appId
배너 단위 (빗금 /)   환경변수 ADMOB_BANNER_ID → npm run sync:app
⛔ 둘을 바꿔 넣지 말 것. 형식 검사가 잡지만 메시지를 읽는 시간이 아깝다
✅ 그 뒤 release를 말면 2.99가 데모 잔존을 막는다 — 통과하면 값이 바뀐 것이다
```

---

#### ㉯ Play Console 앱 만들기

**화면이 묻는 것** (출처: support.google.com/googleplay/android-developer/answer/9859152)

```
1  기본 언어(default language)   → **한국어**
                                 ⚠ 나중에 바꿀 수 있는지 문서가 말하지 않는다.
                                   🔴 화면에 안내가 있는지 볼 것
2  앱 이름                       → **Peace in Mind**   ✅ 나중에 바꿀 수 있다(문서 명시)
3  앱 / 게임                     → **앱**              ✅ 나중에 바꿀 수 있다(문서 명시)
4  무료 / 유료                   → **무료**            ⛔⛔ 아래 참조
5  지원 연락처 이메일             → appleap385@gmail.com
                                 ★ 앱·방침과 같은 주소다(회귀가 지킨다)
6  선언 3종                      개발자 프로그램 정책 · 미국 수출법 ·
                                 **Play 앱 서명 약관**
                                 ★ 6번이 A2와 닿는다 — 여기서 이미 동의하게 된다
```

**⛔⛔ 되돌릴 수 없는 자리 — 셋**

```
① 패키지명   io.github.appleap385.peaceinmind
            이미 굳었고 바꿀 수 없다. **삭제해도 재사용 못 한다**(문서 명시)
② 무료 → 유료   ⛔ **불가능하다.** 문서 원문 —
            "You can change your app from paid to free. Once your app has been
             offered for free, the app can't be changed to paid."
            (출처: support.google.com/googleplay/android-developer/answer/6334373)
            → 유료로 갈 길을 남기려면 **새 앱을 새 패키지명으로** 만들어야 한다
            ★ 우리는 무료 + 광고로 확정이라 문제없다. 다만 **되돌릴 수 없다는 것**을 알고 고른다
③ 앱 서명 키   ⛔ A2에서 정한다. 2.94 참조 — 잃으면 그 앱이 끝난다
⚠ 기본 언어는 문서가 침묵한다. 되돌릴 수 있는지 🔴 화면에서 볼 것
```

**앱을 만든 뒤 A2가 열린다**

```
어디    앱 만들기 → 좌측 메뉴 **"앱 무결성" 또는 "설정 > 앱 서명"**
무엇을 볼 것
  ✅✅ **A2는 닫혔다** (2026-09-04 · 콘솔 화면 확인)
  A2-1  Play 앱 서명이 **기본 적용**으로 확인됐다 — 2.94의 미확인 전제가 닫혔다
  A2-2  ★ 고를 것이 없었다 — **앱 서명 키가 이미 「사용 중」**이고 Google이 생성해 뒀다
  A2-3  🔴 **업로드 키 인증서 절이 비어 있다** — "첫 App Bundle을 업로드하면
        여기에 인증서 지문이 표시됩니다". 우리가 업로드 키를 만들어야 한다
```

---

#### 🔴 콘솔 확인 목록 — 앱 생성을 기준으로 다시 정렬했다

> ⚠ 앞선 목록(A/B/C)은 화면 순서를 몰라 임의로 나눈 것이었다.
> **앱을 만들기 전에 보이는 것**과 **만든 뒤에야 열리는 것**으로 다시 갈랐다.

```
[1단계] AdMob — 앱을 만들기 전
  ✅ C2  닫혔다 — 앱을 만들었다(2.100)
  ✅ C1  닫혔다 — 만들 수 있다. ⚠ 상태 문구는 **「검토 필요」**다,
        「제한된 게재」가 아니다 (2.102). **화면 문구가 정본이다**
  ✅ C5  닫혔다 — 지급 정보가 AdSense에서 자동 연동됐다 (2.102)
  🔴 C5b **세금 정보**는 그 화면에 없었다. 어디에 있는지 남은 확인이다
  ✅ C7  닫혔다 — 끄기로 확정했고(2.100 ㈘), 화면에 안 보이는 것도 맞다.
        ★ 파이어베이스 SDK가 앱에 없다(logcat 실측 · 2.102) — 켤 자리도 걷을 것도 없다

[2단계] AdMob — 앱을 만든 뒤
  ✅ C3  닫혔다 — 콘솔이 **크기를 묻지 않는다.** 크기는 코드가 정한다 (2.102)
        ★ 그래서 배너를 320×50으로 내려도 **단위를 새로 만들지 않는다**(2.104 ②)
  🔴 C4  아동 대상 — **자리가 둘이다.** 콘솔(앱 설정 COPPA)과 코드(initialize 인자).
        지금 코드는 UNSPECIFIED다. 갈리지 않게 함께 정할 것 (2.102)

[3단계] Play — 앱을 만들기 전
  A1  개발자 계정 **본인확인**이 어디에 나오고 얼마나 걸린다고 안내하는가
  B4  🔴 기본 언어를 나중에 바꿀 수 있다는 안내가 있는가 (문서가 침묵한다)

[4단계] Play — 앱을 만든 뒤 (여기서 A2가 열린다)
  A2  ★★ 앱 서명 — 필수 표시 여부 · Google 생성 선택지 · AAB 필수 여부
      ← 2.94의 미확인 전제를 닫는 자리다. **가장 먼저 볼 것**
  B1  "광고" 항목의 위치. 지금 답은 **"광고 포함"**이다
  B2  "데이터 보안" 양식의 위치와 **"기기 또는 기타 ID"**가 수집/공유 양쪽에 있는지
  B3  "타겟층 및 콘텐츠"의 아동 대상 설정 → 방침 5절("아동 대상 아님")에 맞춘다

[5단계] 출시 뒤
  C6  ★ AdMob에서 스토어를 연결한 **뒤에도 App ID가 그대로인가**
      → 바뀌면 재빌드다. 바뀌지 않는다는 것이 세션의 읽기이고 **확인이 필요하다**
```

---

### 2.101 ★ 실제 AdMob 값으로 교체 — 데모를 걷고 릴리스까지 확인했다 (2026-09-02)

> 2.98의 교체 6단계를 그대로 밟았다.
> ⛔ **값은 이 문서에 적지 않는다** — 아래 「값을 어디까지 적는가」.

**한 일**

```
App ID      android/admob.properties 의 admob.appId          ← 실제 값 (물결 ~)
배너 단위    ADMOB_BANNER_ID=<실제 단위> npm run sync:app       ← 실제 값 (빗금 /)
✅ 퍼블리셔 번호가 저장소의 ads.txt(pub-5074778849873789)와 같다 —
  AdSense와 같은 계정이라는 것이 여기서 한 번 더 확인된다(2.97에서 예상한 대로다).
★ 이것이 **세션이 검증할 수 있는 유일한 교차 확인**이다.
  앱 번호·단위 번호가 콘솔의 것과 같은지는 **사람이 화면과 대조해야 한다** —
  형식이 맞고 앱이 뜨면 통과하므로, 남의 앱 값을 넣어도 아무 신호가 없다
```

#### ✅ 6단계를 실측으로 닫았다

```
4단계  앱이 죽지 않는다
      pid 살아 있음 · FATAL 0건 · "Missing application ID" 0건
      ★ 이 한 줄이 **App ID가 실제로 읽혔다**는 증거다 —
        없거나 형식이 틀리면 시작하자마자 죽는다(2.98)
5단계  산출물에 데모 잔존 0
      debug APK · release APK · **AAB** 셋을 zip으로 열어 전수 검사했다
      실제 App ID    resources.arsc / resources.pb 에 1건 (매니페스트 resValue)
      실제 배너 단위  assets/public/assets/index-*.js 에 1건
      ⛔ dex의 데모 문자열은 우리 값이 아니다 — 바로 아래 절
6단계  bannerAdLoaded → bannerAdImpression
      showBanner  adId=<실제 단위> · LARGE_BANNER · BOTTOM_CENTER · margin 0
      요청 URL     client=<우리 퍼블리셔> · slotname=<우리 단위 뒷번호>
                  format=320x100_as  ← LARGE_BANNER가 그대로 적용된다(2.98 재확인)
게이트  assembleRelease + bundleRelease → BUILD SUCCESSFUL
      ★ `Task :app:verifyNoDemoAdIds` 가 **실행됐다**(SKIPPED도 UP-TO-DATE도 아니다).
        입출력이 없는 태스크라 매번 돈다 — ⓪-2 규칙대로 눈으로 확인했다
      npm test 244건 통과 · python scripts/stats.py --check 통과
⚠ release 산출물은 아직 **서명이 없다**(app-release-unsigned.apk).
  업로드 키는 A2에서 정한다 — 2.100 참조. Play에 올릴 수 있는 상태가 아니다
```

#### ⚠⚠ 데모 퍼블리셔는 **SDK 안에도 있다** — 게이트를 APK 전체로 넓히지 말 것

```
release classes.dex · debug classes4.dex 에 3940256099942544 가 있다.
문맥을 뜯어 보니 구글 Mobile Ads SDK가 들고 있는 **자기 시험 단위 상수**다 —
  ca-app-pub-3940256099942544/1033173712 (전면) · /5224354917 (리워드) 등 5종
  @capacitor-community/admob 의 AdOptions.java 에도 같은 상수가 있다
  (classes2.dex의 ca-app-pub-0000000000000000~0000000000 도 SDK 자리표다)
★ 그래서 2.99가 검사 범위를 admob.properties + assets/public 으로 **좁힌 것이 맞았다.**
⛔ "APK를 통째로 훑는 편이 안전하다"로 넓히면 **영구 오탐**이 된다 —
  플러그인을 지우지 않는 한 절대 통과할 수 없는 게이트가 되고, 그러면 사람이 끈다
```

#### 🔴 App ID 귀속은 에뮬레이터로 **확인할 수 없다** — 닫지 못한 자리

```
요청 URL에 우리 App ID가 **안 실린다.** 대신 구글 샘플 앱 코드가 실린다 —
  app_wp_code=ca-app-pub-3940256099942544 & app_code=3347511713   ← 샘플 App ID
  client=<우리 퍼블리셔> & slotname=<우리 단위>                     ← 실제 단위
같은 로그에 근거가 있다 —
  `I Ads: This request is sent from a test device.` · URL에 `adtest=on`
  화면의 배너에도 **"Test Ad"** 라벨이 붙는다.
  에뮬레이터는 AdMob이 **자동으로 시험 기기로 잡는** 자리다.

✅ 확인된 것    설치된 APK의 resources.arsc에 실제 App ID가 1건이고 앱이 죽지 않았다
              → SDK가 우리 App ID를 **읽었다**
⛔ 확인 못 한 것  그 App ID로 **귀속이 잡히는가.** 시험 요청에는 안 실리니 볼 수 없다
⛔⛔ 시험 기기 지정을 빼고 실제 노출을 만들어 확인하려 하지 말 것 —
  개발자 자기 노출·클릭은 **무효 트래픽**이고 계정이 걸릴 수 있다.
  그것이 애초에 데모 단위와 시험 기기가 있는 이유다
★ 이 자리는 **콘솔에서 실사용 노출이 잡히는지**로만 닫힌다 —
  2.100의 ⏳ 기다림 2(앱 준비성 심사)가 끝난 뒤다.
  ⚠ 그때까지 "광고가 안 찬다"는 관측으로 아무 결론도 내리지 말 것(2.100 제한 게재)
```

#### 🔴 남는 구멍 — 배너 단위가 **환경변수뿐이다** (판단 필요)

```
지금 상태   ADMOB_BANNER_ID를 빠뜨리고 릴리스를 말면
          __ADMOB_BANNER_ID__ = "" → adsEnabled = false →
          **광고도 고지도 없는 앱**이 나가고 **게이트 전부가 통과한다**
          · verifyNoDemoAdIds는 "데모가 있는가"만 본다. 없으면 통과다
          · npm test는 **반대 방향만** 본다 — verses.test.js가
            `!/ca-app-pub-\d/.test(src)` 로 **소스에 값이 없는 것**을 지킨다.
            "빌드 산출물에 값이 들어 있는가"를 보는 테스트는 하나도 없다
            ★ 그래서 (가)로 갈 때도 값을 **소스에 적으면 안 된다** —
              properties 파일은 소스가 아니라 통과하지만, ads.js에 적으면 이 게이트가 깨진다
★ ⓪-2의 모양 그대로이고, **데모가 남는 실패보다 신호가 더 없다** —
  데모는 광고가 나오지만 이쪽은 "배너가 없는 화면"으로 보일 뿐이다.
  ⚠ 2.99를 만든 근거가 그대로 이쪽에도 걸린다. 그때는 이 방향을 안 봤다

세션 의견   (가) 배너 단위도 admob.properties로 옮긴다.
              한 파일이 값 둘을 갖고 **잊을 환경변수가 사라진다.**
              ⚠ vite가 그 파일을 읽어야 한다 — 지금은 gradle만 읽는다
          (나) verifyNoDemoAdIds가 "실제 단위가 **있는가**"도 본다.
              번들에 ca-app-pub-…/… 가 하나도 없으면 릴리스를 세운다
권고      (가)+(나). (가)가 원인을 없애고 (나)가 실수를 잡는다.
          ⛔ 하나만 고른다면 (나)다 — 잊는 것을 막는 것이 게이트의 일이다
```

#### 🔴 값을 어디까지 적는가 — 이 문서는 **공개 저장소에 있다**

```
세션 판단   ⛔ 뒷번호(App ID의 ~ 뒤 · 단위의 / 뒤)를 **적지 않았다.**
근거      ① 저장소 규약이 이미 그렇다 — 값은 gitignore된 admob.properties와
            환경변수에만 둔다(2.98). 문서에 적으면 그 규약이 무의미해진다
          ② 퍼블리셔 번호는 이미 공개다(ads.txt는 공개가 목적이다).
            앱·단위 뒷번호는 다르다 — 남의 앱에서 우리 단위를 돌려
            무효 트래픽을 만드는 길을 굳이 열지 않는다
          ⚠ **비밀은 아니다.** APK를 뜯으면 나온다.
            불필요한 노출을 안 만드는 것이지, 숨긴다고 안전해지는 값이 아니다
🔴 사용자가 "적어 두는 편이 낫다"고 판단하면 적는다 —
  값이 로컬 파일 하나에만 있는 것은 **그 파일을 잃으면 콘솔을 다시 봐야 한다**는 뜻이다
★ 지금 값이 있는 곳   로컬 app/android/admob.properties (App ID) ·
                   이 세션이 빌드에 넘긴 환경변수 (배너 단위)
```

#### ⚠ 로컬 상태가 바뀌었다 — 다음 세션이 알아야 한다

```
admob.properties가 이제 **실제 App ID**를 들고 있다(데모가 아니다).
→ 로컬에서 release를 말면 실제 값이 든 산출물이 나온다. 그것이 의도다
→ 다시 데모로 시험하려면 두 값을 **함께** 되돌려야 한다.
  ⛔ 하나만 되돌리면 App ID와 단위의 퍼블리셔가 갈린다
데모 값은 admob.properties.example 과 3절 실행 환경에 적혀 있다
```

---

### 2.102 ★ 배너 단위를 admob.properties로 · 게이트를 양방향으로 · 콘솔 결과 (2026-09-02)

> 2.101이 남긴 🔴 구멍을 사용자 결정대로 **(가)+(나) 둘 다** 막았다.
> ⛔ 값은 여전히 이 문서에 적지 않는다 — 2.101 「값을 어디까지 적는가」가 확정됐다.

#### ✅ (가) 잊을 자리를 없앴다 — 환경변수가 사라졌다

```
전    배너 단위는 ADMOB_BANNER_ID 환경변수로만 들어왔다
      → 빠뜨리면 빈 문자열 → adsEnabled=false → **광고도 고지도 없는 앱**
후    app/android/admob.properties 가 값 둘을 갖는다 (admob.appId · admob.bannerId)
      vite가 앱 빌드에서만 이 파일을 읽는다 (readAdmobBannerId)
★ 되돌릴 때 퍼블리셔가 갈리던 문제(2.101)도 같이 닫힌다 — 값 둘이 한 파일에 있다
⚠ 웹 빌드는 이 파일을 보지 않는다. 웹은 AdSense다 —
  그래서 gh-pages CI에 파일이 없어도 웹 빌드는 선다 (build.yml 확인함)
```

#### ✅ (나) 게이트가 **양방향**을 본다

```
전    verifyNoDemoAdIds 는 "데모가 남았는가"만 봤다. 값이 **없으면 통과**한다
후    [3] 을 더했다 — 번들에 admob.properties의 단위와 **같은 값**이 있는가
★ 같은 값을 찾는 것이 요점이다. "값은 고쳤는데 cap sync를 안 돌렸다"까지 걸린다
```

#### ⓪-2 규칙대로 **일부러 어긋내 실패를 봤다**

```
admob.bannerId 를 지우고 build:app   → Error: admob.bannerId 형식이 틀렸습니다: ''   ✅ 섰다
번들에서 단위를 지우고 게이트        → [3] 앱 번들에 배너 광고 단위가 없습니다  BUILD FAILED  ✅ 섰다
되돌린 뒤                          → BUILD SUCCESSFUL · npm test 247건 통과         ✅
★ 번들 해시가 이전과 같다 (index-BQOaE0v-.js) — **값의 출처만 바뀌고 산출물은 같다**
```

⚠ 회귀 3건을 appbuild.test.js에 더했다(가드가 **지워지는 것**을 잡는다).
그중 하나가 처음에 조용히 통과했다 — 보통 문자열의 `\d`가 `d`로 뭉개졌다.
**vite.config.js가 `[\s\S]`로 당한 것과 같은 모양이다.** `String.raw`로 고쳤다.

---

#### ✅ 콘솔 확인 결과 (사용자가 화면에서 확인)

```
C1  ⚠ 앱·단위를 만들 수 있다. 상태 문구는 **「검토 필요」**다
    ⛔ 「제한된 게재」가 아니다 — 2.100이 예상한 문구와 다르다. 화면 문구가 정본이다
C3  ✅ 콘솔이 배너 **크기를 묻지 않는다.** 적응형/고정 구분도 없다
    화면 문구 "다음 광고 단위 ID를 사용하여 코드를 통합할 때 광고 유형, 크기,
    게재위치를 지정합니다" → **크기는 코드가 정한다.** 아래 ②에서 크기를 바꿔도
    콘솔에서 단위를 새로 만들 필요가 없다는 뜻이다
C5  ✅ 지급 정보가 AdSense에서 자동 연동됐다 (2.96·2.97의 읽기가 맞았다)
    결제 계정 「애드센스(대한민국)」 · AdSense pub-5074778849873789 · 수취인 채워짐 ·
    지급 기준액 US$100
    ⚠ **세금 정보는 그 화면에 없었다.** 확인 항목으로 남긴다 (아래 목록 C5b)
C7  ✅ 닫혔다 — 화면에 안 보이는 것이 맞다
    ★ 로그가 근거다. 이 빌드의 logcat이 그대로 말한다 —
      "The Google Mobile Ads SDK will not integrate with Firebase.
       Admob/Firebase integration requires the latest Firebase SDK jar,
       but Firebase SDK is either missing or out of date"
      → 파이어베이스 SDK가 앱에 **없다.** 사용자 측정항목은 그 SDK로 걷는 것이라
        콘솔에서 켤 자리도 안 나오고, 켜도 걷을 것이 없다
    ⛔ 이것은 2.100 ㈘의 결정(끄기)과 **같은 방향이다.** 넣지 않기로 한 것이
      코드에도 실제로 없다 — 결정과 산출물이 어긋나지 않았다
```

#### 🔴 C4 아동 대상 — 위치 둘이다 (사용자가 화면에서 확인할 것)

```
① 콘솔    AdMob → 왼쪽 **앱** → 해당 앱 → **앱 설정(App settings)**
          거기에 COPPA(아동 온라인 개인정보 보호법) 항목이 있다
          ⛔ 세션이 화면을 못 봤다. 문서 기준의 위치다 — 화면이 다르면 화면이 맞다
② ★ 코드  **여기가 더 중요하다.** 플러그인이 initialize()에서
          tagForChildDirectedTreatment · tagForUnderAgeOfConsent 를 받는다
          (node_modules/@capacitor-community/admob AdMob.java:248~)
          지금 우리는 `initialize({})` 로 **빈 값**을 넘긴다 → UNSPECIFIED다
⚠ 방침 5절이 "아동 대상 아님"이다. 콘솔만 맞추고 코드를 UNSPECIFIED로 두면
  **두 자리가 갈린다.** 콘솔 화면을 본 뒤에 함께 정할 것 — 🔴 판단 대기다
```

---

#### ⚠ 관찰 — 요청 URL에 **우리 App ID가 실렸다** (2.101과 다르다)

```
2.101   app_wp_code=<구글 샘플> · app_code=<샘플>   ← 샘플 App ID가 실렸다
오늘    app_wp_code=<우리 퍼블리셔> · app_code=<우리 앱 번호>
        client·slotname 은 그때도 지금도 우리 값이다
⛔ **결론을 적지 않는다.** 왜 달라졌는지 모르고, 이것이 귀속을 증명하지도 않는다.
  여전히 `adtest=on`이고 배너에 "Test Ad"가 붙는다 — 시험 요청이다.
  2.101의 🔴(귀속 확인 불가)는 **그대로 열려 있다.** 콘솔 실사용 노출로만 닫힌다
```

---

### 2.103 ⛔ 배너 아래 빈 공간 — 원인은 edge-to-edge가 **아니었다** (2026-09-02 · 진단)

> ⛔ **진단만이다. 아무것도 고치지 않았다.** 안은 아래에 있고 사용자가 고른다.
> ★ 가설이 틀렸다. 방향은 맞았지만 **기전이 반대다** — 그래서 고치는 자리가 다르다.

#### 세운 가설과 실측

```
가설   Android 15 + target 35↑ 라서 edge-to-edge가 강제된다 →
      웹뷰가 시스템 바 **뒤까지** 그려지는데 AdView는 인셋을 존중한다 →
      그 차이만큼 배너 아래로 내용이 보인다
실측   ⛔ 웹뷰는 시스템 바 뒤로 안 그린다. **이미 인셋 안에 들어가 있다.**
      그리고 AdView가 그 **안에서 인셋을 한 번 더** 뺀다 — 두 번 세어진 것이다
```

#### 실측값 (에뮬레이터 pym_phone_360 · API 35 · 1080×1920 · density 3.0)

```
화면            1080 × 1920 px        = 360 × 640 dp
navigationBars  frame=[0,1848][1080,1920]  bottom = 72 px = **24 dp**
WebView 경계     [0,72][1080,1848]     ← 위아래가 **이미 인셋만큼 깎여 있다**
AdView 경계      [60,1476][1020,1776]  ← 960×300 px = 320×100 dp (LARGE_BANNER ✓)

웹뷰 아래끝 1848 − 배너 아래끝 1776 = **72 px = 24 dp = 정확히 하단 인셋**

웹에서 잰 것 (CDP Runtime.evaluate — 실제로 돌려서 받은 값이다)
  innerHeight 592dp (=1776px)   ← 640이 아니다. 웹뷰가 592dp뿐이다
  --safe-area-inset-bottom "0px" · env(safe-area-inset-bottom) "0px"
  screenY 0 · screen.height 640 · screen.availHeight 640
  shell paddingBottom "140px" (= 40 + bottomInset 100) ← 지금 계산은 맞게 돌고 있다
```

#### 두 번 세어지는 자리 — 코드 두 곳이다

```
① Capacitor 8 SystemBars.java (플러그인 내장)
   shouldPassthroughInsets = WebView메이저 ≥ 140 && viewport-fit=cover
   ⛔ 이 기기의 WebView는 **124**다 → false → 아래 가지로 간다
     v.setPadding(left, top, right, systemBarsInsets.bottom)   ← 웹뷰 부모에 패딩
     그리고 인셋을 0으로 만들어 돌려준다 → **CSS 변수가 0px로 주입된다**
   ★ WebView 경계 [0,72][1080,1848]이 이 가지가 돈 **증거**다

② @capacitor-community/admob BannerExecutor.java:135~147
   if (SDK_INT >= 35) { ... setMargins(…, margins[3] + bottomInset) }
   ⛔ **같은 패딩된 부모 안**에서 하단 인셋을 다시 뺀다
     (AdView 부모가 패딩된 그 뷰다 — 1776 = 1848 − 72 이 그것을 말한다)
```

#### ⚠⚠ 그래서 "인셋을 읽어 더한다"는 **이 기기에서 안 된다**

```
웹이 읽을 수 있는 인셋이 **0px이다** (위 실측). 더할 값이 없다.
screen.height − innerHeight = 48dp 는 위아래 **합**이라 하단만 못 가른다 (screenY도 0).
★ 이것이 이 진단의 실질적인 결론이다 — 사용자가 제시한 세 안 중
  「인셋을 읽어 더한다」와 「웹뷰를 인셋 안으로 넣는다」는 이미 답이 나와 있다.
  웹뷰는 **이미 인셋 안에 있고**, 인셋은 **웹에서 안 보인다**.
```

#### ⚠⚠ 소급 정정 (2026-09-02 · 2.112에서 알았다) — **「못 읽는다」는 갈래 B에서만이다**

> ⛔ **이 절의 결론 하나가 좁아진다.** 위에서 "웹이 읽는 인셋이 0px이다"라고 적은 것은
> **갈래 B(WebView <140)에서만 참이다.** 갈래 A에서는 실제 값이 온다.

```
실측
  에뮬레이터 (갈래 B · WebView 124)   --safe-area-inset-bottom = **0px** · env() = 0px
  태블릿     (갈래 A · WebView 151)   --safe-area-inset-bottom = **48px** · env() = 48px
                                    (상단도 24px로 실제 값이다)
★ 갈래 A는 passthrough라 Capacitor가 **인셋을 그대로 넘긴다.**
  갈래 B는 코드가 인셋을 **0으로 만들어 주입한다**(SystemBars.java) — 그래서 0이다
```

**★ 이 사실로 다시 볼 값이 있는 결정 — 정리했다 (⛔ 지금 고치지 않는다)**

```
① 2.103 배경면 도입 (안 1)          ⛔ 다시 볼 값 **없다.** 그대로 맞다
   근거 — 배경면은 "인셋을 몰라도 틈을 덮는다"가 요점이었는데,
   갈래 A에서 인셋을 알게 돼도 **덮어야 할 틈은 그대로 있다**(배너가 인셋만큼 위에 앉는다).
   ★ 오히려 태블릿에서 좌우 251dp를 채우는 값을 했다(2.111) — 안 ①이었다면 샜다
② 2.109 아래 여백은 못 줄인다        ⛔ 다시 볼 값 **없다.** 갈래 A에서도 같다
   근거 — 아래 여백 = 인셋이고, 인셋을 **읽는다고 배너가 내려오지 않는다.**
   배너 위치는 플러그인이 정하고 그 margin은 인셋을 더한다.
   ⚠ 이론상 margin에 음수(−인셋)를 넘겨 배너를 내릴 수는 있다. 그러나
     **내리면 제스처 바와 겹친다** — 그것이 안 되는 진짜 이유이고 인셋을 아는 것과 무관하다
③ 2.112 배너 튀어나옴               ✅ **이 사실로 풀었다.** 안 2가 그것이다
④ 2.105 상단 검은 띠 (안 3 · 그대로) ⛔ 다시 볼 값 **없다.**
   근거 — 그 띠는 **갈래 B에서만 생긴다**(갈래 A는 웹뷰가 화면 전체다 · 2.111 실측).
   갈래 B에서는 인셋을 못 읽으므로 새 사실이 닿지 않는다
⑤ 2.107 · 2.111 키보드 대응        ⛔ 다시 볼 값 **없다.**
   근거 — 뷰포트 축소로 판정하고 인셋을 쓰지 않는다. 태블릿에서도 줄어드는 것을 실측했다
⑥ 2.104 BANNER_GAP 40 고정         ✅ **③에서 대체됐다** (간격이 둘로 갈렸다)

★ 남는 것은 하나다 — **갈래 B에서는 여전히 못 읽는다.** 그 갈래에 걸린 결정
  (④ 상단 띠 · ③의 UNKNOWN 상한 가정)은 새 사실로 나아지지 않는다
```

#### ⚠ 갈래가 둘이라는 것이 더 큰 문제다 (회귀가 못 보는 자리)

```
갈래 A  WebView ≥ 140 (실기기 대부분)  웹뷰가 화면 전체 → 배너 아래끝 1848 → 틈 72px
갈래 B  WebView < 140 (이 에뮬레이터)   웹뷰가 592dp   → 배너 아래끝 1776 → 틈 72px
★ **틈의 크기는 둘 다 같다.** 그런데 웹이 읽는 safe-area 값은
  A에서 24dp · B에서 0dp로 **정반대다.**
⛔ 그래서 `env(safe-area-inset-bottom)`을 더하는 안은 A에서만 맞고 B에서 안 맞는다.
  ⚠⚠ **이 에뮬레이터로는 A를 시험할 수 없다** — 이미지에 Play 스토어가 없어
    WebView가 124에 묶여 있다. 3절 「일하는 방식」 ①(표본 창)이 여기 그대로 걸린다.
```

#### 🔴 안 셋 — 사용자가 고른다

```
안 1  ★ 권고. **띠 뒤에 배경 한 겹을 깔고, 그 띠가 틈을 덮는다** (③과 같은 장치다)
     웹뷰 **자기 아래끝**에 높이 BANNER_HEIGHT 의 불투명 띠를 fixed로 깐다.
     배너 아래끝은 웹뷰 아래끝보다 인셋만큼 위에 있으므로,
     그 틈(인셋 ≤ 48dp)이 띠 높이(50~100dp) 안에 **항상 들어간다.**
     ✅ 인셋 값을 **몰라도 된다** → 갈래 A·B가 같은 코드로 맞는다
     ✅ ③(광고가 목록과 분리되어 떠 있는 느낌)이 같은 변경으로 함께 풀린다
     ⚠ 배너 윗변이 띠 윗변보다 인셋만큼 **위로 삐져나온다** →
       띠 윗변에 1px 구분선을 그으면 배너 좌우 20dp 구간에만 선이 보인다.
       ⛔ 그래서 구분선 대신 **그라데이션 페이드**로 위를 닫는 것이 낫다 (③ 참조)
안 2  ⛔ 인셋을 읽어 더한다 — **못 한다.** 갈래 B에서 웹이 읽는 값이 0이다
안 3  ⚠ 배너 위치를 바꾼다 — showBanner 에 `margin: -<인셋dp>` 를 넘기면
     플러그인 계산이 상쇄되어 배너가 웹뷰 아래끝에 붙는다.
     ⛔ 그런데 그 인셋 값을 **웹이 모른다**(안 2와 같은 벽).
     ⛔ 플러그인 최신이 8.1.0이고 이 계산을 끄는 설정이 없다 (확인함)
```

⚠ **A/B 어느 갈래든 틈은 하단 인셋 하나뿐이다.** 안 1은 그 크기를 몰라도 덮으므로
갈래를 나누지 않는다 — **시험할 수 없는 갈래가 있다는 것**이 안 1을 고른 이유다.

---

### 2.104 ★ 셋을 한 번에 고쳤다 — 배경면 · 320×50 · 간격 상수 (2026-09-02 · ✅ 실측 완료)

> ✅ **적용했다.** 2.103의 안 1로 갔다 (사용자 결정).
> ⛔ 아래 수치는 전부 **실기기 화면이 아니라 실측이다** — 사용자 지시대로
> 눈짐작을 근거로 쓰지 않았다. uiautomator 경계 · CDP 평가 · logcat 요청 URL이 출처다.

#### ⛔ 먼저 — edge-to-edge 가설은 **반대였다** (사용자 요청으로 그대로 남긴다)

```
사용자 가설   Android 15 + target 35↑ 라 edge-to-edge가 강제된다 →
            웹뷰가 시스템 바 **뒤까지** 그려지는데 AdView는 인셋을 존중한다 →
            그 차이만큼 배너 아래로 내용이 보인다
실측        ⛔ 웹뷰는 시스템 바 뒤로 안 그린다. **이미 인셋 안에 들어가 있다**
            (WebView 경계 [0,72][1080,1848] — 위아래가 깎여 있다).
            그리고 AdView가 그 **안에서 인셋을 한 번 더** 뺀다. 두 번 세어진 것이다
★ 방향은 맞았다 — "배너가 웹 기준보다 위에 앉는다"는 관측은 정확했다.
  ⛔ 그러나 **기전이 반대라 고치는 자리가 달랐다.** 가설대로였다면
    "인셋을 읽어 더한다"가 답인데, 실측에서는 웹이 읽는 인셋이 **0px이라** 그 길이 없다.
★ 사용자가 같은 날 눈짐작으로 두 번 틀렸다(①의 기전 · "배너 높이가 줄었다"는 판단).
  ⛔ 그래서 이 절의 확인은 **전부 계측으로만** 했다. 화면을 보고 하는 말을 근거로 쓰지 않는다.
```

#### ✅ 무엇을 고쳤나 — 값 하나가 넷을 움직인다

```
BANNER_HEIGHT  100 → 50        adsApp.js    광고 자체 (320×50)
adSize      LARGE_BANNER → BANNER   bannerNative.js
BANNER_GAP  새 상수 = 40        adsApp.js    ⛔ 배너 높이와 **별개다**
bannerSpace = BANNER_HEIGHT + BANNER_GAP = 90
배경면       Shell에 fixed 두 겹 (면 + 위쪽 페이드) — 높이가 bannerSpace다
★ 이 값 하나가 넷을 함께 움직인다 — 광고 자리 · 배경면 · 하단 여백 · 떠 있는 버튼
```

#### ✅ 실측 (에뮬레이터 pym_phone_360 · API 35 · 1080×1920 · dpr 3)

```
요청 URL      format=320x50_mb          ← 320×50이다 (전에는 320x100_as)
             adSize":"BANNER"           logcat의 showBanner 인자
AdView 경계   [60,1626][1020,1776]      = 320 × 50 dp ✓
WebView 경계  [0,72][1080,1848]         (바뀌지 않았다 — 인셋 이중 계산은 그대로다)

화면 아래끝(=웹뷰 아래끝 1848) 기준, CSS px로:
  배너          아래끝 24 · 윗변 74
  배경면(불투명)  0 ~ 90        ← **배너 [24,74]를 완전히 담는다**
                              배경 rgb(20,30,36)=T.ink · 그림자 rgba(0,0,0,.45) 0 -6px 20px
  페이드         89 ~ 114      ← 1px 겹쳐 실틈을 막는다
★ 배너 아래 틈 24px을 배경면이 덮는다 → **내용이 비치지 않는다** (실측 확인)
```

#### ✅ 하단 요소와 배너 상단선 사이 간격 — 0이었던 것이 40이 됐다

```
                     filled=true    filled=false     차이
다시 적기 (fixed)        114            24            90
이 앱에 대해              130            40            90
다시 적어보기             183            93            90
탭 줄(이어서/말씀/찬양)     214            —
★ 배너 윗변이 74다 → **간격 = 114 − 74 = 40 css px** (전에는 0이었다)
★ 셋이 정확히 **90(=bannerSpace)씩** 함께 움직인다 — 값 하나에 묶인 것이 실측으로 확인됐다
```

#### ✅ filled=false — 넷이 함께 사라진다 (비행기 모드로 실제 재현)

```
bannerAdFailedToLoad 발생 · 뷰 계층에 AdView 없음
배경면 개수 **0**  ← 그리지 않는다
하단 요소가 전부 bottomInset 0 자리로 내려온다 (위 표의 오른쪽 열)
★ 어제 안 ②를 기각한 사유("광고가 없을 때 배경 띠가 남는다")가 실측으로 닫혔다
```

#### 판단들 — 왜 그 값·그 모양인가

```
BANNER_GAP = 40   ⚠ 이 값은 「간격」이자 「인셋 흡수분」이다.
                 배너는 하단 인셋만큼 위에 앉는데 그 인셋을 웹에서 못 읽는다(2.103).
                 실제 간격 = 24(떠 있는 버튼 오프셋) + GAP − 인셋 = 64 − 인셋
                 → 제스처 40dp · 3버튼(48dp) 16dp. **어느 갈래에서도 0이 아니다.**
                 ⛔ 줄이려면 그 부등식을 다시 풀 것. 눈으로 정하지 말 것
면 색 T.ink      ⚠ inkDeep이 아니다. 긴 목록의 아래쪽 배경이 이미 inkDeep이라
                 같은 색이면 **면이 있는지 없는지 보이지 않는다**
전체 폭          ⛔ 카드처럼 좌우를 띄우면 화면 맨 아래 모서리로 내용이 다시 샌다
좌우 20dp        배너가 320dp 고정이라 360dp 화면에서 저절로 남는다.
                 ★ 2.96이 "감수한 대가"로 적었던 것이 이제 쓸모가 됐다
페이드 24        ⛔ 1px 실선을 넣지 않는다 (사용자 결정). 배너 윗변이 면 위로
                 삐져나올 때 선이 좌우 20dp에만 보이기 때문이다.
                 ★ 페이드는 하드 엣지가 없어 삐져나와도 티가 안 난다 —
                   인셋을 못 읽는다는 제약이 그대로 이 선택의 근거다
그림자            ⛔ 배경면에만 줄 수 있다. 네이티브 AdView에는 CSS가 닿지 않는다.
                 "광고가 카드 위에 놓인" 모양까지가 한계다 (사용자 합의)
```

#### ⚠ 웹(AdSense) AD_HEIGHT는 **100 그대로 두었다** — 사용자 판단

```
근거   앱의 띠는 화면 **최하단에 떠 있는 것**이고, 웹의 광고는 목록 **항목 사이에
      끼우는 것**이다. 웹은 그 자리를 320×100으로 설계했고,
      색·여백 위계 실측(2.35·2.36)이 그 값 **위에** 서 있다 — 내리면 그 실측을 다시 해야 한다
★ 매체가 갈린 것을 값에도 반영한다. 같은 숫자를 두 매체가 공유하면
  한쪽을 고칠 때마다 다른 쪽이 흔들린다. ads.js의 상수 옆에 이 근거를 적어 두었다
```

#### ✅ ③ 분리감 — 확정 (2026-09-02 · 사용자 확인)

```
사용자가 실기기 화면으로 확정했다 — **배경면과 페이드가 목록과 광고를 끊는다.**
마지막 목록 항목이 페이드에 잠겨 배너 뒤로 사라지는 것도 **의도대로**다.
★ 이로써 2.104의 셋(①②③)이 전부 닫혔다.
```

#### ⛔ 남은 미검증 — 정리했다 (2026-09-02 · 둘이 닫히고 하나가 남았다)

```
✅ 닫힘 ①  갈래 A의 **기하**
          2.105의 실험(insetsHandling "disable")으로 웹뷰를 화면 전체로 만들어 쟀다.
          예측 배너 [1698,1848] · 배경면 [1650,1920] → **실측 완전 일치**
✅ 닫힘 ②  **다른 인셋 값**(3버튼 내비게이션 48dp)
          adb 오버레이로 3버튼으로 바꿔 실측했다 (끝난 뒤 제스처로 원복).
            웹뷰 [0,72][1080,1776] (하단 인셋 48dp)
            AdView [60,1482][1020,1632]      ← 예측과 완전 일치
            화면 아래끝 기준 css px — 배너 [48,98] · 배경면 [0,90]
            ★ 배너 아래 48px을 배경면이 **전부 덮는다. 누수 0**
            ★ 배너 윗변이 배경면 위로 **8px 삐져나온다** — 2.104가 예측한 그대로이고
              **페이드 안이라 하드 엣지가 없다.** 1px 실선을 뺀 판단이 여기서 값을 했다
            하단 요소 간격 = 114 − 98 = **16px** ← 부등식 예측(24+40−48) 정확히 일치
          ⛔ 이제 인셋 24dp·48dp 두 값에서 실측됐다. 산수만이던 자리가 아니다

⚠ 남은 것 하나 —  **진짜 갈래 A(WebView ≥140 passthrough)의 키보드 동작**
   ★ 2.107에서 **근거가 크게 좁혀졌다** — 웹뷰를 줄이는 것은 부모 패딩이고(실측:
     부모 [0,0,1080,1920]는 그대로인데 웹뷰만 [0,72][1080,1095]로 줄었다),
     갈래 A도 **같은 줄에서 같은 imeInsets.bottom을 쓴다**(코드).
     → 갈래 A에서도 줄어든다고 볼 근거가 충분하다. 그래도 **돌려 본 것은 아니다**
   왜 남나   "disable" 실험은 기하는 같지만 IME 처리가 다르다.
            진짜 갈래 A는 `v.setPadding(0,0,0, keyboardVisible ? imeInsets.bottom : 0)`로
            **키보드가 뜰 때만** 하단 패딩을 준다. 지금(갈래 B)은 adjustResize로
            웹뷰 전체가 줄어든다(592 → 341 실측).
   무엇에 걸리나  2.106의 키보드 문제와 직결된다. 갈래 A에서 뷰포트가 어떻게 줄어드는지
            모르면 2.106의 안이 그쪽에서도 맞는지 말할 수 없다
   ⚠ 이 에뮬레이터로는 못 본다 (WebView 124 고정 · google_apis 이미지)
```

#### ⚠ (이력) 2026-09-02 당시의 미검증 기록 — 위 「남은 미검증」이 최신이다

```
무엇     Capacitor SystemBars는 **WebView 140 이상 + viewport-fit=cover**이면
        다른 가지로 간다 — 웹뷰가 화면 전체가 되고 safe-area가 24dp가 된다.
        이 기기의 WebView는 **124**라 그 가지를 밟지 못한다
왜 못 하나 AVD 이미지가 `google_apis`(Play 스토어 없음)라 WebView를 갱신할 수 없다.
        ⚠ D 드라이브 여유는 522GB로 넉넉하다 — 막는 것은 용량이 아니라
          `google_apis_playstore` 이미지를 받고 계정으로 로그인해야 한다는 점이다.
          그렇게 해도 그 이미지의 WebView가 140 이상이라는 보장은 없다
실기기    ⛔ 지금 붙어 있는 기기가 없다(adb devices에 에뮬레이터뿐).
        ★ **Android 15+ 실기기/태블릿이면 거의 확실히 갈래 A다** (WebView가 Play로 자동 갱신된다).
          확인 한 줄:  adb shell dumpsys webviewupdate | findstr "Current WebView package"

★ 다만 이 안은 **갈래에 의존하지 않도록 만든 것이다.** 산수로는 결과가 같다 —
    갈래 A   웹뷰 [0,1920] · 배너 [1698,1848] · 배경면 [1650,1920]
             → 배너가 면 안에 담기고, 위 16dp · 아래 24dp. 간격도 40dp로 같다
    갈래 B   (실측) 위와 상대 위치가 **동일하다**
  ⚠ 그래도 **실측이 아니다.** 출시 후 실기기 관찰 항목으로 남긴다 (사용자 결정)
```

---

### 2.105 ★ 상단 검은 여백 — 진단과 안 · **안 3으로 확정** (2026-09-02)

> ⛔ **진단과 안만이다. 아무것도 고치지 않았다** (사용자 지시).
> 실험을 위해 설정을 잠시 바꿨다가 **원복했다** — 저장소 상태는 그대로다.
> ⚠ 하단(2.103·2.104)과 **별개 문제다.** 그 작업에서 다루지 않았다.

#### ① 결함인가 의도인가 — **결함이다. 정확히는 부작용이다**

```
검은 자리의 색   실측 (11,18,22) = #0B1216
그 색의 출처     styles.xml 의 android:windowBackground = @color/pym_ink_deep
왜 그 값인가     ★ 2.91에서 **흰 번쩍임**을 막으려고 정했다.
               AppTheme.NoActionBar의 부모가 DayNight라 라이트 모드 기기에서
               windowBackground가 흰색이고, 웹뷰 첫 프레임 전까지 그 흰 화면이 보였다
⛔ **상태바 외관을 정한 결정은 어디에도 없다.** HANDOFF·styles.xml·capacitor.config·
  AndroidManifest 전부 확인했다. 상태바가 그 색인 것은 **번쩍임 수정의 부작용이다**
⚠ index.html의 theme-color(#141E24)는 웹/PWA용이다 — 안드로이드 네이티브 상태바와 무관하다
```

#### ② 실측 — 문서 **위쪽**에 있을 때 가장 심하다

```
입력 화면(문서 맨 위)   y=0~71  (11,18,22)   ← windowBackground
                     y=72    (34,26,40)   ← T.plum · 그라디언트의 가장 밝은 자리
                     경계에서 +23 / +8 / +18 점프한다. **여기가 제일 눈에 띈다**
결과 화면(끝까지 스크롤) y=0~71  (11,18,22)
                     y=72    (14,22,27)   ← 차이가 거의 없다
★ 그라디언트가 위는 plum, 아래는 inkDeep이라 **스크롤 위치에 따라 경계 대비가 변한다.**
  이 성질이 아래 안 2를 죽인다

⚠ 겸사 발견 — **하단에도 같은 이음매가 있다**
  y=1846 (20,30,36) 배경면(T.ink) → y=1849 (11,18,22) windowBackground
  2.104의 배경면이 T.ink라 이 이음매가 전보다 조금 또렷해졌다.
  ⛔ 갈래 A에서는 배경면이 제스처 바 뒤까지 이어지므로 이 이음매가 **없다**
```

#### ③ ★ 갈래 문제가 상단에도 걸린다 — 그런데 **방향이 반대다**

```
갈래 B (WebView 124 · 이 에뮬레이터)   웹뷰가 인셋 안 → **검은 띠가 생긴다**
갈래 A (WebView ≥140 · 실기기 대부분)  웹뷰가 화면 전체 → **검은 띠가 없다**
★ 하단은 갈래와 무관하게 틈이 생겼는데, 상단은 **갈래 B에서만 생긴다.**
  즉 실기기 대부분에서는 이미 맞을 가능성이 크다 — ⚠ 그러나 미검증 가정이다
```

**실험으로 확인했다** — `insetsHandling: "disable"`로 웹뷰를 화면 전체로 만들어 봤다.

```
WebView 경계   [0,72][1080,1848]  →  **[0,0][1080,1920]**
y=0 색        (11,18,22)         →  **(34,26,40) = plum. 검은 띠가 사라진다**
y=72 이음매    +23/+8/+18 점프     →  없다 (34,26,40 → 37,33,45 → 41,44,53 연속)
상단 충돌      첫 콘텐츠 top 225px · 상태바 72px → **여유 153px(51dp).** 충돌 없다
```

**★ 부산물 — 2.104의 「갈래 A 미검증」이 기하에 한해 닫혔다.**

```
2.104에 적은 예측   배너 [1698,1848] · 배경면 [1650,1920]
이 실험의 실측       배너 [1698,1848] · 배경면 [1650,1920]   ← **완전히 일치**
★ 배경면 설계가 갈래 A에서도 그대로 맞는다는 것이 실측으로 확인됐다.
⚠ 완전한 갈래 A는 아니다 — 진짜 갈래 A는 WebView ≥140의 passthrough라
  env()/CSS 변수 주입과 IME 처리가 다르다. **기하만 같다.**
  다만 우리 코드는 env()를 쓰지 않으므로 기하가 같으면 충분하다
```

#### 🔴 안 — 사용자가 고른다

**안 1 ★ 권고 — `insetsHandling: "disable"` (갈래를 하나로 만든다)**

```
방식   capacitor.config.json 에 plugins.SystemBars.insetsHandling = "disable"
✅ 검은 띠가 사라진다 (실측)
✅ 갈래 A·B가 **같아진다.** 인셋 값을 몰라도 맞는다 — 하단에서 세운 것과 같은 기준
✅ 하단 배경면 설계가 그대로 유효하다 (실측)
✅ 하단 이음매도 함께 없어진다 — 배경면이 제스처 바 뒤까지 이어진다
✅ 웹(PWA)에 영향 없다. Capacitor 설정이다

⚠⚠ 대가 1 — **키보드. 이것이 유일한 실질 위험이고 이 앱은 입력이 핵심이다**
   지금 설정   키보드가 뜨면 웹뷰가 줄어든다 (innerHeight 592 → **341**)
   "disable"   웹뷰가 **안 줄어든다** (640 고정). 키보드가 화면을 덮을 뿐이다
   실측 결과   ✅ **둘 다 입력란이 보인다.** 입력란이 244~294로 위쪽에 있기 때문이다
   ⛔ 그러나 "disable"에서는 브라우저가 포커스된 요소를 **끌어올릴 수 없다.**
     지금 보이는 것은 레이아웃이 운 좋게 위쪽이라서다 — 입력란이 아래로 내려가는
     변경이 생기면 그날 가려지고, **화면에 아무 신호가 없다**

⚠ 대가 2 — 상단 여백이 인셋 비의존 고정값이 된다
   표준 상태바(24dp)에서 여유 51dp. 컷아웃이 큰 기기(40~48dp)면 27~35dp로 얇아진다.
   충돌은 아니지만 얇다.
   ⛔ 하단은 "덮어서" 인셋 무지를 흡수했는데 상단은 **비워야** 해서 같은 수법이 안 통한다.
     덮는 것은 값을 몰라도 되지만 비우는 것은 값을 알아야 한다
```

**안 2 ⛔ 상태바 색을 앱 배경 상단색과 맞춘다 — 실측이 죽인다**

```
⛔ 같은 windowBackground가 **상단과 하단 양쪽**에 보인다 (실측: y=0~71과 y=1849~1919가
  둘 다 (11,18,22)). 그라디언트는 위 plum·아래 inkDeep이라 **한 색으로 둘을 못 맞춘다**
⛔ 뷰포트 상단색이 **스크롤에 따라 변한다** (plum → inkDeep). 고정색은 한 시점에만 맞다
⛔ android:statusBarColor 는 API 35+에서 폐기(무시)다. 쓸 수 없다
⛔ windowBackground를 바꾸면 2.91이 고친 **흰 번쩍임** 자리를 건드린다
★ 네이티브에 세로 그라디언트를 칠하는 변형도 같은 이유로 죽는다 —
  **웹 레이어는 스크롤하는데 네이티브 색은 고정이다.** 어느 한 스크롤 위치에만 맞는다
```

**안 3 아무것도 안 한다 (갈래 B의 열화로 받아들인다)**

```
근거   실기기 대부분이 갈래 A이고 거기서는 띠가 없다 (위 실험이 그 기하를 보였다)
✅ 비용 0 · 위험 0
⚠ 그러나 갈래 B 기기는 실재한다. 그리고 "실기기는 갈래 A"는 **아직 미검증 가정이다**
```

#### ✅ 결정 — **안 3(아무것도 안 한다)** 으로 간다 (2026-09-02 · 사용자 확정)

```
① 안 1(insetsHandling "disable")은 **키보드 대응 공간을 아예 없앤다.**
  웹뷰가 640 고정이면 줄지도 않는다 — 2.106의 문제를 더 나쁘게 만든다.
  ★ 이 앱은 감정을 **자연어로 입력받는 것이 시작점**이라 입력 경험이 핵심이다
② 띠는 갈래 A(실기기 대부분)에서 안 보이고, 스크롤하면 대비가 거의 사라진다
  (문서 아래에서 (14,22,27) — 실측). **문서 위쪽에서만 보이는 문제다**
③ 되돌릴 수 있는 자리다 — 채널·서명 키와 다르다. 설정 한 줄이면 다시 연다
```

⚠⚠ **재검토 조건 — 이것이 걸리면 다시 연다.**

```
조건   실기기(태블릿 · Android 15+)에서 상단 검은 띠가 **실제로 보이는가**
확인법  기기를 붙이고  adb shell dumpsys webviewupdate | findstr "Current WebView package"
      → 140 이상이면 갈래 A다. 그 상태에서 **입력 화면**(문서 맨 위)을 본다
      ⛔ 결과 화면을 스크롤해서 보지 말 것 — 거기서는 원래 대비가 없다
보이면  안 1을 다시 판단한다. ⚠ 그때도 키보드 대가(①)는 그대로 걸린다 —
      2.106이 다른 방법으로 풀려 있으면 저울이 달라진다
안 보이면 이 결정이 맞았던 것이다. 그대로 둔다
★ 근거 ②가 "실기기는 갈래 A다"라는 **미검증 가정 위에 서 있다.** 그것을 확인하는 것이
  이 재검토의 전부다 — 가정이 맞으면 문제가 없고, 틀리면 저울을 다시 단다
```

⚠ (이력) 당시 세션이 남긴 문장 — 판단은 위에서 닫혔다.
⚠ **판단이 필요한 자리는 하나로 좁혀진다** — 안 1의 키보드 대가를 받을 것인가.
띠를 없애는 값이 "포커스 끌어올림을 잃는 것"보다 큰지가 전부다.
⛔ 세션이 정하지 않는다. 실측은 위에 다 있다.

---

### 2.106 ★ 키보드가 「마음 들여다보기」를 가린다 — 진단과 안 · **안 1로 확정**(2.107에서 적용) (2026-09-02)

> ⛔ **진단과 안만이다. 아무것도 고치지 않았다** (사용자 지시).
> ⚠ 사용자가 붙인 두 번째 이미지는 **직접 편집한 것**이고 실제 동작이 아니다 —
> 원하는 모습을 그려 보인 것이다. 아래 수치는 전부 세션이 잰 것이다.

#### ★ 원인 — 사용자의 가설이 맞다. **크기 문제가 아니라 스크롤 문제다**

```
공간은 확보된다   키보드가 뜨면 웹뷰가 줄어든다 (adjust=resize · 실측 innerHeight 592 → 341)
그런데 안 움직인다  scrollY = **0**. 스크롤 여유가 160px 있는데(문서 501 − 뷰포트 341) 0이다
```

**왜 브라우저가 스크롤하지 않는가 — 스크롤할 이유가 없기 때문이다.**

```
브라우저 기본 동작은 **포커스된 요소**를 보이게 한다. 그 요소는 입력란이고,
입력란은 bottom 294 < 341이라 **이미 보인다.** → 스크롤이 일어나지 않는다.
⛔ 가려지는 것은 입력란이 아니라 **그 아래 버튼**이고, 버튼은 포커스 대상이 아니다.
★ 그래서 "입력란이 보이니 괜찮다"는 판정과 "버튼이 가린다"는 증상이 동시에 성립한다.
```

**코드는 관여하지 않는다** (확인함).

```
scrollIntoView · autoFocus · onFocus  → 소스에 **하나도 없다**
window.scrollTo({top:0})            → 있다. 그러나 의존성이
                                     [phase, mode, selectedCategory, result, showAbout, dailyVerse]라
                                     **포커스·키보드와 무관하다.** 이 증상과 관계없다
AndroidManifest에 windowSoftInputMode 없음 → Capacitor 기본 adjust=resize (dumpsys 확인)
```

#### ✅ 실측 — 341 안에 **충분히 들어간다**

```
                 키보드 전(592)      키보드 후(341)
innerHeight        592                341
scrollY              0                  0        ← 안 움직인다
docScrollHeight    592                501        ← 스크롤 여유 160px
입력란            [244, 294]         [244, 294]   ✅ 보인다 (294 < 341, 여유 47)
마음 들여다보기     [320, 371]         [320, 371]   ⛔ **371 > 341 → 30px 잘린다**
골라서 찾을래요     [389, 409]         [389, 409]   ⛔ 완전히 밖

★ 입력란 위 244 ~ 버튼 아래 371 = **127px.** 뷰포트 341이 그보다 훨씬 크다.
  필요한 스크롤은 **30px**뿐이고 가용 여유는 160px이다 — 스크롤로 풀린다
```

#### ✅ 스크롤로 실제 풀리는지 **실험으로 확인했다**

```
block:"nearest" (버튼 기준)   scrollY 30  입력란 [214,265] ✓ · 버튼 [291,341] ✓
                            ⚠ "골라서 찾을래요"는 여전히 잘린다
block:"center" (입력란 기준)  scrollY 99  입력란 [145,196] ✓ · 버튼 [222,272] ✓
                            ✅ **"골라서 찾을래요" [290,310]까지 전부 보인다**
✅ 사용자 제약 충족 — 입력란이 화면 위로 사라지지 않는다 (top 145 > 0, 여유 넉넉하다)
```

**★ 「내려가면 원래대로」는 공짜다 (실측).**

```
키보드를 내리면  innerHeight 592 · docScrollHeight 592 → 스크롤 여유가 **0이 되어**
              브라우저가 scrollY를 **스스로 0으로 되돌린다** (scrollY 99 → 0 실측).
⛔ 돌아오는 쪽에 코드가 필요 없다. 올리는 쪽만 만들면 된다
```

#### ⚠ 먼저 알아 둘 것 — **기능이 막히지는 않는다**

```
input의 onKeyDown이 Enter에 onSubmit을 건다. 키보드의 **→ 키로 제출된다.**
즉 이것은 **시각·조작 편의 문제이지 기능 차단이 아니다.** 안을 고를 때 그 무게로 볼 것
```

#### 🔴 안 셋 — 사용자가 고른다

```
안 1 ★ 권고 — 뷰포트가 줄어든 뒤 입력란을 scrollIntoView({block:"center"})
   ⚠ **타이밍이 요점이다.** focus 시점에는 아직 592라 스크롤할 것이 없다.
     축소된 **뒤에** 해야 하므로 window(또는 visualViewport)의 resize를 듣는다
   ✅ 셋(입력란·버튼·전환)이 전부 보인다 (실측)
   ✅ 입력란이 위로 사라지지 않는다 (실측 top 145)
   ✅ 내려갈 때는 아무것도 안 해도 된다 (실측)
   대가  브라우저 기본 동작에 코드가 개입한다. 회귀가 필요하다 —
        "포커스 후 축소되면 셋이 다 보인다"를 순수 함수로 단언할 수 있는 모양이 아니라
        (레이아웃 값이라) **소스 검사로만** 지킬 수 있다. 그 한계를 알고 넣어야 한다
   ⚠ 갈래 A(WebView ≥140)의 IME 처리가 달라 그쪽 동작은 **미검증이다**(2.104 남은 미검증)

안 2 — 같은 방식이되 block:"nearest" (버튼만 기준)
   ✅ 움직임이 30px로 작아 화면이 덜 튄다
   ⛔ "골라서 찾을래요"는 여전히 가린다 — 셋 중 하나가 남는다
   ⚠ 그 버튼은 **다른 입력 방식으로 가는 통로**다. 가려도 되는지가 판단이다

안 3 — 레이아웃을 줄여 스크롤 없이 341에 담는다 (키보드가 뜨면 hero를 접는다)
   ✅ 스크롤이 필요 없어져 코드가 브라우저 동작에 개입하지 않는다
   ⛔ 2.35·2.36의 색·여백 위계 실측을 건드린다. **호흡하는 빛(orb)이 사라진다**
   ⛔ 셋 중 가장 침습적이고, 되돌리기도 가장 어렵다
```

---

### 2.107 ★ 키보드가 올라오면 입력란을 끌어온다 — 안 1 적용 (2026-09-02 · ✅ 실측 완료)

> ✅ **2.106의 안 1로 갔다** (사용자 결정).
> 근거 — 「골라서 찾을래요」는 자연어로 감정을 옮기기 어려운 사람이 **키워드 선택으로 가는
> 통로**다. 그 상태에 있는 사람에게는 유일한 경로일 수 있어 가려두면 안 된다.
> 안 2(`nearest`)는 그것을 남기고, 안 3은 2.35·2.36 위계 실측을 건드린다.

#### ★ 먼저 닫은 것 — 갈래 A에서도 뷰포트가 **줄어든다**

```
사용자 우려   "갈래 A(passthrough)는 웹뷰를 줄이지 않고 IME 인셋만 패딩으로 준다 →
            뷰포트가 안 줄어 scrollIntoView가 아예 안 듣는다"
⛔ 뒤집힌다 — **그 패딩이 곧 웹뷰를 줄이는 기전이다.**

실측이 그것을 보인다 (키보드가 뜬 상태의 뷰 계층)
  부모 ViewGroup  [0,0][1080,1920]     ← **안 줄었다.** 창은 그대로다
  WebView        [0,72][1080,1095]    ← 높이 1023px = 341dp. **웹뷰만 줄었다**
  → 위 72 · 아래 825(=IME 275dp)의 **부모 패딩**이 웹뷰를 줄인 것이다

코드가 두 갈래에 같은 값을 쓴다 (SystemBars.java)
  갈래 A  v.setPadding(0, 0, 0, keyboardVisible ? imeInsets.bottom : 0)
  갈래 B  v.setPadding(left, top, right, keyboardVisible ? imeInsets.bottom : bars.bottom)
  ★ **아래값이 같다.** 다른 것은 위/좌/우뿐이다
  → 갈래 A 예측: 640 → 365dp (1920 − 825). 역시 크게 줄고 resize가 온다
⚠ 여전히 **진짜 갈래 A를 돌린 것은 아니다.** 근거는 "같은 줄이 같은 값을 쓴다"는
  코드 읽기 + "그 패딩이 실제로 웹뷰를 줄인다"는 실측이다. 아래 미검증 참조
```

#### ✅ 무엇을 듣는가 — visualViewport와 window를 **둘 다**

```
실측(갈래 B)   innerHeight 592 → 341 · visualViewport.height 592 → 341   **둘 다 온다**
★ 그래도 둘을 다 듣는다 — 레이아웃은 그대로인데 **IME가 덮기만 하는** 구성에서는
  visualViewport만 줄어든다. 하단에서 세운 기준과 같다 — 시험할 수 없는 갈래가
  있으면 **넓게 잡는다**
⚠ 먼저 온 쪽이 prev를 갱신하므로 뒤에 온 쪽은 축소량 0으로 보고 아무것도 안 한다
⛔ focus에 걸지 않았다. focus 시점에는 뷰포트가 **아직 592라** 스크롤할 것이 없다
```

#### ✅ 실측 — 사용자가 요구한 넷을 전부 확인했다

```
① 셋이 다 보이는가 (키보드 뜬 뒤 · 뷰포트 341)
     scrollY  0 → **99** (훅이 스크롤했다)
     입력란            [145, 196]   ✅
     마음 들여다보기     [222, 272]   ✅  (전에는 [320,371]로 371 > 341이라 잘렸다)
     골라서 찾을래요     [290, 310]   ✅  (전에는 [389,409]로 완전히 밖이었다)
② 입력란이 위로 사라지지 않는가   top 145 > 0 ✅ (실험 예측값과 정확히 일치)
③ 내리면 scrollY가 0으로 오는가  99 → **0** ✅ (코드 없이 브라우저가 한다)
④ 다른 화면으로 새지 않는가      ★ **리스너 수준에서 쟀다** (CDP getEventListeners)
     입력 화면   window {error:1, resize:1} · visualViewport {resize:1}
     선택 화면   window {error:1}          · visualViewport {}   ← **0개**
     결과 화면   window {error:1, scroll:1} · visualViewport {}   ← **0개**
                (scroll은 원래 있던 FloatingRestart의 것이다)
     → TextMode가 언마운트되면 리스너가 **사라진다.** 구조로 지켜진다
```

#### ⛔ 이 수정이 못 하는 것 — 뷰포트가 안 줄면 아무 일도 안 한다

```
웹은 키보드를 직접 볼 수 없다. **뷰포트가 줄어드는 것으로만 안다.**
실제로 줄지 않는 구성을 이 세션에서 둘 봤다 —
  · insetsHandling "disable" (2.105 실험)       innerHeight 640 고정
  · 에뮬레이터의 **스타일러스 필기 모드**          innerHeight 592 고정
    ⚠ 이 자리에서 한 번 헛짚었다. 필기 모드를 켜 둔 채 재고 "안 줄어든다"고 볼 뻔했다.
      끄고 다시 재서 341을 얻었다 — **측정 환경 자체가 변수였다**
그런 자리에서 이 훅은 **조용히 아무 일도 하지 않는다.** 증상이 수정 전과 같아진다.
⛔ 웹에서 고칠 방법이 없다. 한계로 적는다
★ 다만 기능이 막히지는 않는다 — input의 onKeyDown이 Enter에 제출을 건다.
  키보드의 → 키로 제출된다
```

#### ⚠⚠ 회귀의 한계 — **소스의 모양만 지킨다**

```
✅ 회귀가 단언할 수 있는 것 (behavior.test.js · 4건)
   · 훅이 있고 **TextMode 안에서만** 호출된다 (다른 화면으로 새지 않는다)
   · 방아쇠가 focus가 아니라 **축소**다 — 임계 상수와 resize 구독이 있다
   · visualViewport와 window를 둘 다 듣는다
   · 포커스가 그 입력란일 때만 움직인다 (회전 같은 다른 축소에 안 끌려간다)
   · block "center"다 — "nearest"로 되돌아가면 회귀가 선다
   · 되돌리는 코드가 없다 (브라우저가 한다)
⛔ 회귀가 **못 하는 것** — 실측으로만 안다. 위 「실측」 절이 유일한 근거다
   · 키보드가 뜬 뒤 셋이 실제로 보이는가        ← 레이아웃 값이다
   · 입력란이 화면 위로 사라지지 않는가          ← 레이아웃 값이다
   · 갈래 A에서 동작하는가                    ← 시험할 방법이 없다
   · 뷰포트가 안 줄어드는 구성에서 조용히 실패하는 것
★ 그래서 **이 절의 수치가 회귀를 대신한다.** 레이아웃을 건드리면 여기를 다시 잰다.
  ⚠ 임계값 120px을 줄이거나 입력란을 아래로 옮기면 **회귀는 통과하면서 화면이 깨진다**
```

#### ⚠⚠ 끝나면 되돌릴 것 — 에뮬레이터의 스타일러스 필기

```
secure stylus_handwriting_enabled 를 **0으로 꺼 두었다** (원래 1이다).
켜져 있으면 입력란을 탭할 때 소프트 키보드 대신 **필기 도구**가 뜨고,
그때는 뷰포트가 줄지 않아(592 고정) 이 절의 실측이 아예 불가능하다.
★ 이 자리에서 한 번 헛짚었다 — 켜 둔 채 재고 "안 줄어든다"고 볼 뻔했다.
⛔ **키보드 검증이 끝나면 1로 되돌린다.** 잊으면 다음 세션이 필기 모드에서 재고
  같은 함정에 걸린다 (사용자 지시 · 2026-09-02).
    adb shell settings put secure stylus_handwriting_enabled 1
```

#### ⛔ 갈래 A를 시험할 방법 — 다시 봤고, 없다

```
붙은 기기      에뮬레이터 하나뿐이다 (adb devices — 태블릿 없음)
시스템 이미지   google_apis 하나뿐이다 (Play 스토어 없음) → WebView 124 고정
google_apis_playstore  받으면 Play로 갱신할 수 있지만 **계정 로그인이 필요하고**,
              그 이미지의 WebView가 140 이상이라는 보장도 없다
⚠ WebView가 /data/app 에 있어 이론상 adb install로 갱신할 수 있다. 그러나
  외부에서 APK를 받아 출처를 검증해야 하는 일이라 세션이 임의로 하지 않았다
★ 실기기 확인 한 줄
    adb shell dumpsys webviewupdate | findstr "Current WebView package"
  140 이상이면 갈래 A다. 그 기기에서 **입력 화면에 키보드를 띄워** 셋이 보이는지 본다
```

---

### 2.108 ✅ 원복 대조 — 오늘 실험 둘이 저장소로 새지 않았다 (2026-09-02)

> 사용자 요청으로 전체를 대조했다. **저장소는 깨끗하다.** 기기 설정 하나만 남아 있다.

```
실험 1  insetsHandling "disable" (2.105 진단)
       → capacitor.config.json 이 **이 세션에서 한 번도 커밋되지 않았다**
         (git log dcece7b..HEAD -- app/capacitor.config.json 이 비어 있다)
       → 지금 파일에 plugins 키가 없다. 원복 확인
실험 2  3버튼 내비게이션 오버레이 (2.104 미검증 ② 실측)
       → cmd overlay list: gestural [x] · threebutton [ ]. 원복 확인
       → 기기 설정이라 저장소와 무관하다

★ ①(상단 검은 여백)이 남아 있는 것은 **정상이다.**
  styles.xml · colors.xml · AndroidManifest.xml 이 이 세션에서 **한 번도 커밋되지 않았다.**
  windowBackground는 2.91이 정한 값 그대로이고, 2.105에서 안 3(그대로 둔다)으로
  확정했으므로 남아 있는 것이 의도다. 실험 원복 때 함께 되돌아간 것은 없다

작업 트리  깨끗(untracked 포함 0건) · origin/main과 동기화됨
```

⚠ **되돌리지 않은 것 하나 — 저장소가 아니라 에뮬레이터 설정이다.**

```
secure stylus_handwriting_enabled = 0   (원래 1이었다. 세션이 껐다)
왜 껐나   켜져 있으면 입력란을 탭할 때 **소프트 키보드 대신 스타일러스 필기 도구**가
         뜨고, 그때는 뷰포트가 줄지 않는다(592 고정). 2.107의 실측이 불가능하다
★ 이 자리에서 한 번 헛짚었다 — 켜 둔 채 재고 "안 줄어든다"고 볼 뻔했다.
  **측정 환경 자체가 변수였다**(2.107에 적었다)
지금 끈 채로 두었다  키보드 검증이 계속되는 동안 필요하기 때문이다.
되돌리려면        adb shell settings put secure stylus_handwriting_enabled 1
```

---

### 2.109 ⛔ 배너 아래 여백이 위보다 크다 — 진단과 안 (2026-09-02 · 고치지 않았다)

> ⛔ **진단과 안만이다.** ①③과 섞지 않았다.

#### ✅ 그 자리가 무엇인가 — 픽셀로 갈랐다. **누수가 아니라 두 겹이다**

```
실측 (제스처 내비 · 배너 [60,1626][1020,1776] · 웹뷰 [0,72][1080,1848] · 화면 1920)

  배너 위 배경면    1578~1625   16 dp   T.ink    (20,30,36)
  배너             1626~1776   50 dp
  배너 아래 배경면   1777~1847   24 dp   T.ink    (20,30,36)   ← ✅ 배경면이다. 누수 아니다
  웹뷰 밖(창배경)   1848~1919   24 dp   inkDeep  (11,18,22)   ← ⚠ **더 어둡다**

★ 아래는 **두 겹이고 합쳐서 48dp다.** 위는 16dp다 — 그래서 3배로 보인다.
★ 사용자가 본 "어두운 띠"는 아래쪽 24dp, 즉 **웹뷰 밖의 창배경**이다.
  2.105가 찾은 「하단 이음매」와 같은 자리다
```

#### ⛔ 아래 여백은 줄일 수 없다 — 구조가 그렇다

```
배경면 높이 = bannerSpace = BANNER_HEIGHT(50) + BANNER_GAP(40) = 90dp (웹뷰 아래끝 기준)
배너       = [인셋, 인셋+50]
  위 여백 = 90 − 인셋 − 50 = **40 − 인셋**
  아래 여백 = **인셋**
  ★ 위 + 아래 = **BANNER_GAP = 40으로 고정**이다. 인셋이 그 40을 나눌 뿐이다
     제스처(24) → 16 / 24      3버튼(48) → −8 / 48 (배너가 위로 삐져나온다)

⛔ 아래를 줄이려면 배너를 내려야 하는데
   · 플러그인 margin에 음수를 주려면 **인셋 값을 알아야 한다** — 웹에서 못 읽는다(2.103)
   · 내리면 배너가 **제스처 바와 겹친다.** 광고가 시스템 UI에 가리는 자리다
⛔ 배경면을 줄이면 **윗변만 내려온다.** 아래끝은 웹뷰 아래끝에 고정이라 아래는 안 변한다
⛔ 배경면 아래끝을 배너 아래끝에 맞추면 그 아래로 **웹뷰 내용이 다시 비친다** —
   2.103의 결함이 그대로 돌아온다
★ 그리고 아래 24dp는 **제스처 바가 그려지는 자리다.** 비워 둔 여백이 아니라 시스템 영역이다
```

#### ★ 「셋을 다 얻는 길」을 찾아봤다 — **있다. 그러나 지금 공짜인 것을 손으로 다시 만든다**

> 사용자 지적이 맞다 — `insetsHandling "disable"`이면 상단 검은 띠와 하단 어두운 띠가
> **둘 다 없어진다**(2.105 실험에서 실측). 웹뷰가 화면 전체가 되어 위 72px에 그라디언트가
> 그려지고, 아래 24dp 창배경도 웹뷰 안으로 들어온다. 하나를 바꿔 둘이 풀린다.
> ⛔ 문제는 그 설정이 2.107(키보드 대응)을 무력화한다는 것이다.

**길 1 — `insetsHandling "disable"` + `@capacitor/keyboard` (패키지를 받아 소스를 읽었다)**

```
✅ keyboardHeight가 **실제로 웹에 온다**
   Keyboard.java  keyboardEventListener.onKeyboardEvent(EVENT_KB_WILL_SHOW,
                    Math.round(imeHeight / density))
   → WindowInsetsCompat.Type.ime() 에서 읽어 **dp로** 넘긴다. SystemBars 설정과 무관하다
⛔⛔ 그런데 `resizeOnFullScreen`은 **Capacitor 8에서 죽은 옵션이다**
   possiblyResizeChildOfContent() 첫 줄이
     if (isSystemBarsPluginPresent()) return;
   이고, 그 판정이 Class.forName("com.getcapacitor.plugin.SystemBars")다 —
   **설정이 아니라 클래스 존재**를 본다. SystemBars는 @capacitor/android 코어에 있으므로
   항상 감지된다. insetsHandling을 "disable"로 꺼도 이 옵션은 아무 일도 하지 않는다

★ 그래서 이 길의 실제 모양
   웹뷰가 안 줄어든다(실측 innerHeight 640 고정) → 문서도 640 → **maxScroll = 0**
   → keyboardHeight를 알아도 **밀 곳이 없다.**
   → 문서에 padding-bottom = keyboardHeight 를 손으로 주어 여유를 만들고,
     스크롤을 손으로 계산해야 한다. **뷰포트 축소를 웹이 흉내내는 것이다**

대가
   ① 새 네이티브 의존성 하나 (@capacitor/keyboard 8.0.5)
   ② 2.105 안 1의 대가가 되살아난다 — 상단 여백이 인셋 비의존 고정값이 된다
     (지금 여유 51dp · 컷아웃이 큰 기기에서 27~35dp)
   ③ ★ 지금 Capacitor가 공짜로 해 주는 **뷰포트 축소를 손으로 다시 만든다.**
     조용한 실패 표면이 는다 — keyboardHeight 이벤트가 안 오거나 늦으면
     **아무 신호 없이** 키보드 대응이 죽는다
   ④ 2.107이 방금 실측으로 닫은 것을 전부 다시 열고 다시 재야 한다
   ⚠ 그리고 그 플러그인이 "disable" 조합에서 실제로 이벤트를 주는지는 **미검증이다**
```

**길 2 — 지금 설정을 유지하고 상단 72px에만 그라디언트를 그린다 ⛔ 죽는다**

```
그 자리는 창배경이고, 창배경은 **상단과 하단 양쪽에 동시에 보인다**(2.105 실측).
세로 그라디언트를 주면 위/아래를 따로 맞출 수는 있다. 그러나 **스크롤을 못 따라간다** —
2.105 안 2가 죽은 이유와 정확히 같다. 웹 레이어는 스크롤하는데 네이티브 색은 고정이다.

상단 경계 Δ (창배경 색 선택별 · 실측값 기준)
                     문서 위쪽(웹뷰 top = plum)   문서 아래쪽(웹뷰 top = 14,22,27)
  inkDeep (지금)         Δ(23, 8, 18)                Δ(3, 4, 5)
  ink     (안 3)         Δ(14, 4,  4)                Δ(6, 8, 9)
  plum                  Δ( 0, 0,  0)                Δ(20, 4, 13)
★ **ink가 최댓값을 최소화한다.** 이것이 안 3을 고르는 정량 근거다 —
  어느 스크롤 위치에서도 가장 덜 틀린 하나의 색이다
```

**★ 정리 — 셋을 다 얻는 길은 하나뿐이고, 그 값은 「지금 공짜인 것을 손으로 다시 만들기」다.**

#### 🔴 안 — 아래는 못 줄인다. 줄일 수 있는 것은 **어두운 띠**다

```
안 1 ⛔ 아래 여백을 줄인다 — **불가능하다.** 위 근거 셋
안 2 ⛔ 위 여백을 아래에 맞춰 늘린다 (BANNER_GAP을 키운다)
     위=아래는 인셋 = GAP/2 일 때만 성립한다 → 제스처면 GAP 48, 3버튼이면 96.
     **하나로 못 맞춘다.** 게다가 전체 높이가 커져 ②(광고를 작게)에 역행한다
안 3 ★ 권고 — **어두운 띠만 없앤다.** windowBackground를 inkDeep → ink(배경면과 같은 색)
     ✅ 하단 이음매가 사라진다   지금 Δ(9,12,14) → **Δ 0**
     ✅ ★ **상단 검은 띠도 함께 옅어진다** — 문서 위쪽 경계가
        지금 inkDeep vs plum Δ(23,8,18) → ink vs plum **Δ(14,4,4)**
        ⚠ 2.105의 결정(안 3 · 그대로 둔다)을 뒤집는 것이 아니다. 색 하나로 **증상만** 줄인다
     ⚠ 대가 ① 문서 **아래쪽**에서는 반대로 조금 벌어진다 — 지금 Δ(3,4,5) → Δ(6,8,9)
     ⚠ 대가 ② 2.91이 흰 번쩍임을 막으려고 정한 값을 건드린다.
        실행 첫 프레임 색이 inkDeep → ink가 된다. **둘 다 어두워 번쩍임은 그대로 막힌다**
     ⚠ 대가 ③ 갈래 A에서는 창배경이 아예 안 보인다 — **효과도 손해도 없다**
     ✅ 되돌리기 쉽다 (colors.xml 한 줄) · 인셋 값을 몰라도 맞는다
안 4 아무것도 안 한다
     아래 48dp 중 24dp는 시스템 바 자리이고 24dp는 인셋이라 **구조적으로 필요하다.**
     갈래 A에서는 어두운 띠가 없어 아래가 24dp만 남는다
```
#### ⛔⛔ 안 3을 적용했다가 **되돌렸다** — 같은 시도를 반복하지 말 것 (2026-09-02)

> ★ 이 절이 남는 이유는 **다음 세션이 같은 길을 다시 가지 않게** 하기 위해서다.
> 근거가 그럴듯했고 실제로 상단은 좋아졌는데, **대가가 자리를 바꿨을 뿐이었다.**

```
한 일    styles.xml 의 android:windowBackground 를 pym_ink_deep → pym_ink
        ⛔ colors.xml은 건드리지 않았다 — gen_icons.py가 theme.js에서 생성하는
          파일이고 두 색이 이미 있어 **가리키는 대상만** 바꿨다

실측 결과 (에뮬레이터 = 갈래 B)
  ✅ 흰 번쩍임은 그대로 막혔다 — 홈에서 18프레임을 잡아 확인. 흰 프레임 0
  ✅ 상단 · 문서 위쪽    Δ(23, 8,18) → Δ(14,4,4)     개선
  ⚠ 상단 · 문서 아래쪽   Δ( 3, 4, 5) → Δ(6,8,9)      악화 (예상했던 대가)
  ✅ 하단 · 배너 있음    Δ( 9,12,14) → Δ0            개선
  ⚠⚠ 하단 · 배너 없음   Δ0 → Δ(9,12,14)             **악화 — 예상 못 한 대가**
     웹뷰 아래끝이 그라디언트 끝(inkDeep)인데 창배경이 ink라 **밝은 띠**가 보인다

되돌린 근거 (사용자 판단)
  ① 대가가 **자리를 바꿨을 뿐이다.** 그리고 **배너 없는 화면이 더 많다**
  ② 그중 입력 화면이 **앱의 첫 화면**이고 가장 자주 보는 자리인데 거기가 악화됐다
  ③ ★ 태블릿에서 상단 띠가 **없는 것이 확인됐다**(2.111) — 실기기 대부분이
    갈래 A라면 이것은 **에뮬레이터 한정 문제**다. 애초에 이 변경의 이유가 약해졌다

★ 남는 교훈 — **한 색이 상단과 하단을 동시에 맞출 수 없다.**
  웹뷰는 스크롤하고 그라디언트는 위가 plum·아래가 inkDeep인데 창배경은 고정이다.
  ink의 Δ 표가 "최댓값을 최소화한다"는 것은 맞았지만, **최댓값이 옮겨 간 자리가
  더 자주 보이는 화면이었다.** Δ만 보고 고르면 안 된다 — **어느 화면에서 보이는지**를
  함께 봐야 한다

---

### 2.110 ⛔ 키보드가 뜨면 「이 앱에 대해」가 가려진다 — 진단과 의견 (2026-09-02 · 고치지 않았다)

> ⚠ **2.107의 실측에 없던 요소다.** 잰 셋은 입력란 · 마음 들여다보기 · 골라서 찾을래요였고
> 「이 앱에 대해」는 목록에 없었다. 그래서 못 봤다 — 3절 「일하는 방식」 ①(표본 창)이다.

#### ✅ 실측 — **3px 차이로** 밖이다

```
키보드 후 (뷰포트 341 · scrollY 99 · 문서 501)
  입력란            [145, 196]   ✅
  마음 들여다보기     [222, 272]   ✅
  골라서 찾을래요     [290, 310]   ✅
  이 앱에 대해        [344, 363]   ⛔ top 344 > 341 — **3px 차이로 완전히 밖이다**
```

#### ✅ 341 안에 넷이 다 들어가는가 — **들어간다**

```
필요한 범위   입력란 top 145 ~ 이 앱에 대해 bottom 363 = **218px**
뷰포트       341 ≥ 218 ✅ 충분하다
더 내릴 양    363 − 341 = **22px**
그때 입력란   top 145 − 22 = **123 > 0** ✅ 위로 사라지지 않는다
스크롤 여유   최대 160 · 현재 99 → 61px 남는다. 22px은 여유 안이다
★ 스크롤로 풀 수 있다. 다른 안이 필요한 상황이 아니다
```

#### 🔴 세션 의견 — **넣지 않는 쪽을 권고한다** (사용자가 정한다)

```
★ 「골라서 찾을래요」와 성격이 다르다.
  그것은 **입력이 막힌 사용자의 대안 경로**다 — 자연어로 감정을 옮기기 어려운 사람이
  키워드 선택으로 가는 유일한 통로일 수 있어 그 순간 보여야 했다.
  「이 앱에 대해」는 **부가 정보(앱 설명·방침)** 다. 감정을 적으려고 키보드를 연 사람이
  그 순간 열 일이 없다.
근거 ①  넣으면 입력란이 **22px 더 올라간다**(145 → 123). 입력 화면의 주인공은 입력란이다.
       부가 정보를 보이려고 주인공을 밀어 올리는 거래다
근거 ②  **막히지 않는다.** 키보드를 내리면 즉시 보인다. 접근이 끊기는 것이 아니다
근거 ③  입력 화면에는 광고가 없다(bannerVisible은 결과 화면뿐).
       About의 광고 고지를 이 화면에서 급히 열어야 할 이유가 없다
⚠ 반대 근거도 적어 둔다 — 3px 차이라 "거의 보이는데 잘린" 모양이라 눈에 걸린다.
  잘린 것이 보이는 것보다 나쁘게 읽힐 수 있다
```

#### 🔴 넣기로 하면 — 안 셋

```
안 1 스크롤 기준을 「이 앱에 대해」까지로 넓힌다 (22px 더 내린다)
    ✅ 넷이 다 보인다 · 입력란 top 123으로 유지 (실측 근거 위)
    ⚠ 그 버튼은 **Shell이 그린다** — TextMode 안에서 ref로 못 잡는다.
      훅이 document에서 찾거나 ref를 Shell까지 내려야 한다 → **구조 결합이 생긴다**
    ⛔ 그리고 문서가 길어지면(인사말이 3줄이 되면) 입력란이 더 올라간다.
      **입력란 top의 하한을 함께 두어야 한다** — 그것이 없으면 조용히 깨진다
안 2 키보드가 뜬 동안 「이 앱에 대해」를 **숨긴다**
    ✅ 문서가 짧아져 애초에 가릴 것이 없다. 입력란을 더 밀어 올리지 않는다
    ⚠ 화면 요소가 키보드 상태로 사라지는 규칙이 하나 는다
    ⚠ 2.107의 훅이 "축소를 듣는다"는 것을 이미 알고 있으므로 배선은 있다
안 3 아무것도 안 한다 (★ 권고)
    ✅ 비용 0 · 입력란이 가장 높은 자리를 지킨다
    ⚠ 3px 차이로 잘린 모양은 남는다
```

⚠ **어느 안이든 2.107의 한계가 그대로 걸린다** — 뷰포트가 줄지 않는 구성에서는
스크롤 자체가 일어나지 않아 아무 효과가 없다. 그리고 회귀는 **소스의 모양만** 지킨다.

---

### 2.111 ★ 태블릿(갈래 A) 실측 · windowBackground ink · 「이 앱에 대해」까지 (2026-09-02)

> ★ **오늘 가장 값진 확인이 여기 있다.** 실기기 태블릿이 **갈래 A**였고,
> 이 세션 내내 「시험할 수 없다」고 적어 온 자리가 전부 닫혔다.

#### ★★ 태블릿 = 갈래 A. 미검증이 **전부 닫혔다**

```
기기      Galaxy Tab S9 FE (SM-X510) · Android 15 (API 35)
WebView  **151.0.7922.200**  → 140 이상 = **갈래 A(passthrough)**
화면      1440×2304 px · density 1.75 → **823 × 1317 dp**
인셋      상태바 42px(24dp) · 하단 84px(**48dp** — 삼성 taskbar)
         ★ 하단이 48dp라 2.104에서 잰 3버튼 갈래가 실기기에 그대로 걸린다

① 상단 검은 띠 — **없다**
   WebView [0,0][1440,2304] = **화면 전체.** 인셋 안으로 안 들어간다
   y=0 픽셀 = (33,26,40) ≈ plum → **그라디언트가 상태바 뒤까지 이어진다**
   ✅ 2.105 안 3의 근거 ②("갈래 A에서는 안 보인다")가 **실기기로 확인됐다**
② 배너 기하 — 예측과 일치
   AdView [440,2133][1000,2220] = 560×87px = **320×50 dp** ✓
   아래끝 2220 = 화면 2304 − 인셋 84 ✓
   배경면 [2147,2304] → 배너 아래 **84px(48dp)을 전부 덮는다. 누수 0**(픽셀 확인)
   배너 윗변이 배경면 위로 14px(8dp) 삐져나온다 — 2.104 예측 그대로, 페이드 안이다
   ★ 좌우로 **251dp씩** 배경면이 드러난다. 안 ①(배경 없이)이었다면 그 넓은 자리로
     목록이 비쳤을 것이다 — **배경면이 태블릿에서 훨씬 더 중요하다**
③ ★★ 키보드 — **갈래 A에서도 뷰포트가 줄어든다**
   innerHeight 1316 → **895** (IME 421dp만큼 줄었다)
   ✅ 2.107이 **코드 읽기로만** 주장한 것이 실기기 실측으로 닫혔다 —
     "웹뷰를 줄이는 것은 부모 패딩이고 갈래 A도 같은 imeInsets.bottom을 쓴다"
   ⚠ 태블릿은 화면이 커서 docScrollHeight 895 = innerHeight → **maxScroll 0**.
     스크롤이 필요 없고 넷이 전부 보인다. 훅은 target 0으로 **아무 일도 안 한다** — 안전하다
```

⛔ **2.104의 마지막 미검증(진짜 갈래 A의 키보드 동작)이 이것으로 닫혔다.**

#### ⚠⚠ windowBackground = ink — 적용했다가 **되돌렸다** (기록은 2.109에 모았다)

```
✅ 흰 번쩍임은 그대로 막힌다 (실측)
   홈에서 실행해 18프레임을 잡아 본문 최대 밝기를 봤다.
   스플래시(jade 아이콘·최대 84,119,109) → inkDeep → 웹뷰. **250 근처 흰 프레임이 없다**
✅ 상단이 좋아졌다 (에뮬레이터 = 갈래 B)
   y=0~71 이 (11,18,22) → **(20,30,36)**. 경계 Δ(23,8,18) → **Δ(14,4,4)**

⚠⚠ 그런데 하단은 **자리를 바꿨을 뿐이다** — 승인 때 몰랐던 대가다
   배너 있는 화면(결과)   웹뷰 아래끝이 배경면(ink) → 창배경(ink)   Δ(9,12,14) → **Δ0**  ✅
   배너 없는 화면(입력·About·위기)
                      웹뷰 아래끝이 그라디언트 끝(inkDeep) → 창배경(ink)
                      **Δ0 → Δ(9,12,14)**  ⚠ 새로 눈에 걸린다 (화면으로 확인함)
   ★ 즉 이 색은 **배너가 있을 때 좋아지고 없을 때 나빠진다.**
     입력 화면 맨 아래 24dp가 주변보다 밝은 띠로 보인다

전체 대차 (갈래 B에서만 의미가 있다 — 갈래 A는 창배경이 아예 안 보인다)
  상단 · 문서 위쪽    Δ(23,8,18) → Δ(14,4,4)    ✅ 개선 (가장 눈에 띄던 자리)
  상단 · 문서 아래쪽   Δ(3,4,5)   → Δ(6,8,9)     ⚠ 악화 (예상했던 대가)
  하단 · 배너 있음    Δ(9,12,14) → Δ0           ✅ 개선
  하단 · 배너 없음    Δ0         → Δ(9,12,14)   ⚠ 악화 (**예상 못 한 대가**)

✅ **되돌렸다** (사용자 판단 · 2026-09-02). 근거와 Δ 표는 **2.109**에 모아 두었다 —
  다음 세션이 같은 시도를 반복하지 않게 하는 것이 그 절의 목적이다
⛔ colors.xml은 **건드리지 않았다** — gen_icons.py가 theme.js에서 생성하는 파일이고
  두 색이 이미 다 있어서, styles.xml에서 **어느 것을 가리킬지만** 바꿨다.
  (사용자는 colors.xml을 처음 건드리는 커밋이라고 적으라 했는데, 실제로는 그 파일을
   건드릴 필요가 없었다. 이 세션에서 **처음 건드린 파일은 styles.xml**이다)
★ 그리고 이것은 **완전한 해결이 아니다.** 상단에 Δ(14,4,4)가 남는다 —
  증상을 줄인 것이지 없앤 것이 아니다
```

#### ✅ 「이 앱에 대해」 — 설계대로 넣었다 (ref 없이)

```
기준을 바꿨다   "요소를 화면에 넣는다"(scrollIntoView) → **"문서를 끝까지 밀되
              입력란 하한에서 멈춘다"**(clamp된 scrollTo)
✅ 아래쪽 요소에 **ref를 달지 않는다.** 「이 앱에 대해」는 Shell이 그리므로
  TextMode에서 잡으려면 구조가 얽힌다 — 문서 끝을 기준으로 두어 피했다
✅ 아래에 요소가 추가돼도 자동으로 포함된다
MIN_INPUT_TOP = 100  입력란 윗변의 하한. 회귀가 값(≥60)과 clamp 사용을 함께 단언한다
              ⛔ 일부러 40으로 낮추고 clamp를 걷어내 **둘 다 무는 것을 확인했다**

⚠⚠ **보장하는 것과 실측인 것을 갈라 둔다**
   보장    입력란 하한 하나뿐이다. 문서가 길어지면 **아래쪽 요소가 먼저 밀린다** —
          그것이 의도한 우선순위다
   실측    「이 앱에 대해」가 보이는 것은 **보장이 아니라 지금 레이아웃의 결과다.**
          ⛔ 문서가 **44px 이상 길어지면 다시 밀린다**
```

---

### 2.112 ★ 배너가 배경면 윗변 위로 튀어나온다 — 진단 · **안 2 적용·실측** (2026-09-02)

> ⛔ **진단과 안만이다.**
> ⚠ 2.104에서 이미 잰 성질이고, 그때 **"페이드 안이라 티가 안 난다"고 판단했다.**
> **그 판단이 틀렸다** — 실기기에서 걸린다 (사용자 지적).

#### ✅ 실측 (태블릿 · 갈래 A · 하단 인셋 48dp)

```
배경면 윗변   y = 2147  (픽셀로 찾았다 · 예상 2146과 일치)
배너 윗변     y = 2133
★ 배너가 배경면 위로 **14px = 8dp 튀어나온다**

페이드 구간   bottom: bottomInset−1 · height: BAND_FADE(24)+1 = 25dp = 43.75px
            → 대략 [2105, 2149]
배너 윗변 2133은 그 **안에 있다** — 페이드 아래쪽 33% 지점, 불투명도 약 67%
⛔ 그런데도 걸린다. **페이드가 배경을 흐릴 뿐 배너 자체는 밝다** —
  광고가 흰 바탕이면 그 윗변이 배경면 윗변보다 위에 있는 단차가 그대로 보인다
★ 태블릿에서 특히 심하다 — 좌우 251dp씩 배경면이 드러나 **면 윗변 라인이 길게 보이고**
  배너가 그 위로 나온 단차가 확연하다. 폰(20dp)에서는 짧아서 덜 보였다
```

#### 구조 — 왜 음수가 되는가

```
배경면 높이 = BANNER_HEIGHT(50) + BANNER_GAP(G)
배너       = [인셋, 인셋+50]
  위 여백 = (50+G) − (인셋+50) = **G − 인셋**
  아래 여백 = 인셋
⛔ G=40인데 인셋이 48이면 위 여백이 **−8**이다. 그것이 튀어나오는 값이다
⚠ 태블릿 하단 인셋 48dp를 만드는 것은 **삼성 taskbar**다. 3버튼 내비와 같은 값이라
  2.104에서 오버레이로 잰 갈래가 실기기에 그대로 걸렸다
```

#### ★★ 새로 안 것 — **갈래 A에서는 인셋을 읽을 수 있다**

```
태블릿(갈래 A) 실측
  --safe-area-inset-bottom = **48px**   env(safe-area-inset-bottom) = **48px**
  --safe-area-inset-top    = 24px       env(safe-area-inset-top)    = 24px
에뮬레이터(갈래 B) 실측 (2.103)
  전부 **0px**

★ 2.103의 "인셋을 못 읽는다"는 제약이 **갈래별로 갈린다.**
  갈래 A는 읽을 수 있고, 갈래 B만 못 읽는다.
  → 배경면 높이에 safe-area를 더하면 **갈래 A에서는 인셋이 정확히 흡수되어
    위 여백이 인셋과 무관하게 G로 고정된다.** 갈래 B만 상수로 때우면 된다
```

#### ⚠ 인셋 상한은 알 수 있는가 — **갈래 A에서는 필요 없고, 갈래 B에서는 모른다**

```
갈래 A   읽으니까 상한이 필요 없다. 이것이 구조로 푸는 길이다
갈래 B   알려진 값은 제스처 24dp · 3버튼 48dp뿐이다. 그보다 큰 값이 없다고
        **보장할 수 없다** — 그래서 상수를 키우는 안은 항상 "충분히 큰가"가 남는다
```

#### 🔴 안 셋 — 사용자가 고른다

```
안 1  BANNER_GAP 40 → 48 이상 (상수만 올린다)
     ✅ 가장 단순하다. 한 값이다
     계산   G=48  위/아래 = 24/24(인셋24) · **0/48**(인셋48) · 배경면 98dp
            G=64  위/아래 = 40/24(인셋24) · 16/48(인셋48) · 배경면 114dp
     ⚠ 대가 — 인셋이 작은 기기에서 배경면이 커진다. 640dp 화면에서
       98dp = 15.3% · 114dp = **17.8%**. ★ ②에서 "100dp는 16%라 크다"고
       내렸던 것보다 커진다 — 광고는 여전히 50dp지만 차지하는 자리가 그렇다
     ⛔ 그리고 갈래 B에서 인셋이 48보다 크면 다시 음수가 된다

안 2 ★ 권고 — **배경면 높이에 safe-area를 더한다** + 갈래별 GAP
     배경면 높이 = BANNER_HEIGHT + safeAreaBottom + (safeAreaBottom > 0 ? 24 : 48)
     계산
       태블릿(A·48)      위 24 · 아래 48 · 배경면 122dp (1317 화면의 **9.3%**)
       갈래A 폰 제스처(24) 위 24 · 아래 24 · 배경면 98dp  (640의 15.3%)
       에뮬(B·24)        위 24 · 아래 24 · 배경면 98dp  (15.3%)
       갈래B 3버튼(48)    위 **0** · 아래 48 · 배경면 98dp
     ✅ 갈래 A에서 **인셋이 정확히 흡수된다** — 위 여백이 인셋과 무관해진다
     ✅ 갈래 A는 상한 가정이 필요 없다. 갈래 B만 상수로 때운다
     ✅ 갈래 판정이 틀려도 **안전한 방향**이다 — safeArea를 0으로 오판하면
       GAP 48이 적용되어 여백이 넉넉해진다
     ⚠ 대가 — 상수가 둘이고 분기가 하나 는다. 그리고 갈래 A 폰에서 배경면 98dp
     ⛔ 갈래 B 3버튼에서 위 여백이 0이다(음수는 아니다). 붙어 보인다

안 3  페이드를 늘린다 (BAND_FADE 24 → 48)
     ⛔ **실측이 부족하다고 말한다.** 지금도 배너 윗변은 페이드 안(불투명도 67%)에
       있는데 걸린다. 페이드는 배경을 흐릴 뿐 **배너의 밝기를 바꾸지 않는다**
     ⚠ 보조로는 쓸 수 있다 — 안 1·2와 함께라면 단차가 더 부드러워진다
```

⚠ 어느 안이든 **아래 여백은 그대로 인셋이다**(2.109 결론 유지) — 줄일 수 없다.
바뀌는 것은 **위 여백뿐이다.**
#### ✅ 안 2를 적용했다 — 실측으로 닫았다 (2026-09-02)

```
새 식   bannerSpace = BANNER_HEIGHT + safeArea + (safeArea > 0 ? 24 : 48)
       위 여백 = safeArea + GAP − 인셋
       ★ 갈래 A는 safeArea = 인셋이라 **위 여백이 GAP(24)으로 고정된다** — 인셋 무관
       ⛔ 갈래 B는 safeArea = 0이라 GAP(48)이 인셋 상한을 가정한다

실측 (셋 다 예측과 정확히 일치)
  태블릿 (갈래A · 48dp)   배경면 윗변 2091 · 배너 윗변 2133 → 위 **24dp** ✅
                        (전에는 배너가 14px 튀어나왔다)
  에뮬 제스처 (갈래B·24)  배경면 윗변 1554 · 배너 윗변 1626 → 위 **24dp** ✅ (전 16dp)
  에뮬 3버튼 (갈래B·48)   배경면 윗변 1482 · 배너 윗변 1482 → 위 **0dp** ⚠
  filled=false          배경면 **0개** · 하단 요소가 전부 bottomInset 0 자리로 내려온다 ✅
                        (다시 적기 24 · 이 앱에 대해 40 · 다시 적어보기 93)
```

**⚠ 남는 성질 — 갈래 B + 3버튼에서 위 여백이 0이다**

```
튀어나오지는 않는다(전에는 −8이었다). 그러나 **딱 붙는다** —
화면으로 확인했다: 목록 항목이 배너 윗변에 닿아 여백이 전혀 없다.
⛔ 이것이 이 안이 남기는 한계다
```

**🔴 0을 없애려면 — 계산 (판단 대기)**

```
갈래 B 위 여백 = GAP_UNKNOWN − 인셋. 하나의 상수가 인셋 24와 48을 **함께** 커버해야 한다

 GAP_U   인셋24 위   인셋48 위   배경면(갈래B)   640dp 화면비
   48       24         **0**       98dp          15.3%   ← 지금
   56       32           8        106dp          16.6%
   64       40          16        114dp          17.8%
   72       48          24        122dp          19.1%

⚠ 대가는 **갈래 B 기기에만** 걸린다 — 갈래 A는 GAP_KNOWN(24)을 쓰므로 영향이 없다
⚠ 그리고 인셋 24 기기가 그만큼 과잉이 된다. 48에서 8을 얻으려면 24에서 32가 된다
★ 갈래 B는 구형 WebView라 점점 줄어든다. **소수 기기의 한 조합**을 위해
  모든 갈래 B 기기의 배경면을 8~24dp 키우는 거래다
⛔ 세션이 정하지 않는다. 지금은 48로 두었다
```
#### ✅ 결정 — **GAP_UNKNOWN 48을 유지한다** (2026-09-02 · 사용자 판단)

```
근거   갈래 B는 구형 WebView라 **점점 줄어든다.** 그 소수 기기의 한 조합
      (갈래 B + 3버튼)을 위해 **모든 갈래 B 기기의 배경면을 키우는 거래**다.
      48에서 8을 얻으려면 인셋 24 기기에서 32가 되는 대가가 크다(위 계산표)
⚠ 그래서 **갈래 B + 3버튼에서 위 여백이 0이다.** 튀어나오지는 않고 딱 붙는다

🔴 다시 열 조건 — **갈래 B + 3버튼 조합을 쓰는 사용자 신고가 실제로 들어올 때**
  그때 위 계산표에서 값을 고른다. 표가 이미 있으므로 다시 재지 않아도 된다
  ⛔ 그 전에는 열지 않는다. "그럴 것 같다"로 키우지 않는다
```

**⚠ 갈래 판정이 틀리면 — 계산해 두었다**

```
env()가 0으로 오는 갈래 A 기기 (있을 수 있다)
  실제 인셋 24 → UNKNOWN(48) 적용 → 배경면 98dp · 위 여백 **24** ✅ 문제없다
  실제 인셋 48 → UNKNOWN(48) 적용 → 배경면 98dp · 위 여백 **0**  ⚠ 위와 같은 성질
★ **오판이 음수를 만들지 않는다** — 이것이 이 안을 고른 이유의 하나다
⛔ 반대(갈래 B인데 safeArea가 실제 인셋보다 작게 잡힘)만 음수가 되는데,
  갈래 B에서는 Capacitor가 **0을 주입**하므로 그 경우가 생기지 않는다
⚠ 인셋이 0인 기기는 "못 읽는다"와 구분되지 않는다 → 항상 UNKNOWN으로 간다.
  위 여백 48로 넉넉해지므로 안전한 방향이다 (회귀가 이것을 단언한다)
```

**⚠⚠ 회귀가 단언하는 것과 못 하는 것**

```
✅ 단언한다 (adsApp.test.js · 순수 함수라 값으로 검사할 수 있다)
   · 간격이 둘이고 UNKNOWN ≥ 48이며 UNKNOWN > KNOWN
   · 갈래 A(safeArea = 인셋)에서 위 여백이 **인셋 0/16/24/48/64/96 전부에서 24로 고정**
   · 갈래 B(safeArea = 0)에서 인셋 24·48 모두 위 여백이 음수가 아니다
   · 상한을 넘는 인셋(64)에서는 음수가 되는 성질 — **문서와 코드가 갈리지 않게 고정한다**
   · safeArea가 음수·NaN·문자열이어도 UNKNOWN으로 떨어진다
   ⛔ 일부러 UNKNOWN을 40으로 낮추고 safeArea를 무시하게 만들어 **둘 다 무는 것을 확인했다**
⛔ 못 한다 — 실측으로만 안다 (위 실측 표가 유일한 근거다)
   · 배경면 윗변이 실제로 배너를 담는가        ← 레이아웃 값이다
   · env()가 그 기기에서 실제로 무엇을 주는가   ← 기기마다 다르다
   · 갈래 판정이 실기기에서 맞는가             ← 태블릿 하나만 봤다
```

#### ✅ 2026-09-03 재검토 — **안 1(그대로 둔다)로 확정. GAP_UNKNOWN 48 유지**

갈래 판정 기준이 틀렸다는 것이 드러나(2.115) 이 절의 판단 근거를 다시 놓았다.
**결론은 유지고, 근거는 오히려 강해졌다.**

```
사용자 판단 (2026-09-03)
  ① C 줄이 **예측**이라 3번(배너 기하)을 재기 전에 값을 바꾸면 안 된다
  ② 갈래 B가 오히려 **더 드물어졌다** — Android 15 + 옛 웹뷰라는 좁은 조합이다.
    "소수라서 받는다"는 근거가 약해진 것이 아니라 **강해졌다**
⛔ 안 4(창을 edge-to-edge로 직접 켠다)는 **하지 않는다** —
  2.105 상단 띠가 갈래 C 기기 전부에서 되살아나고,
  2.115의 시스템 바 색 수정이 통째로 무의미해진다
```

**새 계산표** — `위 여백 = safeArea + GAP − 웹좌표계 인셋`

```
                        safeArea  GAP  웹좌표 인셋   위 여백    아래 틈   배경면
갈래 A 태블릿(48)          48      24      48       24 ✅실측    48      122dp
갈래 A 폰 제스처(24)        24      24      24       24         24       98dp
갈래 B 에뮬 제스처(24)       0      48      24       24 ✅실측    24       98dp
갈래 B 3버튼(48)            0      48      48        0 🔴        48       98dp
갈래 C 이 휴대폰(3버튼)      0      48       0       48 ✅**실측**  0        98dp
```

✅ **C 줄은 2026-09-03에 실측으로 바뀌었다.** 예측(위 48 · 아래 0 · 배경면 98dp)이
그대로 맞았다 — 상세는 바로 아래 「3번 배너 기하」 절이다.
★ 예측이 맞았으므로 안 3(갈래 C를 따로 판정)의 명분이 없어졌다.

⚠ 안 1을 골랐어도 **남는 것이 있다** — 갈래 C에서 위 48 · 아래 0이면 배너가
아래로 치우쳐 보인다. 2.114(중앙 정렬)가 다루는 문제가 갈래 C에서 더 크게
나타난다. 그쪽 판단은 여전히 열려 있다.

#### ✅ 3번 배너 기하 — **갈래 C를 실측했다. 예측이 정확히 맞았다** (2026-09-03)

시험 기기 등록이 끝나 **시험 광고가 항상 채워지므로** 실제 광고 위험 0으로 쟀다.

```
기기   Galaxy Z Flip (SM-F700N) · Android 13 · **갈래 C** · 3버튼(48dp)
확인   배너에 **「테스트 광고」 라벨**이 붙었다 (한국어 로케일) → 콘솔 등록이 먹었다
```

**실측** — 화면 좌표(px)와 웹 좌표(dp)가 정확히 맞아떨어진다.

```
창(웹뷰)     y = 92 … 2492 px   = 800dp   (상태바 92 · 내비 144 를 뺀 값)
배경면       y = 2198 … 2492 px = **294px = 98dp**   (CDP: top 702 · bottom 800 · h 98)
배너         y = 2342 … 2492 px = **150px = 50dp**
  위 여백    2342 − 2198 = **144px = 48dp**   ⟵ 예측 48dp **적중**
  아래 틈    2492 − 2492 = **0dp**            ⟵ 예측 0 **적중**
좌우         배너 320dp가 360dp 화면 가운데 → 양옆 **20dp씩 배경면이 드러난다**
            (x=0..60 과 x=1020..1080 이 배경면 색 #141E24)
safeArea    env(safe-area-inset-bottom) = **0**  (갈래 C 정의 그대로)
```

★ **계산표의 갈래 C 줄이 예측에서 실측으로 바뀌었다.** 근거는 「창이 시스템 바 밑으로
안 들어가므로 배너가 웹뷰 아래끝에 붙고 웹 좌표계 인셋이 0이 된다」였고, 그대로였다.

**🔴 그래서 GAP_UNKNOWN 48을 다시 본 결과 — 안 1(유지)이 그대로 선다**

```
✅ 갈래 C에서 위 48 · 아래 0 · 배경면 98dp — **음수도 아니고 배너가 배경면 안에 있다**
✅ 배경면이 배너 아래를 **완전히 덮는다**(누수 0). 배너 아래끝과 배경면 아래끝이 같다
✅ 예측이 맞았으므로 안 3(갈래 C를 따로 판정)의 명분이 없어졌다 —
   지금 식이 갈래 C를 이미 안전하게 다룬다
⛔ 값을 바꾸지 않았다. 새로 물을 판단도 없다
```

⚠ **다만 2.114(중앙 정렬)에 재료가 하나 생겼다.** 갈래 C에서 배너는 배경면의
**맨 아래**에 붙고 위로만 48dp가 남는다 — 비대칭이 갈래 A(24/48)보다 크다.

```
갈래 A 태블릿   위 24 · 아래 48   배너가 위쪽에 치우친다
갈래 C 휴대폰   위 48 · 아래 **0**  배너가 **바닥에 붙는다**
⚠ 배경면(#141E24)과 문서 아래쪽(#0B1216)의 Δ는 (9,12,14)로 작아서
  비대칭이 눈에 크게 띄지는 않는다 — 2.109가 잰 것과 같은 대비다
⛔ 2.114의 판단은 여전히 열려 있다. 이 실측은 그 판단의 재료일 뿐이고
  여기서 결론을 내지 않는다
```

**⚠ 광고가 채워지는 조건 — 앞서 헛짚은 것을 정정한다**

```
09-03 09:16  등록 전 · 실제 광고 단위   **No fill (code 3)**
09-03 11:47  등록 전 · 실제 광고 단위   **Network error (code 2)** · 60초 타임아웃
09-03 11:51  **등록 후** · 시험 광고      **bannerAdLoaded 3초**
⚠ 중간에 배터리 세이버를 끄고 성공한 회차가 있어 "세이버가 광고를 막는다"고
  적을 뻔했다. **A/B로 반증했다** — 세이버 ON에서도 3초에 채워진다.
  ⛔ 광고 채움과 배터리 세이버는 무관하다. 갈린 것은 **시험 기기 등록 여부**다
★ 그리고 logcat의 setTestDeviceIds 안내 줄은 **등록 뒤에도 계속 찍힌다.**
  콘솔 등록은 서버 쪽이라 SDK가 모른다 — ⛔ **그 줄로 등록 여부를 판단하지 말 것.**
  판단 근거는 배너의 **「테스트 광고」 라벨** 하나뿐이다
```



---

### 2.113 ⛔⛔ 실기기는 **시험 기기가 아니다** — 실제 광고가 나가고 있다 (2026-09-02)

> ⛔ **누르지 말 것.** 개발자 본인의 노출·클릭은 **무효 트래픽**이고 계정이 걸릴 수 있다.

#### 실측 — 태블릿에는 「Test Ad」 라벨이 **없다**

```
에뮬레이터   `adtest=on` · 배너에 **"Test Ad"** 라벨 · "This request is sent from a test device"
           → AdMob이 **자동으로 시험 기기로 잡는다** (2.101에서 확인한 성질)
태블릿      ⛔ adtest 없음 · 라벨 없음 · 실제 상품 광고가 떴다(배너를 확대해 확인)
           logcat이 그대로 말한다 —
             I Ads: Use RequestConfiguration.Builder().setTestDeviceIds(
                      Arrays.asList("<이 기기의 시험 기기 ID>"))
                    to get test ads on this device.
           ★ 이 문장이 뜬다는 것 자체가 **지금은 시험 기기가 아니라는 뜻이다**
⚠ 시험 기기 ID는 logcat에 찍힌다. 이 문서에는 적지 않는다 —
  값을 문서에 두지 않는 규약과 같다(2.101). 필요하면 그때 로그에서 읽는다
```

#### 🔴 시험 기기로 등록하는 두 길 — **콘솔 쪽을 권고한다**

```
길 1 ★ 권고 — AdMob 콘솔에서 등록 (코드를 안 건드린다)
   AdMob → 설정 → **시험 기기** → 기기 추가 → 플랫폼 Android →
   식별자에 logcat의 그 ID를 넣는다 (또는 기기의 광고 ID)
   ✅ 릴리스에 아무것도 실리지 않는다 · 언제든 뺄 수 있다 · 기기마다 한 번이면 된다
   ⚠ 반영에 시간이 걸릴 수 있다. 등록 뒤 앱을 다시 띄워 **"Test Ad" 라벨을 눈으로 확인**할 것

길 2 ⛔ 코드로 등록 — **권하지 않는다**
   플러그인이 initialize({ initializeForTesting: true, testingDevices: [...] })를 받는다
   (AdMob.java:250~ · setTestDeviceIds로 넘어간다)
   ⛔ 그러나 그 값을 소스에 적으면 **릴리스에도 실린다.**
     `build:app`에는 debug/release 구분이 없다(2.99에 적힌 그대로다) —
     즉 "시험 설정이 출시본에 남는" 조용한 실패를 새로 만드는 것이다
   ★ 2.99·2.102가 막으려던 것과 **같은 모양**이다. 그래서 코드로 넣지 않는다
```

⚠ **그때까지는 실기기에서 배너를 누르지 않는다.** 화면을 보는 것(노출)까지도
가급적 줄이는 편이 낫다 — 노출도 트래픽이다.
★ 에뮬레이터는 자동으로 시험 기기라 마음껏 봐도 된다.

---

### 2.114 ⛔ 배너를 배경면 중앙에 놓을 수 있는가 — 진단과 안 (2026-09-02 · 고치지 않았다)

> ⛔ **진단과 안만이다.**

#### ✅ 먼저 — 사용자가 본 것이 맞다. 그리고 **웹이 배너를 올릴 수 있다**

```
실측(태블릿)  배너 [2133,2220] · 배경면 [2091,2304]
            위 여백 42px(24dp) · 아래 여백 84px(48dp)
⚠ 아래가 더 큰데 왜 "붙어 보이나" — **그 48dp를 taskbar가 덮는다.**
  눈에 보이는 여백은 위 24dp뿐이고 아래는 시스템 바다. 관측이 정확했다

★ 그리고 위치를 바꿀 수 있다 — BannerExecutor가
    하단 마진 = adOptions.margin × density + bottomInset
  으로 계산한다. **margin을 주면 배너가 그만큼 올라간다.** 지금은 margin: 0이다
```

#### 계산 — 「보이는 배경면」(시스템 바 위) 안에서 중앙에 놓으려면

```
조건                    배경면   보이는 면   지금 위/아래   중앙에 필요한 margin
태블릿 (갈래A·48)        122dp     74dp       24 / 0            12dp
갈래A 폰 (24)             98dp     74dp       24 / 0            12dp
에뮬 갈래B (24)           98dp     74dp       24 / 0            12dp
갈래B 3버튼 (48)          98dp     50dp        0 / 0             0dp

⚠⚠ 그런데 **margin은 위 여백을 그만큼 먹는다** — 위 여백 = (sa+G−인셋) − margin
   태블릿에 margin 12를 주면 위 여백이 24 → **12로 줄어든다**
   갈래B 3버튼은 이미 0이라 margin을 주면 **다시 튀어나온다**(2.112가 막 고친 것)
```

#### 🔴 안 — 그리고 세션 의견

```
안 1 ★ 권고 — **그대로 둔다**
   ① ⚠ 사용자 지적대로 **아래가 시스템 바에 붙는 것이 안드로이드에서 흔한 형태다.**
     AdMob도 배너를 화면 하단에 붙이는 것을 표준으로 본다
   ② "떠 있는 느낌"은 이미 **좌우**(폰 20dp · 태블릿 251dp)와 **위 24dp**가 만든다.
     아래까지 띄우면 배너가 콘텐츠 쪽으로 더 올라와 화면을 더 먹는다
   ③ ⛔ **margin을 주면 위 여백을 그만큼 잃는다.** 2.112에서 막 고친 것을
     되돌리는 방향이고, 갈래 B 3버튼에서는 즉시 다시 튀어나온다
   ④ margin은 **showBanner를 처음 부를 때만** 적용된다(그 뒤는 resume/hide다).
     회전으로 인셋이 바뀌어도 반영되지 않는다 — 새 조용한 실패 자리다

안 2  margin을 safeArea에서 계산해 넘긴다 (갈래 A에서만 12, 갈래 B는 0)
   ✅ 갈래 A에서 보이는 면 기준 중앙이 된다
   ⚠ 대가 — 위 여백 24 → 12. 배경면을 함께 키우면(GAP_KNOWN 24 → 36) 유지되지만
     배경면이 122 → 134dp가 된다
   ⛔ 그리고 ④의 한계(처음 한 번만)가 그대로 걸린다

안 3  배경면을 아래로 더 늘린다  ⛔ **못 한다.** 이미 웹뷰 아래끝까지다
```

---

### 2.115 ★★ 갈래 판정 기준이 틀렸다 · 시스템 바 색 · 배터리 세이버 — 휴대폰 실기기 (2026-09-03)

> ★★ **이 세션에서 가장 값진 것은 갈래 판정 기준이 틀렸다는 확인이다.**
> 그 위에 세웠던 "실기기 대부분 갈래 A"가 무너지고, 그것을 근거로 삼았던
> 결정 몇 개가 함께 흔들린다. ⛔ 흔들리는 것을 이번에 고치지는 않았다 —
> 아래 「흔들리는 결정」 목록이 그 정리다.

#### 기기 — Galaxy Z Flip (SM-F700N) · Android 13

```
serial     R39N500G7YF     삼성 · bloomq · Android 13 (SDK 33) · 보안패치 2024-01-01
화면       1080×2636 · 480dpi(3.0x) = **360×878.7dp**   ★ 폴더블 내부 화면 기준
상태바     92px = 30.7dp        내비 144px = **48dp — 3버튼이 켜져 있다**
앱 창      mAppBounds (0,91)–(1080,2492)  → 웹뷰 **360×800dp**
WebView    145.0.7632.79   (≥140인데 갈래 A가 아니다 — 아래를 볼 것)
진동       Vibrator id=0 · AMPLITUDE_CONTROL **있다** (태블릿에 없던 것)
```

#### ★★ ① 갈래를 가르는 것은 WebView 버전이 아니다

**웹뷰 안에서 직접 읽었다** (광고 없이 CDP `Runtime.evaluate`로):

```
휴대폰 (WebView 145 · Android 13)
  env(safe-area-inset-bottom) = **0**      --safe-area-inset-bottom = **0**
  키보드를 올려도 0이다.  window.innerHeight = 800dp (= 878.7 − 30.7 − 48)
```

**WebView 145는 ≥140인데 safeArea가 0이다.** 2.112가 적은
「갈래 A = WebView ≥140」은 **이 기기에서 성립하지 않는다.**

```
⛔ 틀린 기준   WebView 버전
★ 맞는 기준   **창이 시스템 바 밑으로 들어가는가**(edge-to-edge)
             들어가지 않으면 웹 좌표계 안에 "안전하지 않은 자리"가 아예 없다.
             읽을 인셋이 없으니 env()는 0일 수밖에 없다 — 웹뷰가 몇이든 같다
```

**Android 15는 targetSdk ≥35 앱에 edge-to-edge를 강제한다.** 태블릿과 에뮬레이터가
둘 다 Android 15였던 것이 "웹뷰 버전이 가른다"처럼 보이게 만든 것이다.
Android 14 이하에는 그 강제가 없고, 이 앱은 edge-to-edge를 스스로 켜지 않는다.

#### ★ 갈래는 둘이 아니라 셋이다

```
갈래 A   창이 edge-to-edge  +  WebView ≥140    safeArea = 인셋          태블릿
갈래 B   창이 edge-to-edge  +  WebView <140    safeArea = 0, 인셋 있음   에뮬레이터
갈래 C   **창이 edge-to-edge가 아니다**          safeArea = 0, 인셋 없음   이 휴대폰
        (Android 14 이하 · 웹뷰 버전 무관)
```

⚠ **C가 B와 다른 점이 핵심이다.** B는 웹 좌표계 아래에 인셋만큼의 자리가 실재하는데
못 읽는 것이고, C는 그 자리 자체가 웹뷰 밖에 있다. 그래서 같은 GAP 48을 써도
위 여백이 B에서는 "48 − 인셋"이고 C에서는 "48 − 0 = 48"이다. 아래 계산표를 볼 것.

#### ⚠ 흔들리는 결정 — ⛔ 이번에 고치지 않았다. 무엇이 흔들리는지만 적는다

```
① 2.105 상단 검은 띠를 그대로 둔다
   흔들리는 근거   "갈래 A(실기기 대부분)에서는 안 보인다 — 에뮬레이터 한정이다"
   ★ 실측         이 휴대폰에서도 **상단 띠는 없었다.** 창이 상태바 밑으로
                 안 들어가므로 웹뷰가 상태바 자리를 아예 안 차지한다 —
                 띠가 생길 자리가 없다
   → **결론은 살아남고 근거만 바뀐다.** "갈래 A라서 없다"가 아니라
     "A와 C 둘 다에서 없고, 에뮬레이터(B)에서만 보인다"가 맞다
   ⚠ 남는 것: Android 15 + 옛 웹뷰(진짜 갈래 B) 실기기에서는 여전히 보인다.
     그 조합이 얼마나 되는지는 **모른다**

② 2.109 windowBackground를 inkDeep으로 되돌린다
   흔들리는 근거   "갈래 A에서는 이 색이 아예 안 보인다 — 태블릿에서 웹뷰가 화면 전체였다"
   ★ 갈래 C에서는 창 배경이 **보일 수 있는 자리**가 다시 생긴다
   → 그러나 이번 시스템 바 수정 뒤 상·하단이 전부 pym_ink_deep이라
     **관측 가능한 차이가 없었다.** 결론 유지. ⛔ 다시 열 근거가 아니다

③ 2.112 GAP_UNKNOWN 48 (위 여백 0을 받아들인 판단)
   흔들리는 근거   "갈래 B + 3버튼은 소수 기기의 한 조합이다"
   ★ 갈래 C가 Android 14 이하 **전부**라 소수가 아니다. 다만 C에서는
     위 여백이 0이 아니라 **48로 예측된다** — 위험의 방향이 반대다
   → 아래 「2.112 재검토」에 계산표와 안을 놓았다. ⛔ 값은 사용자가 고른다

④ 2.114 배너를 배경면 중앙에 놓는 안 2 (갈래 A에서만 margin 12)
   흔들리는 근거   "갈래 A가 실기기 대부분"
   → 갈래 C에는 margin이 안 들어간다. **적용 범위가 예상보다 훨씬 좁다**

⑤ 「실기기 대부분이 갈래 A다」라는 문장 자체
   ⛔ **근거가 없어졌다.** WebView는 Play로 자동 갱신되지만 **안드로이드 버전은
     아니다.** 갈래를 가르는 것이 OS 버전이므로 자동 갱신 논리가 통하지 않는다
   ⚠ Android 14 이하의 실제 비중은 이 문서가 모른다 — 추정으로 적지 말 것
```

#### ⛔ 「선행 조건」 절차 ①의 전제가 틀렸다 — 배너는 결과 화면에만 있다

2.113이 적은 「앱을 띄우고 logcat에서 기기 ID를 읽는다」는 **배너가 첫 화면에
있다고 전제한 절차다.** 실제로는 `adsApp.js`의 `bannerVisible`이 허용하는 화면이
**결과 화면 하나뿐이라**, 앱을 띄우기만 해서는 광고 요청 자체가 없고 ID도 안 찍힌다.

```
★ 바뀐 사실   ID는 **광고를 요청해야** 찍힌다 = 결과 화면까지 가야 한다
             = 그 행위가 곧 **실제 노출 1회**다. 노출 없이 뽑는 길은 없다
⚠ 이번에는 실제 노출도 0이었다 — 광고가 **No fill(code 3)** 로 안 채워졌다.
  요청은 나갔고 ID는 찍혔지만 배너는 뜨지 않았다
★ 그래서 **배너 기하(3번)는 아직 못 쟀다.** 시험 기기로 등록하면 시험 광고가
  **항상 채워지므로**, 등록 뒤에 재는 것이 실제 광고 위험 0으로 재는 길이다
```

⛔ 등록 전에 기하를 재려고 실제 광고를 반복 요청하지 말 것 — No fill이면 어차피
못 재고, 채워지면 그것이 노출이다.

#### ✅ 시스템 바 색을 고쳤다 — **확정** (사용자 확정 · 2026-09-03)

**증상** — 갈래 C(Android 14 이하 · 라이트 모드)에서 앱이 밝은 띠 둘에 끼인다.

```
상단 92px   #757575      하단 144px  #F6F6F6      사이 앱  #211A28 ~ #0B1216
Δ 상단 (84,91,77)        Δ 하단 (235,228,224)
```

**원인** — `styles.xml`의 `AppTheme.NoActionBar`가 **parent를 명시해 이름의 점 상속을
끊는다.** 이름은 `AppTheme`을 물려받는 것처럼 보이지만 `parent` 속성이 이긴다.
그래서 `AppTheme`이 정의한 `colorPrimaryDark = pym_ink_deep`이 오지 않고,
statusBarColor가 AppCompat 기본값 `primary_dark_material_light` **#757575**로 떨어진다.
(#757575 = (117,117,117)로 실측 픽셀과 정확히 일치했다.)

⚠ **windowBackground와 자리가 다르다.** 시스템 바 배경은 windowBackground가 아니라
statusBarColor·navigationBarColor다. 2.109가 잰 것은 Android 15에서 바가 **투명해**
창 배경이 비쳤던 경우다. ⛔ **2.109를 다시 열 근거가 아니다.**

**수정** — `AppTheme.NoActionBar`에 넉 줄. 색과 아이콘을 **함께** 뒤집는다.

```
android:statusBarColor            @color/pym_ink_deep
android:navigationBarColor        @color/pym_ink_deep
android:windowLightStatusBar          false
android:windowLightNavigationBar      false   (API 27+ · tools:targetApi로 표시)
```

**실측 — 예상과 실측을 갈라 적는다**

```
휴대폰 (Android 13 · 갈래 C)
  예상  상·하단이 pym_ink_deep이 된다
  실측  ✅ 상단 #0B1216 · 하단 #0B1216.  Δ 상단 (23,8,18) · Δ 하단 **(0,0,0)**
        ★ 상단 Δ(23,8,18)은 2.109가 에뮬레이터에서 잰 값과 같다 — 감수한 수준이다
태블릿 (Android 15 · 갈래 A)
  예상  edge-to-edge라 네 줄이 **무시된다** → 변화 없음
  실측  ✅ 무시됐다. 화면 맨 위 픽셀이 #211A28로 **앱 그라디언트 그대로다** —
        바가 투명해 앱이 그 밑까지 그린다. 우리가 넣은 #0B1216이 아니다
```

★ 즉 이 수정의 효과는 **갈래 C에 갇힌다.** 그것이 의도다.

#### ★★ 배터리 세이버가 구절 알림을 **364일 밀어 놓는다**

> ⚠ **이 절의 조건 서술은 2.116 ①에서 정정됐다.** 세이버가 켜진 것만으로는
> 안 밀린다 — **세이버 ON + 앱이 포그라운드에 없음** 둘이 함께여야 한다.

알림을 실측하려고 시각을 2분 뒤로 옮겼는데 **울리지 않았다.** 알람 레코드를 열어
보니 원인이 적혀 있었다.

```
policyWhenElapsed: requester=-3m0s  app_standby=-4m30s  device_idle=--
                   battery_saver=**+364d23h56m44s**  tare=-4m34s
whenElapsed=+364d23h56m44s     ← 이 값이 실제 발동 시각이다
```

```
확인   settings get global low_power = **1** (배터리 세이버 켜짐)
검증   low_power 0으로 끄자 **즉시** 발동했다 (09:31:25 · 예정 09:28에서 3분 25초 지연)
       다시 1로 되돌려 두었다 — 기기를 원래대로 남긴다
```

⛔ **이것은 조용한 실패다.** 앱은 14일치를 정상으로 예약했다고 믿고, 사용자에게는
토글이 「켜짐」으로 보이며, 아무 경고도 없다. 그런데 알림은 오지 않는다.

```
왜   SCHEDULE_EXACT_ALARM을 매니페스트에서 뺐다(tools:node=remove · 3절 앱 사실).
     그래서 예약이 **부정확 알람**이고, 배터리 세이버는 부정확 알람을 미룬다
⚠ 정확 알람으로 바꾸는 것은 **가볍지 않다** — Play 정책상 사용 사유를 대야 하고,
  「알람·리마인더」 권한을 사용자가 따로 켜야 한다. ⛔ 이번에 손대지 않았다
★ 확인되지 않은 것: 삼성 「앱 절전」·「사용 안 함 앱 절전」이 따로 더 미루는지
```

#### ✅ 2.94 VIBRATE 권한 질문을 닫았다 — **권한 없이 울린다**

태블릿에 진동 모터가 없어 못 닫았던 것을 이 휴대폰에서 닫았다.

```
APK가 선언한 권한 (aapt dump permissions · 병합 결과)
  INTERNET · AD_ID · ACCESS_NETWORK_STATE · RECEIVE_BOOT_COMPLETED · WAKE_LOCK ·
  POST_NOTIFICATIONS · ACCESS_ADSERVICES_{AD_ID,ATTRIBUTION,TOPICS} ·
  FOREGROUND_SERVICE · DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION
  ⛔ **android.permission.VIBRATE는 없다.** 우리도 플러그인도 선언하지 않는다

실측 (dumpsys vibrator_manager)
  startTime 09-03 09:31:26.392 · durationMs **1102** · status **finished**
  attrs Usage=NOTIFICATION
  uid **1000** · opPkg **android**
  reason Notification (io.github.appleap385.peaceinmind 10198)
```

★ **uid가 1000(system)이고 opPkg가 android다.** 우리 앱이 진동을 요청한 것이 아니라
**시스템이 채널 설정을 보고 대신 울렸다.** 그래서 VIBRATE 권한이 필요 없다 —
통설이 맞았고, 이제 실측이 있다. ⛔ VIBRATE를 추가하지 말 것.

⚠ 태블릿에서 보이던 `ignored_unsupported`는 **모터가 없어서**였다. 권한 문제가 아니었다.

#### ✅ POST_NOTIFICATIONS 경로 · 알림 예약 — 실기기에서 확인했다

```
경로   About(「이 앱에 대해」)을 여는 순간 권한을 묻는다 (About.jsx의 mount effect).
      ⛔ 입력 화면에서는 묻지 않는다 — 설계 그대로다
실측   허용 누름 → granted=true → 예약 생성 → 채널 morning_verse 생성
      채널 importance=3 · mVibrationEnabled=**true** · sound 기본 알림음
알림   실제 수신 확인 — 「다니엘 2:22 / 그는 깊고 은밀한 일을 나타내시고 어두운데…」
      ★ 시각 중립 문구(2.93)가 실기기에서도 지켜졌다
2.93   notify_seen이 앱을 여러 번 열어도 **14에 머물렀고**, 알림이 실제로 하나
      나간 뒤에 15가 됐다. **누적 버그는 재발하지 않는다**
```

⛔ **새로 드러난 구멍** — Android 13+ 신규 설치 기기에서 사용자가 About을 **한 번도
열지 않으면** 권한을 영영 묻지 않는다. 그런데 토글의 저장값은 기본 「켜짐」이라
About을 열면 화면에도 「켜짐」으로 보인다. 즉 **사용자는 켜져 있다고 믿고 알림은
안 온다.** 실측: 설치 직후 예약 **0건** · 권한 granted=false · 채널 미생성.
⚠ 진단만 했다. 고치지 않았다.

#### ⚠ 설치가 알림 예약을 지우는가 — 이번에는 **안 지웠다**

```
태블릿   설치 직전 95 → `install -r` 직후 **95** → 앱을 열자 98
휴대폰   신규 설치라 비교 대상 없음 (0 → 0 → 권한 허용 뒤 생성)
        ⚠ 위 숫자는 dumpsys alarm에서 우리 패키지가 걸린 **줄 수**다.
          예약 건수와 1:1이 아니다 — 변화의 방향만 읽을 것
```

⚠ 3절의 「설치는 예약을 지운다」는 **같은 debug 키로 덮어쓰는 경우에는 관측되지
않았다.** 지우는 것은 패키지 삭제(uninstall)다. ⛔ 다만 앱을 한 번 여는 습관은
유지할 것 — refreshSchedule이 창을 다시 채우는 자리다.

#### 🔴 2.112 GAP_UNKNOWN 재검토 — 계산표와 안 (⛔ 값은 사용자가 고른다)

**근거가 바뀌었다.** "갈래 B + 3버튼은 소수 기기의 한 조합"이 판단의 전제였는데,
갈래가 셋이고 Android 14 이하가 전부 갈래 C다. 다만 **C에서는 위 여백이 0이 아니다.**

식은 그대로다.  `위 여백 = safeArea + GAP − 웹좌표계 인셋`

```
                        safeArea  GAP  웹좌표 인셋   위 여백    아래 틈   배경면
갈래 A 태블릿(48)          48      24      48       24 ✅실측    48      122dp
갈래 A 폰 제스처(24)        24      24      24       24         24       98dp
갈래 B 에뮬 제스처(24)       0      48      24       24 ✅실측    24       98dp
갈래 B 3버튼(48)            0      48      48        0 🔴        48       98dp
갈래 C 이 휴대폰(3버튼)      0      48       0       48 ✅실측     0        98dp
```

⚠ **갈래 C 줄은 예측이다.** 창이 바 밑으로 안 들어가므로 배너가 웹뷰 아래끝에
붙고 웹 좌표계 인셋이 0이 된다고 본 것이다. **시험 기기 등록 뒤 3번에서 재야 한다.**
⛔ 재기 전에 이 줄을 근거로 값을 바꾸지 말 것.

```
안 1  그대로 둔다 (GAP_UNKNOWN 48 유지)
     ✅ 갈래 C(다수로 추정)에서 위 48 · 아래 0 — 넉넉하다
     ⚠ 대가는 갈래 B 3버튼의 위 여백 0뿐이고, 그 조합은
       **Android 15 + WebView <140**이라 오히려 드물다
     ★ "소수라서 받아들인다"는 근거가 **B에 대해서는 오히려 강해졌다**
     ⚠ 다만 갈래 C에서 위 48 · 아래 0은 배너가 **아래로 치우쳐 보인다** —
       2.114가 다룬 중앙 정렬 문제가 갈래 C에서 더 크게 나타난다

안 2  GAP_UNKNOWN을 낮춘다 (예: 48 → 32)
     계산  갈래 C       위 32 · 아래 0 · 배경면 82dp (878.7dp의 9.3%)
           갈래 B 3버튼  위 **−16** 🔴 음수 — 배너가 배경면 위로 튀어나온다
     ⛔ 2.112가 막 닫은 결함이 되살아난다. 권하지 않는다

안 3  갈래 C를 따로 판정해 GAP을 나눈다 (상수가 셋이 된다)
     판정법  safeArea가 0이면서 창이 edge-to-edge가 아닌 경우 —
            웹에서는 구별할 수단이 마땅치 않다. 네이티브에서 넘겨야 한다
     ✅ 세 갈래를 각각 맞출 수 있다
     ⚠ 대가 — 네이티브에 판정 코드가 생기고, 조용한 실패 자리가 하나 는다
       (값이 안 오면 어느 쪽으로 떨어지는지 정해야 한다)

안 4  창을 edge-to-edge로 직접 켠다 (모든 기기를 갈래 A/B로 모은다)
     ✅ 갈래 C가 사라진다. 판정이 둘로 줄고 태블릿과 같은 규칙이 된다
     ⛔ 대가가 크다 — 2.105 상단 검은 띠가 **갈래 C 기기 전부에서 되살아난다.**
       지금 띠가 없는 이유가 "창이 바 밑으로 안 들어가서"이기 때문이다
     ⛔ 그리고 이번 시스템 바 색 수정이 통째로 무의미해진다
```

⚠ **어느 안이든 3번 실측이 먼저다.** 갈래 C의 위 여백이 예측대로 48인지,
아니면 배너가 다른 자리에 앉는지가 안 1과 안 3을 가른다.

#### ✅ 결정 — **안 1(그대로 둔다)로 확정** (사용자 확정 · 2026-09-03)

GAP_UNKNOWN 48을 유지한다. 근거와 새 계산표는 **2.112의 「2026-09-03 재검토」**
절에 함께 두었다 — 그 절이 이 판단의 정본이다.
⛔ 안 4(edge-to-edge를 직접 켠다)는 **하지 않는다.**
★ 3번을 잰 뒤 C 줄을 실측으로 바꾸고 그때 다시 본다.

---

### 2.116 진단 셋 — 배터리 세이버 · 알림 권한 구멍 · ads.txt (2026-09-03 · ⛔ 고치지 않았다)

#### ★★ ① 배터리 세이버 — 조건은 **둘**이다. 「세이버 ON」만으로는 안 밀린다

⚠ **2.115에 적은 진단을 정정한다.** 거기서는 "배터리 세이버가 켜져 있으면 밀린다"고
적었는데, 같은 기기에서 세이버가 켜진 채로 **안 밀린 순간**을 관측했다. 조건이 둘이다.

```
실측 (Galaxy Z Flip · Android 13 · low_power=1 고정 · 같은 알람 · 같은 flags)
  앱을 방금 사용한 직후          battery_saver = **−8m25s**   → 제약 없음
  앱을 홈으로 내리고 2분 뒤       battery_saver = **+364d23h58m** → 사실상 무기한
  그대로 5분 뒤                  battery_saver = **+364d23h55m** → 풀리지 않는다
  그대로 9분 뒤                  battery_saver = **+364d23h51m** → 그대로다
  ★ 남은 시간이 경과한 만큼만 줄어든다 = **미뤄진 목표 시각이 고정**이라는 뜻이다.
    「잠깐 뒤로 미룬다」가 아니라 **사실상 취소**다
  배터리 세이버를 끈 순간         **즉시 발동**했다 (09:31:25 · 예정 09:28)
```

★ **조건 = 배터리 세이버 ON  +  앱이 포그라운드에 없음.**
앱을 열면 풀리고, 백그라운드로 2분쯤 지나면 다시 걸린다.

⛔ **그래서 이 기능에 치명적이다.** 구절 알림은 아침 09:00에 온다. 그 시각에
앱이 포그라운드일 이유가 없다 — **알림을 받으려면 알림을 받기 전에 앱을 열어야
한다**는 순환이 된다. 사용자에게는 "알림이 안 오는 앱"이다.

**우리 알람의 성질** (dumpsys alarm)

```
tag    io.github.appleap385.peaceinmind/…localnotifications.TimedNotificationPublisher
type   RTC (WAKEUP 아님) · window=+1h · flags=**0x0**
코드   notify.js — schedule: { at: …, allowWhileIdle: **false** }
```

**대조군 — 같은 기기의 다른 알람 120건**

```
전체 120건 중 배터리 세이버가 미룬 것   **우리 것 하나뿐**
ALLOW_WHILE_IDLE 계열(0x4·0x8·0x40) 63건 중 미뤄진 것   **0건**
  제3자 앱 포함 — com.google.android.googlequicksearchbox(0x5) ·
  com.microsoft.office.officehubrow(0x8) 전부 제약 없음
⚠ 그러나 **AWI가 아닌 알람 중에도 안 미뤄진 것이 많다**
  (com.facebook.katana 0x0 · GMS 0x0 등) — 그 앱들이 다른 이유로 면제됐을 수 있다.
  ⛔ 따라서 "AWI가 배터리 세이버를 넘는다"를 이 자료로 **단정할 수 없다**
```

**배제한 것** — 다른 원인이 아니다.

```
doze 화이트리스트   우리 앱 없음 (목록 154개)
app standby        bucket **10 = ACTIVE** (정책값도 음수라 제약 아님)
appops             RUN_ANY_IN_BACKGROUND = allow · "No operations"
삼성 앱 절전        stopped=false · suspended=false · hidden=false
device_idle 정책    `--` (제약 아님)
```

⚠ **제조사·OS 버전 차이는 못 쟀다.** 태블릿(삼성 · Android 15)으로 대조하려던
중에 USB에서 빠졌다. ⛔ 태블릿은 아무것도 바뀌지 않았다(명령이 전부 실패했다) —
`low_power=0` 그대로다. **삼성 Android 13 한 대의 실측이 전부다.**

**⚠ 태블릿의 14일 알림 지연 관찰은 유효하다** — 그 기기는 `low_power=0`이고
알람 정책값도 음수(−19m29s)였다. 지금까지 잰 42분·2분 지연은 **배터리 세이버가
꺼진 상태의 값**이므로 의미가 달라지지 않는다.

**🔴 안 — ⛔ 값을 정하지 않았다. 사용자가 고른다**

```
안 A  allowWhileIdle 을 true 로 (notify.js 한 줄)
     ✅ **권한이 필요 없다.** SCHEDULE_EXACT_ALARM과 무관하다 (2.107 ①에서 확인)
     ✅ type이 RTC → RTC_WAKEUP으로 따라온다 (플러그인 setExactIfPossible)
     ⛔ **배터리 세이버를 넘는지는 미검증이다.** 위 대조군으로는 단정 못 한다
     ★ 재는 법 — 바꿔서 설치하고 **같은 조건**(세이버 ON · 앱 백그라운드 2분)에서
       dumpsys alarm의 battery_saver 정책값이 음수가 되는지 본다. 5분이면 된다
     ⚠ 되면 지연이 줄고, 안 되면 원상복구다. 실험 비용이 가장 싸다

안 B  앱이 이 상태를 감지해 사용자에게 알린다
     ⛔ **지금 수단이 없다.** PowerManager.isPowerSaveMode()는 네이티브 API이고,
       설치된 플러그인은 admob · local-notifications 둘뿐이다
       (@capacitor/device조차 없고, 있어도 배터리 잔량·충전 여부만 준다)
     ⚠ 네이티브 한 조각이나 플러그인 추가가 필요하다 — 2.98 이후 네이티브를
       늘리지 않는다는 흐름과 부딪힌다
     ★ 감지되면 About에 "배터리 세이버가 켜져 있어 알림이 늦거나 안 올 수
       있습니다"를 조건부로 띄울 수 있다

안 C  감수하고 About에 상시 문구로 적는다
     ✅ 코드가 거의 안 는다. 정직하다
     ⛔ 대부분의 사용자는 About을 안 연다 — ②의 구멍과 같은 자리에서 무력해진다

안 D  SCHEDULE_EXACT_ALARM을 되돌린다
     ⛔ **2.92가 기각한 것이다.** 지연이 아무리 커도 이건 아니라고 이미 못박았다
```

#### ✅ 안 A를 적용했다 — **배터리 세이버를 넘는다** (2026-09-03 · 사용자 확정)

`notify.js`의 `schedule.allowWhileIdle`을 **false → true**로 바꿨다. 한 줄이다.

**⛔ 먼저 확인한 것 — 2026-08-31의 멈춤 자리를 건드리지 않는다**

플러그인 코틀린 소스를 직접 읽었다. 설정 화면을 여는 조건은 `isExactNotification`
하나뿐이고 `allowWhileIdle`과 **무관하다.**

```
LocalNotificationsPlugin.kt
  honorExact = notifications.any { it.isExactNotification }
  if (honorExact && SDK >= S && !canScheduleExactAlarms()) → 설정 화면을 연다
LocalNotificationManager.kt · setExactIfPossible
  useExact = isExactNotification && canScheduleExactAlarms()
  useExact == false 이고 allowWhileIdle == true
    → alarmManager.**setAndAllowWhileIdle(RTC_WAKEUP, …)**
```

★ `isExactNotification: false`를 그대로 두었으므로 `honorExact`가 false다 —
**설정 화면도 안 열리고 schedule()도 안 멈춘다.**

**실측 — 같은 기기 · 같은 배터리 세이버(ON) · 같은 백그라운드 조건**

```
                    알람 type      flags   battery_saver 정책      실제 발동
before (false)      RTC            0x0     **+364d23h58m**        ⛔ 안 온다
after  (true)       **RTC_WAKEUP**  0x20    −1m18s / −4m20s / −7m21s  ✅ 온다
```

```
end-to-end 확인 (배터리 세이버 ON · 앱 백그라운드)
  예정 10:23:00  →  실제 발동 **10:24:31**  (1분 31초 늦음 · window +1m31s 안)
  channel=morning_verse · 진동 1110ms status=finished
★ 「영영 안 온다」가 「1분 31초 늦게 온다」가 됐다
```

**✅ 태블릿(Android 15)에서도 같다 — OS 버전 차이가 아니다** (2026-09-03)

⛔ 새 빌드를 깔기 **전에** 「전」을 먼저 쟀다. 깔았으면 대조군이 사라졌다.

```
Galaxy Tab S9 FE · Android 15 · 갈래 A · 같은 앱 · 앱 백그라운드
  before (allowWhileIdle=false · type=RTC)
     세이버 OFF                 battery_saver = −1h41m15s      제약 없음
     세이버 ON · 2분             battery_saver = **+364d23h57m**
     세이버 ON · 3분             +364d23h56m · whenElapsed **+364d**
     세이버 OFF로 복구            −5s              즉시 풀린다
  after  (allowWhileIdle=true · type=RTC_WAKEUP · flags=0x20)
     세이버 ON · 2분             battery_saver = **−1m20s**     제약 없음
     세이버 ON · 3분             −2m31s · whenElapsed **+21h13m** (요청한 09:00)
```

★ **두 기기가 같은 답을 준다.** 배터리 세이버 지연은 Android 13 전용이 아니고,
allowWhileIdle은 두 버전 모두에서 그것을 넘는다.
⚠ 둘 다 **삼성**이다. 다른 제조사는 여전히 미검증이다.

**⚠ 회귀** — `notify.test.js`에 두 축을 **함께** 못박았다. 일부러 false로 되돌려
실패하는 것까지 확인했다.

```
· schedule의 allowWhileIdle이 true인가        ← 조용히 false로 돌아가는 것을 막는다
· isExactNotification이 false로 남아 있는가    ← 정확 알람으로 새는 것을 막는다
⛔ 둘은 **다른 축이다.** 하나를 보고 다른 하나를 바꾸지 말 것
```

**★ RTC_WAKEUP이 잠든 기기를 깨우는 것 — 세션 의견 (판단은 사용자 몫이다)**

★ **켜 두는 쪽이 맞다고 본다.** 근거는 이 알림의 성질이다.

```
빈도    하루 한 번이다. 깨우는 비용이 하루 1초 남짓이다
시각    **사용자가 직접 고른 시각**이다. 09:00을 고른 것은 "그때 알려 달라"는
       요청이고, 깨우지 않으면 그 요청을 지킬 수 없다
경계    알람시계와 다른 점이 그대로 남는다 —
       · 창이 +1h라 **정각을 약속하지 않는다**
       · 화면을 켜지 않고 전체화면 인텐트도 쓰지 않는다
       · 채널 mBypassDnd=**false** (실측) — **방해 금지를 존중한다**
         한밤중 시각을 고른 사용자도 DND를 켜 두면 조용하다
```

**✅ 확정 (사용자 확정 · 2026-09-03)** — RTC_WAKEUP을 켜 둔다.

```
⛔ **About 문구에 "깨움"을 더하지 않는다.** 사용자 확정이다.
   근거 — 알림을 켜고 시각을 고르는 행위에 **이미 그 기대가 들어 있다.**
   문구를 늘리면 앱이 자기 동작을 설명하기 시작하는데, 이 앱이 피해온 방향이다
⚠ 다만 **「깨움」은 2026-09-03에 새로 생긴 성질이다.** 이전에는 type=RTC라
   잠든 기기를 깨우지 않았다. 다음 세션이 "원래 그랬다"고 읽지 않도록 적어 둔다.
   ⛔ 이 사실을 About 문구로 옮기지 말 것 — 기록은 여기 두고 화면은 그대로다
```


**⚠ 2.92 「알람시계의 감각을 쓰지 않는다」와 어디까지 양립하는가**

2.107 ②가 이미 축을 갈라 놓았다 — **기기를 깨우는 것과 정확한 것은 다른 축이다.**
그 판단이 여기서도 그대로 선다.

```
2.92가 거절한 것   **정확한 알람** — SCHEDULE_EXACT_ALARM · setExact ·
                 "09:00 정각에 반드시 울린다"는 약속
안 A가 하는 것     여전히 **비정확 알람**이다. 창(window +1h)이 그대로 있고
                 몇 분~수십 분 늦을 수 있다.
                 바뀌는 것은 "절전 때문에 **무기한** 밀리는 것"을 막는 것뿐이다
★ 판단           **양립한다.** "늦을 수 있다"는 감수하고 "안 온다"만 막는 것이라
                 알람시계의 감각을 빌리지 않는다
⚠ 다만 RTC → RTC_WAKEUP은 **기기를 깨운다.** 잠든 기기를 깨워서까지 부르는 것이
  이 앱의 태도에 맞는지는 별개의 물음이다 — 그건 사용자가 볼 자리다
```

#### ★ ② About을 한 번도 안 열면 알림이 영영 안 온다 (Android 13+)

**사슬** — 어느 한 곳도 고장 나지 않았는데 결과가 침묵이다.

```
① readSettings()가 DEFAULT_ON = true 를 돌려준다   ← 저장된 값이 없어도 켜짐이다
② App.jsx가 시작할 때 refreshSchedule()을 부른다
③ refreshSchedule은 권한이 없으면 **조용히 0을 돌려준다**
   ("권한이 없으면 조용히 멈춘다. 여기서 묻지 않는다 — 토글이 묻는 자리다")
④ 묻는 곳은 About.jsx의 mount effect **하나뿐**이다
⑤ Android 13+ 는 POST_NOTIFICATIONS가 런타임 권한이다
→ 사용자가 About을 안 열면 ④가 안 돌고, 권한이 없어 ③이 0을 돌려주고,
  ①이 계속 true라 **화면에는 「켜짐」으로 보인다**
```

**실측** (설치 직후 · About 열기 전)

```
POST_NOTIFICATIONS   granted=false · flags에 **USER_SET 없음** = 물은 적이 없다
알람 예약             **0건**
알림 채널             morning_verse **미생성** (default 채널만 있다)
About의 토글          「켜짐」
About을 연 뒤         권한 팝업 → 허용 → 예약 생성 → 채널 생성 → 실제 수신
```

**⚠ 2.93이 고친 것과 다른 자리다.**

```
2.93 · About.jsx가 다루는 것   **거부한 뒤**의 상태다.
  ensurePermission이 false를 주면 setEnabled(false)로 **저장값도 함께 내린다** —
  "⛔ 거부하면 설정도 꺼진다. 스위치와 저장값이 어긋나면 안 된다"
이번 것                      **묻지도 않은** 상태다.
  아무 코드도 돌지 않으므로 저장값을 내릴 기회 자체가 없다.
  기본값 true가 그대로 남아 화면과 실제가 갈린다
★ 즉 거부 경로는 동기화되고 **미요청 경로만 동기화되지 않는다**
```

⚠ Android 12 이하에서는 이 구멍이 없다 — POST_NOTIFICATIONS가 런타임 권한이
아니라 ③이 통과한다. **새 기기일수록 걸린다.**

**🔴 안 셋 — ⛔ 고르지 않았다. 두 갈래로 갈라서 놓는다**

★ 먼저 물어야 할 것은 **「화면과 실제가 맞기만 하면 되는가, 알림이 실제로 와야
하는가」**다. 답에 따라 고를 안이 갈린다.

```
갈래 ①  화면과 실제를 맞춘다 (알림은 여전히 안 온다)
  안 A′  refreshSchedule이 권한 없음을 만나면 **저장값을 false로 내린다**
        ✅ 의도를 지킨다 — **묻지 않는다.** 토글이 묻는 자리라는 규칙 그대로다
        ✅ 화면이 「꺼짐」이 되어 거짓말이 사라진다. 사용자가 켜면 그때 묻는다
        ⚠ 대가 — "기본 켜짐"의 약속은 여전히 안 지켜진다. 다만 **말은 맞는다**
        ⛔ 위험 — 권한을 **못 읽은 경우**(플러그인 실패·catch)와 **없는 경우**를
          갈라야 한다. 안 가르면 일시적 실패에 설정이 꺼진다

  안 A″  저장값은 두고 **표시만** 실제를 반영한다 (About이 권한을 함께 읽어
        「꺼짐 — 알림 권한이 없습니다」로 보여준다)
        ✅ 저장값을 안 건드려 되돌리기 쉽다
        ⛔ 대가 — About을 **열어야만** 보인다. 안 여는 사람에게는 아무 변화가 없다
          (구멍의 원인이 "About을 안 연다"인데 해결도 About 안에 둔다)

갈래 ②  알림이 실제로 오게 한다 (묻는 자리를 늘린다)
  안 B   첫 실행에서 묻는다
        ⛔ **의도와 정면 충돌한다.** About.jsx가 "입력 화면은 건드리지 않는다"고
          못박아 둔 것을 뒤집는다
        ⛔ 그리고 위험하다 — 맥락 없는 요청은 거절률이 높고, **안드로이드는
          두 번 거절하면 다시 못 묻는다.** 한 번 잘못 물으면 회복이 없다

  안 C   DEFAULT_ON을 false로 내린다
        ✅ 처음부터 화면과 실제가 일치한다 (꺼짐 = 안 온다)
        ⛔ "기본 켜짐"이라는 **제품 결정을 뒤집는 것**이다
        ⛔ Android 12 이하도 함께 꺼진다 — **지금 잘 되던 기기를 나쁘게 만든다**
        ⚠ 기존 사용자는 저장값이 있어 영향 없다. 신규 설치만 해당된다

  안 D   **맥락이 생긴 순간에 묻는다** — 결과 화면을 한 번 본 뒤, 또는 두 번째
        방문에 "아침에 구절 한 절 보내드릴까요?" 한 줄과 함께 묻는다
        ✅ 의도의 **정신**을 지킨다 — "설정이 있는 자리로 직접 들어온 순간"의
          확장이지, 맥락 없이 첫 화면에서 묻는 것이 아니다
        ✅ 알림이 실제로 온다
        ⚠ 대가 — 새 UI 조각이 생긴다. 언제·몇 번 물을지, 거절 뒤 다시 안 묻는
          규칙까지 정해야 한다 (두 번 거절이면 영구 거절이라 규칙이 필수다)
```

⚠ **갈래 ①과 ②는 배타적이지 않다.** 안 A′는 어느 쪽을 고르든 함께 넣을 수 있다 —
"화면이 거짓말하지 않는다"는 별개의 성질이기 때문이다. ⛔ 그러나 조합을 정하는 것도
사용자 몫이라 여기서 고르지 않았다.

#### 🔴 **A′ + D로 간다** (사용자 확정 · 2026-09-03) — ⛔ 설계만이다. 구현은 승인 뒤

```
근거 (사용자)  화면이 거짓말하지 않는 것과 알림이 실제로 오는 것은 **다른 문제**이고
              둘 다 필요하다. A′만 하면 정직해지지만 알림은 여전히 안 오고,
              D만 하면 묻기 전까지 화면이 거짓말한다
⛔ B는 하지 않는다   의도와 정면 충돌이고 **회복이 없다**(두 번 거절 = 영구 거절)
⛔ C는 하지 않는다   Android 12 이하도 함께 꺼져 **잘 되던 기기가 나빠진다**
```

**A′ 설계 — 권한을 둘이 아니라 셋으로 읽는다**

지금 코드가 두 경우를 못 가른다. 여기에 곧장 `setEnabled(false)`를 넣으면
**일시적 실패에 설정이 꺼진다.**

```js
try {
  const status = await ln.checkPermissions();
  if (status.display !== "granted") return 0;   // ← 거부
} catch {
  return 0;                                      // ← 못 읽음.  둘이 똑같다
}
```

★ **순수 함수로 꺼내 세 값을 만든다** (2.83 ②가 상태 기계를 `flow.js`로 꺼낸 방식).

```
permissionOutcome(raw, threw) → "granted" | "denied" | "unknown"

  granted   raw?.display === "granted"
  denied    display가 **아는 값**이면서 granted가 아닐 때 —
            "denied" · "prompt" · "prompt-with-rationale"
  unknown   그 밖 전부
            · checkPermissions()가 throw했다
            · api()가 null이다 (웹 빌드 · 플러그인 없음)
            · raw가 객체가 아니거나 display가 문자열이 아니다
            · display가 **처음 보는 문자열**이다

⛔ 판정을 **화이트리스트로 한다.** "granted가 아니면 denied"로 짜면 플러그인이
  새 값을 돌려주는 날 조용히 설정이 꺼진다. 모르는 값은 unknown이라야 안전하다
```

`refreshSchedule`에서 세 갈래로 나눈다.

```
granted   지금대로 예약한다
unknown   0을 돌려주고 **저장값을 건드리지 않는다** (오늘 동작 그대로)
denied    0을 돌려주고 **그때만** NOTIFY_ON을 false로 내린다
          ⚠ 이미 false면 쓰지 않는다 — 불필요한 쓰기와 경합을 피한다
```

★ **"prompt"(아직 묻지 않음)를 denied 쪽에 두는 것이 이 안의 핵심이다** —
고치려는 구멍이 정확히 그 상태다. Android 12 이하는 checkPermissions가 granted를
주므로 **영향이 없다.**

⚠ **부수 효과 — About의 자동 팝업이 사라진다.** 이것을 모르고 넣으면 안 된다.

```
지금    About을 열면 mount effect가 s.on(true)을 보고 ensurePermission()을 부른다
A′ 뒤   앱 시작에서 이미 on=false로 내려가 있어 그 effect가 `if (!s.on) return`에
       걸린다 → **About을 열어도 안 묻는다.** 토글을 눌러야 묻는다
★ 그래서 A′ 혼자서는 "묻는 자리"가 오히려 하나 줄어든다.
  **D가 그 자리를 대신한다** — 둘을 함께 넣기로 한 이유가 여기 있다
```

**회귀** — 순수 함수라 **값으로** 단언한다 (소스 문자열 검사가 아니다).

```
· denied면 저장값이 내려간다
· throw면 저장값을 **안 건드린다**
· 플러그인이 없으면(api() null) 저장값을 **안 건드린다**
· 처음 보는 문자열은 unknown이다        ← 화이트리스트가 살아 있는지
· "prompt"는 denied다                  ← 이 안의 핵심 판정
· 이미 false일 때 쓰기가 일어나지 않는다
```

**D 안 — "맥락이 생긴 순간"을 무엇으로 잡는가**

```
(가) 결과 화면을 **처음 본 직후**
     ✅ 앱이 무엇을 주는지 이미 본 자리다. 맥락이 가장 크다
     ⚠ 첫 방문이라 "다시 올 사람"인지 아직 모른다. 한 번 보고 떠날 사람에게도 묻는다
(나) ★ **두 번째 방문**에서 결과 화면을 본 직후   ← 세션 권고
     ✅ 돌아왔다는 신호가 있다. 맥락과 의사가 함께 선다
     ✅ 판단할 재료가 **이미 저장되어 있다** — LAST_VISIT_AT · VISIT_DATE ·
       VISIT_COUNT_TODAY (db.js KEYS). 새로 셀 것이 없다
     ⚠ 한 번 쓰고 떠나는 사람에게는 영영 안 묻는다 — 그것이 옳을 수도 있다
(다) 구절을 실제로 읽은 뒤 (이어서 읽기 진입)
     ⚠ 도달률이 낮다. 표본이 얇아 사실상 거의 안 묻는 것과 같다
```

**언제·몇 번 규칙 — ⛔ 시스템 팝업을 먼저 띄우지 않는다**

```
★ 앱 안에서 한 줄과 [받을래요] / [아니요]를 **먼저** 보여주고,
  [받을래요]일 때만 시스템에 묻는다.
  ★★ 이것이 「두 번 거절이면 영구 거절」을 **구조적으로** 피하는 길이다 —
     시스템 팝업이 최대 **한 번만** 소모된다.
     ⚠ "한 번만 묻고 그 뒤엔 About에만 둔다"보다 안전하다. 그쪽은 그 한 번이
       시스템 팝업이라 거절이 곧 한 번 소모다

앱 안 안내    딱 **한 번** 띄운다 — 새 저장 키 하나가 필요하다 (예: NOTIFY_ASK_SHOWN)
[아니요]      다시 안 띄운다. 켜는 길은 About에만 남는다
[받을래요]    ensurePermission() → 거절되면 저장값 false 그대로 두고 다시 안 묻는다
안 띄우는 조건  이미 granted · 저장값이 **명시적으로** false · 이미 한 번 띄웠다 ·
             권한이 **unknown**(못 읽었으면 묻지 않는다)
```

**대가**

```
⚠ 새 UI 조각 하나 · 새 저장 키 하나 · 회귀 필요
⚠ 결과 화면은 **광고가 뜨는 유일한 화면**이다(adsApp.js의 bannerVisible).
  그 화면에 요소를 하나 더 얹는 것이 위계에 걸리는지 봐야 한다 — 2.36 규칙 5항
⛔ 위기 화면·알림으로 열린 구절에는 절대 띄우지 않는다 — bannerVisible과 같은 원칙이다
⚠ A′와 D는 **함께 들어가야 뜻이 선다.** A′만 먼저 넣으면 묻는 자리가 줄어
  구멍이 더 깊어진다
```

#### ⛔⛔ **폐기 — 위 「A′ + D」 중 D는 버렸다** (2026-09-03 · 사용자 확정)

> ⚠ **위 설계를 지우지 않는다.** 왜 버렸는지 남기는 것이 이 절의 목적이다.

```
버린 것   D — 「두 번째 방문에서 결과 화면을 본 직후 안내 상자를 띄운다」
         (문구 안 · 자리 안 · [받을래요] 토큰 안 셋까지 만들었다)
남는 것   A′ — 권한을 셋으로 읽고 화이트리스트로 판정한다. **그대로 간다**

왜 버렸나
  ⛔ 세션이 **화면에 새 요소가 생기는 결정을 사용자에게 묻지 않았다.**
    D를 안으로 낸 것 자체가 "안내 상자를 만든다"를 전제했고, 그 전제를
    승인 절차에 올리지 않았다. 사용자가 그것을 잘못이라고 지적했다.
  ★ 그리고 더 나은 길이 있었다 — **이미 있는 탭 행에 토글을 얹는다**(2.119).
    결과 화면에 요소가 **늘지 않고**, About에 들어가야만 있던 알림 설정이
    결과 화면에서 바로 보이므로 D가 풀려던 문제도 함께 풀린다.
★ 살아남은 것 — D를 설계하며 얻은 것 둘은 2.119로 옮겼다
  · 「시스템 팝업을 먼저 띄우지 않는다」는 소모 관리 원칙
  · A′가 About 자동 팝업을 없애는 부수 효과를 **무엇이 대신하는가**라는 물음
```




#### ⚠ ③ ads.txt — **파일과 서빙에는 흠이 없다**

AdSense 상태가 「확인됨」 → 「찾을 수 없음」으로 바뀐 건이다. 우리 쪽을 전부 쟀다.

```
URL      https://appleap385-aaz0919.github.io/ads.txt
HTTP     **200** · Content-Type **text/plain; charset=utf-8** ✅
내용     google.com, pub-5074778849873789, DIRECT, f08c47fec0942fa0
        ✅ 퍼블리셔 번호가 2.55 ⑤에 적힌 것과 **정확히 같다**
바이트    59 · **BOM 없음** · 줄바꿈 LF 1개(CRLF 0) · 끝에 개행 있음 ✅
레포본과  base64로 받아 바이트 비교 — **완전히 동일**
http://  → 301 → https 같은 경로 (정상)
robots   robots.txt 자체가 없다(404) — 크롤러를 막는 규칙이 없다 ✅
Last-Modified  2026-08-25 08:19 GMT
  ⚠ 내용이 바뀐 것이 아니다. 그날 index.html만 고친 커밋(9c2d40c)이 있었고
    GitHub Pages는 재배포하면 모든 파일의 Last-Modified를 갱신한다
```

**레포 상태** — 정상이다.

```
appleap385-aaz0919/appleap385-aaz0919.github.io
  Pages   status **built** · legacy · source main /
  루트    ads.txt(59) · index.html(4189) — 둘뿐이다
  커밋    08-24 ff599f5 도메인 루트 + ads.txt
         08-25 c481734 · 9c2d40c 표시명 변경 (index.html)
  ⛔ Actions 실패나 미배포 없음. 도메인 루트·/PeaceYourMind/ 둘 다 200
  ⛔ **저장소를 건드리지 않았다** — 읽기만 했다
```

**★ github.io 공유 도메인 — 2.55 ⑤의 전제는 지금도 맞다**

```
github.io는 **Public Suffix List**에 올라 있다.
→ ads.txt 규격이 말하는 "루트 도메인"은 apex(github.io)가 아니라
  **appleap385-aaz0919.github.io** 자신이다. 지금 위치가 규격상 옳다
⚠ 다만 apex를 보러 가면 이상하게 보인다 —
  https://github.io/robots.txt → **301 리다이렉트**
  https://github.io/ads.txt   → 따라가면 200 · **text/html** (ads.txt가 아니다)
  ★ 크롤러가 PSL을 안 쓰고 apex를 봤다면 "찾을 수 없음"이 나올 수 있다.
    ⛔ 그러나 구글이 실제로 어느 쪽을 봤는지는 우리가 알 수 없다 — 추정이다
```

**남는 가설 — 우리 쪽에서 더 잴 것이 없다**

```
① 크롤러의 일시적 실패·재확인 대기      우리 파일은 08-24 이후 내용이 안 바뀌었다
② AdSense 사이트 항목이 apex(github.io)로 잡혀 있다   콘솔에서만 확인 가능
③ 공유 호스팅 도메인에 대한 구글 쪽 정책   문서로 확인되지 않았다
⚠ 심사가 「준비 중」인 것과 이 상태 변화가 인과인지도 **모른다**
⛔ 진단만 했다. 아무것도 고치지 않았고 다른 저장소를 건드리지 않았다
```

★ **콘솔에서 확인할 것** — 사이트 목록의 항목이 `appleap385-aaz0919.github.io`인지
`github.io`인지. 그 한 가지가 가설 ②를 가른다.

---

### 2.117 ⛔ 시험 기기 등록 — **콘솔은 AAID, 코드는 해시다** (2026-09-03)

> ★ 다음 세션이 여기서 막힌다. 두 값은 **서로 다른 물건이고 변환되지 않는다.**

```
콘솔에 넣는 값   **광고 ID(AAID/IDFA)** · UUID 형식
                예 00000000-aaaa-bbbb-cccc-ddddeeeeffff
                AdMob → 설정 → 시험 기기 → 기기 추가 → Android
코드에 넣는 값   **해시** · 32자 16진수
                RequestConfiguration.Builder().setTestDeviceIds(…)
                logcat이 알려주는 값이 이쪽이다
⛔ 콘솔은 32자 16진수를 **거부한다.** logcat 값을 그대로 넣으면 막힌다
```

**⚠ 둘의 관계를 계산으로 확인하지 못했다.**

```
AAID를 MD5로 돌려 보았다 — 소문자·대문자·하이픈 제거·원시 16바이트·SHA1까지
전부 logcat 값과 **불일치**했다.
→ "해시에서 AAID를 얻는다"도, "AAID로 해시를 검산한다"도 **할 수 없다.**
  각각 제자리에서 따로 구해야 한다
⚠ 왜 안 맞는지는 모른다. SDK 25.4.0이 다른 식별자를 쓸 수도 있다
  (APK가 ACCESS_ADSERVICES_AD_ID를 선언한다) — 추정이고 확인하지 않았다
```

**AAID를 뽑는 법 — adb로는 못 읽는다**

```
⛔ 안 되는 것
  settings get secure advertising_id      → null
  /data/data/com.google.android.gms/…/adid_settings.xml → Permission denied (루트 필요)
  cmd adservices_manager                  → No shell command implementation
✅ 되는 것 — 광고 설정 화면을 adb로 열고 눈으로 읽는다
  adb -s <serial> shell am start -a com.google.android.gms.settings.ADS_PRIVACY
  → com.google.android.gms.adid.settings.AdsSettingsActivity 가 뜬다
  → 화면 맨 아래 「내 광고 ID:」 에 UUID가 있다
  ⚠ uiautomator dump는 이 화면에서 **빈 결과**를 준다. 스크린샷으로 읽는다
★ 기기 설정에서 가는 길 (One UI · Android 13)
  설정 → Google → 모든 서비스 → 광고 → 「내 광고 ID」
  (또는 설정 검색창에 "광고")
```

**⚠ 이 값은 개인 식별자다 — 어디에 적을 것인가**

```
판단   admob.properties(gitignore 대상)에 해시와 나란히 적어 두는 것은
      **조건부로 괜찮다.** 그 파일이 이미 실제 광고 단위 ID를 들고 있고,
      기기 ID를 적어 두는 관행도 2.113에서 정한 것이다
⛔ 그러나 조건이 있다 — **지금 .gitignore는 정확한 경로 하나만 막는다**
      .gitignore:72   app/android/admob.properties
      → admob.properties.bak · .local · .save 같은 **사본은 추적 대상이 된다**
      ⚠ 실제로 2026-09-03 세션이 작업 중에 .bak을 만들었다(지웠다).
        사람이 만들면 조용히 커밋된다 — 경보가 없다
✅ **넓히고 적었다 (사용자 확정 · 2026-09-03)**
     .gitignore   app/android/admob.properties*
                  !app/android/admob.properties.example   ← 견본은 되살린다
     확인          .bak · .local 이 무시되고 .example 은 추적된다
     기록          admob.aaid.phone.SM-F700N = <AAID>   ⛔ 값은 이 문서에 없다
⚠ AAID는 **사용자가 재설정할 수 있다**(같은 화면의 「광고 ID 재설정」).
  재설정하면 콘솔 등록이 끊긴다 — 시험 광고가 안 나오면 이것부터 의심할 것
⛔ 저장소에는 어떤 형태로도 커밋하지 않는다. 이 문서에도 값을 적지 않는다
```

---

### 2.118 🔴 하단 배너 요구 사양과 설계안 — ⛔ 구현하지 않았다. 사용자가 고른다 (2026-09-03)

> ★ 지금까지 조건별로 땜질해 왔다. **목표 모양을 못 박고 역산한다**는 것이
> 사용자 지시다. 아래 사양 넷이 기준이다.

```
① 배너는 하단에 고정되고 콘텐츠 스크롤과 무관하게 항상 보인다
② ⛔ 콘텐츠가 배너·배경면 아래로 **지나가지 않는다.** 스크롤 끝에서 배너 영역만큼
   여백이 남고 콘텐츠는 그 위에서 멈춘다
   ★ 지금과 다르다 — 현재는 마지막 항목이 페이드에 잠겨 배경면 뒤로 사라진다
③ 광고와 배경면은 하나의 세트로 묶여 움직이고 콘텐츠와 겹치지 않는다
④ ★ 배경면의 상하 여백이 같다. 배너가 배경면 가운데 놓인다
```

#### ★★ 먼저 — 플러그인 소스를 읽어 2.114의 대가가 **거의 해소된다**는 것을 확인했다

`@capacitor-community/admob` `BannerExecutor.java` 실물이다.

```java
int densityMargin = (int) (adOptions.margin * density);
mAdViewLayoutParams.setMargins(sideMargin, densityMargin, sideMargin, densityMargin);

// set Safe Area only for Android 15+
if (Build.VERSION.SDK_INT >= VANILLA_ICE_CREAM) {          // ← API 35
    rootView.setOnApplyWindowInsetsListener((v, insets) -> {   // ← **리스너다**
        int bottomInset = insets.getSystemWindowInsetBottom();
        mAdViewLayoutParams.setMargins(m[0], m[1], m[2], m[3] + bottomInset);
        ...
    });
}
```

```
★ 둘을 새로 알았다
  ① 인셋 보정은 **Android 15+에서만** 돈다. Android 14 이하(갈래 C)는 창이
    애초에 인셋 밖이라 더할 것이 없다 — 이중 계산이 아니다
  ② 그 보정이 **리스너**다. 회전으로 인셋이 바뀌면 **네이티브가 스스로 따라간다**
⛔ 그래서 2.114의 "margin이 showBanner 첫 호출에만 적용된다"는 **오해를 부른다.**
   고정되는 것은 우리가 넘긴 margin **값**이고, 인셋은 리스너가 갱신한다.
★ 아래 설계는 margin을 **상수**로 쓴다 → 인셋에 의존하지 않으므로 재적용 문제가 없다
⚠ 남는 위험: 그 리스너가 decorView의 OnApplyWindowInsetsListener를 **덮어쓴다.**
  Capacitor나 다른 코드가 같은 자리를 쓰면 충돌한다 — **미확인이다**
```

#### 좌표 모형 — 화면 아래끝을 0으로 놓고 위를 +로 센다

```
I   하단 시스템 인셋(dp)          제스처 24 · 3버튼/태블릿 taskbar 48
P   웹뷰가 아래에서 잘린 양        갈래 A·B = 0 · 갈래 C = I
s   env(safe-area-inset-bottom)  갈래 A = I · 갈래 B = 0 · 갈래 C = 0
X   배경면을 웹 좌표 바닥에서 띄우는 양 (웹이 정한다)
M   showBanner의 margin (dp · 네이티브)
H   배경면 높이 · m 상하 여백

배너 아래끝 = I + M     ★ 세 갈래 모두 같은 식이다
                        (A·B는 리스너가 I를 더하고, C는 창이 이미 I만큼 올라와 있다)
배경면 아래끝 = P + X
아래 여백 = (I + M) − (P + X)
위 여백  = (P + X + H) − (I + M + 50)
④ 를 걸면   m = (I + M) − (P + X)   ·   **H = 50 + 2m**
```

#### 🔴 설계안 셋

```
안 1  웹만 고친다 — X = s(읽은 값) · M = m · H = 50+2m
     ✅ 네이티브 코드 0. showBanner의 margin 인자만 0 → m
     ✅ 갈래 A·C에서 **정확히** 대칭이 된다 (아래 표)
     ⛔ 갈래 B에서 s가 0인데 진짜 인셋은 I다 → 아래 여백이 I+m이 되고
       위 여백이 m−I → **m < I이면 음수. 배너가 배경면 위로 튀어나온다**

안 2  네이티브가 참 인셋을 주입한다 — X = I−P (주입값) · M = m · H = 50+2m
     ✅ **세 갈래 전부 정확하다. 갈래 구분 자체가 사라진다**
     ✅ 인셋 리스너라 회전에 강하다
     ⚠ 네이티브 조각이 생긴다 (MainActivity에서 WindowInsets → CSS 변수 주입)
     ⛔ 조용한 실패 자리가 는다 — 값이 안 오면 무엇으로 떨어질지 정해야 한다
       (폴백을 지금의 GAP_UNKNOWN 방식으로 두면 ④를 그때만 포기하는 것이다)

안 3  안 1 + 갈래 B 폴백 — B로 판정되면 ④를 포기하고 지금 방식(음수 방지)을 쓴다
     ✅ 갈래 A·C에서 ④를 얻고, B에서 **음수는 막는다**
     ⛔ 사양이 갈래마다 달라진다. "목표 모양을 못 박는다"는 이번 지시와 어긋난다
     ⚠ 판정 코드가 늘고 분기가 둘이 된다
```

#### 계산표 — m = 16 (H = 82dp) 기준

```
                       I    P    s    X    M   배너[아래,위]  배경면[아래,위]  위/아래   배경면
갈래 A 태블릿(48)       48    0   48   48   16   [ 64, 114]    [ 48, 130]     16 / 16   82dp
갈래 A 폰 제스처(24)     24    0   24   24   16   [ 40,  90]    [ 24, 106]     16 / 16   82dp
갈래 B 에뮬 제스처(24)   24    0    0    0   16   [ 40,  90]    [  0,  82]    -8 / 40 🔴  82dp
갈래 C 폰 3버튼(48)     48   48    0    0   16   [ 64, 114]    [ 48, 130]     16 / 16   82dp
                                                          (숫자는 화면 아래끝에서 dp)
★ 안 2를 쓰면 갈래 B 줄의 X가 24가 되어 [24,106] · 16/16 으로 A·C와 같아진다
```

**m 후보별 배경면 높이** — 갈래 C의 "배너의 두 배(98dp)"가 어떻게 되는가

```
m = 12   H = 74dp   (휴대폰 878.7dp의 8.4% · 태블릿 1317dp의 5.6%)
m = 16   H = 82dp   (9.3% · 6.2%)
m = 24   H = 98dp   (11.2% · 7.4%)   ⟵ 지금과 **같은 높이**지만 위48/아래0 → 24/24
지금     H = 98dp   위 48 · 아래 0 (갈래 C 실측)
★ 즉 ④는 배경면을 반드시 키우지 않는다. m=12·16이면 **오히려 줄어든다**
⛔ 갈래 A에서 m ≤ I 여야 X = s − m ≥ 0 이다. 제스처(24)에서 m ≤ 24 —
  m=24가 상한이고, 그 위로는 갈래 A에서 배경면이 인셋 밑으로 내려가야 해서 못 한다
```

#### ② 를 지키는 두 길 — 대가가 크게 다르다

```
(가) 문서에 padding-bottom = H + X 를 준다
     ✅ 변경이 작다. 지금 구조(배경면 = fixed 오버레이) 그대로다
     ⛔ **스크롤 중에는 콘텐츠가 배경면 뒤로 지나간다.** ②를 글자 그대로
       읽으면 미달이다 — 끝에서만 여백이 남는다
(나) 배경면을 오버레이가 아니라 **레이아웃 행**으로 만든다 (스크롤 영역이 짧아진다)
     ✅ **구조적으로** 콘텐츠가 배경면 아래로 갈 수 없다. ②를 엄격히 만족한다
     ⛔ 대가가 크다 — 스크롤 컨테이너가 문서가 아니게 된다.
       ⚠⚠ **2.107의 키보드 처리가 window 스크롤을 쓴다**(inputTop − MIN_INPUT_TOP를
         scrollTo로 맞춘다). 스크롤 주체가 바뀌면 그 코드가 통째로 걸린다.
         입력 화면에는 배너가 없으므로 화면마다 스크롤 주체가 갈리게 되고,
         그것이 조용한 실패 자리가 된다
★ 사양 ②를 어느 강도로 읽을지가 (가)와 (나)를 가른다 — 이것도 사용자 판단이다
```

#### 페이드는 어떻게 되는가 — **없애는 쪽을 권고한다**

```
페이드(BAND_FADE 24)가 하던 일 둘
  ① 배너 윗변이 배경면 밖으로 삐져나올 때 하드 엣지를 감춘다 (2.112)
     → ④가 삐져나옴을 없앤다. **이 이유는 사라진다**
  ② 스크롤 콘텐츠가 배경면에 닿는 경계를 흐린다
     → 사용자가 싫다고 한 것이 정확히 이 효과다 ("마지막 항목이 페이드에 잠긴다")
★ 그러므로 페이드는 **버린다.**
⚠ 그러면 스크롤 경계가 하드 컷이 된다. 이 저장소는 "구간이 끝난다"를
  **1px 선**으로 말한 전례가 있다(2.36 3차 · 탭 줄 위 구분선이 위계를 풀었다).
  ★ 페이드 → 1px 선이 관용에 맞는 대체다. ⛔ 다만 이것도 별개 판단이다
```

#### 회귀로 단언할 수 있는 것과 못 하는 것

```
✅ 할 수 있다 (순수 함수 · 값으로)
   · H == 50 + 2m
   · 항등식 **위 여백 == 아래 여백** 을 모형 함수로 (I·P·s를 넣어 계산)
   · X가 읽은 safeArea(안 1) / 주입값(안 2)을 그대로 쓴다
   · **M이 상수다** — showBanner에 넘기는 margin이 인셋에 의존하지 않는다
   · safeArea가 0·24·48·64·NaN·문자열일 때의 H·X
   · 광고 미채움이면 H·X·M이 전부 0으로 접힌다
⛔ 못 한다 — 실측으로만 안다
   · 네이티브 배너가 실제로 그 자리에 앉는가
   · 플러그인이 Android 15+에서만 인셋을 더한다는 것 (버전이 오르면 바뀐다)
   · 회전 뒤 실제 위치
   · 갈래 B의 진짜 인셋
   ★ 그래서 **버전 고정이 회귀의 일부다** — package.json의 admob 버전을 단언하는
     테스트를 두면 "플러그인이 조용히 바뀌었다"를 잡을 수 있다
```


#### ✅ **안 2로 확정** (사용자 확정 · 2026-09-03) — 네이티브가 참 인셋을 주입한다

```
사용자 근거
  · 세 갈래 전부 정확하고 **갈래 구분이 사라진다**
  · ★ **안 2만이 「틀려도 음수가 안 된다」는 미덕을 회복한다.** 그것이 2.112 이후
    이 설계를 지탱한 성질이고, ④를 걸면서 그것을 잃는 것이 안 1·3의 결정적 약점
  · 갈래마다 사양이 갈리는 안 3은 "목표 모양을 못 박는다"와 어긋난다
잠정값  **m = 16 (H = 82dp)**
```

**⚠⚠ 정정 — 「갈래 A 제스처에서 m ≤ 24」는 틀렸다. 상한이 없다**

세션이 앞에서 그 제약을 적었고 사용자가 "코드 주석과 문서에 박아라"고 했는데,
**박기 전에 다시 계산해 보니 성립하지 않는다.** 틀린 제약을 코드에 박으면 안 된다.

```
어디서 나왔나  X = I − m · M = 0 으로 풀던 중간 형태에서 나온 것이다.
              그 형태에서는 X ≥ 0 이라야 하므로 m ≤ I 였다
확정 형태     X = I − P (주입값) · M = m · H = 50 + 2m
  배경면 아래끝 = P + X = **I**            (모든 갈래에서 같다)
  배너 아래끝  = I + M = I + m
  아래 여백 = (I+m) − I = **m**            ← I와 무관
  위 여백  = (I + 50 + 2m) − (I + m + 50) = **m**   ← I와 무관
★ 즉 **m에 기하적 상한이 없다.** m=40이어도 성립한다(갈래 C 검산: 배너[88,138] ·
  배경면[48,178] · 40/40).
⚠ 남는 상한은 **심미 판단**이다 — H가 화면의 몇 %를 먹는가.
  m=12 → 74dp(휴대폰 8.4%) · m=16 → 82dp(9.3%) · m=24 → 98dp(11.2%)
```

**폴백 — ⛔ 주입값이 없으면 ④를 포기한다**

```
왜   ④(상하 대칭)는 **상한을 크게 잡아 덮는 방어를 금지한다.** 그래서 주입값이
     없을 때 대칭식을 그대로 쓰면 갈래 B에서 위 여백이 음수가 된다
규칙  주입값 있음  →  X = 주입값 · M = m · H = 50 + 2m        (④ 만족)
     주입값 없음  →  **2.112의 옛 식으로 떨어진다**
                    H = 50 + safeArea + (safeArea > 0 ? 24 : 48)
                    ⛔ 비대칭이지만 **음수가 안 된다.** 그것이 폴백의 목적이다
⚠ 폴백이 조용한 실패가 되지 않게 — 회귀가 「주입값 없음 → 옛 식」을 단언하고,
  개발 빌드에서 콘솔에 한 줄 남긴다. ⛔ 화면에는 아무것도 드러내지 않는다
```

**✅ 리스너 충돌 — 확인했다. 겹치지 않는다**

```
캐패시터   SystemBars.java:199
          ViewCompat.setOnApplyWindowInsetsListener(**webView.getParent()**, …)
AdMob     BannerExecutor.java:135
          **decorView**.setOnApplyWindowInsetsListener(…)  → `return insets`
우리      지금 아무 데도 안 건다 (app/android/app/src 에 WindowInsets 없음)
★ 서로 **다른 뷰**다. 그리고 AdMob이 원본 insets를 그대로 돌려주므로
  자식(웹뷰 부모)으로 가는 디스패치가 끊기지 않는다
⛔ 그래서 안 2의 네이티브 조각은 **decorView와 webView.getParent() 둘 다 피한다.**
  → **WebView 자신**에 건다. 그 자리는 비어 있다
```

**네이티브 조각 — 어디에 얼마나**

```
자리   app/android/app/src/main/java/…/MainActivity.java
      지금 3줄짜리 빈 BridgeActivity다. 여기서 onCreate 뒤에 한 번 건다
분량  약 25~30줄 (리스너 + 계산 + evaluateJavascript 한 줄)
하는 일
  ① ViewCompat.setOnApplyWindowInsetsListener(webView, …)
  ② I  = insets.getInsets(Type.systemBars()).bottom
  ③ P  = 화면 높이 − (웹뷰의 화면상 아래끝)      ← getLocationOnScreen + height
  ④ X  = max(0, I − P)                        ← **웹 좌표계 안에 들어온 인셋**
  ⑤ webView.evaluateJavascript(
       "document.documentElement.style.setProperty('--inset-bottom-real','" + Xdp + "px')")
  ⑥ return insets   ← ⛔ 원본을 그대로 넘긴다. 안 그러면 캐패시터 쪽이 끊긴다
⚠ 회전·멀티윈도우에서 리스너가 다시 불리므로 값이 따라간다
⛔ 이 조각이 늘어나면 안 된다 — 인셋 하나만 넘긴다. 다른 용도를 얹지 말 것
```

#### ✅ ② 는 **(가) 문서 padding-bottom = H + X** 로 확정 (사용자 확정)

```
사용자 근거  (나)는 스크롤 주체가 문서가 아니게 되어 2.107 키보드 처리가 걸리고,
           화면마다 스크롤 주체가 갈리는 **새 조용한 실패 자리**를 만든다.
           그 대가가 "스크롤 중에도 안 지나간다"는 이득보다 크다
```

**⚠⚠ 정정 — 페이드는 「버린다」가 아니라 「남긴다」. 실측이 뒤집었다**

세션이 앞에서 "페이드를 버리고 1px 선으로"를 권고했고 사용자가 동의했는데,
**(가)에서 페이드를 꺼 보니 결론이 달라진다.**

```
실측 (갈래 C · 결과 화면 · 스크롤 55% 지점)
  페이드 있음   마지막 줄이 배경면으로 부드럽게 사라진다
  페이드 없음   **글자가 가로로 선명하게 잘린다** — 획 중간에서 끊긴 것이 보인다
★ 페이드의 두 역할을 갈라야 한다
  ① 배너 윗변이 삐져나올 때 하드 엣지를 감춘다  →  ④가 없앤다. **이 이유는 사라진다**
  ② 스크롤 경계를 흐린다                      →  (가)에서는 **여전히 필요하다**
⛔ 사용자가 싫다고 한 것은 **「스크롤 끝에서 마지막 항목이 잠기는 것」**이고,
  그것은 padding-bottom = H + X 가 고친다 — 끝에서는 항목이 배경면 위에서 멈춘다
★ 그러므로 **페이드를 남기고 padding을 더한다**가 두 요구를 다 만족한다
⚠ 1px 선은 넣지 않는다 — 여기는 "구간이 끝나는" 자리가 아니라 "잘리는" 자리다.
  2.36 3차의 선은 종류가 갈리는 경계에 쓴 것이라 뜻이 다르다
⛔ 이 정정은 실측 화면이 근거다. 다시 버리자는 제안이 나오면 그 화면부터 볼 것
```


#### ⛔⛔ 실기기에서 터진 사고 셋 — **다음 세션이 같은 증상을 보면 바로 알아보게 적는다**

> ★ 셋 다 회귀가 못 잡는 자리였다. 순수 함수는 전부 통과했고 화면만 틀렸다.

```
① 웹뷰에 인셋 리스너를 걸었더니 **뷰포트가 800 → 848dp로 늘었다**
   증상   앱이 내비 바 밑까지 내려간다. 하단 48dp가 콘텐츠에 먹힌다
   원인   View.setOnApplyWindowInsetsListener를 달면 그 뷰의 **기본 인셋 처리가
         대신 건너뛰어진다.** `return insets`로 원본을 넘겨도 소용없다 —
         건너뛰어진 것은 소비가 아니라 **뷰 자신의 처리**다
   ⛔ 2.118이 "미확인 위험"으로 적어 둔 그 자리에서 실제로 터졌다
   고침   리스너를 떼고 **addOnLayoutChangeListener로 읽기만** 한다.
         ViewCompat.getRootWindowInsets()는 디스패치 체인에 안 들어간다

② 주입값이 **페이지 로드에 덮여** 사라진다 → 폴백에 머문다
   증상   배경면이 82가 아니라 **98dp**(갈래 C) · **122dp**(갈래 A). 대칭이 안 된다
   원인   푸시가 로드보다 빠르다. documentElement의 인라인 스타일이 새 문서에서
         사라지는데, 그때 레이아웃은 안 바뀌므로 리스너가 다시 안 불린다
   ⛔ "같은 값은 다시 넣지 않는다"는 최적화가 이것을 **더 단단히 막았다**
   고침   중복 방지를 없애고 **0·400·1200·3000ms에 네 번** 넣는다.
         onResume에서도 같은 묶음을 다시 건다

③ 주입값을 넣어도 **리액트가 모른다**
   증상   ②와 같다 — 값은 CSS에 있는데 화면은 폴백이다
   원인   훅이 resize에서만 다시 읽는데 주입은 마운트 뒤에 온다
   고침   네이티브가 값을 넣은 뒤 `pym:inset` 이벤트를 쏘고 훅이 그것을 듣는다
★ ②와 ③은 **증상이 같다.** 배경면이 82가 아니면 둘 다 의심할 것 —
  CSS 변수가 비어 있으면 ②, 값은 있는데 화면이 안 바뀌면 ③이다
```

#### ✅ 실측 — 세 갈래 중 둘 (2026-09-03)

```
갈래 C · Galaxy Z Flip · Android 13 · 3버튼(48dp) · dpr 3.0
  배경면 2246..2491 (246px = 82.0dp) · lift 0 · 주입값 0.0px
  위 여백 2246..2293  **48px = 16.0dp**
  배너   2294..2443  150px = 50.0dp
  아래   2444..2491  **48px = 16.0dp**
  ★ 바뀐 것 — 위 48 · 아래 0 · 98dp  →  **16 · 16 · 82dp**

갈래 A · Galaxy Tab S9 FE · Android 15 · taskbar(48dp) · dpr 1.75
  배경면 2077..2219 (143px = 81.7dp) · lift 48dp · 주입값 48.0px
  위 여백 2077..2104  **28px = 16.0dp**
  배너   2105..2191   87px = 49.7dp
  아래   2192..2219  **28px = 16.0dp**
  배경면 아래로 84px(48dp)가 남는다 — 인셋 위에 떠 있다(설계대로)
  ★ env(safe-area-inset-bottom)=48 과 주입값 48 이 **일치한다** —
    브라우저가 참인 갈래에서 둘이 맞으니 주입 계산이 옳다는 교차 확인이다

갈래 C **폴백** (주입을 일부러 끊고)
  배경면 98dp · lift 16dp · 위 48 · 아래 0 — **2026-09-03 이전과 같은 기하**
  ⛔ 음수 없음. 폴백이 지켜야 할 단 하나가 그것이다

스크롤   끝에서 마지막 콘텐츠 아래끝 694 < 배경면 윗변 718 — **안 잠긴다**(24dp 여유)
        바닥 여백 122px = 40 + 82 + 0
회전    가로 뷰포트 800×336 · 배경면 82dp · lift 0 — **세로 복귀 뒤에도 82dp**
        ★ margin이 상수라 회전에 굳을 것이 없다(2.118의 예측대로)
```

#### ⛔ 사고 ④ — 갈래 B에서 배너가 **배경면 위로 튀어나왔다**(위 −8dp)

```
증상   갈래 B(에뮬 · Android 15 · WebView 124)에서만. A·C는 멀쩡했다
원인   X = I − P 가 틀렸다. 배너 아래끝을 플러그인 소스로 풀면
         배너 = P + margin + (SDK ≥ 35 ? I : 0)
       뒤 항이 BannerExecutor의 인셋 리스너인데 **부모가 이미 P만큼 올라와
       있으면 인셋이 두 번 들어간다.** 갈래 B가 정확히 그 경우다(P=24 · SDK 35)
고침   **X = (SDK ≥ 35) ? I : 0**
       실측 X — 갈래 A 48(P=0) · 갈래 B 24(P=24) · 갈래 C 0(SDK 33)
★ 확인   배너 아래끝이 **세 갈래 모두 화면 바닥에서 64dp**다. 그 한 값이 맞으면
        나머지가 따라온다 (64 = P + 16 + (SDK≥35 ? I : 0))
⚠ 이 식은 플러그인 동작에 매인다 — pluginPins.test.js가 버전을 못 박는 이유다
```

#### ✅✅ 세 갈래 실측 — **전부 16.0 / 50 / 16.0** (2026-09-03)

```
갈래 A 태블릿  Android 15 · dpr 1.75 · 주입 48   위 16.0 · 배너 49.7 · 아래 16.0
갈래 B 에뮬    Android 15 · WebView 124 · 주입 24  위 16.0 · 배너 50.0 · 아래 16.0
갈래 C 휴대폰  Android 13 · dpr 3.0 · 주입 0     위 16.0 · 배너 50.0 · 아래 16.0
★ 요구 사양 ④ 달성. 배경면은 세 갈래 모두 82dp(50+2m)다
⚠ 픽셀 스캔의 색 허용치를 6으로 두면 배경면 아래 그림자·그라디언트가 배경면으로
  잡혀 **아래 여백이 40dp로 잘못 나온다.** 허용치 2로 잴 것
```

#### ⚠ 회전 — 세로 기하는 유지되는데 **배너가 가로에서 왼쪽으로 치우친다**

```
실측   가로(2636×1080)에서 배경면은 화면 하단 82dp 전체 폭인데,
      배너는 x≈1318 이후로 없다. 왼쪽 절반에만 있다
원인   플러그인이 좌우 마진을 **showBanner 때 한 번** 계산한다 —
      margins[0]=margins[2]=(defaultWidthPixels − adWidth)/2 (BannerExecutor.java).
      세로 폭(1080)으로 잡힌 값이 가로(2636)에서도 그대로 쓰인다
⛔ **우리가 만든 것이 아니다.** margin(하단)만 상수로 바꿨고 좌우는 손대지 않았다 —
  2026-09-03 이전에도 같았을 것이다(가로를 잰 적이 없어 몰랐다)
★ 세로 기하(위·아래 16dp)는 회전 뒤에도 그대로다. 어긋나는 것은 **좌우 중앙**뿐이다
⚠ 분류는 ② — 사용자에게 묻는다. 고치려면 회전 때 showBanner를 다시 불러야 하고,
  그것은 광고를 새로 요청하는 것이라 값이 다르다
```


#### ⛔⛔ 실사용이 실측을 두 번 뒤집었다 — **"더 나올 자리가 없다"는 전제가 깨졌다**

> ★ 세션이 "갈래 셋을 다 실측했으니 새 조건이 나올 자리가 없다"고 적었다.
> **그 전제가 두 번 깨졌다.** 그리고 두 번 다 사용자가 먼저 발견했다.

```
① 갈래 B의 **이중 계산** — X = I − P 가 틀렸다 (위 −8dp)
   세션이 세 갈래를 다 잰 뒤에 나왔다. 플러그인이 Android 15+ 에서만 인셋을
   더하는데, 부모가 이미 올라와 있으면 두 번 들어간다
② 토글이 **손가락으로 안 눌린다**
   세션은 CDP click으로 검증했다 — 그것은 히트 테스트를 건너뛰고,
   **표식이 없는 상태만** 봤다. 실제 원인은 세션이 넣은 가드였다
③ 배경면 아래 **틈으로 콘텐츠가 샌다**
   ④(대칭)를 지키려 배경면을 띄운 자리가 여전히 웹뷰 안이라
   스크롤 콘텐츠가 그리로 지나갔다. 갈래 B에서만 드러난다
   (갈래 C는 lift 0이라 틈이 없고, 갈래 A는 시스템 바 뒤라 덜 띈다)
⛔ **교훈** — 갈래를 다 쟀다는 것은 "그 조건에서 그 값이었다"일 뿐이다.
  ★ 검증을 **손가락과 같은 경로로** 해야 한다. CDP click·JS 주입은 편하지만
    히트 영역·터치 우선순위·실제 상태를 건너뛴다.
  ★ 그리고 **실측이 화면과 갈리면 실측을 의심한다** — 아래 허용치 사건이 그것이다
```

#### ⛔ 픽셀 허용치 함정 — 세션이 스스로를 속였다

```
증상   갈래 B 아래 여백이 **40dp**로 나왔다. 실제로는 16dp였다
원인   배경면 색(#141E24) 판정 허용치를 6으로 뒀다. 배경면 **아래쪽 그라디언트**가
      #161921·#191C25 인데 (20,30,36)과의 차가 (2,5,3)·(1,2,3)이라 **범위 안**이다
      → 그라디언트가 배경면으로 잡혀 아래 여백이 두 배 넘게 부풀었다
고침   허용치 **2**. scripts/measure_banner.py 에 상수로 박고 주석을 달았다
⚠ 색으로 경계를 찾을 때는 **경계 바깥 색이 무엇인지** 먼저 보고 허용치를 정한다.
  이 화면은 배경면(#141E24)과 문서(#0B1216)의 차가 (9,12,14)라 허용치 6이면
  중간 그라디언트를 못 가른다
```

#### ⚠ 세션이 금지된 명령을 썼다 — `am force-stop`

```
언제   2026-09-03 · 네이티브를 다시 설치하며 프로세스를 확실히 내리려고 썼다
왜 금지  안드로이드가 그 uid의 AlarmManager 예약을 **전부 지운다**(3절 실행 환경)
결과   예약이 지워졌고, 앱을 한 번 열어 refreshSchedule이 다시 잡았다(22줄 확인)
⛔ `install -r` 이 이미 프로세스를 내린다 — force-stop을 쓸 이유가 없었다
```

#### ✅ 세 갈래 일괄 측정 절차 — `scripts/measure_banner.py`

```
왜   세 갈래가 **하나의 식을 공유한다.** 갈래 B를 고치면 A·C가 함께 바뀐다 —
     실제로 X 식을 고쳤을 때 셋이 다 움직였다. 손으로 셋을 돌리면 빠뜨린다
쓰는 법   python scripts/measure_banner.py            (붙어 있는 기기 전부)
        ⚠ 각 기기가 **결과 화면 + 배너 채워짐** 상태여야 한다. 화면은 안 넘긴다
기준     위 16 · 아래 16 · 배경면 82 · **배너 아래끝 64dp**
        ★ 배너 아래끝 64dp가 항등식이다 — P + margin + (SDK≥35 ? I : 0)
2026-09-03 결과
  R39N500G7YF (갈래 C)  16.0 / 50.0 / 16.0 · 82.0 · 64.0  OK
  R54X204JA8V (갈래 A)  16.0 / 49.7 / 16.0 · 81.7 · 64.0  OK
  emulator-5554 (갈래 B) 16.0 / 50.0 / 16.0 · 82.0 · 64.0  OK
★ **이것이 기준선이다.** 배너 식을 건드린 뒤에는 반드시 셋을 다시 재고,
  어긋나면 고친 것을 되돌린다
```

#### ⛔ 토글이 손가락으로 안 눌리던 것 — 가드가 사용자를 가뒀다

```
증상   구절 알림 토글이 OFF에서 눌리지 않는다. 아무 반응도 없다
원인   세션이 넣은 「팝업 최대 1회」 가드다. 거부 표식(OFF_BY=permission)이 있으면
      setEnabled가 즉시 false를 돌려주고 토글이 되돌아간다.
      ⛔ 2.119 설계에는 "그러면 시스템 알림 설정으로 보낸다"고 적혀 있었는데
        **세션이 가드만 만들고 탈출구를 안 만들었다**
가름   데이터를 지우니 같은 좌표를 손가락으로 눌러도 정상 동작했다 —
      **히트 영역 문제가 아니다**(34×20dp이지만 눌린다)
고침   가드를 **자동 경로에만** 건다. `setEnabled(next, { userInitiated: true })`
      ★ 안드로이드는 **두 번** 거절해야 영영 안 띄운다. 한 번 뒤에는 한 번 더
        물을 수 있고, 그 한 번은 사용자가 요청했을 때 쓴다
      ⚠ 이미 영구 거절이면 팝업 없이 즉시 denied가 온다 — 소모할 것이 없다
실측   거부 → 표식 남음 → **다시 탭하니 팝업이 다시 떴다** (예전엔 무반응)
⚠ 남은 것 — 두 번 거절한 뒤에는 여전히 켤 수 없다(안드로이드 공통).
  시스템 알림 설정으로 보내려면 네이티브 인텐트가 필요하다. ⛔ 안 만들었다
```

#### 🔴 출시 후 항목 — 가로에서 배너가 왼쪽으로 치우친다 (사용자 확정 · 지금 안 고친다)

```
증상   가로에서 배경면은 하단 전체 폭인데 배너가 왼쪽 절반에만 있다
원인   플러그인이 좌우 마진을 showBanner 때 **한 번만** 계산한다
      margins[0]=margins[2]=(defaultWidthPixels − adWidth)/2 (BannerExecutor.java)
대가   고치려면 회전 때 showBanner를 다시 불러야 하고 **광고를 새로 요청**하게 된다
⛔ 출시 후로 미룬다(사용자 확정). 세로 기하는 회전 뒤에도 그대로다
```

#### ⚠ 지금 설계에서 **버리는 것** — 명시한다

```
1. 2.112 「배경면이 인셋을 흡수한다」
   갈래 A에서 배경면이 인셋 위로 뜬다(X = s). 인셋 자리에는 문서 배경이 보인다.
   Δ(9,12,14) 수준이라 크지는 않지만 **의도적으로 버리는 것**이다
2. 2.112 「틀려도 음수가 안 된다」는 미덕
   ④가 상하를 같게 요구하므로 **상한을 크게 잡아 덮는 방어가 불가능하다.**
   안 1은 갈래 B에서 실제로 음수가 된다. 안 2만 이것을 회복한다
3. 페이드 (BAND_FADE)
4. GAP_KNOWN(24) · GAP_UNKNOWN(48) 두 상수 체계 → **m 하나**로 대체된다
5. 갈래 A의 「위 여백이 인셋과 무관하게 GAP으로 고정」이라는 구조적 미덕
   이제 X가 인셋을 따라가야 하므로 **인셋 읽기에 더 의존한다**
6. (안 2를 고르면) 「네이티브를 늘리지 않는다」는 흐름
```


---

### 2.119 결과 화면 탭 행 「구절 알림」 토글 — 설계와 **구현·실측** (2026-09-03)

#### ⛔⛔ 토글이 안 켜졌던 진짜 원인 — **OS 영구 거절이었다**

> ★ 세션은 이것을 **재현하지 못했다.** 사용자가 원인을 알려주었다.

```
증상   토글이 OFF에서 눌리지 않는다. 누르는 반응은 있는데 켜지지 않고 되돌아온다
세션의 오진   저장값 OFF_BY 표식(user/permission)을 의심했다.
            ⛔ **OFF_BY=user 는 다시 켜는 것을 막지 않는다** — 가드는 permission만 본다
진짜 원인   **안드로이드가 이미 영구 거절 상태**였다. 두 번 거절하면 OS가 팝업을
          더 띄우지 않고 requestPermissions가 **즉시 denied**를 돌려준다.
          그러면 setEnabled가 false를 주고 토글이 되돌아간다 — 우리 코드는 정상이다
왜 재현이 안 됐나   세션의 시험 기기는 pm clear를 여러 번 해서 권한 상태가
          **초기화돼 있었다.** 영구 거절 상태를 만들지 못했으니 증상도 안 나왔다
★ 지금 빌드   사용자가 **직접 누르면** 가드를 통과한다(userInitiated).
          그래서 "우리 표식 때문에 갇히는" 경우는 없어졌다
🔴 남은 것   영구 거절 뒤에는 여전히 켤 수 없다 — **안드로이드 공통이다.**
          시스템 알림 설정으로 보내거나 토스트로 알려야 한다. ⛔ 아직 안 만들었다
⚠ 다음 세션이 같은 신고를 받으면 **권한 상태부터 본다**
   adb shell dumpsys package io.github.appleap385.peaceinmind | grep POST_NOTIFICATIONS
   granted=false 이고 flags에 USER_SET 이 있으면 영구 거절을 의심한다
```

#### (설계) 결과 화면 탭 행에 「구절 알림」 토글

> ★ 2.116 ②의 D(안내 상자)를 대신한다. **결과 화면에 요소가 늘지 않는다.**

```
목표 모양
  이어 읽기 │ 말씀 20  찬양 20            [│]  구절 알림 (토글)

  · 「이어서 읽기」 → **「이어 읽기」** 로 줄여 간격을 확보한다
  · 「구절 알림」과 토글은 **오른쪽 정렬**. 찬양 탭과 거리를 둔다
  · **같은 행**에 둔다
```

**왜 이 방식이 나은가** (사용자 근거)

```
· 새 요소를 만드는 것이 아니라 **이미 있는 행**을 쓴다 → 결과 화면에 요소가 안 는다
· About에 들어가야만 있던 알림 설정이 **결과 화면에서 바로 보인다**
  → D가 풀려던 문제(About을 안 열면 권한을 영영 안 묻는다)가 함께 풀린다
★ 문구를 「구절 알림」으로 정한 근거 — **안드로이드 알림 채널 이름이 이미
  「구절 알림」이다**(3절 앱 사실 · channel morning_verse). 시스템 설정 화면과
  앱 안 표기가 같아지므로 사용자가 설정에서 그 항목을 찾을 때 이름이 일치한다
```

**✅ 폭 실측 — 360dp에서 한 행에 들어간다** (Galaxy Z Flip · 갈래 C · 실기기)

```
지금        행 [20,340] = 320dp · 내용 끝 **225.2dp** → 오른쪽 여유 **114.8dp**
            이어서 읽기[20,90] · 말씀20[119,164.1] · 찬양20[180.1,225.2]
「이어 읽기」로 줄인 뒤   탭 끝 **212dp** (13.2dp 절약)
설정 그룹   구분선 없음 **93.5dp** [246.5,340] · 탭과 사이 **34.5dp**
           구분선 있음 **116.5dp** [223.5,340] · 탭과 사이 11.5dp(+구분선 14dp 여백)
✅ **줄바꿈 없음.** 숫자 배지(20)까지 포함해 한 행에 들어간다
```

**⚠ 탭과 설정의 성격 차이 — 오른쪽 정렬만으로는 약하다. 구분선을 권고한다**

```
문제   앞 세 항목은 **화면을 바꾸는 탭**이고 이것은 **설정**이다. 성격이 다른데
      같은 행에 있으면 네 번째 탭으로 오해할 여지가 있다
실측   오른쪽 정렬만(여백 34.5dp)  — 갈리기는 하나 약하다
      **세로 구분선 추가**        — 확실히 갈린다
★ 권고 — 세로 구분선. **이 행이 이미 쓰는 관용이다**:
  「이어 읽기 │ 말씀 찬양」에서 본문 묶음과 영상 형식 묶음을 같은 선으로 갈랐다.
  세 번째 묶음(설정)도 같은 선으로 가르는 것이 일관된다
  (styles.divider — width 1 · height 11 · #ffffff1f · margin 0 14px)
⚠ 대가 — 선이 셋이 되면 "말씀 │ 찬양 │ 구절 알림"처럼 **세 묶음이 동등해** 보일
  여지가 미세하게 있다. 다만 토글의 **형태**가 그것을 막는다(아래 규칙 2)
```

**★ 규칙 2(2.36)가 여기서 어떻게 적용되나 — 토글은 이미 만족한다**

```
규칙 2  "글자만 나란히 놓인 컨트롤은 색으로 구분되지 않는다. 컨트롤이라는
       표시(선·면·형태)가 없으면 읽는 글과 같은 종류로 읽힌다"
★ 토글은 **형태**를 가진 컨트롤이다. 글자만인 탭들과 종류가 다르게 보인다 —
  규칙 2가 요구하는 표시를 스위치 자체가 들고 있다
⚠ 라벨 「구절 알림」은 글자지만 토글에 붙어 있어 **라벨로 읽힌다.** 단독 컨트롤이
  아니므로 규칙 2의 위험에 해당하지 않는다
토큰 안  라벨 #8DA29E(muted) 85% · 13px
        스위치 34×20dp · radius 10 · 손잡이 16dp
        켜짐  트랙 rgba(127,179,163,.55) · 손잡이 #7FB3A3
        꺼짐  트랙 rgba(255,255,255,.12) · 손잡이 #8DA29E
```

**동작 설계 — About 토글과 **같은 함수**를 쓴다**

```
누르면   setEnabled(next)  ← About.jsx의 toggle이 부르는 바로 그 함수다
  켜기   ensurePermission() → granted면 NOTIFY_ON=true · OFF_BY 지움 · refreshSchedule()
        거부면 **false를 돌려주고** 호출부가 토글을 되돌린다
  끄기   NOTIFY_ON=false · OFF_BY=user · cancelAll()
⛔ 새 함수를 만들지 않는다. 두 자리가 갈리면 한쪽만 고쳐진다
회귀    · 결과 화면 토글과 About 토글이 **같은 함수**를 부른다 (소스 단언)
       · 거부되면 저장값이 켜지지 않는다 (순수 판정으로)
       · 끄면 OFF_BY=user 가 남는다
```

**⛔ 시스템 팝업 소모 — 묻는 자리가 하나 느는 것이니 규칙을 세운다**

```
제약   Android 13+ 는 **두 번 거절하면 OS가 더 이상 안 띄운다.** 회복이 없다
묻는 자리
  ① About mount effect   ⚠ A′ 뒤에는 저장값이 false라 **안 돈다**
  ② About 토글
  ③ **결과 화면 토글** (새로 생기는 자리)
⛔ 그대로 두면 사용자가 토글을 껐다 켰다 하며 팝업을 계속 소환할 수 있다
★ 규칙 — **거절이 한 번이라도 기록되면 그 뒤로는 시스템에 묻지 않는다.**
  · setEnabled(true)가 권한 거부로 실패하면 **OFF_BY=permission 을 남긴다**
    (⚠ 지금은 그 자리에서 아무것도 안 남긴다 — A′에 이 한 줄을 더해야 한다)
  · 그 표식이 있으면 토글을 켜도 ensurePermission을 부르지 않고
    **시스템 알림 설정 화면으로 보낸다**
★ 그러면 우리가 소모하는 팝업은 **최대 1회**다. OS의 두 번째 기회를 남긴다
```

**켜져 있을 때 · 껐다 켜기**

```
켜짐   토글이 켜짐으로 보인다. 누르면 꺼진다(cancelAll) — 예약이 사라진다
다시 켜기  ensurePermission이 **이미 granted면 팝업 없이 통과**하고 예약이 다시 잡힌다
⚠ 껐다 켜는 것은 자유다. 팝업을 다시 소모하지 않는다
```

**⚠ 시각 설정은 여기 두지 않는다**

```
⛔ 결과 화면에는 **켜고 끄는 것만** 둔다. 시각은 About에만 있다.
  결과 화면이 설정 화면이 되면 안 된다 (사용자 확정)
```

**✅ 위기 화면 · 알림으로 열린 구절 — 구조적으로 안전하다**

```
확인  CrisisScreen.jsx 와 DailyVerse.jsx 는 **ResultTabs를 임포트하지 않는다**
     (grep 결과 0건). 두 화면은 탭 행을 아예 그리지 않는다
★ 그러므로 "토글만 빼는" 분기가 **필요 없다.** 새 조건문이 안 생긴다
⚠ 회귀로 못 박는다 — 그 두 파일이 ResultTabs를 임포트하지 않는다는 단언.
  나중에 누가 탭 행을 재사용하면 토글이 따라 들어가는 것을 막는다
```

**★ A′의 부수 효과를 이 토글이 대신하는가 — 대신한다**

```
A′의 부수 효과   저장값이 false로 내려가므로 About의 mount effect가
                `if (!s.on) return` 에 걸려 **자동으로 묻지 않게 된다**
대신하는 것      결과 화면 토글이 **보이는 자리**가 된다. About을 열지 않아도
                사용자가 알림을 켤 수 있고, 그때 묻는다
★ D보다 낫다 — D는 "한 번 띄우고 사라지는" 안내였고, 토글은 **결과 화면에
  항상 있다.** 놓쳐도 다음에 다시 보인다
⚠ 단, 화면이 「꺼짐」으로 보이는 것이 정직해지는 것은 A′의 몫이다.
  **A′와 이 토글은 함께 들어가야 한다** — A′ 없이 토글만 넣으면 토글이
  「켜짐」인데 알림이 안 오는 상태가 그대로 남는다
```

---

### 2.120 ★ 영구 거절을 **감지할 수 있다** · 토스트 마무리 (2026-09-03)

#### ⛔⛔ 먼저 — 두 번 거절한 상태는 **알 수 있다.** 우리가 셀 필요가 없다

> ★ 세션이 소스와 실기기 양쪽으로 확인했다. 「알 수 없다」던 전제가 틀렸다.

**Capacitor가 이미 세고 있다** — 소스 근거

```
requestPermissions 결과가 올 때마다 Bridge.validatePermissions(Bridge.java:1161)가
  ActivityCompat.shouldShowRequestPermissionRationale(activity, perm)
을 읽어 SharedPreferences「PluginPermStates」에 적는다.
  rationale true  → "prompt-with-rationale"   (아직 한 번 더 물을 수 있다)
  rationale false → **"denied"**              (영구 거절)
checkPermissions는 SDK 33+ 에서 super를 부르고(LocalNotificationsPlugin.kt:462),
Bridge.getPermissionStates가 **그 캐시를 그대로** 웹에 돌려준다.
★ 그래서 우리가 거부 횟수를 셀 이유가 없다 — OS 플래그가 근원이고 캐시가 그 사본이다
```

**✅ 실측 — 갈래 B(에뮬레이터 · Android 15)에서 네 상태를 다 만들어 봤다**

```
상태            OS 플래그                     checkPermissions().display
아직 안 물음     (플래그 없음)                 prompt
한 번 거절       USER_SET                     **prompt-with-rationale**
★ 영구 거절      USER_SET|USER_FIXED          **denied**
허용            granted=true                  granted
⛔ requestPermissions의 **반환값은 셋을 못 가른다** — 한 번 거절도 영구 거절도
  똑같이 {"display":"denied"}다. 그것은 areNotificationsEnabled()라서 2값뿐이다
  (LocalNotificationsPlugin.getNotificationPermissionText)
★ 그래서 **물어본 뒤 checkPermissions를 한 번 더** 부르는 것이 판정이다
⚠ 영구 거절 상태에서도 GrantPermissionsActivity는 **뜬다.** 다만 TRANSLUCENT라
  화면에는 아무것도 안 보이고 즉시 끝난다(logcat·연속 스크린샷으로 확인).
  ⛔ "팝업이 뜨는가"를 눈으로 보고 판정하지 말 것 — 액티비티는 뜬다
```

**⛔⛔ 물어보기 전 값은 믿으면 안 된다 — 오탐을 실제로 만들어 봤다**

```
만든 상태   캐시 "denied" · OS는 USER_SET만 (USER_FIXED 해제)
물어보기 전  checkPermissions → **"denied"**   ← 낡은 캐시다
실제로 누르면 **시스템 팝업이 떴다**            ← 안내를 띄웠으면 사용자가 헤맨다
물어본 뒤   캐시가 새로 쓰여 판정이 스스로 맞는다
★ 이유 — validatePermissions가 플러그인 콜백보다 **먼저** 돈다
  (Plugin.triggerPermissionCallback → validatePermissions → 그다음 콜백)
⛔ 그래서 setEnabled가 false를 돌려준 **뒤에만** 묻는다. 이 순서가 설계의 전부다
```

**⚠ 한계 — 이것도 완전하지 않다. 다만 위험한 쪽이 아니다**

```
① 앱 데이터 삭제   캐시가 지워져 prompt로 돌아간다. ⚠ OS 플래그도 함께 초기화되므로
                 **둘이 어긋나지 않는다** — 실제로 다시 물을 수 있는 상태가 맞다
② 미사용 앱 자동 권한 재설정(Android 11+)   OS는 풀리는데 캐시는 "denied"로 남는다.
                 → 물어보기 전에 판정하면 오탐이다. **물어본 뒤 판정이 이것을 지운다**
③ 설정에서 껐다 켠 경우   ✅ 실측 — 설정에서 알림을 끄면 **USER_SET만** 붙는다
                 (USER_FIXED 아님). 즉 **영구 거절이 아니고**, 판정도 그렇게 나온다
④ Android 12 이하   checkPermissions가 areNotificationsEnabled()를 준다. 거기서의
                 "denied"는 설정에서 끈 것이고 런타임 팝업 자체가 없으므로
                 **안내할 내용이 같다**(설정에서 켜야 한다). 갈래를 안 나눈다
⛔ 앱이 직접 횟수를 세는 방식은 **채택하지 않았다.** ①②③을 하나도 못 잡는다
```

**★ 시험 상태를 다시 만드는 법 — 휴대폰이 granted라 필요하다**

```
⛔ pm clear 를 쓰지 말 것 — 알림 시각·NOTIFY_SEEN까지 다 지워진다
⛔ am force-stop 을 쓰지 말 것 — 예약이 지워진다(3절 함정 ③).
   ⚠ 세션이 이번에도 한 번 썼다. 프로세스만 내리려면
     adb shell input keyevent KEYCODE_HOME  뒤에
     adb shell "run-as <pkg> kill <pid>"   ← 이것이 안전한 방법이다
     (am kill · am kill-all은 포그라운드 앱에 안 듣는다 — 실측)

[영구 거절 만들기]
  adb -s <기기> shell pm revoke io.github.appleap385.peaceinmind \
      android.permission.POST_NOTIFICATIONS
  adb -s <기기> shell pm set-permission-flags io.github.appleap385.peaceinmind \
      android.permission.POST_NOTIFICATIONS user-set user-fixed
  ⚠ 이것만으로는 캐시가 안 생긴다(= 물어보기 전에는 "prompt"로 보인다).
    **토글을 한 번 눌러야** 판정이 선다 — 그것이 정상 흐름이다

[되돌리기]  pm grant … POST_NOTIFICATIONS
           pm clear-permission-flags … user-set user-fixed
           run-as <pkg> rm -f …/shared_prefs/PluginPermStates.xml
           그다음 프로세스를 내렸다 올린다 — ⛔ 캐시는 메모리에 남아서
           파일만 지우면 안 바뀐다(실측)

[손으로 하려면]  설정에서 알림 끄기 → 토글 ON → 팝업에서 거절
           ⚠ 설정에서 끈 것이 USER_SET을 채우므로 그 거절이 **두 번째**가 된다
```

#### ✅ 만든 것 — 영구 거절 뒤의 탈출구

```
판정      isBlocked(raw, threw)  — notifyPermission.js · 순수 함수
         ⛔ permissionOutcome(3값)에 **4번째 값으로 넣지 않았다.** 넣으면
           shouldTurnOff·enableDecision의 기존 갈래를 조용히 지나친다
         ★ 「권한이 있나」와 「물어볼 수 있나」는 다른 축이다 (2.107 ②와 같은 논리)
조회      isPermissionBlocked()  — notify.js. ⛔ setEnabled 실패 **직후에만** 부른다
화면      App.jsx toggleNotify — 실패하면 토글을 되돌리고, blocked일 때만 토스트
문구      **"기기 설정에서 알림을 켜 주세요"** (사용자 확정)
         ★ About의 확정 문구와 같은 어휘다 —
           "기기 설정에서 이 앱의 알림을 켜면 다시 받을 수 있습니다"
         ⛔ 「휴대폰」이라 쓰지 않는다 — 태블릿에서 틀린 말이 된다
         ⛔ 설정 **경로**를 박지 않는다 — 기기마다 다르다
⛔ 시스템 설정 화면으로 **보내지 않는다.** 알려만 준다 (사용자 확정)
⛔ 토글은 OFF로 남는다
토스트 모양   성공 토스트와 **완전히 같다** (같은 toastStyle을 쓴다)
⚠⚠ **뒷일** — 이 절에서 만든 토글·토스트가 **웹에도 나갔다**(2.122).
   `__IS_APP__` 분기가 빠져 있었고, 다음 날 두 겹으로 막았다.
   ⛔ 이 절만 읽고 "앱 전용이다"라고 믿지 말 것 — 그 조건은 2.122에서 들어갔다
✅ 실측(갈래 B) — 한 번 거절 상태에서 누르면 **팝업이 뜨고 토스트는 안 뜬다.**
   영구 거절 상태에서 누르면 **토스트가 뜨고 토글은 false로 남는다**
⚠ About의 토글에는 이 토스트가 안 뜬다 — About에는 이미 문장 자리가 있고
  거기 확정 문구가 있다("알림 권한이 없어 보낼 수 없어요…"). 토스트가 생긴 이유가
  "탭 행에는 문장을 넣을 자리가 없다"였으므로 About에는 필요가 없다
```

#### ✅ 토스트 마무리 — 배선은 되어 있었다. 안 보인 이유가 따로 있다

**⛔ 진단 — 「페이드만 있다」가 아니었다**

```
옛 코드   toastOut 0.5s ease · translateY(0) → **-6px**  ← 배선은 정상이었다
실측(갈래 B · 프레임 단위 · 옛 빌드)
   opacity 0.75 시점 — 이동 1.32px
   opacity 0.50 시점 — 이동 **3.08px**   (나가는 구간의 34% 시점에 벌써 반투명)
   opacity 0.25 시점 — 이동 4.36px
   총 이동 5.97px
★ 원인은 두 겹이다
  ① 거리가 6px — 들어올 때(10px)의 60%뿐
  ② opacity와 transform에 **같은 곡선**이 걸린다. ease는 앞이 빠른 곡선이라
    움직임이 쌓이기 전에 먼저 사라진다 → 눈에는 페이드로만 남는다
```

**✅ 고친 값과 실측**

```
거리    6px → **10px** (들어온 거리와 같게)
곡선    ease의 **정확한 거울** — (1−x2,1−y2,1−x1,1−y1) = cubic-bezier(.75,0,.75,.9)
       ⛔ 손으로 뒤집지 말 것. 위 식이 유일한 근거다
       ★ 들어올 때 빠르게 들어와 부드럽게 앉으니, 나갈 때는 부드럽게 떠서 빠르게 사라진다
시간    들어옴 0.7s → **0.45s** · 나감도 **0.45s** (대칭)
머무름   2.6초 → **4.6초** · 총 노출 **5.05초** (정지 구간 4.15초)
구조    hide는 TOAST_MS에, 제거는 TOAST_MS + TOAST_OUT_MS에 — **더하는 구조 유지**

[세 갈래 실측 — 나가는 구간]
갈래     총 노출    총 이동      opacity 0.75    0.50         0.25
C 폰     5039ms   9.38px     263ms 2.55px   346ms 5.65px  395ms 8.12px
A 태블릿  5045ms   8.80px     291ms 2.71px   360ms 5.12px  408ms 7.84px
B 에뮬   5051ms   9.41px     264ms 2.59px   352ms 5.71px  401ms 8.18px
★ 핵심 — opacity 0.5 시점의 이동이 **3.08 → 5.7px**이고, 그 시점이
  나가는 구간의 34% → **78%** 로 밀렸다. 보이는 동안 더 오래, 더 멀리 움직인다
✅ 갈래 A의 정지 top이 36(= env 24 + 12)으로 나온다 — env 하나로 세 갈래가 맞는다는
  2.119의 결론이 이 빌드에서도 유지된다
```

**★ .rise(0.7s)와 갈라도 되는가 — 갈라도 된다**

```
.rise    **화면 진입**이다. 콘텐츠가 자리를 잡는 움직임이고 사용자는 그 화면에 머문다.
        느린 것이 이 앱의 결(호흡 10초)과 맞는다
토스트    **알림**이다. 방금 누른 것에 대한 응답이라 늦으면 "눌렸나?"를 의심하게 된다
★ 차분함은 **머무는 시간**(4.6초)이 만들고, 등장은 응답성이 만든다 —
  역할이 갈리므로 값이 갈리는 것이 맞다
⚠ 대신 **곡선은 그대로 뒀다**(ease). 속도만 다르고 성격은 안 다르게 한다
```

**★ 코드 자리 — 값을 고치려면 여기다**

```
app/src/App.jsx
  TOAST_TEXT      "정해진 시각에 구절 한 절을 보내드릴게요"   성공
  TOAST_BLOCKED   "기기 설정에서 알림을 켜 주세요"            영구 거절
  TOAST_MS        4600     떠 있는 시간
  TOAST_IN_MS     450      들어오는 시간
  TOAST_OUT_MS    450      나가는 시간  ★ 들어오는 것과 **같다**(대칭)
  TOAST_OUT_EASE  "cubic-bezier(.75,0,.75,.9)"   ease의 거울
  @keyframes toastIn / toastOut   둘 다 **10px** — 거리가 같아야 대칭이다
  toggleNotify    실패 → setNotifyOn(false) → isPermissionBlocked() → 토스트
app/src/lib/notifyPermission.js   isBlocked()          순수 판정
app/src/lib/notify.js             isPermissionBlocked() 플러그인 조회
⛔ 총 노출은 **파생값이다** — TOAST_MS + TOAST_OUT_MS. 따로 적힌 상수가 없다
```

**⛔ 회귀가 못 박은 것 — 이것을 어기면 테스트가 선다 (284 → 293건)**

> ⚠ **293은 이 절 시점의 수다.** 다음 날 2.122가 다섯을 더해 **314건**이 됐다.

```
app/test/adsApp.test.js
  · toastOut의 거리가 toastIn과 같다(둘 다 10px)
  · TOAST_IN_MS == TOAST_OUT_MS == 450
  · TOAST_OUT_EASE가 ease의 거울값 그대로다
  · 들어오는 곡선이 ease다 (결이 갈리지 않게)
  · TOAST_MS == 4600 이고, **더하는 구조**가 살아 있다 —
    hide는 TOAST_MS에, 제거는 TOAST_MS + TOAST_OUT_MS에
  · 안내 판정이 **켜기 실패 뒤**에 있다 (소스 위치 순서로 단언)
  · 안내 문구가 확정값 그대로다
  · ⛔ **설정 화면으로 보내는 코드가 없다** (APP_NOTIFICATION_SETTINGS·openSettings)
  · 실패하면 토글을 **먼저** 되돌린다
app/test/notifyPermission.test.js
  · isBlocked는 정확히 "denied"만 true다
  · prompt · prompt-with-rationale · granted는 false다
  · 못 읽으면(throw·null·문자열·이상한 값) false다 — 모르면 말하지 않는다
  · ★ blocked가 outcome과 **다른 축**임을 값으로 단언한다
  · notify.js가 display를 직접 비교하지 않는다 (판정이 한 곳이다)
★ 세 갈래 실측값(5039~5051ms)은 **회귀가 못 박지 않는다** — 기기·프레임 사정이
  섞이는 값이라 단언하면 깨진다. 회귀는 **상수와 구조만** 본다
```

**⚠ 실측 도구 — 저장소에 안 넣었다**

```
방법     웹뷰에 rAF 표본기를 심는다(CDP Runtime.evaluate). 매 프레임의
        opacity·top·animationName을 모은다. ⛔ 화면 녹화보다 정확하다
누르기   CDP Input.dispatchTouchEvent. ⛔ element.click()과 다르다(히트 테스트를 지난다).
        ⚠ 그래도 **손가락 검증이 아니다** — 재는 것은 움직임이지 터치가 아니다(함정 ①)
프레임 띠  adb screenrecord → ffmpeg → 토스트가 건드린 행·열만 잘라 이어 붙인다
⚠ **에뮬레이터 screenrecord는 5fps다.** 450ms 움직임을 못 담는다 —
  갈래 B의 프레임 띠는 만들 수 없다. CDP Page.startScreencast도 4fps였다.
  ★ 갈래 B는 수치로만 확인한다. 실기기 둘은 45fps·23fps로 충분하다
⛔ CDP로 앱 페이지를 잡을 때 **광고 웹뷰가 같은 프로세스에 여러 개** 있다.
  /json/list에서 url에 localhost가 든 page를 골라야 한다 — 첫 page는 광고다
```

---

### 2.121 ✅ ads.txt **「승인됨」** — 기다린 것이 맞았다 (2026-09-03 조사 · 09-04 확인)

> ⛔ 그 저장소(appleap385-aaz0919.github.io)를 이번에도 **건드리지 않았다.** 읽기만 했다.

**★ 사용자의 걱정에 대한 답 — 문서상 별개다**

```
✅ 사이트 상태 문서는 「승인 상태」와 「ads.txt 상태」를 **다른 절**로 둔다.
   ads.txt가 승인의 전제라는 문장은 **없다**
✅ ads.txt 가이드 원문 — "ads.txt를 반드시 사용해야 하는 것은 아니지만,
   사용을 적극 권장합니다"  → **필수가 아니다**
⚠ 다만 ads.txt 상태가 「승인되지 않음」(파일은 있는데 우리 퍼블리셔 ID가 없음)이
  되면 **광고 요청이 거부된다.** 지금은 그 상태가 아니다 — 「찾을 수 없음」이다
★ 즉 지금 상태가 승인을 막고 있다는 근거는 문서에 없다
```

**① 승인 조건과 ads.txt의 관계 — 별개 심사다** (위)

**② 구글 크롤러가 실제로 왔는지 확인할 방법 — ⛔ 없다**

```
GitHub Pages는 **접근 로그를 주지 않는다.** 서버 로그도, 크롤러 방문 기록도 없다.
→ 우리가 볼 수 있는 유일한 신호는 **AdSense 콘솔의 상태 값** 자체다
⛔ 그래서 "크롤러가 왔는데 못 읽었다"와 "아직 안 왔다"를 우리 쪽에서 가를 수 없다
```

**③ GitHub Pages가 User-Agent를 다르게 다루는가 — ⛔ 아니다 (실측)**

```
같은 URL을 네 UA로 받아 비교했다
  Mediapartners-Google · Googlebot · AdsBot-Google · curl
→ 넷 다 **200 · text/plain; charset=utf-8 · 59바이트 · 같은 ETag("6a8d500d-3b")**
  · 같은 Last-Modified(2026-08-25 08:19 GMT) · 같은 본문
★ UA 가설은 죽었다. 우리 쪽 서빙에 갈래가 없다
```

**④ 재확인 주기 — 문서에 주기가 없다. 대신 조건이 있다**

```
원문   "변경사항이 애드센스에 반영되는 데 며칠이 걸릴 수 있습니다.
       사이트에서 **광고 요청을 많이 하지 않는 경우 최대 한 달**이 걸릴 수도 있습니다"
★★ 이것이 우리 사정과 정확히 맞는다 — **우리 사이트는 광고 요청이 0건이다.**
   AD_SLOT이 비어 있어 번들러가 광고 가지를 통째로 접는다(2.55 · 배포본 JS에
   adsbygoogle 0건 실측). 즉 크롤러가 우리를 자주 볼 이유가 구조적으로 없다
⚠ 「확인됨」→「찾을 수 없음」으로 **되돌아간 것**에 대한 설명은 문서에 없다.
   확인에 유효기간이 있고 광고 요청이 없으면 갱신이 안 되는 구조라면 설명이 되지만
   ⛔ **추정이다.** 문서에 그런 문장은 없다
⛔ **「업데이트 확인」 버튼은 우리 콘솔에 없다** (2026-09-03 · 사용자가 화면을 봤다).
  도움말이 그 버튼을 말하지만 **사이트 상세 화면에 나타나지 않는다.**
  ★ 사용자 관찰 — 소유권 확인 방식이 **「애드센스 코드 스니펫」**으로 되어 있어서로
    보인다. 그 버튼은 **「Ads.txt 스니펫」** 방식을 골랐을 때 나타나는 것 같다
  ⚠ 추정이다. 문서가 방식별 차이를 말하지 않는다
  ⛔ **그래서 수동 재확인을 요청할 방법이 없다.** 자동 크롤을 기다리는 수밖에 없다
```

**⑤ 열흘 넘게 「준비 중」인 것 — 아직 정상 범위 안이다**

```
원문   "사이트를 확인 중입니다. 검토 절차는 일반적으로 며칠 이내에 완료되지만,
       경우에 따라 **2~4주**가 소요될 수 있습니다"
08-24 등록 → 09-03 = **10일.** 문서가 말하는 상한(4주)의 절반도 안 지났다
⛔ 지금 시점에 "막혔다"고 볼 근거가 없다
```

#### ✅✅ 결과 — **「승인됨」으로 바뀌었다** (2026-09-04 · 사용자가 콘솔에서 확인)

```
★ 기다린 것이 맞았다. 손대지 않고 둔 것이 답이었다
★ **소유권 확인 방식을 안 바꾼 판단도 결과적으로 맞았다.** 「Ads.txt 스니펫」으로
  바꿔 「업데이트 확인」 버튼을 만들려던 유혹을 눌렀는데, 바꿨다면 이미 통과한
  초록 체크를 흔들면서 **아무 이득도 없었을 것**이다
✅ **우리 쪽 파일에 흠이 없다는 실측이 확인됐다** (2.116 ③ · 아래 ③).
  200 · text/plain · BOM 없음 · LF · 레포본과 바이트 동일 · UA 넷이 같은 응답

⛔⛔ **apex(github.io) 가설을 닫는다.**
  파일도 URL도 서빙도 **하나도 안 바꿨는데 상태만 바뀌었다.**
  → 도메인 문제가 아니었다. github.io가 Public Suffix List에 있어 우리 위치가
    규격상 옳다는 2.55 ⑤의 전제가 처음부터 맞았다.
  ⛔ 다음에 같은 증상을 봐도 이 가설로 돌아가지 말 것

⚠ **무엇이 바뀌어서 승인됐는지는 모른다.**
  우리가 한 일이 없으므로 구글 쪽에서 일어난 일이다.
  ★ **재확인 주기가 돌았을 가능성이 가장 크다** — 문서가 "며칠 ~ 광고 요청이
    적으면 최대 한 달"이라 말하고, 우리는 광고 요청이 0건이다. 08-24 등록
    기준으로 열흘 남짓이 지난 시점이다.
  ⛔ **추정이다.** 확인할 방법이 없다 — GitHub Pages가 접근 로그를 안 준다

⚠ **「업데이트 확인」 버튼을 끝내 못 찾았다**(2026-09-03 · 사용자가 화면을 봤다).
  도움말은 그 버튼을 말하는데 사이트 상세 화면에 나타나지 않았다.
  ★ 소유권 확인 방식이 **「애드센스 코드 스니펫」**이라 그 절에 안 나타났던 것으로
    보인다 — 그 버튼은 **「Ads.txt 스니펫」** 방식에서 나오는 것 같다.
  ⛔ **추정이다.** 문서가 방식별 차이를 말하지 않는다.
  ★ 그리고 **없어도 됐다.** 수동 재확인 없이 자동 크롤로 풀렸다

⛔ **「동의 메시지 만들기」(CMP)에 손대지 않는다.**
  사이트 상세에 주의 표시로 떠 있다. ⚠ **유럽 출시를 결정할 때** 볼 항목으로
  이미 보류했다 — 주의 표시가 떠 있다는 이유로 처리하지 말 것

🔴 웹 쪽에 남은 것은 **사이트 승인 하나**다 (아래 ⑤)
```

#### (이력) 결정 — 기다린다 (2026-09-03 · 사용자 확정)

> ✅ 위에서 결과가 나왔다. 아래는 그때의 판단 근거다.

```
왜 기다리나
  ① ads.txt는 **승인을 막는 조건이 아니다.** 문서 근거가 있다(위 표·원문 인용)
  ② 우리 사이트는 **광고 요청이 0건**이라 재확인이 **최대 한 달** 걸릴 수 있다.
     문서가 그 조건을 명시한다 — 늦는 것이 이상 징후가 아니다
  ③ 수동 재확인을 **요청할 수단이 없다**(위 「업데이트 확인」 항목)
  ④ 리뷰 요청 **2026-08-24 17:07**. 화면 안내가 "며칠 ~ 2~4주"다.
     → 09-21경까지는 **범위 안**이다

⛔⛔ **소유권 확인 방식을 바꾸지 않는다** (사용자 확정)
  지금 **초록 체크로 통과한 상태**다(애드센스 코드 스니펫).
  「Ads.txt 스니펫」으로 바꾸면 「업데이트 확인」 버튼이 생길지 모르나,
  ★ **통과해 있는 확인이 풀릴 위험**이 있다. 얻는 것보다 잃을 것이 크다.
  ⛔ 다음 세션이 "버튼을 만들려고" 방식을 바꾸지 말 것. 이것이 그 결정이다

⛔ **「동의 메시지 만들기」(CMP)에 손대지 않는다**
  사이트 상세에 주의 표시로 떠 있다. ⚠ 이것은 **유럽 출시를 결정할 때** 보기로
  이미 보류한 항목이다 — 지금 건드릴 항목이 아니다. 주의 표시가 떠 있다는 이유로
  처리하지 말 것

🔴 다시 볼 시점
  · 승인이 **09-21경(4주)** 를 넘기면 그때 본다
  · 그때도 「찾을 수 없음」이면 **우리 쪽에 잴 것이 남아 있지 않다** —
    AdSense 지원 문의가 남은 길이다
⛔ 그 저장소(appleap385-aaz0919.github.io)를 건드리지 말 것. 읽기만 한다
```

**출처**

```
사이트 상태 확인      support.google.com/adsense/answer/12170222
ads.txt 문제 해결     support.google.com/adsense/answer/12171244
ads.txt 가이드        support.google.com/adsense/answer/12171612
```


### 2.122 ⛔⛔ 모바일 웹에 **동작하지 않는 토글**이 나갔다 — 검사의 방향이 한쪽뿐이었다 (2026-09-04)

> ★ 사용자가 모바일 웹에서 발견했다. 세션은 앱만 보고 있었다.

#### ⛔ 무엇이 있었나

```
증상    모바일 웹 결과 화면에 「구절 알림」 토글이 보이고, **눌러도 아무 일이 없다**
원인    2.119에서 탭 행에 토글을 얹으면서 `__IS_APP__` 분기가 빠졌다.
       About에는 있었고(About.jsx `{IS_APP ? <NotifySettings /> : null}`)
       **새로 만든 자리에는 없었다**
왜 죽나  웹에는 로컬 알림이 없다. 플러그인이 없으니 notify.js의 api()가 null을
       돌려주고 setEnabled가 조용히 false를 준다 → 토글이 되돌아간다.
       ⛔ 오류도 안 나고 화면에 아무 표시도 없다 — **조용한 실패**다
배포    2026-09-03 07:25:12Z (run 77 · fece9fd)에 처음 나갔다.
       ⚠ 약 18시간 나가 있었다. 그다음 배포(run 78 · d8e69b0)에도 그대로 있었다
확인법   배포본 번들을 받아 문자열을 셌다 —
       role:"switch" 1 · aria-checked 1 · "기기 설정에서 알림을 켜" 1
```

#### ★★ 왜 게이트 293건이 **전부 통과했나** — 이것이 이 절의 핵심이다

```
검사의 종류가 셋인데, **셋 다 이 결함을 볼 수 없는 종류**였다.

① 순수 함수 검사   값만 본다. **무엇이 그려지는지 모른다**
② 소스 모양 검사   "그 파일에 조건이 있나"를 본다.
                 ⛔ 결함은 **조건이 없는 새 파일**에 있었다. 검사 대상이 아니면
                   검사는 아무 말도 하지 않는다 — 이것이 구조적 사각이다
③ 산출물 검사     **앱 쪽에만** 있었다(앱 번들에 AdSense가 남았나 · vite closeBundle).
                 ⛔ **웹 쪽 산출물 검사는 아예 없었다**

★★ 즉 「웹 전용이 앱으로 새는」 방향만 있었고 **「앱 전용이 웹으로 새는」 방향은
   통째로 무방비였다.** 결함이 아니라 **검사의 비대칭**이 근본 원인이다.
⚠ 이 유형은 전에도 있었다 — 2.102(환경변수를 빠뜨려도 게이트가 다 통과),
  behavior.test.js의 "세 번째 경로"(소스 검사가 통과한 채 경로가 고장). 세 번째다.
```

#### ✅ 고친 것 — About과 **같은 조건**을 두 겹으로

```
① 주는 쪽   App.jsx
   const notify = IS_APP ? { on: notifyOn, onToggle: toggleNotify } : null;
   ★ ResultTabs의 계약이 이미 "notify를 안 주면 안 그린다"였다. 막는 자리는
     **그리는 쪽이 아니라 주는 쪽**이다 — 넘겨받은 prop을 몰래 무시하게 만들지 않는다
② 그리는 쪽 ResultTabs.jsx
   {IS_APP && notify ? (
   ★★ ②가 왜 **또** 필요한가 — `notify`만으로 막으면 그것은 **런타임 조건**이라
     번들러가 마크업을 못 접는다. 실측으로 확인했다:
       ①만 넣었을 때  웹 산출물 role:"switch" **1** · notifyCell **2**  ← 남는다
       ①+②           웹 산출물 role:"switch" **0** · notifyCell **0**
     About이 컴파일 시점 상수로 접는 것과 같은 모양이어야 한다
⛔ 새 조건을 만들지 않았다. 세 파일이 **글자까지 같은 한 줄**을 쓴다 —
   const IS_APP = typeof __IS_APP__ === "boolean" ? __IS_APP__ : false;
   ⛔ window.Capacitor·isNativePlatform 같은 런타임 판단을 쓰지 않는다.
     그러면 빌드가 아니라 실행 시점에 갈려 번들러가 아무것도 못 접는다(회귀가 막는다)

✅ **토스트도 함께 사라졌다.** setToast를 부르는 자리가 toggleNotify 하나뿐이고,
   그것이 웹에서 닿지 않는다. 웹 산출물에서 토스트 문구 둘 다 **0건**이다
⚠ @keyframes toastIn/toastOut은 웹 산출물에 남는다(2건씩). Shell의 <style> 문자열이라
   항상 나간다 — **CSS일 뿐 UI가 아니다.** 이것을 보고 "토스트가 샜다"고 읽지 말 것
✅ **앱 번들은 바이트 동일하다.** IS_APP이 true라 두 조건이 접혀 사라진다 —
   sync 뒤 gradle이 156개 태스크를 **전부 up-to-date**로 건너뛴 것이 그 증거다.
   ⛔ 그래서 기기 재설치가 필요 없었다. 앱 동작은 손대지 않았다
```

#### ✅ 만든 것 — 웹 산출물 가드 (방향을 대칭으로)

```
자리    app/vite.config.js  webBuildGuards()  — 웹 빌드에만 붙는다
        plugins: [react(), IS_APP ? appBuildGuards() : webBuildGuards()]
검사    dist/ 의 .html/.js/.css/.webmanifest 를 훑어 **앱 전용 표식**을 찾는다
표식    role:"switch"                          이 앱의 스위치는 알림 토글 둘뿐이다
        "aria-label":"구절 알림"                 결과 화면 토글
        "정해진 시각에 구절 한 절을 보내드릴게요"   성공 토스트
        "기기 설정에서 알림을 켜 주세요"           영구 거절 안내 토스트
        ⚠ 표식은 **산출물에서 확인한 모양**이다. 소스 모양을 넣으면 번들러가 바꾼 뒤
          안 걸리고, 그때는 조용히 통과한다
⛔ **양성 대조** — MUST_EXIST("이어 읽기")를 못 찾으면 빌드를 세운다.
   가드가 있는 것처럼 보이면서 아무것도 안 보는 것이 가장 나쁜 결과다
   (2026-08-31에 그렇게 당했다 — "가드가 가려지는 것은 가드가 없는 것보다 나쁘다")

✅ 역검증 — 셋 다 실제로 세웠다
   ① ResultTabs 게이트를 빼면  → "웹 번들에 앱 전용 요소가 남아 있습니다"로 빌드 정지
   ② MUST_EXIST를 깨뜨리면     → "웹 가드가 산출물을 못 읽었습니다"로 빌드 정지
   ③ 회귀는 게이트 하나를 뺄 때마다 **3건씩** 실패한다
⚠ CI가 이것을 지난다 — deploy-app.yml이 `npm test`와 `npm run build`(웹)를 둘 다 돌린다
```

#### ⏸ 남은 같은 유형 하나 — **고치지 않았다. 판단이 필요하다**

```
대상   DailyVerse (알림을 눌러 열리는 구절 화면)
상태   ⚠ 웹 산출물에 **마크업이 남아 있다** ("오늘의 구절" 2건 · "지금 마음을 적어볼까요" 1건)
왜 결함이 아닌가   웹에서 **닿을 수 없다.** setDailyVerse를 부르는 자리가
                listenForTaps 콜백 하나뿐이고, 웹에서는 api()가 null이라
                리스너가 아예 등록되지 않는다. 그려질 길이 없다
⚠ 그러나 **같은 구멍이다** — 런타임으로만 갈리므로, 나중에 dailyVerse를 세팅하는
  길이 하나 더 생기면(예: URL 파라미터) 그날 웹에 그려진다
안   ResultTabs와 같이 `IS_APP &&`를 한 줄 더해 컴파일 시점에 접는다.
     ⛔ 지금 보이는 결함이 없으므로 **사용자 판단을 기다린다.**
     ⚠ 하면 웹 번들이 조금 줄고, 위 표식 목록에 두 문구를 더할 수 있다
```

#### ⚠ 배포 — 아직 사용자에게 반영되지 않았다

```
고침 커밋   484ab01 (2026-09-04) · main에 push 완료
⏱ 배포     deploy-app.yml이 push에 걸려 돈다. ⚠ Actions 지연이 21분~10시간이다(2.78)
⛔ **gh-pages에 반영될 때까지 모바일 웹에는 결함이 남아 있다.**
   확인법   curl -s https://appleap385-aaz0919.github.io/PeaceYourMind/ 로 번들 이름을 얻고
           그 .js에서 role:"switch" 가 **0건**이면 반영된 것이다
```


### 2.123 ✅ Play 콘솔이 풀렸다 — 앱을 만들었고 **A2가 닫혔다** (2026-09-04)

> ⚠ 이 절은 오전에 「막혔다」로 적혔다가 **같은 날 풀렸다.** 아래가 최신이다.

#### (이력) 막혀 있던 것 — 계정 확인이 순환했다

```
증상   콘솔 홈에 남은 작업이 「연락처 전화번호 인증」 하나인데 **비활성**이고,
      그 페이지는 "다른 인증을 먼저 하라", 개발자 인증 페이지는 "홈에서 하라" —
      **세 화면이 서로를 가리켰다.** 앱 만들기 버튼도 비활성이었다
대응   화면 캡처와 함께 지원팀에 문의했다
✅ 결과  **전화번호 인증이 통과됐다.** 순환이 풀렸다
⚠ 무엇이 풀었는지는 기록해 두지 않았다 — 지원팀 조치인지 시간이 지나 풀린 것인지
  모른다. ⛔ 같은 증상을 다시 보면 **먼저 기다려 보는 것**도 선택지다
```

#### ✅ 앱을 만들었다

```
앱 이름     Peace in Mind
패키지      io.github.appleap385.peaceinmind
계정        895495136242283982O · 개인 계정
```

#### ✅✅ A2가 닫혔다 — **우리가 고를 것이 없었다**

```
앱 서명 키   **「사용 중」**이고 **Google이 이미 생성해 뒀다.**
           → 2026-08-31의 「판단 1」(Google 생성)이 **자동으로 실현됐다**
Play 앱 서명  **기본 적용**이라는 것이 콘솔 화면으로 확인됐다
           ⛔ 이것으로 **2.94의 미확인 전제가 닫힌다** —
             "2021-08 이후 신규 앱은 AAB 필수 + Play 앱 서명 필수"라는
             세션의 조사 결과가 맞았다.
           ★ 맞았지만, **확인한 뒤에야 전제로 쓴다**는 규칙(9193행)이 값을 했다
🔴 업로드 키  **인증서 절이 비어 있다** —
           "첫 App Bundle을 업로드하면 여기에 인증서 지문이 표시됩니다"
           → **우리가 만들어 첫 AAB에 서명해 올리면 등록되는 구조**다.
             콘솔에서 미리 등록하는 절차가 아니다. **업로드가 곧 등록**이다
           → 설계와 구현은 **2.125**에 있다
```

#### ✅ 앱 콘텐츠 선언 — **전부 끝났다**

```
개인정보처리방침   제출
로그인 세부정보    제한 없음
광고             **포함**
콘텐츠 등급       전 지역 **전체이용가** · 설명어 없음
타겟층           **만 18세 이상만** · 미성년자 차단 안 함
데이터 안전       기기 또는 기타 ID **수집·공유** · 목적 **광고** · **필수** ·
                임시 아님 · **암호화 전송** · 계정 생성 없음
광고 ID 선언      **사용함** · 광고 목적
건강·금융·정부 앱  모두 해당 없음
```

#### ⚠ 이메일이 둘이다 — **가르는 이유가 있다**

```
스토어 등록정보 연락처   **appleap385@gmail.com**   ← 사용자에게 **보이는** 주소다
계정 연락처 · IARC      **aaz0919@gmail.com**       ← 사용자에게 **안 보인다**
★ 그래서 그대로 둔다. 하나로 합치지 않는다 —
  보이는 주소는 앱의 문의처(About·2.56에서 확정한 값)와 같아야 하고,
  계정 주소는 구글이 우리에게 연락하는 통로다. 역할이 다르다
⛔ "이메일이 두 개라 헷갈린다"는 이유로 통일하지 말 것. 위 구분이 그 답이다
```

#### ⏸ 남은 것 둘

```
① **스토어 등록정보**   카테고리 · 태그 · 연락처 · 본문(짧은/전체 설명) · 이미지
② **첫 AAB 업로드**     ⛔ 업로드 키가 있어야 한다 (2.125)
```

---

### 2.124 ✅ AdMob 상태 — 「검토 필요」는 **정상이다** (2026-09-04)

> ⚠ 이 표시를 결함으로 읽지 말 것. 스토어에 없으니 못 채우는 요건일 뿐이다.

```
앱 상태     **「검토 필요」**
왜 정상인가  준비성 요건 셋이 **스토어 등재를 전제로** 한다.
           우리 앱은 아직 Play에 없으므로(2.123) 채울 수가 없다.
           → **Play 출시 뒤에 자동으로 풀리는 항목**이다
실적       시험 요청 **3건** · 노출 **3건** · 수입 **0**
           ★ 세션이 배너를 확인하며 만든 것이다. 시험 기기 등록(2.117)이
             되어 있어 「테스트 광고」로 나갔고, 그래서 수입이 0인 것이 맞다
           ⛔ 실적이 0이 아니라고 놀라지 말 것 — 무효 트래픽이 아니라 시험이다

⛔ **Firebase 연결은 하지 않는다** (사용자 확정).
  근거 — 사용자 측정항목과 **같은 계열**이다. 이 앱은 기기 안에서만 동작하고
  아무것도 전송하지 않는다는 것이 방침이고(About 문구·2.51), 분석 SDK를 넣으면
  그 문장이 거짓이 된다.
  ⚠ AdMob 콘솔이 계속 권할 것이다. 권한다고 하지 말 것 — **이미 정한 것이다**
```


### 2.125 ✅ 업로드 키 — **만들었고 서명된 AAB가 나온다** (2026-09-04)

> ✅ 설계 승인 → 설정·게이트 → **사용자가 키 생성** → 서명 확인까지 같은 날 끝났다.
> ⛔ 비밀번호는 사용자만 안다. 세션은 받은 적이 없다.

#### ⛔⛔ 가장 중요한 것 — 키도 비밀번호도 **저장소 밖**이다

```
⛔ admob.properties와 **다르게 간다.** 그것은 저장소 안(gitignore)에 있다.
★ 사용자 결정 (2026-09-04) — 비밀번호가 든 파일이면 **성격이 키와 같다.**
  저장소 안에 두는 이유가 "gradle이 찾기 쉬워서"뿐이라면 그 편의보다 위험이 크다.
근거 셋
  · 이 저장소는 **공개**다. 새면 남이 우리 이름으로 AAB를 올릴 수 있다
  · .gitignore는 실수 하나로 뚫린다 — **2026-09-03에 admob.properties.bak 이
    정확한 경로 무늬를 뚫는 것을 실제로 봤다**(2.117)
  · 폴더 통째 백업·압축 전송에는 .gitignore가 아무 힘이 없다
```

#### ★ 경로를 어디에 적는가 — **Gradle 사용자 홈**. 이것도 저장소 밖이다

```
파일   %GRADLE_USER_HOME%/gradle.properties
      ⚠ 이 기기는 GRADLE_USER_HOME이 이미 D로 잡혀 있다 →
        D:/jaehyuk.myung/android/gradle/gradle.properties
내용   pymKeystoreProperties=<저장소 밖 경로>/pym-upload.properties
★ 그 파일에는 **경로만** 있다. ⛔ 비밀번호를 거기 적지 않는다 —
  흔한 관행이지만 그러면 비밀이 두 곳으로 흩어지고 백업 대상이 는다

[⛔ 왜 환경변수가 아닌가]
  이 저장소는 2026-09-02에 ADMOB_BANNER_ID를 **환경변수에서 파일로 옮겼다**(2.102).
  빠뜨리면 조용히 깨졌기 때문이다. 같은 실수를 되풀이하지 않는다.
[⛔ 왜 저장소 안의 포인터 파일이 아닌가]
  "밖으로 뺀다"는 결정과 어긋난다 — 파일 하나가 다시 안으로 들어온다.
[★ 왜 이것인가]
  Gradle 자신의 표준 기제다. 새로 발명하지 않고, 셸이든 Android Studio든
  **같게 읽힌다.** ⚠ -PpymKeystoreProperties=... 로도 넘어가서 나중에 CI가
  필요해지면 길이 있다 — 지금은 쓰지 않는다
```

#### ✅ 키를 만드는 명령 (확정값 · ⛔ 아직 실행하지 않았다)

```bash
keytool -genkeypair -v \
  -storetype PKCS12 \
  -keystore <저장소 밖 경로>/pym-upload.jks \
  -alias pym-upload \
  -keyalg RSA -keysize 2048 -sigalg SHA256withRSA \
  -validity 10000 \
  -dname "CN=Peace in Mind, OU=Upload Key, O=appleap385, C=KR"
```

```
별칭      pym-upload      저장소의 pym 접두사와 같은 결. ⚠ 비밀이 아니다
형식      PKCS12          이 JDK의 기본값이다(java.security:286 keystore.type=pkcs12).
                         ⛔ JKS를 강요하면 keytool이 마이그레이션 경고를 낸다
알고리즘   RSA 2048        ★ 업로드 키는 **교체 가능한** 키다. 암호학적 수명을 늘려도
                         얻는 것이 적고, 구글 문서 예시와 같아 도구 사고가 가장 적다
서명       SHA256withRSA   JDK 21 기본값과 같지만 **명시한다** — 기본이 바뀌어도
                         문서와 실물이 안 갈린다
유효기간   10000일         ≈2054년. Play는 **2033-10-22 이후**까지 유효할 것을 요구한다
DN        위 한 줄        ⚠ 업로드 인증서는 **사용자에게 배포되지 않는다**(기기의 앱은
                         Google 키로 서명된다). 실명·주소를 넣을 이유가 없다.
                         ★ -dname으로 박으면 대화형 질문이 사라져 절차가 재현된다
⛔ -storepass 를 명령줄에 쓰지 않는다 — 셸 기록에 남는다
```

#### ⛔ 비밀번호 조건 (값은 사용자가 정한다 · 세션은 모른다)

```
★ store와 key 비밀번호를 **같게** 한다 — PKCS12는 둘을 가르는 것을 잘 다루지 않고,
  같게 두면 지켜야 할 비밀이 셋에서 **둘**로 준다 (13절 1항 정정 참조)
· 20자 이상 무작위(암호 관리자 생성) 또는 4~5단어 패스프레이즈
· ⛔ 다른 곳에서 쓰는 비밀번호를 **재사용하지 않는다**
· ⚠ **ASCII만.** .properties 문법에서 `\` 는 이스케이프, 줄 첫머리 `#` 은 주석,
  `=` `:` 는 구분자다. 안전한 문자집합 — 영문 대소문자 + 숫자 + - _ . @ ! * + ? ~
· ⚠ 앞뒤 공백을 두지 않는다 — gradle이 비밀번호를 **trim하지 않는다**(의도적이다.
  공백도 비밀번호의 일부이므로 가공하면 원인 모를 서명 실패가 된다)
```

#### ✅ 넣은 것 — 설정·게이트 (키 없이도 안전하게 돈다)

```
.gitignore              *.jks · *.keystore · *.p12 · *.pem · keystore.properties*
                        + !app/android/keystore.properties.example
                        ★ 파일은 밖에 있으므로 이것은 **이중 방어**다 —
                          실수로 안에 복사하는 순간을 잡는 것이 목적이다.
                        ⛔ 이것이 유일한 방어라고 믿지 말 것
keystore.properties.example   견본. 형식 · 경고 · keytool 명령 · 비밀번호 조건.
                        ⛔ 값은 <<CHANGE_ME>> 다
app/build.gradle        signingConfigs.release + buildTypes.release.signingConfig
                        + verifyReleaseSigning 태스크
```

#### ★★ 키가 없을 때 — **debug는 돌고 release는 선다**

```
⛔⛔ **configuration 시점에 throw하면 debug까지 죽는다.**
   admob.properties는 그래도 된다 — App ID는 debug에도 필요한 값이라서다.
   업로드 키는 **릴리스에만** 필요하다. 세 기기의 일상 시험이 debug이고,
   새로 clone한 사람이 debug를 못 만들면 개발 자체가 막힌다.
★ 그래서 **task 시점**에 세운다 — verifyNoDemoAdIds(2.99)가 쓰는 관용 그대로다.
   tasks.matching { assembleRelease · bundleRelease }.configureEach {
       dependsOn verifyNoDemoAdIds, verifyReleaseSigning }
   ⛔ **bundleRelease를 함께** 건다. Play가 받는 것은 AAB다
⛔ 준비가 안 되면 signingConfig는 **null**이다. 빈 config를 물리면 AGP가
   "storeFile not set"으로 죽는데 그 메시지는 무엇을 하라고 말해 주지 않는다.
   우리 게이트가 먼저 말하게 둔다

게이트가 보는 것 넷
  [1] 포인터(gradle property)가 있는가        [2] 그 파일이 실재하는가
  [3] 값 넷이 채워졌는가 · 견본값이 아닌가     [4] storeFile 경로에 키가 실재하는가
⚠ 비밀번호가 틀린 경우는 검사하지 않는다 — **AGP가 시끄럽게 실패한다.**
  조용히 지나가지 않으므로 우리가 볼 이유가 없다
```

**✅ 역검증 — 다섯 갈래를 실제로 확인했다 (2026-09-04)**

```
① 키 없이 assembleDebug      ✅ **BUILD SUCCESSFUL** (156 tasks) — 그대로 돈다
② 키 없이 assembleRelease    ⛔ verifyReleaseSigning에서 정지 · [1] 안내
③ 없는 파일을 가리킬 때       ⛔ 정지 · [2] 안내 (bundleRelease로 확인 — AAB도 막힌다)
④ 견본값 그대로일 때          ⛔ 정지 · [4] "견본 값이 그대로입니다: storeFile,
                              storePassword, keyPassword"
⑤ 회귀 넷을 하나씩 깨뜨려     ⛔ 매번 3건씩 실패 (dependsOn 제거 · signingConfig
                              분기 제거 · .gitignore 무늬 제거)
회귀 314 → **318건**
```

#### ⛔⛔ 한글 경로 함정 — **두 파일의 규칙이 다르다** (2026-09-04 실측)

```
사용자의 보관 폴더가 **D:\jaehyuk.myung\바탕 화면\백업용\keys** 였다.
디스크의 실제 이름이 「바탕 화면」이다(Desktop이 아니다 — 확인했다).

pym-upload.properties 의 storeFile   ✅ **한글을 써도 된다**
    ⛔ 원래는 깨졌다. Properties.load(InputStream)이 **ISO-8859-1**로 읽어서다 —
      D:/jaehyuk.myung/바탕 화면/... → D:/jaehyuk.myung/ë°í íë©´/...
    ✅ build.gradle을 **withReader("UTF-8")** 로 고쳤다. 실측으로 한글이 살아났다
    ⚠ 비밀번호도 같은 경로로 읽힌다 — ASCII만 쓰기로 한 약속이 이제
      **안전망**이지 취향이 아니게 됐다

%GRADLE_USER_HOME%/gradle.properties 의 포인터   ⛔ **한글을 쓰면 깨진다**
    Gradle 자신이 ISO-8859-1로 읽는다. **우리가 못 고친다.** 실측했다
    ★ 그래서 **작업 폴더는 ASCII 경로**로 둔다. 한글 폴더는 백업본이 간다
    ⚠ 굳이 가리켜야 하면 \uXXXX 이스케이프뿐이다 —
      D:/jaehyuk.myung/\uBC14\uD0D5 \uD654\uBA74/\uBC31\uC5C5\uC6A9/keys/...
      읽기 어렵고 틀리기 쉽다. **마지막 수단이다**

★ 실패는 **조용하지 않다** — 게이트가 깨진 경로를 그대로 찍어 준다([2]·[5]).
  그래도 사람의 시간을 버리므로 처음부터 ASCII로 간다
⛔ admob.properties도 같은 방식(InputStream)으로 읽지만 값이 전부 ASCII ID라
  지금은 문제가 없다. ⚠ 거기에 한글이 들어갈 일이 생기면 같이 고칠 것
```

#### ✅✅ 키를 만들었다 — 실물 값 (2026-09-04)

```
별칭       pym-upload
생성       2026-09-04 12:19 KST
유효기간    **2054-01-20** (10000일)
           ✅ Play 요구(2033-10-22 이후)를 훌쩍 넘긴다
알고리즘    RSA 2048 · SHA256withRSA · PKCS12
DN         CN=Peace in Mind, OU=Upload Key, O=appleap385, C=KR
SHA-256    AF:23:0D:4D:25:95:89:61:95:F0:A9:AF:59:80:EE:2A:13:2E:77:AC:38:F8:E4:60:C3:28:FC:B7:8D:9E:DE:22
★ 지문은 **비밀이 아니다.** 나중에 "지금 쓰는 키가 그것인지"를 확인하는 값이다 —
  그래서 저장소에 둔다. Play 콘솔도 첫 업로드 뒤 같은 값을 보여줄 것이다

키 파일   D:/jaehyuk.myung/claude_demo/**Demo_29_PYM_backup**/keys/pym-upload.jks
값 파일   D:/jaehyuk.myung/claude_demo/**Demo_29_PYM_backup**/keys/pym-upload.properties
포인터    %GRADLE_USER_HOME%/gradle.properties 에 pymKeystoreProperties 한 줄
         (= D:/jaehyuk.myung/android/gradle/gradle.properties)
⛔ 비밀번호는 **사용자만 안다.** 값 파일에 직접 채웠고 세션은 받은 적이 없다
✅ 한글이 없는 경로라 ISO-8859-1 문제가 없다 (위 「한글 경로 함정」)
```

**⚠⚠ 폴더 이름이 저장소와 비슷하다 — 혼동 위험**

```
저장소     D:/jaehyuk.myung/claude_demo/**Demo_29_PYM**/
키         D:/jaehyuk.myung/claude_demo/**Demo_29_PYM_backup**/keys/
           ↑ `_backup` 다섯 글자 차이뿐이다
✅ 저장소 **밖**이 맞다 — 형제 폴더이고 git이 못 본다(확인했다)
⛔⛔ 그러나 **부모 폴더(claude_demo)를 통째로 압축·백업·전송하면 둘이 함께 나간다.**
   .gitignore가 아무 힘이 없는 자리이고, 이것이 지금 남은 가장 큰 유출 경로다
   ⚠ 저장소를 남에게 넘기거나 폴더째 올릴 일이 생기면 **Demo_29_PYM만** 다룬다
⚠ 경로를 손으로 적을 때 `_backup`을 빠뜨리면 게이트가 "[5] 키 파일이 없습니다"로
  세운다 — 조용히 틀리지는 않는다
```

**✅ 검증 — 서명된 AAB가 실제로 나왔다**

```
① 게이트 둘이 실행됐다      :app:verifyNoDemoAdIds · :app:verifyReleaseSigning
② 서명 태스크가 돌았다      :app:signReleaseBundle
③ BUILD SUCCESSFUL          196 tasks
④ 산출물                    app/build/outputs/bundle/release/**app-release.aab**
                           7,789,956 bytes · ⛔ unsigned가 아니다
⑤ AAB 서명 지문             keytool -printcert -jarfile 로 읽었다
                           SHA256: AF:23:0D:...:DE:22  → **사용자가 알려준 값과 일치**
                           소유자·발행자 = 위 DN · 서명 알고리즘 SHA256withRSA
⑥ APK도 같은 키             assembleRelease → **app-release.apk**(unsigned 아님)
                           apksigner verify --print-certs →
                           af230d4d2595896195f0a9af5980ee2a132e77ac38f8e460c328fcb78d9ede22
                           ⚠ APK는 keytool -printcert -jarfile 로 **안 읽힌다** —
                             v2/v3 서명이 APK Signing Block에 있어서다. apksigner를 쓴다
```

**⚠ 남아 있는 낡은 산출물 하나**

```
app/build/outputs/apk/release/app-release-unsigned.apk   (2026-09-02 19:38)
서명 설정을 넣기 **전에** 만들어진 것이다. 지금 assembleRelease는 app-release.apk를 낸다.
⚠ build/ 안이라 gitignore 대상이고 언제든 재생성되지만, **이름이 비슷해 잘못 올릴 수 있다.**
★ 지우는 것을 권한다 — 또는 ./gradlew clean 이 한 번에 치운다
⛔ 세션이 지우려 했으나 권한이 막혔다. 사용자가 지운다
```

#### 🔴 남은 것

```
· **첫 AAB 업로드** — 그것으로 콘솔의 업로드 키 인증서 절이 채워진다(2.123)
  ⚠ 업로드 뒤 콘솔이 보여주는 지문이 위 SHA-256과 같은지 **한 번 대조한다**
· 보관 — 13절 4항 · 백업 목록 ①-[1]
· ⛔ **Actions Secret에 키를 넣지 않는다.** 지금 필요 없고, 넣으면 다시 못 읽는다
```


### 2.126 ✅ 배너가 결과 화면을 벗어나도 안 사라졌다 — **원인 확정 · 고쳤다** (2026-09-04)

> ★ 2026-09-04 후반에 **닫혔다.** 재현 → 원인 확정 → 안 1 적용 → 재현 절차로 확인.
> ⚠ 아래 「진행 중」이던 서술은 원인이 확정된 뒤 다시 썼다.

#### ⛔ 증상

```
결과 화면을 한 번 거치면 배너가 **다른 화면에도 남는다**
  · About        본문("모든 영상의 저작권은…")을 가린다
  · 알림 구절 화면  장 본문 12절과 13절 사이에 끼어 가린다
⛔ **배경면(band)이 없다.** 웹은 맞게 돈다 — 배경면 요소를 0개로 그린다.
  어긋난 것은 **네이티브 배너 하나**이고, 그래서 본문 위에 여백 없이 그냥 얹힌다
⛔ 이것으로 **2.115의 「배너는 결과 화면에만 있다」가 이 빌드에서 깨졌다**
```

#### ★★ 원인 — **경합 조건이다** (확정 · 2026-09-04)

```
`created`는 showBanner가 **완주한 뒤에야** true가 되는데,
숨기기는 그 깃발을 **문 앞에서** 본다.

  결과 진입 → setBannerShown(true) → initialize + showBanner (창)
     ↳ 그 창 안에 화면을 떠남 → setBannerShown(false)
       → 그 순간 created가 **아직 false** → `if (created)`가 거짓 → hide를 건너뜀
     ↳ showBanner 완주 → 네이티브가 배너를 붙임
       → **떠난 화면 위에 뜬 채 갇힌다** (다음에 결과 화면을 다시 거칠 때까지)

창의 길이  **0.59초** (에뮬 실측 · 찬 프로세스). 대부분이 initialize다.
         ⚠ 실기기·느린 망에서는 더 길다
```

**★ 기각했던 가설 ①과 어떻게 다른가 — 여기가 핵심이다**

```
① 이었던 것   "created 깃발이 **끝내** false로 남는다"
             → 로그가 created=true를 보여줘 **정당하게 기각됐다**
실제였던 것   "**떠나는 그 순간**의 created가 false다"
             → 깃발은 결국 true가 된다. 값이 틀린 것이 아니라 **읽는 시점**이 틀렸다
⛔ 같은 변수를 놓고도 **다른 결함**이다. ①을 기각한 것은 옳았고,
  그 기각이 이 결함을 가린 것도 사실이다 — 변수 이름이 같으면 다 봤다고 여기게 된다
★ 교훈: 깃발 하나를 기각할 때는 **"값이 틀렸나"와 "언제 읽나"를 따로** 물어야 한다
```

**⛔⛔ 왜 앞 세션이 못 잡았나 — 시험 도구가 창을 매번 비켜 갔다**

```
① **시험이 결과 화면에서 10초를 기다린 뒤** 나갔다(btrace2.py의 sleep(10)).
   창이 0.59초인데 10초를 기다렸으니 **재현될 리가 없었다.**
   ★★ **시간에 달린 결함은 "천천히 시험하면" 안 보인다.**
     자동 시험은 사람보다 느리게(대기를 넣어) 움직이는 일이 흔하고,
     그 대기가 바로 결함이 사는 자리를 지운다
② 화면 판정이 `button[role=switch]`였다 — **About에도 있다**(About.jsx:318).
   About을 결과 화면으로 읽었다. 이제 `[role=tablist]`로 가른다
③ 그리고 catch {} 가 실패를 통째로 삼켰다(②에서 고쳤다 · bf252af)
```

#### ✅ 고친 것 — **안 1(두 겹)** · 사용자 승인 2026-09-04

```
ㄱ **숨기기에 조건이 없다** — `if (created) hide()` 를 없앴다.
   만든 적 없을 때 불러도 무해하다(플러그인이 거부하고, 거부는 로그에 남는다)
ㄴ **최후 방어** — showBanner가 끝난 자리에서 **마지막 요청(wanted)을 다시 본다.**
   ⛔ ㄱ만으로는 모자란다는 것이 **실측으로 확인됐다** — 고친 뒤 첫 재현에서
     ㄱ의 hide가 `You tried to hide a banner that was never shown`으로 **거부됐고**,
     배너를 내린 것은 ㄴ이었다. 브리지 스레드 순서는 보장이 없다
ㄷ **만드는 중이면 또 만들지 않는다(skip)** — 플러그인의 showBanner는 두 번째
   호출에서 `call.resolve()`를 안 부르고 돌아간다(BannerExecutor.java:66).
   그대로 두면 약속이 영영 안 풀린다
⚠ ㄴ은 wanted=true여도 **resume을 부른다.** 만드는 동안 끼어든 hide가 배너를
   GONE으로 만들어 놓았을 수 있다 — 그러면 결과 화면인데 배너가 없다

판단은 **adsApp.js의 순수 함수 둘**로 꺼냈다 — bannerRequest · bannerSettled.
  ★ 그래야 겹친 요청을 **값으로 단언**할 수 있다. 화면 검사로는 못 만든다
상태는 셋이다 — created(만들었나) · creating(만드는 중인가) · wanted(마지막 요청)
```

**⛔ 안 2(직렬화)를 기각한 이유** — hide가 show 완주를 기다리므로 **0.6초짜리
실제 노출**이 생긴다. adsApp.js의 "늦게 사라지는 것이 사고다"에 정면으로 어긋난다.

**③-ㄷ 조사 결과 — 플러그인에 가시 상태를 읽는 API가 없다.** 배너 쪽은
`showBanner · hideBanner · resumeBanner · removeBanner` + 리스너뿐이다
(banner-definitions.interface.d.ts 전수 확인). 깃발을 없앨 수 없어 ③-ㄴ으로 갔다.

#### ✅ 확인 — 재현 절차 그대로 (에뮬 · debug 빌드 · 부모 RelativeLayout으로 판정)

```
① 결함이 났던 창    찬 프로세스 · 결과 진입 **0.4초** 뒤 나감
                 고치기 전 ⛔ VISIBLE (10초 관찰 내내) → 고친 뒤 ✅ GONE
                 로그: hide 거부됨 → **만들기 끝 — 마지막 요청은 false → hide**
② 대조            같은 찬 프로세스 · **5초** 뒤 나감 → ✅ GONE (전에도 정상이던 경로)
③ 빠른 왕복        결과→About→결과→About을 250ms·520ms 간격으로
                 → 되돌아온 결과 화면 ✅ VISIBLE · 마지막 About ✅ GONE
                 (250ms 회차에서 skip → resume 경로가 실제로 돌았다)
④ 세 화면         About ✅ G · 위기 화면 ✅ G · 알림 구절 화면 ✅ G
```

**⛔ 판정 도구 — 두 가지를 바로잡았고, 둘 다 스스로 한 번 속은 뒤에 고쳤다**

```
① 화면 판정  `[role=tablist]`로 가른다. ⛔ role=switch는 About에도 있다
   ⚠ **글자로도 안 된다** — "이 앱에 대해"는 입력 화면의 **버튼 이름**이기도 해서
     입력 화면이 About으로 잡혔다(실측). 그 화면에만 있는 버튼으로 가른다:
       결과=[role=tablist] · About="이 기기에서 기록 지우기" ·
       알림 구절="지금 마음을 적어볼까요" · 입력="마음 들여다보기"
② 배너 판정  ⛔⛔ **AdView의 V를 보면 안 된다.** 숨겨도 AdView는 V다 —
   플러그인이 숨기는 것은 **부모 RelativeLayout**이다(BannerExecutor.java:170).
   `dumpsys activity top`의 View Hierarchy에서 **AdView 바로 윗줄**을 읽는다.
   ⚠ uiautomator dump로는 이 계층이 아예 안 보인다(실측)
③ 알림 구절 화면으로 들어가기 — 그늘(shade) 좌표 누르기가 **세 번 다 빗나갔다.**
   알림 탭이 만드는 인텐트를 그대로 보내는 것이 확실하다:
     am start -a android.intent.action.MAIN -n <pkg>/.MainActivity -f 0x24000000
       --ei LocalNotificationId <id> --es LocalNotificationUserAction tap
       --es LocalNotficationObject '<알림 JSON>'
   ⛔ **-a ACTION_MAIN이 없으면 플러그인이 그냥 돌아간다**(LocalNotificationsPlugin.kt:55)
   ⛔ 그리고 adb shell은 인자를 한 줄로 이어 붙인다 — JSON을 홑따옴표로 감쌀 것
```

#### ✅ 회귀 — 값으로 단언한다 (adsApp.test.js)

```
· 숨기기에는 조건이 없다 — created·creating·wanted **여덟 조합 전부** hide
· 만드는 중이면 skip · 만들었으면 resume · 처음이면 create
· ★ 요청이 겹치면 **마지막 것이 이긴다** (결과→다른 화면 · 창 안에서)
· ★ 빠른 왕복에서 **중간 요청이 갇히지 않는다** (skip → settled → resume)
· 만들다 실패하면 creating이 풀린다 (다음 요청이 영영 skip이면 안 된다)
· 판단이 넘겨받은 상태를 **고치지 않는다**(불변)
· 소스 계약 — 숨기기 갈래에 `if (`가 다시 들어오면 실패 · `if (created)`가 없다
⛔ **화면 검사는 회귀로 못 만든다**(기기와 실제 광고가 필요하다).
  그래서 「통으로 밟기」 절차에 항목으로 박았다 —
  **"결과 화면에 들어가자마자(1초 안에) 떠나면 배너가 없는가"**
  ★ 이것을 잡은 것이 바로 그 밟기였다(2.127)
```

### 2.127 ★★ 통으로 밟기 — **처음으로 끝까지 밟았다** (2026-09-04)

> ★ 3절의 「전체 흐름을 통으로 밟은 적이 없다」가 **이것으로 닫힌다.**
> ⛔ 그리고 이 밟기가 2.126을 잡았다 — 조각으로만 봤으면 못 봤다.

#### ✅ 신규 설치에서 열까지 — 전부 통과

```
방법   uninstall → **릴리스 APK** 설치 (앱 데이터도 알림 채널도 없는 상태)

①  첫 실행        ✅ 첫 방문 인사 "반가워요. 오늘 마음은 어떤가요?"
②  입력           ✅ 한 줄 적기 → 세분류 화면(설계된 경로 · RESULT.CATEGORY)
③  구절           ✅ 요한복음 14:27 · 탭 말씀 20 · 찬양 20
④  영상           ✅ 썸네일 20개
⑤  이어 읽기       ✅ 요한복음 14장 · 절 번호 · 이전/다음
⑥ ★ 토글 꺼짐     ✅ **신규 설치에서 aria-checked=false** (DEFAULT_ON=false 확인)
⑦ ★ 팝업→허용→토스트 ✅ **손가락 좌표(adb input tap)로 눌러 순서대로 돌았다**
                     권한 granted · 예약 14건 · 토스트 문구 그대로
⑧  알림 채널       ✅ importance=3 · **mVibrationEnabled=true** · 기기 기본 알림음
                     → 2.94의 채널 설정이 신규 설치에서 그대로 만들어진다
⑨ ★ 알림 수신     ✅ 시각을 3분 뒤로 바꿔 **실제로 받았다**(히브리서 4:9-10)
⑩ ★ 알림 탭→구절   ✅ 「오늘의 구절」 화면이 열리고 장 본문이 이어진다
✅ 그리고 **릴리스 APK가 실제로 돈다** — 서명본을 설치해 끝까지 밟았다
```

#### ⚠ 부수 확인 둘

```
⚠ **첫 실행이 말씀 2 · 찬양 3이었다**
  부팅 직후라 망이 안 붙어 원격 목록을 못 받고 **씨앗 데이터**로 돌았다.
  앱을 다시 열자 20/20이 됐다(캐시에 videos.json이 들어왔다).
  ⛔ 결함이 아니다 — 오프라인 우선 설계 그대로다.
  ★ 그래도 기록한다: **첫 실행이 곧 첫 인상**이다. 망이 느린 곳에서 처음 여는
    사람은 영상이 2~3개인 화면을 본다. 언젠가 판단할 자리다
  ⚠ LAST_CHECKED_AT은 version.json을 받은 **뒤에** 쓰이므로, 실패하면 안 쓰이고
    다음 실행에 다시 시도한다 — 그 설계는 맞다

✅ **릴리스 APK에서 CDP가 붙는 것은 앱 결함이 아니다**
  에뮬레이터가 userdebug(ro.debuggable=1)라 시스템 전체가 열려 있어서다.
  실기기는 user / ro.debuggable=0 이라 안 열린다(둘 다 실측).
  ⛔ APK는 debuggable이 아니다(flags에 DEBUGGABLE 없음). 걱정할 자리가 아니다
```

#### ⚠ 통으로 밟을 때 걸린 도구 함정 둘

```
① 버튼이 뷰포트 밖이면 탭이 안 닿는다
   DailyVerse의 버튼이 y=847인데 innerHeight=592였다. 좌표 환산은 맞았지만
   화면 밖이라 아무 일도 안 났고, **추적 로그가 통째로 비었다.**
   ★ 누르기 전에 scrollIntoView하고 **좌표를 다시 읽는다**
② ⛔ 결과 화면 판정에 role=switch를 쓰지 말 것 — About에도 있다 (2.126)
```


### 2.128 ★ 공감 문구 — **좋은 상태에 끝을 붙이지 않는다** (2026-09-04)

> ★ B2가 닫혔다. 고친 것은 **두 줄**이고, 얻은 것은 **경계선 하나**다.

#### ⛔ 무엇이 문제였나

```
taxonomy.yaml 에 같은 구문이 둘 있었다.
  [flutter.thrill] "이런 기분은 오래 가지 않으니 지금 누려요."
  [calm.ease]      "여유는 오래 머물지 않으니 지금 누려요."

구조   `X는 오래 [가지/머물지] 않으니 → 지금 누려요`
⛔ 지금을 누릴 **이유로 미래의 상실을 끌어온다.** 좋은 상태를 인정하러 온
  문장이 그 상태에 **끝을 붙여** 그늘을 드리운다.
★ 그리고 이 앱의 마무리 문구는 이미 반대쪽에 서 있었다 —
  "뿌듯한 마음, 오래 두고 가요" · "이 상태를 좀 더 두고 가요".
  고친 두 줄이 제 결에서 혼자 벗어나 있었던 것이다
```

#### ✅ 고친 것

```
[flutter.thrill] → "이런 기분이 드는 날이군요. 지금 여기 그대로 두어요."
                  원문의 **두 절 구조를 지키되** 뒷받침을 상실에서
                  **인정 + 머무름**으로 갈아끼웠다
[calm.ease]     → "숨 쉴 자리가 있는 하루였군요."
                  **권하지 않고 인정만 한다** — 격려하지 않기 원칙(2.92)에
                  가장 가까운 쪽을 사용자가 골랐다
⛔ 억지로 밝게 만들지 않았다. 미래 시제가 둘 다 없다
✅ 게이트 실측(데이터 재생성 뒤) — 최대 유사도 0.525 · 0.593 (임계 0.85)
   messages_test.py 전부 통과 · 옛 문구 잔존 0건
```

#### ★★ 경계선 — **병은 단어가 아니라 방향이다**

> ⛔ 다음에 같은 계열을 볼 때 **이 절을 먼저 읽어라.** 아래 셋을 또 의심하지 말 것.

```
✅ 병이다     **좋은 상태에 끝을 붙이는 것**
             "이런 기분은 오래 가지 않으니…" — 지금의 값을 미래의 상실로 받친다

⛔ 병이 아니다 ① **드묾** — 끝을 예고하지 않는다
                 joy.delight  "이런 날은 흔치 않죠."
                 joy.grateful "좋은 걸 좋다고 느낀 날이네요. 드문 일이에요."
                 joy.grateful "이런 마음이 드는 날은 귀해요."
               ★ 사용자 판단(2026-09-04) — "지금이 특별하다는 **인정**이지
                 곧 사라진다는 **예고**가 아니다. 값을 대비에서 빌려오는 것은
                 지금을 귀하게 여기는 자연스러운 방식이고 미래를 끌어들이지 않는다"
               ⛔ **그대로 둔다.** 이 셋을 고치면 인정할 말이 남지 않는다

             ② **부정 감정의 "지나가요"** (12건 · 마무리 문구)
               조급함·긴장·짜증·화·막막함·허함·심심함에 붙어 있다.
               거기서 "지나간다"는 그늘이 아니라 **덜어주는 말**이다
             ③ **"잠깐"** (5건) — "여기 잠깐 앉았다 가요" 계열.
               미래가 아니라 **지금의 크기**를 말한다
             ④ **"사라지지 않아요"** — 눌러둔 마음·말하지 못한 것.
               상실이 아니라 **남는다**는 쪽이다

★ 그래서 판정은 **같은 단어를 찾는 것이 아니라** 두 가지를 함께 묻는 것이다.
    ㄱ 붙은 감정이 **좋은 상태인가**
    ㄴ 그 상태에 **끝·상실·변화를 예고**하는가
  둘 다 참일 때만 병이다. 어휘 검색만 하면 26건이 걸리는데 그중 둘뿐이었다
```

#### ✅ 전수로 훑었다 — 같은 병은 둘뿐이다

```
공감 문구 240건 · 마무리 문구 245건 = **485건 전부**를 봤다.
어휘 후보 26건 → 위 기준으로 갈라 **2건만 병**이었다.
긍정 분류(joy · flutter · calm)의 마무리 문구 70건은 **깨끗하다**.
⛔ 앞으로 문구를 늘릴 때 이 병이 다시 들어올 자리는 **긍정 분류의 공감 문구**다 —
  거기서만 "좋은 상태 + 끝"이 성립한다
```


## 3. 다음 세션 할 일 — Phase 4 운영과 남은 과제

> ⚠ **이 목록은 참고 자료이지 자동 착수 지시가 아니다.**
> 이전에 세션이 여기를 읽고 알아서 시작한 적이 있다. 무엇을 할지는 **사용자가
> 정한다.** 세션을 열면 상태를 보고하고 **지시를 기다린다.**
> 여기 적힌 것은 "물어보면 답할 수 있게" 정리해 둔 것이다.

### 오늘까지 (2026-08-31 종료 시점)

```
Phase 0  채널 승인 15개 · 이단 규정 1c 대조                        2026-08-18
Phase 1  구절 큐레이션                                            2026-08-18~
Phase 2  배치 본체 · 태깅/폴백 전략 · Actions 워크플로               2026-08-19
Phase 3  앱 구현 · 배포  https://appleap385-aaz0919.github.io/PeaceYourMind/
Phase 4  사전 보강 · UI 개정 · 구절 확장                            2026-08-19
         원인 조사(2.25) → 수정 [0]~[7] 완료(2.26~2.33)            2026-08-20
         이어서 읽기 탭(2.34) · 색/여백 위계(2.35~2.36)             2026-08-20
         부정 계열 커버리지(2.37)                                   2026-08-20
         장 문맥 재검토(2.38) · 시편 실측(2.39) · 반복 보강(2.40)     2026-08-20
         비시편 한계선 확정(2.41) · cron 대조(2.42)                  2026-08-20
         수치 단일화 stats.py(2.43) · [6] 동점 처리(2.44)              2026-08-24
         3차 발굴 준비 — 검색어 세트 · 위기 후보 표시(2.45)             2026-08-24
         3차 발굴 실행 — 통과 22 · 승인 0 · 목표 미충족(2.46)           2026-08-24
         후보 판정 — 사전 2건 · 접두어 방어 · C채널 1c 대조(2.47)        2026-08-24
         ★ 첫 승인 1건 — allowlist 15→16 · 프로그램명 사전 제외(2.48)    2026-08-24
         ★ 승인 후 배치 — 말씀 탭 찬양 11→0 · 폴백 28.2→23.3%(2.49)     2026-08-24
         출시 준비 정리 — 이메일 점검 · 광고 정책 확인(2.50)              2026-08-24
         기록 삭제 버튼 · 인피드 광고 결정 · 방침 개정(2.51)           2026-08-24
         ★ AdSense 정책 확인 — 앱에서는 못 쓴다(2.52)                 2026-08-24
         앱 AdSense 위험 완화 조사(2.53) · ★ 광고 결정 확정(2.54)        2026-08-24
         ★ 웹 광고 구현 · 유저 사이트 레포 · ads.txt · 심사 대기(2.55)    2026-08-24
         CONTACT 확정 · 이메일 노출을 게이트로 · OPERATOR 노출처(2.56)     2026-08-24
         ★ 찬양 3곳 승인 allowlist 16→19 · OPERATOR 확정(2.57)            2026-08-24
         About 광고 절 — AD_SLOT에 묶음 · "말씀" 오독 차단(2.57 ⑥)        2026-08-24
         About 기록 절 한 행 정렬 · 서체 진단 정정(2.57 ⑦)               2026-08-24
         About 색 통일 13px muted · 떠 있는 돌아가기(2.57 ⑧⑨)            2026-08-24
         "앱 N건"=선언 수 확정 · quiet_worship 10→207 확인(2.55 ⑪)       2026-08-25
         ★ 표시명 Peace in Your Mind — 경로·레포·PYM은 구명 유지(2.58)   2026-08-25
         ★ 표시명 재변경 Peace in Mind — short_name 13자(2.58 ⑥)          2026-08-25
         ★ 형식 보장 폴백 MEDIA_FLOOR=4 — 말씀 탭 0건 해소(2.59)         2026-08-25
         분류 실패에서 입력창 비우기(2.60)                             2026-08-25
         ★ 말씀 3곳 승인 allowlist 19→22(2.61) · 실패 문구·경로(2.62)   2026-08-25
         `구려`·`구질` 어휘 추가 — 어절 결합만(2.63)                    2026-08-25
         ★ 영상 상한을 탭별로 — 총량 20 → 탭당 20(2.64)                2026-08-26
         빈 광고 칸 32px 여백 수정 — 프로덕션 전용 결함(2.65)           2026-08-26
         폴백 천장 20 유지 확정(2.66) · MEDIA_FLOOR 정식 경보로(2.67)   2026-08-26
         ★ 배치 하루 2회 전환 — KST 18:30 + 06:30 슬롯 추가(2.68)       2026-08-26
         ★ 배치를 죽이던 임포트 누락 수정 — 게이트 3종이 놓쳤다(2.69)     2026-08-26
         ★ CI 게이트에 진입점 드라이런 — 역검증까지(2.70)                2026-08-26
         ★ 08-26 배치 결과 — 48/48 탭 충족 · 06:30 미발동(2.71)          2026-08-27
         ★ 탭 20 초과 D안 — tab_over_cap 경보 + unknown 지표(2.72)        2026-08-27
         ★ unknown 조사 — 64%가 제3의 콘텐츠였다(2.73)                    2026-08-27
         ★ 제3범주 제외 시뮬레이션·오탐 실측(2.74)                        2026-08-27
         ★ 제3범주 폴백 제외 구현 — 사전 11종 + S3(2.75)                  2026-08-27
         ★ 방법론 — 하루치 오탐 0은 믿을 수 없다(2.76)                      2026-08-27
         ★ 제3범주 필터 첫 실전 — 63건 제외 · 중복은 안 줄었다(2.77)         2026-08-28
         ⚠⚠ Actions 지연 악화 — 10시간 25분(2.78)                        2026-08-28
         ★ 주간 진단 게이트를 지연에 견디게 — 예약회차+경과일(2.79)          2026-08-28
         ★ 안 E 적용 sermon_min 1800→1200 — unknown −67%(2.80)            2026-08-28
         ★ 단독 '워십' 제거 · 주제분 섞기 · 폴백 최신순(2.81)                2026-08-28
         ★ 배치 실전 확인 + unknown 격리 — 탭 정확히 20 · 중복 0(2.82)        2026-08-28
         안 A′ · 폴백 90일 컷 · 주제분 사다리 · 신고 7건 교정(2.83~2.89)      2026-08-28
         ★ Capacitor 안드로이드 래핑 1단계 — 번들 실행 · 앱 빌드 분리(2.90)     2026-08-31
         ★ 아이콘 03-c '끊긴 원 · 숨' + gen_icons.py 복원(2.91)               2026-08-31
         ★ 구절 알림 — 정한 시각에 구절 한 절(2.92)                           2026-08-31
         알림 문구 시각 중립화 · NOTIFY_SEEN 누적 버그(2.93)                  2026-08-31
         ★ 알림 소리·진동 — 원인이 세 겹이었다 · 채널 진동 켬(2.94)            2026-08-31
```

**규모**

<!-- STATS:BEGIN 규모 -->
<!-- scripts/stats.py --write 로 생성한다. 손으로 고치지 말 것. -->
```
구절       303  = 감정 293 + 위기 10
           감정 중 시편 163 (55.6%) · 비시편 130
           read_from 지정 18건 · 검토 후 제외 15건
장 본문     178개 장 4,442절 (39권) — 감정 구절이 닿는 장만
           app/public/krv/ · .gitignore 대상 · 매 빌드 재생성
주제       24개 · 제목 어휘 97 · 형식 어휘 30
감정 분류   대분류 9 · 세분류 24 · 키워드 923
채널       allowlist 22 (하한 15까지 여유 7)
           blocklist 0 · reviewed_out 13
게이트      파이썬 5종 + 앱 310건
```
<!-- STATS:END -->

**앱 사실** — ⛔ 손으로 적지 않는다. 아래 블록이 코드에서 읽어 채운다.

<!-- STATS:BEGIN 앱 -->
<!-- scripts/stats.py --write 로 생성한다. 손으로 고치지 말 것. -->
```
앱 아이디    io.github.appleap385.peaceinmind
표시 이름    Peace in Mind
버전        0.1.0  (versionCode 100)
SDK        min 24 · compile 36 · target 36
권한 선언    INTERNET · AD_ID
권한 제거    SCHEDULE_EXACT_ALARM   (tools:node=remove)
           ⛔ **이 줄은 우리가 선언한 것만이다.**
             SDK·플러그인이 병합하는 권한은 여기 보이지 않는다 —
             전부로 읽지 말 것. 실제 목록은 APK로 확인한다(2.92·2.98)
알림 채널    id morning_verse · importance 3 · vibration true · sound 지정 안 함
           ⛔ 채널은 한 번 만들어지면 코드로 못 바꾼다(2.94)
알림 예약    id 4100~4113 (14일 창) · 기본 시각 09:00 · 기본값 켜짐 false
운영자       개인 개발자가 만들어 운영합니다
문의처       appleap385@gmail.com
```
<!-- STATS:END -->

게이트는 전부 종료 코드 0 · API 소모 0이다.
⚠ `stats.py --check`는 **로컬 게이트다** — CI(build.yml)는 verify_verses ·
  tagging_test · spread_test만 돈다. 커밋 전에 손으로 돌려야 한다.

---

### ✅ 끝난 것 — 웹 AdSense 광고 구현 (2026-08-24)

2.51절 설계대로 만들었고 배포했다. 상세는 **2.55절**.

```
구현      자리 계산 순수 함수(ads.js) · AdSlot 컴포넌트 · 회귀 31건
레포      appleap385-aaz0919.github.io 신설 — 도메인 루트 404 해소 + ads.txt
방침      aboutads.info 일괄 거부 한 줄 추가
남은 것    AdSense 심사 대기 -> 승인되면 AD_SLOT 한 줄을 채운다
```

⚠ **승인 전까지 사용자에게는 광고가 없다.** slot이 비면 요소를 그리지 않고,
  번들러가 광고 가지를 통째로 접는다(배포본 JS에 adsbygoogle 0건 — 실측).

⚠ 승인 뒤 `AD_SLOT`을 채우면 **그 순간 화면에 층이 하나 더 생긴다.**
  채우기 전에 360px에서 위계를 다시 잴 것 — 절차는 2.36절 규칙 5항이다.

---

### ★★ 다음 세션은 여기서 시작한다 (2026-09-03 세션 종료 시점)

> ⚠ **착수 지시가 아니다.** 상태를 보고하고 기다린다. 무엇을 할지는 사용자가 정한다.
> ⚠ 아래 「2026-08-31 시점의 정리」는 이력이다. 이 블록이 최신이다.

#### ⛔⛔ 먼저 읽을 것 — 오늘 밟은 함정 다섯

> ★ 다섯 다 **회귀가 통과한 채로** 틀렸다. 순수 함수는 맞았고 화면·기기가 달랐다.

```
① ⛔ **CDP click은 히트 테스트를 건너뛴다**
   세션이 토글을 CDP로 눌러 "동작한다"고 보고했는데 손가락으로는 안 눌렸다.
   ★ 터치를 검증할 때는 **adb input tap으로 실제 좌표**를 누른다.
     좌표는 getBoundingClientRect + (갈래별 상단 오프셋)으로 환산한다
② ⛔ **픽셀 허용치 함정** — 배경면 색 판정 허용치를 6으로 두면 그 아래
   그라디언트가 배경면으로 잡혀 **아래 여백이 40dp로 부풀었다**(실제 16).
   ★ 허용치는 **2**다. scripts/measure_banner.py 에 상수로 박았다
③ ⛔ **am force-stop을 쓰지 말 것.** 알람 예약이 전부 지워진다.
   세션이 한 번 썼다(예약은 앱을 열어 복구). `install -r`이 이미 프로세스를 내린다
④ ⛔ **배경면을 띄우면 그 아래로 콘텐츠가 지나간다.** ④(상하 대칭)를 지키려
   인셋만큼 띄웠더니 그 자리가 여전히 웹뷰 안이라 스크롤 콘텐츠가 비쳤다.
   덮개(bandCoverStyle)로 막았다 — **갈래 B에서만 드러났다**
⑤ ⛔ **뷰에 인셋 리스너를 달면 그 뷰의 기본 인셋 처리가 건너뛰어진다.**
   웹뷰에 달았다가 뷰포트가 800 → 848dp로 늘었다. 읽기만 한다(레이아웃 리스너)
★★ 그리고 **"더 나올 자리가 없다"고 쓴 전제가 세 번 깨졌다.** 갈래를 다 쟀다는
  것은 "그 조건에서 그 값이었다"일 뿐이다. 실측과 실사용이 갈리면 **실측을 의심한다**
```

#### ✅ 배너 잔존 결함 — **닫혔다** (2026-09-04 · 2.126)

```
원인   **경합 조건**이었다 — `created`는 showBanner가 완주한 뒤에야 true가 되는데
      숨기기가 그 깃발을 문 앞에서 봤다. 만드는 창(0.59초) 안에 화면을 떠나면
      hide를 통째로 건너뛰고, 뒤늦게 뜬 배너가 떠난 화면 위에 갇혔다
      ★ 기각했던 가설 ①("끝내 false로 남는다")과 **다른 결함**이다 —
        값이 틀린 것이 아니라 **읽는 시점**이 틀렸다
고침   안 1(두 겹) — ㄱ 숨기기에 조건 없음 · ㄴ 만든 뒤 마지막 요청을 다시 본다
      ⛔ ㄱ만으로는 모자란 것이 **실측으로 확인됐다** — ㄱ의 hide가 거부됐고
        배너를 내린 것은 ㄴ이었다
확인   ① 결함이 났던 창(0.4초) ✅ ② 대조(5초) ✅ ③ 빠른 왕복 ✅
      ④ About · 위기 화면 · 알림 구절 화면 셋 다 ✅ (부모 RelativeLayout = G)
회귀   값 단언 7건 + 소스 계약 1건 · 통으로 밟기에 항목 1개
⚠ 에뮬에 **debug 빌드**가 들어 있다(고친 판). 실기기 둘은 아직 옛 빌드다
```

#### ⏸ 그다음 순서 — **B1이 닫혔으므로 여기부터다**

```
① ✅ **공감 문구 수정 — 끝났다** (2.128)
   두 줄을 고쳤고 경계선("드묾은 병이 아니다")을 문서에 남겼다

② **스크린샷** (방침은 아래)  ← ★ 여기서 시작한다
   ⛔ 실기기 둘이 **배너 고치기 전 빌드**다 — 먼저 새 빌드를 넣는다

③ **AAB 새로 빌드** — ⛔ 코드가 바뀌면 지금 AAB는 낡는다
   게이트 둘 통과 확인 · 서명 지문이 AF:23:…:22 인지 확인

④ **첫 AAB를 Play 내부 테스트에 업로드**
   ⚠ 콘솔이 보여주는 업로드 인증서 지문이 위 SHA-256과 같은지 **대조한다**
   ★ 그때 **앱 서명 키 지문**도 함께 HANDOFF에 적는다(아직 없다)

⑤ **비공개 테스트 요건 확인** — 테스터 인원·기간을 콘솔에서 본다.
   → 그것으로 **출시 일정**을 정한다
```

#### ★ 스크린샷 방침 (2026-09-04 · 사용자 확정 · ✅ 결함은 닫혔다)

```
휴대전화용   **에뮬레이터** pym_phone_360 (1080×1920 = 9:16)
구도        **ㄱ+ㄴ** — 배너 없는 화면(입력·장 본문·알림 구절)을 주력으로,
            결과 화면은 배너 영역을 **프레임 밖**에 두고 한 장
            ⛔ **ㄷ(시험 기기 등록 풀기)는 안 한다.** 개발자 노출을 만들 이유가 없다
구절 고르기   **3줄 안팎**으로 고른다. ⛔ 구절이 잘리면 안 되고,
            영상 두세 개가 함께 보이게 한다
상단 72px    **자르기(1080×1848)와 덮기(1080×1920) 둘 다** 만들어 비교한다
태블릿용     **실기기**(Galaxy Tab S9 FE). ⚠ 재설치하면 알림 예약이 지워지니
            **앱을 한 번 연다**
⛔ 확정하지 말고 보여줄 것 — 상단 처리와 결과 화면 구도는 사용자가 정한다
```

#### ✅ 여기까지 끝난 것 — 출시 준비

```
A2 앱 서명 키    ✅ 닫힘 — Google이 이미 생성해 뒀다. 고를 것이 없었다 (2.123)
업로드 키        ✅ 만들었다 (2.125) · 별칭 pym-upload · 유효 2054-01-20
                지문 AF:23:0D:4D:25:95:89:61:95:F0:A9:AF:59:80:EE:2A:
                     13:2E:77:AC:38:F8:E4:60:C3:28:FC:B7:8D:9E:DE:22
서명된 AAB       ✅ 확인 — 게이트 둘 통과 · app-release.aab · 지문 대조 일치
앱 콘텐츠 선언    ✅ **전부** — 방침 · 로그인 · 광고 포함 · 등급(전체이용가) ·
                타겟층(만 18세 이상) · 데이터 안전 · 광고 ID · 건강/금융/정부 없음
스토어 설정      ✅ 카테고리 **도서/참고자료** · 태그 **자기계발 · 종교문헌**
                (⛔ 「명상」은 뺐다 — 설명에 근거가 없다)
                연락처 **appleap385@gmail.com**
                ⚠ 계정·IARC는 aaz0919@gmail.com — **사용자에게 안 보이는 주소**라
                  그대로 둔다. 합치지 않는다 (2.123)
등록정보 본문     ✅ 수정 — 「아무것도 보내지 않습니다」를 「적은 내용은 기기에
                남습니다」로 바꾸고 **「광고」 절**을 더했다
그래픽 이미지     ✅ app/store/feature-graphic-1024x500.png (생성기가 만든다)
아이콘           ✅ app/public/icons/icon-512.png 그대로 (512×512 · 투명 없음)
ads.txt          ✅ **「승인됨」** (2.121) · ⛔ apex 가설도 닫혔다
통으로 밟기       ✅ **처음으로 끝까지 밟았다** (2.127) — ①~⑩ 전부 통과
```

#### ✅ 어제 결함 하나가 배포된 채로 발견됐고, **고쳐서 배포됐다** (2.122)

```
모바일 웹에 **눌러도 아무 일이 없는** 「구절 알림」 토글이 나가 있었다.
✅ 고쳤고(484ab01) **배포까지 끝났다** — run 79 (2026-09-04 00:11Z).
   배포본 번들에서 role:"switch" **0건** · 양성 대조("이어 읽기") 1건 확인
★★ 배운 것 — 게이트 293건이 다 통과했다. **검사의 방향이 한쪽뿐이었기 때문이다.**
  「앱 전용이 웹으로 새는」 방향에 검사가 아예 없었다. 이제 있다(webBuildGuards).
  ⛔ 이 교훈은 3-부록 「일하는 방식」 ⓪-3에 규칙으로 남겼다 —
    **검사 대상이 아니면 검사는 아무 말도 안 한다**
⏸ 같은 유형 하나가 남았다 — DailyVerse(W1). ✅ 지금 결함이 아니라는 데 동의를 받았고,
  **성질만 문서에 남긴 채** 판단 목록에 둔다
```

#### ⚠ 코드 쪽 화면 작업 — **거의** 끝났다 (2.126이 하나 열려 있다)

> ⛔ 아래는 그대로 유효하다. 다만 **배너가 화면을 벗어날 때의 동작**은
> 2.126으로 다시 열려 있다 — 배치·크기가 아니라 **보이고 숨는 것**의 문제다.

```
★ 다음 세션이 화면을 다시 열 이유가 없다. 넷 다 **실측으로 닫혔다**.

배너    세 갈래 전부 **위 16 · 배너 50 · 아래 16 · 배경면 82dp** (2.118 안 2)
       ★ 건드리면 scripts/measure_banner.py 로 셋을 한 번에 잰다
토글    결과 화면 탭 행 「구절 알림」 · About과 같은 setEnabled (2.119)
토스트   상단 중앙 · 내용 폭 알약 · 문구 확정 · 들어옴/나감 대칭 0.45s ·
       머무름 4.6초 · 총 노출 5.05초 (2.119 · 2.120 · 세 갈래 실측)
권한 경로 A′(3값 · 화이트리스트) · enableDecision(순수 함수) ·
       영구 거절 감지와 탈출구 토스트 (2.119 · 2.120 · 네 상태 실측)

⛔ 여기에 손대려면 **먼저 왜 여는지 적을 것.** 세 번 "더 나올 자리가 없다"는
  전제가 깨졌고(3절 함정), 그때마다 화면을 다시 팠다. 지금은 근거가 다 있다
```

#### ✅ 오늘 확정·완료된 것

```
배너      세 갈래 전부 **위 16 · 배너 50 · 아래 16 · 배경면 82dp** (2.118 안 2)
         네이티브가 --inset-bottom-real 을 주입한다. X = (SDK≥35) ? I : 0
         폴백(주입 없음)은 옛 비대칭식으로 떨어져 **음수가 안 된다**
         ★ scripts/measure_banner.py 로 세 갈래를 한 번에 잰다.
           ⛔ 배너를 건드릴 때마다 돌린다. 기준은 **배너 아래끝 64dp** 항등식
토글      결과 화면 탭 행에 「구절 알림」 · 「이어 읽기」로 줄임 · 세로 구분선
         About 토글과 **같은 setEnabled**를 쓴다. 위기·알림 화면은 ResultTabs를
         안 쓰므로 분기가 없다
A′       권한을 셋으로 읽는다(granted/denied/unknown · 화이트리스트).
         켜기 판단은 **enableDecision** 순수 함수 한 곳이다
         DEFAULT_ON=false · 재진입 방어 · 팝업 가드는 **자동 경로에만**
토스트     상단 중앙 · 내용 폭 알약 · radius 999 · 테두리 jade 55 + 블러
         문구 **"정해진 시각에 구절 한 절을 보내드릴게요"**
         움직임 **안 ㄱ(.rise의 거울 · 위에서 내려앉기)**
         ⚠ 시간은 **0.45s로 바뀌었다** — 아래 「그다음에 끝낸 것」·2.120을 볼 것
         상단 인셋은 env(safe-area-inset-top) 하나로 세 갈래가 다 맞는다
그 밖     2.115 시스템 바 색 · 2.116 allowWhileIdle(배터리 세이버를 넘는다) ·
         2.94 VIBRATE 종결 · 플러그인 버전 고정 테스트
```

#### ✅ 그다음에 끝낸 것 (2026-09-03 후반 · 2.120 · 2.121)

```
토스트 움직임  나감 6px → **10px** · 곡선은 ease의 정확한 거울
             cubic-bezier(.75,0,.75,.9) · 들어옴 0.7 → **0.45s** · 나감도 0.45s
             ⛔ 배선은 원래 되어 있었다. 6px + 같은 곡선이라 페이드에 묻혔던 것이다
머무는 시간    2.6 → **4.6초** · 총 노출 **5.05초** (세 갈래 실측 5039~5051ms)
★ 영구 거절    **감지된다.** 물어본 뒤 checkPermissions가 "denied"면 영구 거절이다.
             Capacitor가 shouldShowRequestPermissionRationale을 이미 캐시에 적는다
             ⛔ 물어보기 **전** 값은 낡을 수 있다 — 오탐을 실제로 만들어 확인했다
탈출구        문구 **"기기 설정에서 알림을 켜 주세요"** · 토스트만 · 설정으로 안 보냄 ·
             토글은 OFF 유지. 성공 토스트와 **같은 모양**
ads.txt      ✅ 승인을 막는 조건이 **아니다**(문서상 별개 항목 · 필수 아님).
             ✅ UA 가설 사망 — 네 UA가 모두 같은 200/text-plain/같은 ETag
             ★ 「준비 중」 10일은 **정상 범위**다(문서 상한 2~4주)
회귀          284 → **293건** → **314건**(2.122)
설치          세 기기 모두 6ce0225. ⚠ 2.122는 **앱 번들을 안 바꿨다**(바이트 동일)
웹 결함       ⛔ 알림 토글이 모바일 웹에 나가 있었다 — 고쳤다(484ab01 · 2.122)
```

#### 🔴 열려 있는 판단 대기

```
⛔ **Play 콘솔이 막혀 있어** 아래 셋은 지금 판단해도 실행할 수 없다 (2.123)
A2   ✅✅ **닫혔다** (2026-09-04) — 앱 서명 키는 Google이 이미 생성해 뒀고
     「사용 중」이다. 고를 것이 없었다. 2.94의 미확인 전제도 함께 닫혔다
K1   ✅✅ **닫혔다** (2026-09-04) — 업로드 키를 만들었고 **서명된 AAB가 나온다**(2.125).
     지문 AF:23:0D:…:DE:22 · 유효 2054-01-20 · 게이트 둘 통과 확인
     ⚠ 남은 것은 **백업 실행**이다 — 키 파일과 비밀번호를 갈라서 둔다(백업 목록)
B4   Play 기본 언어 (나중에 바꿀 수 있는지 문서가 침묵한다)
C4   아동 대상(TFCD/TFUA) — 콘솔(COPPA)과 코드(initialize 인자) 자리가 둘이다 (2.102)

🔴 **지금 열려 있는 결함** — 판단이 아니라 작업이다
B1   ✅✅ **닫혔다** (2026-09-04 · 2.126) — 경합 조건이었다. 안 1(두 겹) 적용 ·
     재현 절차 넷으로 확인 · 회귀 8건 · 통으로 밟기 항목 1개
B2   ✅✅ **닫혔다** (2026-09-04 · 2.128) — 두 줄을 고쳤다. 485건 전수로 훑어
     같은 병이 둘뿐임을 확인했고, **"드묾은 병이 아니다 · 끝을 붙이는 것이
     병이다"**라는 경계선을 2.128에 남겼다

★ 지금 판단할 수 있는 것
S1   ✅ **정해졌다** — 프레임 밖에 두는 구도(ㄴ)로 간다. ⛔ ㄷ(등록 풀기)는 안 한다
S2   ⏸ 상단 72px 처리와 결과 화면 구도 — **둘 다 만들어 보여준 뒤** 사용자가 고른다
W1   ⏸ **DailyVerse를 컴파일 시점에 접을 것인가** (2.122 마지막 절)
     ✅ **지금 결함이 아니라는 데 사용자가 동의했다** — 그려질 길이 없다.
     ⚠ 다만 "나중에 세팅하는 길이 하나 더 생기면 그날 웹에 그려진다"는
       성질이 남아 있고, 그것을 문서에 남기기로 했다. 목록에 그대로 둔다
C5b  세금 정보 — AdSense 연동 화면에 없었다. 어디에 있는지 (2.102)

✅ 닫힌 것
2.114  배너를 배경면 중앙에 — 2.118 안 2가 대신 풀었다
2.119a 토스트 마무리 넷 (2.120)
2.119b 영구 거절 뒤 탈출구 — 토스트로 알린다. 설정으로 보내지 않는다 (2.120)
2.121  ★ **ads.txt 「승인됨」** — 기다린 것이 맞았다. ⛔ apex 가설도 닫혔다

🔴 출시 후 항목
· **배너 채움률** — 지금은 시험 광고라 알 수 없다. 스토어 연결·준비성 심사가
  끝나야 실제 채움률이 나온다. ⛔ 그 전 수치로 판단하지 말 것
· 가로에서 배너가 왼쪽으로 치우친다 — 플러그인이 좌우 마진을 showBanner 때
  한 번만 계산한다. 고치려면 광고를 새로 요청해야 한다 (2.118)
· **R8**(코드 축소·난독화) — 아직 손대지 않았다. 켜면 플러그인 리플렉션이
  깨질 수 있어 출시 뒤 별도로 본다
```

#### ⏸ 기다리는 것 — ⛔ 할 일이 아니다. 시점이 오면 본다

```
✅ Play 지원팀 답변      **더 기다릴 것이 없다** — 계정 확인 순환이 풀렸다(2.123)
① AdSense **사이트 승인**    리뷰 요청 2026-08-24 17:07 · 화면 안내 "며칠 ~ 2~4주"
                          → **09-21경까지 범위 안이다.** 그 전에 조바심 내지 말 것
                          ⚠ 웹 쪽에 남은 것은 이것 **하나**다 (ads.txt는 닫혔다)
                          ⛔ 「동의 메시지 만들기」(CMP)에 손대지 않는다 —
                            유럽 출시를 결정할 때 볼 항목으로 이미 보류했다
② 알림 지연 **14일 관찰**    태블릿(갈래 A)에서 돈다.
                          ⚠⚠ **기준선이 2026-09-03에 다시 그어졌다** — 그날
                            재설치로 예약이 지워졌다 다시 잡혔다(사용자 승인).
                          ⛔ **2026-09-17 전에 결론을 적지 말 것**
✅ 웹 배포 반영            **끝났다** — run 79 (2026-09-04 00:11Z · 484ab01).
                          배포본에서 role:"switch" **0건** 확인
```

#### ⏸ 멈춰 둔 것 — **출시 뒤로** 미룬다

```
· **채널 발굴** — allowlist 22, 하한 15까지 여유 7이라 급하지 않다
· **탭에 안 맞는 영상** — 제3범주 필터·안 E 적용 뒤 지표가 안정적이다(2.80~2.82)
⛔ 둘 다 데이터 품질 작업이고, 지금 할 일(등록정보·스크린샷·첫 AAB)과 무관하다.
  ★ 병목을 비켜 두고 이쪽을 하면 출시가 그만큼 늦어진다 — 그래서 미룬다
```

#### 🖥 기기 상태 (2026-09-04 세션 종료 시점 · **실측**)

> ⚠ 상세 제원과 **에뮬레이터를 다시 띄우는 명령**은 3-부록 「실행 환경」에 있다.
> ★ **이 PC에만 있는 것**(백업해야 할 것)은 3-부록 「백업 목록」에 있다.

```
휴대폰   Galaxy Z Flip (SM-F700N · R39N500G7YF) · Android 13 · **갈래 C**
        ✅ 붙어 있다 · 권한 granted · 알람 예약 있음 · 시험 기기 등록 완료
        ⚠ 배터리 세이버 켜짐(low_power=1) — **원래 상태다**(2.116 ①)
        ⚠ 설치본은 **2.126을 고치기 전 빌드**다. 배너 동작을 보려면 다시 넣는다
태블릿   Galaxy Tab S9 FE (SM-X510 · R54X204JA8V) · Android 15 · **갈래 A**
        ✅ 붙어 있다 · 권한 granted · **예약 14건** · low_power=0
        ⚠ 설치본은 **2.126을 고치기 전 빌드**다. ⛔ 재설치하면 예약이 지워진다
        ⏱ 알림 지연 14일 관찰 — ⚠ 기준선 **2026-09-03**. ⛔ 09-17 전에 결론 금지
에뮬     pym_phone_360 (emulator-5554) · Android 15 · **갈래 B**
        ✅ 켜져 있다 · 권한 granted · **예약 14건** · 알림 시각 **09:00으로 되돌렸다**
        ⚠ 설치본이 **debug 빌드**이고 **2.126을 고친 판**이다(2026-09-04 16:08 빌드).
          ★ [banner] 로그가 나온다 — 릴리스는 안 나온다
        ⚠ 세션이 `am kill`로 여러 번 찬 시작을 만들었다(⛔ force-stop 아님).
          예약 14건은 살아 있고 **알림 시각은 09:00으로 되돌렸다**
        ⚠ 조사 중 알림을 두 번 받았다(07:24 · 07:30) — 시각을 잠깐 옮겼던 것이다

✅ 세션이 바꾼 것은 되돌렸다
   svc power stayon → false · 알림 시각 → 09:00 ·
   권한 플래그·Capacitor 캐시 → 조사 중 바꾼 것은 마지막 설치로 초기화됐다
⚠ 스크래치패드(스크린샷·시험 스크립트)는 **컨텍스트와 함께 사라진다.**
  다시 필요하면 2.126·2.127의 절차대로 재현해서 찍는다
```


---

### (이력) 2026-08-31 시점의 정리 — 위 블록이 최신이다


> ⚠ 위 경고를 다시 읽을 것 — **이 절은 착수 지시가 아니다.** 상태를 보고하고 기다린다.

광고가 끝나 **자동으로 다음이 되는 일은 없다.** 아래는 물어보면 답할 수 있게
정리해 둔 것이고, 무엇을 할지는 사용자가 정한다.

```
★ 안드로이드 출시    **여기가 지금의 주 전선이다** (2.90~2.94 · 2026-08-31).
                  ✅ 된 것 — 래핑 1단계 · 아이콘 · 구절 알림 · 알림 소리/진동
                  🔴 남은 것 — 서명 키 · 스토어 등록정보 · 스크린샷 ·
                    데이터 안전 신고 · 내부 테스트 트랙
                  ⚠ 채널 설정(importance·sound·vibration)은 **출시 전에 확정해야 한다.**
                    출시 뒤에는 기존 사용자에게 영영 반영되지 않는다 (2.94)
                  ⚠ 스토어 설명·스크린샷에서 **오프라인 동작과 알림을 드러낼 것** —
                    심사자가 '웹과 뭐가 다른가'를 묻는 자리다 (2.54 c)
심사 대기 중       AdSense 검토 결과. 승인되면 AD_SLOT 한 줄 (2.55 ⑩)
🔴 판단 4건        Play Console 등록 · 앱 서명 키 · 아동 대상 설정 · iOS 출시 여부
                  (아래 출시 준비 표 · 서명 키 판단 자료는 13절에 있다)
✅ 닫힘            "앱 N건" = 선언 수로 확정 (2.55 ⑪ · 2026-08-25)
                  운영자명은 2.57 ⑤에서 확정됐다 — 위 3건 표기가 낡아 고쳤다
⚠⚠ 최우선 관찰    **Actions 지연 악화 — 상세는 2.78절.**
                  21~42분 → 3시간 28분 → **10시간 25분**. 08-27 21:30Z
                  슬롯은 3시간 가까이 미실행이었다.
                  · 12시간 등간격이 무너졌다(두 슬롯 간격 1시간 35분)
                  · ✅ 주간 진단이 건너뛰어지던 잠복 위험은 **고쳤다**(2.79)
                    — 예약 발동 회차 + 경과일 8일 안전망. 지연 자체는 그대로다
                  ⛔ 예약 시각을 성급히 앞당기지 말 것(2.78 ④) — 지연 분산이
                    21분~10시간이라 상수로 볼 근거가 없다
✅ 닫힘            제3범주 폴백 제외 — 구현·첫 실전 확인 완료(2.75·2.77).
                  63건 제외 · 폴백에서 100% 제거 · 48/48 유지 · MUST_KEEP 4건 잔존
                  ⚠ 단 **중복 노출은 안 줄었다**(19→22화면) — 원인은 unknown
                    판별 실패다. "부수적으로 준다"던 예상이 틀렸다(2.77 ②)
✅ 닫힘            형식 판별 — **안 E 적용 완료**(2.80 ①). sermon_min 1800→1200.
                  unknown 79→26 · 중복 노출 61→15회 · 최대 확산 18→9화면
                  ⚠ 남는 위험: 사전에 없는 제3범주 2건 (새 프로그램마다 는다)
✅ 닫힘            unknown 양쪽 탭 제외 — **적용 완료**(2.82 ②).
                  탭 정확히 20 · 중복 노출 0/24 · 주제분 −6뿐
                  ⚠ unknown_ratio가 선행 지표다. 오르면 손해도 커진다
⛔ 기각            길이 하한 600 하향(2.82 ③). 하향은 worship→sermon이 아니라
                  worship→unknown이다. 300s에서 풀리는 20건 중 18건이 진짜 찬양이고,
                  [9] 적용 후에는 그것이 전부 화면에서 사라져 순수 손실이 된다
                  ⛔ "설교가 찬양으로 잘못 분류된다"를 근거로 다시 낮추지 말 것 —
                    그 문제는 안 A′로 푼다
✅ 닫힘            안 A′ — **적용 완료**(2.83 ①). 설교자 크레딧 구간을 sermon 신호로.
                  판정 변화 2건(사용자가 지목한 3:00·9:59) · 오탐 0 ·
                  48/48 탭 20건 그대로 · unknown 27건 그대로.
                  ★ 근거가 바뀐 것은 28건 — 길이 규칙에 기대던 26건이 구조로 옮겨갔다
                  ⛔ 2.5순위(장절 뒤·채널 앞)다. **1순위 앞으로 올리지 말 것**
✅ 닫힘            프로그램명 오인 — **단독 '워십' 제거로 4건 교정 완료**(2.81 ①).
                  오탐 0 · 정탐 손실 0 · 찬양 탭 건수 변화 없음
✅ 닫힘            **형식 오분류 7건 중 6건 교정**(2.89). 안 A(곡명 나열) ·
                  안 B('찬양' 어휘 좁히기) · 안 C(제3범주를 주제분에도).
                  판정 변화 8건 / 1,927건 · 오탐 0 · 48/48 탭 20건 그대로
                  ⛔ 안 A의 자리는 **동점 뒤·장절 앞**이다. 1순위 앞으로 올리지 말 것
                  ⛔ 곡명 임계는 3이다. 2로 내리면 날짜·괄호가 곡명으로 잘린다
✅ 닫힘            제3범주 필터 경계 — **주제분에도 적용**(2.89 ③, 2.77 ④를 닫는다).
                  "주제어가 걸렸어도 인터뷰는 인터뷰다"가 사용자 판단이다
⛔ 못 고침          "새해, 넘어져도 다시 일어나는 회복의 비밀"(256s)은 제목에 형식
                  신호가 하나도 없다. 감수한다 (2.88 ④)
                  ⛔⛔ 이 건을 근거로 worship_max_seconds를 낮추지 말 것 —
                    길이로 worship 확정된 47건 중 진짜 찬양이 25건이고,
                    unknown 격리 이후로는 그것이 전부 순수 손실이 된다
⚠⚠ 관찰 항목       **2.89의 오탐 0은 7일치 코퍼스(1,927건)에서 잰 값이다**(2.89 ⑥).
                  확인되지 않은 것 셋 — ① 다른 채널이 '/' 관행을 쓰기 시작할 때
                  안 A의 반응 ② 안 C는 새 프로그램마다 사람이 사전에 넣어야 하고
                  안 하면 조용히 쌓인다(경보 없음) ③ 계절성 제목(부활절·성탄절 특집)을
                  못 봤다
                  ⛔ **배치 3~5회 뒤에 볼 것** (기준선은 2026-08-28 07:49 실측):
                    unknown_ratio 0.012 · media_types(sermon 1088 · worship 660) ·
                    **reason: songlist 8건** · reason: speaker 27건 ·
                    WORSHIP_MUST_NOT 게이트 실패 여부
                  ⚠⚠ **매치 수 ≠ 근거 수.** songlist는 규칙에 **매치**되는 제목이
                    117건이지만 실제 **근거**가 되는 것은 8건이다 — 안 A가 1.6순위라
                    대부분 1순위에서 이미 확정되기 때문이다.
                    ⛔ 117과 비교하지 말 것. 오히려 그 값에 가까워지면 규칙이
                      1순위 앞으로 올라간 것을 의심해야 한다 (상세는 2.89 ⑥)
✅ 확인            **배치 실전 확인 완료**(2.87 · `run 33143782965`). 폴백 무작위와
                  주제분 사다리가 함께 처음 돌았고 로컬 예측과 거의 일치했다.
                  ★ tab_max_exposed 21→20 · tabs_over_cap 10→0 (2.82 ② 실전 확인)
                  ★ 폴백 고유 39→225건 · 같은 영상이 평균 8.59→1.45개 화면
                  ⚠ 말씀 겹침만 예측(2.6%)과 어긋난 4.5% — 코퍼스 차이이고
                    같은 코퍼스로 재현된다. 예측을 평균 하나로 좁혀 말한 것이 잘못
✅ 닫힘            **폴백 90일 컷 + 화면별 무작위 적용 완료**(2.84). 겹침이 풀렸다.
                  48/48 탭 20건 유지 · 겹침 말씀 100%→2.6% · 찬양 100%→38%
                  나이 중앙값 1~2일 → 13~17일 (상한 90일)
                  ⛔ 컷과 무작위는 짝이다. 하나만 되돌리면 둘 다 무의미해진다
                  ⛔ 60일로 내리지 말 것 — 찬양 채널 여유 4개 · 겹침 44%
⚠⚠ 채널 발굴로 연결  **찬양 겹침 38%는 알고리즘의 바닥이다**(2.84 ⑥).
                  90일 안에 재고 3건 이하인 찬양 채널 6개(CGN·CBS설교·광주극동·
                  C채널·CBSJOY·극동방송)가 무는 9자리가 어느 화면에나 들어간다.
                  말씀은 15채널 전부 재고 ≥3이라 2.6%다 — **차이는 규칙이 아니라 재고다**
                  ★ 아래 '3차 채널 발굴'에서 **찬양 계열**을 우선할 근거가 이것이다
                  ⚠ 여유 5채널. 기여 큰 찬양 채널 5개를 더 잃으면 20을 못 채운다
✅ 확정            폴백 헤더 문구 "요즘 올라온 것들" **그대로 둔다**(2.84 ⑨).
                  ⛔ 기간을 명시하는 문구로 바꾸지 말 것 — 무게 중심은
                    "딱 맞는 건 아니지만"이고 정직성 주장은 신선도가 아니다
✅ 닫힘            **주제분 신선도 사다리 적용 완료**(2.86). 90일 먼저, 못 채우면 전체.
                  주제분 622→633 · 폴백 35.2%→34.1% · 주제분 0인 탭 1개 그대로
                  나이 75분위 88일→53일 · 1년 초과 영상 보이는 탭 21→14/48
                  신고 건 anger.irritation [찬양] 950일 → 86일
                  ⛔ **하드 컷으로 바꾸지 말 것** — 주제분 0인 탭이 1→6개가 되어
                    "이 마음에 맞춰 고른 영상" 절이 사라지는 화면이 생긴다
                  ★ "48/48 유지"는 안전 신호가 아니다 — 폴백이 무엇이든 메운다
⚠ 정정됨           2.81 ⑤ "날짜 컷을 넣지 말 것"은 **부분적으로 뒤집혔다**(2.81 ⑤ 정정).
                  근거였던 "얇은 채널이 통째로 빠진다"가 실측에서 성립하지 않았다.
                  ⛔ 다만 **주제분 하드 컷 금지는 여전히 유효**하다
✅ 닫힘            길이 규칙 오분류(10분 이하 설교 클립이 찬양으로 확정) —
                  **안 A′가 그 2건을 잡았다**(2.83 ①). 2.73 ⑧에서 연 별건이다
✅ 닫힘            분류 실패에서 입력창 비우기 — **실제로는 안 되고 있었다.**
                  배포는 정상이었고 `RESULT.CATEGORY`(대분류만 맞은 경우) 경로에
                  비우는 코드가 아예 없었다. 배포 번들 원문으로 확인 (2.83 ②)
                  ★ 상태 기계를 `app/src/lib/flow.js`로 꺼내 고쳤고,
                    회귀를 **소스 문자열 검사 → 그래프 순회 검사**로 바꿨다
                  ⛔ 화면 이동을 App.jsx에 낱개 세터로 다시 만들지 말 것
✅ 닫힘            탭 노출 20 초과 → **D안(경보) 채택·구현 완료** (2.72)
                  ⛔ B·C는 기각됐다. 20으로 맞추려면 A뿐이고 사용자가
                    비용을 치르겠다고 말하기 전엔 착수하지 않는다
그다음 후보        ★ 주제 재배정(아래) · Capacitor 래핑 · 3차 후보 추가 승인 ·
                  4차 발굴 재개 판단
```

---

#### ✅ 닫힘 — 제3범주 폴백 제외 (2.73 ⑦ → 2.74 → 2.75)

'가(폴백 제외)'로 결정·구현했다. **48/48 탭이 20건을 유지하는 것을 적용 후
재확인했다.** 상세는 2.75, 방법론은 2.76.

⛔ **아래 옛 경고는 오류였다.** 실측으로 반증됐다(2.74 ①) — 남겨 두는 것은
같은 혼동을 다시 하지 않기 위해서다.

> ~~'가'는 2.64·2.66과 정면 충돌한다. 빼면 주제분 0인 탭이 20을 못 채운다.~~

**후보 수(1,148건)와 실효 재고를 혼동한 것이다.** 폴백 실효 천장은
`THEME_MAX_PER_CHANNEL(3) × 그 탭에 보이는 채널 수`이고, 57건을 빼도 채널이
21개 그대로면 천장이 안 움직인다. **폴백 공급을 논할 때는 후보 수가 아니라
채널 수를 봐라.** 한계점은 채널 10개 미만이다.

**남은 관련 별건 — 길이 규칙 오분류**(2.73 ⑧, ⏸ 미착수): 10분 이하 설교 클립이
찬양으로 확정된다(현재 2건 · 4%). 확정 오분류라 사용자가 [찬양]에서 설교를 본다.
안 A′(말미 `- OOO 목사`)를 길이 규칙 앞에 두면 일부가 막힌다 — 함께 볼 것.

---

#### ★ 그다음 후보 — 주제 재배정 검토 (2.71 ③)

세 화면의 [말씀] 탭이 주제분 0·1·1로 폴백에만 의존한다. **공급이 아니라 배정
문제다** — 배정된 주제가 찬양 편중이거나 비어 있다.

```
anger.irritation   self_control 0(critical) · quiet_worship 246(전부 찬양)
exhaustion.tired   rest 3            · quiet_worship 246
calm.ease          creation 2        · quiet_worship 246
```

`media_type_gap: quiet_worship에 sermon 0건`이 이 상태를 정확히 가리키고 있다.

⚠ **착수 전에 [5] 주제 집합 분리(2.32절)와의 충돌을 반드시 확인할 것.**
2.32는 "동일 목록 5쌍 → 0쌍"을 만들려고 주제 배정을 손본 작업이다. 배정을 다시
건드리면 **그 성과가 되돌아갈 수 있다.** 특히 joy 3쌍은 2주제로 갈라 하나가
합집합을 갖는 구조라 여유가 없다. 재배정안을 만들면 먼저 동일 목록 쌍 수를
다시 재고, 늘어나면 그 안은 버릴 것.

⚠ 대안 축도 같이 볼 것 — 배정을 바꾸는 대신 **`quiet_worship`에 sermon이 들어올
길을 여는 것**(주제 어휘 보강 또는 말씀 채널 보강)이 2.32와 충돌하지 않는다.
어느 쪽이 싼지는 재보고 정한다.

---

#### 그다음 — Capacitor 래핑

출시 준비 6번. **광고 2단계와 묶여 있다**(2.54절).
착수 시 `server.url`·`WebView API for Ads`·"앱이 웹보다 더 하는 것"을 함께 정해야 한다.
⚠ `server.url`을 실제 사이트로 두면 **오프라인 첫 실행 전략과 충돌한다** — 그때 점검할 것.

---

#### 그다음 — 3차 채널 발굴 후속

발굴은 **끝났다**(2.46절, 761 units). 승인 1건(C채널방송:설교)까지 했고 배치도 돌렸다.
남은 것은 **판단**이다.

```
승인 대기 후보   시트가 로컬에 있다 — channel_candidates.theme.20260824-014724.md
                ✅ 시트의 승인 후보는 **전부 처리됐다** (allowlist 22)
                   찬양 3곳(2.57) · 말씀 3곳(2.61) · C채널 계열 3건(승인 1 · 제외 2)
                🔴 남은 것은 두 교회의 교단명뿐 — 지어내지 않고 비워 뒀다(2.61 ③)
                절차와 형식은 channel_criterion_c.20260824.md
공급 문제        self_control 0 · rest 3 · creation 2 · quiet_worship 246 · 위기 채널 0
                ⚠ 수치는 배포본 data/diagnostics.json 의 theme_pool 에서 읽는다
                  (위는 2026-08-26 배치 기준으로 갱신했다)
                ✅ **2.61 ① 답 나왔다** — 말씀 3곳 승인 + 탭당 20의 결과를
                  2026-08-26 배치에서 읽었다(2.71). [말씀] 탭은 4~7건대를
                  **벗어났다 — 48/48 탭이 20건 이상**이다.
                  ⚠ 다만 세 화면(anger.irritation·exhaustion.tired·calm.ease)은
                    주제분이 0·1·1이고 나머지가 전부 폴백이다. 건수는 찼지만
                    "주제와 무관한 것으로 찼다" — 별도 과제로 넘어갔다(2.71 ③)
4차 발굴        ⏸ **보류다.** "분노를 다루는 기독교 콘텐츠가 한국 유튜브에 실재하는가"에
                먼저 답한다 — 같은 결론이 두 번 나왔으므로(2.28 · 2.46)
                검색어를 더 넣는 것은 세 번째 실패가 될 가능성이 높다
```

---

### 문서가 코드보다 뒤에 서는 문제 — 사례 5건과 🔴 재발 방지 제안 (2026-08-31)

> ⛔ **제안이다. 구현하지 않았다.** 사용자 승인 뒤에 만든다.

**같은 병이 다섯 번 나왔다.** 전부 "코드가 앞서가고 문서가 안 따라간 것"이고,
증상은 하나다 — **다음 사람이 이미 끝난 것을 미결로 읽는다.**

```
① 출시 준비 표 #6      "미착수"로 남음. 2.90에서 끝났다              상태 표기
② 13절 앱 래핑 블록     "정해야 할 것" 5개 중 4개가 이미 정해져 있었다   상태 + 값
③ 표 #7 원격 자산      "설계는 있음 · 🟢 작업만". 2.90에서 확정됐다     상태 표기
④ About TODO 3건      "셋 다 비어 있다". 2026-08-24에 채워졌다        ★ 값
⑤ 문의처 이메일        aaz0919@gmail.com. 같은 날 2.56에서 바뀌었다    ★ 값
```

★ **오늘 알림 건이 꼬인 것도 같은 병의 6번째였다.** "채널 진동이 꺼져 있다"는
사실이 문서에 없었고, 있었어도 코드에서 읽어야 알 수 있는 값이었다.

**⚠ 다섯 건이 두 종류로 갈린다 — 이것이 제안의 뼈대다.**

```
값의 불일치 (④⑤)    코드에 실제 값이 있다 → **기계가 잡을 수 있다**
상태의 불일치 (①②③)  "완료/미착수"는 사람의 판단이다 → 기계가 못 읽는다
```

---

**제안 A — 값을 생성물로 바꾼다 (`stats.py`에 두 번째 블록)**

`stats.py --check`가 이미 규모 수치에 대해 이 일을 하고 있다. **구조를 그대로 쓴다.**

```
지금    BEGIN = "<!-- STATS:BEGIN 규모 -->" 하나뿐이다 (stats.py:52)
        _regions() 가 BEGIN/END 구간을 전부 찾아 _apply() 가 갈아 끼운다
바꿀 것  BEGIN을 이름 인자로 일반화한다 — "규모" 와 "앱" 두 블록
새 블록  <!-- STATS:BEGIN 앱 -->  ← 전부 저장소 파일에서 읽는다
          appId · appName          capacitor.config.json
          versionName · versionCode package.json (계산식은 vite.config.js)
          minSdk · target/compile   android/variables.gradle
          권한 목록                 android/app/src/main/AndroidManifest.xml
                                    (⛔ tools:node="remove" 반영해서)
          알림 채널                 notify.js — id · importance · vibration · sound 유무
          알림 예약 id 대역          notify.js ID_BASE · notifySelect.js WINDOW_DAYS
          OPERATOR · CONTACT        About.jsx
```

```
비용    stats.py 약 40~60줄 (측정 함수 + 렌더 + BEGIN 일반화)
        HANDOFF에서 손으로 적힌 같은 값들을 블록으로 대체
얻는 것  ④⑤가 구조적으로 불가능해진다. 게이트가 이미 CI에 있어 추가 배선이 없다
        ★ 오늘 알림 건도 여기 걸린다 — 채널 설정이 문서에 실측으로 박힌다
⚠ 한계   블록 **밖의** 산문은 여전히 손이다. 값을 두 번 적으면 다시 갈라진다
        → 규칙: 이 값들은 **블록에만 적고 산문은 블록을 가리킨다**
```

---

**제안 B — 상태 표기에 역참조 게이트 (①②③용)**

"완료/미착수"는 못 읽지만, **그 말과 모순되는 코드 사실**은 읽을 수 있다.

```
예시   표에 "Capacitor 래핑 | 미착수" 가 있는데 app/android/ 가 존재하면 → 실패
      표에 "원격 자산 범위 | 설계는 있음" 인데 vite.config.js에 capacitor 모드가
      있으면 → 실패
구현   항목별 규칙을 손으로 적어야 한다. 출시 준비 표는 항목이 12개라 감당 가능
비용   새 스크립트 하나 (~80줄) + 규칙 12줄
⚠ 약점  규칙을 적는 것 자체를 잊으면 그만이다. A만큼 구조적이지 않다
       ⛔ 과설계 위험 — 표가 커지면 규칙 관리가 표보다 무거워진다
```

---

**제안 C — 가장 싼 것: 절마다 "언제 낡는가"를 적는다 (게이트 아님)**

```
상태 표기 옆에 근거 커밋/절 번호를 반드시 단다 (지금 부분적으로 하고 있다)
"미착수"·"결정 필요"·"비어 있다" 세 어휘를 쓸 때는 그 반대가 되는 조건을 함께 적는다
  예) "미착수 (app/android/ 가 생기면 이 줄을 고칠 것)"
비용 0. ⚠ 효과도 사람에 달렸다 — 다섯 번 실패한 것이 정확히 이 방식이다
```

---

**✅ 적용 — A와 C를 했다 (2026-08-31 · 사용자 승인). B는 보류.**

```
A   stats.py에 두 번째 블록 「앱」을 넣었다. 3절에 자리가 있고 --check가 게이트다.
    읽는 것  appId · 표시 이름 · 버전/versionCode · SDK 3종 · 권한(선언/제거) ·
            알림 채널(id·importance·vibration·sound) · 알림 id 대역/창 ·
            기본 시각/기본값 · OPERATOR · CONTACT
    ⛔ 커밋된 파일만 읽는다. node_modules·빌드 산출물은 보지 않는다 —
      같은 커밋이면 언제나 같은 답이어야 --check가 게이트로 성립한다
    ★ 만들자마자 값을 하나 틀렸다 — WINDOW_DAYS 정규식이 SEEN_WINDOW_DAYS(30)를
      먼저 물어 "30일 창"이 나왔다(정답 14). **조용히 틀린 값이 문서에 박히는 것이
      바로 이 블록이 막으려던 일이다.** \b를 넣어 고쳤고 주석에 남겼다
C   값은 블록에만 적고 산문은 블록을 가리킨다. 13절 Capacitor 블록에서
    중복 값을 걷어냈다
B   보류. 규칙을 손으로 유지하는 장치라 "규칙을 잊는" 실패로 되돌아온다
```

⚠ **정정** — 앞서 "게이트가 이미 CI에 있어 추가 배선이 없다"고 적었는데 **틀렸다.**
  `stats.py --check`는 CI(build.yml)에 없다 — 로컬 게이트다. CI는 verify_verses ·
  tagging_test · spread_test만 돈다. 🔴 CI에 넣을지는 따로 판단할 것.

---

**🔴 판단 — 무엇을 할 것인가 (2026-08-31 판단 완료. 아래는 그때의 정리다)**

```
권한다  A를 한다. ④⑤ 같은 값 불일치가 **다시 일어날 수 없게** 되고,
       기존 게이트에 얹히므로 배선 비용이 없다
       ★ 오늘 알림 건(채널 설정이 문서에 없던 것)까지 함께 닫힌다
보류    B는 지금 하지 않기를 권한다. 규칙을 손으로 유지하는 장치라
       "규칙을 잊는" 같은 실패로 되돌아온다. A로 값을 닫은 뒤에도
       상태 불일치가 또 나오면 그때 근거를 갖고 판단하는 편이 낫다
같이    C는 A와 함께 한다 — 비용이 0이고 A가 못 덮는 자리를 조금 덮는다
```

---

### 보류 중 — 미루기로 **판단한** 것들 (잊은 것이 아니다)

각 항목에 **다시 열 조건**이 있다. 상세는 괄호 안 절 번호.

| 항목 | 지금 | 다시 열 조건 |
|---|---|---|
| **세션 재개** (2.34절 0항 · 3절 2-B) | 설계 없음. 앱은 여전히 "다시 열면 처음부터"다 | 착수 결정 시. ⚠ 개인정보처리방침과 **묶여 있다** — 저장 항목이 늘면 문안도 바뀐다 |
| **위기 채널 승인** (9번) | 승인 **16개** 전부 `crisis_eligible: false`. 상담 안내 + 구절 10건은 정상 | 정신건강 전문 사역·상담 채널을 실제로 찾았을 때. ⚠ CRISIS_MIN_VIDEOS=12라 1~2개로는 안 찬다<br>★ **따로 찾으러 나갈 필요가 없다** — 발굴 시트의 `위기 후보` 열이 표시한다(2.45절). 3차에서 3개가 걸렸으나 셋 다 목적 불일치였다(2.46절) |
| **영상 설명란 재검토** (11번) | 제목만 본다. FYM에서 오차단 사고가 났던 경로 | 그때의 근거를 뒤집을 실측이 있을 때. `--dump` 확장이 먼저 필요 |
| **문구 12개 확장** (11번) | 세분류당 10건 = 480건 | 실사용에서 문구 반복 신호. ⚠ 유사도 게이트 여유가 **0.0015뿐**이다(0.8485/0.85) |
| **가변 페이징** (2.40절) | 이어서 읽기 고정 4절. 절 길이 4~166자라 페이지 높이가 100~660px | 페이지 길이가 들쭉날쭉하다는 신호. ⚠ 절 번호가 주는 "몇 절씩" 감각을 잃는다 |
| **오프라인 첫 방문** (2.34절) | 장 본문을 못 받는다. 문장으로 말한다.<br>⚠ **광고 2단계와 묶였다** — `server.url`을 실제 사이트로 두면 이 전략과 충돌한다(2.54 a) | "네트워크가 없어도 구절 화면은 떠야 한다"를 **장 본문까지 확장**한다고 판단할 때. 174장 지연 청크(+166KB) 안이 대기 중 |
| **색 위계 미점검 화면** (2.35·2.36절) | **위기·입력·About 화면은 아직 재지 않았다** | 그 화면에서 "위계가 안 잡힌다"는 말이 나올 때. 절차는 2.36절 규칙 5항 |
| **같은 장 반복 잔여** (2.40절) | 시편 119장 ×4(boredom.dull, 12건 중 33%) 외 8화면 각 ×2 | 추가만으로는 못 푼다. 교체를 검토하려면 119편 4건 중 약한 것을 가려야 한다 |
| **4차 채널 발굴** (2.46·2.49절) | ⏸ **의도적으로 보류했다.** 같은 결론이 두 번 나왔다 — 어휘가 아니라 물량 | ★ "분노를 다루는 기독교 콘텐츠가 한국 유튜브에 실재하는가"에 먼저 답한 뒤. 위 진입점 참조 |
| **`bible_story` 사전 보강** (2.48절) | 3차 승인으로 저수확 경보는 해소됐다. 어휘는 `강해` 하나가 주력이다 | 다시 얇아질 때. ⛔ 프로그램명(복음강단·비전메시지)을 넣는 안은 **이미 기각했다** — 프로그램은 주제를 말하지 않는다 |
| **release에서 R8 켜기** (2.98절) | ⏸ **지금 켜지 않는다**(2026-09-02 판단). `minifyEnabled false`가 Capacitor 기본값이라 AdMob SDK가 축약 없이 실린다. release APK 7.71 MiB | ★ **출시 후 크기가 실제로 문제가 될 때.**<br>근거 — 7.71 MiB는 문제될 크기가 아니다. 리플렉션 위험을 안는 대가에 비해 얻는 것이 작고, ⛔ **지금 켜면 광고가 안 나오거나 알림이 안 오는 문제가 생겨도 원인이 R8인지 가리기 어려워진다.** 변수를 하나 더 만드는 셈이다 |
| ✅ **VIBRATE 권한 확인 — 닫혔다** (2.94절 · 2026-09-03) | ~~확인하지 못했다~~ → **휴대폰에서 실측해 닫았다.** VIBRATE 없이 1102ms 울렸고, 기록의 `uid=1000 · opPkg=android`가 **시스템이 대신 울렸다**는 증거다. ⛔ VIBRATE를 추가하지 않는다(사용자 확정) | 진동 되는 기기(휴대폰)를 잡았을 때.<br>★ **출시 차단 항목이 아니다.** 채널과 달리 권한은 굳지 않는다 — 매니페스트 한 줄이라 **앱 업데이트로 나중에 추가할 수 있다.** 채널 설정처럼 재설치를 요구하지 않는다 |
| **플러그인이 만드는 `Default` 채널** (2.94절) | @capacitor/local-notifications가 로드 시점에 **무조건** 만든다(`LocalNotificationsPlugin.kt`의 `load()`). 사용자 설정 화면에 '구절 알림' 옆에 영문 **Default**가 하나 더 보인다. 우리 알림은 전부 `morning_verse`로 나가므로 **쓰이지 않는 채널이다** | ★ **출시 항목이다.** 유령 채널을 거절한 것(2.93)과 같은 결의 문제다.<br>⚠ `deleteChannel`로 지워도 다음 실행에 다시 생긴다 — 플러그인이 매번 만든다. 없애려면 플러그인 패치나 네이티브 개입이 필요하고, 그 값이 맞는지부터 판단할 것 |
| **앱 광고 2단계** (2.54절) | 방향은 AdSense로 확정. **시점만 미뤘다**<br>⚠ **전제가 바뀌었다**(2.90) — server.url을 쓰지 않기로 했다. (a)를 다시 판단해야 한다 | Capacitor 착수 시. (a)`server.url` (b)`WebView API for Ads` (c)앱이 웹보다 더 하는 것 — 셋을 갖춘 뒤 켠다 |
| **3차 후보 승인 판단** (2.46·2.47·2.57·2.61절) | ✅ **완료 — allowlist 16→19→22.** 찬양 3곳(택피아노·mini Music·Worship Flow, 2.57) + 말씀 3곳(분당우리교회·갓피플TV·사랑의교회, 2.61).<br>⚠ C채널 계열은 **이미 전부 처리돼 있었다** — 설교는 승인됨, 굿데이·TV는 기준 3으로 제외(2.61 ②).<br>🔴 남은 것은 두 교회의 **교단명**뿐이다 — 저장소에 1차 자료가 없어 비워 뒀다 | 교단명이 확인되면 `affiliation` 한 줄씩. 제외 2건을 되살리려면 **기준 3에 대한 판단**이 따로 필요하다(이단 문제가 아니었다) |
| **GDPR·CMP 문구** (2.55 ⑨) | ★ **재료가 생겼다(2.97 ②)** — AdMob을 붙이면 구글 인증 CMP인 `user-messaging-platform`(UMP) SDK가 **이미 앱에 들어온다.** ⚠ 들어오는 것과 구성하는 것은 다르다(동의 양식은 콘솔에서 만들어 코드로 불러야 한다). 그래도 "구축"의 비용이 크게 준다.<br>⛔ **방침에 넣지 않기로 확정했다.** CMP 없이 문구만 넣으면 방침이 하지 않는 일을 말하게 된다. 미구축은 위반이 아니라 EEA·영국 광고 제한일 뿐이다 | ★ **유럽 출시를 결정할 때.** 그때는 문구가 아니라 **인증 CMP 구축**이 먼저다 |

---

### ★ 확정된 예외 — 다시 시도하지 말 것 (2026-08-20 확정)

**비시편 확대의 한계선** (2.39 · 2.41절 · `verses.yaml` 상단에도 기록)

```
calm.stable   91%   "평안히 가라"는 작별 인사, "그 땅이 평안하니라"는 정치적 평온
calm.ease     82%   개역한글의 `안식`은 압도적으로 **율법 규례 용어**다
joy.delight   77%   몸짓 축(뛰놀며·춤추며)의 비시편 구절이 전부 웃사의 죽음·
                    미갈의 업신여김·금송아지·심판 문맥이거나 같은 장 중복을 만든다
joy.grateful  75%   감사는 서신서에 많지만 명령형이거나 연보 문맥뿐이다.
                    1인칭 감사 고백은 시편의 장르다
```

> **시편 몰림은 큐레이션의 게으름이 아니라 정경 장르 분포의 결과다.**
> 서신서 calm 어휘 16건이 한 건도 예외 없이 편지 인사·명령형 권면·교리 서술이었다.
> **억지로 채우면 품질이 떨어진다** — 아래 15건 중 하나를 넣는 것이 된다.

**품질 때문에 못 넣은 15건** (근거 전문은 2.41절)

```
번영신학 인접      고후 9:15 (9장 전체가 연보 독려)
지배 어휘 겹침     살전 3:9(보답할꼬) · 대상 13:8(힘을 다하여 + 9-10절 웃사의 죽음)
기존 수록 중복     골 3:16 · 행 2:28(시 16:11 인용) · 눅 6:21(팔복 병행구)
같은 장 중복 유발   렘 31:13 · 사 12:3
절 단위 함정       느 8:10(109자·핵심이 맨 끝) · 민 14:18 · 사 53:4
질책·요구         잠 14:29/15:18/16:32/19:11 · 욜 2:13 · 느 9:31 · 히 13:15 · 롬 12:15
장 문맥           요 15:11 · 눅 15:32
```

⚠ 하나를 다시 후보로 올리려면 **그 사유가 왜 더 이상 유효하지 않은지**를 먼저 적을 것.
⛔ **"비율이 아직 55%대다"는 다시 열 조건이 아니다.** 비율은 결과이지 목표가 아니다.

---

### 스토어 출시 준비 — 항목별 상태와 **판단이 필요한 것** (2026-08-24 갱신)

> 표기 규칙 — `🔴 판단 필요`는 사용자가 정해야 진행되는 것이고,
> `🟢 작업만`은 정해진 것을 만들기만 하면 되는 것이다.

| # | 항목 | 상태 | 판단 |
|---:|---|---|---|
| 1 | About "영상" 문구 개정 | **완료** — 문구 교체 + 회귀 2건 (아래 1절) | — |
| 2 | 이메일 노출 점검 | **완료 — 확정 문의처 1건만** (아래 2절 · 2.56 ②).<br>⚠ "0건" 전제는 문의처를 넣으며 의도적으로 바뀌었고, 이제 게이트가 지킨다 | — |
| 3 | 광고 도입 | **웹 구현 완료** — 320×100 · 3개 · 자리 규칙 · 회귀 31건(2.55).<br>앱은 AdSense를 나중에(2.54) | — (승인 후 `AD_SLOT` 한 줄) |
| 4 | 개인정보처리방침 페이지 | **완료** — 배포됨 · 문의처 채움 · About에서 링크 | — |
| 5 | 제작자 · 연락처 | **완료** — `CONTACT` `appleap385@gmail.com` (앱·방침 동일) · `OPERATOR` "개인 개발자가 만들어 운영합니다"(2.57 ⑤).<br>둘 다 비지 않았는지 회귀가 지킨다 | — |
| 8 | 기록 삭제 버튼 | **완료** — About 화면 · 회귀 3건 | — |
| 9 | `ads.txt` | **완료** — 유저 사이트 레포 루트.<br>⚠ gh-pages 루트가 아니다 — 도메인 루트에서만 읽힌다(2.55 ⑤) | — |
| 10 | AdSense **아동 대상 설정 확인** | **방침 쪽 완결** — 5절이 "아동 대상 아님"으로 명시.<br>AdSense 계정 설정만 미확인 | 🔴 ★ 계정 설정이 방침과 어긋나면 **그 자체가 문제다**. 고칠 것은 방침이 아니라 설정 쪽이다 |
| 6 | Capacitor 래핑 (안드로이드) | **완료 — 1단계**(2.90 · 2026-08-31). 번들 실행 · 앱 빌드 분리 · AdSense 물리적 제거 · 아이콘(2.91) · 구절 알림(2.92~2.94).<br>⛔ **`server.url`을 쓰지 않기로 했다** → 광고 2단계의 전제가 바뀌었다(2.54 a를 다시 판단해야 한다) | 🔴 iOS도 낼 것인가 — 안드로이드와 **독립 판단**이다 |
| 7 | 원격 자산 범위 | **확정 — 2.90.** 번들 = 앱 셸·verses·taxonomy·seed-videos·**krv 178장(942KB)** / 원격 = `data/videos.json` · `version.json` **둘뿐** · 서비스워커는 앱에서 끈다.<br>★ krv가 APK에 들어가 **오프라인 첫 실행에서도 이어서 읽기가 열린다** | 🔴 남은 것은 **고지 문구 하나** — 13절의 ⚠를 그대로 옮기지 말 것("하루 한 번"도 "두 번"도 아니다) |
| 13 | **Play Console 개발자 계정** | 🔴 **미등록 — 등록 착수 예정.** ⚠ 이 문서에 기록이 **0건이었다**(2026-08-31 발견).<br>등록비 US$25 1회 · **본인확인(신분증·주소)에 며칠 걸릴 수 있다** | 🔴 ★ **A갈래 전체의 선행 조건이자 병목이다.** 계정이 없으면 서명 키도, 내부 테스트 트랙도, 데이터 안전 신고도 **제출할 곳이 없다.** 등록을 먼저 걸어 두고 나머지를 병행하는 것이 순서다.<br>⚠ 등록 과정에서 **Play 앱 서명 선택**이 나온다 — 아래 「앱 서명 키」 절을 먼저 읽을 것 |
| 14 | **앱 서명 키** | ✅✅ **앱 서명 키는 닫혔다 (2026-09-04)** — 콘솔에서 「사용 중」이고 **Google이 이미 생성해 뒀다.** 판단 1이 자동으로 실현됐다.<br>🔴 **업로드 키는 미생성.** 콘솔의 업로드 키 인증서 절이 비어 있고 첫 AAB 업로드로 등록되는 구조다.<br>🔴 **판단 2 미결 — 보관처.** ⏸ 2026-09-04 설계안을 냈고 **승인 대기 중**이다 | ★ **잃으면 되돌릴 방법이 없다** — 같은 패키지명으로 영영 업데이트를 못 올린다. 채널 불변성(2.94)과 같은 종류의 비대칭이고 **더 무겁다.**<br>⛔ **Actions Secret은 백업이 아니다** — 넣고 나면 **다시 읽을 수 없다.** 원본을 지우면 그것으로 끝이다. 보관처를 정할 때 이 줄을 먼저 읽을 것 |
| 12 | 도메인 루트 페이지 | **완료** — `appleap385-aaz0919.github.io` 레포 신설·Pages 켬(2.55 ⑤) | — |

⛔ **출시 준비는 공급 문제와 독립적이다.** 영상 풀이 얇아도 앱은 정상 동작하므로
   "채널이 더 모이면 그때"로 미룰 이유가 없다. 순서는 사용자가 정한다.

---

#### 1. About "영상" 문구 — ✅ 완료 (보증 어휘를 빼되 침묵하지 않았다)

**현재 문안** (`app/src/components/About.jsx` 영상 절 첫 문단)

```
사람이 확인해 승인한 채널의 영상만 담습니다. 검색으로 모으지 않으며,
앱이 유튜브에 직접 묻지도 않습니다. 영상을 누르면 유튜브로 이동합니다.
```

**★ 판단 — "아무 말도 안 한다"보다 "다르게 말한다"가 낫다.**

```
아무 말도 안 하면    "이 앱은 영상을 어떻게 고르는가"에 답이 없어진다.
                   이단 채널 오해가 생겼을 때 **화면에서 댈 근거가 사라진다** —
                   allowlist는 저장소에 있지 사용자가 보는 곳에 없다.
                   그리고 "검색으로 모으지 않는다"는 사실은 오해를 **가장 빨리
                   푸는 문장**이다. 무작위 수집이 아니라는 것이 요지이기 때문이다.
다르게 말하면        문제는 "승인"이라는 낱말이 **품질·정통성 보증**으로 읽히는 것이다.
                   실제로 하는 일은 "채널 목록을 미리 정해 두고 그 안에서만
                   가져온다"이고, 그것은 보증이 아니라 **사실 진술**이다.
                   보증 어휘만 빼고 메커니즘은 남긴다.
```

**제안 문안**

```
영상은 미리 정해 둔 채널 목록에서만 가져옵니다. 검색으로 모으지 않으며,
앱이 유튜브에 직접 묻지도 않습니다. 영상을 누르면 유튜브로 이동합니다.
```

```
뺀 것    "사람이 확인해 승인한"   → 보증·심사 주장을 없앤다
남긴 것  "미리 정해 둔 목록에서만" → 무작위 수집이 아니라는 사실
        "검색으로 모으지 않으며"  → 오해를 가장 빨리 푸는 문장
        "앱이 유튜브에 직접 묻지도 않습니다" → 개인정보 측면에서도 유효하다
```

⚠ 둘째 문단(저작권·면책)은 **그대로 둔다.** "영상의 내용은 각 채널의 견해이며
  이 앱의 입장과 같지 않을 수 있습니다"가 이미 입장 분리를 하고 있고,
  첫 문단에서 보증 어휘를 빼면 그 둘이 서로 어긋나지 않게 된다
  (지금은 "승인했다"와 "우리 입장이 아니다"가 미묘하게 충돌한다).

**✅ 적용 완료 (2026-08-24)** — 위 제안 문안으로 교체했고 회귀 테스트 2건을 붙였다.

```
app/test/verses.test.js
  "영상 절에 보증 어휘가 없다"       승인·심사·검증·인증·보증이 채널을 수식하면 실패
  "어떻게 고르는지는 여전히 말한다"   "미리 정해 둔 채널 목록"·"검색으로 모으지 않으며"가
                                 사라지면 실패 — **침묵도 회귀로 본다**
고장 주입으로 확인했다 — 옛 문구로 되돌리면 두 검사가 모두 걸린다
앱 테스트 110 → 112
```

⚠ **개정 전에는 이 문구를 고정하는 검사가 하나도 없었다.** 그래서 바꿔도 게이트가
  걸리지 않았고 되돌아와도 걸리지 않았다. 문안이 책임 범위에 닿는데 방어가 없었다.

---

#### 2. 이메일 노출 점검 — 완료 (2026-08-24)

```
배포본(gh-pages 전체)   0건   ★ 실제 노출면은 깨끗하다
추적 파일               2건
  HANDOFF.md:22        appleap385@gmail.com — 커밋 author를 적어 둔 문서 기록
  About.jsx:38         CONTACT = ""  주석의 예시가 pym@example.com (가짜)
커밋 author            80건 전부 appleap385@gmail.com
회사 이메일             0건 — 첫 커밋 amend(1절 기록)가 유효했음이 확인됐다
```

⚠ HANDOFF의 gmail 표기는 **의도적으로 남긴다.** 저장소를 새로 clone하면 author를
  다시 지정해야 하고, 그 값이 무엇인지 적혀 있어야 한다(1절).
⚠ 출시 시 `CONTACT`에 실제 주소를 넣으면 **그때부터 앱 화면에 공개된다.**
  개인 주소를 쓸지 서비스 전용 주소를 만들지가 5번 항목의 판단이다.

---

#### 3. 인피드 광고 — 실측과 정책 (2026-08-24 도입 결정)

**★ 결정이 바뀌었다.** 같은 날 앞서 "출시 시점에 넣지 않는다"고 적었으나
**인피드 광고를 넣기로 확정**했다. 자체 팝업이 정책 위반이라는 판정(아래)은 그대로다.

**실측 — 360px 뷰포트, 헤드리스 크로미움으로 잰 값**

```
콘텐츠 폭        320px   ★ AdSense 320 규격과 **정확히 일치한다** (좌우 여백 20px씩)
항목 간격        16px    (list의 grid gap)
썸네일           92 × 52

제목 1줄 항목    52px    meta 41px < 썸네일 52px → **썸네일이 높이를 정한다**
제목 2줄 항목    62px    meta 62px > 썸네일 52px → **meta가 높이를 정한다**
                        제목 21px/줄(line-height 21px) · 부제 16px · 사이 4px
```

⚠ 재현: `vite preview` 후 playwright로 결과 화면까지 가서 `getBoundingClientRect()`.
  브라우저 확장이 안 붙어 있어 헤드리스로 쟀다. 시드 데이터 4건이 전부 2줄이라
  1줄 값은 DOM에서 제목을 짧게 바꿔 재측정했다.

**광고 규격이 어떻게 맞물리는가**

```
320×50    폭 정확히 일치. 높이 50px는 **1줄 항목(52px)과 2px 차이**다.
          → 너무 잘 섞인다. 아래 정책 항목과 충돌한다
320×100   폭 정확히 일치. 높이 100px는 2줄 항목(62px)의 1.6배.
          → 항목 하나보다 크지만 화면(800px)의 1/8이라 압도하지 않는다
```

**밀도 — 20건 리스트 기준 (전부 2줄로 계산, 총 1,544px)**

```
광고 3개 × 50px    총 1,742px · 광고 비중  8.6% · 광고 하나당 영상 6.7개
광고 4개 × 50px    총 1,808px · 광고 비중 11.1% · 광고 하나당 영상 5.0개
광고 3개 × 100px   총 1,892px · 광고 비중 15.9% · 광고 하나당 영상 6.7개
광고 4개 × 100px   총 2,008px · 광고 비중 19.9% · 광고 하나당 영상 5.0개
```

★ **네 안 모두 Better Ads Standards의 모바일 광고 밀도 상한(30%) 아래다.**
  4개 × 100px(19.9%)도 여유가 10%p 있다. **밀도는 제약이 아니다** —
  무엇을 고를지는 톤이 정한다.

**★ 추천 — 320×100 · 3개 (영상 5·11·17번 뒤)**

```
왜 100인가   320×50은 1줄 항목과 2px 차이라 **구분이 안 된다.**
            그건 톤에 좋은 것이 아니라 **정책 위반 쪽으로 가는 것**이다(아래).
            100px는 "다른 것"임이 분명하면서 화면을 지배하지 않는다
왜 3개인가   5·11·17번 뒤에 두면 첫 광고까지 5항목(약 390px)을 지나간다.
            결과 화면을 열자마자 광고가 보이지 않는 것이 이 앱에는 중요하다
⚠ 4개로 늘리면 첫 광고가 4항목(약 312px)으로 당겨진다. 수익 차이보다
  "열자마자 광고"가 주는 인상이 크다 — 실사용을 보고 판단할 것
```

**★ 정책 — "톤을 덜 깨려고 섞으면" 그것이 위반이다**

```
"It's important that visitors to your site are not misled in any way and are
 able to easily distinguish content from Google Display ads."
"Format site content so that it is difficult to distinguish it from ads."  ⛔ 금지 행위
"ads may be labelled 'Sponsored Links' or 'Advertisements', but not
 'Favorite Sites' or 'Today's Top Offers'."
   → AdSense Program policies https://support.google.com/adsense/answer/48182
     Ad placement policies    https://support.google.com/adsense/answer/1346295
```

⚠ **여기서 방향이 갈린다.** 톤 보존을 "영상 항목처럼 보이게 만들기"로 풀면 위반이다.
  ⛔ 92×52 썸네일 + 제목 + 부제 레이아웃을 광고에 흉내내지 말 것.
  ⛔ 라벨을 "추천"·"함께 보기" 같은 말로 바꾸지 말 것 —
    허용되는 것은 **"광고"(Advertisements) 또는 "스폰서 링크"(Sponsored Links)**뿐이다.
  ✅ 톤 보존은 **"조용하게"**로 풀어야지 **"비슷하게"**로 풀면 안 된다.

**톤을 덜 깨는 처리 — FYM 팔레트 안에서만**

```
라벨      "광고" · 11.5px · letterSpacing 0.2em · color T.muted
          → 폴백 층 헤더(heading)와 **같은 언어**다. 이 앱은 이미 층을
            "자간과 색"으로 가르고 있고, 광고도 하나의 층으로 읽히게 한다
배경      #ffffff08 (썸네일 자리 배경 #ffffff0d보다 한 단계 옅게)
          → 면을 만들되 카드처럼 도드라지지 않는다
테두리    두지 않는다. VideoList 주석이 "썸네일 자체가 시각적 블록이라
          테두리를 더하면 면이 겹친다"고 이미 판단했다 — 같은 이유가 적용된다
여백      위아래 20px (항목 간격 16px보다 넓게)
          → **간격이 곧 경계다.** 층이 갈렸다는 신호를 여백으로 준다
라운드    3px (썸네일 borderRadius와 같게)
```

⚠ 색은 `T`(theme.js)에 있는 값과 기존에 쓰인 알파값만 쓴다. **새 색을 만들지 않는다.**
⚠ 2.35·2.36절이 색·여백·구조 신호로 위계를 세 번 만에 잡았다.
  광고 블록을 넣으면 그 위계에 층이 하나 더 생긴다 — **넣은 뒤 그 절의 절차로 다시 잴 것.**

**⛔ 광고를 넣지 않는 곳 (확정)**

```
위기 화면        도움이 급한 자리다
"이어서 읽기" 탭  성경 본문을 읽는 자리다
```

⚠ 인피드는 **우리가 직접 자리를 정해 넣는 방식**이라 이 제외가 구현으로 보장된다.
  자동 게재(비네트)의 "페이지 단위로만 꺼진다"는 함정(아래)이 인피드에는 없다 —
  ★ **이것이 인피드를 고른 실질적 이유이기도 하다.**

**⛔ 자체 팝업 인터스티셜은 여전히 안 된다 — 다시 꺼내지 않도록 남긴다**

계획했던 "영상 클릭 → 팝업 광고 → 닫으면 유튜브"는 정책 위반이다.

```
"Displayed in pop-ups or pop-unders..."                     ← 팝업 광고가 금지 열거 항목
"pop-ups or pop-unders that interfere with site navigation" ← 닫아야 진행하는 구조
"Floating box scripts"
"Be careful when placing links, play buttons ... near ads"  ← 오클릭 유도
"publishers may not ask others to click or view their ads"  ← 안내 문구도 위험
```

★ 핵심은 트리거가 아니라 **자체 구현이냐 Google 구현이냐**였다.
⚠ 자동 게재(비네트)를 나중에 검토한다면 전제가 있다 — 자동 게재는 **페이지 단위**로
  동작하는데 PWA는 단일 페이지라 화면 전환이 URL 이동이 아니다. 공식 제외 방법은
  **링크 단위**이지 화면 상태 단위가 아니다. 위기 화면 제외가 보장되지 않으면 쓰지 않는다.

**따라오는 것**

```
개인정보처리방침   ✅ 광고 조항을 넣어 다시 썼다 (아래 4절)
스토어 신고        "광고 포함"으로 신고해야 한다. Capacitor 래핑 시 함께 처리
ads.txt           ✅ 완료 — 유저 사이트 레포 루트에 올렸다 (2.55 ⑤)
                  ⚠ gh-pages 루트가 **아니다.** 거기 두면 /PeaceYourMind/ads.txt가
                    되어 무효다. 도메인 루트에서만 읽힌다
GDPR              ⛔ 고려하지 않는다 — 유럽 출시 안 함이 전제다 (2026-08-24 결정)
                  ★ 확정 근거는 2.55 ⑨ — CMP 없이 문구만 넣으면 방침·구현이 어긋난다.
                    미구축은 위반이 아니라 그 지역 광고 제한일 뿐이다
```

---

#### 4. 개인정보처리방침 — 문안 확정, **문의처만 비어 있다**

전문은 `app/public/privacy/index.html`에 있다. 아래는 그 근거와 남은 것이다.

**★ 코드에서 확인하고 썼다 — 추측한 문장은 없다**

```
IndexedDB 키    app/src/lib/db.js의 KEYS 상수를 그대로 옮겼다
Cache Storage   app/public/sw.js의 세 캐시(shell·thumbs·krv)
외부 접속       배포본(gh-pages) 자산 전수에서 호스트를 추출해 확인했다
분석 도구       0건 — analytics·gtag·sentry·pixel·beacon·WebSocket 전부 없다
입력 텍스트     classify()가 메모리에서만 쓰고 저장·전송 경로가 없다
```

**★ 외부로 나가는 것은 네 곳뿐이다 (재감사 2026-08-24)**

```
GitHub Pages                              앱 파일 · data/ · krv/
img.youtube.com                           썸네일
fonts.googleapis.com · fonts.gstatic.com  글꼴 (호스트는 둘, 상대는 Google 하나)
www.youtube.com                           영상 클릭 시 이동
```

⚠ 배포본에서 `www.w3.org`(372건)와 `reactjs.org`(31건)도 잡히지만 **요청이 아니다** —
  앞은 SVG 네임스페이스 문자열, 뒤는 React 오류 메시지 안의 URL이다.
  ★ 재감사할 때 이 둘을 외부 요청으로 세지 말 것.
⚠ Google Fonts는 **모든 화면에서** 일어난다(썸네일과 달리 영상 화면에 국한되지 않는다).
  처음 목록에 없던 항목이고, `app/index.html`을 봐야 나온다.

**남은 것 — ✅ 없다** (2026-08-31 대조. 아래는 2026-08-24 시점의 기록이다)

⚠ **아래 이메일은 낡은 값이었다.** 같은 날(2026-08-24) 2.56 ①에서
  `appleap385@gmail.com`으로 확정됐고 **그것이 지금 실제값이다**(About.jsx `CONTACT` ·
  방침 8절 · 회귀가 둘의 일치를 지킨다). OPERATOR도 2.57 ⑤에서 채워졌다.
  ⛔ 아래 문단을 현재 상태로 읽지 말 것 — 값만 정정하고 나머지 서술은 기록으로 남긴다.

```
✅ 8절 문의처를 채웠다 (2026-08-24) — 이메일 appleap385@gmail.com.
   "운영자" 줄과 "(준비 중)" 안내는 뺐다.
   ★ About.jsx의 CONTACT도 **같은 주소로** 채웠다. 방침은 새 탭으로 열리는 외부
     페이지라, 앱 안에서 문의할 곳을 찾는 사람이 방침 8절까지 내려가야 하는 상태를
     만들지 않는다. "만든 곳" 절은 버전 때문에 이미 떠 있어 줄 하나가 붙었을 뿐이다.
   ⚠ 이 시점에는 OPERATOR(운영자명)가 비어 있었다 — 빈 문자열이면 그 줄을 안 그린다.
     ✅ **2.57 ⑤에서 "개인 개발자가 만들어 운영합니다"로 채워졌다.**
회귀 2건으로 고정했다 — 방침에 이메일이 있는가 · 방침과 앱이 **같은 주소**를 말하는가.
  두 곳이 갈리면 어느 쪽이 맞는지 알 수 없다. 불일치 주입으로 확인했다.
```

**⛔ 목록에서 다루지 않는 것 — 스토어 배포 지역 제한**

사용자가 직접 처리한다. 이 목록에 넣지 말 것 (2026-08-24 결정).

---

#### 5~7. 나머지

```
                ⚠ 세션 재개(보류 표)와도 묶여 있다 — 저장 항목이 늘면 문안이 바뀐다
제작자 · 연락처    `About.jsx`의 OPERATOR · CONTACT (지금 빈 문자열, 빈 값이면 안 그린다)
                🔴 개인인지 팀/법인인지부터 정해야 한다. 연락처는 넣는 순간 공개된다(2절)
Capacitor 래핑    🔴 iOS도 낼 것인가 · 번들 ID · 최소 지원 OS
                ⚠ 서비스워커·IndexedDB가 웹뷰에서 그대로 도는지 먼저 확인
원격 자산 범위     🟢 번들: taxonomy · verses · seed-videos · krv 장 본문
                (장 수는 위 규모 블록이 유일한 출처다 — 여기 적지 않는다)
                원격: data/videos.json · version.json · 썸네일(i.ytimg.com)
                ⚠ 위기 화면 상담 연락처는 **번들에 있어야 한다**
```

---

### ★ 일하는 방식 — 실측이 뒤집은 것들 (읽고 시작할 것)

> 아래는 특정 기능이 아니라 **다음에도 똑같이 걸릴 함정**이다.
> 전부 이 저장소에서 실제로 틀렸던 사례이고, 각 항목의 절 번호에 원본이 있다.

**⓪ 결정이 나온 자리에서 바로 문서에 넣는다.**

```
값이 어긋나는 것은 stats.py가 잡는다(3절 제안 A). 그러나 **결정이 어긋나는 것은
못 잡는다** — 사람의 결정은 코드에 없기 때문이다.
⛔ 그 병에 도구를 만들지 않는다. 도구가 없는 자리다.
★ 2026-08-31에 실제로 작동한 경로는 하나뿐이었다 —
  사용자가 결정을 말하고, 세션이 **그 자리에서 바로** HANDOFF에 넣었다.
  광고 노출 범위(2.95)가 그렇게 닫혔다. 미룬 결정은 여섯 번 어긋났다.
```

**⓪-2 ★ 실패가 성공처럼 보이는 자리 — 2026-09-02 하루에 셋 나왔다**

```
① 작업 스케줄러가 python을 못 찾았다 (0x80070002)
   등록은 "Ready"였고 화면에 아무 문제도 없었다. **매일 조용히 실패할 상태**였다.
   → 등록한 뒤 Start-ScheduledTask로 한 번 돌려 LastTaskResult 0을 보고서야 알았다
② 데모 App ID가 틀려도 화면이 멀쩡하다
   데모 광고가 잘 나온다. 실제 단위로 바꾸는 것을 잊어도 **아무 신호가 없다.**
   ⚠ 반대로 값이 없거나 형식이 틀리면 앱이 즉시 죽는다 — 두 실패의 모양이 정반대다
③ bannerNative.js가 bare specifier로 404를 받았다
   `@vite-ignore` + 변수 지정자라 vite가 묶지 않았고, 런타임 import가 404 →
   catch → 조용히 null. **배너가 안 뜨는데 화면에 오류가 하나도 없었다.**
④ ★ 정규식 이스케이프가 뭉개져 **회귀가 조용히 통과했다** (2026-09-02 · 2.102)
   테스트가 `viteConfig.includes("ca-app-pub-\d+\/\d+")`로 짜였는데
   보통 문자열에서 `\d`는 유효한 이스케이프가 아니라 **`d`로 뭉개진다.**
   찾는 문자열이 `ca-app-pub-d+/d+`가 되어 **영원히 못 찾는다** —
   이번엔 실패로 드러났지만, 반대 방향(`!includes`)이었으면 **영원히 통과**했다.
   → `String.raw`로 고쳤다. ⛔ 소스를 문자열로 검사할 때는 raw를 쓴다
   ⚠⚠ **같은 모양이 두 번째다.** vite.config.js가 `new RegExp` + 템플릿
     리터럴에서 `[\s\S]`가 `[sS]`로 뭉개져 조용히 빗나간 적이 있다(2026-08-31).
     ★ 공통점은 "정규식을 문자열에 담았다"는 것이다 — 담는 순간 이스케이프가
       한 겹 더 생기고, **그 겹을 틀려도 문법 오류가 안 난다.**
```

★ **셋의 공통점은 "실패가 성공처럼 보인다"는 것이다.**
등록만 하고 한 번 돌려보지 않으면, 값만 넣고 결과를 안 보면 못 잡는다.
이 저장소는 같은 모양을 전에도 겪었다 — 2.92의 결함 넷도 전부 "화면에는 아무
오류도 없고 알림만 안 왔다"였다.

**규칙** — **장치를 만들면 그 장치가 실제로 도는지 한 번 확인한다.**
스케줄러는 한 번 돌려 종료 코드를 보고, 게이트는 일부러 어긋내 실패를 보고,
콜백은 로그에서 그 이벤트가 찍힌 것을 본다.
⛔ 이것을 위한 도구를 만들지 않는다. 만드는 순간 **그 도구가 도는지**를 또 확인해야 한다.

**⓪-3 ★★ 검사 대상이 아니면 검사는 아무 말도 안 한다 (2026-09-04 · 2.122)**

```
모바일 웹에 **눌러도 아무 일이 없는** 알림 토글이 배포됐다. 그날 회귀 293건이
**전부 통과했다.** 결함이 있던 곳은 **조건이 없는 새 파일**이었고, 소스 모양
검사는 "그 파일에 조건이 있나"만 보므로 **대상이 아니면 아무 말도 하지 않는다.**
산출물 검사는 앱 쪽에만 있었다 — 반대 방향은 통째로 무방비였다.

★ 규칙 — **새 파일·새 화면을 만들 때, 기존 게이트가 그것을 보는지 확인한다.**
  "회귀가 다 통과했다"는 것은 "결함이 없다"가 아니라 **"그 결함을 보는 검사가
  있다"** 일 때만 의미가 있다.
⚠ 짝이 되는 검사가 한쪽 방향에만 있으면 반대 방향을 함께 만든다 —
  앱에 웹이 새는 것을 보면, 웹에 앱이 새는 것도 봐야 한다.
```

**⓪-4 ★★ 네이티브 뷰가 어긋나면 어떤 자동 검사도 못 본다 (2026-09-04 · 2.126)**

```
배너가 결과 화면을 벗어나도 안 사라지는 결함이 있었다.
⛔ **웹은 맞게 돌았다** — 배경면을 0개로 그렸다. JS 상태도 DOM도 정상이다.
  어긋난 것은 **네이티브 뷰 하나**이고 그것은 웹에서 보이지도 읽히지도 않는다.
→ 순수 함수 검사 · 소스 모양 검사 · 웹 산출물 검사(2.122) **셋 다 볼 수 없다.**
  ⛔ 화면 검사를 회귀로 만들 수도 없다 — 기기와 실제 광고가 필요하다.

★ 그래서 이 종류는 **사람이 통으로 밟아야** 잡힌다.
  실제로 2026-09-04에 통으로 밟다가 잡혔다 — 조각(배너·토글·토스트·권한)으로만
  봤을 때는 전부 통과했다(2.127).
★ 규칙 — **출시 전에 신규 설치에서 한 번 통으로 밟는다.** 그 절차에
  "결과 화면을 벗어나면 배너가 없는가"처럼 **자동 검사가 못 보는 항목**을 박아 둔다.
⚠ 그리고 그 밟기를 자동화할 때는 **판정 자체를 먼저 검증한다** —
  이번에 결과 화면 판정(role=switch)이 About에도 참이라 시험이 통째로 헛돌았다.
```

**★★ 통으로 밟기에 박은 항목 — 자동 검사가 못 보는 것들 (2026-09-04 · 2.126)**

```
⛔ **결과 화면에 들어가자마자(1초 안에) 떠나면 배너가 없는가**
   · About · 위기 화면 · 알림 구절 화면 셋에서 각각 본다
   · 판정은 **AdView 부모 RelativeLayout**의 가시성이다(AdView 자신이 아니라)
       adb shell dumpsys activity top | grep -B1 "AdView{"
       → 부모가 G(GONE)여야 한다. V면 결함이다
   ⚠⚠ **천천히 밟으면 안 보인다.** 창이 0.59초다 — 결과 화면에서 몇 초 머문 뒤
     떠나면 언제나 통과한다. 앞 세션이 정확히 그렇게 헛돌았다(2.126)
   ★ 이 항목은 회귀로 만들 수 없다 — 기기와 실제 광고가 필요하다
```

---

**① ★ 표본 창이 좁으면 방향까지 틀린다 — 세 번째다**

```
2.9절 ③   쇼츠 비율로 실격시켰다 → 검토 시트는 10건만 보는데 배치는 50~100건을 돈다
          기관형 채널 14건이 콘텐츠 문제 없이 떨어져 있었다
2.29절    조사에서 `의지` 오탐을 1건으로 봤다 → 코퍼스 전체로는 3건이었다
          노출된 화면만 봤기 때문이다
2.49절    "bible_story는 안 는다"고 예측했다 → 저수확 경보가 해소됐다
          발굴 시트의 최근 10건(1/10 태깅)으로 판단했는데 배치는 채널당 100건을 걷는다
```

**규칙** — 발굴 시트의 10건으로 **채널 성격은 볼 수 있어도 태깅 수확은 못 잰다.**
수확을 알아야 하면 승인하고 배치를 한 번 돌리거나, 그 채널의 업로드를 더 봐야 한다.

**② 검색어를 정할 때는 그 어휘가 실측 코퍼스에 몇 건 있는지부터 센다**

```
2.45절   `성가대 찬양`으로 짰다. 같은 줄 주석에는 "채널 제목 관행이 '찬양대'"라고
        적혀 있었다 — **근거와 실행이 한 줄 안에서 어긋났다**
        실측: 찬양대 6건 · 성가대 0건. 한국 개신교는 압도적으로 찬양대를 쓴다
```

**규칙** — `retag_titles.py --probe <어간>`이 그 도구다. 검색어든 사전 어휘든
**먼저 세고 나서 넣는다.** "그럴 것 같다"로 넣지 않는다.

⚠ 단, **0건이 곧 기각은 아니다.** 무엇에 대해 0건인지가 갈린다.
```
2026-08-20   `피아노 찬양` 코퍼스 0건 → 뺐다 (검색창 어휘였다)
2026-08-24   `피아노 찬양` 코퍼스 0건 → 넣었다
             발굴이 데려온 **실제 채널의 실제 제목**에 그 관행이 있었기 때문
```
그래서 넣고 뺄 때는 **어떤 근거로 0건을 읽었는지**를 함께 적는다.

**③ 같은 수치를 문서에 두 번 적지 않는다 — 생성 구간으로 만든다**

```
2.43절   장 본문·앱 테스트·구절 수가 1절과 3절에서 갈려 있었다.
        구절은 **양쪽이 똑같이 틀렸다** — 잘못 적힌 값이 복사됐기 때문이다
        어느 쪽이 최신인지 문서로는 가릴 수 없었다
2.49절   build.yml이 `echo "구절 대조 통과 (250건)"`을 찍고 있었다 (실제 303건)
```

**규칙** — `scripts/stats.py`가 규모를 재고 HANDOFF의 표시 구간을 채운다.
`--check`가 게이트다. **숫자를 손으로 적고 싶으면 그 자리에 생성 구간을 만들 것.**

⚠ 그리고 **넣지 말아야 할 것**이 더 중요하다. 코퍼스에 따라 매일 달라지는 값
(풀 크기·폴백 비율·총 노출)은 stats.py에 넣지 않는다 — 넣으면 `--check`가
매일 실패하는 게이트가 되어 아무도 안 보게 된다. 그런 값은 **출처를 붙인다**
(어느 도구가, 어느 코퍼스로, 언제).

**④ 지표가 화면 내용을 못 보는 자리에 있으면, 좋아져도 의미가 없다**

```
2.44절   [6] 동점 처리를 보류했다. 근거는 "0건 화면이 4 → 0으로 줄었다"였다
        그런데 실사용 제보는 "말씀 탭에 찬양 영상이 계속 보인다"였다
        0건 화면 수는 **토글이 비었는가만 센다.**
        토글 안에 엉뚱한 것이 들어 있는지는 세지 않는다 — 차 있으면 0이 아니다
```

**규칙** — 보류 사유가 "지표가 좋아졌다"일 때는, **그 지표가 증상을 볼 수 있는
자리에 있는지 먼저 확인한다.** 못 보는 지표는 좋아져도 의미가 없고,
그때 정한 "다시 열 조건"도 영영 안 걸린다.

**⑤ 같은 결론이 두 번 나오면 방법을 바꾼다**

```
2.28절 · 2.46절   self_control에 대해 "어휘가 아니라 물량"이라는 답이 두 번 나왔다
                 세 번째로 검색어를 더 넣는 것은 판단이 아니라 반복이다
```

**규칙** — 두 번째 같은 답이 나오면 **다음 질문을 바꾼다.** 위 진입점의
"분노를 다루는 콘텐츠가 실재하는가"가 그렇게 나온 질문이다.

**⑥ 사전이 채널·프로그램 이름을 콘텐츠로 착각하지 않게 한다**

```
2.46·2.47절   `[쉼과회복]`을 모든 제목에 붙이는 채널 → 주일예배 라이브가 rest로 태깅
              오탐 다섯 번째 유형이다 (2.29절의 넷에 이어)
2.48절        그래서 프로그램명(복음강단·비전메시지)을 사전에 넣지 않았다.
              프로그램은 주제를 말하지 않는다 — 매회 내용이 다르다
```

⚠ **다만 대괄호를 전부 떼는 안은 실측이 뒤집었다** — 태깅 118건 상실
(`[생명의 삶 큐티]` 100건 · `[매일성경]` 18건, 전부 daily_word).
`큐티`는 형식이자 **내용**이고 `복음강단`은 아니다. **프로그램명이라고 다 같지 않다.**

---

### 완료된 것 — 다시 하지 말 것

```
원인 조사 (2.25절)            "감정 체계와 안 맞는 느낌"의 원인 넷. 다시 조사하지 말 것
수정 [0]~[7] (2.26~2.33)      전부 완료. 판단 근거는 각 절에
이어서 읽기 탭 (2.34)          장 문맥 축이 여기서 생겼다
색·여백 위계 (2.35·2.36)       ★ 세 번 만에 맞췄다. 답은 탭 줄 위 1px 구분선이다
부정 계열 커버리지 (2.37)       사전에 `나쁘`가 하나도 없었다. 회귀 43건 고정
장 문맥 재검토 (2.38)          299건 전수. 3건 제외 · read_from 17건 · 게이트 2종
시편 비율 실측 (2.39)          정경 장르 분포다. 위 예외 참조
같은 장 반복 보강 (2.40)        5건 추가, 전부 비시편. 하한 미달 해소
비시편 한계선 (2.41)           2건 추가 후 네 화면을 예외로 확정
cron 대조 (2.42)              ★ 재현이 어긋났고 원인은 코퍼스였다 — 아래
수치 단일화 (2.43)             stats.py가 규모 블록을 생성한다. 손으로 적지 말 것
[6] 동점 처리 (2.44)           ★ 24건 회복. **보류 판단이 틀렸던 기록도 그 절에 있다**
3차 발굴 준비·실행 (2.45·2.46)  검색어 세트 `theme` · 위기 후보 표시 · 761 units
후보 판정 (2.47)               quiet_worship 어휘 2건 · 채널명 접두어 방어 · C채널 1c
첫 채널 승인 (2.48)            C채널방송:설교. ⛔ 프로그램명을 사전에 넣는 안은 기각했다
승인 후 배치 (2.49)            말씀 탭 찬양 콘티 11→0 · 폴백 28.2→23.3% · 20건 미달 2→0
```

**⚠ retag_titles.py --screens의 유효 범위** (2.42절에서 확인)

```
같은 날    선정·폴백 로직 검증에 정확하다
다른 날    ±1% 오차가 정상이다. 배치가 매일 채널별 최근 100건을 새로 걷기 때문
사전·채널 변경 효과 측정에는 여전히 유효하다 — 같은 titles.json으로 전후를 비교하면 된다
⛔ 절대 수치를 배포본과 맞추는 용도로 쓰지 말 것
```


---

## 3-부록. 상세 자료 (위 요약의 근거)

> 아래는 **위 3절 요약이 참조하는 상세**다. 위에서 필요한 항목만 골라 읽으면 된다.
> 8~13번의 번호는 옛 편제이고 그대로 둔다 — 다른 절들이 "11번 참조"처럼
> 번호로 가리키고 있어서, 번호를 바꾸면 그 참조가 전부 깨진다.


### 8. 완료된 것 — 다시 하지 말 것

**원인 조사 (2.25절)** — "감정 체계와 안 맞는 느낌"의 원인을 넷으로 갈랐고
증거·수치를 2.25절에 남겼다. **다시 조사하지 말 것.** 같은 것을 다시 재야 할 때는
조사가 아니라 [재측정](위)이고, 방법은 `retag_titles.py --screens` 한 줄이다.

**주제 사전 어간 보강 (2.17절)** — 폴백 39.2%→33.3%, `theme_fallback_heavy` 9→5.
도구가 남았으니 사전을 다시 만질 때는 그것으로 실측한다.

```
python scripts/retag_titles.py --probe <후보어간>   오탐을 눈으로 센다
python scripts/retag_titles.py --screens           화면 폴백 비율까지 재현
```

⚠ 사전에서 더 짜낼 것은 별로 없다. 설교 제목에는 주제어가 구조적으로 없고
(설교자+교회+날짜+"문학적 제목"+본문), 찬양 곡명은 이미 걷었다.

**구절 확장 250 → 299건 (2.24절)** — 세분류당 10~15로 **불균일하게** 넓혔다.
목표에 못 미친 채 멈춘 곳이 7개이고 그것이 결과다. 확장 중 세운 절차 둘을
다음 큐레이션에서도 쓴다.

```
① 후보를 내기 전 [제외 목록] 대조가 1순위다
   verses.yaml 말미 + 세분류 헤더 주석 + 개별 note의 [제외 기록]
   ★ 빼먹어서 실제로 두 번 걸렸다 (빌 4:13 · 시 27:14) — 승인 후에 발견하면
     되돌리는 비용이 든다
② 채택하면서 위험을 적게 되면 그 자리에서 판단을 끝낸다
   note는 사람이 읽고 화면에는 본문이 뜬다. 위험을 적는 것으로는 아무것도
   막지 못한다. 2026-08-19에 네 건이 그렇게 들어왔다가 되돌려졌다
   (마 10:19 · 요 11:6 · 전 5:12 · 시 103:10)
   빠른 판별법: **그 화면 사용자가 읽는 첫 20자를 소리 내어 읽는다**
```

**남은 큐레이션 과제** — 한 화면 안의 같은 시편 반복. 시 119→`boredom.dull`
4회 · 시 42→`sadness.sorrow` 2회 등. "다른 구절 보기"가 id 순환이라 같은 편이
연달아 나올 수 있다(2.24절 편중 자료).

### 지난 한 바퀴 — 수정 [0]~[7]의 판단 근거 (2026-08-20)

> 전부 완료됐다. **다시 하지 말 것.** 여기 남기는 이유는 *왜 그렇게 했는지*가
> 사전·매핑을 다시 만질 때 필요하기 때문이다. 실측 수치는 2.26~2.33절에 있다.

원인 조사 결과와 **증거·재현 방법은 2.25절**에 있다. 요약하면 —
"이 마음에 맞춰 고른 영상"이라고 쓰인 자리의 **43%(454건 중 196건)가 감정 신호 없이**
배치돼 있고, 원인이 넷으로 갈렸다. 아래는 그 넷을 고치는 순서다.

```
[0] ★ 위기 사전 구멍     자살 어휘 누락 (긴급)                안전    ✔ 2026-08-20 완료
[1] 폴백 품질 필터       모집·행사·기념·홍보 제외             원인 ②  ✔ 2026-08-20 완료
[2] 태깅 원천 재점검     quiet_worship 어휘 개정 (곡명 사전 X)  원인 ②  ✔ 2026-08-20 완료
─────────────── ★ 여기서 다시 잰다 ───────────────
[3] 태깅 오탐 제거       부정문맥·고유명사·어절경계·표면일치    원인 ③  ✔ 2026-08-20 완료
★ 재측정 완료 (2.30절) — 그 결과로 아래 순서를 바꿨다
[5] 주제 집합 분리       joy 3 · boredom 2 · unfair 기본형식   원인 ③  ✔ 2026-08-20 완료
[4] 세분류별 오프셋      ★ 목적이 바뀌어 되살아났다             원인 ②  ✔ 2026-08-20 완료
                        (폴백 겹침 해소 → 주제분 순서 분산)
[7] 분류 개선            긍정 8종 + 부정 표현 처리             원인 ①  ✔ 2026-08-20 완료
[6] 동점 처리            필요성 급감 — media_defaults 문제다   원인 ④  보류

★ [0]~[5][7] 완료. 남은 것은 [6](보류)과 채널 발굴(10번)이다.
  다음 세션은 **오늘 09:30 UTC cron 결과를 로컬 재현치와 대조하는 것**부터 한다.
```

**순서에 뜻이 있다.** [1][2]는 폴백의 **질**과 **양**을 각각 줄이고, 그 둘이
[4] 이후가 무엇을 고쳐야 하는지를 바꾼다. 아래 [재측정] 항목을 반드시 지날 것.

---

**[1] 폴백 품질 필터 — ✔ 완료 (2026-08-20)** · 상세는 2.26절

구현: `lib/selection.py`의 `promo_reason()` / `drop_promotional()`,
`build_subcategories`가 미태깅 목록에만 적용. 검증은 `spread_test.py` 10번.

```
폴백 후보  788 → 768건 (20건 제외) · 정상 콘텐츠 손실 0건
화면 노출  454 → 454건 · 폴백 151 → 151 · 성립불가 0개
실제 교체  4종 / 노출 23건분
```

**아래는 그때의 판단 근거다 — 사전을 손볼 때 읽을 것.**

**[1] 폴백 품질 필터 — 감정에 안 맞는 것과 콘텐츠가 아닌 것은 다르다**

폴백은 2.14절에서 "감정에 딱 맞지는 않아도 그 채널의 최근 영상"을 허용한 층이다.
그런데 실측에서 나온 것은 그 범위 밖이다.

```
광주극동방송 어린이합창단 신입단원 모집 '2026전도대회 호남권연합찬양'   모집 공고
세상을 사는 지혜 | 옹기장이 40주년 카운트다운콘서트 2026              행사 홍보
✔극동방송 70주년 기념 호남권 전도대회 | FEBC 호남권 전도대회 2부 LIVE   기념 행사
[생방송] 2026 나라사랑축제 (익산)                                    행사 중계
[CTS 추천픽] 독립유공자 26인 집안… | 광복 81주년…                    기념일 특집
```

**모집·행사·기념·홍보를 폴백에서 뺀다.** 주제분에는 적용하지 않는다 — 주제어가
걸린 것은 그 나름의 근거가 있고, 두 층은 판단 기준이 다르다.

⚠ **8건 미만이 되면 채우지 말고 그대로 두고 경보를 낸다.** 폴백은 메우는 장치이지
해결이 아니다(2.14절). 화면이 얇아지는 것이 아무거나 채우는 것보다 낫고,
얇아졌다는 사실 자체가 [2]와 10번(채널 발굴)의 근거가 된다.
기존 `theme_too_few`(최종 < 8건, critical)가 이미 그 자리에 있다 — 새 경보를
만들기 전에 그것으로 충분한지 먼저 볼 것.

⚠ 제목 blocklist를 급히 만들면 정상 영상을 떨어뜨린다는 판단이 2.12절에 있다.
어휘를 하나 넣을 때마다 `retag_titles.py --probe <어휘>`로 무엇이 걸리는지 눈으로 센다.

**[2] 태깅 원천 재점검 — 사전이 얇은 게 아니라 세계가 다른 것인지**

두 가지를 실측한다. **이 둘이 [4] 이후의 필요성을 정한다.**

*(a) `quiet_worship` 계열이 검색어 시대의 유물인가*

```
quiet_worship  잔잔한 찬양 · 조용한 찬양 · 묵상 찬양 · 피아노 찬양 · CCM 피아노 · 새벽 찬양
self_control   노하기를 · 마음을 다스리 · 화가 날 때 · 분을 품
creation       창조 · 자연 묵상 · 하늘이 선포
```

`quiet_worship` 6개는 **전부 검색어**다. "잔잔한 찬양"은 영상을 **찾을 때** 쓰는 말이지
교회가 콘티 제목에 **쓰는** 말이 아니다. 2.13절이 진단한 "검색으로 영상을 찾던 세계의
어휘"가 이 주제에 통째로 남아 있는 것으로 보인다. 2.17절은 이 주제의 후보 어휘가
1,067건에 없다는 것만 확인하고 **채널 구성 문제로 결론지었는데, 어휘 문제일 수 있다.**
`quiet_worship`은 6개 세분류가 쓰는 주제라(불안·짜증·지침·평온 계열) 영향이 크다.

→ 실제 콘티 제목이 잔잔한 찬양을 무엇이라 부르는지부터 본다
  (`--probe 피아노` · `--probe 묵상` · `--probe 연주` · `--probe 경배`).
  **부르는 말이 없다면** 그때 비로소 채널 구성 문제이고 10번으로 넘어간다.

*(b) 곡명 사전으로 태깅률을 올릴 수 있는가*

`--songs` 실측: 콘티에서 갈라낸 곡명 **408종(연 480회) 중 46종(11.3%)만** 잡힌다.
미적중 362종의 상위는 이렇다.

```
6회 이달의 옹기장이 HYMN    4회 부르신 곳에서       4회 태산을 넘어 험곡에 가도
3회 성도여 다 함께         3회 할 수 있다 하신 이는  3회 나는 예배자입니다
3회 빛의 사자들이여        3회 나의 하나님         3회 밤이나 낮이나
2회 주 여호와는 광대하시도다 2회 에벤에셀 하나님      2회 문들아 머리 들어라
```

"태산을 넘어 험곡에 가도"(courage·hope)·"부르신 곳에서"(guidance)처럼 **뜻은 분명한데
어간으로는 잡히지 않는** 곡이 많다. 어간을 줄이는 방식은 2.17절에서 한계에 닿았고
(설교 제목에는 주제어가 구조적으로 없다), 남은 길은 **곡명 → 주제 사전**이다.

⚠ 2.17절이 "곡 하나만 잡는 문자열은 themes.yaml을 곡목록으로 만든다"며 뺐던 판단과
정면으로 부딪친다. 그래서 **곡명 사전을 themes.yaml에 넣을 것인지 별도 파일로 둘
것인지부터 정해야 한다** — themes.yaml은 큐레이션 문서라는 원칙이 5절에 있다.
상위 30~50곡만 붙여 `--screens`로 재 보고, 움직이는 폭을 본 뒤에 결정한다.

---

### [재측정] — [1][2] 뒤에 [4] 이후를 다시 판단했다 (2026-08-20 완료 · 2.30절)

**순서를 건너뛰지 말 것.** [1]과 [2]가 끝나면 `retag_titles.py --screens`로 화면을
다시 재고, 그 숫자로 아래를 다시 판단한다.

```
[4] 폴백 오프셋      폴백이 화면당 12건에서 크게 줄면 겹침 자체가 줄어 이득이 작아진다.
                    그대로면 [4]가 1순위로 올라온다
[5] 주제 집합 분리   joy 3종이 같은 이유는 폴백이 아니라 매핑이라 [1][2]와 무관하게 남는다.
                    다만 풀이 두꺼워지면 채널 상한·라운드로빈이 목록을 갈라 줄 수는 있다
10번 3차 발굴       [2](a)에서 quiet_worship이 "유물"로 판정되면 **찾아야 할 채널의
                    성격이 바뀐다.** 2.17절이 특정한 "잔잔한 연주 찬양"이 어휘 문제였다면
                    그 채널은 애초에 필요 없었던 것이 된다
```

측정은 API 0이다. 배치를 돌릴 필요가 없다(2.23절에서 재현이 배포본과 일치함을 확인했다).

---

**[3] 태깅 오탐 4유형 제거** — 증거는 2.25절 [증거 E]

```
부정 문맥   "불행을 기뻐하는 죄" 유형. 지금 밝은 화면 4개에 나가고 있다
            → 4.5절이 이미 대안을 적어 뒀다. 주제마다 exclude_keywords를 다는 것보다
              **전역 규칙 하나**("죄·심판·멸망·패망"이 있으면 밝은 계열을 붙이지 않는다)가
              단순하다. 사전이 복잡해지는 쪽을 피할 것
고유명사    "감사드림교회" 유형. "…교회"·"…선교회" 앞 어절은 기관명이다
어절 경계   "42회 복" → 회복 / "인생의 지" → 의지. 4.7절의 그 문제다
            ★ 제목 태깅은 **배치 전용**이라 앱과 무관하게 고칠 수 있다.
              입력 분류([7])의 어절 경계 문제와 성격이 다르다 — 그쪽은 두 구현이
              문자 단위로 같아야 해서 훨씬 비싸다. 둘을 한 덩어리로 보지 말 것
표면 일치   "안식일의 주인"(안식일 규례 설교)이 rest로 간다.
            **이 유형은 기계가 못 막는다.** 미뤄도 되는 유일한 항목이다
```

**[4] 폴백 오프셋** — `lib/selection.py`

세분류별 오프셋을 주면 폴백 앞부분 겹침이 해소된다. 6절 미뤄둔 판단에 "실사용 로그를
본 뒤"로 적혀 있었는데 **미룰 근거가 사라졌다** — 로그 없이도 5쌍이 20건 전부 동일하다는
것이 확인됐다(2.25 [증거 C]). 다만 위 [재측정]을 지난 뒤에 착수한다.

**[5] joy 3종·boredom 2종 주제 집합 분리** — `themes.yaml`

```
joy.proud · joy.delight · joy.grateful   셋 다 gratitude + joy_praise
boredom.dull · boredom.novelty           둘 다 bible_story + daily_word
```

집합이 같으면 풀이 같고 선정이 결정적이라 결과가 같아진다. **매핑이 틀린 게 아니라
구분하지 않는 것**이다. 2.7절 확정 규칙 3이 **구절**에서 한 일을 주제에서도 해야 한다 —
거기서는 지배 어휘를 갈랐다(`joy.delight`=몸짓 · `flutter.thrill`=강도 ·
`boredom.dull`=다시 보게 · `boredom.novelty`=모르는 것).

⚠ 주제를 새로 만들 것인가, 기존 24개 안에서 매핑만 바꿀 것인가부터 정한다.
themes.yaml은 큐레이션 문서라 배치 튜닝 값을 섞지 않는다는 원칙이 5절에 있다.

**[6] 동점 처리** — `lib/tagging.py`

제목에 "찬양"이 명시된 콘티 9종이 sermon으로 판정된다. 판별 규칙이 "**한쪽만** 걸릴 때
확정한다"인데 곡명 안의 `말씀`·예배 이름 `금요기도`가 sermon 어휘로 함께 걸려 동점이
되기 때문이다(2.25 [증거 F]). 화면 4개가 기본 토글에서 주제분 0건이 되는 원인이라
체감이 크다.

→ 세로줄 앞(곡명 구간)과 뒤(채널·예배명 구간)를 갈라 보면 대부분 풀린다.
  `retag_titles.py`의 `song_names()`가 **이미 그 분해를 한다** — 복제하지 말고
  `lib/`으로 올려 양쪽이 같은 함수를 쓰게 할 것.

**[7] 분류 — 부정 표현 + 감정어 가중치** — `app/src/lib/classify.js` · `taxonomy.yaml`

증거는 2.25절 [증거 D]. 점수 규칙 `(키워드 수 × 1000) + 길이 합`에서 **감정어가 짧아
상황 묘사어에 진다.** 그리고 **부정 표현 처리가 아예 없어** "재미있는 게 하나도 없어요"가
즐거움 화면으로 간다.

```
⚠ 이 원인만 구절까지 함께 틀린다.  구절은 세분류에 직접 붙어 있어 폴백 개념이 없지만,
   세분류 자체가 틀리면 그 화면의 구절이 나간다. 영상·구절 양쪽에 영향이 있는 유일한 항목
⚠ 실측 코퍼스가 없다.  입력을 기록하지 않는 것이 이 앱의 설계다(2.19절).
   프로브 문장을 만들어 재는 것이 유일한 방법이고, 2.25 [증거 D]의 30건이 그 출발점이다
⚠ 앱(JS)과 배치(Python) 두 구현이 문자 단위로 같아야 한다 (normalize.parity.test.js)
⚠ 위기 검사보다 앞에 어떤 것도 두지 않는다 (classify.js 헤더 주석)
```

빈도는 낮지만(10%) 성격이 달라 마지막에 둔 것이지 덜 중요해서가 아니다.


### 9. crisis_eligible 채널 승인 — 위기 화면에 영상이 없다

**현황 (2026-08-20 실측)**

```
승인 채널 15개 중 crisis_eligible: true   0개
배포본 crisis 블록                        {"source": "none", "videos": []}
매일 뜨는 경보                            crisis_no_channels · crisis_empty
위기 구절                                 10건 — 정상 제공된다
상담 안내                                 정상 (109 등, 화면 최상단)
영상이 0건일 때 화면                       "지금은 영상 없이, 위 연락처를 곁에 둬요."
```

**의도된 안전 동작이지 결함이 아니다.** 위기 화면의 필수 요소(상담 안내 → 구절)는
전부 나가고 있고, 영상 자리는 비활성 버튼이 아니라 **문장으로** 상태를 말한다.

**경보는 2026-08-20에 정리했다 — 사실 보고를 Issue로 내지 않는다.**

```
crisis_no_channels   Summary   채널 0개. 사람이 승인하기 전까지 매일 같은 값이다
crisis_empty         Issue     채널이 **있는데도** 비었다 = 사건
crisis_carried_over  Issue     채널이 **있는데도** 미달 = 사건
crisis_stale         Issue     시간이 갈수록 악화된다 = 사건
```

★ **위기 경보 4종을 한 덩어리로 보지 말 것.** 그리고 채널이 0개인 동안에는
뒤의 셋이 **아예 발동하지 않는다**(`lib/crisis.py`가 파생 경보를 접는다) —
채널이 없으면 풀이 비는 것은 필연이고, 그 사실은 `crisis_no_channels`가 이미
말했다. **원인 경보 하나만 남기고 파생은 접는다.** 그래서 지금 상태에서
위기 관련 Issue는 0건이다(직전에는 매일 2건이었다).

**승인에 무엇이 필요한가**

```
1. 대상 채널의 성격    정신건강 전문 사역 · 기독교 상담 사역 중심 (PLAN.md 7절)
                      지금 목록 15개에는 그런 채널이 **한 곳도 없다** —
                      교회 5 · 방송사/선교단체 10이고 전부 일반 사역이다
                      → 3차 발굴(10번)에서 별도 갈래로 찾아야 한다
2. 일반 승인 기준      기준 0(개신교·한국어) · 1(정체 확인 a·b·c) · 4(쇼츠 제외 건수)
                      를 먼저 전부 통과해야 한다. crisis_eligible은 그 위에 얹는다
3. 위기 전용 추가 판단  일반 승인보다 엄격하다. 판단 축이 다르다 —
                      · 조건을 다는가 ("믿으면·회개하면·기도하면 낫는다")
                      · 치유·간증 서사로 몰아가는가
                      · 상담 자격·소속이 확인되는가 (개인 채널이면 특히)
                      ⚠ 감정 화면에서 문제가 되지 않는 표현이 위기 화면에서는
                        문제가 된다 — 구절 큐레이션에서 이미 겪은 것이다
                        (시편 34:18을 위기 풀에서 뺀 이유, 2.7절)
4. 물량 요건          CRISIS_MIN_VIDEOS = 12. 미달이면 필터를 완화하지 않고
                      직전 결과를 유지한다(lib/crisis.py). 채널 1~2개로는
                      12건이 안 차서 승인해도 화면이 그대로일 수 있다
```

**판단 자체는 사람이 한다** — 일반 승인과 같은 원칙이고, 위기 풀에서는 더 그렇다.
`channel_allowlist.yaml`에 `crisis_eligible: true`를 적는 것으로 켜진다(기본 false).

⚠ 이 항목은 **급하지 않다.** 영상이 없다고 위기 화면이 깨지지 않는다.
반대로 급하게 채우면 위기 상태의 사용자에게 조건부 위로를 내밀 위험이 있다.

### 10. 3차 채널 발굴 — 상세 (위 ★3번의 근거)

승인 15개는 `MIN_ALLOWLIST_SIZE`와 같아 여유가 없기도 하다.

⚠ **[1][2]를 하고 [재측정]을 지난 뒤에 착수한다.** 순서를 바꾸면 안 되는 이유가
조사로 분명해졌다 — 지금 채널을 늘리면 원인 넷이 그대로인 채 **폴백 후보만 늘어난다.**
폴백 151건이 이미 영상 23종을 20개 화면이 돌려 쓰는 구조라(2.25 [증거 A]),
후보가 늘어도 겹침 구조 자체는 그대로다.

**무엇을 찾을지는 [2]가 정했다.** 2.17절은 폴백 과다의 원인을 `self_control`·
`quiet_worship` 두 주제의 빈 풀로 보고 "분노를 다루는 설교와 잔잔한 연주 찬양이
없다"고 결론지었다. [2](a)는 그 어휘가 **검색어라서 안 걸리는 것 아닌가**를 물었고,
답은 **주제마다 갈렸다** — `quiet_worship`은 어휘 문제였고(고쳤다),
`self_control`은 물량 문제였다. 그래서 아래 두 순위가 모두 살아 있다.

**[2] 실측으로 두 순위가 확정됐다 (2026-08-20, 2.28절)** — 어느 쪽도 어휘로는 못 푼다.

```
1순위  분노·마음 다스림을 실제로 다루는 콘텐츠   self_control 풀이 0건이다
       ★ 어휘 문제가 아님이 확인됐다. 후보 13종을 전부 쟀다 —
         분노 0 · 온유 0 · 원망 0 · 감정 0 · 견디 0 · 참으 1(오탐)
         `다스리` 4건은 **전부 방향이 반대**였다
           "하나님이 다스리십니다"(스가랴 6장) · "오셔서 다스리소서"
           → 하나님의 통치이지 내 마음을 다스리는 것이 아니다
       찾을 것: **화·분노·미움·원망을 사람의 문제로 다루는 설교**
         예) 분노 조절 · 용서와 미움 · 감정 다스리기 · 관계 갈등 강해
         상담·심리 결이 있는 사역 채널이 이 갈래에 가깝다
       걸려 있는 화면: anger.irritation · anger.rage (self_control 매핑)

★ 2026-08-20 [5]에서 확정된 것 — **집합 분리로는 못 푼다**
       anger.irritation  self_control 0 + quiet_worship 12
       exhaustion.tired  rest         0 + quiet_worship 12
       calm.ease         creation     1 + quiet_worship 12
       **세 화면이 12건짜리 quiet_worship 하나에 갇혀 있다.**
       매핑을 바꿔 보려 했으나 anger.irritation에 peace를 주면
       anxiety.restless(peace+quiet_worship)와 **새로 100% 동일**해진다(실측).
       주제를 돌려막을 여유가 없다는 뜻이다.
       세분류별 오프셋(2.32절)으로 **순서만** 갈라 뒀다 — 스크롤하면 같은 12건이다.
       → **채널을 늘리는 것 말고 방법이 없다.**

★★ 2026-08-28 — **2순위가 더 무거워졌다. 폴백 무작위가 재고 부족을 드러냈다.**
       폴백에 화면별 무작위를 넣자(2.84) 말씀 겹침은 100% → **2.6%** 로 풀렸는데
       찬양은 **38%에서 멈춘다.** 원인이 규칙이 아니라 재고임이 수치로 갈렸다.
         90일 안에 재고 3건 이하인 찬양 채널 6개(CGN 1 · CBS설교 1 · 광주극동 1 ·
         C채널 1 · CBSJOY 2 · 극동방송 3)가 무는 **9자리가 어느 화면에나 들어간다**
         (찬양 폴백 천장 33의 27%). 말씀은 15채널 전부 재고 ≥3이라 그 강제분이 0이다.
       ⚠ **여유는 5채널이다** — 기여 큰 찬양 채널 5개를 더 잃으면 탭이 20을 못 채운다
         (말씀은 9개). 승인 취소나 업로드 중단이 여기서 먼저 터진다.
       ★ 찾을 것: **꾸준히 올리는** 찬양·연주 채널. 재고 총량보다
         **90일 안에 3건 이상**을 내는지가 이 문제의 기준이다 — 백로그가 큰 채널
         (옹기장이 1030일 · mini Music 1028일 · 택피아노 867일)은 90일 컷 뒤에
         남는 것이 3·0·0건이라 도움이 안 된다.
       ⚠ 주제분 쪽도 같은 재고에 걸려 있다 — 1년 넘은 영상이 보이는 21개 탭이
         전부 찬양이고, 사다리를 넣어도 **14개가 남는다**(2.86 ⑦).
       ★★ **주제별로 보면 더 분명하다 — 90일 안에 찬양이 0건인 주제가 10개다.**
         self_control · justice · forgiveness · patience · lament ·
         comfort · renewal · rest · daily_word · bible_story
         (반대로 90일 안에 말씀이 0건인 주제는 3개뿐 — self_control · strength ·
          quiet_worship). ⛔ 규칙으로는 못 고친다. **이 10개 주제에 신선한 찬양을
         공급하는 채널**이 이 항목의 구체적 목표다.

2순위  조용한 찬양·연주 채널                    quiet_worship 공급이 2개 채널뿐
       ★ [2](a)로 어휘는 고쳤다(찬양대·Instrumental·연주곡·CCM PIANO).
         그런데 그 어휘를 쓰는 채널이 **꿈의교회 찬양대 6건 ·
         ANOINTING Instrumental 5건 · 옹기장이 1건**뿐이다.
       그래서 5개 화면의 겹침이 82~90%로 남는다 —
         짜증 ↔ 여유 90% · 짜증 ↔ 피로 82% · 여유 ↔ 피로 82%
         12건 중 8건이 5개 화면 전부에 나간다
       ⚠ **[4] 폴백 오프셋·[5] 주제 집합 분리로도 안 풀린다.**
         나눌 원본이 12건뿐이라 어떻게 나눠도 같은 영상이 돌아온다.
         **채널을 늘리는 것 말고 방법이 없는 유일한 항목이다.**
       찾을 것: 성가대/합창단 정기 업로드 · Instrumental·피아노 찬양 시리즈
         (검색어로는 "잔잔한 찬양"이 통하지만 채널 제목 관행은
          "찬양대"·"Instrumental"이다 — 발굴 검색어와 태깅 사전을 섞지 말 것)
```

**영상 다양성(확장 풀)도 여기서 함께 판단한다.** 채널당 상한 3 · 화면당 20 ·
폴백 상한 12(60%)를 손대거나 채널당 수집량을 늘리는 선택지인데, 수집량을 늘리면
**미태깅도 함께 는다**(2.17절). [1][2] 뒤 재측정 없이 이 값들을 만지지 말 것.

`--query-set`으로 세트를 고르고, 3차는 1·2차와 다른 갈래로 짠다(`C채널`이 후보).
기검토 제외 11건은 `channel_reviewed_out.yaml`이 자동으로 상위 N에서 뺀다.
새 채널을 넣은 뒤에는 `--dump-titles`로 덤프를 갱신하고
`retag_titles.py --screens`로 화면이 실제로 나아졌는지 확인한다.

### 11. 보류 중 — 미루기로 **판단한** 것들

> 잊은 것이 아니라 미룬 것이다. 각 항목에 **다시 열 조건**을 적어 둔다.

**[6] 동점 처리** (`lib/tagging.py`) — 필요성이 급감했다

```
원래 문제   제목에 "찬양"이 명시된 콘티가 sermon으로 판정된다.
            판별 규칙이 "**한쪽만** 걸릴 때 확정"인데 곡명 안의 `말씀`·
            예배명 `금요기도`가 sermon 어휘로 함께 걸려 동점이 되고,
            그러면 채널 content_type으로 떨어진다
지금        기본 토글로 주제분 0건인 화면이 4개 → **0개**가 됐다(2.30·2.32절).
            남아 있던 anger.unfair는 **형식 오판이 아니라** media_defaults
            문제였고 [5]에서 sermon으로 바꿔 해소했다
다시 열 조건  토글을 눌렀을 때 빈 화면이 나온다는 제보 · 또는 재측정에서
            토글 주제분 0건 화면이 다시 생길 때
해법        곡명 구간(세로줄 앞)과 예배명 구간을 갈라 본다.
            `retag_titles.py`의 `song_names()`가 이미 그 분해를 한다 —
            복제하지 말고 `lib/`으로 올릴 것
```

**위기 채널 승인** (`crisis_eligible`) — 급하지 않다. 9번에 상세

```
지금        승인 15개 전부 false. 위기 화면은 상담 안내 + 구절 10건이 정상 노출되고
            영상 자리는 문장으로 상태를 말한다("지금은 영상 없이…")
다시 열 조건  정신건강 전문 사역·상담 사역 채널을 실제로 찾았을 때
⚠           급하게 채우면 위기 상태의 사용자에게 조건부 위로를 내밀 위험이 있다.
            물량 요건도 있다 — CRISIS_MIN_VIDEOS = 12, 채널 1~2개로는 안 찬다
```

**영상 설명란(description) 재검토** — FYM이 명시적으로 배제한 경로다

```
지금        제목만 본다. 설명란은 읽지 않는다
왜 배제됐나  FYM에서 설명란을 보다가 **오차단 사고**가 났다(2.13절 선택지 d).
            설명란에는 채널 소개·구독 안내·타임스탬프가 섞여 있어 신호 대 잡음이 나쁘다
다시 열 조건  **그때의 근거를 뒤집을 실측**이 있을 때. 지금 코퍼스로 검증할 수 있다 —
            titles.json에 설명란이 없으므로 --dump 확장이 먼저 필요하다
⚠           쿼터는 늘지 않는다(videos.list 응답에 이미 들어 있다).
            늘어나는 것은 **오탐 위험**이다
```

**한 화면 안의 같은 시편 반복** — 구절 쪽 과제

```
지금        시 119 → boredom.dull 4회 · 시 103 → anger.irritation 3회
            시 31·42 → sadness.sorrow · 시 145 → joy.grateful
문제        "다른 구절 보기"가 id 순환이라 같은 편이 연달아 나올 수 있다
다시 열 조건  구절 큐레이션을 다시 열 때 함께 본다 (아래 "큐레이션을 다시 열 때")
해법 후보    회전 순서를 편(篇) 기준으로 섞거나, 같은 편이 연속하지 않게 정렬
```

**문구 12개 확장** — 지금 세분류당 10건이다

```
지금        공감 240건 + 마무리 240건 = 480건 (세분류 24 × 각 10건)
            MIN_MESSAGES = 10은 **하한**이지 정원이 아니다(2.20절에서 이름을 고쳤다)
12개로 늘리면  576건 (+96건)
⚠ 먼저 볼 것  2.20절의 유사 문구 게이트(difflib 0.85)가 걸린다.
            현재 최고값이 **0.8485로 임계까지 여유가 0.0015뿐**이다 —
            joy.delight "오늘은 신나는 걸로 골라볼게요" ↔
            boredom.novelty "오늘은 낯선 걸로 골라볼게요"
            문구를 늘리면 충돌이 늘어난다. **임계값을 올리지 말고** 축을 옮길 것
다시 열 조건  실사용에서 문구가 반복돼 보인다는 신호 · 또는 구절 확장과 함께
```

**진단 경보 2건** — 정의부터 다시 세워야 한다

```
blocklist_tiers 연결      taxonomy.yaml에 이식만 해두고 배치에 넘기지 않았다.
                         켤지는 "어떤 제목이 걸리는지" 실측을 보고 정한다
channel_zero_yield 조건   지금은 "필터 후 잔존 0"만 잡는다. 잔존 100건인데 태깅
                         0인 채널(CGN 성경통독)이 서비스 기여로는 똑같이 0인데
                         경보가 안 뜬다. 다만 폴백 도입으로 그 채널도 화면에
                         나가게 됐으므로 **"기여 0"의 정의부터** 다시 세워야 한다
```

> **폴백 중복 노출**은 여기서 뺐다 — [4]로 승격돼 2026-08-20에 처리됐다
> (세분류별 오프셋, 2.32절).

### 12. FYM 저장소에서 할 일 5건 (PYM에서는 못 고친다)

```
4.3절  lib/allowlist.py · lib/channel_blocklist.py의 _is_blank 결함
       (YAML에서 값을 비운 키가 None이라 빈 필드가 검사를 통과한다)
4.4절  normalize 반복 축약 예시가 실제 동작과 다르다
       scripts/lib/normalize.py:19 · taxonomy.yaml:240
2.19절 ★ placeholder 풀이 분류 정확도를 깎는다 — FYM에도 그대로 있다
2.20절 유사 문구 게이트가 없다 — 완전 일치만 본다
2.21절 "다시 적어보기" 링크 대비 2.27:1 — PYM과 같은 값(#ffffff40)이다
```

**2.20 유사 문구 게이트** — PYM은 2026-08-19에 `messages_test.py`에 유사도
0.85 검사를 넣었다. FYM에는 아직 완전 일치 검사만 있고, 문구 풀 구조가 같아
같은 종류의 근접 중복이 있을 가능성이 높다. PYM의 구현(`difflib`,
세분류 간만 검사, 임계값 근거 주석)을 그대로 가져가면 된다.

**2.21 링크 대비** — `common.jsx` `Closing`의 "다시 적어보기"가 두 앱 모두
`#ffffff40`이고, 배경 `#141E24` 기준 **2.27:1**이다. 상호작용 요소의 최소선으로
보는 3:1에 못 미친다. PYM은 `#ffffff66`(3.78:1)으로 올렸다.

```
같이 볼 것  전환 링크는 FYM이 이미 T.muted(6.28:1)로 밝다 —
            PYM이 #ffffff55(2.99:1)였고 FYM 값을 가져와 맞췄다.
            즉 이 항목은 **FYM에서 PYM으로** 온 것이 아니라 반대 방향이다
남은 것     PYM "이 앱에 대해" 버튼 #ffffff33 = 1.90:1. 둘 다 손대지 않았다
```

**placeholder 건이 셋 중 가장 실질적이다.** PYM이 2026-08-19에 고쳤고
(2.19절) FYM에는 옛 풀 6개가 그대로 있다. 두 앱이 같은 분류기 구조를 쓰므로
문제도 같다.

```
FYM taxonomy.yaml ui.placeholders 중 고쳐야 할 3개
  "무슨 일이 있었나요?"            사건형 — 답이 사건이 되면 안 걸린다 (3/8)
  "오늘 하루, 어떻게 지나갔어요?"    사건형
  "오늘 마음, 어땠어요?"           평가형 유발 — 가장 나쁘다 (1/6)
그리고 방향이 없는 1개
  "잘 정리되지 않아도 괜찮아요. 떠오르는 대로"
```

PYM이 쓴 6개와 작성 기준 5항을 그대로 가져가면 된다
(`taxonomy.yaml` `ui.placeholders` 주석). **FYM 쪽에서 실측을 다시 할
필요는 없다** — 분류기와 사전 구조가 같아 결과가 같다. 다만 FYM은 감정
체계만 있고 구절 계층이 없으므로 문구 자체는 그대로 쓸 수 있다.

### 13. 스토어 출시 준비 — 확정 필요 항목

> 지금은 **웹(PWA)으로만** 돌고 있다. 스토어에 올리려면 아래가 전부 필요하다.

**앱 래핑 — Capacitor** (✅ **안드로이드 1단계 완료** — 2.90~2.94 · 2026-08-31)

```
왜 필요한가   구글 플레이·애플 앱스토어는 웹 URL을 받지 않는다. 네이티브 껍데기가 필요하다
왜 Capacitor  이미 PWA로 완성돼 있어 웹뷰를 그대로 싣는 방식이 가장 적은 변경이다
              (React Native·Flutter는 앱을 다시 쓰는 일이다)

✅ 정해졌다   앱 아이디 · 표시 이름 · SDK · 권한 · 알림 채널
             ⛔ **값은 여기 적지 않는다.** 3절 「앱 사실」 블록이 코드에서 읽어
               채운다(stats.py --check가 게이트다). 두 곳에 적으면 갈라진다
             아이콘·스플래시  03-c "끊긴 원 · 숨" — gen_icons.py가 생성한다 (2.91)
             웹뷰 급전 방식   ⛔ server.url을 쓰지 않는다. 번들로 돈다 (2.90)
             권한 병합       플러그인이 POST_NOTIFICATIONS · RECEIVE_BOOT_COMPLETED ·
                            WAKE_LOCK을 넣는다. 병합 결과는 APK로 확인했다(2.92)

🔴 남은 미결   **iOS도 낼 것인가 — 이것 하나뿐이다.**
             Apple 개발자 계정 연 US$99 · 심사 기준이 다르다
             ★ 안드로이드와 **독립 판단**이다. 안드로이드를 먼저 내도 아무 손해가 없다
```

⚠ **"먼저 확인"으로 적어 뒀던 것은 답이 나왔다 — 항목에서 뺀다.**
서비스워커는 앱에서 **끈다.** 앱 셸과 장 본문이 이미 APK 안에 있어 캐시할 것이
썸네일뿐이고, 그 하나를 위해 웹뷰의 서비스워커 동작을 따로 검증할 이유가 없다(2.90).
IndexedDB는 웹뷰에서 그대로 돌았다 — 알림 설정·NOTIFY_SEEN이 실기기에서 읽고 쓰인 것으로
확인됐다(2.92~2.94).

---

**앱 서명 키 — 🔴 판단 자료 (착수 전 · 2026-08-31)**

> ⛔ **아직 아무것도 만들지 않았다.** 이 절은 판단을 위한 자료다.
> Play Console 등록 과정에서 Play 앱 서명 선택이 나오므로 **등록 전에** 읽어야 한다.

**★ 채널 불변성과 같은 종류의 비대칭이다 — 그리고 더 무겁다.**

```
채널     한 번 만들어지면 코드로 못 바꾼다 → **재설치**가 되돌린다      (2.94)
서명 키   한 번 올리면 그것이 그 앱의 정체다 → **되돌릴 방법이 없다**
         잃으면 같은 패키지명으로 업데이트를 영영 못 올린다.
         새 앱으로 다시 시작해야 하고 설치 수·리뷰·평점이 0이 된다
공통     "일단 하고 나중에 고친다"가 성립하지 않는다.
         시작 전이 유일한 기회다 — 오늘 알림 채널에서 겪은 것과 같은 모양이다
```

**1. 키스토어가 무엇을 만드는가**

```bash
keytool -genkeypair -v \
  -keystore pym-upload.jks -alias pym-upload \
  -keyalg RSA -keysize 2048 -validity 10000
```

```
나오는 것   .jks 파일 하나 + 키스토어 비밀번호 + 별칭(alias) + 키 비밀번호
⚠⚠ **정정 (2026-09-04 · 2.125)** — 위 "넷이 다 있어야 한다"는 이제 맞지 않는다.
   ★ **PKCS12로 만들고 store·key 비밀번호를 같게** 하기로 정했다.
     · PKCS12는 둘을 가르는 것을 잘 다루지 않는다
     · 같게 두면 **지켜야 할 비밀이 셋에서 둘**(키 파일 · 비밀번호)로 준다
   ⚠ 별칭(pym-upload)은 **비밀이 아니다** — 견본 파일과 복구 문서에 적는다.
     잃어도 keytool -list 로 다시 읽을 수 있다(비밀번호가 있으면).
   → 실제로 백업해야 하는 것은 **둘**이다: .jks 파일 · 비밀번호
   ⛔ 그래도 "파일만 백업하고 비밀번호를 잃는" 것이 가장 흔한 실패라는 것은 그대로다
-validity  10000일(약 27년). Play는 2033-10-22 이후까지 유효할 것을 요구한다
```

**2. Play 앱 서명 vs 자체 관리 — ★ 사실은 고를 수 있는 것이 아니다**

```
★ 2021년 8월 이후 Play에 **신규 등록**하는 앱은 AAB가 필수이고,
  AAB에는 **Play 앱 서명이 필수다.** 우리는 신규 앱이므로 자체 관리는 선택지가 아니다.
→ 그러므로 판단할 것은 "어느 쪽이냐"가 아니라 **키를 누가 만들고 업로드 키를 어디에
  보관하느냐**다.
```

✅✅ **이 전제는 닫혔다 — 콘솔 화면으로 확인됐다** (2026-09-04 · 사용자).
  ★ **Play 앱 서명이 기본 적용**이고, **앱 서명 키가 이미 「사용 중」**이며
    **Google이 이미 생성해 두었다.** 우리가 고를 것이 없었다.
  → 조사 결과("2021-08 이후 신규 앱은 AAB 필수 + Play 앱 서명 필수")가 맞았다.
  ⚠ 조사 결과와 화면 확인을 섞지 않는다는 규칙(9193행)이 여기서 값을 했다 —
    맞았지만, 맞은 것을 **확인한 뒤에야** 전제로 쓴다.

| | Play 앱 서명 (신규 앱은 필수) | 자체 관리 |
|---|---|---|
| 최종 앱 서명 키 | Google이 보관 | 내가 보관 |
| 내가 키를 잃었을 때 | ★ **복구된다** — 업로드 키 재설정을 신청한다 | ⛔ **복구 불가.** 그 앱은 끝난다 |
| 내가 올리는 것 | **업로드 키**로 서명한 AAB | 최종 키로 서명한 APK/AAB |
| 대가 | 최종 서명 키를 Google이 쥔다.<br>Play 밖 배포본과 서명이 달라진다 | (신규 앱에는 해당 없음) |

⚠ 복구가 된다고 해서 가벼운 것은 아니다 — **업로드 키 재설정은 며칠 걸리고 그동안
  업데이트가 멈춘다.** 위험이 "앱의 죽음"에서 "며칠의 정지"로 줄어드는 것뿐이다.

**3. 개인 개발자가 잃어버리는 전형적 경로**

```
① 노트북 교체·초기화     .jks가 프로젝트 폴더에만 있었다. 가장 흔하다
② .gitignore에 넣고 끝   저장소에 안 올린 것은 맞다. 그런데 **어디에도 백업이 없다**
③ 비밀번호만 증발        파일은 백업했는데 비밀번호는 머릿속에 있었다
④ CI 비밀에만 존재       Actions Secret에 넣고 원본을 지웠다. Secret은 다시 못 읽는다
⑤ 반대 방향의 사고       공개 저장소에 .jks와 gradle.properties를 함께 올려 유출
```

**방어책**

```
· 서로 다른 매체 **두 곳 이상** + 오프라인 하나
  예) 암호 관리자(비밀번호·alias) + 암호화된 클라우드(.jks) + 물리 매체
· ⛔ 비밀번호를 .jks와 **같은 곳에 두지 말 것** — 같이 잃고 같이 새어 나간다
· ★ 복구 문서를 하나 남긴다 — 파일 위치 · alias · 만든 날짜 · 백업한 곳.
  ⛔ 비밀번호 자체는 적지 않는다. ⛔ 이 저장소가 아니라 개인 보관처에 둔다
· 저장소에는 절대 넣지 않는다 (.gitignore + key.properties 패턴)
· ⚠ Play 앱 서명이 있어도 위 절차는 그대로 한다. 재설정은 공짜가 아니다
```

**4. 판단 — 1은 확정, 2는 미결**

```
✅✅ 판단 1 — **닫혔다. 그리고 우리가 고를 것이 없었다** (2026-09-04 확인)
   2026-08-31에 "앱 서명 키는 Google이 생성하게 한다"로 확정했는데,
   콘솔을 보니 **이미 그렇게 되어 있었다** — 앱 서명 키가 「사용 중」이고
   Google이 생성해 둔 상태다. 판단이 **자동으로 실현됐다.**
   ★ 근거는 그대로 유효하다 — Play 밖 배포 계획이 없고, 자기 키 경로는
     생성·백업·업로드 세 곳에서 틀릴 수 있다

🔴 **남은 것은 업로드 키다** — 아직 만들지 않았다
   콘솔의 「업로드 키 인증서」 절이 **비어 있고**
   "첫 App Bundle을 업로드하면 여기에 인증서 지문이 표시됩니다"라고 나온다.
   → **우리가 업로드 키를 만들어 첫 AAB에 서명해 올리면 등록되는 구조**다.
   ⛔ 콘솔에서 미리 등록하는 절차가 아니다. 업로드가 곧 등록이다

🔴 판단 2 여전히 미결 — **업로드 키를 어디에 보관할 것인가**
   ⛔ **Actions Secret은 백업이 아니다.** 넣고 나면 다시 읽을 수 없다 —
     원본을 지우면 그것으로 끝이다. 3항 ④가 그 사고다
   ⚠ 키를 만드는 순간 보관이 시작된다. **키 생성 전에 정해져 있어야 한다**
   ⏸ 2026-09-04 세션이 설계안을 냈다. **사용자 승인 전이라 아직 만들지 않았다**
```

**5. ★ 잃었을 때 무슨 일이 일어나는가 — 정확히 (2026-09-04)**

```
⛔ **앱은 죽지 않는다.** 사용자 기기의 앱 정체는 **최종 앱 서명 키**가 정하고,
  그것은 Google이 갖고 있다. 업로드 키는 "누가 올릴 수 있는가"만 정한다.
  · 이미 설치된 앱   그대로 돈다
  · 스토어의 기존 버전  그대로 설치된다
  · 사용자에게 보이는 영향  **없다**
⚠ 대신 **업데이트가 멈춘다.** Play에 업로드 키 재설정을 신청해야 하고
  **며칠 걸린다.** 그동안 새 버전을 올릴 수 없다
★ 자체 관리였다면 **그 앱이 끝났을 것**이다 — 같은 패키지명으로 영영 못 올린다.
  그 차이가 Play 앱 서명의 값이고, 판단 1의 근거가 결과적으로 옳았던 이유다
⛔ 그렇다고 가벼운 것은 아니다. "며칠의 정지"도 대가이고, 재설정은 공짜가 아니다 —
  보관 절차(4항)를 건너뛸 근거가 되지 못한다
```

⚠ **콘솔 화면이 진실이다.** 위 내용은 2026-08-31 시점의 일반 지식이고, 등록 화면의
  선택지 문구·순서는 바뀔 수 있다. 등록할 때 실제 화면을 보고 위 판단을 확정할 것.
  ⛔ 화면이 다르다고 그 자리에서 즉흥으로 고르지 말 것 — 되돌릴 수 없는 자리다.

**원격 자산 범위 — 무엇을 앱에 넣고 무엇을 받아올 것인가** (✅ **구성 확정** — 2.90 · 2026-08-31)

```
✅ 앱 번들    앱 셸 · taxonomy.json · verses.json · seed-videos.json ·
             **krv 178장(942KB)**
             ★ krv가 APK에 들어가 **오프라인 첫 실행에서도 이어서 읽기가 열린다** —
               보류 표의 "오프라인 첫 방문"이 앱에서는 공짜로 풀린다
             ⛔ 서비스워커는 앱에서 끈다. 파일도 남기지 않는다
✅ 앱 원격    data/videos.json · version.json **둘뿐이다.** 오리진이 localhost라
             절대 URL이어야 하고, GitHub Pages가 CORS 허용을 준다(실측)

웹(PWA) 구조  번들에 들어가는 것   taxonomy.json · verses.json · seed-videos.json
             원격에서 받는 것     data/videos.json (서버가 하루 2회 갱신) · version.json
             썸네일               YouTube CDN에서 직접 (i.ytimg.com)
🔴 남은 것    스토어 앱이 원격 데이터를 받는 것을 어떻게 고지할 것인가
             ⚠ 예전에 여기 적어둔 "영상 목록을 하루 한 번 내려받는다"는 **틀린 문구다.**
               서버 갱신과 앱 확인은 주기가 다르고, 고지 대상은 **앱의 동작**이다.
                 서버   build.yml이 하루 2회 갱신 (KST 18:30 · 06:30)
                 앱     sync.js CHECK_INTERVAL_MS = 6시간. PWA에는 크론이 없어
                        **앱을 열었을 때** 마지막 확인이 6시간을 넘었으면 받는다
               따라서 "하루 한 번"도 "하루 두 번"도 아니다. 정확한 고지는
               **"앱을 열 때 최대 6시간에 한 번 영상 목록을 내려받는다"** 쪽이다
               (실제 다운로드 횟수는 사용자가 앱을 얼마나 자주 여는지에 달렸다).
               ※ 배포된 방침 페이지(app/public/privacy/index.html)에는 다행히
                 횟수를 못박은 문장이 없다 — "갱신 주기를 지키는 데 씁니다"뿐이라
                 지금 배포본은 부정확하지 않다. 스토어 제출 문구를 새로 쓸 때
                 위 숫자를 그대로 옮기지 말 것.
             오프라인에서 어디까지 동작해야 하는가 — 지금은 시드 144건으로 산다
             썸네일이 외부 도메인이라는 사실 (앱 심사에서 네트워크 사용으로 잡힌다)
⚠ 위기 화면   상담 연락처는 **번들에 있어야 한다.** 원격이 죽어도 그 화면은 떠야 한다
             (지금 taxonomy.json 안에 있다 — 이 구조를 깨지 말 것)
```

**스토어 등록정보 · 데이터 안전 · 스크린샷 — 🔴 초안 (2026-08-31 · 승인 대기)**

> ⛔ **초안이다. 아무것도 제출하지 않았고 스크린샷도 찍지 않았다.**
> 콘솔 접근이 필요 없는 것만 먼저 만들었다(사용자가 계정을 등록하는 동안 병행).
> ⚠ 문구는 **앱의 목소리를 지킨다** — 앱이 격려하지 않기로 한 원칙(2.92)이
> 스토어 문구에도 적용된다. 과장하지 않는다.

**① 등록정보 초안**

```
앱 이름 (30자 이내)
  Peace in Mind
  ⛔ 표시 이름은 이미 확정이고 회귀가 지킨다 (2.58 ⑥). 여기서 바꾸지 말 것

짧은 설명 (80자 이내) — 52자
  마음을 적으면 성경 한 절을 찾아 드립니다. 네트워크 없이도 열리고,
  정한 시각에 한 절을 보냅니다.
  ★ 오프라인과 알림을 여기 넣은 이유 — 심사자가 "웹과 뭐가 다른가"를 묻는
    자리이기 때문이다 (2.54 c)
```

```
자세한 설명 (4000자 이내) — 초안

  지금 마음을 한 줄 적으면, 그 마음에 닿는 성경 구절 한 절을 찾아 드립니다.
  고르기 어려우면 목록에서 골라도 됩니다.

  구절 한 절에서 멈추지 않습니다. 그 구절이 있는 장을 이어서 읽을 수 있고,
  본문은 개역한글판을 한 글자도 고치지 않고 그대로 싣습니다.

  · 네트워크가 없어도 열립니다
    구절과 성경 본문 178개 장이 앱 안에 들어 있습니다. 비행기 안에서도,
    신호가 없는 곳에서도 처음 실행부터 동작합니다.

  · 정한 시각에 구절 한 절
    원하는 시각을 정하면 그 시각에 한 절을 보냅니다. 시각은 언제든 바꾸거나
    끌 수 있고, 알림은 기기 안에서만 만들어집니다.

  · 적은 내용은 기기에 남습니다
    로그인도 계정도 없습니다. 적은 글과 읽은 기록은 기기 안에만 남고,
    앱 안에서 지울 수 있습니다.

  · 광고
    화면 아래에 배너 하나가 있습니다. 앱은 무료이고 그 배너가 비용을 감당합니다.
    광고를 맞추기 위해 기기의 광고 ID가 쓰이며, 그 밖에 적은 글이나 읽은 기록은
    광고에 쓰이지 않습니다.

  · 영상은 정해 둔 채널에서만
    미리 검토한 채널 목록에서만 가져옵니다. 검색으로 모으지 않습니다.
    영상의 저작권은 각 채널에 있고, 앱은 링크만 제공합니다.

  이 앱은 의료 서비스나 상담 서비스가 아닙니다. 진단이나 치료를 제공하지
  않습니다. 마음이 많이 힘들거나 위급하다고 느껴질 때는 전문 상담 연락처를
  이용해 주세요.
    자살예방 상담전화 109
    정신건강 위기상담전화 1577-0199

  성경 본문: 성경전서 개역한글판, 대한성서공회
```

```
⚠ 문구를 고칠 때 지킬 것
  ⛔ "위로", "치유", "회복" 같은 결과를 약속하지 않는다 — 앱이 화면에서
    하지 않는 말을 스토어에서 하면 안 된다
  ⛔ 면책 두 문단은 단정형을 유지한다. 부드럽게 쓰면 면책이 아니게 된다
  ★ 숫자는 실측만 쓴다 — "178개 장"은 규모 블록에서 온 값이다
  ⛔ **성경 구절 원문을 넣지 않는다** — 저작권 문제가 될 수 있다.
    앱이 무엇을 하는지 설명하되 구절 자체는 인용하지 않는다.
    ✅ 2026-09-04 확인 — 지금 본문에 인용이 **없다**. 출처 표기
      "성경전서 개역한글판, 대한성서공회"만 있고 그것은 앱의 표기(verses.js)와 같다
  ⛔ **광고를 숨기지도 과장하지도 않는다** (2026-09-04 · 사용자 확정).
    ★ 「광고」 절의 마지막 줄이 특히 중요하다 — 신고한 것은 **광고 ID뿐**이고
      사용자가 적은 글은 아니라는 구분이 **사용자에게도 심사자에게도** 필요하다

⚠⚠ **2026-09-04 개정 — 왜 고쳤나**
  옛 문구는 「· 아무것도 보내지 않습니다 / 적은 내용은 기기 밖으로 나가지
  않으며」였다. 그 초안은 **앱에 광고가 없던 2026-08-31**에 쓰였다.
  그 뒤 AdMob 배너가 들어갔고 데이터 안전에 **기기 ID 수집·공유**를 신고했다
  (2.123). 제목부터 신고와 충돌했다 —
  ⛔ 스토어 설명이 데이터 안전 신고와 어긋나면 그 자체가 정책 위반이다.
  ★ 같은 사고가 ② 데이터 안전 초안에도 있었다(거기도 폐기 표시를 달았다).
    **광고를 켜는 변경은 문서 여러 곳을 동시에 낡게 만든다** — 다음에 이런
    변경을 하면 스토어 문구·데이터 안전·방침을 함께 훑을 것
```


**①-2 카테고리·태그 — ✅ 확정 (2026-09-04)**

```
카테고리   **도서/참고자료**
태그       **종교문헌 · 자기계발**   (셋 → 둘)
⛔ **「명상」 태그를 뺐다** (2026-09-04 · 사용자 확정)
   근거   설명 어디에도 명상·호흡이 없다. 앱에 호흡 애니메이션이 있지만
          그것은 **로딩 표시**이지 명상 기능이 아니다.
   ★ 없는 기능을 말하지 않는 것이 이 앱의 태도다(2.92 격려하지 않기).
     태그를 남기려면 설명에 근거를 만들어야 하는데, 그것은 **없는 기능을
     만들어 말하는 것**이 된다. 그래서 뺐다
⚠ 사용자가 콘솔에서 뺀다. 세션은 콘솔에 못 들어간다

설명이 카테고리를 뒷받침하는가 — ✅ 검토했다
  도서/참고자료   성경 본문 178개 장 · 개역한글 그대로 싣는다 → 정확히 이 범주
  종교문헌        본문이 전부 그것이다
  자기계발        "마음을 적으면 한 절을 찾아 준다"가 뒷받침한다
⚠ 면책 두 문단("의료 서비스나 상담 서비스가 아닙니다")이 **건강 앱
  「해당 없음」 신고를 뒷받침한다.** 지우지 말 것
```

**①-3 이미지 자산 — ✅ 만들었다 (2026-09-04)**

```
아이콘 512×512   app/public/icons/icon-512.png
                ✅ **그대로 쓴다.** 512×512 · RGB(투명 없음) · 47,652 bytes
                ⚠ Play는 아이콘의 **투명을 허용하지 않는다.** 검사를 넣었다 —
                  gen_feature_graphic.py 가 크기·투명·1MB를 함께 본다
그래픽 1024×500  app/store/feature-graphic-1024x500.png
                ★ **scripts/gen_feature_graphic.py 가 만든다** — 손으로 그리지 않는다.
                  색을 theme.js에서 읽는다(gen_icons.py와 같은 출처)
                모양  앱 배경 그라데이션 + 「끊긴 원 · 숨」 표식 + 이름 (안 ㄱ)
                ⛔ 구절도 스크린샷 축소본도 넣지 않는다
                ⚠ 1024×500에서는 **띠(banding)가 보였다** — 8비트 반올림 때문이고
                  아이콘(512)에서는 안 보이던 것이다. 고정 씨앗 디더로 흩었다
                ⚠ 안전 여백 검사가 있다 — 내용이 가운데 80%를 벗어나면 멈춘다
                ⛔ 서체를 못 찾으면 **기본 서체로 조용히 넘어가지 않고 멈춘다**

⚠ 필수 규격 (2026-09-04 확인)
  아이콘        512×512 · PNG/JPEG · 1MB 이하 · **투명 불가**
  그래픽        1024×500 · PNG/JPEG · 15MB 이하
  휴대전화      **최소 2장** · 짧은 변 320px 이상 · 긴 변 3840px 이하 · 16:9 또는 9:16
  태블릿        7인치·10인치 각 최소 1장 (태블릿 지원을 표시하려면)
```

**② 데이터 안전(Data safety) 신고 초안**

> ⛔⛔ **아래는 폐기됐다 — 2026-09-04에 실제로 신고한 내용과 정반대다.**
> 이 초안은 **앱에 광고가 없던 2026-08-31**에 쓰였다. 그 뒤 AdMob 배너가 들어갔다.
> ✅ **실제 신고**(2.123) — 광고 **포함** · 기기 또는 기타 ID **수집·공유** ·
>   목적 **광고** · 필수 · 임시 아님 · 암호화 전송 · 계정 생성 없음
> ⛔ 아래 "수집 없음 / 공유 없음 / 광고 없음"을 **다시 쓰지 말 것.**
>   지금 그대로 제출하면 **사실과 다르고 그 자체가 정책 위반이다.**
> ★ 남겨 두는 이유는 (가)의 썸네일 논점이 아직 유효해서다.

(이력) 근거는 방침의 "기기 밖으로 아무것도 나가지 않는다"이고, 코드가 그것을 지켰다.

```
데이터 수집    없음
데이터 공유    없음
  근거  로그인·계정·서버 전송이 없다. 사용자가 적은 글은 기기의 IndexedDB에만
       남고 어디로도 가지 않는다. 회귀가 이것을 지킨다
데이터 삭제    앱 안에 "이 기기에서 기록 지우기"가 있다 (About 화면)
암호화 전송    수집이 없으므로 해당 없음
```

```
🔴 판단이 필요한 두 자리 — 여기는 내가 정하면 안 된다

(가) 썸네일이 외부 도메인이다
    앱이 i.ytimg.com(YouTube CDN)에서 썸네일을 직접 받는다. 그 요청에는
    **IP 주소가 실린다.** 우리가 "수집"하는 것은 아니지만, 제3자 서버로
    자동 요청이 나가는 것은 사실이다.
    → 데이터 안전은 "앱이 수집·공유하는 것"을 묻는다. 우리는 아니다.
      다만 심사자가 이 지점을 물을 수 있으므로 **답을 준비해 두는 편이 낫다.**
    ⚠ data/videos.json · version.json도 같은 성격이다 (GitHub Pages)

(나) ✅ **닫혔다** — 광고 **포함**으로 신고했다 (2026-09-04 · 2.123).
    아래 (이력)이 예고한 "광고를 켜는 날 신고를 함께 바꾼다"가 그날이었다.
(이력) 광고 신고 — 지금은 "광고 없음"이 맞다
    2.90에서 앱 빌드가 AdSense를 물리적으로 들어냈고 산출물 전수 검사가
    광고 호스트 0건을 확인한다. 지금 시점의 정직한 신고는 "광고 없음"이다.
    ⛔ 2.54 f의 "광고 포함으로 신고한다"는 **앱에 광고를 켤 때의 이야기다.**
      지금 그대로 신고하면 사실과 다르다.
    ⚠ 광고 2단계를 켜는 날 이 신고를 **반드시 함께 바꿔야 한다.**
      바꾸지 않으면 그것 자체가 정책 위반이다
```

**③ 스크린샷 계획 — 🔴 승인 대기 (⛔ 아직 찍지 않았다)**

```
⚠ 지금 연결된 실기기는 **태블릿이다** (Galaxy Tab S9 FE).
  Play는 폼팩터별로 스크린샷을 따로 받는다 — 휴대전화용이 별도로 필요하다.
```

```
제안 — 에뮬레이터 AVD 하나로 둘을 겸한다

  해상도   1080 × 1920 · 480dpi (density 3.0) → **360 × 640dp**
          ★ 360dp를 고른 이유가 둘이다
            ① Play 휴대전화 스크린샷 요건(9:16 · 각 변 320~3840px)을 그대로 만족한다
            ② 2.36 규칙 5항의 **360px 위계 점검을 실제 휴대폰 폭에서** 처음 돌린다
               ⚠ 지금까지 위계 검증은 전부 태블릿에서 나왔다.
                 광고 자리가 생기면 이 확인이 더 중요해진다
  API     35 (Android 15) — 실기기와 같은 세대라 렌더 차이가 적다
          ⚠ 36 이미지는 아직 흔들릴 수 있다. 34가 대안이다
  이미지   Google APIs (Play 서비스 **없는** 것으로 충분하다 — 우리 앱은 안 쓴다)
  준비     sdkmanager로 시스템 이미지 내려받기(약 1.5GB) → avdmanager로 AVD 생성
          ⚠ 지금 SDK에 시스템 이미지가 **0개**이고 AVD도 없다(2026-08-31 확인)
```

```
어느 화면을 몇 장 — 5장 제안 (Play 최대 8장)

  1  입력 화면        "언제 와도 자리는 비워둘게요" — 앱이 무엇을 묻는지
  2  결과 화면        구절 + 이어서 읽기 입구
  3  장 본문          이어서 읽기 — ★ 오프라인에서 되는 것이 여기다
  4  알림 구절 화면    정한 시각에 도착한 한 절 — ★ 웹이 못 하는 것
  5  About 알림 설정   시각을 정하는 자리 + "기기 안에서만 동작"

  ⛔ 위기 화면은 넣지 않는다. 그 화면은 필요한 사람에게만 조용히 닿는 자리이고,
    스토어 앞면에 세울 성질이 아니다. 광고를 그 화면에서 뺀 것과 같은 판단이다
  ⚠ 4번은 알림을 실제로 띄워야 한다 — 에뮬레이터에서 시각을 몇 분 뒤로 잡는다
  ⚠ 태블릿용은 실기기로 따로 찍는다 (같은 5장)
```

⛔ **승인 뒤에 찍는다.** 화면 선택과 장수를 먼저 확정할 것.

---

**"이 앱에 대해" 화면의 상수 3건 — ✅ 셋 다 채워졌다** (2026-08-31 대조)

2026-08-19에 구조와 문구만 넣고 값을 비워 뒀던 자리다. **지금은 전부 값이 있고**
회귀가 비지 않았는지 지킨다(`verses.test.js`). 아래는 `app/src/components/About.jsx`의
실제 값이다.

```
OPERATOR      "개인 개발자가 만들어 운영합니다"        (2.57 ⑤ 확정)
              ⚠ 라벨이 없다 — 값이 그대로 화면 문장이 된다. 그래서 문장형으로 쓴다
CONTACT       "appleap385@gmail.com"                (2.56 ① 확정)
              ⚠ 방침 8절과 **같은 주소**여야 한다. 회귀가 둘의 일치를 지킨다
PRIVACY_URL   __IS_APP__ ? PRIVACY_URL_APP : PRIVACY_URL_WEB
              앱은 절대 https 주소를 연다 — 앱 오리진이 localhost라 상대 경로는 404다
              ⛔ 번들에 방침 사본을 넣지 않는다 (2.90 — 소유권 확인 태그가 들어간다)
```

⚠ **이 블록은 2026-08-31까지 "셋 다 비어 있다"고 적혀 있었다.** 값은 2026-08-24에
  채워졌는데 문서가 일주일 뒤까지 따라오지 않았다 — 3절 「문서가 코드보다 뒤에 서는
  문제」에 다섯 번째 사례로 등재했다.

⚠ **개인정보처리방침은 구글 플레이·애플 앱스토어 **양쪽 모두 필수**이고,
데이터를 수집하지 않는 앱도 면제되지 않는다.** "수집하지 않는다"는 사실
자체를 문서로 밝혀야 한다. 흔한 오해라 여기 적어 둔다.

PYM은 실제로 수집하지 않는다 — 로그인·계정·서버 전송이 없고, 방문 기록과
마지막으로 고른 형식만 기기의 IndexedDB에 남는다. 그래서 처방침 본문은
"기록" 절의 문장을 그대로 쓰면 된다. **필요한 것은 그것을 호스팅할 URL**이다
(gh-pages에 정적 페이지를 하나 더 올리는 것으로 해결된다).

**이미 들어간 것 — 다시 만들지 말 것**

```
앱 버전       package.json → vite define(__APP_VERSION__) → About 화면
              손으로 적지 않는다. 적으면 반드시 실제 버전과 어긋난다
성경 출처     4.5절·2.18절 참조. 이 화면의 표기는 삭제 금지
콘텐츠 면책   영상 저작권은 각 채널, 앱은 링크만 제공, 내용은 채널의 견해
위기 면책     의료·상담 서비스가 아니며 진단·치료를 제공하지 않는다.
              위급할 때는 표시된 전문 상담 연락처를 이용하도록 안내한다
```

면책 두 절만 단정형("아닙니다"·"이용해 주세요")을 쓴다. 앱 전체는 조용한
어조지만 면책은 흐리면 면책이 아니다 — 부드럽게 쓰다가 "의료 서비스가 아니다"가
읽히지 않으면 그 문장은 제 일을 못 한 것이다.

### ★★ 백업 목록 — **저장소만 clone해서는 안 되는 것들** (2026-09-04)

> ★ 다음 세션이 "이 PC가 죽으면 무엇이 없어지나"를 물으면 **이 절을 읽는다.**
> ⚠ 2026-09-04 시점에 실제로 훑어서 만든 목록이다. 새 비밀·새 로컬 설정이
> 생기면 **여기에 함께 적는다** — 적지 않으면 다음 사람이 못 찾는다.

#### ★ 먼저 — 실제로 백업할 것은 **셋뿐이다**

```
① app/android/admob.properties        ⛔ 잃으면 시험 기기 등록을 다시 해야 한다
② Demo_29_PYM_backup/keys/ 의 파일 둘   ✅ 만들었다(2026-09-04). **가장 무겁다**
③ %GRADLE_USER_HOME%/gradle.properties  ⚠ 한 줄짜리다. 내용을 알면 다시 쓰면 된다
                                       → 사실상 ②의 부속이라 함께 적어 두면 끝난다

★ 나머지는 전부 ② 또는 ③이다 — 저장소에 있거나, 다시 발급받거나, 다시 만들면 된다.
⛔ 그러니 **①과 ②만 물리적으로 백업하면 된다.** 목록이 길어 보이지만 그렇다.
```

#### ⛔ ① 잃으면 되돌릴 수 없다 (또는 대가가 크다)

```
[1] 업로드 키  ✅ **만들었다** (2026-09-04 · 2.125)
    키 파일    D:/jaehyuk.myung/claude_demo/Demo_29_PYM_backup/keys/pym-upload.jks
    값 파일    D:/jaehyuk.myung/claude_demo/Demo_29_PYM_backup/keys/pym-upload.properties
               ⛔ 비밀번호가 들어 있다. 키 파일과 **다른 곳**에도 사본을 두지 말 것 —
                 지금 둘이 같은 폴더에 있다. 백업할 때는 **갈라서** 둔다
    포인터     %GRADLE_USER_HOME%/gradle.properties 의 pymKeystoreProperties 한 줄
    지문       SHA-256 AF:23:0D:4D:25:95:89:61:95:F0:A9:AF:59:80:EE:2A:
                       13:2E:77:AC:38:F8:E4:60:C3:28:FC:B7:8D:9E:DE:22
    ⚠⚠ 폴더 이름이 저장소(Demo_29_PYM)와 **다섯 글자 차이**다(Demo_29_PYM_backup).
       ⛔ 부모 폴더 claude_demo 를 통째로 압축하면 **저장소와 키가 함께 나간다**
    잃으면     ⛔ 앱은 **죽지 않는다** — 최종 앱 서명 키는 Google이 쥔다.
               이미 설치된 앱도 스토어 버전도 그대로 돈다.
               ⚠ 대신 Play에 **업로드 키 재설정**을 신청해야 하고 **며칠 걸린다.**
                 그동안 업데이트를 못 올린다 (13절 5항)
    보관       암호 관리자(비밀번호) + 암호화 클라우드(.jks) + 오프라인 매체(.jks)
               ⛔ 비밀번호를 .jks와 같은 곳에 두지 않는다

[2] app/android/admob.properties
    무엇       admob.appId · admob.bannerId · admob.testDevice.* · admob.aaid.*
    잃으면     appId·bannerId는 ②다(AdMob 콘솔에서 다시 읽는다).
               ⛔ 그러나 **시험 기기 ID(AAID)는 기기에서 다시 뽑아야 한다** —
                 ★ 그런데 그 절차 자체가 **실제 광고를 한 번 요청하는 일**이다
                   (2.113 — 결과 화면까지 가야 logcat에 ID가 찍힌다).
                   개발자 노출이 한 번 더 생기고, 무효 트래픽 위험이 그만큼 는다
    → 그래서 ①로 둔다. 파일 하나 복사로 그 위험을 피할 수 있다
    ⚠ 이 파일에는 **개인 기기를 지목하는 안정적 식별자**가 들어 있다. 공개 금지
```

#### ⚠ ② 잃어도 다시 발급·확인할 수 있다 (어디서 어떻게)

```
[3] Actions Secret  YOUTUBE_API_KEY   (GitHub → Settings → Secrets → Actions)
    ⛔⛔ **다시 읽을 수 없다.** GitHub는 Secret 값을 보여주지 않는다 —
      덮어쓰기만 된다. "백업"이라는 말이 성립하지 않는 자리다
    ★ 그래도 ②인 이유 — **GCP에서 다시 발급받을 수 있다.**
      경로  console.cloud.google.com → 해당 프로젝트 → API 및 서비스 →
            사용자 인증 정보 → **API 키** → 새로 만들거나 기존 키 「키 표시」
            (YouTube Data API v3가 사용 설정돼 있어야 한다)
      그다음 GitHub Secret에 새 값을 넣고, 로컬 셸에도 export 한다
    ⚠ 로컬에서도 필요하다 — Actions Secret은 러너 전용이라 안 잡힌다.
      build_videos.py(드라이런 제외)와 suggest_channels.py가 그 키를 쓴다
    ⚠ 키를 새로 만들면 **쿼터가 아니라 키가 바뀔 뿐**이다. 일일 상한은 프로젝트 단위다
    ⛔ secrets.GITHUB_TOKEN은 백업 대상이 아니다 — Actions가 자동으로 준다

[4] AdMob App ID · 배너 단위 ID       AdMob 콘솔 → 앱 → 앱 설정 / 광고 단위
[5] 시험 기기 등록                     AdMob 콘솔 → 설정 → 시험 기기 (목록이 남아 있다)
    ⚠ 콘솔에 목록은 남지만 **AAID 값 자체**를 다시 얻으려면 기기에서 뽑아야 한다 → [2]
[6] Play Console 앱                    계정에 남아 있다 (앱 자체는 클라우드다)
[7] 앱 서명 키                         ⛔ 우리가 가진 적이 없다. **Google이 보관한다**
[8] 인증서 지문(업로드/앱 서명)          Play Console → 앱 무결성 → 앱 서명
    ✅ **업로드 키 지문은 적었다** (2.125 · 위 ①-[1]). 비밀이 아니다
    🔴 앱 서명 키 지문은 콘솔에만 있다 — 첫 업로드 뒤에 함께 적으면 좋다
[9] GitHub 저장소 · gh-pages           원격에 있다. clone하면 온다
```

#### ✅ ③ 다시 만들면 된다 (백업 불필요)

```
[10] app/public/krv/                  ⛔ **저장소에 없지만 백업도 필요 없다.**
     ★ 원천이 저장소에 있다 — **data/krv/bible_1961_krv.json** (추적된다).
       app/public/krv/ 는 gen_krv_chapters.py 가 **매 빌드 재생성**하는 산출물이다
     ⚠ 앱 빌드에 **필요하다**(APK에 942KB로 들어간다 · 오프라인 이어 읽기).
       그런데 `npm run gen:data`가 만들므로 clone만 하면 된다.
       ⛔ "구절 추가할 때만 필요한 것"이 아니다 — **빌드마다 필요하고 매번 생성된다**
[11] app/src/data/{taxonomy,verses,themes}.json   같은 이유. gen:data 산출물
[12] node_modules/ · dist/ · dist-app/ · app/android/build/ · .gradle/   전부 산출물
[13] app/android/local.properties     sdk.dir 한 줄. ⚠ Android Studio·gradle이
     처음 열 때 다시 만든다. 손으로 써도 된다
[14] .quota_log.json                  YouTube API 사용량 **기록**이다. 잃으면
     "지금까지 얼마나 썼나"의 이력만 사라진다. 쿼터 자체는 구글이 센다
[15] AVD  pym_phone_360               D:/jaehyuk.myung/android/avd/
     ⚠ 3.5GB. 다시 만들면 된다(실행 환경 절의 avdmanager 절차).
     ⛔ 백업 대상이 아니다 — 크고, 재현 가능하고, 기기 상태가 아니라 도구다
[16] 세션 작업 산출물  .measure/ · 실측 스크립트  전부 임시다
[17] app/android/app/google-services.json   **없다.** Firebase를 안 쓴다(2.124)
```

#### ★ 새 PC에서 처음부터 — clone 뒤 무엇을 놓고 무엇을 깔면 도는가

```
[0] 깔 것
    Git · Node 20+ (지금 v24.14.1) · Python 3.12+ (지금 3.14.3)
    JDK 21          지금 D:/jaehyuk.myung/android/jdk21
    Android SDK     platform-tools · build-tools · platforms;android-36 ·
                    (에뮬레이터를 쓸 거면) emulator + system-images/android-35/...
    ⚠ 경로를 D로 두는 이유 — C 여유가 8GB뿐이다(실행 환경 절)

[1] clone 하고 의존성
    git clone https://github.com/appleap385-aaz0919/PeaceYourMind.git
    cd PeaceYourMind/app && npm ci

[2] ⚠ **백업에서 되돌릴 것 하나** (①-[2])
    app/android/admob.properties      ← 백업본을 그대로 복사
    ⛔ 없으면 **빌드가 선다.** 그것이 의도다(build.gradle이 메시지로 안내한다)
    ★ 급하면 admob.properties.example 의 데모 값으로도 debug는 돈다.
      ⛔ 다만 그 상태로 릴리스를 말면 verifyNoDemoAdIds 가 세운다(2.99)

[3] ✅ 여기까지 하면 **debug가 돈다**
    cd app && npm run sync:app
    cd app/android && export JAVA_HOME=<JDK 21 경로> && ./gradlew assembleDebug
    ⚠ local.properties(sdk.dir)는 gradle이 알아서 만들거나 한 줄 써 준다

[4] ⛔ **release를 만들려면 둘이 더 필요하다** (①-[1])
    ㄱ. 업로드 키 파일을 저장소 **밖**에 놓는다      <경로>/pym-upload.jks
    ㄴ. 그 옆에 값 파일                              <경로>/pym-upload.properties
        (app/android/keystore.properties.example 을 복사해 값을 채운다)
    ㄷ. %GRADLE_USER_HOME%/gradle.properties 에 **한 줄**
          pymKeystoreProperties=<경로>/pym-upload.properties
    ✅ 그다음   cd app/android && ./gradlew bundleRelease
    ⛔ 하나라도 빠지면 verifyReleaseSigning 이 **무엇이 없는지 말하고 세운다**(2.125)

[5] ⚠ API를 쓰는 스크립트를 돌리려면
    export YOUTUBE_API_KEY="..."     (②-[3]에서 발급)
    ⛔ 앱 빌드에는 필요 없다. 배치·채널 발굴에만 쓴다

[6] 환경변수 둘 (편의 · 없어도 되지만 있으면 C가 안 찬다)
    GRADLE_USER_HOME = D:\jaehyuk.myung\android\gradle
    ANDROID_AVD_HOME = D:\jaehyuk.myung\android\avd
    ⚠ 2026-09-04 확인 — 이 PC는 **둘 다 사용자 환경변수로 설정돼 있다**(레지스트리)
    ⛔ GRADLE_USER_HOME을 옮기면 [4]-ㄷ의 gradle.properties 위치도 함께 옮긴다
```

#### ⚠ 이 목록이 낡는 자리

```
· 새 비밀이 생기면(다른 API 키 · 다른 Secret) **여기에 함께 적는다**
· 업로드 키를 만들면 ①-[1]의 "아직 만들지 않았다"를 지운다
· 첫 AAB 업로드 뒤에는 ②-[8] 지문을 HANDOFF에 적는다
⛔ 게이트가 이 절을 검사하지 않는다 — 사람이 갱신해야 하는 자리다
```

---

### 실행 환경 — 앱 빌드와 시험 기기 (2026-08-31)

```bash
export JAVA_HOME="D:/jaehyuk.myung/android/jdk21"   # ⚠ 없으면 gradlew가 바로 선다
export PATH="$JAVA_HOME/bin:$PATH"
ADB="D:/jaehyuk.myung/android/sdk/platform-tools/adb.exe"   # PATH에 없다

cd app && npm run sync:app          # vite --mode capacitor + cap sync android
cd app/android && ./gradlew assembleDebug
"$ADB" install app/build/outputs/apk/debug/app-debug.apk
```

⚠ **채널 설정을 바꿨으면 `install`이 아니라 `uninstall` 후 설치해야 한다.**
안드로이드는 기존 채널의 importance·sound·vibration을 갱신하지 않는다(2.94절).
앱 데이터 삭제로도 채널은 안 지워진다 — 패키지 삭제만이 지운다.

**에뮬레이터 — 휴대폰 폭 검증·스크린샷용 (2026-08-31 준비 완료)**

```
AVD        pym_phone_360      D:/jaehyuk.myung/android/avd/pym_phone_360.avd
해상도      1080 x 1920 · 480dpi  =  **360 x 640 dp**
           ★ 하나로 둘을 겸한다 — Play 휴대전화 스크린샷 요건(9:16)을 만족하고,
             2.36 규칙 5항의 360px 위계 점검을 실제 휴대폰 폭에서 처음 돌린다
이미지      system-images/android-35/google_apis/x86_64  (Android 15 · Play 서비스 없음)
           D 드라이브에 3.5GB. ⚠ 예상 1.5GB보다 컸다
가속        ✅ WHPX 사용 가능 (emulator -accel-check)
```

⛔ **ANDROID_AVD_HOME을 반드시 두고 쓴다.**

```
사용자 환경변수  ANDROID_AVD_HOME = D:\jaehyuk.myung\android\avd   (영구 설정됨)
왜             ⚠ 기본값은 C:\Users\<계정>\.android\avd 다.
               **C 여유가 8GB뿐**이라 AVD가 거기 생기면 곧 찬다
확인함          avdmanager가 실제로 D에 만들었다 — C의 .android/avd는 비어 있다
⚠ 시스템 이미지는 원래 D로 간다 (SDK 아래). 옮길 것은 AVD뿐이다
⚠ config.ini의 disk.dataPartition.size를 10G → 4096M로 줄였다
```

```bash
export ANDROID_HOME="D:/jaehyuk.myung/android/sdk"
"$ANDROID_HOME/emulator/emulator.exe" -avd pym_phone_360 &
```

⚠ **sdkmanager는 새 `android` CLI로 위임된다.** 옛 표기(`system-images;android-35;...`)를
그대로 넘기면 cmd가 `;`에서 인자를 쪼개 "Package not found"로 죽는다.
→ 새 CLI는 **슬래시 표기**를 쓴다.

```bash
"$ANDROID_HOME/cmdline-tools/latest/bin/android.exe" sdk install \
  "system-images/android-35/google_apis/x86_64"
```

---

**AdMob App ID — 없으면 빌드가 선다 (2026-09-02)**

```
app/android/admob.properties.example 을 app/android/admob.properties 로 복사하고
값을 채운다. ⛔ admob.properties는 .gitignore 대상이다.

⚠ **값이 둘이다** (2026-09-02 · 2.102에서 배너 단위가 환경변수에서 이 파일로 왔다).

  admob.appId      시험용 데모  ca-app-pub-3940256099942544~3347511713
  admob.bannerId   시험용 데모  ca-app-pub-3940256099942544/9214589741

⛔ ADMOB_BANNER_ID 환경변수는 **없어졌다.** 빠뜨리면 광고도 고지도 없는 앱이
  나가는데 게이트가 전부 통과했기 때문이다(2.102). 값을 고친 뒤에는
  `npm run sync:app`을 다시 돌린다 — gradle만 다시 돌리면 번들이 그대로다.

⚠⚠ **이 기기의 admob.properties는 2026-09-02부터 실제 값 둘을 들고 있다**(2.101·2.102).
  위 데모 값은 시험으로 되돌릴 때 쓰는 것이고, 되돌릴 때는 **둘을 함께** 되돌린다 —
  하나만 바꾸면 퍼블리셔가 갈린다.
```

**⛔⛔ 실기기를 붙일 때마다 — 시험 기기 등록이 **선행 조건**이다 (2026-09-02 · 2.113)**

```
왜   에뮬레이터는 AdMob이 **자동으로** 시험 기기로 잡는다. 실기기는 아니다 —
     붙이는 즉시 **실제 광고가 나간다.** 개발자 노출·클릭은 무효 트래픽이고
     계정이 걸릴 수 있다
순서 ① **결과 화면까지 가서** logcat에서 기기 ID를 읽는다
       ⛔ 앱을 띄우기만 해서는 안 나온다 — **배너는 결과 화면에만 있다**(2.115).
         ID는 광고를 **요청해야** 찍히므로 뽑는 행위가 곧 노출 시도다.
         ⚠ 요청이 No fill이면 노출은 없고 ID만 찍힌다 (2026-09-03 휴대폰이 그랬다)
       adb -s <serial> logcat -d | grep -o 'setTestDeviceIds(Arrays.asList("[A-F0-9]*"))'
       ★ 이 줄이 **보인다는 것 자체가 아직 시험 기기가 아니라는 뜻이다**
     ② AdMob 콘솔 → 설정 → **시험 기기** → 기기 추가 → Android → 그 ID
     ③ 앱을 다시 띄워 배너에 **"Test Ad" 라벨**이 붙는 것을 눈으로 확인한다
     ④ 그때까지 **배너를 누르지 않는다.** 노출도 줄이는 편이 낫다
⛔ 코드(initializeForTesting)로 넣지 않는다 — build:app에 debug/release 구분이 없어
  **시험 설정이 출시본에 남는다**(2.99·2.102와 같은 모양). 사용자 확정 방침이다
★ 등록한 기기 ID는 **app/android/admob.properties**(gitignore 대상)에 적어 둔다.
  키 이름은 admob.testDevice.<구분> 이고 **코드는 읽지 않는다** — 적어 두는 것이 목적이다.
  ⛔ 저장소는 공개다. 기기 ID는 개인 기기를 지목하는 안정적 식별자라 커밋하지 않는다
```

**기기·환경 상태 (2026-09-04 세션 종료 시점 · 마지막에 다시 쟀다)**

```
✅ 실기기 둘 — 빌드 6ce0225 · 권한 granted · 토글 켜짐 · 예약 **14건**
⛔ 에뮬레이터는 **꺼져 있다.** 위 「에뮬레이터」 블록의 명령으로 다시 띄운다
⚠ 2.122(웹 토글 결함)는 **앱 번들을 안 바꿨다** — 바이트 동일이라 재설치가 없었다

휴대폰   Galaxy Z Flip (SM-F700N · **R39N500G7YF**) · Android 13 · **갈래 C**
        1080×2636 · 480dpi = 360×878.7dp · 웹뷰 360×800dp · 3버튼(48dp)
        WebView 145 · env(safe-area-inset-bottom)=0 · 주입값 0.0px
        ✅ **시험 기기 등록 완료** · 알림 시각 09:00
        ⚠ 배터리 세이버 켜짐(low_power=1)이 **원래 상태다.** 켜져 있으면 앱이
          백그라운드일 때 알림이 밀린다(2.116 ①) — 알림을 실측하려면 끈다
태블릿   Galaxy Tab S9 FE (SM-X510 · **R54X204JA8V**) · Android 15 · **갈래 A**
        1440×2304 · 1.75x = 823×1317dp · 인셋 상단 24 · 하단 48(taskbar)
        env=48 · 주입값 48.0px · ✅ **시험 기기 등록 완료** · low_power=0
        ⏱ 알림 지연 14일 관찰이 여기서 돈다.
          ⚠⚠ **기준선이 2026-09-03에 다시 그어졌다** — 이 날 재설치로 예약이
            지워졌다 다시 잡혔다(사용자 승인). ⛔ **09-17 전에 결론을 적지 말 것**
에뮬     pym_phone_360 (**emulator-5554**) · Android 15 · WebView 124 · **갈래 B**
        360×640dp · 웹뷰 592dp · env=0 · 주입값 24.0px
        ⛔ **2026-09-04 종료 시점에 꺼져 있다** — 위 명령으로 다시 띄운다
        ★ 1080×1920(9:16)이라 Play 휴대전화 스크린샷 요건을 그대로 만족한다.
          ⚠ 스크린샷을 찍을 때 **상단 띠 처리**가 필요하다 — 3절 「스크린샷 방침」
✅ 시험 기기 등록   두 실기기 모두 콘솔에 등록됐다(AAID 기준 · 2.117).
   배너에 「테스트 광고」 라벨이 붙는 것을 눈으로 확인했다 —
   ⛔ 그래도 광고를 **누르지는 않는다**
   ⚠ 그 라벨이 **스크린샷에도 찍힌다** — 스토어용을 어떻게 만들지는 3절 판단 S1이다
✅ 세션이 바꾼 것은 전부 되돌렸다
   svc power stayon → false(실기기 둘) · low_power → 원래대로(휴대폰 1 · 태블릿 0)
   회전 잠금 → 원래대로 · 알림 시각 → 09:00
   권한 플래그(pm set/clear-permission-flags) → 원래대로
   Capacitor 권한 캐시(PluginPermStates.xml) → 지웠다.
     ⚠ 태블릿에만 빈 파일(`<map />`)이 남아 있다 — 내용이 없어 판정에 영향이 없다
   /sdcard/toast.mp4 녹화본 → 셋 다 지웠다(확인함)
★ 배너를 건드렸으면 **python scripts/measure_banner.py** 로 셋을 한 번에 잰다
```

**⚠ 이 기기에 깔린 것 — 저장소 밖 (2026-09-03)**

```
pip install websocket-client   토스트를 프레임 단위로 재려고 넣었다(2.120).
                              ⛔ requirements.txt에 **넣지 않았다** — 파이프라인이
                                쓰지 않고 시험 도구만 쓴다
⚠ 실측 도구(cdp.py · toast_measure.py 등)는 **저장소에 없다.** 방법만 2.120에 적었다
```

**(이력) 기기·환경 상태 (2026-09-02 세션 종료 시점)**

```
에뮬레이터  pym_phone_360 (emulator-5554) · Android 15 · WebView **124** = **갈래 B**
          1080×1920 · 480dpi = 360×640dp · 하단 인셋 24dp(제스처)
          설치된 빌드  이 세션의 debug (2.112 안 2까지 들어간 것)
          ✅ 원복 완료 — 제스처 내비 · 비행기 모드 해제 · stylus_handwriting 1
          ⚠ 3버튼으로 바꿔 재려면
            adb shell cmd overlay enable com.android.internal.systemui.navbar.threebutton
            (끝나면 disable + gestural enable)
          ⚠ 키보드를 재려면 stylus_handwriting_enabled 를 **0으로 꺼야 한다** —
            켜져 있으면 탭에 필기 도구가 떠 뷰포트가 안 줄어든다(2.107에서 헛짚었다)

태블릿     Galaxy Tab S9 FE (SM-X510 · serial R54X204JA8V) · Android 15
          WebView **151** = **갈래 A** · 1440×2304 · 1.75x = 823×1317dp
          인셋 상단 24dp · 하단 **48dp**(삼성 taskbar — 3버튼과 같은 값)
          설치된 빌드  이 세션의 debug (2.112 안 2까지)
          ⛔ **시험 기기 미등록 상태다 — 배너를 누르지 말 것**(2.113)
          ✅ 설치가 데이터를 보존했고(같은 debug 키) 앱을 열자 알림 예약 97건이 복구됐다
          ⚠ 설치는 예약을 지운다. **앱을 한 번 열어야** refreshSchedule이 다시 잡는다
          ⏸ **2026-09-03 오전에 USB에서 빠졌다.** 그 뒤 작업은 휴대폰만으로 했다

✅ **태블릿 재연결 — 아래 순서대로 끝냈다 (2026-09-03). 기록으로 남긴다**

```
⛔⛔ **①을 먼저 한다. 새 빌드를 설치하면 「전」을 영영 못 잰다.**
   지금 태블릿에 깔린 것은 **allowWhileIdle=false 시절의 빌드**다(09-03 오전).
   설치해 버리면 대조군이 사라진다

① (설치 전) 안 A의 「전」을 Android 15에서 잰다
   settings put global low_power 1        ← 실험. 끝나면 반드시 0으로 되돌린다
   앱을 홈으로 내리고 2분 이상 기다린 뒤
   dumpsys alarm | grep -A2 peaceinmind/com.capacitorjs → battery_saver 값
   ★ 보는 것: Android 15에서도 **+364일**로 밀리는가.
     휴대폰(Android 13)에서만 쟀으므로 OS 버전 차이가 미검증이다
② low_power를 **0으로 되돌린다** (원래 상태다. 14일 관찰이 이 위에서 돈다)
③ 새 빌드를 설치하고 앱을 한 번 연다 (예약 복구)
④ (설치 후) 같은 조건으로 다시 잰다 — 정책값이 음수가 되는가
⑤ ✅ AAID는 사용자가 기기에서 직접 읽어 콘솔에 등록했다 (두 기기 모두 등록 완료).
   admob.properties의 admob.aaid.tablet.SM-X510 에 적어 두었다
★ ①~④ 결과는 2.116 ①의 「태블릿에서도 같다」 절에 있다
⚠ 시스템 바 색 대조는 **이미 끝났다**(2.115) — Android 15에서 무시되는 것을 확인했다.
  다시 잴 것이 없다
⚠ 배너 기하(3번)는 **휴대폰 쪽 항목이다.** 태블릿은 2.111·2.112에서 이미 쟀다
```

휴대폰     Galaxy Z Flip (SM-F700N · serial R39N500G7YF) · **Android 13**
          WebView **145** 인데 **갈래 C** — 창이 edge-to-edge가 아니다 (2.115)
          1080×2636 · 480dpi = 360×878.7dp · 웹뷰 360×800dp
          인셋 상단 30.7dp · 하단 **48dp(3버튼)** — 웹 좌표계에서는 둘 다 0으로 읽힌다
          설치된 빌드  2026-09-03 debug (시스템 바 색 수정 포함)
          ⛔ **시험 기기 등록 대기 — 배너를 누르지 말 것.** ID는 뽑아 두었다
            (app/android/admob.properties 의 admob.testDevice.phone.SM-F700N)
          ✅ 진동 모터 있음 — 2.94를 여기서 닫았다
          ⚠ **배터리 세이버가 켜져 있다**(low_power=1). 켜져 있으면 구절 알림이
            364일 뒤로 밀린다(2.115). 알림을 실측하려면 먼저 꺼야 한다
          ⚠ 알림 권한은 허용됨 · 알림 시각 09:00으로 되돌려 두었다
```

⚠ **새로 clone하면 APK 빌드가 선다. 그것이 의도다** — 이 값이 없거나 틀리면
앱이 시작하자마자 죽으므로(2.98), 실패를 실행 시점이 아니라 빌드 시점으로 당겼다.
메시지가 무엇을 하라고 말한다.

```
> AdMob App ID가 없습니다.
    android/admob.properties.example 을 android/admob.properties 로 복사하고 값을 채우세요.
    시험용 데모 ID(계정 불필요): ca-app-pub-3940256099942544~3347511713
    ⛔ admob.properties는 저장소에 커밋하지 않습니다.
```

✅ 형식도 검사한다 — 광고 단위 ID(`/` 구분)를 App ID 자리에 넣으면 잡는다.

---

**시험 기기 — Galaxy Tab S9 FE (`R54X204JA8V`) · Android 15**

```
⛔ 진동 모터가 없다   android.hardware.vibrator 미보유 (FE 계열 공통)
                    모든 진동 요청이 ignored_unsupported로 기록된다
                    → **채널 진동은 이 기기로 검증할 수 없다.** 휴대폰이 필요하다
⛔ 알림 LED가 없다    channel.mLights=false로 둔 것과 무관하게 확인 불가
⚠ 태블릿이다        홈 화면 아이콘·레이아웃이 휴대폰과 다르게 보인다
```

⚠ **다음 세션이 이것을 모르면 진동을 처음부터 다시 조사한다.** 2.94절이 그 기록이다.

**알림을 로그로 확인하는 법** — 사람 귀는 이 조사에서 세 번 틀렸다.

```bash
# 우리 채널로 나갔는가 (여기부터 본다)
"$ADB" shell logcat -b events -d | grep notification_enqueue | grep peaceinmind
# 채널 실제 등록값
"$ADB" shell dumpsys notification --noredact | grep -A4 "AppSettings: io.github.appleap385"
# 소리가 실제로 재생됐는가 (player event:started)
"$ADB" shell dumpsys audio | grep -E "piid|Ringer mode|STREAM_NOTIFICATION" -A3
# 예약 상태
"$ADB" shell dumpsys alarm | grep peaceinmind -A3
```

⛔ **앱을 강제 종료하지 말 것** — 안드로이드가 그 uid의 AlarmManager 예약을 전부
  지운다. 예약해 둔 알림이 통째로 사라진다. 백그라운드로 내리는 것은 무해하다.

---

### 실행 환경 — ⚠ 로컬 셸에 API 키가 **반드시** 필요하다

```bash
export YOUTUBE_API_KEY="..."        # PowerShell: $env:YOUTUBE_API_KEY="..."
```

**Actions Secret은 러너 전용이라 로컬 실행에는 잡히지 않는다.** 키 없이
`build_videos.py`(드라이런 제외)와 `suggest_channels.py`를 돌리면 거기서 멈춘다.

**API를 쓰지 않는 것 (키 없이 돈다)** — 다음 세션 작업 대부분이 여기 속한다.

```
retag_titles.py (4개 모드) · verify_verses.py · fill_verses.py · gen_*.py
tagging_test.py · spread_test.py · messages_test.py · 앱 테스트 69개
build_videos.py --dry-run
```

```bash
# 배치 (약 62 units) — --dump-titles를 켜두면 이후 분석이 전부 API 0으로 된다
python scripts/build_videos.py --previous dist/videos.json --dump-titles --out-dir dist

# 앱 (로컬 확인)
cd app && npm run dev        # http://localhost:5173/PeaceYourMind/  (점유 시 5174)
cd app && npm test           # 69개
```

**API 누적과 다음 실행**

```
로컬 누적    2026-08-18(PT) 1,506 units — 발굴 2회(661×2) + 배치 3회(61·61·62)
             2026-08-19(PT)에는 로컬 소모 0. 오늘 작업은 전부 API 0이었다
Actions      2026-08-19 09:59 UTC cron 64 units (러너 쿼터는 로컬 로그에 안 잡힌다)
             그날 여유 9,436 / 일일 상한 9,500 · 하드캡 9,800
다음 cron    ★ 2026-08-26부터 **하루 2회**다 (2.68절)
               09:30 UTC = KST 18:30   기존 슬롯. 주간 진단·기준선은 여기서만 돈다
               21:30 UTC = KST 06:30   신규. KST 00시 예약발행·새벽 물량을 겨냥
             **키 없이 도는 자동 실행이다**(Actions Secret). 로컬에서 배치를
             돌릴 때만 아래 환경변수가 필요하다.
             ⚠ Actions 지연으로 예약보다 30분쯤 늦게 시작하는 것이 정상이다
             ⚠ 위 "Actions 64 units"는 채널 15개 시절 값이다. 22채널은 1회 ~93,
               하루 2회 186 (일일 상한 9,500 대비 2.0%)
```

쿼터는 **태평양 자정**에 리셋된다. `.quota_log.json`은 커밋하지 않는다(기기마다 다름).

⚠ **2026-08-20 cron부터 [1] 폴백 품질 필터가 실린다** (2.26절). 수정 전 기준선은
2.25절 수치(폴백 151 · 감정신호 없음 196)이고, 그것은 2026-08-19 배치가 남긴 값이다.
새 실행에서 볼 것은 `build_report.json`의 `tagging.fallback_excluded` —
로컬 재현에서 20건이었다. 크게 벗어나면 사전이 과하게 잡고 있다는 뜻이다.
구절은 앱 번들이라 배치와 무관하다 — 확장 전후로 화면이 달라 보이면 영상 쪽 변화다.

⚠ **[1]~[7]은 전부 API 0으로 검증된다.** 배치를 돌려 확인할 필요가 없다 —
`retag_titles.py --screens`가 배포본과 일치함이 2.23절에서 확인됐다.
실 배치는 고치고 나서 cron이 알아서 돌 때 반영된다.

### 채널 승인 — 완료 (2026-08-18)

기준 1c 대조와 등록은 2.11절, 사실과 출처는 `channel_criterion_c.20260818.md`에 있다.

**등록 15건** — `channel_allowlist.yaml`

```
교회 5   오륜교회(예장합동 수경노회) · 꿈의교회(기감 경기연회) ·
         낮은담교회(기침) · 우리들교회(교단 미표기) · 베이직교회(교단 미표기)
기관 10  CTS기독교TV · CGN · CGN 생명의 삶 · CGN 성경통독 ·
         CBSJOY · CBS설교 · ANOINTING · 옹기장이선교단 ·
         극동방송 · 광주극동방송

content_type     sermon 6 · mixed 5 · devotion 2 · worship 2
                 → media_type 판별에서 3순위다(제목 → 장절 → 채널 → 길이).
                   2026-08-19 개정 전에는 1순위여서 찬양 81건이 sermon으로 잘못
                   판정됐다 (2.14절)
crisis_eligible  전건 false — 위 9번 참조
```

**15는 하한선과 같다** (`MIN_ALLOWLIST_SIZE = 15`). 한 건이라도 빠지면
`allowlist_undersized` 경보가 뜬다.

**제외 1건** — `channel_reviewed_out.yaml`, 연세중앙교회, `criterion: "1c"`,
`recheckable: true`. 재검토 조건은 **예장합신 제85회(2000) 결의 원문 확인**이다.
2차 자료로는 재검토하지 않는다 — 이번에 그쪽 서술이 통합 결의와 어긋났다.

> **승인 판단은 사람이 했다.** 조사는 사실과 출처만 모아 시트에 올렸고,
> 승인/부결 열은 비운 채 넘겼다. 이 원칙은 다음 발굴에서도 같다 —
> 검색은 이단 채널도 똑같이 데려오고, 그들도 주일예배와 성경 강해를 올린다.
> `자동 점검: 통과`는 승인이 아니라 **객관적 실격 사유가 없다**는 뜻이다.

**다음에 채널을 추가할 때 그대로 쓸 것**

- `affiliation` / `affiliation_verified`가 비면 로더가 실패한다. 근거 없는 승인을
  구조로 막아 둔 것이고, 고장 테스트로 실제 차단을 확인했다(2.11절).
- `affiliation_verified`에는 a·b·c를 무엇으로 확인했는지 **URL을 포함해** 적는다.
- **`channel_id`는 이름이 아니라 채널 설명의 고유 표지로 대조한다.** 동명 채널이
  실재하고, 그중에는 아예 다른 교회도 있다. 금지 ID 3건은 allowlist 헤더에 있다.

### 3차 발굴 메모 (위 10번의 상세)

`--query-set`으로 세트를 고른다. 1·2차 검색어는 보존돼 있다.
3차를 짤 때는 **1·2차와 다른 갈래**여야 한다 — 2차에서 중복이 3건뿐이었던 것은
어휘를 분리한 결과다. `C채널`은 2차에서 6종 제한 때문에 뺐으니 후보다.

**기검토 제외 11건은 자동으로 상위 N에서 빠진다**(`channel_reviewed_out.yaml`).
재검토하려면 그 파일에서 항목을 지워야 한다 — 재검토는 판단이지 자동화 대상이 아니다.
11건 중 연세중앙교회는 `criterion: "1c"`이고, 재검토 조건이 **합신 총회 원문 확인**이라
검색으로는 풀리지 않는다(2.11절).

### 준비 완료 (2026-08-18)

**API 키** PYM 전용 발급 + Actions Secret 등록 완료 (로컬 실행 요건은 위 참조).

**`gen_verses_json.py`** — `verses.yaml` -> `verses.json`. API 소모 0.
`note`·`verified*`를 제거하고 `crisis`를 최상위로 분리한다.
출력 60,613바이트 (PLAN 4.1 예상 50KB 근사).
게이트 5종: `verified: false` 잔존 / 위기·감정 풀 분리 / 입출력 id 집합 일치 /
개수 기대치(240 + 10) / theme·emotion_tags가 themes.yaml에 있는가.
**본문은 공백 정규화 후 원문과 다시 대조한다** — YAML 폴디드 스칼라가 붙이는
개행이 앱 문자열에 섞이던 것을 여기서 잡았다.

**`suggest_channels.py`** — FYM 이식 + PYM 고유 변경.
`lib/`에 quota · quota_log · youtube · actions_status · normalize ·
channel_blocklist를 이식했고 allowlist는 PYM 필드로 새로 썼다.

**`build_videos.py` + `lib/themes.py`·`tagging.py`·`filters.py`·`alerts.py`**
(2026-08-19). 일일 배치 본체와 그 부품이다. 드라이런 전 과정 통과,
테스트 2종(`tagging_test.py`·`spread_test.py`) 통과. 경위는 2.12절.

### 변함없는 원칙

실제 API를 소모하는 실행은 명령어와 예상 쿼터를 먼저 제시하고 승인을 받는다.
`--dry-run`은 API를 부르지 않지만 파일은 쓴다 — 산출물 경로가 겹치지 않는지
항상 확인한다 (FYM 사고의 교훈, 2.8절).

### 구절 쪽에서 넘기는 것

- **CI 게이트 연결** — `verify_verses.py --check`를 배포 전 단계에 건다.
  `verified: false`가 하나라도 있으면 배포되지 않는다.
- **주제별 구절 수 편중** — 영상 풀 설계 시 참고. `hope` 21 · `joy_praise` 20 ·
  `gratitude` 16이 많고, `forgiveness` 2 · `presence`/`love`/`strength`/`justice`/
  `creation` 5로 적다. `forgiveness`가 2건인 것은 의도적이다(3차분 2.6절 참조).

### 큐레이션을 다시 열 때 — 남은 것

- **길이 상한은 정해졌다** — 카드 UI가 나온 뒤 실측으로 22/20/18px 3단계를
  확정했고(2.18절 [8]), 최장 `jer.17.7`(131자)이 360×640에 들어오는 것을
  확인했다. **상한 구간(18px)이 한계선이라 여기서 더 키우면 넘친다.**
  즉 이 항목은 "상한을 정한다"가 아니라 "이 선을 지킨다"로 바뀌었다.
- **1차분 `[검토 필요]` 2건** — `isa.30.15` · `php.4.19` (4.1절). 그대로 남아 있다.
- **복수 태깅 13건 소급 검토** — 확장 후에도 13건 그대로다(실측). 확장분은
  전부 단일 태깅으로 넣었다. 확정 규칙 1을 소급하면 재검토 대상이다.
- **한 화면 안의 같은 시편 반복** — 2026-08-19 확장에서 새로 드러난 것.
  시 119→`boredom.dull` 4회 등. "다른 구절 보기"가 id 순환이라 연달아 나올
  수 있다. 상세는 2.24절 편중 자료.

---

## 4. 열려 있는 결정 — 다음 세션이 판단해야 할 것

### 4.1 1차분에 남은 2건의 선정 판단 (본문 아님)

첫 세션은 본문 정확성만 다뤘고, 2차분에서도 1차분 구절의 선정 판단은
건드리지 않았다. 아래 2건은 `note`에 `[검토 필요]`가 붙은 채
`verified: true`(본문은 원문과 일치)로 남아 있다.
**본문이 맞다는 것과 수록해야 한다는 것은 다른 판단이다.**

- `isa.30.15` — 후반부 "너희가 원치 아니하고"가 책망 맥락.
  전반부만 쓰면 proof-texting, 전체를 쓰면 톤이 꺾인다. 초안 note는 제외 쪽으로 기울어 있다.
- `php.4.19` — 공급 약속 구절. 번영신학 오용이 많고, `themes.yaml`의
  전역 금지어(`축복`) 원칙과의 일관성 문제가 제기돼 있다.

### 4.2 `verified_at` 형식 불일치 3건 (기능 영향 없음)

`psa.131.2` · `1pe.5.7` · `isa.41.10`의 `verified_at` 값이
`\"2026-08-18\"`로 백슬래시가 섞여 있다 (YAML 파싱 시 리터럴 문자열이 된다).
1차분 작성 시의 오타다.

**사용자가 "그대로 둔다"를 선택했다** — 웹 원문 대조 이력을 원형 보존하는 쪽.
스크립트는 기존 `verified: true`를 건드리지 않으므로 이 값은 유지된다.
`verified_at`을 날짜로 파싱하는 코드를 나중에 쓸 일이 생기면 이 3건이 걸린다는 것만 기억할 것.

### 4.3 **FYM에 남아 있는 결함 — 그쪽 세션에서 고쳐야 한다**

> ⚠ **이 항목은 PYM이 아니라 FYM(`Demo_28_FYM`)의 할 일이다.**
> PYM에서 발견했지만 PYM에서는 고칠 수 없다 — 다른 저장소다.

**증상** `scripts/lib/allowlist.py`와 `scripts/lib/channel_blocklist.py`의
필수 필드 검사가 **빈 값을 통과시킨다.**

```python
missing = [f for f in REQUIRED_FIELDS if not str(entry.get(f, "")).strip()]
```

YAML에서 값을 비운 키(`reviewed_by:`)는 빈 문자열이 아니라 `None`으로 파싱된다.
`str(None)`이 `"None"`이라 truthy가 되어 검사를 빠져나간다.
기본값 `""`는 **키 자체가 없을 때만** 쓰이므로 이 경로를 막지 못한다.

**왜 실제로 문제가 되는가** FYM `suggest_channels.py`가 내보내는 붙여넣기용
YAML 블록에는 `note`가 자동 수집 문구로 채워져 있지만, 사람이 그 항목을 손보다가
필드를 비워두는 일이 생긴다. 그러면 검증 없이 화이트리스트에 들어간다 —
로더가 "필수 필드가 있다"고 판정하기 때문이다.

**PYM에서 한 수정** `lib/allowlist.py`에 `_is_blank()`를 두고 두 로더에 적용했다.

```python
def _is_blank(value: Any) -> bool:
    return value is None or not str(value).strip()

missing = [f for f in REQUIRED_FIELDS if _is_blank(entry.get(f))]
```

**영향 범위 — PYM에서 미리 확인해 뒀다 (2026-08-18)**

```
결함이 있는 파일   scripts/lib/allowlist.py:107
                  scripts/lib/channel_blocklist.py:72
                  (`str(entry.get(` 로 훑어 두 곳뿐임을 확인했다)

현재 실제 피해     없음 — channel_allowlist.yaml 16개 채널 전부 필드가 차 있다
```

**즉 지금 잘못 들어간 채널은 없다.** 잠재 결함이므로 급하지는 않지만,
FYM도 `suggest_channels.py`로 후보를 계속 추가하는 구조라 언제든 발현될 수 있다.

**FYM에서 할 일**
1. 두 파일에 `_is_blank()` 적용 (위 PYM 수정과 동일)
2. 고친 뒤 빈 필드 YAML로 거부되는지 한 번 확인
3. 채널을 새로 추가하기 전에 고쳐 두는 편이 낫다 — 추가 시점이 발현 시점이다

### 4.4 **FYM에 남아 있는 잘못된 주석 — 그쪽 세션에서 고쳐야 한다**

> ⚠ PYM에서 발견했지만 PYM에서는 고칠 수 없다 — 다른 저장소다.
> 4.3절(`_is_blank` 결함)과 같은 성격의 항목이다.

**증상** 반복 축약 규칙의 **예시가 실제 동작과 다르다.**

```
FYM scripts/lib/normalize.py:19   "짜증나아아아" → "짜증나아"   (주석)
FYM taxonomy.yaml:240             example: "짜증나아아아 → 짜증나아 / ㅠㅠㅠㅠ → ㅠㅠ"
실제 동작                          "짜증나아아아" → "짜증나아아"
```

**어느 쪽이 틀렸는가 — 예시가 틀렸다.** 규칙은 "3회 이상 반복되는 **문자**를
2회로 축약"이고 구현이 그대로 한다. "짜증나아아아"에서 줄어드는 것은 "아" 3개이며
앞의 "나"는 별개 음절이라 남는다. 예시대로 "짜증나아"가 되려면 3개를 1개로 줄여야
하는데 그건 규칙과 다르다.

**실제 사용자 입력으로 검증했다 (PYM에서)**

```
ㅋㅋㅋㅋㅋ      -> ㅋㅋ            축약
짜증나아        -> 짜증나아         2회는 그대로
짜증나아아      -> 짜증나아아       "아" 2개 — 대상 아님
짜증나아아아    -> 짜증나아아       "아" 3개 → 2개
너무너무너무    -> 그대로           어절 반복은 대상이 아니다
괜찮아아아!!!   -> 괜찮아아         문장부호 제거 후 축약
```

**분류 결과는 흔들리지 않는다** — "짜증나"와 "짜증나아아아"가 같은 세분류로 가고,
"죽고싶"과 "죽고싶ㅠㅠㅠㅠ"는 둘 다 위기다. 즉 **동작상 버그는 없고 문서가 틀린 것**
이며, 그래서 급하지는 않지만 고치지 않으면 다음 사람이 예시를 믿고 잘못된 전제로
작업하게 된다(축약 후에도 음절이 남는다는 사실을 놓친다).

**FYM에서 할 일**
1. `scripts/lib/normalize.py`의 주석 예시 교체
2. `taxonomy.yaml` `normalization.example` 교체
3. PYM이 쓴 문구를 그대로 가져가면 된다 (PYM 세 곳을 2026-08-19에 고쳤다 —
   `scripts/lib/normalize.py` · `taxonomy.yaml` · `app/src/lib/normalize.js`)

### 4.5 부정 키워드가 없어 못 막는 오탐 2건 — 판단 필요 (2026-08-19 발견)

사전 보강(2.17절) 실측 중에 드러났다. **이번 개정이 만든 것이 아니라 원래
있던 것이고, 어간을 줄여도 그대로다.**

```
"[아침예배] 불행을 기뻐하는 죄 (겔 25:1-17)"              → joy_praise
"[생명의 삶 큐티] 하나님 백성의 패망을 기뻐하는 죄"        → joy_praise
```

`기뻐`(기쁨의 찬양)에 걸렸는데 내용은 **남의 불행을 기뻐하는 죄**에 대한 설교다.
`joy.delight`·`joy.grateful` 화면은 즐거운 사람이 보는 자리라 어긋남이 크다.

**고치려면 태깅 엔진에 부정 키워드가 있어야 한다.** 지금은 긍정 매칭만 있다.
넣으려면 `themes.yaml` 스키마(`exclude_keywords`) · `lib/tagging.py` ·
로더 검증 · `tagging_test.py`가 함께 움직인다. 그래서 이번 범위에서 뺐다.

```
판단할 것   1) 2건이 이 비용을 정당화하는가 — 매일 도는 배치라 오늘 2건이지
              고정된 2건은 아니다. 며칠치 추이를 보고 정하는 것이 맞다
            2) 부정 키워드를 열면 사전이 복잡해진다. 대안은 "죄"·"심판"처럼
              방향을 뒤집는 어휘가 제목에 있으면 밝은 계열 주제를 붙이지 않는
              **전역 규칙** 하나다 — 주제마다 예외를 적는 것보다 단순하다
관찰 방법   python scripts/retag_titles.py --probe 기뻐
```

### 4.6 헤드라인이 360px에서 단어 중간에서 끊긴다 — 두 앱 공통 (2026-08-19)

```
FYM  "오후는 어떻게 지나가고 있나 / 요"
PYM  "반가워요. 오늘 마음은 어떤가 / 요?"
```

25px 명조에 폭 316~320px이면 두 줄이 되고, 기본 `word-break: normal`이
한글을 음절 단위로 끊는다. `word-break: keep-all`을 주면 어절 단위로 접혀
"…오늘 마음은 / 어떤가요?"가 된다.

**PYM에 넣지 않았다.** 이번 지시가 "FYM 실제 스타일에 맞춰라"였고 이건 FYM의
현재 동작이라, 한쪽만 고치면 두 앱이 갈린다. 공감 문구·About 본문에는 이미
`keep-all`이 들어 있어 **같은 앱 안에서도 규칙이 갈려 있는 상태**다.

```
판단할 것   두 앱에 함께 넣을 것인가 (FYM 쪽도 같이 고쳐야 한다)
            keep-all은 긴 어절이 있으면 오히려 넘칠 수 있다 —
            인사 문구 풀에는 그런 어절이 없어 위험은 낮다
확인 방법   360px에서 인사 풀 전체를 돌려 보고 결정한다
```

### 4.7 정규화가 어절 경계를 지운다 — 2음절 키워드 236개 공통 (2026-08-19 발견)

`normalize()`는 띄어쓰기를 지운다. 우회를 막기 위한 설계이고("죽 고 싶" →
"죽고싶") 그 목적에는 맞다. 그런데 부작용이 있다 — **짧은 키워드가 어절
경계를 넘어 매칭된다.**

```
"말도 안 되지 쳐다보기도 싫어요"  →  "말도안되지쳐다보기도싫어요"  →  "지쳐" 적중
"쉽지 치우기도 싫어요"          →  "쉽지치우기도싫어요"          →  "지치" 적중
```

세분류 키워드 893개 중 **2음절이 236개(26%)**이고 전부 같은 조건이다.
`지쳐`를 넣으며 발견했지만 그것이 만든 문제가 아니다(2.19절).

```
고치려면   매칭에 어절 경계 개념을 넣어야 한다. 정규화 문자열과 함께
           "원문에서 이 위치가 어절 시작인가"를 들고 다녀야 하고,
           앱(JS)과 배치(Python) 두 구현이 문자 단위로 같아야 한다
           (normalize.parity.test.js가 그 동일성을 검사 중이다)
안 고치면   오탐은 대개 **같은 계열 안**에서 난다 — 위 두 예는 모두
           exhaustion으로 가고, 방향이 반대인 화면으로 가지는 않았다.
           위기 키워드는 대부분 3음절 이상이라 이 위험이 낮다
판단 필요   실사용에서 이런 입력이 실제로 들어오는지 알 수 없다 —
           입력을 기록하지 않기 때문이다. 그것이 이 앱의 설계이므로
           **"실측 후 판단"이 불가능한 항목**이라는 점을 알고 정해야 한다
```

두 앱 공통 과제다. FYM도 같은 normalize를 쓴다.

**★ 2026-08-20 — 이 항목을 둘로 갈랐다. 한 덩어리가 아니었다.**

```
제목 태깅 (배치)   lib/tagging.py       ✔ 고쳤다 (2.29절)
                  normalize.parity 제약을 받지 않는다 — 배치 전용이다
                  경계 판정을 정규화와 분리했다(_normalized_with_gaps).
                  2음절 이하 키워드에만 적용해 오탐 4건 제거·정탐 손실 0
입력 분류 (앱)     app/src/lib/         남아 있다 — 3절 [7]에서 판단한다
                  앱(JS)·배치(Python) 두 normalize가 문자 단위로 같아야 하고
                  (normalize.parity.test.js), 실측 코퍼스도 없다(입력을
                  기록하지 않는 것이 이 앱의 설계다). **비용이 완전히 다르다.**
```

아래 원문은 두 구현을 묶어 놓은 시점의 기록이다. 제목 태깅 쪽은 해결됐다.

### 4.8 ★ `.rise`의 transform이 `position: fixed`를 무력화한다 (2026-08-19 발견)

**앞으로 화면에 고정할 요소를 넣을 때 반드시 걸리는 함정이라 따로 적는다.**

```css
.rise{ animation: rise .7s ease both }
@keyframes rise { from{opacity:0;transform:translateY(10px)} to{opacity:1;transform:translateY(0)} }
```

`fill-mode: both`라 애니메이션이 끝나도 마지막 키프레임의 `transform:
translateY(0)`이 남는다. 계산값은 `matrix(1,0,0,1,0,0)`이고 **`none`이 아니다.**
CSS 규칙상 `none`이 아닌 transform은 **fixed 자손의 containing block을 만든다.**

즉 `.rise` 안에 있는 `position: fixed` 요소는 **뷰포트가 아니라 그 div를
기준으로 잡히고, 화면에 붙어 있지 않고 문서와 함께 흐른다.**

**FYM과 PYM의 동작이 갈린 이유가 정확히 이것이었다.** 컴포넌트 스타일은
글자 단위로 같았고(`position: fixed; right: 20; bottom: 24`) 렌더 위치만 달랐다.

```
             렌더 위치              스크롤 +310px 시 뷰포트 위치 변화
FYM   App 최상위, .rise 바깥          0px    (진짜 fixed)
PYM   Result 안 .rise 내부         −310px    (문서에 붙어 흐름)
```

⚠ **앞으로 fixed 요소를 둘 때는 `.rise` 바깥, App 최상위에 렌더할 것.**
`transform`뿐 아니라 `filter`·`perspective`·`will-change`·`backdrop-filter`도
같은 효과를 낸다. 증상이 "fixed인데 안 붙는다"로 나타나므로 CSS만 봐서는
원인을 못 찾는다 — 조상을 거슬러 올라가며 계산된 transform을 봐야 한다.

이번 건은 그 실측 방법까지 남긴다: 스크롤 전후로 `getBoundingClientRect().top`을
재서 변화가 0이면 진짜 fixed, 스크롤량만큼 움직이면 조상이 containing block을
만들고 있는 것이다.

---

## 5. 하지 않은 것 (범위 밖이었음을 명시)

- **폴백 반영 후의 실 배치** — 코드는 확정됐고 실측 데이터(`dist/titles.json`)로
  검증했지만, 새 코드로 API를 다시 돌리지는 않았다. `dist/videos.json`은 아직
  개정 전 구조(`themes` 키)다
- **사전 어간 보강** — 곡명 겹침 7.8% → 19% 추정치까지만 냈다(2.14절).
  어간을 줄일 때마다 오탐 실측이 필요해 별도 작업으로 뺐다
- **폴백 중복 노출** — worship 기본값 세분류 17개가 폴백 앞부분을 공유한다.
  세분류별 오프셋으로 해소되지만 실사용 로그를 본 뒤 판단한다 (2.14절)
- **`channel_zero_yield` 경보 조건 보강** — 지금은 "필터 후 잔존 0"만 잡는다.
  실측에서 잔존 100건인데 태깅 0인 채널(CGN 성경통독)이 나왔다. 다만 폴백
  도입으로 그 채널도 화면에 나가게 됐으므로, 이제 "기여 0"의 정의부터 다시
  세워야 한다 — 태깅 0이 곧 무기여가 아니다
- **제목 용어 blocklist** — `apply_filters`에 자리만 뒀고 비워 뒀다(2.12절).
  Phase 3에서 FYM taxonomy.yaml을 이식하면 들어온다
- **taxonomy.yaml(감정 체계) 이식** — Phase 3 몫이다. 그래서 themes.yaml 빌드
  검증 1번(mapping ↔ 세분류 대조)이 아직 건너뛰기 상태다
- 앱 코드 — `app/`에는 `verses.json`만 있다
- `verses.yaml`의 `note`·선정 판단 변경 — 본문 `text`만 고쳤다
- **1c 대조의 미완 부분** — 합동·합신·기감·기침의 이단 규정 목록을 1차 자료로
  전수 대조하지 못했다. 공개된 통합 목록을 찾지 못해서다. 승인 15건은 그 한계를
  전제로 한 판단이고, 시트 1절이 경계를 명시한다
- **연세중앙교회 재검토** — 합신 85회 결의 원문 확인은 웹으로 풀리지 않았다.
  총회에 직접 조회해야 한다
- ~~GitHub Actions 워크플로 2종 (build / deploy-app)~~ — **2026-08-19 완료** (2.15 · 2.16절)
- `themes.yaml` 수정 — 이번 세션도 **읽기만 했다.** 태깅·판별 상수(주제당 15~20건,
  채널당 3, 미태깅 경보 임계)는 코드에 뒀다. themes.yaml은 큐레이션 문서라
  배치 튜닝 값을 섞으면 두 종류의 변경이 한 파일에서 뒤엉킨다 (lib/themes.py 주석)
- `themes.yaml` 주제 정의 수정 — 개별 구절 채택에서 intent와 어긋난 판단을 여러 번
  했지만(빌 4:13 제외, `self_control`을 "쏟아냄"으로 해석, 잠언 분노 구절
  대부분 제외) 주제 정의는 건드리지 않았다. 근거는 각 `note`에 있다

---

## 6. 파일 지도

```
PLAN.md                      목표·제약·아키텍처·단계별 계획
themes.yaml                  주제 24개 + 감정→주제 매핑 + 키워드 설계 원칙
channel_allowlist.yaml       채널 승인 기준 + 승인 15개 (2026-08-18). 하한선과 같다
channel_blocklist.yaml       채널 차단 (등록 0개) — PYM에서는 2차 안전망
channel_reviewed_out.yaml    검토 후 제외 기록 11건 — 발굴 시 상위 N에서 뺀다
channel_criterion_c.*.md     기준 1c(이단 규정 대조) 근거 시트. 대조 범위·출처 계층이
                             여기 있다. 채널을 추가할 때 같은 형식으로 남긴다
verses.yaml                  구절 299건 (감정 289 + 최상위 crisis 10). 검수 절차가 헤더에
HANDOFF.md                   이 문서

data/krv/
  bible_1961_krv.json        개역한글 원문 66권 (커밋 대상 — 대조의 기준)
  SOURCE.md                  출처·채택 근거·갱신 절차

scripts/
  build_videos.py            **일일 배치 진입점** — 오케스트레이션 + CLI (단계별 로직은 lib)
  verify_verses.py           본문 대조 + verified 갱신 + --check 게이트
  fill_verses.py             빈 text를 원문에서 추출해 채움 + --verify 경로 검증
  gen_verses_json.py         verses.yaml -> verses.json (앱 번들, crisis 최상위 분리)
  suggest_channels.py        채널 후보 발굴 — search를 쓰는 유일한 스크립트 (월 1회)
  tagging_test.py            주제·형식 판별 + themes.yaml 게이트 6종 (고장 주입)
  spread_test.py             채널 분산·형식 균형·위기 유지·무결성 단언 (고장 주입)
  retag_titles.py            제목 덤프 재태깅 분석 — 사전을 고칠 때 쓰는 저울.
                             게이트가 아니다. build_subcategories를 직접 불러
                             화면까지 재현한다(--screens)
  requirements.txt           PyYAML · requests
  lib/themes.py              themes.yaml 로더 + 빌드 검증 6종 + 선정 정책 상수
  lib/tagging.py             제목 → 주제 / media_type 판별 (API 0).
                             구조 신호 3종 — 본문 장절 · 설교자 크레딧(2.83 ①) ·
                             곡명 나열(2.89 안 A). 어휘 좁히기 1종(2.89 안 B)
  lib/collect.py             승인 채널 → uploads → 후보 + 승인 목록 밖 배제
  lib/selection.py           채널 분산 + 형식 균형 + 폴백 선정 (spread_test.py가 검증).
                             폴백 90일 컷(drop_stale) + 화면별 무작위 — 2.84
                             주제분 신선도 사다리(90일 먼저, 못 채우면 전체) — 2.86
  lib/crisis.py              위기 풀 — 먼저 확정, 미달 시 직전 결과 유지
  lib/report.py              videos.json · version.json · build_report.json 기록
  lib/results.py             위 모듈들이 주고받는 자료형 (순환 의존 방지)
                             SubcategoryResult = 화면 단위(주제분 + 폴백)
  lib/filters.py             videos.list 응답 필터 (이용불가·지역·길이·쇼츠·차단채널)
  lib/alerts.py              자동 경보 정의 (FYM 이식 + PYM 고유 5종)
  lib/krv_source.py          원문 로더 · 책 이름 매핑 · ref 파서 · 절 합본 방어
                             → BOOK_KEYS를 tagging.py가 장절 패턴에 재사용한다
  lib/verses_io.py           verses.yaml 라인 단위 in-place 편집 (주석 보존)
  lib/allowlist.py           channel_allowlist.yaml 로더 (PYM 필드 확장)
  lib/channel_blocklist.py   channel_blocklist.yaml 로더
  lib/reviewed_out.py        channel_reviewed_out.yaml 로더 (발굴 도구 전용)
  lib/quota.py               쿼터 회계 · 비용표 · 배치 예상량
  lib/quota_log.py           일일 누적 기록 (PT 기준, PYM 예약값 200)
  lib/youtube.py             API 클라이언트 + 드라이런 클라이언트
                             (playlistItems 페이지네이션 · 드라이런 제목/채널 귀속)
  lib/actions_status.py      Actions 배치 실행 여부 조회 (gh, 쿼터 0)
  lib/normalize.py           문자열 정규화 (한글 키워드 매칭용)

app/                         PWA (Phase 3) — gh-pages 루트에 배포된다
  src/App.jsx                화면 조립 (입력 → 로딩 1000ms → 결과/위기).
                             ⚠ 화면 **이동**은 여기 없다 — src/lib/flow.js가 정한다
  src/lib/flow.js            화면 흐름 상태 기계 (순수). 입력창을 비우는 유일한 자리.
                             ★ behavior.test.js가 이 그래프를 걸어 불변식을 검사한다
  src/components/            VerseCard(구절·길이별 크기) · MediaToggle(비활성화 금지)
                             VideoList(주제분/폴백 층 분리) · CrisisScreen(토글·폴백 없음)
                             About(출처 표기)
  src/lib/verses.js          감정 구절 / 위기 구절 격리 · 구절 회전
  src/lib/videos.js          토글·층 분리 · crisis 격리
  src/lib/payload.js         캐시 교체 관문 (옛 스키마 categories 거부)
  src/data/                  taxonomy.json · verses.json (생성물, 커밋 안 함)
                             seed-videos.json (최초 실행용 시드, 커밋함)
  test/                      188개 — 위기 경로는 데이터·소스 양쪽을 본다.
                             화면 이동은 flow.js 상태 기계를 실제로 순회한다

.github/workflows/
  build.yml                  데이터 배치 하루 2회 (09:30 · 21:30 UTC) → gh-pages data/
                             = KST 18:30 · 06:30. 근거는 2.68절
  deploy-app.yml             앱 배포 → gh-pages 루트 (keep_files: true 필수)

dist/                        배치 산출물 (커밋 대상 아님)
  videos.json                앱이 받는 파일 — **세분류별 목록**(source 표기) + crisis 최상위
  titles.json                필터 통과 전량의 제목 (--dump-titles). 태깅 분석 원자료
  version.json               앱이 "새 데이터가 있나"만 확인하는 경량 파일
  build_report.json          **사람이 읽는 리포트** — 채널별 기여·주제별 분포·경보
```

**쓰는 법**

```bash
python scripts/fill_verses.py             # 빈 text를 원문에서 채운다 (큐레이션 진입점)
python scripts/fill_verses.py --verify    # 추출 경로가 옳은지 검증
python scripts/verify_verses.py           # 대조 + 일치 건 verified 갱신
python scripts/verify_verses.py --check   # 검사만. CI 빌드 게이트용 (종료 코드)
python scripts/gen_verses_json.py         # 앱 번들 생성 (API 0)
python scripts/suggest_channels.py --query-set institution --dry-run
                                          # 채널 발굴 (실행 시 661 units)

python scripts/build_videos.py --dry-run  # 배치 전 과정 검증 (API 0)
python scripts/build_videos.py            # 실제 배치 (약 60 units)
python scripts/tagging_test.py            # 판별 규칙 (API 0)
python scripts/spread_test.py             # 선정 로직 (API 0)

python scripts/retag_titles.py --probe 신실   # 사전 후보 오탐 실측 (API 0)
python scripts/retag_titles.py --screens      # 화면 폴백 비율 재현 (API 0)
```

⚠ **사전(themes.yaml)을 고칠 때 `--dry-run`으로 확인하지 말 것.** 드라이런
제목은 사전에서 합성한 것이라 무엇을 넣어도 걸린다. 실측 덤프에 다시 거는
`retag_titles.py`가 그 자리의 도구다 (2.17절).

큐레이션 흐름은 `fill_verses.py` -> `verify_verses.py` 순이다.
앞이 본문을 만들고 뒤가 안전망으로 받는다.

배치 흐름은 `build_videos.py --dry-run` -> (승인) -> `build_videos.py`다.
드라이런과 실제 실행은 **산출물 이름이 갈린다**(`videos.dry-run.json`) —
드라이런이 실제 파일을 덮지 않는다.

워크플로에 넣을 때는 배포 전 단계에 `verify_verses.py --check`와 테스트 2종을 건다.
`verified: false`가 하나라도 있으면 배포되지 않는다.
